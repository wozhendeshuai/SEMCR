{"old_hunk": "@@ -68,7 +73,8 @@ def is_on(self):\n     def brightness(self):\n         \"\"\"Return the brightness of the light.\"\"\"\n         if self._device.is_dimmable and self._device.has_brightness:\n-            return self._device.brightness\n+            # Convert Abode brightness (0-100) to HASS brightness (0-255)\n+            return int(self._device.brightness) * 255 / 100", "hunk": "@@ -74,7 +74,8 @@ class AbodeLight(AbodeDevice, Light):\n         \"\"\"Return the brightness of the light.\"\"\"\n         if self._device.is_dimmable and self._device.has_brightness:\n             # Convert Abode brightness (0-100) to HASS brightness (0-255)\n-            return int(self._device.brightness) * 255 / 100\n+            # Abode will return 100 during initialization of Abode\n+            return ceil(int(self._device.brightness) * 255 / 100)\n \n     @property\n     def hs_color(self):\n", "comment": "Does the device return 100% ever here? I assume the upper limit should be 99 again.", "ids": [306614, "f16d267dcc561110d7e1b57caa7ea2c3f0b3c92f", "1d0a28a149784e600893f88b9d8c2d8507ba082e"], "repo": "home-assistant-core", "ghid": 13711, "old": "         \"\"\"Return the brightness of the light.\"\"\"\n         if self._device.is_dimmable and self._device.has_brightness:\n             # Convert Abode brightness (0-100) to HASS brightness (0-255)\n-            return int(self._device.brightness) * 255 / 100\n     @property\n     def hs_color(self):", "new": "         \"\"\"Return the brightness of the light.\"\"\"\n         if self._device.is_dimmable and self._device.has_brightness:\n             # Convert Abode brightness (0-100) to HASS brightness (0-255)\n+            # Abode will return 100 during initialization of Abode\n+            return ceil(int(self._device.brightness) * 255 / 100)\n     @property\n     def hs_color(self):", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -68,7 +73,8 @@ def is_on(self):\n     def brightness(self):\n         \"\"\"Return the brightness of the light.\"\"\"\n         if self._device.is_dimmable and self._device.has_brightness:\n-            return self._device.brightness\n+            # Convert Abode brightness (0-100) to HASS brightness (0-255)\n+            return int(self._device.brightness) * 255 / 100", "msg": "Does the device return 100% ever here? I assume the upper limit should be 99 again."}
{"old_hunk": "@@ -51,7 +51,12 @@ def turn_on(self, **kwargs):\n                 *kwargs[ATTR_HS_COLOR]))\n \n         if ATTR_BRIGHTNESS in kwargs and self._device.is_dimmable:\n-            self._device.set_level(kwargs[ATTR_BRIGHTNESS])\n+            # Convert HASS brightness (0-255) to Abode brightness (0-100)\n+            brightness = int(kwargs[ATTR_BRIGHTNESS] * 100 / 255)", "hunk": "@@ -52,11 +52,8 @@ class AbodeLight(AbodeDevice, Light):\n \n         if ATTR_BRIGHTNESS in kwargs and self._device.is_dimmable:\n             # Convert HASS brightness (0-255) to Abode brightness (0-100)\n-            brightness = int(kwargs[ATTR_BRIGHTNESS] * 100 / 255)\n             # If 100 is sent to Abode, 99 is returned causing an error\n-            if brightness == 100:\n-                brightness = 99\n-            self._device.set_level(brightness)\n+            self._device.set_level(ceil(kwargs[ATTR_BRIGHTNESS] * 99 / 255))\n         else:\n             self._device.switch_on()\n \n", "comment": "If 99 is the upper limit the range should be mapped to 0...99.", "ids": [306613, "f16d267dcc561110d7e1b57caa7ea2c3f0b3c92f", "1d0a28a149784e600893f88b9d8c2d8507ba082e"], "repo": "home-assistant-core", "ghid": 13711, "old": "         if ATTR_BRIGHTNESS in kwargs and self._device.is_dimmable:\n             # Convert HASS brightness (0-255) to Abode brightness (0-100)\n-            brightness = int(kwargs[ATTR_BRIGHTNESS] * 100 / 255)\n             # If 100 is sent to Abode, 99 is returned causing an error\n-            if brightness == 100:\n-                brightness = 99\n-            self._device.set_level(brightness)\n         else:\n             self._device.switch_on()", "new": "         if ATTR_BRIGHTNESS in kwargs and self._device.is_dimmable:\n             # Convert HASS brightness (0-255) to Abode brightness (0-100)\n             # If 100 is sent to Abode, 99 is returned causing an error\n+            self._device.set_level(ceil(kwargs[ATTR_BRIGHTNESS] * 99 / 255))\n         else:\n             self._device.switch_on()", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -51,7 +51,12 @@ def turn_on(self, **kwargs):\n                 *kwargs[ATTR_HS_COLOR]))\n \n         if ATTR_BRIGHTNESS in kwargs and self._device.is_dimmable:\n-            self._device.set_level(kwargs[ATTR_BRIGHTNESS])\n+            # Convert HASS brightness (0-255) to Abode brightness (0-100)\n+            brightness = int(kwargs[ATTR_BRIGHTNESS] * 100 / 255)", "msg": "If 99 is the upper limit the range should be mapped to 0...99."}
{"old_hunk": "@@ -84,7 +84,8 @@\n CONFIG_SCHEMA = vol.Schema({\n     vol.Required(DOMAIN): vol.Schema({\n         vol.Optional(CONF_IGNORE, default=[]):\n-            vol.All(cv.ensure_list, [vol.In(SERVICE_HANDLERS)])\n+            vol.All(cv.ensure_list, [\n+                vol.In({**CONFIG_ENTRY_HANDLERS, **SERVICE_HANDLERS})])", "hunk": "@@ -85,7 +85,7 @@ CONFIG_SCHEMA = vol.Schema({\n     vol.Required(DOMAIN): vol.Schema({\n         vol.Optional(CONF_IGNORE, default=[]):\n             vol.All(cv.ensure_list, [\n-                vol.In({**CONFIG_ENTRY_HANDLERS, **SERVICE_HANDLERS})])\n+                vol.In(list(CONFIG_ENTRY_HANDLERS) + list(SERVICE_HANDLERS))])\n     }),\n }, extra=vol.ALLOW_EXTRA)\n \n", "comment": "This is inefficient, as you're creating a dictionary just to iterate over the keys. More efficient would be `list(CONFIG_ENTRY_HANDLERS) + list(SERVICE_HANDLERS)`", "ids": [306725, "06a346e0b5382dfa31480be01728c6ab399a24c5", "3bb4ce0ae93ca6077fa9cb7c725c530558a60cea"], "repo": "home-assistant-core", "ghid": 13741, "old": "     vol.Required(DOMAIN): vol.Schema({\n         vol.Optional(CONF_IGNORE, default=[]):\n             vol.All(cv.ensure_list, [\n-                vol.In({**CONFIG_ENTRY_HANDLERS, **SERVICE_HANDLERS})])\n     }),\n }, extra=vol.ALLOW_EXTRA)", "new": "     vol.Required(DOMAIN): vol.Schema({\n         vol.Optional(CONF_IGNORE, default=[]):\n             vol.All(cv.ensure_list, [\n+                vol.In(list(CONFIG_ENTRY_HANDLERS) + list(SERVICE_HANDLERS))])\n     }),\n }, extra=vol.ALLOW_EXTRA)", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -84,7 +84,8 @@\n CONFIG_SCHEMA = vol.Schema({\n     vol.Required(DOMAIN): vol.Schema({\n         vol.Optional(CONF_IGNORE, default=[]):\n-            vol.All(cv.ensure_list, [vol.In(SERVICE_HANDLERS)])\n+            vol.All(cv.ensure_list, [\n+                vol.In({**CONFIG_ENTRY_HANDLERS, **SERVICE_HANDLERS})])", "msg": "This is inefficient, as you're creating a dictionary just to iterate over the keys. More efficient would be `list(CONFIG_ENTRY_HANDLERS) + list(SERVICE_HANDLERS)`"}
{"old_hunk": "@@ -34,7 +40,12 @@\n _LOGGER = logging.getLogger(__name__)\n \n CONFIG_SCHEMA = vol.Schema({\n-    DOMAIN: {}\n+    DOMAIN: vol.Schema({\n+        vol.Optional(CONF_FEEDBACK, default=False): cv.boolean,", "hunk": "@@ -41,7 +41,7 @@ _LOGGER = logging.getLogger(__name__)\n \n CONFIG_SCHEMA = vol.Schema({\n     DOMAIN: vol.Schema({\n-        vol.Optional(CONF_FEEDBACK, default=False): cv.boolean,\n+        vol.Optional(CONF_FEEDBACK): cv.boolean,\n         vol.Optional(CONF_PROBABILITY, default=0): vol.Coerce(float),\n         vol.Optional(CONF_SITE_IDS, default=['default']):\n             vol.All(cv.ensure_list, [cv.string]),\n", "comment": "Shouldn't this be configurable per site id?", "ids": [306825, "11b4dcc7c0a5e372650b5e99b4a3ffdfa4e754c0", "a9caf7e2b7d55aa66878e4b4b909a5da36512036"], "repo": "home-assistant-core", "ghid": 13746, "old": " CONFIG_SCHEMA = vol.Schema({\n     DOMAIN: vol.Schema({\n-        vol.Optional(CONF_FEEDBACK, default=False): cv.boolean,\n         vol.Optional(CONF_PROBABILITY, default=0): vol.Coerce(float),\n         vol.Optional(CONF_SITE_IDS, default=['default']):\n             vol.All(cv.ensure_list, [cv.string]),", "new": " CONFIG_SCHEMA = vol.Schema({\n     DOMAIN: vol.Schema({\n+        vol.Optional(CONF_FEEDBACK): cv.boolean,\n         vol.Optional(CONF_PROBABILITY, default=0): vol.Coerce(float),\n         vol.Optional(CONF_SITE_IDS, default=['default']):\n             vol.All(cv.ensure_list, [cv.string]),", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -34,7 +40,12 @@\n _LOGGER = logging.getLogger(__name__)\n \n CONFIG_SCHEMA = vol.Schema({\n-    DOMAIN: {}\n+    DOMAIN: vol.Schema({\n+        vol.Optional(CONF_FEEDBACK, default=False): cv.boolean,", "msg": "Shouldn't this be configurable per site id?"}
{"old_hunk": "@@ -104,10 +102,12 @@ class BroadlinkData(object):\n \n     def __init__(self, interval, ip_addr, mac_addr, timeout):\n         \"\"\"Initialize the data object.\"\"\"\n-        import broadlink\n         self.data = None\n-        self._device = broadlink.a1((ip_addr, 80), mac_addr, None)\n-        self._device.timeout = timeout\n+        self.interval = interval", "hunk": "@@ -103,7 +103,6 @@ class BroadlinkData(object):\n     def __init__(self, interval, ip_addr, mac_addr, timeout):\n         \"\"\"Initialize the data object.\"\"\"\n         self.data = None\n-        self.interval = interval\n         self.ip_addr = ip_addr\n         self.mac_addr = mac_addr\n         self.timeout = timeout\n", "comment": "self.interval was added but not used", "ids": [306885, "c3ffc798383142978e3f68f6657f61dc01354381", "1baa901021fc464a6e5285c4194d637a87526901"], "repo": "home-assistant-core", "ghid": 13761, "old": "     def __init__(self, interval, ip_addr, mac_addr, timeout):\n         \"\"\"Initialize the data object.\"\"\"\n         self.data = None\n-        self.interval = interval\n         self.ip_addr = ip_addr\n         self.mac_addr = mac_addr\n         self.timeout = timeout", "new": "     def __init__(self, interval, ip_addr, mac_addr, timeout):\n         \"\"\"Initialize the data object.\"\"\"\n         self.data = None\n         self.ip_addr = ip_addr\n         self.mac_addr = mac_addr\n         self.timeout = timeout", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -104,10 +102,12 @@ class BroadlinkData(object):\n \n     def __init__(self, interval, ip_addr, mac_addr, timeout):\n         \"\"\"Initialize the data object.\"\"\"\n-        import broadlink\n         self.data = None\n-        self._device = broadlink.a1((ip_addr, 80), mac_addr, None)\n-        self._device.timeout = timeout\n+        self.interval = interval", "msg": "self.interval was added but not used"}
{"old_hunk": "@@ -119,8 +118,16 @@ def __init__(self, interval, ip_addr, mac_addr, timeout):\n         if not self._auth():\n             _LOGGER.warning(\"Failed to connect to device\")\n \n+    def _connect(self):\n+        import broadlink\n+        self._device = broadlink.a1((self.ip_addr, 80), self.mac_addr, None)\n+        self._device.timeout = self.timeout\n+\n     def _update(self, retry=3):\n         try:\n+            self._connect()\n+            if not self._auth():", "hunk": "@@ -125,9 +125,6 @@ class BroadlinkData(object):\n \n     def _update(self, retry=3):\n         try:\n-            self._connect()\n-            if not self._auth():\n-                _LOGGER.warning(\"Failed to connect to device\")\n             data = self._device.check_sensors_raw()\n             if data is not None:\n                 self.data = self._schema(data)\n", "comment": "This will generate a authorization call for each update", "ids": [306889, "1baa901021fc464a6e5285c4194d637a87526901", "bbfca9f4bf09ce1edd83e6366ef43adcde86e23a"], "repo": "home-assistant-core", "ghid": 13761, "old": "     def _update(self, retry=3):\n         try:\n-            self._connect()\n-            if not self._auth():\n-                _LOGGER.warning(\"Failed to connect to device\")\n             data = self._device.check_sensors_raw()\n             if data is not None:\n                 self.data = self._schema(data)", "new": "     def _update(self, retry=3):\n         try:\n             data = self._device.check_sensors_raw()\n             if data is not None:\n                 self.data = self._schema(data)", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -119,8 +118,16 @@ def __init__(self, interval, ip_addr, mac_addr, timeout):\n         if not self._auth():\n             _LOGGER.warning(\"Failed to connect to device\")\n \n+    def _connect(self):\n+        import broadlink\n+        self._device = broadlink.a1((self.ip_addr, 80), self.mac_addr, None)\n+        self._device.timeout = self.timeout\n+\n     def _update(self, retry=3):\n         try:\n+            self._connect()\n+            if not self._auth():", "msg": "This will generate a authorization call for each update"}
{"old_hunk": "@@ -0,0 +1,73 @@\n+\"\"\"\n+Support for Tahoma heatpump derogation.\n+\n+For more details about this platform, please refer to the documentation at\n+https://home-assistant.io/components/climate.tahoma/\n+\"\"\"\n+\n+from homeassistant.components.climate import (\n+    ClimateDevice,\n+    SUPPORT_AWAY_MODE)\n+from homeassistant.components.tahoma import (\n+    DOMAIN as TAHOMA_DOMAIN, TahomaDevice)\n+from homeassistant.const import TEMP_CELSIUS\n+\n+\n+def setup_platform(hass, config, add_devices, discovery_info=None):\n+    \"\"\"Set up Tahoma controller devices.\"\"\"\n+    controller = hass.data[TAHOMA_DOMAIN]['controller']\n+    devices = []\n+    for device in hass.data[TAHOMA_DOMAIN]['devices']['climate']:\n+        devices.append(TahomaClimate(device, controller))\n+    add_devices(devices, True)\n+\n+\n+class TahomaClimate(TahomaDevice, ClimateDevice):\n+    \"\"\"Representation of a Tahoma heat pump derogration device.\"\"\"\n+\n+    def __init__(self, tahoma_device, controller):\n+        \"\"\"Initialize the climate device.\"\"\"\n+        super().__init__(tahoma_device, controller)\n+        if self.tahoma_device.type == \\\n+           'io:EnergyConsumptionSensorsHeatPumpComponent':\n+            self._name = 'Heat pump derogation ('+self.name+')'", "hunk": "@@ -30,7 +30,7 @@ class TahomaClimate(TahomaDevice, ClimateDevice):\n         super().__init__(tahoma_device, controller)\n         if self.tahoma_device.type == \\\n            'io:EnergyConsumptionSensorsHeatPumpComponent':\n-            self._name = 'Heat pump derogation ('+self.name+')'\n+            self._name = 'Heat pump derogation - {0}'.format(self.name)\n         self._support_flags = SUPPORT_AWAY_MODE\n         self._away = tahoma_device.active_states['core:DerogationOnOffState'] \\\n             == \"off\"\n", "comment": "Please use string formatting. I'm not sure if we are using bracket somewhere for the name.", "ids": [306903, "949c21ddf2f3ac8b6e8ccd880868b1ba3869e3f2", "4a3e68847cf4a1f29ea0a1111cd813975221dfff"], "repo": "home-assistant-core", "ghid": 13763, "old": "         super().__init__(tahoma_device, controller)\n         if self.tahoma_device.type == \\\n            'io:EnergyConsumptionSensorsHeatPumpComponent':\n-            self._name = 'Heat pump derogation ('+self.name+')'\n         self._support_flags = SUPPORT_AWAY_MODE\n         self._away = tahoma_device.active_states['core:DerogationOnOffState'] \\\n             == \"off\"", "new": "         super().__init__(tahoma_device, controller)\n         if self.tahoma_device.type == \\\n            'io:EnergyConsumptionSensorsHeatPumpComponent':\n+            self._name = 'Heat pump derogation - {0}'.format(self.name)\n         self._support_flags = SUPPORT_AWAY_MODE\n         self._away = tahoma_device.active_states['core:DerogationOnOffState'] \\\n             == \"off\"", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -0,0 +1,73 @@\n+\"\"\"\n+Support for Tahoma heatpump derogation.\n+\n+For more details about this platform, please refer to the documentation at\n+https://home-assistant.io/components/climate.tahoma/\n+\"\"\"\n+\n+from homeassistant.components.climate import (\n+    ClimateDevice,\n+    SUPPORT_AWAY_MODE)\n+from homeassistant.components.tahoma import (\n+    DOMAIN as TAHOMA_DOMAIN, TahomaDevice)\n+from homeassistant.const import TEMP_CELSIUS\n+\n+\n+def setup_platform(hass, config, add_devices, discovery_info=None):\n+    \"\"\"Set up Tahoma controller devices.\"\"\"\n+    controller = hass.data[TAHOMA_DOMAIN]['controller']\n+    devices = []\n+    for device in hass.data[TAHOMA_DOMAIN]['devices']['climate']:\n+        devices.append(TahomaClimate(device, controller))\n+    add_devices(devices, True)\n+\n+\n+class TahomaClimate(TahomaDevice, ClimateDevice):\n+    \"\"\"Representation of a Tahoma heat pump derogration device.\"\"\"\n+\n+    def __init__(self, tahoma_device, controller):\n+        \"\"\"Initialize the climate device.\"\"\"\n+        super().__init__(tahoma_device, controller)\n+        if self.tahoma_device.type == \\\n+           'io:EnergyConsumptionSensorsHeatPumpComponent':\n+            self._name = 'Heat pump derogation ('+self.name+')'", "msg": "Please use string formatting. I'm not sure if we are using bracket somewhere for the name."}
{"old_hunk": "@@ -0,0 +1,116 @@\n+\"\"\"\n+Support for USCIS Case Status.\n+\n+For more details about this platform, please refer to the documentation at\n+https://home-assistant.io/components/sensor.uscis/\n+\"\"\"\n+\n+import logging\n+from datetime import timedelta\n+import voluptuous as vol\n+\n+from homeassistant.helpers.entity import Entity\n+from homeassistant.util import Throttle\n+from homeassistant.components.sensor import PLATFORM_SCHEMA\n+from homeassistant.helpers import config_validation as cv\n+from homeassistant.const import CONF_FRIENDLY_NAME\n+\n+\n+_LOGGER = logging.getLogger(__name__)\n+\n+REQUIREMENTS = ['lxml==3.5.0']\n+\n+DEFAULT_NAME = \"USCIS\"\n+\n+PLATFORM_SCHEMA = PLATFORM_SCHEMA.extend({\n+    vol.Optional(CONF_FRIENDLY_NAME, default=DEFAULT_NAME): cv.string,\n+    vol.Required('case_id'): cv.string,\n+})\n+\n+\n+def setup_platform(hass, config, add_devices, discovery_info=None):\n+    \"\"\"Setting the platform in HASS and Case Information.\"\"\"\n+    uscis = UscisSensor(config['case_id'], config[CONF_FRIENDLY_NAME])\n+    uscis.update()\n+    if uscis.valid_case_id:\n+        add_devices([uscis])\n+    else:\n+        _LOGGER.error(\"Setup USCIS Sensor Fail\"\n+                      \" check if your Case ID is Valid\")\n+    return\n+\n+\n+class UscisSensor(Entity):\n+    \"\"\"USCIS Sensor will check case status on daily basis.\"\"\"\n+\n+    HOURS_TO_UPDATE = timedelta(hours=24)\n+\n+    CURRENT_STATUS = \"current_status\"\n+    LAST_CASE_UPDATE = \"last_update_date\"\n+    CASE_DATE_PATTERN = r\"[(A-Za-z)]*\\s[\\d]*,\\s[\\d]*\"\n+    URL = \"https://egov.uscis.gov/casestatus/mycasestatus.do\"\n+    APP_RECEIPT_NUM = \"appReceiptNum\"\n+    INIT_CASE_SEARCH = \"initCaseSearch\"\n+    CASE_STATUS = \"CHECK STATUS\"\n+    UPDATE_TEXT_XPATH = \"/html/body/div[2]/form/div/div[1]\" \\", "hunk": "@@ -18,7 +18,7 @@ from homeassistant.const import CONF_FRIENDLY_NAME\n \n _LOGGER = logging.getLogger(__name__)\n \n-REQUIREMENTS = ['lxml==3.5.0']\n+REQUIREMENTS = ['uscisstatus==0.1']\n \n DEFAULT_NAME = \"USCIS\"\n \n", "comment": "This is too much protocol specific information and should be extracted into an external package.", "ids": [306919, "b4d376cf772f9a1da6745a3e4351d54f30f5916a", "1ef3fb25ac283a23699779c20bebb7b313b57e7a"], "repo": "home-assistant-core", "ghid": 13764, "old": " _LOGGER = logging.getLogger(__name__)\n-REQUIREMENTS = ['lxml==3.5.0']\n DEFAULT_NAME = \"USCIS\"", "new": " _LOGGER = logging.getLogger(__name__)\n+REQUIREMENTS = ['uscisstatus==0.1']\n DEFAULT_NAME = \"USCIS\"", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -0,0 +1,116 @@\n+\"\"\"\n+Support for USCIS Case Status.\n+\n+For more details about this platform, please refer to the documentation at\n+https://home-assistant.io/components/sensor.uscis/\n+\"\"\"\n+\n+import logging\n+from datetime import timedelta\n+import voluptuous as vol\n+\n+from homeassistant.helpers.entity import Entity\n+from homeassistant.util import Throttle\n+from homeassistant.components.sensor import PLATFORM_SCHEMA\n+from homeassistant.helpers import config_validation as cv\n+from homeassistant.const import CONF_FRIENDLY_NAME\n+\n+\n+_LOGGER = logging.getLogger(__name__)\n+\n+REQUIREMENTS = ['lxml==3.5.0']\n+\n+DEFAULT_NAME = \"USCIS\"\n+\n+PLATFORM_SCHEMA = PLATFORM_SCHEMA.extend({\n+    vol.Optional(CONF_FRIENDLY_NAME, default=DEFAULT_NAME): cv.string,\n+    vol.Required('case_id'): cv.string,\n+})\n+\n+\n+def setup_platform(hass, config, add_devices, discovery_info=None):\n+    \"\"\"Setting the platform in HASS and Case Information.\"\"\"\n+    uscis = UscisSensor(config['case_id'], config[CONF_FRIENDLY_NAME])\n+    uscis.update()\n+    if uscis.valid_case_id:\n+        add_devices([uscis])\n+    else:\n+        _LOGGER.error(\"Setup USCIS Sensor Fail\"\n+                      \" check if your Case ID is Valid\")\n+    return\n+\n+\n+class UscisSensor(Entity):\n+    \"\"\"USCIS Sensor will check case status on daily basis.\"\"\"\n+\n+    HOURS_TO_UPDATE = timedelta(hours=24)\n+\n+    CURRENT_STATUS = \"current_status\"\n+    LAST_CASE_UPDATE = \"last_update_date\"\n+    CASE_DATE_PATTERN = r\"[(A-Za-z)]*\\s[\\d]*,\\s[\\d]*\"\n+    URL = \"https://egov.uscis.gov/casestatus/mycasestatus.do\"\n+    APP_RECEIPT_NUM = \"appReceiptNum\"\n+    INIT_CASE_SEARCH = \"initCaseSearch\"\n+    CASE_STATUS = \"CHECK STATUS\"\n+    UPDATE_TEXT_XPATH = \"/html/body/div[2]/form/div/div[1]\" \\", "msg": "This is too much protocol specific information and should be extracted into an external package."}
{"old_hunk": "@@ -9,7 +9,7 @@\n import voluptuous as vol\n \n from homeassistant.components.cover import (\n-    SUPPORT_CLOSE, SUPPORT_OPEN, SUPPORT_SET_POSITION)\n+    SUPPORT_CLOSE, SUPPORT_OPEN, SUPPORT_SET_POSITION, SUPPORT_STOP)", "hunk": "@@ -9,7 +9,7 @@ from zlib import adler32\n import voluptuous as vol\n \n from homeassistant.components.cover import (\n-    SUPPORT_CLOSE, SUPPORT_OPEN, SUPPORT_SET_POSITION, SUPPORT_STOP)\n+    SUPPORT_CLOSE, SUPPORT_OPEN, SUPPORT_SET_POSITION)\n from homeassistant.const import (\n     ATTR_SUPPORTED_FEATURES, ATTR_UNIT_OF_MEASUREMENT,\n     ATTR_DEVICE_CLASS, CONF_PORT, TEMP_CELSIUS, TEMP_FAHRENHEIT,\n", "comment": "`SUPPORT_STOP` not necessary here", "ids": [307142, "e58bd5c19124853e1006d9b28d521715f5833d38", "f2b049a80d6b1088aff3e0b51ac77f7e2570882a"], "repo": "home-assistant-core", "ghid": 13819, "old": " import voluptuous as vol\n from homeassistant.components.cover import (\n-    SUPPORT_CLOSE, SUPPORT_OPEN, SUPPORT_SET_POSITION, SUPPORT_STOP)\n from homeassistant.const import (\n     ATTR_SUPPORTED_FEATURES, ATTR_UNIT_OF_MEASUREMENT,\n     ATTR_DEVICE_CLASS, CONF_PORT, TEMP_CELSIUS, TEMP_FAHRENHEIT,", "new": " import voluptuous as vol\n from homeassistant.components.cover import (\n+    SUPPORT_CLOSE, SUPPORT_OPEN, SUPPORT_SET_POSITION)\n from homeassistant.const import (\n     ATTR_SUPPORTED_FEATURES, ATTR_UNIT_OF_MEASUREMENT,\n     ATTR_DEVICE_CLASS, CONF_PORT, TEMP_CELSIUS, TEMP_FAHRENHEIT,", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -9,7 +9,7 @@\n import voluptuous as vol\n \n from homeassistant.components.cover import (\n-    SUPPORT_CLOSE, SUPPORT_OPEN, SUPPORT_SET_POSITION)\n+    SUPPORT_CLOSE, SUPPORT_OPEN, SUPPORT_SET_POSITION, SUPPORT_STOP)", "msg": "`SUPPORT_STOP` not necessary here"}
{"old_hunk": "@@ -99,6 +99,8 @@ def get_accessory(hass, state, aid, config):\n         if device_class == 'garage' and \\\n                 features & (SUPPORT_OPEN | SUPPORT_CLOSE):\n             a_type = 'GarageDoorOpener'\n+        elif features & (SUPPORT_OPEN | SUPPORT_CLOSE):\n+            a_type = 'WindowCoveringBasic'", "hunk": "@@ -99,10 +99,10 @@ def get_accessory(hass, state, aid, config):\n         if device_class == 'garage' and \\\n                 features & (SUPPORT_OPEN | SUPPORT_CLOSE):\n             a_type = 'GarageDoorOpener'\n-        elif features & (SUPPORT_OPEN | SUPPORT_CLOSE):\n-            a_type = 'WindowCoveringBasic'\n         elif features & SUPPORT_SET_POSITION:\n             a_type = 'WindowCovering'\n+        elif features & (SUPPORT_OPEN | SUPPORT_CLOSE):\n+            a_type = 'WindowCoveringBasic'\n \n     elif state.domain == 'light':\n         a_type = 'Light'\n", "comment": "Please put this case below the `support_set_position` check. Otherwise a cover that allows for open, close and position would be limited to the basic case.", "ids": [307143, "e58bd5c19124853e1006d9b28d521715f5833d38", "f2b049a80d6b1088aff3e0b51ac77f7e2570882a"], "repo": "home-assistant-core", "ghid": 13819, "old": "         if device_class == 'garage' and \\\n                 features & (SUPPORT_OPEN | SUPPORT_CLOSE):\n             a_type = 'GarageDoorOpener'\n-        elif features & (SUPPORT_OPEN | SUPPORT_CLOSE):\n-            a_type = 'WindowCoveringBasic'\n         elif features & SUPPORT_SET_POSITION:\n             a_type = 'WindowCovering'\n     elif state.domain == 'light':\n         a_type = 'Light'", "new": "         if device_class == 'garage' and \\\n                 features & (SUPPORT_OPEN | SUPPORT_CLOSE):\n             a_type = 'GarageDoorOpener'\n         elif features & SUPPORT_SET_POSITION:\n             a_type = 'WindowCovering'\n+        elif features & (SUPPORT_OPEN | SUPPORT_CLOSE):\n+            a_type = 'WindowCoveringBasic'\n     elif state.domain == 'light':\n         a_type = 'Light'", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -99,6 +99,8 @@ def get_accessory(hass, state, aid, config):\n         if device_class == 'garage' and \\\n                 features & (SUPPORT_OPEN | SUPPORT_CLOSE):\n             a_type = 'GarageDoorOpener'\n+        elif features & (SUPPORT_OPEN | SUPPORT_CLOSE):\n+            a_type = 'WindowCoveringBasic'", "msg": "Please put this case below the `support_set_position` check. Otherwise a cover that allows for open, close and position would be limited to the basic case."}
{"old_hunk": "@@ -38,6 +40,7 @@\n PLATFORM_SCHEMA = PLATFORM_SCHEMA.extend({\n     vol.Optional(CONF_HOST): cv.string,\n     vol.Optional(CONF_NAME, default=DEFAULT_NAME): cv.string,\n+    vol.Optional(CONF_MAX_VOLUME, default=SUPPORTED_MAX_VOLUME): int,", "hunk": "@@ -40,7 +40,8 @@ DEFAULT_SOURCES = {'tv': 'TV', 'bd': 'Bluray', 'game': 'Game', 'aux1': 'Aux1',\n PLATFORM_SCHEMA = PLATFORM_SCHEMA.extend({\n     vol.Optional(CONF_HOST): cv.string,\n     vol.Optional(CONF_NAME, default=DEFAULT_NAME): cv.string,\n-    vol.Optional(CONF_MAX_VOLUME, default=SUPPORTED_MAX_VOLUME): int,\n+    vol.Optional(CONF_MAX_VOLUME, default=SUPPORTED_MAX_VOLUME):\n+        vol.All(vol.Coerce(int), vol.Range(min=1, max=SUPPORTED_MAX_VOLUME)),\n     vol.Optional(CONF_SOURCES, default=DEFAULT_SOURCES):\n         {cv.string: cv.string},\n })\n", "comment": "Instead of validating this as int, validate it with a range: ``` vol.All(vol.Coerce(int), vol.Range(min=0, max=SUPPORTED_MAX_VOLUME)) ```", "ids": [307170, "c268d59a3fa4fddd46595de79ea3eae905104cfe", "5ac0e75cea5053d0fae95b0447948ac765b6fbb1"], "repo": "home-assistant-core", "ghid": 13822, "old": " PLATFORM_SCHEMA = PLATFORM_SCHEMA.extend({\n     vol.Optional(CONF_HOST): cv.string,\n     vol.Optional(CONF_NAME, default=DEFAULT_NAME): cv.string,\n-    vol.Optional(CONF_MAX_VOLUME, default=SUPPORTED_MAX_VOLUME): int,\n     vol.Optional(CONF_SOURCES, default=DEFAULT_SOURCES):\n         {cv.string: cv.string},\n })", "new": " PLATFORM_SCHEMA = PLATFORM_SCHEMA.extend({\n     vol.Optional(CONF_HOST): cv.string,\n     vol.Optional(CONF_NAME, default=DEFAULT_NAME): cv.string,\n+    vol.Optional(CONF_MAX_VOLUME, default=SUPPORTED_MAX_VOLUME):\n+        vol.All(vol.Coerce(int), vol.Range(min=1, max=SUPPORTED_MAX_VOLUME)),\n     vol.Optional(CONF_SOURCES, default=DEFAULT_SOURCES):\n         {cv.string: cv.string},\n })", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -38,6 +40,7 @@\n PLATFORM_SCHEMA = PLATFORM_SCHEMA.extend({\n     vol.Optional(CONF_HOST): cv.string,\n     vol.Optional(CONF_NAME, default=DEFAULT_NAME): cv.string,\n+    vol.Optional(CONF_MAX_VOLUME, default=SUPPORTED_MAX_VOLUME): int,", "msg": "Instead of validating this as int, validate it with a range: ``` vol.All(vol.Coerce(int), vol.Range(min=0, max=SUPPORTED_MAX_VOLUME)) ```"}
{"old_hunk": "@@ -51,11 +54,15 @@ def setup_platform(hass, config, add_devices, discovery_info=None):\n     host = config.get(CONF_HOST)\n     hosts = []\n \n+    max_volume = config.get(CONF_MAX_VOLUME)\n+    if max_volume > SUPPORTED_MAX_VOLUME:", "hunk": "@@ -54,15 +54,11 @@ def setup_platform(hass, config, add_devices, discovery_info=None):\n     host = config.get(CONF_HOST)\n     hosts = []\n \n-    max_volume = config.get(CONF_MAX_VOLUME)\n-    if max_volume > SUPPORTED_MAX_VOLUME:\n-        max_volume = SUPPORTED_MAX_VOLUME\n-\n     if CONF_HOST in config and host not in KNOWN_HOSTS:\n         try:\n             hosts.append(OnkyoDevice(\n                 eiscp.eISCP(host), config.get(CONF_SOURCES),\n-                name=config.get(CONF_NAME), max_volume=max_volume))\n+                name=config.get(CONF_NAME), max_volume=config.get(CONF_MAX_VOLUME)))\n             KNOWN_HOSTS.append(host)\n         except OSError:\n             _LOGGER.error(\"Unable to connect to receiver at %s\", host)\n", "comment": "This can be removed as it will be handled by the config.", "ids": [307171, "c268d59a3fa4fddd46595de79ea3eae905104cfe", "5ac0e75cea5053d0fae95b0447948ac765b6fbb1"], "repo": "home-assistant-core", "ghid": 13822, "old": "     host = config.get(CONF_HOST)\n     hosts = []\n-    max_volume = config.get(CONF_MAX_VOLUME)\n-    if max_volume > SUPPORTED_MAX_VOLUME:\n-        max_volume = SUPPORTED_MAX_VOLUME\n-\n     if CONF_HOST in config and host not in KNOWN_HOSTS:\n         try:\n             hosts.append(OnkyoDevice(\n                 eiscp.eISCP(host), config.get(CONF_SOURCES),\n-                name=config.get(CONF_NAME), max_volume=max_volume))\n             KNOWN_HOSTS.append(host)\n         except OSError:\n             _LOGGER.error(\"Unable to connect to receiver at %s\", host)", "new": "     host = config.get(CONF_HOST)\n     hosts = []\n     if CONF_HOST in config and host not in KNOWN_HOSTS:\n         try:\n             hosts.append(OnkyoDevice(\n                 eiscp.eISCP(host), config.get(CONF_SOURCES),\n+                name=config.get(CONF_NAME), max_volume=config.get(CONF_MAX_VOLUME)))\n             KNOWN_HOSTS.append(host)\n         except OSError:\n             _LOGGER.error(\"Unable to connect to receiver at %s\", host)", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -51,11 +54,15 @@ def setup_platform(hass, config, add_devices, discovery_info=None):\n     host = config.get(CONF_HOST)\n     hosts = []\n \n+    max_volume = config.get(CONF_MAX_VOLUME)\n+    if max_volume > SUPPORTED_MAX_VOLUME:", "msg": "This can be removed as it will be handled by the config."}
{"old_hunk": "@@ -137,3 +118,41 @@ def unique_id(self):\n         \"\"\"Return a unique ID.\"\"\"\n         home = self._tibber_home.info['viewer']['home']\n         return home['meteringPointData']['consumptionEan']\n+\n+    @Throttle(MIN_TIME_BETWEEN_UPDATES)\n+    async def _fetch_data(self):\n+        try:\n+            await self._tibber_home.update_info()\n+            await  self._tibber_home.update_price_info()\n+        except (asyncio.TimeoutError, aiohttp.ClientError):\n+            return\n+        data = self._tibber_home.info['viewer']['home']\n+        self._device_state_attributes['app_nickname'] = data['appNickname']\n+        self._device_state_attributes['grid_company'] = \\\n+            data['meteringPointData']['gridCompany']\n+        self._device_state_attributes['estimated_annual_consumption'] = \\\n+            data['meteringPointData']['estimatedAnnualConsumption']\n+\n+    def _update_current_price(self):\n+        state = None\n+        max_price = None\n+        min_price = None\n+        for key, price_total in self._tibber_home.price_total.items():\n+            price_time = dt_util.as_utc(dt_util.parse_datetime(key))\n+            price_total = round(price_total, 3)\n+            time_diff = (now - price_time).total_seconds()/60", "hunk": "@@ -137,6 +137,7 @@ class TibberSensor(Entity):\n         state = None\n         max_price = None\n         min_price = None\n+        now = dt_util.utcnow()\n         for key, price_total in self._tibber_home.price_total.items():\n             price_time = dt_util.as_utc(dt_util.parse_datetime(key))\n             price_total = round(price_total, 3)\n", "comment": "undefined name 'now'", "ids": [307220, "69c8366ce5404a4193a28a955e5efb3309f014b6", "febc2c6610f7b26827acc5abd5d1e75574f57eaa"], "repo": "home-assistant-core", "ghid": 13865, "old": "         state = None\n         max_price = None\n         min_price = None\n         for key, price_total in self._tibber_home.price_total.items():\n             price_time = dt_util.as_utc(dt_util.parse_datetime(key))\n             price_total = round(price_total, 3)", "new": "         state = None\n         max_price = None\n         min_price = None\n+        now = dt_util.utcnow()\n         for key, price_total in self._tibber_home.price_total.items():\n             price_time = dt_util.as_utc(dt_util.parse_datetime(key))\n             price_total = round(price_total, 3)", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -137,3 +118,41 @@ def unique_id(self):\n         \"\"\"Return a unique ID.\"\"\"\n         home = self._tibber_home.info['viewer']['home']\n         return home['meteringPointData']['consumptionEan']\n+\n+    @Throttle(MIN_TIME_BETWEEN_UPDATES)\n+    async def _fetch_data(self):\n+        try:\n+            await self._tibber_home.update_info()\n+            await  self._tibber_home.update_price_info()\n+        except (asyncio.TimeoutError, aiohttp.ClientError):\n+            return\n+        data = self._tibber_home.info['viewer']['home']\n+        self._device_state_attributes['app_nickname'] = data['appNickname']\n+        self._device_state_attributes['grid_company'] = \\\n+            data['meteringPointData']['gridCompany']\n+        self._device_state_attributes['estimated_annual_consumption'] = \\\n+            data['meteringPointData']['estimatedAnnualConsumption']\n+\n+    def _update_current_price(self):\n+        state = None\n+        max_price = None\n+        min_price = None\n+        for key, price_total in self._tibber_home.price_total.items():\n+            price_time = dt_util.as_utc(dt_util.parse_datetime(key))\n+            price_total = round(price_total, 3)\n+            time_diff = (now - price_time).total_seconds()/60", "msg": "undefined name 'now'"}
{"old_hunk": "@@ -490,6 +481,45 @@ def async_get_tts_audio(self, message, language, options=None):\n             ft.partial(self.get_tts_audio, message, language, options=options))\n \n \n+class TextToSpeechUrlView(HomeAssistantView):\n+    \"\"\"TTS view to get a url to a generated speech file.\"\"\"\n+\n+    requires_auth = False", "hunk": "@@ -484,7 +484,7 @@ class Provider(object):\n class TextToSpeechUrlView(HomeAssistantView):\n     \"\"\"TTS view to get a url to a generated speech file.\"\"\"\n \n-    requires_auth = False\n+    requires_auth = True\n     url = '/api/tts_get_url'\n     name = 'api:tts:geturl'\n \n", "comment": "If we expose the `cache` attribute in the post body, an attacker could use this HTTP view to create quite a bit of load on the server without auth, I think. With a single request with a long text message the server would go and download the TTS audio file which would be much larger in size than the input. Of course requiring auth would make this a bit harder to use too...", "ids": [307278, "d7b08098244ea6cef65729c653cc32851d59504b", "b84f22322ddfd06ffad8af37ab8d38d8c02dd564"], "repo": "home-assistant-core", "ghid": 13882, "old": " class TextToSpeechUrlView(HomeAssistantView):\n     \"\"\"TTS view to get a url to a generated speech file.\"\"\"\n-    requires_auth = False\n     url = '/api/tts_get_url'\n     name = 'api:tts:geturl'", "new": " class TextToSpeechUrlView(HomeAssistantView):\n     \"\"\"TTS view to get a url to a generated speech file.\"\"\"\n+    requires_auth = True\n     url = '/api/tts_get_url'\n     name = 'api:tts:geturl'", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -490,6 +481,45 @@ def async_get_tts_audio(self, message, language, options=None):\n             ft.partial(self.get_tts_audio, message, language, options=options))\n \n \n+class TextToSpeechUrlView(HomeAssistantView):\n+    \"\"\"TTS view to get a url to a generated speech file.\"\"\"\n+\n+    requires_auth = False", "msg": "If we expose the `cache` attribute in the post body, an attacker could use this HTTP view to create quite a bit of load on the server without auth, I think. With a single request with a long text message the server would go and download the TTS audio file which would be much larger in size than the input. Of course requiring auth would make this a bit harder to use too..."}
{"old_hunk": "@@ -81,12 +81,20 @@ def callback_notify(notify):\n     if not config[CONF_OPTIMISTIC]:\n         from pymediaroom import install_mediaroom_protocol\n \n-        already_installed = hass.data.get(DISCOVERY_MEDIAROOM, False)\n+        already_installed = hass.data.get(DISCOVERY_MEDIAROOM, None)\n         if not already_installed:\n-            await install_mediaroom_protocol(\n+            hass.data[DISCOVERY_MEDIAROOM] = await install_mediaroom_protocol(\n                 responses_callback=callback_notify)\n+\n+            async def stop_discovery(event):", "hunk": "@@ -86,7 +86,8 @@ async def async_setup_platform(hass, config, async_add_devices,\n             hass.data[DISCOVERY_MEDIAROOM] = await install_mediaroom_protocol(\n                 responses_callback=callback_notify)\n \n-            async def stop_discovery(event):\n+            @callback\n+            def stop_discovery(event):\n                 \"\"\"Stop discovery of new mediaroom STB's.\"\"\"\n                 _LOGGER.debug(\"Stopping internal pymediaroom discovery.\")\n                 hass.data[DISCOVERY_MEDIAROOM].close()\n", "comment": "Should this not be just a `@callback` imported from homeassistant.core?", "ids": [307338, "47e8868bcee54f60b7522f6552bd08aa0d97be34", "2c827bd0405043869a246f71b8b7ae9a9bc5b252"], "repo": "home-assistant-core", "ghid": 13918, "old": "             hass.data[DISCOVERY_MEDIAROOM] = await install_mediaroom_protocol(\n                 responses_callback=callback_notify)\n-            async def stop_discovery(event):\n                 \"\"\"Stop discovery of new mediaroom STB's.\"\"\"\n                 _LOGGER.debug(\"Stopping internal pymediaroom discovery.\")\n                 hass.data[DISCOVERY_MEDIAROOM].close()", "new": "             hass.data[DISCOVERY_MEDIAROOM] = await install_mediaroom_protocol(\n                 responses_callback=callback_notify)\n+            @callback\n+            def stop_discovery(event):\n                 \"\"\"Stop discovery of new mediaroom STB's.\"\"\"\n                 _LOGGER.debug(\"Stopping internal pymediaroom discovery.\")\n                 hass.data[DISCOVERY_MEDIAROOM].close()", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -81,12 +81,20 @@ def callback_notify(notify):\n     if not config[CONF_OPTIMISTIC]:\n         from pymediaroom import install_mediaroom_protocol\n \n-        already_installed = hass.data.get(DISCOVERY_MEDIAROOM, False)\n+        already_installed = hass.data.get(DISCOVERY_MEDIAROOM, None)\n         if not already_installed:\n-            await install_mediaroom_protocol(\n+            hass.data[DISCOVERY_MEDIAROOM] = await install_mediaroom_protocol(\n                 responses_callback=callback_notify)\n+\n+            async def stop_discovery(event):", "msg": "Should this not be just a `@callback` imported from homeassistant.core?"}
{"old_hunk": "@@ -216,40 +223,51 @@ def async_from_config_file(config_path: str,\n \n \n @core.callback\n-def async_enable_logging(hass: core.HomeAssistant, verbose: bool = False,\n-                         log_rotate_days=None, log_file=None) -> None:\n+def async_enable_logging(hass: core.HomeAssistant,\n+                         verbose: bool = False,\n+                         log_rotate_days=None,\n+                         log_file=None,\n+                         log_no_color: bool = True) -> None:", "hunk": "@@ -227,7 +227,7 @@ def async_enable_logging(hass: core.HomeAssistant,\n                          verbose: bool = False,\n                          log_rotate_days=None,\n                          log_file=None,\n-                         log_no_color: bool = True) -> None:\n+                         log_no_color: bool = False) -> None:\n     \"\"\"Set up the logging.\n \n     This method must be run in the event loop.\n", "comment": "This should not default to True", "ids": [307373, "55855cdbf97e62e2d390063d515d24abf22c08eb", "6792aab25596a3af359fcd96632ef40ee97c8dac"], "repo": "home-assistant-core", "ghid": 13929, "old": "                          verbose: bool = False,\n                          log_rotate_days=None,\n                          log_file=None,\n-                         log_no_color: bool = True) -> None:\n     \"\"\"Set up the logging.\n     This method must be run in the event loop.", "new": "                          verbose: bool = False,\n                          log_rotate_days=None,\n                          log_file=None,\n+                         log_no_color: bool = False) -> None:\n     \"\"\"Set up the logging.\n     This method must be run in the event loop.", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -216,40 +223,51 @@ def async_from_config_file(config_path: str,\n \n \n @core.callback\n-def async_enable_logging(hass: core.HomeAssistant, verbose: bool = False,\n-                         log_rotate_days=None, log_file=None) -> None:\n+def async_enable_logging(hass: core.HomeAssistant,\n+                         verbose: bool = False,\n+                         log_rotate_days=None,\n+                         log_file=None,\n+                         log_no_color: bool = True) -> None:", "msg": "This should not default to True"}
{"old_hunk": "@@ -43,11 +43,6 @@ def setup_platform(hass, config, add_devices, discovery_info=None):\n     port = config.get(CONF_PORT)\n \n     deluge_api = DelugeRPCClient(host, port, username, password)\n-    try:\n-        deluge_api.connect()\n-    except ConnectionRefusedError:\n-        _LOGGING.error(\"Connection to Deluge Daemon failed\")\n-        return", "hunk": "@@ -43,6 +43,11 @@ def setup_platform(hass, config, add_devices, discovery_info=None):\n     port = config.get(CONF_PORT)\n \n     deluge_api = DelugeRPCClient(host, port, username, password)\n+    try:\n+        deluge_api.connect()\n+    except ConnectionRefusedError:\n+        _LOGGER.error(\"Connection to Deluge Daemon failed\")\n+        raise PlatformNotReady\n \n     add_devices([DelugeSwitch(deluge_api, name)])\n \n", "comment": "You could revert this and `raise PlatformNotReady` here. That would try to set up the platform again later.", "ids": [307384, "e0555c9cd49cd3df3dd7f8875528d4c7a752e9eb", "d293b8b86e4b2c21ceec4e8093a24109d7c75c98"], "repo": "home-assistant-core", "ghid": 13932, "old": "     port = config.get(CONF_PORT)\n     deluge_api = DelugeRPCClient(host, port, username, password)\n     add_devices([DelugeSwitch(deluge_api, name)])", "new": "     port = config.get(CONF_PORT)\n     deluge_api = DelugeRPCClient(host, port, username, password)\n+    try:\n+        deluge_api.connect()\n+    except ConnectionRefusedError:\n+        _LOGGER.error(\"Connection to Deluge Daemon failed\")\n+        raise PlatformNotReady\n     add_devices([DelugeSwitch(deluge_api, name)])", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -43,11 +43,6 @@ def setup_platform(hass, config, add_devices, discovery_info=None):\n     port = config.get(CONF_PORT)\n \n     deluge_api = DelugeRPCClient(host, port, username, password)\n-    try:\n-        deluge_api.connect()\n-    except ConnectionRefusedError:\n-        _LOGGING.error(\"Connection to Deluge Daemon failed\")\n-        return", "msg": "You could revert this and `raise PlatformNotReady` here. That would try to set up the platform again later."}
{"old_hunk": "@@ -46,7 +47,7 @@\n ATTR_DATA = 'data'\n CONF_STATE = 'state'\n \n-OFF_STATES = [STATE_IDLE, STATE_OFF]\n+OFF_STATES = [STATE_IDLE, STATE_OFF, STATE_PAUSED, STATE_UNAVAILABLE]", "hunk": "@@ -47,7 +47,6 @@ CONF_SERVICE_DATA = 'service_data'\n ATTR_DATA = 'data'\n CONF_STATE = 'state'\n \n-OFF_STATES = [STATE_IDLE, STATE_OFF, STATE_PAUSED, STATE_UNAVAILABLE]\n REQUIREMENTS = []\n _LOGGER = logging.getLogger(__name__)\n \n", "comment": "This doesn't seem to be used anymore.", "ids": [307417, "f4505da3e8e949b2652e11d3b8988b6a8bb57ef6", "046c5ef551cd87a4e748493697b43a8cef60c286"], "repo": "home-assistant-core", "ghid": 13936, "old": " ATTR_DATA = 'data'\n CONF_STATE = 'state'\n-OFF_STATES = [STATE_IDLE, STATE_OFF, STATE_PAUSED, STATE_UNAVAILABLE]\n REQUIREMENTS = []\n _LOGGER = logging.getLogger(__name__)", "new": " ATTR_DATA = 'data'\n CONF_STATE = 'state'\n REQUIREMENTS = []\n _LOGGER = logging.getLogger(__name__)", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -46,7 +47,7 @@\n ATTR_DATA = 'data'\n CONF_STATE = 'state'\n \n-OFF_STATES = [STATE_IDLE, STATE_OFF]\n+OFF_STATES = [STATE_IDLE, STATE_OFF, STATE_PAUSED, STATE_UNAVAILABLE]", "msg": "This doesn't seem to be used anymore."}
{"old_hunk": "@@ -0,0 +1,142 @@\n+\"\"\"\n+Support for mijnafvalwijzer trash pickup monitoring.\n+\n+For more details about this platform, please refer to the documentation at\n+https://home-assistant.io/components/sensor.mijnafvalwijzer/\n+\"\"\"\n+import homeassistant.helpers.config_validation as cv\n+from homeassistant.helpers.entity import Entity\n+from homeassistant.components.sensor import PLATFORM_SCHEMA\n+from homeassistant.const import (CONF_NAME)\n+\n+import voluptuous as vol\n+from datetime import datetime, timedelta\n+\n+import requests\n+import asyncio\n+import logging\n+\n+_LOGGER = logging.getLogger(__name__)\n+\n+DEFAULT_NAME = 'MijnAfvalwijzer Sensor'\n+ICON = 'mdi:delete-empty'\n+SENSOR_PREFIX = 'trash_'\n+CONST_POSTCODE = \"postcode\"\n+CONST_HUISNUMMER = \"huisnummer\"\n+CONST_TOEVOEGING = \"toevoeging\"\n+\n+PLATFORM_SCHEMA = PLATFORM_SCHEMA.extend({\n+    vol.Optional(CONF_NAME, default=DEFAULT_NAME): cv.string,\n+    vol.Required(CONST_POSTCODE): cv.string,\n+    vol.Required(CONST_HUISNUMMER): cv.string,\n+    vol.Optional(CONST_TOEVOEGING, default=\"\"): cv.string,\n+})\n+\n+\n+@asyncio.coroutine\n+def async_setup_platform(hass, config, async_add_devices, discovery_info=None):\n+    postcode = config.get(CONST_POSTCODE)\n+    huisnummer = config.get(CONST_HUISNUMMER)\n+    toevoeging = config.get(CONST_TOEVOEGING)\n+    url = (\"http://json.mijnafvalwijzer.nl/?method=postcodecheck&postcode={0}&street=&huisnummer={1}&toevoeging={2}&platform=phone&langs=nl&\").format(postcode, huisnummer, toevoeging)", "hunk": "@@ -8,12 +8,16 @@ import homeassistant.helpers.config_validation as cv\n from homeassistant.helpers.entity import Entity\n from homeassistant.components.sensor import PLATFORM_SCHEMA\n from homeassistant.const import (CONF_NAME)\n+from homeassistant.util import Throttle\n \n import voluptuous as vol\n-from datetime import datetime, timedelta\n+from datetime import timedelta\n \n import requests\n import asyncio\n+import json\n+import argparse\n+import datetime\n import logging\n \n _LOGGER = logging.getLogger(__name__)\n", "comment": "line too long (183 > 79 characters)", "ids": [307570, "6149794e6c00ae10057f495b889ffaced33b1e7e", "dc97a7c718d73fe54b23d1d5a35a7c5b3ca55b50"], "repo": "home-assistant-core", "ghid": 13972, "old": " from homeassistant.helpers.entity import Entity\n from homeassistant.components.sensor import PLATFORM_SCHEMA\n from homeassistant.const import (CONF_NAME)\n import voluptuous as vol\n-from datetime import datetime, timedelta\n import requests\n import asyncio\n import logging\n _LOGGER = logging.getLogger(__name__)", "new": " from homeassistant.helpers.entity import Entity\n from homeassistant.components.sensor import PLATFORM_SCHEMA\n from homeassistant.const import (CONF_NAME)\n+from homeassistant.util import Throttle\n import voluptuous as vol\n+from datetime import timedelta\n import requests\n import asyncio\n+import json\n+import argparse\n+import datetime\n import logging\n _LOGGER = logging.getLogger(__name__)", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -0,0 +1,142 @@\n+\"\"\"\n+Support for mijnafvalwijzer trash pickup monitoring.\n+\n+For more details about this platform, please refer to the documentation at\n+https://home-assistant.io/components/sensor.mijnafvalwijzer/\n+\"\"\"\n+import homeassistant.helpers.config_validation as cv\n+from homeassistant.helpers.entity import Entity\n+from homeassistant.components.sensor import PLATFORM_SCHEMA\n+from homeassistant.const import (CONF_NAME)\n+\n+import voluptuous as vol\n+from datetime import datetime, timedelta\n+\n+import requests\n+import asyncio\n+import logging\n+\n+_LOGGER = logging.getLogger(__name__)\n+\n+DEFAULT_NAME = 'MijnAfvalwijzer Sensor'\n+ICON = 'mdi:delete-empty'\n+SENSOR_PREFIX = 'trash_'\n+CONST_POSTCODE = \"postcode\"\n+CONST_HUISNUMMER = \"huisnummer\"\n+CONST_TOEVOEGING = \"toevoeging\"\n+\n+PLATFORM_SCHEMA = PLATFORM_SCHEMA.extend({\n+    vol.Optional(CONF_NAME, default=DEFAULT_NAME): cv.string,\n+    vol.Required(CONST_POSTCODE): cv.string,\n+    vol.Required(CONST_HUISNUMMER): cv.string,\n+    vol.Optional(CONST_TOEVOEGING, default=\"\"): cv.string,\n+})\n+\n+\n+@asyncio.coroutine\n+def async_setup_platform(hass, config, async_add_devices, discovery_info=None):\n+    postcode = config.get(CONST_POSTCODE)\n+    huisnummer = config.get(CONST_HUISNUMMER)\n+    toevoeging = config.get(CONST_TOEVOEGING)\n+    url = (\"http://json.mijnafvalwijzer.nl/?method=postcodecheck&postcode={0}&street=&huisnummer={1}&toevoeging={2}&platform=phone&langs=nl&\").format(postcode, huisnummer, toevoeging)", "msg": "line too long (183 > 79 characters)"}
{"old_hunk": "@@ -61,7 +80,23 @@ def camera_image(self):\n             _LOGGER.warning(\"Could not read camera %s image from file: %s\",\n                             self._name, self._file_path)\n \n+    def update_file_path(self, file_path):\n+        \"\"\"Update the local_file camera file path.\"\"\"\n+        if os.path.isfile(file_path):\n+            self._file_path = file_path", "hunk": "@@ -84,6 +84,7 @@ class LocalFile(Camera):\n         \"\"\"Update the local_file camera file path.\"\"\"\n         if os.path.isfile(file_path):\n             self._file_path = file_path\n+            self.schedule_update_ha_state()\n         else:\n             _LOGGER.warning(\"Invalid file_path: %s\", file_path)\n \n", "comment": "I think you need to call `self.schedule_update_ha_state()` (or something like that) here to push the state change to the state machine  ", "ids": [307602, "a441a2fdaaebd45c65fb34b5f39a7711c718209e", "f90d279a210bba9e7caa79248cc5f63cdcd6b497"], "repo": "home-assistant-core", "ghid": 13976, "old": "         \"\"\"Update the local_file camera file path.\"\"\"\n         if os.path.isfile(file_path):\n             self._file_path = file_path\n         else:\n             _LOGGER.warning(\"Invalid file_path: %s\", file_path)", "new": "         \"\"\"Update the local_file camera file path.\"\"\"\n         if os.path.isfile(file_path):\n             self._file_path = file_path\n+            self.schedule_update_ha_state()\n         else:\n             _LOGGER.warning(\"Invalid file_path: %s\", file_path)", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -61,7 +80,23 @@ def camera_image(self):\n             _LOGGER.warning(\"Could not read camera %s image from file: %s\",\n                             self._name, self._file_path)\n \n+    def update_file_path(self, file_path):\n+        \"\"\"Update the local_file camera file path.\"\"\"\n+        if os.path.isfile(file_path):\n+            self._file_path = file_path", "msg": "I think you need to call `self.schedule_update_ha_state()` (or something like that) here to push the state change to the state machine  "}
{"old_hunk": "@@ -4,14 +4,15 @@\n For more details about this component, please refer to the documentation at\n https://home-assistant.io/components/doorbird/\n \"\"\"\n-import asyncio\n import logging\n-\n+import asyncio", "hunk": "@@ -5,13 +5,14 @@ For more details about this component, please refer to the documentation at\n https://home-assistant.io/components/doorbird/\n \"\"\"\n import logging\n+\n import asyncio\n import voluptuous as vol\n-import homeassistant.helpers.config_validation as cv\n \n from homeassistant.components.http import HomeAssistantView\n from homeassistant.const import CONF_HOST, CONF_USERNAME, \\\n     CONF_PASSWORD, CONF_NAME, CONF_DEVICES, CONF_MONITORED_CONDITIONS\n+import homeassistant.helpers.config_validation as cv\n from homeassistant.util import slugify\n \n REQUIREMENTS = ['DoorBirdPy==0.1.3']\n", "comment": "Please sort imports   within groups standard library, 3rd party and homeassistant. There should be a blank line between the three groups.", "ids": [307705, "1b194109ff851e440cbae3ce2d148c0c087a95f2", "1ef8fff71ef9578b35165cd2da5caf59ec93ad2e"], "repo": "home-assistant-core", "ghid": 13994, "old": " https://home-assistant.io/components/doorbird/\n \"\"\"\n import logging\n import asyncio\n import voluptuous as vol\n-import homeassistant.helpers.config_validation as cv\n from homeassistant.components.http import HomeAssistantView\n from homeassistant.const import CONF_HOST, CONF_USERNAME, \\\n     CONF_PASSWORD, CONF_NAME, CONF_DEVICES, CONF_MONITORED_CONDITIONS\n from homeassistant.util import slugify\n REQUIREMENTS = ['DoorBirdPy==0.1.3']", "new": " https://home-assistant.io/components/doorbird/\n \"\"\"\n import logging\n+\n import asyncio\n import voluptuous as vol\n from homeassistant.components.http import HomeAssistantView\n from homeassistant.const import CONF_HOST, CONF_USERNAME, \\\n     CONF_PASSWORD, CONF_NAME, CONF_DEVICES, CONF_MONITORED_CONDITIONS\n+import homeassistant.helpers.config_validation as cv\n from homeassistant.util import slugify\n REQUIREMENTS = ['DoorBirdPy==0.1.3']", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -4,14 +4,15 @@\n For more details about this component, please refer to the documentation at\n https://home-assistant.io/components/doorbird/\n \"\"\"\n-import asyncio\n import logging\n-\n+import asyncio", "msg": "Please sort imports   within groups standard library, 3rd party and homeassistant. There should be a blank line between the three groups."}
{"old_hunk": "@@ -9,7 +9,8 @@\n \n import voluptuous as vol\n \n-from homeassistant.const import CONF_HOST, CONF_USERNAME, CONF_PASSWORD\n+from homeassistant.const import CONF_HOST, CONF_USERNAME, CONF_PASSWORD, \\", "hunk": "@@ -9,8 +9,8 @@ import logging\n \n import voluptuous as vol\n \n-from homeassistant.const import CONF_HOST, CONF_USERNAME, CONF_PASSWORD, \\\n-    CONF_DEVICES, CONF_NAME\n+from homeassistant.const import CONF_HOST, CONF_USERNAME, \\\n+    CONF_PASSWORD, CONF_NAME\n from homeassistant.components.http import HomeAssistantView\n import homeassistant.helpers.config_validation as cv\n \n", "comment": "'homeassistant.const.CONF_DEVICES' imported but unused", "ids": [307669, "2d9fb0aa35b60d10d00533ac3b761adb5fcb2c94", "332e6d16794f7adb466c66ad0e9900742ab3c039"], "repo": "home-assistant-core", "ghid": 13994, "old": " import voluptuous as vol\n-from homeassistant.const import CONF_HOST, CONF_USERNAME, CONF_PASSWORD, \\\n-    CONF_DEVICES, CONF_NAME\n from homeassistant.components.http import HomeAssistantView\n import homeassistant.helpers.config_validation as cv", "new": " import voluptuous as vol\n+from homeassistant.const import CONF_HOST, CONF_USERNAME, \\\n+    CONF_PASSWORD, CONF_NAME\n from homeassistant.components.http import HomeAssistantView\n import homeassistant.helpers.config_validation as cv", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -9,7 +9,8 @@\n \n import voluptuous as vol\n \n-from homeassistant.const import CONF_HOST, CONF_USERNAME, CONF_PASSWORD\n+from homeassistant.const import CONF_HOST, CONF_USERNAME, CONF_PASSWORD, \\", "msg": "'homeassistant.const.CONF_DEVICES' imported but unused"}
{"old_hunk": "@@ -48,31 +47,35 @@\n \n def setup_platform(hass, config, add_devices, discovery_info=None):\n     \"\"\"Set up the DoorBird switch platform.\"\"\"\n-    device = hass.data.get(DOORBIRD_DOMAIN)\n+    for doorstation in hass.data[DOORBIRD_DOMAIN]:\n+\n+        device = doorstation.device\n+\n+        switches = []", "hunk": "@@ -47,11 +47,12 @@ PLATFORM_SCHEMA = PLATFORM_SCHEMA.extend({\n \n def setup_platform(hass, config, add_devices, discovery_info=None):\n     \"\"\"Set up the DoorBird switch platform.\"\"\"\n+    switches = []\n+\n     for doorstation in hass.data[DOORBIRD_DOMAIN]:\n \n         device = doorstation.device\n \n-        switches = []\n         for switch in SWITCHES:\n \n             _LOGGER.debug(\"Adding DoorBird switch %s\",\n", "comment": "Move this out of the loop. It's accessed outside the loop.", "ids": [307707, "1b194109ff851e440cbae3ce2d148c0c087a95f2", "1ef8fff71ef9578b35165cd2da5caf59ec93ad2e"], "repo": "home-assistant-core", "ghid": 13994, "old": " def setup_platform(hass, config, add_devices, discovery_info=None):\n     \"\"\"Set up the DoorBird switch platform.\"\"\"\n     for doorstation in hass.data[DOORBIRD_DOMAIN]:\n         device = doorstation.device\n-        switches = []\n         for switch in SWITCHES:\n             _LOGGER.debug(\"Adding DoorBird switch %s\",", "new": " def setup_platform(hass, config, add_devices, discovery_info=None):\n     \"\"\"Set up the DoorBird switch platform.\"\"\"\n+    switches = []\n+\n     for doorstation in hass.data[DOORBIRD_DOMAIN]:\n         device = doorstation.device\n         for switch in SWITCHES:\n             _LOGGER.debug(\"Adding DoorBird switch %s\",", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -48,31 +47,35 @@\n \n def setup_platform(hass, config, add_devices, discovery_info=None):\n     \"\"\"Set up the DoorBird switch platform.\"\"\"\n-    device = hass.data.get(DOORBIRD_DOMAIN)\n+    for doorstation in hass.data[DOORBIRD_DOMAIN]:\n+\n+        device = doorstation.device\n+\n+        switches = []", "msg": "Move this out of the loop. It's accessed outside the loop."}
{"old_hunk": "@@ -0,0 +1,70 @@\n+\"\"\"\n+Support for Qwikswitch Binary Sensors.\n+\n+For more details about this platform, please refer to the documentation at\n+https://home-assistant.io/components/binary_sensor.qwikswitch/\n+\"\"\"\n+import logging\n+\n+from homeassistant.components.binary_sensor import BinarySensorDevice\n+from homeassistant.components.qwikswitch import QSEntity, DOMAIN as QWIKSWITCH\n+from homeassistant.core import callback\n+\n+DEPENDENCIES = [QWIKSWITCH]\n+\n+_LOGGER = logging.getLogger(__name__)\n+\n+\n+async def async_setup_platform(hass, _, add_devices, discovery_info=None):\n+    \"\"\"Add binary sensor from the main Qwikswitch component.\"\"\"\n+    if discovery_info is None:\n+        return\n+\n+    qsusb = hass.data[QWIKSWITCH]\n+    _LOGGER.debug(\"Setup qwikswitch.binary_sensor %s, %s\",\n+                  qsusb, discovery_info)\n+    devs = [QSBinarySensor(sensor) for sensor in discovery_info[QWIKSWITCH]]\n+    add_devices(devs)\n+\n+\n+class QSBinarySensor(QSEntity, BinarySensorDevice):\n+    \"\"\"Sensor based on a Qwikswitch relay/dimmer module.\"\"\"\n+\n+    _val = False\n+\n+    def __init__(self, sensor):\n+        \"\"\"Initialize the sensor.\"\"\"\n+        from pyqwikswitch import SENSORS\n+\n+        super().__init__(sensor['id'], sensor['name'])\n+        self.channel = sensor['channel']\n+        self.sensor_type = sensor['type']", "hunk": "@@ -38,9 +38,9 @@ class QSBinarySensor(QSEntity, BinarySensorDevice):\n \n         super().__init__(sensor['id'], sensor['name'])\n         self.channel = sensor['channel']\n-        self.sensor_type = sensor['type']\n+        sensor_type = sensor['type']\n \n-        self._decode, _ = SENSORS[self.sensor_type]\n+        self._decode, _ = SENSORS[sensor_type]\n         self._invert = not sensor.get('invert', False)\n         self._class = sensor.get('class', 'door')\n \n", "comment": "This looks like it's only used in this method. Maybe make it a local variable?", "ids": [307742, "4882b4186c67da0383e146542f859e488204c40f", "2ac0c5769d68b15d518e03bf411482089a93e830"], "repo": "home-assistant-core", "ghid": 14008, "old": "         super().__init__(sensor['id'], sensor['name'])\n         self.channel = sensor['channel']\n-        self.sensor_type = sensor['type']\n-        self._decode, _ = SENSORS[self.sensor_type]\n         self._invert = not sensor.get('invert', False)\n         self._class = sensor.get('class', 'door')", "new": "         super().__init__(sensor['id'], sensor['name'])\n         self.channel = sensor['channel']\n+        sensor_type = sensor['type']\n+        self._decode, _ = SENSORS[sensor_type]\n         self._invert = not sensor.get('invert', False)\n         self._class = sensor.get('class', 'door')", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -0,0 +1,70 @@\n+\"\"\"\n+Support for Qwikswitch Binary Sensors.\n+\n+For more details about this platform, please refer to the documentation at\n+https://home-assistant.io/components/binary_sensor.qwikswitch/\n+\"\"\"\n+import logging\n+\n+from homeassistant.components.binary_sensor import BinarySensorDevice\n+from homeassistant.components.qwikswitch import QSEntity, DOMAIN as QWIKSWITCH\n+from homeassistant.core import callback\n+\n+DEPENDENCIES = [QWIKSWITCH]\n+\n+_LOGGER = logging.getLogger(__name__)\n+\n+\n+async def async_setup_platform(hass, _, add_devices, discovery_info=None):\n+    \"\"\"Add binary sensor from the main Qwikswitch component.\"\"\"\n+    if discovery_info is None:\n+        return\n+\n+    qsusb = hass.data[QWIKSWITCH]\n+    _LOGGER.debug(\"Setup qwikswitch.binary_sensor %s, %s\",\n+                  qsusb, discovery_info)\n+    devs = [QSBinarySensor(sensor) for sensor in discovery_info[QWIKSWITCH]]\n+    add_devices(devs)\n+\n+\n+class QSBinarySensor(QSEntity, BinarySensorDevice):\n+    \"\"\"Sensor based on a Qwikswitch relay/dimmer module.\"\"\"\n+\n+    _val = False\n+\n+    def __init__(self, sensor):\n+        \"\"\"Initialize the sensor.\"\"\"\n+        from pyqwikswitch import SENSORS\n+\n+        super().__init__(sensor['id'], sensor['name'])\n+        self.channel = sensor['channel']\n+        self.sensor_type = sensor['type']", "msg": "This looks like it's only used in this method. Maybe make it a local variable?"}
{"old_hunk": "@@ -131,9 +131,12 @@ def entity_picture(self):\n     @property\n     def device_state_attributes(self):\n         \"\"\"Return the state attributes.\"\"\"\n-        return {\n-            ATTR_ATTRIBUTION: CONF_ATTRIBUTION,\n-        }\n+        attrs = {ATTR_ATTRIBUTION: CONF_ATTRIBUTION}\n+\n+        if self._code is None or \"weather\" in self._type:", "hunk": "@@ -133,7 +133,7 @@ class YahooWeatherSensor(Entity):\n         \"\"\"Return the state attributes.\"\"\"\n         attrs = {ATTR_ATTRIBUTION: CONF_ATTRIBUTION}\n \n-        if self._code is None or \"weather\" in self._type:\n+        if self._code is not None and \"weather\" in self._type:\n             attrs['condition_code'] = self._code\n \n         return attrs\n", "comment": "Should the attribute added always (`attrs['condition_code'] = None`) even if \"weather\" isn't in `self._type`?", "ids": [307753, "f897663b9b193a5da88708c1dd1d76af7dc88bb4", "49aa7deb3d9af10b314f9e846e09532f1cec5c6d"], "repo": "home-assistant-core", "ghid": 14011, "old": "         \"\"\"Return the state attributes.\"\"\"\n         attrs = {ATTR_ATTRIBUTION: CONF_ATTRIBUTION}\n-        if self._code is None or \"weather\" in self._type:\n             attrs['condition_code'] = self._code\n         return attrs", "new": "         \"\"\"Return the state attributes.\"\"\"\n         attrs = {ATTR_ATTRIBUTION: CONF_ATTRIBUTION}\n+        if self._code is not None and \"weather\" in self._type:\n             attrs['condition_code'] = self._code\n         return attrs", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -131,9 +131,12 @@ def entity_picture(self):\n     @property\n     def device_state_attributes(self):\n         \"\"\"Return the state attributes.\"\"\"\n-        return {\n-            ATTR_ATTRIBUTION: CONF_ATTRIBUTION,\n-        }\n+        attrs = {ATTR_ATTRIBUTION: CONF_ATTRIBUTION}\n+\n+        if self._code is None or \"weather\" in self._type:", "msg": "Should the attribute added always (`attrs['condition_code'] = None`) even if \"weather\" isn't in `self._type`?"}
{"old_hunk": "@@ -30,7 +30,10 @@ class SecuritySystem(HomeAccessory):\n     def __init__(self, *args, config):\n         \"\"\"Initialize a SecuritySystem accessory object.\"\"\"\n         super().__init__(*args, category=CATEGORY_ALARM_SYSTEM)\n-        self._alarm_code = config[ATTR_CODE]\n+        try:\n+            self._alarm_code = config[ATTR_CODE]", "hunk": "@@ -30,10 +30,7 @@ class SecuritySystem(HomeAccessory):\n     def __init__(self, *args, config):\n         \"\"\"Initialize a SecuritySystem accessory object.\"\"\"\n         super().__init__(*args, category=CATEGORY_ALARM_SYSTEM)\n-        try:\n-            self._alarm_code = config[ATTR_CODE]\n-        except KeyError:\n-            self._alarm_code = False\n+        self._alarm_code = config.get(ATTR_CODE)\n         self.flag_target_state = False\n \n         serv_alarm = add_preload_service(self, SERV_SECURITY_SYSTEM)\n", "comment": "You can shorten it a bit using get: `.get(ATTR_CODE, False)`", "ids": [307837, "18c6e179c93688227aa2e44ce29e6cf8debc3a39", "c99a8c0dc2c384712255d432c315154e1d5bfce8"], "repo": "home-assistant-core", "ghid": 14025, "old": "     def __init__(self, *args, config):\n         \"\"\"Initialize a SecuritySystem accessory object.\"\"\"\n         super().__init__(*args, category=CATEGORY_ALARM_SYSTEM)\n-        try:\n-            self._alarm_code = config[ATTR_CODE]\n-        except KeyError:\n-            self._alarm_code = False\n         self.flag_target_state = False\n         serv_alarm = add_preload_service(self, SERV_SECURITY_SYSTEM)", "new": "     def __init__(self, *args, config):\n         \"\"\"Initialize a SecuritySystem accessory object.\"\"\"\n         super().__init__(*args, category=CATEGORY_ALARM_SYSTEM)\n+        self._alarm_code = config.get(ATTR_CODE)\n         self.flag_target_state = False\n         serv_alarm = add_preload_service(self, SERV_SECURITY_SYSTEM)", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -30,7 +30,10 @@ class SecuritySystem(HomeAccessory):\n     def __init__(self, *args, config):\n         \"\"\"Initialize a SecuritySystem accessory object.\"\"\"\n         super().__init__(*args, category=CATEGORY_ALARM_SYSTEM)\n-        self._alarm_code = config[ATTR_CODE]\n+        try:\n+            self._alarm_code = config[ATTR_CODE]", "msg": "You can shorten it a bit using get: `.get(ATTR_CODE, False)`"}
{"old_hunk": "@@ -1,6 +1,10 @@\n \"\"\"\n Support for toggling Amcrest IP camera settings.\n \n+Latest change may change switch entity_id.\n+Please check your automation and update the identity-id if you have previously\n+assigned a name to your camera as it appends\n+the name of the camera to the switch id.", "hunk": "@@ -10,7 +10,7 @@\n     <h1>Error 503 Backend is unhealthy</h1>\n     <p>Backend is unhealthy</p>\n     <h3>Guru Mediation:</h3>\n-    <p>Details: cache-sea4427-SEA 1645536119 878469277</p>\n+    <p>Details: cache-sea4435-SEA 1645536119 574651609</p>\n     <hr>\n     <p>Varnish cache server</p>\n   </body>\n", "comment": "This information should be moved to a breaking PR section in the description of this GitHub PR", "ids": [307862, "ef24cffadb84a757d0d41db2d23586ea9670e0d9", "50c9149f40b3538628c53a8c00a6947647fe3632"], "repo": "home-assistant-core", "ghid": 14028, "old": "     <h1>Error 503 Backend is unhealthy</h1>\n     <p>Backend is unhealthy</p>\n     <h3>Guru Mediation:</h3>\n-    <p>Details: cache-sea4427-SEA 1645536119 878469277</p>\n     <hr>\n     <p>Varnish cache server</p>\n   </body>", "new": "     <h1>Error 503 Backend is unhealthy</h1>\n     <p>Backend is unhealthy</p>\n     <h3>Guru Mediation:</h3>\n+    <p>Details: cache-sea4435-SEA 1645536119 574651609</p>\n     <hr>\n     <p>Varnish cache server</p>\n   </body>", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -1,6 +1,10 @@\n \"\"\"\n Support for toggling Amcrest IP camera settings.\n \n+Latest change may change switch entity_id.\n+Please check your automation and update the identity-id if you have previously\n+assigned a name to your camera as it appends\n+the name of the camera to the switch id.", "msg": "This information should be moved to a breaking PR section in the description of this GitHub PR"}
{"old_hunk": "@@ -0,0 +1,173 @@\n+\"\"\"\n+Support for Hydrawise cloud.\n+\n+For more details about this platform, please refer to the documentation at\n+https://home-assistant.io/components/hydrawise\n+\"\"\"\n+import asyncio\n+from datetime import timedelta\n+import logging\n+\n+from requests.exceptions import ConnectTimeout, HTTPError\n+import voluptuous as vol\n+\n+from homeassistant.const import (\n+    ATTR_ATTRIBUTION, CONF_ACCESS_TOKEN, CONF_SCAN_INTERVAL)\n+import homeassistant.helpers.config_validation as cv\n+from homeassistant.helpers.dispatcher import (\n+    async_dispatcher_connect, dispatcher_send)\n+from homeassistant.helpers.entity import Entity\n+from homeassistant.helpers.event import track_time_interval\n+\n+REQUIREMENTS = ['hydrawiser==0.1.0']\n+\n+_LOGGER = logging.getLogger(__name__)\n+\n+ALLOWED_WATERING_TIME = [5, 10, 15, 30, 45, 60]\n+\n+CONF_ATTRIBUTION = \"Data provided by hydrawise.com\"\n+CONF_WATERING_TIME = 'watering_minutes'\n+\n+NOTIFICATION_ID = 'hydrawise_notification'\n+NOTIFICATION_TITLE = 'Hydrawise Setup'\n+\n+DATA_HYDRAWISE = 'hydrawise'\n+DOMAIN = 'hydrawise'\n+DEFAULT_WATERING_TIME = 15\n+\n+KEY_MAP = {\n+    'auto_watering': 'Automatic Watering',\n+    'is_watering': 'Watering',\n+    'manual_watering': 'Manual Watering',\n+    'next_cycle': 'Next Cycle',\n+    'status': 'Status',\n+    'watering_time': 'Watering Time',\n+    'rain_sensor': 'Rain Sensor',\n+}\n+\n+ICON_MAP = {\n+    'auto_watering': 'mdi:autorenew',\n+    'is_watering': '',\n+    'manual_watering': 'mdi:water-pump',\n+    'next_cycle': 'mdi:calendar-clock',\n+    'status': '',\n+    'watering_time': 'mdi:water-pump',\n+    'rain_sensor': ''\n+}\n+\n+UNIT_OF_MEASUREMENT_MAP = {\n+    'auto_watering': '',\n+    'is_watering': '',\n+    'manual_watering': '',\n+    'next_cycle': '',\n+    'status': '',\n+    'watering_time': 'min',\n+}\n+\n+BINARY_SENSORS = ['is_watering', 'status', 'rain_sensor']\n+\n+SENSORS = ['next_cycle', 'watering_time']\n+\n+SWITCHES = ['auto_watering', 'manual_watering']\n+\n+SCAN_INTERVAL = timedelta(seconds=20)\n+\n+SIGNAL_UPDATE_HYDRAWISE = \"hydrawise_update\"\n+\n+CONFIG_SCHEMA = vol.Schema({\n+    DOMAIN: vol.Schema({\n+        vol.Required(CONF_ACCESS_TOKEN): cv.string,\n+        vol.Optional(CONF_SCAN_INTERVAL, default=SCAN_INTERVAL):\n+            cv.time_period,\n+    }),\n+}, extra=vol.ALLOW_EXTRA)\n+\n+\n+def setup(hass, config):\n+    \"\"\"Set up the Hunter Hydrawise component.\"\"\"\n+    conf = config[DOMAIN]\n+    access_token = conf.get(CONF_ACCESS_TOKEN)\n+    scan_interval = conf.get(CONF_SCAN_INTERVAL)\n+\n+    try:\n+        from hydrawiser.core import Hydrawiser\n+\n+        hydrawise = Hydrawiser(user_token=access_token)\n+        hass.data[DATA_HYDRAWISE] = HydrawiseHub(hydrawise)\n+    except (ConnectTimeout, HTTPError) as ex:\n+        _LOGGER.error(\n+            \"Unable to connect to Hydrawise cloud service: %s\", str(ex))\n+        hass.components.persistent_notification.create(\n+            'Error: {}<br />'\n+            'You will need to restart hass after fixing.'\n+            ''.format(ex),\n+            title=NOTIFICATION_TITLE,\n+            notification_id=NOTIFICATION_ID)\n+        return False\n+\n+    def hub_refresh(event_time):\n+        \"\"\"Call Hydrawise hub to refresh information.\"\"\"\n+        _LOGGER.debug(\"Updating Hydrawise Hub component\")\n+        hass.data[DATA_HYDRAWISE].data.update_controller_info()\n+        dispatcher_send(hass, SIGNAL_UPDATE_HYDRAWISE)\n+\n+    # Call the Hydrawise API to refresh updates\n+    track_time_interval(hass, hub_refresh, scan_interval)\n+\n+    return True\n+\n+\n+class HydrawiseHub(object):\n+    \"\"\"Representation of a base Hydrawise device.\"\"\"\n+\n+    def __init__(self, data):\n+        \"\"\"Initialize the entity.\"\"\"\n+        self.data = data\n+\n+\n+class HydrawiseEntity(Entity):\n+    \"\"\"Entity class for Hydrawise devices.\"\"\"\n+\n+    def __init__(self, data, sensor_type):\n+        \"\"\"Initialize the Hydrawise entity.\"\"\"\n+        self.data = data\n+        self._sensor_type = sensor_type\n+        self._name = \"{0} {1}\".format(\n+            self.data.get('name'), KEY_MAP.get(self._sensor_type))\n+        self._state = None\n+\n+    @property\n+    def name(self):\n+        \"\"\"Return the name of the sensor.\"\"\"\n+        return self._name\n+\n+    @asyncio.coroutine\n+    def async_added_to_hass(self):\n+        \"\"\"Register callbacks.\"\"\"\n+        async_dispatcher_connect(\n+            self.hass, SIGNAL_UPDATE_HYDRAWISE, self._update_callback)\n+\n+    def _update_callback(self):\n+        \"\"\"Call update method.\"\"\"\n+        self.schedule_update_ha_state(True)\n+\n+    @property\n+    def unit_of_measurement(self):\n+        \"\"\"Return the units of measurement.\"\"\"\n+        return UNIT_OF_MEASUREMENT_MAP.get(self._sensor_type)\n+\n+    @property\n+    def device_state_attributes(self):\n+        \"\"\"Return the state attributes.\"\"\"\n+        temp = self.hass.data['hydrawise'].data.\\\n+            controller_info['controllers'][0]['last_contact_readable']\n+        return {\n+            ATTR_ATTRIBUTION: CONF_ATTRIBUTION,\n+            'identifier': self.data.get('relay'),\n+            'last contact:': temp", "hunk": "@@ -10,7 +10,7 @@\n     <h1>Error 503 Backend is unhealthy</h1>\n     <p>Backend is unhealthy</p>\n     <h3>Guru Mediation:</h3>\n-    <p>Details: cache-sea4428-SEA 1645536130 2929162851</p>\n+    <p>Details: cache-sea4434-SEA 1645536130 1981489614</p>\n     <hr>\n     <p>Varnish cache server</p>\n   </body>\n", "comment": "Looks like that this should be `'last_contact'`.", "ids": [307991, "6e0841bf31dab7d52983d485d616d11e1f52dc4a", "83296f34fde9ef5f3d69d498d365e960a8892d3e"], "repo": "home-assistant-core", "ghid": 14055, "old": "     <h1>Error 503 Backend is unhealthy</h1>\n     <p>Backend is unhealthy</p>\n     <h3>Guru Mediation:</h3>\n-    <p>Details: cache-sea4428-SEA 1645536130 2929162851</p>\n     <hr>\n     <p>Varnish cache server</p>\n   </body>", "new": "     <h1>Error 503 Backend is unhealthy</h1>\n     <p>Backend is unhealthy</p>\n     <h3>Guru Mediation:</h3>\n+    <p>Details: cache-sea4434-SEA 1645536130 1981489614</p>\n     <hr>\n     <p>Varnish cache server</p>\n   </body>", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -0,0 +1,173 @@\n+\"\"\"\n+Support for Hydrawise cloud.\n+\n+For more details about this platform, please refer to the documentation at\n+https://home-assistant.io/components/hydrawise\n+\"\"\"\n+import asyncio\n+from datetime import timedelta\n+import logging\n+\n+from requests.exceptions import ConnectTimeout, HTTPError\n+import voluptuous as vol\n+\n+from homeassistant.const import (\n+    ATTR_ATTRIBUTION, CONF_ACCESS_TOKEN, CONF_SCAN_INTERVAL)\n+import homeassistant.helpers.config_validation as cv\n+from homeassistant.helpers.dispatcher import (\n+    async_dispatcher_connect, dispatcher_send)\n+from homeassistant.helpers.entity import Entity\n+from homeassistant.helpers.event import track_time_interval\n+\n+REQUIREMENTS = ['hydrawiser==0.1.0']\n+\n+_LOGGER = logging.getLogger(__name__)\n+\n+ALLOWED_WATERING_TIME = [5, 10, 15, 30, 45, 60]\n+\n+CONF_ATTRIBUTION = \"Data provided by hydrawise.com\"\n+CONF_WATERING_TIME = 'watering_minutes'\n+\n+NOTIFICATION_ID = 'hydrawise_notification'\n+NOTIFICATION_TITLE = 'Hydrawise Setup'\n+\n+DATA_HYDRAWISE = 'hydrawise'\n+DOMAIN = 'hydrawise'\n+DEFAULT_WATERING_TIME = 15\n+\n+KEY_MAP = {\n+    'auto_watering': 'Automatic Watering',\n+    'is_watering': 'Watering',\n+    'manual_watering': 'Manual Watering',\n+    'next_cycle': 'Next Cycle',\n+    'status': 'Status',\n+    'watering_time': 'Watering Time',\n+    'rain_sensor': 'Rain Sensor',\n+}\n+\n+ICON_MAP = {\n+    'auto_watering': 'mdi:autorenew',\n+    'is_watering': '',\n+    'manual_watering': 'mdi:water-pump',\n+    'next_cycle': 'mdi:calendar-clock',\n+    'status': '',\n+    'watering_time': 'mdi:water-pump',\n+    'rain_sensor': ''\n+}\n+\n+UNIT_OF_MEASUREMENT_MAP = {\n+    'auto_watering': '',\n+    'is_watering': '',\n+    'manual_watering': '',\n+    'next_cycle': '',\n+    'status': '',\n+    'watering_time': 'min',\n+}\n+\n+BINARY_SENSORS = ['is_watering', 'status', 'rain_sensor']\n+\n+SENSORS = ['next_cycle', 'watering_time']\n+\n+SWITCHES = ['auto_watering', 'manual_watering']\n+\n+SCAN_INTERVAL = timedelta(seconds=20)\n+\n+SIGNAL_UPDATE_HYDRAWISE = \"hydrawise_update\"\n+\n+CONFIG_SCHEMA = vol.Schema({\n+    DOMAIN: vol.Schema({\n+        vol.Required(CONF_ACCESS_TOKEN): cv.string,\n+        vol.Optional(CONF_SCAN_INTERVAL, default=SCAN_INTERVAL):\n+            cv.time_period,\n+    }),\n+}, extra=vol.ALLOW_EXTRA)\n+\n+\n+def setup(hass, config):\n+    \"\"\"Set up the Hunter Hydrawise component.\"\"\"\n+    conf = config[DOMAIN]\n+    access_token = conf.get(CONF_ACCESS_TOKEN)\n+    scan_interval = conf.get(CONF_SCAN_INTERVAL)\n+\n+    try:\n+        from hydrawiser.core import Hydrawiser\n+\n+        hydrawise = Hydrawiser(user_token=access_token)\n+        hass.data[DATA_HYDRAWISE] = HydrawiseHub(hydrawise)\n+    except (ConnectTimeout, HTTPError) as ex:\n+        _LOGGER.error(\n+            \"Unable to connect to Hydrawise cloud service: %s\", str(ex))\n+        hass.components.persistent_notification.create(\n+            'Error: {}<br />'\n+            'You will need to restart hass after fixing.'\n+            ''.format(ex),\n+            title=NOTIFICATION_TITLE,\n+            notification_id=NOTIFICATION_ID)\n+        return False\n+\n+    def hub_refresh(event_time):\n+        \"\"\"Call Hydrawise hub to refresh information.\"\"\"\n+        _LOGGER.debug(\"Updating Hydrawise Hub component\")\n+        hass.data[DATA_HYDRAWISE].data.update_controller_info()\n+        dispatcher_send(hass, SIGNAL_UPDATE_HYDRAWISE)\n+\n+    # Call the Hydrawise API to refresh updates\n+    track_time_interval(hass, hub_refresh, scan_interval)\n+\n+    return True\n+\n+\n+class HydrawiseHub(object):\n+    \"\"\"Representation of a base Hydrawise device.\"\"\"\n+\n+    def __init__(self, data):\n+        \"\"\"Initialize the entity.\"\"\"\n+        self.data = data\n+\n+\n+class HydrawiseEntity(Entity):\n+    \"\"\"Entity class for Hydrawise devices.\"\"\"\n+\n+    def __init__(self, data, sensor_type):\n+        \"\"\"Initialize the Hydrawise entity.\"\"\"\n+        self.data = data\n+        self._sensor_type = sensor_type\n+        self._name = \"{0} {1}\".format(\n+            self.data.get('name'), KEY_MAP.get(self._sensor_type))\n+        self._state = None\n+\n+    @property\n+    def name(self):\n+        \"\"\"Return the name of the sensor.\"\"\"\n+        return self._name\n+\n+    @asyncio.coroutine\n+    def async_added_to_hass(self):\n+        \"\"\"Register callbacks.\"\"\"\n+        async_dispatcher_connect(\n+            self.hass, SIGNAL_UPDATE_HYDRAWISE, self._update_callback)\n+\n+    def _update_callback(self):\n+        \"\"\"Call update method.\"\"\"\n+        self.schedule_update_ha_state(True)\n+\n+    @property\n+    def unit_of_measurement(self):\n+        \"\"\"Return the units of measurement.\"\"\"\n+        return UNIT_OF_MEASUREMENT_MAP.get(self._sensor_type)\n+\n+    @property\n+    def device_state_attributes(self):\n+        \"\"\"Return the state attributes.\"\"\"\n+        temp = self.hass.data['hydrawise'].data.\\\n+            controller_info['controllers'][0]['last_contact_readable']\n+        return {\n+            ATTR_ATTRIBUTION: CONF_ATTRIBUTION,\n+            'identifier': self.data.get('relay'),\n+            'last contact:': temp", "msg": "Looks like that this should be `'last_contact'`."}
{"old_hunk": "@@ -0,0 +1,87 @@\n+\"\"\"\n+Support for Hydrawise sprinkler.\n+\n+For more details about this platform, please refer to the documentation at\n+https://home-assistant.io/components/binary_sensor.hydrawise/\n+\"\"\"\n+import logging\n+\n+import voluptuous as vol\n+\n+import homeassistant.helpers.config_validation as cv\n+from homeassistant.components.hydrawise import (\n+    BINARY_SENSORS, DATA_HYDRAWISE, HydrawiseEntity, ICON_MAP)\n+from homeassistant.components.binary_sensor import (\n+    BinarySensorDevice, PLATFORM_SCHEMA)\n+from homeassistant.const import CONF_MONITORED_CONDITIONS\n+\n+DEPENDENCIES = ['hydrawise']\n+\n+_LOGGER = logging.getLogger(__name__)\n+\n+PLATFORM_SCHEMA = PLATFORM_SCHEMA.extend({\n+    vol.Optional(CONF_MONITORED_CONDITIONS, default=list(BINARY_SENSORS)):", "hunk": "@@ -10,7 +10,7 @@\n     <h1>Error 503 Backend is unhealthy</h1>\n     <p>Backend is unhealthy</p>\n     <h3>Guru Mediation:</h3>\n-    <p>Details: cache-sea4421-SEA 1645536132 1463884786</p>\n+    <p>Details: cache-sea4431-SEA 1645536133 3774957026</p>\n     <hr>\n     <p>Varnish cache server</p>\n   </body>\n", "comment": "`BINARY_SENSORS` is a list: `BINARY_SENSORS = ['is_watering', 'status', 'rain_sensor']`", "ids": [307997, "6e0841bf31dab7d52983d485d616d11e1f52dc4a", "83296f34fde9ef5f3d69d498d365e960a8892d3e"], "repo": "home-assistant-core", "ghid": 14055, "old": "     <h1>Error 503 Backend is unhealthy</h1>\n     <p>Backend is unhealthy</p>\n     <h3>Guru Mediation:</h3>\n-    <p>Details: cache-sea4421-SEA 1645536132 1463884786</p>\n     <hr>\n     <p>Varnish cache server</p>\n   </body>", "new": "     <h1>Error 503 Backend is unhealthy</h1>\n     <p>Backend is unhealthy</p>\n     <h3>Guru Mediation:</h3>\n+    <p>Details: cache-sea4431-SEA 1645536133 3774957026</p>\n     <hr>\n     <p>Varnish cache server</p>\n   </body>", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -0,0 +1,87 @@\n+\"\"\"\n+Support for Hydrawise sprinkler.\n+\n+For more details about this platform, please refer to the documentation at\n+https://home-assistant.io/components/binary_sensor.hydrawise/\n+\"\"\"\n+import logging\n+\n+import voluptuous as vol\n+\n+import homeassistant.helpers.config_validation as cv\n+from homeassistant.components.hydrawise import (\n+    BINARY_SENSORS, DATA_HYDRAWISE, HydrawiseEntity, ICON_MAP)\n+from homeassistant.components.binary_sensor import (\n+    BinarySensorDevice, PLATFORM_SCHEMA)\n+from homeassistant.const import CONF_MONITORED_CONDITIONS\n+\n+DEPENDENCIES = ['hydrawise']\n+\n+_LOGGER = logging.getLogger(__name__)\n+\n+PLATFORM_SCHEMA = PLATFORM_SCHEMA.extend({\n+    vol.Optional(CONF_MONITORED_CONDITIONS, default=list(BINARY_SENSORS)):", "msg": "`BINARY_SENSORS` is a list: `BINARY_SENSORS = ['is_watering', 'status', 'rain_sensor']`"}
{"old_hunk": "@@ -0,0 +1,173 @@\n+\"\"\"\n+Support for Hydrawise cloud.\n+\n+For more details about this platform, please refer to the documentation at\n+https://home-assistant.io/components/hydrawise", "hunk": "@@ -10,7 +10,7 @@\n     <h1>Error 503 Backend is unhealthy</h1>\n     <p>Backend is unhealthy</p>\n     <h3>Guru Mediation:</h3>\n-    <p>Details: cache-sea4428-SEA 1645536127 2929140661</p>\n+    <p>Details: cache-sea4434-SEA 1645536127 1981470922</p>\n     <hr>\n     <p>Varnish cache server</p>\n   </body>\n", "comment": "`/` missing at he end of the link.", "ids": [307985, "6e0841bf31dab7d52983d485d616d11e1f52dc4a", "83296f34fde9ef5f3d69d498d365e960a8892d3e"], "repo": "home-assistant-core", "ghid": 14055, "old": "     <h1>Error 503 Backend is unhealthy</h1>\n     <p>Backend is unhealthy</p>\n     <h3>Guru Mediation:</h3>\n-    <p>Details: cache-sea4428-SEA 1645536127 2929140661</p>\n     <hr>\n     <p>Varnish cache server</p>\n   </body>", "new": "     <h1>Error 503 Backend is unhealthy</h1>\n     <p>Backend is unhealthy</p>\n     <h3>Guru Mediation:</h3>\n+    <p>Details: cache-sea4434-SEA 1645536127 1981470922</p>\n     <hr>\n     <p>Varnish cache server</p>\n   </body>", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -0,0 +1,173 @@\n+\"\"\"\n+Support for Hydrawise cloud.\n+\n+For more details about this platform, please refer to the documentation at\n+https://home-assistant.io/components/hydrawise", "msg": "`/` missing at he end of the link."}
{"old_hunk": "@@ -0,0 +1,112 @@\n+\"\"\"\n+Support for Hydrawise cloud.\n+\n+For more details about this platform, please refer to the documentation at\n+https://home-assistant.io/components/switch.hydrawise\n+\"\"\"\n+import logging\n+\n+import voluptuous as vol\n+\n+import homeassistant.helpers.config_validation as cv\n+from homeassistant.components.hydrawise import (\n+    ALLOWED_WATERING_TIME, CONF_ATTRIBUTION, CONF_WATERING_TIME,\n+    DATA_HYDRAWISE, DEFAULT_WATERING_TIME, HydrawiseEntity, SWITCHES)\n+from homeassistant.components.switch import PLATFORM_SCHEMA, SwitchDevice\n+from homeassistant.const import (\n+    ATTR_ATTRIBUTION, CONF_MONITORED_CONDITIONS)\n+\n+DEPENDENCIES = ['hydrawise']\n+\n+_LOGGER = logging.getLogger(__name__)\n+\n+PLATFORM_SCHEMA = PLATFORM_SCHEMA.extend({\n+    vol.Optional(CONF_MONITORED_CONDITIONS, default=list(SWITCHES)):", "hunk": "@@ -10,7 +10,7 @@\n     <h1>Error 503 Backend is unhealthy</h1>\n     <p>Backend is unhealthy</p>\n     <h3>Guru Mediation:</h3>\n-    <p>Details: cache-sea4460-SEA 1645536131 1936798455</p>\n+    <p>Details: cache-sea4430-SEA 1645536131 1460222522</p>\n     <hr>\n     <p>Varnish cache server</p>\n   </body>\n", "comment": "`SWITCHES` is defined as list.", "ids": [307994, "6e0841bf31dab7d52983d485d616d11e1f52dc4a", "83296f34fde9ef5f3d69d498d365e960a8892d3e"], "repo": "home-assistant-core", "ghid": 14055, "old": "     <h1>Error 503 Backend is unhealthy</h1>\n     <p>Backend is unhealthy</p>\n     <h3>Guru Mediation:</h3>\n-    <p>Details: cache-sea4460-SEA 1645536131 1936798455</p>\n     <hr>\n     <p>Varnish cache server</p>\n   </body>", "new": "     <h1>Error 503 Backend is unhealthy</h1>\n     <p>Backend is unhealthy</p>\n     <h3>Guru Mediation:</h3>\n+    <p>Details: cache-sea4430-SEA 1645536131 1460222522</p>\n     <hr>\n     <p>Varnish cache server</p>\n   </body>", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -0,0 +1,112 @@\n+\"\"\"\n+Support for Hydrawise cloud.\n+\n+For more details about this platform, please refer to the documentation at\n+https://home-assistant.io/components/switch.hydrawise\n+\"\"\"\n+import logging\n+\n+import voluptuous as vol\n+\n+import homeassistant.helpers.config_validation as cv\n+from homeassistant.components.hydrawise import (\n+    ALLOWED_WATERING_TIME, CONF_ATTRIBUTION, CONF_WATERING_TIME,\n+    DATA_HYDRAWISE, DEFAULT_WATERING_TIME, HydrawiseEntity, SWITCHES)\n+from homeassistant.components.switch import PLATFORM_SCHEMA, SwitchDevice\n+from homeassistant.const import (\n+    ATTR_ATTRIBUTION, CONF_MONITORED_CONDITIONS)\n+\n+DEPENDENCIES = ['hydrawise']\n+\n+_LOGGER = logging.getLogger(__name__)\n+\n+PLATFORM_SCHEMA = PLATFORM_SCHEMA.extend({\n+    vol.Optional(CONF_MONITORED_CONDITIONS, default=list(SWITCHES)):", "msg": "`SWITCHES` is defined as list."}
{"old_hunk": "@@ -0,0 +1,173 @@\n+\"\"\"\n+Support for Hydrawise cloud.\n+\n+For more details about this platform, please refer to the documentation at\n+https://home-assistant.io/components/hydrawise\n+\"\"\"\n+import asyncio\n+from datetime import timedelta\n+import logging\n+\n+from requests.exceptions import ConnectTimeout, HTTPError\n+import voluptuous as vol\n+\n+from homeassistant.const import (\n+    ATTR_ATTRIBUTION, CONF_ACCESS_TOKEN, CONF_SCAN_INTERVAL)\n+import homeassistant.helpers.config_validation as cv\n+from homeassistant.helpers.dispatcher import (\n+    async_dispatcher_connect, dispatcher_send)\n+from homeassistant.helpers.entity import Entity\n+from homeassistant.helpers.event import track_time_interval\n+\n+REQUIREMENTS = ['hydrawiser==0.1.0']\n+\n+_LOGGER = logging.getLogger(__name__)\n+\n+ALLOWED_WATERING_TIME = [5, 10, 15, 30, 45, 60]\n+\n+CONF_ATTRIBUTION = \"Data provided by hydrawise.com\"\n+CONF_WATERING_TIME = 'watering_minutes'\n+\n+NOTIFICATION_ID = 'hydrawise_notification'\n+NOTIFICATION_TITLE = 'Hydrawise Setup'\n+\n+DATA_HYDRAWISE = 'hydrawise'\n+DOMAIN = 'hydrawise'\n+DEFAULT_WATERING_TIME = 15\n+\n+KEY_MAP = {\n+    'auto_watering': 'Automatic Watering',\n+    'is_watering': 'Watering',\n+    'manual_watering': 'Manual Watering',\n+    'next_cycle': 'Next Cycle',\n+    'status': 'Status',\n+    'watering_time': 'Watering Time',\n+    'rain_sensor': 'Rain Sensor',\n+}\n+\n+ICON_MAP = {\n+    'auto_watering': 'mdi:autorenew',\n+    'is_watering': '',\n+    'manual_watering': 'mdi:water-pump',\n+    'next_cycle': 'mdi:calendar-clock',\n+    'status': '',\n+    'watering_time': 'mdi:water-pump',\n+    'rain_sensor': ''\n+}\n+\n+UNIT_OF_MEASUREMENT_MAP = {\n+    'auto_watering': '',\n+    'is_watering': '',\n+    'manual_watering': '',\n+    'next_cycle': '',\n+    'status': '',\n+    'watering_time': 'min',\n+}\n+\n+BINARY_SENSORS = ['is_watering', 'status', 'rain_sensor']\n+\n+SENSORS = ['next_cycle', 'watering_time']\n+\n+SWITCHES = ['auto_watering', 'manual_watering']\n+\n+SCAN_INTERVAL = timedelta(seconds=20)", "hunk": "@@ -10,7 +10,7 @@\n     <h1>Error 503 Backend is unhealthy</h1>\n     <p>Backend is unhealthy</p>\n     <h3>Guru Mediation:</h3>\n-    <p>Details: cache-sea4428-SEA 1645536132 2929179348</p>\n+    <p>Details: cache-sea4434-SEA 1645536132 1981503101</p>\n     <hr>\n     <p>Varnish cache server</p>\n   </body>\n", "comment": "Is there a benefit to go with 20 s instead of the default of 30 s?", "ids": [307996, "6e0841bf31dab7d52983d485d616d11e1f52dc4a", "83296f34fde9ef5f3d69d498d365e960a8892d3e"], "repo": "home-assistant-core", "ghid": 14055, "old": "     <h1>Error 503 Backend is unhealthy</h1>\n     <p>Backend is unhealthy</p>\n     <h3>Guru Mediation:</h3>\n-    <p>Details: cache-sea4428-SEA 1645536132 2929179348</p>\n     <hr>\n     <p>Varnish cache server</p>\n   </body>", "new": "     <h1>Error 503 Backend is unhealthy</h1>\n     <p>Backend is unhealthy</p>\n     <h3>Guru Mediation:</h3>\n+    <p>Details: cache-sea4434-SEA 1645536132 1981503101</p>\n     <hr>\n     <p>Varnish cache server</p>\n   </body>", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -0,0 +1,173 @@\n+\"\"\"\n+Support for Hydrawise cloud.\n+\n+For more details about this platform, please refer to the documentation at\n+https://home-assistant.io/components/hydrawise\n+\"\"\"\n+import asyncio\n+from datetime import timedelta\n+import logging\n+\n+from requests.exceptions import ConnectTimeout, HTTPError\n+import voluptuous as vol\n+\n+from homeassistant.const import (\n+    ATTR_ATTRIBUTION, CONF_ACCESS_TOKEN, CONF_SCAN_INTERVAL)\n+import homeassistant.helpers.config_validation as cv\n+from homeassistant.helpers.dispatcher import (\n+    async_dispatcher_connect, dispatcher_send)\n+from homeassistant.helpers.entity import Entity\n+from homeassistant.helpers.event import track_time_interval\n+\n+REQUIREMENTS = ['hydrawiser==0.1.0']\n+\n+_LOGGER = logging.getLogger(__name__)\n+\n+ALLOWED_WATERING_TIME = [5, 10, 15, 30, 45, 60]\n+\n+CONF_ATTRIBUTION = \"Data provided by hydrawise.com\"\n+CONF_WATERING_TIME = 'watering_minutes'\n+\n+NOTIFICATION_ID = 'hydrawise_notification'\n+NOTIFICATION_TITLE = 'Hydrawise Setup'\n+\n+DATA_HYDRAWISE = 'hydrawise'\n+DOMAIN = 'hydrawise'\n+DEFAULT_WATERING_TIME = 15\n+\n+KEY_MAP = {\n+    'auto_watering': 'Automatic Watering',\n+    'is_watering': 'Watering',\n+    'manual_watering': 'Manual Watering',\n+    'next_cycle': 'Next Cycle',\n+    'status': 'Status',\n+    'watering_time': 'Watering Time',\n+    'rain_sensor': 'Rain Sensor',\n+}\n+\n+ICON_MAP = {\n+    'auto_watering': 'mdi:autorenew',\n+    'is_watering': '',\n+    'manual_watering': 'mdi:water-pump',\n+    'next_cycle': 'mdi:calendar-clock',\n+    'status': '',\n+    'watering_time': 'mdi:water-pump',\n+    'rain_sensor': ''\n+}\n+\n+UNIT_OF_MEASUREMENT_MAP = {\n+    'auto_watering': '',\n+    'is_watering': '',\n+    'manual_watering': '',\n+    'next_cycle': '',\n+    'status': '',\n+    'watering_time': 'min',\n+}\n+\n+BINARY_SENSORS = ['is_watering', 'status', 'rain_sensor']\n+\n+SENSORS = ['next_cycle', 'watering_time']\n+\n+SWITCHES = ['auto_watering', 'manual_watering']\n+\n+SCAN_INTERVAL = timedelta(seconds=20)", "msg": "Is there a benefit to go with 20 s instead of the default of 30 s?"}
{"old_hunk": "@@ -0,0 +1,173 @@\n+\"\"\"\n+Support for Hydrawise cloud.\n+\n+For more details about this platform, please refer to the documentation at\n+https://home-assistant.io/components/hydrawise\n+\"\"\"\n+import asyncio\n+from datetime import timedelta\n+import logging\n+\n+from requests.exceptions import ConnectTimeout, HTTPError\n+import voluptuous as vol\n+\n+from homeassistant.const import (\n+    ATTR_ATTRIBUTION, CONF_ACCESS_TOKEN, CONF_SCAN_INTERVAL)\n+import homeassistant.helpers.config_validation as cv\n+from homeassistant.helpers.dispatcher import (\n+    async_dispatcher_connect, dispatcher_send)\n+from homeassistant.helpers.entity import Entity\n+from homeassistant.helpers.event import track_time_interval\n+\n+REQUIREMENTS = ['hydrawiser==0.1.0']\n+\n+_LOGGER = logging.getLogger(__name__)\n+\n+ALLOWED_WATERING_TIME = [5, 10, 15, 30, 45, 60]\n+\n+CONF_ATTRIBUTION = \"Data provided by hydrawise.com\"\n+CONF_WATERING_TIME = 'watering_minutes'\n+\n+NOTIFICATION_ID = 'hydrawise_notification'\n+NOTIFICATION_TITLE = 'Hydrawise Setup'\n+\n+DATA_HYDRAWISE = 'hydrawise'\n+DOMAIN = 'hydrawise'\n+DEFAULT_WATERING_TIME = 15\n+\n+KEY_MAP = {\n+    'auto_watering': 'Automatic Watering',\n+    'is_watering': 'Watering',\n+    'manual_watering': 'Manual Watering',\n+    'next_cycle': 'Next Cycle',\n+    'status': 'Status',\n+    'watering_time': 'Watering Time',\n+    'rain_sensor': 'Rain Sensor',\n+}\n+\n+ICON_MAP = {\n+    'auto_watering': 'mdi:autorenew',\n+    'is_watering': '',\n+    'manual_watering': 'mdi:water-pump',\n+    'next_cycle': 'mdi:calendar-clock',\n+    'status': '',\n+    'watering_time': 'mdi:water-pump',\n+    'rain_sensor': ''\n+}\n+\n+UNIT_OF_MEASUREMENT_MAP = {\n+    'auto_watering': '',\n+    'is_watering': '',\n+    'manual_watering': '',\n+    'next_cycle': '',\n+    'status': '',\n+    'watering_time': 'min',\n+}", "hunk": "@@ -10,7 +10,7 @@\n     <h1>Error 503 Backend is unhealthy</h1>\n     <p>Backend is unhealthy</p>\n     <h3>Guru Mediation:</h3>\n-    <p>Details: cache-sea4428-SEA 1645536128 2929144427</p>\n+    <p>Details: cache-sea4434-SEA 1645536128 1981474235</p>\n     <hr>\n     <p>Varnish cache server</p>\n   </body>\n", "comment": "I would suggest to merge those dicts to something like ``` 'auto_watering': ['Automatic Watering', 'mdi:autorenew', ''] .... ``` Easier to maintain in the future.", "ids": [307986, "6e0841bf31dab7d52983d485d616d11e1f52dc4a", "83296f34fde9ef5f3d69d498d365e960a8892d3e"], "repo": "home-assistant-core", "ghid": 14055, "old": "     <h1>Error 503 Backend is unhealthy</h1>\n     <p>Backend is unhealthy</p>\n     <h3>Guru Mediation:</h3>\n-    <p>Details: cache-sea4428-SEA 1645536128 2929144427</p>\n     <hr>\n     <p>Varnish cache server</p>\n   </body>", "new": "     <h1>Error 503 Backend is unhealthy</h1>\n     <p>Backend is unhealthy</p>\n     <h3>Guru Mediation:</h3>\n+    <p>Details: cache-sea4434-SEA 1645536128 1981474235</p>\n     <hr>\n     <p>Varnish cache server</p>\n   </body>", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -0,0 +1,173 @@\n+\"\"\"\n+Support for Hydrawise cloud.\n+\n+For more details about this platform, please refer to the documentation at\n+https://home-assistant.io/components/hydrawise\n+\"\"\"\n+import asyncio\n+from datetime import timedelta\n+import logging\n+\n+from requests.exceptions import ConnectTimeout, HTTPError\n+import voluptuous as vol\n+\n+from homeassistant.const import (\n+    ATTR_ATTRIBUTION, CONF_ACCESS_TOKEN, CONF_SCAN_INTERVAL)\n+import homeassistant.helpers.config_validation as cv\n+from homeassistant.helpers.dispatcher import (\n+    async_dispatcher_connect, dispatcher_send)\n+from homeassistant.helpers.entity import Entity\n+from homeassistant.helpers.event import track_time_interval\n+\n+REQUIREMENTS = ['hydrawiser==0.1.0']\n+\n+_LOGGER = logging.getLogger(__name__)\n+\n+ALLOWED_WATERING_TIME = [5, 10, 15, 30, 45, 60]\n+\n+CONF_ATTRIBUTION = \"Data provided by hydrawise.com\"\n+CONF_WATERING_TIME = 'watering_minutes'\n+\n+NOTIFICATION_ID = 'hydrawise_notification'\n+NOTIFICATION_TITLE = 'Hydrawise Setup'\n+\n+DATA_HYDRAWISE = 'hydrawise'\n+DOMAIN = 'hydrawise'\n+DEFAULT_WATERING_TIME = 15\n+\n+KEY_MAP = {\n+    'auto_watering': 'Automatic Watering',\n+    'is_watering': 'Watering',\n+    'manual_watering': 'Manual Watering',\n+    'next_cycle': 'Next Cycle',\n+    'status': 'Status',\n+    'watering_time': 'Watering Time',\n+    'rain_sensor': 'Rain Sensor',\n+}\n+\n+ICON_MAP = {\n+    'auto_watering': 'mdi:autorenew',\n+    'is_watering': '',\n+    'manual_watering': 'mdi:water-pump',\n+    'next_cycle': 'mdi:calendar-clock',\n+    'status': '',\n+    'watering_time': 'mdi:water-pump',\n+    'rain_sensor': ''\n+}\n+\n+UNIT_OF_MEASUREMENT_MAP = {\n+    'auto_watering': '',\n+    'is_watering': '',\n+    'manual_watering': '',\n+    'next_cycle': '',\n+    'status': '',\n+    'watering_time': 'min',\n+}", "msg": "I would suggest to merge those dicts to something like ``` 'auto_watering': ['Automatic Watering', 'mdi:autorenew', ''] .... ``` Easier to maintain in the future."}
{"old_hunk": "@@ -0,0 +1,87 @@\n+\"\"\"\n+Support for Hydrawise sprinkler.\n+\n+For more details about this platform, please refer to the documentation at\n+https://home-assistant.io/components/binary_sensor.hydrawise/\n+\"\"\"\n+import logging\n+\n+import voluptuous as vol\n+\n+import homeassistant.helpers.config_validation as cv\n+from homeassistant.components.hydrawise import (\n+    BINARY_SENSORS, DATA_HYDRAWISE, HydrawiseEntity, ICON_MAP)\n+from homeassistant.components.binary_sensor import (\n+    BinarySensorDevice, PLATFORM_SCHEMA)\n+from homeassistant.const import CONF_MONITORED_CONDITIONS\n+\n+DEPENDENCIES = ['hydrawise']\n+\n+_LOGGER = logging.getLogger(__name__)\n+\n+PLATFORM_SCHEMA = PLATFORM_SCHEMA.extend({\n+    vol.Optional(CONF_MONITORED_CONDITIONS, default=list(BINARY_SENSORS)):\n+        vol.All(cv.ensure_list, [vol.In(BINARY_SENSORS)]),\n+})\n+\n+\n+def setup_platform(hass, config, add_devices, discovery_info=None):\n+    \"\"\"Set up a sensor for a hydrawise device.\"\"\"\n+    hydrawise = hass.data[DATA_HYDRAWISE].data\n+\n+    sensors = []\n+    for sensor_type in config.get(CONF_MONITORED_CONDITIONS):\n+        if sensor_type in ['status', 'rain_sensor']:\n+            sensors.append(\n+                HydrawiseBinarySensor(\n+                    hydrawise.controller_status, sensor_type))\n+\n+        else:\n+            # create a sensor for each zone\n+            for zone in hydrawise.relays:\n+                zone_data = zone\n+                zone_data['running'] = \\\n+                    hydrawise.controller_status.get('running', False)\n+                sensors.append(HydrawiseBinarySensor(zone_data, sensor_type))\n+\n+    add_devices(sensors, True)\n+    return True\n+\n+\n+class HydrawiseBinarySensor(HydrawiseEntity, BinarySensorDevice):\n+    \"\"\"A sensor implementation for hydrawise device.\"\"\"\n+\n+    @property\n+    def is_on(self):\n+        \"\"\"Return true if the binary sensor is on.\"\"\"\n+        return self._state\n+\n+    def update(self):\n+        \"\"\"Get the latest data and updates the state.\"\"\"\n+        _LOGGER.debug(\"Updating Hydrawise binary sensor: %s\", self._name)\n+        mydata = self.hass.data['hydrawise'].data\n+        if self._sensor_type == 'status':\n+            self._state = mydata.status == 'All good!'", "hunk": "@@ -10,7 +10,7 @@\n     <h1>Error 503 Backend is unhealthy</h1>\n     <p>Backend is unhealthy</p>\n     <h3>Guru Mediation:</h3>\n-    <p>Details: cache-sea4421-SEA 1645536128 1463854793</p>\n+    <p>Details: cache-sea4431-SEA 1645536128 3774924209</p>\n     <hr>\n     <p>Varnish cache server</p>\n   </body>\n", "comment": "Should be `True` or `False`, it's a binary sensor.", "ids": [307987, "6e0841bf31dab7d52983d485d616d11e1f52dc4a", "83296f34fde9ef5f3d69d498d365e960a8892d3e"], "repo": "home-assistant-core", "ghid": 14055, "old": "     <h1>Error 503 Backend is unhealthy</h1>\n     <p>Backend is unhealthy</p>\n     <h3>Guru Mediation:</h3>\n-    <p>Details: cache-sea4421-SEA 1645536128 1463854793</p>\n     <hr>\n     <p>Varnish cache server</p>\n   </body>", "new": "     <h1>Error 503 Backend is unhealthy</h1>\n     <p>Backend is unhealthy</p>\n     <h3>Guru Mediation:</h3>\n+    <p>Details: cache-sea4431-SEA 1645536128 3774924209</p>\n     <hr>\n     <p>Varnish cache server</p>\n   </body>", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -0,0 +1,87 @@\n+\"\"\"\n+Support for Hydrawise sprinkler.\n+\n+For more details about this platform, please refer to the documentation at\n+https://home-assistant.io/components/binary_sensor.hydrawise/\n+\"\"\"\n+import logging\n+\n+import voluptuous as vol\n+\n+import homeassistant.helpers.config_validation as cv\n+from homeassistant.components.hydrawise import (\n+    BINARY_SENSORS, DATA_HYDRAWISE, HydrawiseEntity, ICON_MAP)\n+from homeassistant.components.binary_sensor import (\n+    BinarySensorDevice, PLATFORM_SCHEMA)\n+from homeassistant.const import CONF_MONITORED_CONDITIONS\n+\n+DEPENDENCIES = ['hydrawise']\n+\n+_LOGGER = logging.getLogger(__name__)\n+\n+PLATFORM_SCHEMA = PLATFORM_SCHEMA.extend({\n+    vol.Optional(CONF_MONITORED_CONDITIONS, default=list(BINARY_SENSORS)):\n+        vol.All(cv.ensure_list, [vol.In(BINARY_SENSORS)]),\n+})\n+\n+\n+def setup_platform(hass, config, add_devices, discovery_info=None):\n+    \"\"\"Set up a sensor for a hydrawise device.\"\"\"\n+    hydrawise = hass.data[DATA_HYDRAWISE].data\n+\n+    sensors = []\n+    for sensor_type in config.get(CONF_MONITORED_CONDITIONS):\n+        if sensor_type in ['status', 'rain_sensor']:\n+            sensors.append(\n+                HydrawiseBinarySensor(\n+                    hydrawise.controller_status, sensor_type))\n+\n+        else:\n+            # create a sensor for each zone\n+            for zone in hydrawise.relays:\n+                zone_data = zone\n+                zone_data['running'] = \\\n+                    hydrawise.controller_status.get('running', False)\n+                sensors.append(HydrawiseBinarySensor(zone_data, sensor_type))\n+\n+    add_devices(sensors, True)\n+    return True\n+\n+\n+class HydrawiseBinarySensor(HydrawiseEntity, BinarySensorDevice):\n+    \"\"\"A sensor implementation for hydrawise device.\"\"\"\n+\n+    @property\n+    def is_on(self):\n+        \"\"\"Return true if the binary sensor is on.\"\"\"\n+        return self._state\n+\n+    def update(self):\n+        \"\"\"Get the latest data and updates the state.\"\"\"\n+        _LOGGER.debug(\"Updating Hydrawise binary sensor: %s\", self._name)\n+        mydata = self.hass.data['hydrawise'].data\n+        if self._sensor_type == 'status':\n+            self._state = mydata.status == 'All good!'", "msg": "Should be `True` or `False`, it's a binary sensor."}
{"old_hunk": "@@ -0,0 +1,69 @@\n+\"\"\"Config flow to configure zone component.\"\"\"\n+\n+import voluptuous as vol\n+\n+import homeassistant.helpers.config_validation as cv\n+from homeassistant import config_entries, data_entry_flow\n+from homeassistant.const import (\n+    CONF_NAME, CONF_LATITUDE, CONF_LONGITUDE, CONF_ICON, CONF_RADIUS)\n+from homeassistant.core import callback\n+from homeassistant.util import slugify\n+\n+from .const import CONF_PASSIVE, DOMAIN\n+\n+\n+@callback\n+def configured_zones(hass):\n+    \"\"\"Return a set of the configured hosts.\"\"\"\n+    return set((slugify(entry.data[CONF_NAME])) for\n+               entry in hass.config_entries.async_entries(DOMAIN))\n+\n+\n+@config_entries.HANDLERS.register(DOMAIN)\n+class ZoneFlowHandler(data_entry_flow.FlowHandler):\n+    \"\"\"Zone config flow.\"\"\"\n+\n+    VERSION = 1\n+\n+    def __init__(self):\n+        \"\"\"Initialize zone configuration flow.\"\"\"\n+        pass\n+\n+    async def async_step_init(self, user_input=None):\n+        \"\"\"Handle a flow start.\"\"\"\n+        errors = {}\n+\n+        if user_input is not None:\n+            if slugify(user_input[CONF_NAME]) not in \\\n+               configured_zones(self.hass):\n+                return self.async_create_entry(\n+                    title='Zone ' + user_input[CONF_NAME],", "hunk": "@@ -10,7 +10,7 @@\n     <h1>Error 503 Backend is unhealthy</h1>\n     <p>Backend is unhealthy</p>\n     <h3>Guru Mediation:</h3>\n-    <p>Details: cache-sea4439-SEA 1645536146 3028682542</p>\n+    <p>Details: cache-sea4444-SEA 1645536146 1540985311</p>\n     <hr>\n     <p>Varnish cache server</p>\n   </body>\n", "comment": "Just use the name. Zone only works in English ;)", "ids": [308073, "a272110ae9846cb3355c994b121b2619f5e4c6fd", "e988efe374cb32eb6a3e5e4e8fb5c78855f2f2c1"], "repo": "home-assistant-core", "ghid": 14059, "old": "     <h1>Error 503 Backend is unhealthy</h1>\n     <p>Backend is unhealthy</p>\n     <h3>Guru Mediation:</h3>\n-    <p>Details: cache-sea4439-SEA 1645536146 3028682542</p>\n     <hr>\n     <p>Varnish cache server</p>\n   </body>", "new": "     <h1>Error 503 Backend is unhealthy</h1>\n     <p>Backend is unhealthy</p>\n     <h3>Guru Mediation:</h3>\n+    <p>Details: cache-sea4444-SEA 1645536146 1540985311</p>\n     <hr>\n     <p>Varnish cache server</p>\n   </body>", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -0,0 +1,69 @@\n+\"\"\"Config flow to configure zone component.\"\"\"\n+\n+import voluptuous as vol\n+\n+import homeassistant.helpers.config_validation as cv\n+from homeassistant import config_entries, data_entry_flow\n+from homeassistant.const import (\n+    CONF_NAME, CONF_LATITUDE, CONF_LONGITUDE, CONF_ICON, CONF_RADIUS)\n+from homeassistant.core import callback\n+from homeassistant.util import slugify\n+\n+from .const import CONF_PASSIVE, DOMAIN\n+\n+\n+@callback\n+def configured_zones(hass):\n+    \"\"\"Return a set of the configured hosts.\"\"\"\n+    return set((slugify(entry.data[CONF_NAME])) for\n+               entry in hass.config_entries.async_entries(DOMAIN))\n+\n+\n+@config_entries.HANDLERS.register(DOMAIN)\n+class ZoneFlowHandler(data_entry_flow.FlowHandler):\n+    \"\"\"Zone config flow.\"\"\"\n+\n+    VERSION = 1\n+\n+    def __init__(self):\n+        \"\"\"Initialize zone configuration flow.\"\"\"\n+        pass\n+\n+    async def async_step_init(self, user_input=None):\n+        \"\"\"Handle a flow start.\"\"\"\n+        errors = {}\n+\n+        if user_input is not None:\n+            if slugify(user_input[CONF_NAME]) not in \\\n+               configured_zones(self.hass):\n+                return self.async_create_entry(\n+                    title='Zone ' + user_input[CONF_NAME],", "msg": "Just use the name. Zone only works in English ;)"}
{"old_hunk": "@@ -16,29 +16,33 @@\n from homeassistant.loader import bind_hass\n from homeassistant.helpers import config_per_platform\n from homeassistant.helpers.entity import Entity, async_generate_entity_id\n+from homeassistant.util import slugify\n from homeassistant.util.async_ import run_callback_threadsafe\n from homeassistant.util.location import distance\n \n+from .config_flow import configured_zones\n+from .const import CONF_PASSIVE, DOMAIN\n+\n _LOGGER = logging.getLogger(__name__)\n \n ATTR_PASSIVE = 'passive'\n ATTR_RADIUS = 'radius'\n \n-CONF_PASSIVE = 'passive'\n-\n DEFAULT_NAME = 'Unnamed zone'\n DEFAULT_PASSIVE = False\n DEFAULT_RADIUS = 100\n-DOMAIN = 'zone'\n \n+HOME_ZONE = 'home'\n ENTITY_ID_FORMAT = 'zone.{}'\n-ENTITY_ID_HOME = ENTITY_ID_FORMAT.format('home')\n+ENTITY_ID_HOME = ENTITY_ID_FORMAT.format(HOME_ZONE)\n \n ICON_HOME = 'mdi:home'\n ICON_IMPORT = 'mdi:import'\n \n STATE = 'zoning'\n \n+ZONES = {}", "hunk": "@@ -10,7 +10,7 @@\n     <h1>Error 503 Backend is unhealthy</h1>\n     <p>Backend is unhealthy</p>\n     <h3>Guru Mediation:</h3>\n-    <p>Details: cache-sea4430-SEA 1645536140 1460286749</p>\n+    <p>Details: cache-sea4483-SEA 1645536140 645229786</p>\n     <hr>\n     <p>Varnish cache server</p>\n   </body>\n", "comment": "We should never use globals. Always use `hass.data` to store data.", "ids": [308069, "a272110ae9846cb3355c994b121b2619f5e4c6fd", "e988efe374cb32eb6a3e5e4e8fb5c78855f2f2c1"], "repo": "home-assistant-core", "ghid": 14059, "old": "     <h1>Error 503 Backend is unhealthy</h1>\n     <p>Backend is unhealthy</p>\n     <h3>Guru Mediation:</h3>\n-    <p>Details: cache-sea4430-SEA 1645536140 1460286749</p>\n     <hr>\n     <p>Varnish cache server</p>\n   </body>", "new": "     <h1>Error 503 Backend is unhealthy</h1>\n     <p>Backend is unhealthy</p>\n     <h3>Guru Mediation:</h3>\n+    <p>Details: cache-sea4483-SEA 1645536140 645229786</p>\n     <hr>\n     <p>Varnish cache server</p>\n   </body>", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -16,29 +16,33 @@\n from homeassistant.loader import bind_hass\n from homeassistant.helpers import config_per_platform\n from homeassistant.helpers.entity import Entity, async_generate_entity_id\n+from homeassistant.util import slugify\n from homeassistant.util.async_ import run_callback_threadsafe\n from homeassistant.util.location import distance\n \n+from .config_flow import configured_zones\n+from .const import CONF_PASSIVE, DOMAIN\n+\n _LOGGER = logging.getLogger(__name__)\n \n ATTR_PASSIVE = 'passive'\n ATTR_RADIUS = 'radius'\n \n-CONF_PASSIVE = 'passive'\n-\n DEFAULT_NAME = 'Unnamed zone'\n DEFAULT_PASSIVE = False\n DEFAULT_RADIUS = 100\n-DOMAIN = 'zone'\n \n+HOME_ZONE = 'home'\n ENTITY_ID_FORMAT = 'zone.{}'\n-ENTITY_ID_HOME = ENTITY_ID_FORMAT.format('home')\n+ENTITY_ID_HOME = ENTITY_ID_FORMAT.format(HOME_ZONE)\n \n ICON_HOME = 'mdi:home'\n ICON_IMPORT = 'mdi:import'\n \n STATE = 'zoning'\n \n+ZONES = {}", "msg": "We should never use globals. Always use `hass.data` to store data."}
{"old_hunk": "@@ -0,0 +1,69 @@\n+\"\"\"Config flow to configure zone component.\"\"\"\n+\n+import voluptuous as vol\n+\n+import homeassistant.helpers.config_validation as cv\n+from homeassistant import config_entries, data_entry_flow\n+from homeassistant.const import (\n+    CONF_NAME, CONF_LATITUDE, CONF_LONGITUDE, CONF_ICON, CONF_RADIUS)\n+from homeassistant.core import callback\n+from homeassistant.util import slugify\n+\n+from .const import CONF_PASSIVE, DOMAIN\n+\n+\n+@callback\n+def configured_zones(hass):\n+    \"\"\"Return a set of the configured hosts.\"\"\"\n+    return set((slugify(entry.data[CONF_NAME])) for\n+               entry in hass.config_entries.async_entries(DOMAIN))\n+\n+\n+@config_entries.HANDLERS.register(DOMAIN)\n+class ZoneFlowHandler(data_entry_flow.FlowHandler):\n+    \"\"\"Zone config flow.\"\"\"\n+\n+    VERSION = 1\n+\n+    def __init__(self):\n+        \"\"\"Initialize zone configuration flow.\"\"\"\n+        pass\n+\n+    async def async_step_init(self, user_input=None):\n+        \"\"\"Handle a flow start.\"\"\"\n+        errors = {}\n+\n+        if user_input is not None:\n+            if slugify(user_input[CONF_NAME]) not in \\\n+               configured_zones(self.hass):\n+                return self.async_create_entry(\n+                    title='Zone ' + user_input[CONF_NAME],\n+                    data=user_input,\n+                )\n+            errors['base'] = 'name_exists'\n+\n+        return self.async_show_form(\n+            step_id='init',\n+            data_schema=vol.Schema({\n+                vol.Required(CONF_NAME): str,\n+                vol.Required(CONF_LATITUDE): cv.latitude,\n+                vol.Required(CONF_LONGITUDE): cv.longitude,\n+                vol.Optional(CONF_RADIUS): vol.Coerce(float),\n+                vol.Optional(CONF_ICON): str,\n+                vol.Optional(CONF_PASSIVE): bool,\n+            }),\n+            errors=errors,\n+        )\n+\n+    async def async_step_import(self, import_config):\n+        \"\"\"Import a configured zone as a config entry.\n+\n+        This flow is triggered by `async_setup` for configured zones.\n+\n+        This will execute for any zone that does not have a\n+        config entry yet (based on latitude and longitude).\n+        \"\"\"\n+        return self.async_create_entry(\n+            title='Zone ' + import_config[CONF_NAME],", "hunk": "@@ -10,7 +10,7 @@\n     <h1>Error 503 Backend is unhealthy</h1>\n     <p>Backend is unhealthy</p>\n     <h3>Guru Mediation:</h3>\n-    <p>Details: cache-sea4439-SEA 1645536148 3028693695</p>\n+    <p>Details: cache-sea4444-SEA 1645536148 1540996020</p>\n     <hr>\n     <p>Varnish cache server</p>\n   </body>\n", "comment": "Use name directly. Should the import task check for duplicates or should that happen inside the setup method?", "ids": [308076, "a272110ae9846cb3355c994b121b2619f5e4c6fd", "e988efe374cb32eb6a3e5e4e8fb5c78855f2f2c1"], "repo": "home-assistant-core", "ghid": 14059, "old": "     <h1>Error 503 Backend is unhealthy</h1>\n     <p>Backend is unhealthy</p>\n     <h3>Guru Mediation:</h3>\n-    <p>Details: cache-sea4439-SEA 1645536148 3028693695</p>\n     <hr>\n     <p>Varnish cache server</p>\n   </body>", "new": "     <h1>Error 503 Backend is unhealthy</h1>\n     <p>Backend is unhealthy</p>\n     <h3>Guru Mediation:</h3>\n+    <p>Details: cache-sea4444-SEA 1645536148 1540996020</p>\n     <hr>\n     <p>Varnish cache server</p>\n   </body>", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -0,0 +1,69 @@\n+\"\"\"Config flow to configure zone component.\"\"\"\n+\n+import voluptuous as vol\n+\n+import homeassistant.helpers.config_validation as cv\n+from homeassistant import config_entries, data_entry_flow\n+from homeassistant.const import (\n+    CONF_NAME, CONF_LATITUDE, CONF_LONGITUDE, CONF_ICON, CONF_RADIUS)\n+from homeassistant.core import callback\n+from homeassistant.util import slugify\n+\n+from .const import CONF_PASSIVE, DOMAIN\n+\n+\n+@callback\n+def configured_zones(hass):\n+    \"\"\"Return a set of the configured hosts.\"\"\"\n+    return set((slugify(entry.data[CONF_NAME])) for\n+               entry in hass.config_entries.async_entries(DOMAIN))\n+\n+\n+@config_entries.HANDLERS.register(DOMAIN)\n+class ZoneFlowHandler(data_entry_flow.FlowHandler):\n+    \"\"\"Zone config flow.\"\"\"\n+\n+    VERSION = 1\n+\n+    def __init__(self):\n+        \"\"\"Initialize zone configuration flow.\"\"\"\n+        pass\n+\n+    async def async_step_init(self, user_input=None):\n+        \"\"\"Handle a flow start.\"\"\"\n+        errors = {}\n+\n+        if user_input is not None:\n+            if slugify(user_input[CONF_NAME]) not in \\\n+               configured_zones(self.hass):\n+                return self.async_create_entry(\n+                    title='Zone ' + user_input[CONF_NAME],\n+                    data=user_input,\n+                )\n+            errors['base'] = 'name_exists'\n+\n+        return self.async_show_form(\n+            step_id='init',\n+            data_schema=vol.Schema({\n+                vol.Required(CONF_NAME): str,\n+                vol.Required(CONF_LATITUDE): cv.latitude,\n+                vol.Required(CONF_LONGITUDE): cv.longitude,\n+                vol.Optional(CONF_RADIUS): vol.Coerce(float),\n+                vol.Optional(CONF_ICON): str,\n+                vol.Optional(CONF_PASSIVE): bool,\n+            }),\n+            errors=errors,\n+        )\n+\n+    async def async_step_import(self, import_config):\n+        \"\"\"Import a configured zone as a config entry.\n+\n+        This flow is triggered by `async_setup` for configured zones.\n+\n+        This will execute for any zone that does not have a\n+        config entry yet (based on latitude and longitude).\n+        \"\"\"\n+        return self.async_create_entry(\n+            title='Zone ' + import_config[CONF_NAME],", "msg": "Use name directly. Should the import task check for duplicates or should that happen inside the setup method?"}
{"old_hunk": "@@ -0,0 +1,69 @@\n+\"\"\"Config flow to configure zone component.\"\"\"\n+\n+import voluptuous as vol\n+\n+import homeassistant.helpers.config_validation as cv\n+from homeassistant import config_entries, data_entry_flow\n+from homeassistant.const import (\n+    CONF_NAME, CONF_LATITUDE, CONF_LONGITUDE, CONF_ICON, CONF_RADIUS)\n+from homeassistant.core import callback\n+from homeassistant.util import slugify\n+\n+from .const import CONF_PASSIVE, DOMAIN\n+\n+\n+@callback\n+def configured_zones(hass):\n+    \"\"\"Return a set of the configured hosts.\"\"\"\n+    return set((slugify(entry.data[CONF_NAME])) for\n+               entry in hass.config_entries.async_entries(DOMAIN))\n+\n+\n+@config_entries.HANDLERS.register(DOMAIN)\n+class ZoneFlowHandler(data_entry_flow.FlowHandler):\n+    \"\"\"Zone config flow.\"\"\"\n+\n+    VERSION = 1\n+\n+    def __init__(self):\n+        \"\"\"Initialize zone configuration flow.\"\"\"\n+        pass\n+\n+    async def async_step_init(self, user_input=None):\n+        \"\"\"Handle a flow start.\"\"\"\n+        errors = {}\n+\n+        if user_input is not None:\n+            if slugify(user_input[CONF_NAME]) not in \\\n+               configured_zones(self.hass):\n+                return self.async_create_entry(\n+                    title='Zone ' + user_input[CONF_NAME],\n+                    data=user_input,\n+                )\n+            errors['base'] = 'name_exists'\n+\n+        return self.async_show_form(\n+            step_id='init',\n+            data_schema=vol.Schema({\n+                vol.Required(CONF_NAME): str,\n+                vol.Required(CONF_LATITUDE): cv.latitude,", "hunk": "@@ -10,7 +10,7 @@\n     <h1>Error 503 Backend is unhealthy</h1>\n     <p>Backend is unhealthy</p>\n     <h3>Guru Mediation:</h3>\n-    <p>Details: cache-sea4439-SEA 1645536147 3028686496</p>\n+    <p>Details: cache-sea4444-SEA 1645536147 1540989255</p>\n     <hr>\n     <p>Varnish cache server</p>\n   </body>\n", "comment": "We will need to update voluptuous-serialize to allow registering custom types.", "ids": [308074, "a272110ae9846cb3355c994b121b2619f5e4c6fd", "e988efe374cb32eb6a3e5e4e8fb5c78855f2f2c1"], "repo": "home-assistant-core", "ghid": 14059, "old": "     <h1>Error 503 Backend is unhealthy</h1>\n     <p>Backend is unhealthy</p>\n     <h3>Guru Mediation:</h3>\n-    <p>Details: cache-sea4439-SEA 1645536147 3028686496</p>\n     <hr>\n     <p>Varnish cache server</p>\n   </body>", "new": "     <h1>Error 503 Backend is unhealthy</h1>\n     <p>Backend is unhealthy</p>\n     <h3>Guru Mediation:</h3>\n+    <p>Details: cache-sea4444-SEA 1645536147 1540989255</p>\n     <hr>\n     <p>Varnish cache server</p>\n   </body>", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -0,0 +1,69 @@\n+\"\"\"Config flow to configure zone component.\"\"\"\n+\n+import voluptuous as vol\n+\n+import homeassistant.helpers.config_validation as cv\n+from homeassistant import config_entries, data_entry_flow\n+from homeassistant.const import (\n+    CONF_NAME, CONF_LATITUDE, CONF_LONGITUDE, CONF_ICON, CONF_RADIUS)\n+from homeassistant.core import callback\n+from homeassistant.util import slugify\n+\n+from .const import CONF_PASSIVE, DOMAIN\n+\n+\n+@callback\n+def configured_zones(hass):\n+    \"\"\"Return a set of the configured hosts.\"\"\"\n+    return set((slugify(entry.data[CONF_NAME])) for\n+               entry in hass.config_entries.async_entries(DOMAIN))\n+\n+\n+@config_entries.HANDLERS.register(DOMAIN)\n+class ZoneFlowHandler(data_entry_flow.FlowHandler):\n+    \"\"\"Zone config flow.\"\"\"\n+\n+    VERSION = 1\n+\n+    def __init__(self):\n+        \"\"\"Initialize zone configuration flow.\"\"\"\n+        pass\n+\n+    async def async_step_init(self, user_input=None):\n+        \"\"\"Handle a flow start.\"\"\"\n+        errors = {}\n+\n+        if user_input is not None:\n+            if slugify(user_input[CONF_NAME]) not in \\\n+               configured_zones(self.hass):\n+                return self.async_create_entry(\n+                    title='Zone ' + user_input[CONF_NAME],\n+                    data=user_input,\n+                )\n+            errors['base'] = 'name_exists'\n+\n+        return self.async_show_form(\n+            step_id='init',\n+            data_schema=vol.Schema({\n+                vol.Required(CONF_NAME): str,\n+                vol.Required(CONF_LATITUDE): cv.latitude,", "msg": "We will need to update voluptuous-serialize to allow registering custom types."}
{"old_hunk": "@@ -0,0 +1,69 @@\n+\"\"\"Config flow to configure zone component.\"\"\"\n+\n+import voluptuous as vol\n+\n+import homeassistant.helpers.config_validation as cv\n+from homeassistant import config_entries, data_entry_flow\n+from homeassistant.const import (\n+    CONF_NAME, CONF_LATITUDE, CONF_LONGITUDE, CONF_ICON, CONF_RADIUS)\n+from homeassistant.core import callback\n+from homeassistant.util import slugify\n+\n+from .const import CONF_PASSIVE, DOMAIN\n+\n+\n+@callback\n+def configured_zones(hass):\n+    \"\"\"Return a set of the configured hosts.\"\"\"\n+    return set((slugify(entry.data[CONF_NAME])) for\n+               entry in hass.config_entries.async_entries(DOMAIN))\n+\n+\n+@config_entries.HANDLERS.register(DOMAIN)\n+class ZoneFlowHandler(data_entry_flow.FlowHandler):\n+    \"\"\"Zone config flow.\"\"\"\n+\n+    VERSION = 1\n+\n+    def __init__(self):\n+        \"\"\"Initialize zone configuration flow.\"\"\"\n+        pass\n+\n+    async def async_step_init(self, user_input=None):\n+        \"\"\"Handle a flow start.\"\"\"\n+        errors = {}\n+\n+        if user_input is not None:\n+            if slugify(user_input[CONF_NAME]) not in \\\n+               configured_zones(self.hass):\n+                return self.async_create_entry(\n+                    title='Zone ' + user_input[CONF_NAME],\n+                    data=user_input,\n+                )\n+            errors['base'] = 'name_exists'\n+\n+        return self.async_show_form(\n+            step_id='init',\n+            data_schema=vol.Schema({\n+                vol.Required(CONF_NAME): str,\n+                vol.Required(CONF_LATITUDE): cv.latitude,\n+                vol.Required(CONF_LONGITUDE): cv.longitude,\n+                vol.Optional(CONF_RADIUS): vol.Coerce(float),", "hunk": "@@ -10,7 +10,7 @@\n     <h1>Error 503 Backend is unhealthy</h1>\n     <p>Backend is unhealthy</p>\n     <h3>Guru Mediation:</h3>\n-    <p>Details: cache-sea4439-SEA 1645536147 3028690176</p>\n+    <p>Details: cache-sea4427-SEA 1645536147 878665540</p>\n     <hr>\n     <p>Varnish cache server</p>\n   </body>\n", "comment": "I don't think that voluptuous serialize can consume this?", "ids": [308075, "a272110ae9846cb3355c994b121b2619f5e4c6fd", "e988efe374cb32eb6a3e5e4e8fb5c78855f2f2c1"], "repo": "home-assistant-core", "ghid": 14059, "old": "     <h1>Error 503 Backend is unhealthy</h1>\n     <p>Backend is unhealthy</p>\n     <h3>Guru Mediation:</h3>\n-    <p>Details: cache-sea4439-SEA 1645536147 3028690176</p>\n     <hr>\n     <p>Varnish cache server</p>\n   </body>", "new": "     <h1>Error 503 Backend is unhealthy</h1>\n     <p>Backend is unhealthy</p>\n     <h3>Guru Mediation:</h3>\n+    <p>Details: cache-sea4427-SEA 1645536147 878665540</p>\n     <hr>\n     <p>Varnish cache server</p>\n   </body>", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -0,0 +1,69 @@\n+\"\"\"Config flow to configure zone component.\"\"\"\n+\n+import voluptuous as vol\n+\n+import homeassistant.helpers.config_validation as cv\n+from homeassistant import config_entries, data_entry_flow\n+from homeassistant.const import (\n+    CONF_NAME, CONF_LATITUDE, CONF_LONGITUDE, CONF_ICON, CONF_RADIUS)\n+from homeassistant.core import callback\n+from homeassistant.util import slugify\n+\n+from .const import CONF_PASSIVE, DOMAIN\n+\n+\n+@callback\n+def configured_zones(hass):\n+    \"\"\"Return a set of the configured hosts.\"\"\"\n+    return set((slugify(entry.data[CONF_NAME])) for\n+               entry in hass.config_entries.async_entries(DOMAIN))\n+\n+\n+@config_entries.HANDLERS.register(DOMAIN)\n+class ZoneFlowHandler(data_entry_flow.FlowHandler):\n+    \"\"\"Zone config flow.\"\"\"\n+\n+    VERSION = 1\n+\n+    def __init__(self):\n+        \"\"\"Initialize zone configuration flow.\"\"\"\n+        pass\n+\n+    async def async_step_init(self, user_input=None):\n+        \"\"\"Handle a flow start.\"\"\"\n+        errors = {}\n+\n+        if user_input is not None:\n+            if slugify(user_input[CONF_NAME]) not in \\\n+               configured_zones(self.hass):\n+                return self.async_create_entry(\n+                    title='Zone ' + user_input[CONF_NAME],\n+                    data=user_input,\n+                )\n+            errors['base'] = 'name_exists'\n+\n+        return self.async_show_form(\n+            step_id='init',\n+            data_schema=vol.Schema({\n+                vol.Required(CONF_NAME): str,\n+                vol.Required(CONF_LATITUDE): cv.latitude,\n+                vol.Required(CONF_LONGITUDE): cv.longitude,\n+                vol.Optional(CONF_RADIUS): vol.Coerce(float),", "msg": "I don't think that voluptuous serialize can consume this?"}
{"old_hunk": "@@ -0,0 +1,88 @@\n+\"\"\"\n+Support for Social Blade .\n+\n+For more details about this platform, please refer to the documentation at\n+https://home-assistant.io/components/sensor.socialblade/\n+\"\"\"\n+\n+import logging\n+from datetime import timedelta\n+import voluptuous as vol\n+\n+from homeassistant.helpers.entity import Entity\n+from homeassistant.util import Throttle\n+from homeassistant.components.sensor import PLATFORM_SCHEMA\n+from homeassistant.helpers import config_validation as cv\n+from homeassistant.const import CONF_FRIENDLY_NAME\n+\n+\n+_LOGGER = logging.getLogger(__name__)\n+\n+REQUIREMENTS = ['socialbladeclient==0.2']\n+\n+DEFAULT_NAME = \"Social Blade\"\n+\n+PLATFORM_SCHEMA = PLATFORM_SCHEMA.extend({\n+    vol.Optional(CONF_FRIENDLY_NAME, default=DEFAULT_NAME): cv.string,\n+    vol.Required('channel_id'): cv.string,", "hunk": "@@ -10,7 +10,7 @@\n     <h1>Error 503 Backend is unhealthy</h1>\n     <p>Backend is unhealthy</p>\n     <h3>Guru Mediation:</h3>\n-    <p>Details: cache-sea4437-SEA 1645536151 1604790611</p>\n+    <p>Details: cache-sea4425-SEA 1645536151 1594728248</p>\n     <hr>\n     <p>Varnish cache server</p>\n   </body>\n", "comment": "Replace `channel_id` with a constant and use it here and later.", "ids": [308101, "667fb373352bb69d2f26a544cd1af02afe7256fd", "eafda4caa22b422b015bc3572237ac638b155a67"], "repo": "home-assistant-core", "ghid": 14060, "old": "     <h1>Error 503 Backend is unhealthy</h1>\n     <p>Backend is unhealthy</p>\n     <h3>Guru Mediation:</h3>\n-    <p>Details: cache-sea4437-SEA 1645536151 1604790611</p>\n     <hr>\n     <p>Varnish cache server</p>\n   </body>", "new": "     <h1>Error 503 Backend is unhealthy</h1>\n     <p>Backend is unhealthy</p>\n     <h3>Guru Mediation:</h3>\n+    <p>Details: cache-sea4425-SEA 1645536151 1594728248</p>\n     <hr>\n     <p>Varnish cache server</p>\n   </body>", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -0,0 +1,88 @@\n+\"\"\"\n+Support for Social Blade .\n+\n+For more details about this platform, please refer to the documentation at\n+https://home-assistant.io/components/sensor.socialblade/\n+\"\"\"\n+\n+import logging\n+from datetime import timedelta\n+import voluptuous as vol\n+\n+from homeassistant.helpers.entity import Entity\n+from homeassistant.util import Throttle\n+from homeassistant.components.sensor import PLATFORM_SCHEMA\n+from homeassistant.helpers import config_validation as cv\n+from homeassistant.const import CONF_FRIENDLY_NAME\n+\n+\n+_LOGGER = logging.getLogger(__name__)\n+\n+REQUIREMENTS = ['socialbladeclient==0.2']\n+\n+DEFAULT_NAME = \"Social Blade\"\n+\n+PLATFORM_SCHEMA = PLATFORM_SCHEMA.extend({\n+    vol.Optional(CONF_FRIENDLY_NAME, default=DEFAULT_NAME): cv.string,\n+    vol.Required('channel_id'): cv.string,", "msg": "Replace `channel_id` with a constant and use it here and later."}
{"old_hunk": "@@ -0,0 +1,88 @@\n+\"\"\"\n+Support for Social Blade .\n+\n+For more details about this platform, please refer to the documentation at\n+https://home-assistant.io/components/sensor.socialblade/\n+\"\"\"\n+\n+import logging\n+from datetime import timedelta\n+import voluptuous as vol\n+\n+from homeassistant.helpers.entity import Entity\n+from homeassistant.util import Throttle\n+from homeassistant.components.sensor import PLATFORM_SCHEMA\n+from homeassistant.helpers import config_validation as cv\n+from homeassistant.const import CONF_FRIENDLY_NAME\n+\n+\n+_LOGGER = logging.getLogger(__name__)\n+\n+REQUIREMENTS = ['socialbladeclient==0.2']\n+\n+DEFAULT_NAME = \"Social Blade\"\n+\n+PLATFORM_SCHEMA = PLATFORM_SCHEMA.extend({\n+    vol.Optional(CONF_FRIENDLY_NAME, default=DEFAULT_NAME): cv.string,\n+    vol.Required('channel_id'): cv.string,\n+})\n+\n+\n+def setup_platform(hass, config, add_devices, discovery_info=None):\n+    \"\"\"Setting the platform in HASS and Channel Information.\"\"\"\n+    social_blade = SocialBladeSensor(config['channel_id'],\n+                                     config[CONF_FRIENDLY_NAME])\n+    social_blade.update()\n+    if social_blade.valid_channel_id:\n+        add_devices([social_blade])\n+    else:\n+        _LOGGER.error(\"Setup Social Blade Sensor Fail\"\n+                      \" check if your channel ID is valid\")", "hunk": "@@ -10,7 +10,7 @@\n     <h1>Error 503 Backend is unhealthy</h1>\n     <p>Backend is unhealthy</p>\n     <h3>Guru Mediation:</h3>\n-    <p>Details: cache-sea4437-SEA 1645536152 1604796170</p>\n+    <p>Details: cache-sea4425-SEA 1645536152 1594733765</p>\n     <hr>\n     <p>Varnish cache server</p>\n   </body>\n", "comment": "Setup should fail if something is wrong.", "ids": [308103, "667fb373352bb69d2f26a544cd1af02afe7256fd", "eafda4caa22b422b015bc3572237ac638b155a67"], "repo": "home-assistant-core", "ghid": 14060, "old": "     <h1>Error 503 Backend is unhealthy</h1>\n     <p>Backend is unhealthy</p>\n     <h3>Guru Mediation:</h3>\n-    <p>Details: cache-sea4437-SEA 1645536152 1604796170</p>\n     <hr>\n     <p>Varnish cache server</p>\n   </body>", "new": "     <h1>Error 503 Backend is unhealthy</h1>\n     <p>Backend is unhealthy</p>\n     <h3>Guru Mediation:</h3>\n+    <p>Details: cache-sea4425-SEA 1645536152 1594733765</p>\n     <hr>\n     <p>Varnish cache server</p>\n   </body>", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -0,0 +1,88 @@\n+\"\"\"\n+Support for Social Blade .\n+\n+For more details about this platform, please refer to the documentation at\n+https://home-assistant.io/components/sensor.socialblade/\n+\"\"\"\n+\n+import logging\n+from datetime import timedelta\n+import voluptuous as vol\n+\n+from homeassistant.helpers.entity import Entity\n+from homeassistant.util import Throttle\n+from homeassistant.components.sensor import PLATFORM_SCHEMA\n+from homeassistant.helpers import config_validation as cv\n+from homeassistant.const import CONF_FRIENDLY_NAME\n+\n+\n+_LOGGER = logging.getLogger(__name__)\n+\n+REQUIREMENTS = ['socialbladeclient==0.2']\n+\n+DEFAULT_NAME = \"Social Blade\"\n+\n+PLATFORM_SCHEMA = PLATFORM_SCHEMA.extend({\n+    vol.Optional(CONF_FRIENDLY_NAME, default=DEFAULT_NAME): cv.string,\n+    vol.Required('channel_id'): cv.string,\n+})\n+\n+\n+def setup_platform(hass, config, add_devices, discovery_info=None):\n+    \"\"\"Setting the platform in HASS and Channel Information.\"\"\"\n+    social_blade = SocialBladeSensor(config['channel_id'],\n+                                     config[CONF_FRIENDLY_NAME])\n+    social_blade.update()\n+    if social_blade.valid_channel_id:\n+        add_devices([social_blade])\n+    else:\n+        _LOGGER.error(\"Setup Social Blade Sensor Fail\"\n+                      \" check if your channel ID is valid\")", "msg": "Setup should fail if something is wrong."}
{"old_hunk": "@@ -0,0 +1,88 @@\n+\"\"\"\n+Support for Social Blade .\n+\n+For more details about this platform, please refer to the documentation at\n+https://home-assistant.io/components/sensor.socialblade/\n+\"\"\"\n+\n+import logging\n+from datetime import timedelta\n+import voluptuous as vol\n+\n+from homeassistant.helpers.entity import Entity\n+from homeassistant.util import Throttle\n+from homeassistant.components.sensor import PLATFORM_SCHEMA\n+from homeassistant.helpers import config_validation as cv\n+from homeassistant.const import CONF_FRIENDLY_NAME\n+\n+\n+_LOGGER = logging.getLogger(__name__)\n+\n+REQUIREMENTS = ['socialbladeclient==0.2']\n+\n+DEFAULT_NAME = \"Social Blade\"\n+\n+PLATFORM_SCHEMA = PLATFORM_SCHEMA.extend({\n+    vol.Optional(CONF_FRIENDLY_NAME, default=DEFAULT_NAME): cv.string,", "hunk": "@@ -10,7 +10,7 @@\n     <h1>Error 503 Backend is unhealthy</h1>\n     <p>Backend is unhealthy</p>\n     <h3>Guru Mediation:</h3>\n-    <p>Details: cache-sea4437-SEA 1645536151 1604787335</p>\n+    <p>Details: cache-sea4425-SEA 1645536151 1594725675</p>\n     <hr>\n     <p>Varnish cache server</p>\n   </body>\n", "comment": "This should be `CONF_NAME`.", "ids": [308100, "667fb373352bb69d2f26a544cd1af02afe7256fd", "eafda4caa22b422b015bc3572237ac638b155a67"], "repo": "home-assistant-core", "ghid": 14060, "old": "     <h1>Error 503 Backend is unhealthy</h1>\n     <p>Backend is unhealthy</p>\n     <h3>Guru Mediation:</h3>\n-    <p>Details: cache-sea4437-SEA 1645536151 1604787335</p>\n     <hr>\n     <p>Varnish cache server</p>\n   </body>", "new": "     <h1>Error 503 Backend is unhealthy</h1>\n     <p>Backend is unhealthy</p>\n     <h3>Guru Mediation:</h3>\n+    <p>Details: cache-sea4425-SEA 1645536151 1594725675</p>\n     <hr>\n     <p>Varnish cache server</p>\n   </body>", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -0,0 +1,88 @@\n+\"\"\"\n+Support for Social Blade .\n+\n+For more details about this platform, please refer to the documentation at\n+https://home-assistant.io/components/sensor.socialblade/\n+\"\"\"\n+\n+import logging\n+from datetime import timedelta\n+import voluptuous as vol\n+\n+from homeassistant.helpers.entity import Entity\n+from homeassistant.util import Throttle\n+from homeassistant.components.sensor import PLATFORM_SCHEMA\n+from homeassistant.helpers import config_validation as cv\n+from homeassistant.const import CONF_FRIENDLY_NAME\n+\n+\n+_LOGGER = logging.getLogger(__name__)\n+\n+REQUIREMENTS = ['socialbladeclient==0.2']\n+\n+DEFAULT_NAME = \"Social Blade\"\n+\n+PLATFORM_SCHEMA = PLATFORM_SCHEMA.extend({\n+    vol.Optional(CONF_FRIENDLY_NAME, default=DEFAULT_NAME): cv.string,", "msg": "This should be `CONF_NAME`."}
{"old_hunk": "@@ -0,0 +1,88 @@\n+\"\"\"\n+Support for Social Blade .\n+\n+For more details about this platform, please refer to the documentation at\n+https://home-assistant.io/components/sensor.socialblade/\n+\"\"\"\n+\n+import logging\n+from datetime import timedelta\n+import voluptuous as vol\n+\n+from homeassistant.helpers.entity import Entity\n+from homeassistant.util import Throttle\n+from homeassistant.components.sensor import PLATFORM_SCHEMA\n+from homeassistant.helpers import config_validation as cv\n+from homeassistant.const import CONF_FRIENDLY_NAME\n+\n+\n+_LOGGER = logging.getLogger(__name__)\n+\n+REQUIREMENTS = ['socialbladeclient==0.2']\n+\n+DEFAULT_NAME = \"Social Blade\"\n+\n+PLATFORM_SCHEMA = PLATFORM_SCHEMA.extend({\n+    vol.Optional(CONF_FRIENDLY_NAME, default=DEFAULT_NAME): cv.string,\n+    vol.Required('channel_id'): cv.string,\n+})\n+\n+\n+def setup_platform(hass, config, add_devices, discovery_info=None):\n+    \"\"\"Setting the platform in HASS and Channel Information.\"\"\"\n+    social_blade = SocialBladeSensor(config['channel_id'],\n+                                     config[CONF_FRIENDLY_NAME])\n+    social_blade.update()\n+    if social_blade.valid_channel_id:\n+        add_devices([social_blade])\n+    else:\n+        _LOGGER.error(\"Setup Social Blade Sensor Fail\"\n+                      \" check if your channel ID is valid\")\n+\n+\n+class SocialBladeSensor(Entity):\n+    \"\"\"Representation of a Social Blade Sensor.\"\"\"\n+\n+    MIN_TIME_BETWEEN_UPDATES = timedelta(hours=2)", "hunk": "@@ -10,7 +10,7 @@\n     <h1>Error 503 Backend is unhealthy</h1>\n     <p>Backend is unhealthy</p>\n     <h3>Guru Mediation:</h3>\n-    <p>Details: cache-sea4437-SEA 1645536152 1604799837</p>\n+    <p>Details: cache-sea4425-SEA 1645536153 1594737325</p>\n     <hr>\n     <p>Varnish cache server</p>\n   </body>\n", "comment": "Move this to the file header.", "ids": [308104, "667fb373352bb69d2f26a544cd1af02afe7256fd", "eafda4caa22b422b015bc3572237ac638b155a67"], "repo": "home-assistant-core", "ghid": 14060, "old": "     <h1>Error 503 Backend is unhealthy</h1>\n     <p>Backend is unhealthy</p>\n     <h3>Guru Mediation:</h3>\n-    <p>Details: cache-sea4437-SEA 1645536152 1604799837</p>\n     <hr>\n     <p>Varnish cache server</p>\n   </body>", "new": "     <h1>Error 503 Backend is unhealthy</h1>\n     <p>Backend is unhealthy</p>\n     <h3>Guru Mediation:</h3>\n+    <p>Details: cache-sea4425-SEA 1645536153 1594737325</p>\n     <hr>\n     <p>Varnish cache server</p>\n   </body>", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -0,0 +1,88 @@\n+\"\"\"\n+Support for Social Blade .\n+\n+For more details about this platform, please refer to the documentation at\n+https://home-assistant.io/components/sensor.socialblade/\n+\"\"\"\n+\n+import logging\n+from datetime import timedelta\n+import voluptuous as vol\n+\n+from homeassistant.helpers.entity import Entity\n+from homeassistant.util import Throttle\n+from homeassistant.components.sensor import PLATFORM_SCHEMA\n+from homeassistant.helpers import config_validation as cv\n+from homeassistant.const import CONF_FRIENDLY_NAME\n+\n+\n+_LOGGER = logging.getLogger(__name__)\n+\n+REQUIREMENTS = ['socialbladeclient==0.2']\n+\n+DEFAULT_NAME = \"Social Blade\"\n+\n+PLATFORM_SCHEMA = PLATFORM_SCHEMA.extend({\n+    vol.Optional(CONF_FRIENDLY_NAME, default=DEFAULT_NAME): cv.string,\n+    vol.Required('channel_id'): cv.string,\n+})\n+\n+\n+def setup_platform(hass, config, add_devices, discovery_info=None):\n+    \"\"\"Setting the platform in HASS and Channel Information.\"\"\"\n+    social_blade = SocialBladeSensor(config['channel_id'],\n+                                     config[CONF_FRIENDLY_NAME])\n+    social_blade.update()\n+    if social_blade.valid_channel_id:\n+        add_devices([social_blade])\n+    else:\n+        _LOGGER.error(\"Setup Social Blade Sensor Fail\"\n+                      \" check if your channel ID is valid\")\n+\n+\n+class SocialBladeSensor(Entity):\n+    \"\"\"Representation of a Social Blade Sensor.\"\"\"\n+\n+    MIN_TIME_BETWEEN_UPDATES = timedelta(hours=2)", "msg": "Move this to the file header."}
{"old_hunk": "@@ -0,0 +1,88 @@\n+\"\"\"\n+Support for Social Blade .\n+\n+For more details about this platform, please refer to the documentation at\n+https://home-assistant.io/components/sensor.socialblade/\n+\"\"\"\n+\n+import logging\n+from datetime import timedelta\n+import voluptuous as vol\n+\n+from homeassistant.helpers.entity import Entity\n+from homeassistant.util import Throttle\n+from homeassistant.components.sensor import PLATFORM_SCHEMA\n+from homeassistant.helpers import config_validation as cv\n+from homeassistant.const import CONF_FRIENDLY_NAME", "hunk": "@@ -10,7 +10,7 @@\n     <h1>Error 503 Backend is unhealthy</h1>\n     <p>Backend is unhealthy</p>\n     <h3>Guru Mediation:</h3>\n-    <p>Details: cache-sea4437-SEA 1645536150 1604784156</p>\n+    <p>Details: cache-sea4425-SEA 1645536150 1594722261</p>\n     <hr>\n     <p>Varnish cache server</p>\n   </body>\n", "comment": "Imports should be grouped and ordered.", "ids": [308099, "667fb373352bb69d2f26a544cd1af02afe7256fd", "eafda4caa22b422b015bc3572237ac638b155a67"], "repo": "home-assistant-core", "ghid": 14060, "old": "     <h1>Error 503 Backend is unhealthy</h1>\n     <p>Backend is unhealthy</p>\n     <h3>Guru Mediation:</h3>\n-    <p>Details: cache-sea4437-SEA 1645536150 1604784156</p>\n     <hr>\n     <p>Varnish cache server</p>\n   </body>", "new": "     <h1>Error 503 Backend is unhealthy</h1>\n     <p>Backend is unhealthy</p>\n     <h3>Guru Mediation:</h3>\n+    <p>Details: cache-sea4425-SEA 1645536150 1594722261</p>\n     <hr>\n     <p>Varnish cache server</p>\n   </body>", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -0,0 +1,88 @@\n+\"\"\"\n+Support for Social Blade .\n+\n+For more details about this platform, please refer to the documentation at\n+https://home-assistant.io/components/sensor.socialblade/\n+\"\"\"\n+\n+import logging\n+from datetime import timedelta\n+import voluptuous as vol\n+\n+from homeassistant.helpers.entity import Entity\n+from homeassistant.util import Throttle\n+from homeassistant.components.sensor import PLATFORM_SCHEMA\n+from homeassistant.helpers import config_validation as cv\n+from homeassistant.const import CONF_FRIENDLY_NAME", "msg": "Imports should be grouped and ordered."}
{"old_hunk": "@@ -0,0 +1,81 @@\n+\"\"\"\n+Support for HomematicIP switch.\n+\n+For more details about this component, please refer to the documentation at\n+https://home-assistant.io/components/switch.homematicip_cloud/\n+\"\"\"\n+\n+import logging\n+\n+from homeassistant.components.switch import SwitchDevice\n+from homeassistant.components.homematicip_cloud import (\n+    HomematicipGenericDevice, DOMAIN as HOMEMATICIP_CLOUD_DOMAIN,\n+    ATTR_HOME_ID)\n+\n+DEPENDENCIES = ['homematicip_cloud']\n+\n+_LOGGER = logging.getLogger(__name__)\n+\n+ATTR_POWER_CONSUMPTION = 'power_consumption'\n+ATTR_ENERGIE_COUNTER = 'energie_counter'\n+ATTR_PROFILE_MODE = 'profile_mode'\n+\n+\n+async def async_setup_platform(hass, config, async_add_devices,\n+                               discovery_info=None):\n+    \"\"\"Set up the HomematicIP switch devices.\"\"\"\n+    from homematicip.device import (\n+        PlugableSwitch, PlugableSwitchMeasuring,\n+        BrandSwitchMeasuring)\n+\n+    if discovery_info is None:\n+        return\n+    home = hass.data[HOMEMATICIP_CLOUD_DOMAIN][discovery_info[ATTR_HOME_ID]]\n+    devices = []\n+    for device in home.devices:\n+        if isinstance(device, BrandSwitchMeasuring):\n+            pass", "hunk": "@@ -34,9 +34,10 @@ async def async_setup_platform(hass, config, async_add_devices,\n     devices = []\n     for device in home.devices:\n         if isinstance(device, BrandSwitchMeasuring):\n+            # BrandSwitch is implemented in the light platform\n             pass\n         elif isinstance(device, PlugableSwitchMeasuring):\n-            devices.append(HomematicipMeasuringSwitch(home, device))\n+            devices.append(HomematicipSwitchMeasuring(home, device))\n         elif isinstance(device, PlugableSwitch):\n             devices.append(HomematicipSwitch(home, device))\n \n", "comment": "Why are you implementing this as a light and not as a switch ?", "ids": [308252, "f23b12002a4fb17f6ac7db5de02103a3ce2f773e", "f09025b5cbb65084f0cb12f9311103a1b21a5835"], "repo": "home-assistant-core", "ghid": 14084, "old": "     devices = []\n     for device in home.devices:\n         if isinstance(device, BrandSwitchMeasuring):\n             pass\n         elif isinstance(device, PlugableSwitchMeasuring):\n-            devices.append(HomematicipMeasuringSwitch(home, device))\n         elif isinstance(device, PlugableSwitch):\n             devices.append(HomematicipSwitch(home, device))", "new": "     devices = []\n     for device in home.devices:\n         if isinstance(device, BrandSwitchMeasuring):\n+            # BrandSwitch is implemented in the light platform\n             pass\n         elif isinstance(device, PlugableSwitchMeasuring):\n+            devices.append(HomematicipSwitchMeasuring(home, device))\n         elif isinstance(device, PlugableSwitch):\n             devices.append(HomematicipSwitch(home, device))", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -0,0 +1,81 @@\n+\"\"\"\n+Support for HomematicIP switch.\n+\n+For more details about this component, please refer to the documentation at\n+https://home-assistant.io/components/switch.homematicip_cloud/\n+\"\"\"\n+\n+import logging\n+\n+from homeassistant.components.switch import SwitchDevice\n+from homeassistant.components.homematicip_cloud import (\n+    HomematicipGenericDevice, DOMAIN as HOMEMATICIP_CLOUD_DOMAIN,\n+    ATTR_HOME_ID)\n+\n+DEPENDENCIES = ['homematicip_cloud']\n+\n+_LOGGER = logging.getLogger(__name__)\n+\n+ATTR_POWER_CONSUMPTION = 'power_consumption'\n+ATTR_ENERGIE_COUNTER = 'energie_counter'\n+ATTR_PROFILE_MODE = 'profile_mode'\n+\n+\n+async def async_setup_platform(hass, config, async_add_devices,\n+                               discovery_info=None):\n+    \"\"\"Set up the HomematicIP switch devices.\"\"\"\n+    from homematicip.device import (\n+        PlugableSwitch, PlugableSwitchMeasuring,\n+        BrandSwitchMeasuring)\n+\n+    if discovery_info is None:\n+        return\n+    home = hass.data[HOMEMATICIP_CLOUD_DOMAIN][discovery_info[ATTR_HOME_ID]]\n+    devices = []\n+    for device in home.devices:\n+        if isinstance(device, BrandSwitchMeasuring):\n+            pass", "msg": "Why are you implementing this as a light and not as a switch ?"}
{"old_hunk": "@@ -0,0 +1,112 @@\n+\"\"\"\n+This component provides support for RainMachine sprinkler controllers.\n+\n+For more details about this component, please refer to the documentation at\n+https://home-assistant.io/components/rainmachine/\n+\"\"\"\n+import logging\n+from datetime import timedelta\n+\n+import voluptuous as vol\n+from requests.exceptions import HTTPError, ConnectTimeout", "hunk": "@@ -8,7 +8,7 @@ import logging\n from datetime import timedelta\n \n import voluptuous as vol\n-from requests.exceptions import HTTPError, ConnectTimeout\n+from requests.exceptions import ConnectTimeout\n \n from homeassistant.helpers import config_validation as cv\n from homeassistant.const import (\n", "comment": "'requests.exceptions.HTTPError' imported but unused<br>'requests.exceptions.ConnectTimeout' imported but unused", "ids": [308274, "a2276d299bc42a4e4aae61910e3d96cd3373cfac", "f7db84925d91bfbe4382fec29a58c268f37a8f2e"], "repo": "home-assistant-core", "ghid": 14085, "old": " from datetime import timedelta\n import voluptuous as vol\n-from requests.exceptions import HTTPError, ConnectTimeout\n from homeassistant.helpers import config_validation as cv\n from homeassistant.const import (", "new": " from datetime import timedelta\n import voluptuous as vol\n+from requests.exceptions import ConnectTimeout\n from homeassistant.helpers import config_validation as cv\n from homeassistant.const import (", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -0,0 +1,112 @@\n+\"\"\"\n+This component provides support for RainMachine sprinkler controllers.\n+\n+For more details about this component, please refer to the documentation at\n+https://home-assistant.io/components/rainmachine/\n+\"\"\"\n+import logging\n+from datetime import timedelta\n+\n+import voluptuous as vol\n+from requests.exceptions import HTTPError, ConnectTimeout", "msg": "'requests.exceptions.HTTPError' imported but unused<br>'requests.exceptions.ConnectTimeout' imported but unused"}
{"old_hunk": "@@ -1,130 +1,68 @@\n \"\"\"Implements a RainMachine sprinkler controller for Home Assistant.\"\"\"\n \n-from datetime import timedelta\n from logging import getLogger\n \n import voluptuous as vol\n \n import homeassistant.helpers.config_validation as cv\n-from homeassistant.components.switch import SwitchDevice\n-from homeassistant.const import (\n-    ATTR_ATTRIBUTION, ATTR_DEVICE_CLASS, CONF_EMAIL, CONF_IP_ADDRESS,\n-    CONF_PASSWORD, CONF_PLATFORM, CONF_PORT, CONF_SCAN_INTERVAL, CONF_SSL)\n-from homeassistant.util import Throttle\n+from homeassistant.components.rainmachine import (\n+    DATA_RAINMACHINE, aware_throttle)\n+from homeassistant.components.switch import PLATFORM_SCHEMA, SwitchDevice\n+from homeassistant.const import ATTR_ATTRIBUTION, ATTR_DEVICE_CLASS\n \n _LOGGER = getLogger(__name__)\n-REQUIREMENTS = ['regenmaschine==0.4.1']\n+DEPENDENCIES = ['rainmachine']\n \n ATTR_CYCLES = 'cycles'\n ATTR_TOTAL_DURATION = 'total_duration'\n \n CONF_ZONE_RUN_TIME = 'zone_run_time'\n \n-DEFAULT_PORT = 8080\n-DEFAULT_SSL = True\n DEFAULT_ZONE_RUN_SECONDS = 60 * 10\n \n-MIN_SCAN_TIME_LOCAL = timedelta(seconds=1)\n-MIN_SCAN_TIME_REMOTE = timedelta(seconds=5)\n-MIN_SCAN_TIME_FORCED = timedelta(milliseconds=100)\n-\n-PLATFORM_SCHEMA = vol.Schema(\n-    vol.All(\n-        cv.has_at_least_one_key(CONF_IP_ADDRESS, CONF_EMAIL),\n-        {\n-            vol.Required(CONF_PLATFORM): cv.string,\n-            vol.Optional(CONF_SCAN_INTERVAL): cv.time_period,\n-            vol.Exclusive(CONF_IP_ADDRESS, 'auth'): cv.string,\n-            vol.Exclusive(CONF_EMAIL, 'auth'):\n-                vol.Email(),  # pylint: disable=no-value-for-parameter\n-            vol.Required(CONF_PASSWORD): cv.string,\n-            vol.Optional(CONF_PORT, default=DEFAULT_PORT): cv.port,\n-            vol.Optional(CONF_SSL, default=DEFAULT_SSL): cv.boolean,\n-            vol.Optional(CONF_ZONE_RUN_TIME, default=DEFAULT_ZONE_RUN_SECONDS):\n-                cv.positive_int\n-        }),\n-    extra=vol.ALLOW_EXTRA)\n+PLATFORM_SCHEMA = PLATFORM_SCHEMA.extend({\n+    vol.Optional(CONF_ZONE_RUN_TIME, default=DEFAULT_ZONE_RUN_SECONDS):\n+        cv.positive_int\n+})\n \n \n def setup_platform(hass, config, add_devices, discovery_info=None):\n     \"\"\"Set this component up under its platform.\"\"\"\n-    import regenmaschine as rm\n+    client = hass.data.get(DATA_RAINMACHINE)\n+    device_name = client.provision.device_name()['name']\n+    device_mac = client.provision.wifi()['macAddress']\n \n-    _LOGGER.debug('Config data: %s', config)\n+    _LOGGER.debug('Config received: %s', config)\n \n-    ip_address = config.get(CONF_IP_ADDRESS, None)\n-    email_address = config.get(CONF_EMAIL, None)\n-    password = config[CONF_PASSWORD]\n     zone_run_time = config[CONF_ZONE_RUN_TIME]\n \n-    try:\n-        if ip_address:\n-            _LOGGER.debug('Configuring local API')\n+    entities = []\n+    for program in client.programs.all().get('programs', {}):\n+        if not program.get('active'):\n+            continue\n \n-            port = config[CONF_PORT]\n-            ssl = config[CONF_SSL]\n-            auth = rm.Authenticator.create_local(\n-                ip_address, password, port=port, https=ssl)\n-        elif email_address:\n-            _LOGGER.debug('Configuring remote API')\n-            auth = rm.Authenticator.create_remote(email_address, password)\n+        _LOGGER.debug('Adding program: %s', program)\n+        entities.append(\n+            RainMachineProgram(\n+                client,", "hunk": "@@ -43,11 +43,7 @@ def setup_platform(hass, config, add_devices, discovery_info=None):\n \n         _LOGGER.debug('Adding program: %s', program)\n         entities.append(\n-            RainMachineProgram(\n-                client,\n-                device_name,\n-                device_mac,\n-                program))\n+            RainMachineProgram(client, device_name, device_mac, program))\n \n     for zone in client.zones.all().get('zones', {}):\n         if not zone.get('active'):\n", "comment": "Please keep the existing formatting, by having as many parameters on one line as possible.", "ids": [308275, "f7db84925d91bfbe4382fec29a58c268f37a8f2e", "bebb949fe55459d0894ddb37c60d61c327d9e293"], "repo": "home-assistant-core", "ghid": 14085, "old": "         _LOGGER.debug('Adding program: %s', program)\n         entities.append(\n-            RainMachineProgram(\n-                client,\n-                device_name,\n-                device_mac,\n-                program))\n     for zone in client.zones.all().get('zones', {}):\n         if not zone.get('active'):", "new": "         _LOGGER.debug('Adding program: %s', program)\n         entities.append(\n+            RainMachineProgram(client, device_name, device_mac, program))\n     for zone in client.zones.all().get('zones', {}):\n         if not zone.get('active'):", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -1,130 +1,68 @@\n \"\"\"Implements a RainMachine sprinkler controller for Home Assistant.\"\"\"\n \n-from datetime import timedelta\n from logging import getLogger\n \n import voluptuous as vol\n \n import homeassistant.helpers.config_validation as cv\n-from homeassistant.components.switch import SwitchDevice\n-from homeassistant.const import (\n-    ATTR_ATTRIBUTION, ATTR_DEVICE_CLASS, CONF_EMAIL, CONF_IP_ADDRESS,\n-    CONF_PASSWORD, CONF_PLATFORM, CONF_PORT, CONF_SCAN_INTERVAL, CONF_SSL)\n-from homeassistant.util import Throttle\n+from homeassistant.components.rainmachine import (\n+    DATA_RAINMACHINE, aware_throttle)\n+from homeassistant.components.switch import PLATFORM_SCHEMA, SwitchDevice\n+from homeassistant.const import ATTR_ATTRIBUTION, ATTR_DEVICE_CLASS\n \n _LOGGER = getLogger(__name__)\n-REQUIREMENTS = ['regenmaschine==0.4.1']\n+DEPENDENCIES = ['rainmachine']\n \n ATTR_CYCLES = 'cycles'\n ATTR_TOTAL_DURATION = 'total_duration'\n \n CONF_ZONE_RUN_TIME = 'zone_run_time'\n \n-DEFAULT_PORT = 8080\n-DEFAULT_SSL = True\n DEFAULT_ZONE_RUN_SECONDS = 60 * 10\n \n-MIN_SCAN_TIME_LOCAL = timedelta(seconds=1)\n-MIN_SCAN_TIME_REMOTE = timedelta(seconds=5)\n-MIN_SCAN_TIME_FORCED = timedelta(milliseconds=100)\n-\n-PLATFORM_SCHEMA = vol.Schema(\n-    vol.All(\n-        cv.has_at_least_one_key(CONF_IP_ADDRESS, CONF_EMAIL),\n-        {\n-            vol.Required(CONF_PLATFORM): cv.string,\n-            vol.Optional(CONF_SCAN_INTERVAL): cv.time_period,\n-            vol.Exclusive(CONF_IP_ADDRESS, 'auth'): cv.string,\n-            vol.Exclusive(CONF_EMAIL, 'auth'):\n-                vol.Email(),  # pylint: disable=no-value-for-parameter\n-            vol.Required(CONF_PASSWORD): cv.string,\n-            vol.Optional(CONF_PORT, default=DEFAULT_PORT): cv.port,\n-            vol.Optional(CONF_SSL, default=DEFAULT_SSL): cv.boolean,\n-            vol.Optional(CONF_ZONE_RUN_TIME, default=DEFAULT_ZONE_RUN_SECONDS):\n-                cv.positive_int\n-        }),\n-    extra=vol.ALLOW_EXTRA)\n+PLATFORM_SCHEMA = PLATFORM_SCHEMA.extend({\n+    vol.Optional(CONF_ZONE_RUN_TIME, default=DEFAULT_ZONE_RUN_SECONDS):\n+        cv.positive_int\n+})\n \n \n def setup_platform(hass, config, add_devices, discovery_info=None):\n     \"\"\"Set this component up under its platform.\"\"\"\n-    import regenmaschine as rm\n+    client = hass.data.get(DATA_RAINMACHINE)\n+    device_name = client.provision.device_name()['name']\n+    device_mac = client.provision.wifi()['macAddress']\n \n-    _LOGGER.debug('Config data: %s', config)\n+    _LOGGER.debug('Config received: %s', config)\n \n-    ip_address = config.get(CONF_IP_ADDRESS, None)\n-    email_address = config.get(CONF_EMAIL, None)\n-    password = config[CONF_PASSWORD]\n     zone_run_time = config[CONF_ZONE_RUN_TIME]\n \n-    try:\n-        if ip_address:\n-            _LOGGER.debug('Configuring local API')\n+    entities = []\n+    for program in client.programs.all().get('programs', {}):\n+        if not program.get('active'):\n+            continue\n \n-            port = config[CONF_PORT]\n-            ssl = config[CONF_SSL]\n-            auth = rm.Authenticator.create_local(\n-                ip_address, password, port=port, https=ssl)\n-        elif email_address:\n-            _LOGGER.debug('Configuring remote API')\n-            auth = rm.Authenticator.create_remote(email_address, password)\n+        _LOGGER.debug('Adding program: %s', program)\n+        entities.append(\n+            RainMachineProgram(\n+                client,", "msg": "Please keep the existing formatting, by having as many parameters on one line as possible."}
{"old_hunk": "@@ -90,22 +90,48 @@\n MAX_RECONNECT_WAIT = 300  # seconds\n \n \n-def valid_subscribe_topic(value: Any, invalid_chars='\\0') -> str:\n-    \"\"\"Validate that we can subscribe using this MQTT topic.\"\"\"\n+def valid_topic(value: Any) -> str:\n+    \"\"\"Validate that this is a valid topic name/filter.\"\"\"\n     value = cv.string(value)\n-    if all(c not in value for c in invalid_chars):\n-        return vol.Length(min=1, max=65535)(value)\n-    raise vol.Invalid('Invalid MQTT topic name')\n+    try:\n+        raw_value = value.encode('utf-8')\n+    except UnicodeError:\n+        raise vol.Invalid(\"MQTT topic name/filter must be valid UTF-8 string.\")\n+    vol.Length(min=1, max=65535)(raw_value)", "hunk": "@@ -97,7 +97,11 @@ def valid_topic(value: Any) -> str:\n         raw_value = value.encode('utf-8')\n     except UnicodeError:\n         raise vol.Invalid(\"MQTT topic name/filter must be valid UTF-8 string.\")\n-    vol.Length(min=1, max=65535)(raw_value)\n+    if not raw_value:\n+        raise vol.Invalid(\"MQTT topic name/filter must not be empty.\")\n+    if len(raw_value) > 65535:\n+        raise vol.Invalid(\"MQTT topic name/filter must not be longer than \"\n+                          \"65535 encoded bytes.\")\n     if '\\0' in value:\n         raise vol.Invalid(\"MQTT topic name/filter must not contain null \"\n                           \"character.\")\n", "comment": "This feels weird. Rather have us just check the length and raise if it's outside of our boundaries.", "ids": [308332, "08e487508a23f5e3dafba9e52a518a544ded3f04", "5d62419716fa5a5b269df567cb1e4f98fa68f456"], "repo": "home-assistant-core", "ghid": 14099, "old": "         raw_value = value.encode('utf-8')\n     except UnicodeError:\n         raise vol.Invalid(\"MQTT topic name/filter must be valid UTF-8 string.\")\n-    vol.Length(min=1, max=65535)(raw_value)\n     if '\\0' in value:\n         raise vol.Invalid(\"MQTT topic name/filter must not contain null \"\n                           \"character.\")", "new": "         raw_value = value.encode('utf-8')\n     except UnicodeError:\n         raise vol.Invalid(\"MQTT topic name/filter must be valid UTF-8 string.\")\n+    if not raw_value:\n+        raise vol.Invalid(\"MQTT topic name/filter must not be empty.\")\n+    if len(raw_value) > 65535:\n+        raise vol.Invalid(\"MQTT topic name/filter must not be longer than \"\n+                          \"65535 encoded bytes.\")\n     if '\\0' in value:\n         raise vol.Invalid(\"MQTT topic name/filter must not contain null \"\n                           \"character.\")", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -90,22 +90,48 @@\n MAX_RECONNECT_WAIT = 300  # seconds\n \n \n-def valid_subscribe_topic(value: Any, invalid_chars='\\0') -> str:\n-    \"\"\"Validate that we can subscribe using this MQTT topic.\"\"\"\n+def valid_topic(value: Any) -> str:\n+    \"\"\"Validate that this is a valid topic name/filter.\"\"\"\n     value = cv.string(value)\n-    if all(c not in value for c in invalid_chars):\n-        return vol.Length(min=1, max=65535)(value)\n-    raise vol.Invalid('Invalid MQTT topic name')\n+    try:\n+        raw_value = value.encode('utf-8')\n+    except UnicodeError:\n+        raise vol.Invalid(\"MQTT topic name/filter must be valid UTF-8 string.\")\n+    vol.Length(min=1, max=65535)(raw_value)", "msg": "This feels weird. Rather have us just check the length and raise if it's outside of our boundaries."}
{"old_hunk": "@@ -100,7 +100,8 @@ class HomeAccessory(Accessory):\n     def __init__(self, hass, name, entity_id, aid, category):\n         \"\"\"Initialize a Accessory object.\"\"\"\n         super().__init__(name, aid=aid)\n-        set_accessory_info(self, name, model=entity_id)\n+        domain = entity_id.split(\".\")[0].replace(\"_\", \" \").title()", "hunk": "@@ -100,7 +100,7 @@ class HomeAccessory(Accessory):\n     def __init__(self, hass, name, entity_id, aid, category):\n         \"\"\"Initialize a Accessory object.\"\"\"\n         super().__init__(name, aid=aid)\n-        domain = entity_id.split(\".\")[0].replace(\"_\", \" \").title()\n+        domain = split_entity_id(entity_id)[0].replace(\"_\", \" \").title()\n         set_accessory_info(self, name, model=domain, serial_number=entity_id)\n         self.category = getattr(Category, category, Category.OTHER)\n         self.entity_id = entity_id\n", "comment": "Use `split_entity_id(entity_id)[0].replace(.....` from `homeassistant.core` instead.", "ids": [308351, "810ceb82485c0b5f53a19441b2126f633d19e53c", "549b1b693b572df906ca55d5f2b97b30a91a7847"], "repo": "home-assistant-core", "ghid": 14114, "old": "     def __init__(self, hass, name, entity_id, aid, category):\n         \"\"\"Initialize a Accessory object.\"\"\"\n         super().__init__(name, aid=aid)\n-        domain = entity_id.split(\".\")[0].replace(\"_\", \" \").title()\n         set_accessory_info(self, name, model=domain, serial_number=entity_id)\n         self.category = getattr(Category, category, Category.OTHER)\n         self.entity_id = entity_id", "new": "     def __init__(self, hass, name, entity_id, aid, category):\n         \"\"\"Initialize a Accessory object.\"\"\"\n         super().__init__(name, aid=aid)\n+        domain = split_entity_id(entity_id)[0].replace(\"_\", \" \").title()\n         set_accessory_info(self, name, model=domain, serial_number=entity_id)\n         self.category = getattr(Category, category, Category.OTHER)\n         self.entity_id = entity_id", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -100,7 +100,8 @@ class HomeAccessory(Accessory):\n     def __init__(self, hass, name, entity_id, aid, category):\n         \"\"\"Initialize a Accessory object.\"\"\"\n         super().__init__(name, aid=aid)\n-        set_accessory_info(self, name, model=entity_id)\n+        domain = entity_id.split(\".\")[0].replace(\"_\", \" \").title()", "msg": "Use `split_entity_id(entity_id)[0].replace(.....` from `homeassistant.core` instead."}
{"old_hunk": "@@ -127,3 +129,14 @@ def deconz_shutdown(event):\n \n     hass.bus.async_listen_once(EVENT_HOMEASSISTANT_STOP, deconz_shutdown)\n     return True\n+\n+\n+async def async_unload_entry(hass, config_entry):\n+    \"\"\"Unload deCONZ config entry.\"\"\"\n+    deconz = hass.data.pop(DOMAIN)\n+    hass.services.async_remove(DOMAIN, SERVICE_DECONZ)\n+    deconz.close()\n+    for component in ['binary_sensor', 'light', 'scene', 'sensor']:\n+        hass.async_add_job(hass.config_entries.async_forward_entry_unload(", "hunk": "@@ -137,6 +137,8 @@ async def async_unload_entry(hass, config_entry):\n     hass.services.async_remove(DOMAIN, SERVICE_DECONZ)\n     deconz.close()\n     for component in ['binary_sensor', 'light', 'scene', 'sensor']:\n-        hass.async_add_job(hass.config_entries.async_forward_entry_unload(\n-            config_entry, component))\n+        await hass.config_entries.async_forward_entry_unload(\n+            config_entry, component)\n+    for event in hass.data[DATA_DECONZ_EVENT]:\n+        del event\n     return True\n", "comment": "No reason to `async_add_job`, you can just await them.", "ids": [308358, "ae710c0a17097cb72f55b7cd8941f0b392429225", "50e766fc8bb863aa980fc1fef929ef27157cb494"], "repo": "home-assistant-core", "ghid": 14115, "old": "     hass.services.async_remove(DOMAIN, SERVICE_DECONZ)\n     deconz.close()\n     for component in ['binary_sensor', 'light', 'scene', 'sensor']:\n-        hass.async_add_job(hass.config_entries.async_forward_entry_unload(\n-            config_entry, component))\n     return True", "new": "     hass.services.async_remove(DOMAIN, SERVICE_DECONZ)\n     deconz.close()\n     for component in ['binary_sensor', 'light', 'scene', 'sensor']:\n+        await hass.config_entries.async_forward_entry_unload(\n+            config_entry, component)\n+    for event in hass.data[DATA_DECONZ_EVENT]:\n+        del event\n     return True", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -127,3 +129,14 @@ def deconz_shutdown(event):\n \n     hass.bus.async_listen_once(EVENT_HOMEASSISTANT_STOP, deconz_shutdown)\n     return True\n+\n+\n+async def async_unload_entry(hass, config_entry):\n+    \"\"\"Unload deCONZ config entry.\"\"\"\n+    deconz = hass.data.pop(DOMAIN)\n+    hass.services.async_remove(DOMAIN, SERVICE_DECONZ)\n+    deconz.close()\n+    for component in ['binary_sensor', 'light', 'scene', 'sensor']:\n+        hass.async_add_job(hass.config_entries.async_forward_entry_unload(", "msg": "No reason to `async_add_job`, you can just await them."}
{"old_hunk": "@@ -69,6 +71,8 @@ def stop_subscription(event):\n     base_url = config.get(CONF_CONTROLLER)\n     light_ids = config.get(CONF_LIGHTS)\n     exclude_ids = config.get(CONF_EXCLUDE)\n+    global unique_entities", "hunk": "@@ -71,7 +71,6 @@ def setup(hass, base_config):\n     base_url = config.get(CONF_CONTROLLER)\n     light_ids = config.get(CONF_LIGHTS)\n     exclude_ids = config.get(CONF_EXCLUDE)\n-    global unique_entities\n     unique_entities = config.get(CONF_UNIQUE_ENTITIES)\n \n     # Initialize the Vera controller.\n", "comment": "Global is not allowed. You can pass config in the discovery_info to the platforms and then pass that on when instantiating the entities.", "ids": [308376, "e5b3ccc885ff1d48dbdc3a52f76915fcda0c0981", "9fceb585329de86e1372316b9fc7f81aacfe27fc"], "repo": "home-assistant-core", "ghid": 14123, "old": "     base_url = config.get(CONF_CONTROLLER)\n     light_ids = config.get(CONF_LIGHTS)\n     exclude_ids = config.get(CONF_EXCLUDE)\n-    global unique_entities\n     unique_entities = config.get(CONF_UNIQUE_ENTITIES)\n     # Initialize the Vera controller.", "new": "     base_url = config.get(CONF_CONTROLLER)\n     light_ids = config.get(CONF_LIGHTS)\n     exclude_ids = config.get(CONF_EXCLUDE)\n     unique_entities = config.get(CONF_UNIQUE_ENTITIES)\n     # Initialize the Vera controller.", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -69,6 +71,8 @@ def stop_subscription(event):\n     base_url = config.get(CONF_CONTROLLER)\n     light_ids = config.get(CONF_LIGHTS)\n     exclude_ids = config.get(CONF_EXCLUDE)\n+    global unique_entities", "msg": "Global is not allowed. You can pass config in the discovery_info to the platforms and then pass that on when instantiating the entities."}
{"old_hunk": "@@ -114,6 +115,13 @@ def state_message_received(topic, payload, qos):\n                 self.hass, self._state_topic, state_message_received,\n                 self._qos)\n \n+        if self._optimistic:\n+            last_state = yield from async_get_last_state(self.hass,\n+                                                         self.entity_id)\n+            if last_state:\n+                self._state = last_state.state\n+                self.async_schedule_update_ha_state()", "hunk": "@@ -120,7 +120,6 @@ class MqttSwitch(MqttAvailability, SwitchDevice):\n                                                          self.entity_id)\n             if last_state:\n                 self._state = last_state.state\n-                self.async_schedule_update_ha_state()\n \n     @property\n     def should_poll(self):\n", "comment": "No need to schedule an update. We write the entity to the state machine right after this method is done.", "ids": [308518, "3c8a067b62707869e9b713df3ec09463af64e9a4", "f9489d64634f2c86bd1bea79b2b06e12b70f06b6"], "repo": "home-assistant-core", "ghid": 14151, "old": "                                                          self.entity_id)\n             if last_state:\n                 self._state = last_state.state\n-                self.async_schedule_update_ha_state()\n     @property\n     def should_poll(self):", "new": "                                                          self.entity_id)\n             if last_state:\n                 self._state = last_state.state\n     @property\n     def should_poll(self):", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -114,6 +115,13 @@ def state_message_received(topic, payload, qos):\n                 self.hass, self._state_topic, state_message_received,\n                 self._qos)\n \n+        if self._optimistic:\n+            last_state = yield from async_get_last_state(self.hass,\n+                                                         self.entity_id)\n+            if last_state:\n+                self._state = last_state.state\n+                self.async_schedule_update_ha_state()", "msg": "No need to schedule an update. We write the entity to the state machine right after this method is done."}
{"old_hunk": "@@ -42,7 +47,13 @@ def setup(hass, config):\n         'requires_api_password': requires_api_password,\n     }\n \n-    host_ip = util.get_local_ip()\n+    host_ip = config.get(CONF_LOCAL_IP)\n+    if host_ip is not None:\n+        host_ip = str(host_ip)", "hunk": "@@ -48,10 +48,11 @@ def setup(hass, config):\n     }\n \n     host_ip = config.get(CONF_LOCAL_IP)\n-    if host_ip is not None:\n-        host_ip = str(host_ip)\n-    else:\n+    if host_ip is None:\n         host_ip = util.get_local_ip()\n+        _LOGGER.info(\n+            \"Listen IP address not specified, auto-detected address is %s\",\n+            host_ip)\n \n     _LOGGER.debug(\"Using %s to annouce %s\", host_ip, ZEROCONF_TYPE)\n \n", "comment": "We should not do this here, we should do this in the config schema: `vol.All(ip_address, str)`", "ids": [308605, "b272d931c4445e7ed1dd3ea6fef0c06a48b01035", "babc5efee66fe8d633a6d3286c3b008c51fe128f"], "repo": "home-assistant-core", "ghid": 14173, "old": "     }\n     host_ip = config.get(CONF_LOCAL_IP)\n-    if host_ip is not None:\n-        host_ip = str(host_ip)\n-    else:\n         host_ip = util.get_local_ip()\n     _LOGGER.debug(\"Using %s to annouce %s\", host_ip, ZEROCONF_TYPE)", "new": "     }\n     host_ip = config.get(CONF_LOCAL_IP)\n+    if host_ip is None:\n         host_ip = util.get_local_ip()\n+        _LOGGER.info(\n+            \"Listen IP address not specified, auto-detected address is %s\",\n+            host_ip)\n     _LOGGER.debug(\"Using %s to annouce %s\", host_ip, ZEROCONF_TYPE)", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -42,7 +47,13 @@ def setup(hass, config):\n         'requires_api_password': requires_api_password,\n     }\n \n-    host_ip = util.get_local_ip()\n+    host_ip = config.get(CONF_LOCAL_IP)\n+    if host_ip is not None:\n+        host_ip = str(host_ip)", "msg": "We should not do this here, we should do this in the config schema: `vol.All(ip_address, str)`"}
{"old_hunk": "@@ -110,7 +110,8 @@ def state_attributes(self):\n             ATTR_WEATHER_TEMPERATURE: show_temp(\n                 self.hass, self.temperature, self.temperature_unit,\n                 self.precision),\n-            ATTR_WEATHER_HUMIDITY: round(self.humidity)\n+            ATTR_WEATHER_HUMIDITY: (round(self.humidity) if self.humidity", "hunk": "@@ -110,10 +110,12 @@ class WeatherEntity(Entity):\n             ATTR_WEATHER_TEMPERATURE: show_temp(\n                 self.hass, self.temperature, self.temperature_unit,\n                 self.precision),\n-            ATTR_WEATHER_HUMIDITY: (round(self.humidity) if self.humidity\n-                                    else None)\n         }\n \n+        humidity = self.humidity\n+        if humidity is not None:\n+            data[ATTR_WEATHER_HUMIDITY] = round(humidity)\n+\n         ozone = self.ozone\n         if ozone is not None:\n             data[ATTR_WEATHER_OZONE] = ozone\n", "comment": "The `if self.humidity` check will not work with falsy values like `0.0`. Write something like `if self.humidity is not None` or, even better, make it an additional check like the other attributes: ```python data = { ATTR_WEATHER_TEMPERATURE: show_temp( self.hass, self.temperature, self.temperature_unit, self.precision), } humidity = self.humidity if humidity is not None: data[ATTR_WEATHER_HUMIDITY] = humidity ```", "ids": [308612, "97165a33a2387fbe14cd1ae9841bfca5598603c7", "dadbd0d402cf05ce4b2cee4d3c401868038384c5"], "repo": "home-assistant-core", "ghid": 14174, "old": "             ATTR_WEATHER_TEMPERATURE: show_temp(\n                 self.hass, self.temperature, self.temperature_unit,\n                 self.precision),\n-            ATTR_WEATHER_HUMIDITY: (round(self.humidity) if self.humidity\n-                                    else None)\n         }\n         ozone = self.ozone\n         if ozone is not None:\n             data[ATTR_WEATHER_OZONE] = ozone", "new": "             ATTR_WEATHER_TEMPERATURE: show_temp(\n                 self.hass, self.temperature, self.temperature_unit,\n                 self.precision),\n         }\n+        humidity = self.humidity\n+        if humidity is not None:\n+            data[ATTR_WEATHER_HUMIDITY] = round(humidity)\n+\n         ozone = self.ozone\n         if ozone is not None:\n             data[ATTR_WEATHER_OZONE] = ozone", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -110,7 +110,8 @@ def state_attributes(self):\n             ATTR_WEATHER_TEMPERATURE: show_temp(\n                 self.hass, self.temperature, self.temperature_unit,\n                 self.precision),\n-            ATTR_WEATHER_HUMIDITY: round(self.humidity)\n+            ATTR_WEATHER_HUMIDITY: (round(self.humidity) if self.humidity", "msg": "The `if self.humidity` check will not work with falsy values like `0.0`. Write something like `if self.humidity is not None` or, even better, make it an additional check like the other attributes: ```python data = { ATTR_WEATHER_TEMPERATURE: show_temp( self.hass, self.temperature, self.temperature_unit, self.precision), } humidity = self.humidity if humidity is not None: data[ATTR_WEATHER_HUMIDITY] = humidity ```"}
{"old_hunk": "@@ -62,25 +61,22 @@\n })\n \n \n-def setup_platform(hass, config, add_devices, discovery_info=None):\n+async def async_setup_platform(hass, config, async_add_devices,\n+                               discovery_info=None):\n     \"\"\"Set up the EBox sensor.\"\"\"\n     username = config.get(CONF_USERNAME)\n     password = config.get(CONF_PASSWORD)\n \n-    try:\n-        ebox_data = EBoxData(username, password)\n-        ebox_data.update()\n-    except requests.exceptions.HTTPError as error:\n-        _LOGGER.error(\"Failed login: %s\", error)\n-        return False\n+    httpsession = hass.helpers.aiohttp_client.async_get_clientsession()\n+    ebox_data = EBoxData(username, password, httpsession)", "hunk": "@@ -70,6 +70,10 @@ async def async_setup_platform(hass, config, async_add_devices,\n     httpsession = hass.helpers.aiohttp_client.async_get_clientsession()\n     ebox_data = EBoxData(username, password, httpsession)\n \n+    if not await ebox_data.async_update():\n+        _LOGGER.error(\"Failed to fetch data\")\n+        return\n+\n     name = config.get(CONF_NAME)\n \n     sensors = []\n", "comment": "Are you no longer validating that the user/pass are correct?", "ids": [308644, "0c712d002d69666d74abd3cb1c4abf52455f8619", "9038413ccb6b3ad28e375f6296f4ee0bc93034b7"], "repo": "home-assistant-core", "ghid": 14183, "old": "     httpsession = hass.helpers.aiohttp_client.async_get_clientsession()\n     ebox_data = EBoxData(username, password, httpsession)\n     name = config.get(CONF_NAME)\n     sensors = []", "new": "     httpsession = hass.helpers.aiohttp_client.async_get_clientsession()\n     ebox_data = EBoxData(username, password, httpsession)\n+    if not await ebox_data.async_update():\n+        _LOGGER.error(\"Failed to fetch data\")\n+        return\n+\n     name = config.get(CONF_NAME)\n     sensors = []", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -62,25 +61,22 @@\n })\n \n \n-def setup_platform(hass, config, add_devices, discovery_info=None):\n+async def async_setup_platform(hass, config, async_add_devices,\n+                               discovery_info=None):\n     \"\"\"Set up the EBox sensor.\"\"\"\n     username = config.get(CONF_USERNAME)\n     password = config.get(CONF_PASSWORD)\n \n-    try:\n-        ebox_data = EBoxData(username, password)\n-        ebox_data.update()\n-    except requests.exceptions.HTTPError as error:\n-        _LOGGER.error(\"Failed login: %s\", error)\n-        return False\n+    httpsession = hass.helpers.aiohttp_client.async_get_clientsession()\n+    ebox_data = EBoxData(username, password, httpsession)", "msg": "Are you no longer validating that the user/pass are correct?"}
{"old_hunk": "@@ -32,7 +31,7 @@\n DEFAULT_NAME = 'EBox'\n \n REQUESTS_TIMEOUT = 15\n-SCAN_INTERVAL = timedelta(minutes=5)\n+MIN_TIME_BETWEEN_UPDATES = timedelta(minutes=15)", "hunk": "@@ -31,6 +31,7 @@ PERCENT = '%'  # type: str\n DEFAULT_NAME = 'EBox'\n \n REQUESTS_TIMEOUT = 15\n+SCAN_INTERVAL = timedelta(minutes=15)\n MIN_TIME_BETWEEN_UPDATES = timedelta(minutes=15)\n \n SENSOR_TYPES = {\n", "comment": "Why change this from SCAN_INTERVAL? With SCAN_INTERVAL, Home Assistant will make sure to not call update more than once every 15 minutes per entity.", "ids": [308643, "0c712d002d69666d74abd3cb1c4abf52455f8619", "eba35118819e0929e9edace3b5904303a332be20"], "repo": "home-assistant-core", "ghid": 14183, "old": " DEFAULT_NAME = 'EBox'\n REQUESTS_TIMEOUT = 15\n MIN_TIME_BETWEEN_UPDATES = timedelta(minutes=15)\n SENSOR_TYPES = {", "new": " DEFAULT_NAME = 'EBox'\n REQUESTS_TIMEOUT = 15\n+SCAN_INTERVAL = timedelta(minutes=15)\n MIN_TIME_BETWEEN_UPDATES = timedelta(minutes=15)\n SENSOR_TYPES = {", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -32,7 +31,7 @@\n DEFAULT_NAME = 'EBox'\n \n REQUESTS_TIMEOUT = 15\n-SCAN_INTERVAL = timedelta(minutes=5)\n+MIN_TIME_BETWEEN_UPDATES = timedelta(minutes=15)", "msg": "Why change this from SCAN_INTERVAL? With SCAN_INTERVAL, Home Assistant will make sure to not call update more than once every 15 minutes per entity."}
{"old_hunk": "@@ -116,28 +114,33 @@ def icon(self):\n         \"\"\"Icon to use in the frontend, if any.\"\"\"\n         return self._icon\n \n-    def update(self):\n+    @asyncio.coroutine\n+    def async_update(self):\n         \"\"\"Get the latest data from EBox and update the state.\"\"\"\n-        self.ebox_data.update()\n+        yield from self.ebox_data.async_update()\n         if self.type in self.ebox_data.data:\n             self._state = round(self.ebox_data.data[self.type], 2)\n \n \n class EBoxData(object):\n     \"\"\"Get data from Ebox.\"\"\"\n \n-    def __init__(self, username, password):\n+    def __init__(self, username, password, httpsession):\n         \"\"\"Initialize the data object.\"\"\"\n         from pyebox import EboxClient\n-        self.client = EboxClient(username, password, REQUESTS_TIMEOUT)\n+        self.client = EboxClient(username, password,\n+                                 REQUESTS_TIMEOUT, httpsession)\n         self.data = {}\n \n-    def update(self):\n+    @Throttle(MIN_TIME_BETWEEN_UPDATES)\n+    async def async_update(self):\n         \"\"\"Get the latest data from Ebox.\"\"\"\n         from pyebox.client import PyEboxError\n         try:\n-            self.client.fetch_data()\n+            await self.client.fetch_data()\n         except PyEboxError as exp:\n             _LOGGER.error(\"Error on receive last EBox data: %s\", exp)\n-            return\n+            return False", "hunk": "@@ -114,10 +114,9 @@ class EBoxSensor(Entity):\n         \"\"\"Icon to use in the frontend, if any.\"\"\"\n         return self._icon\n \n-    @asyncio.coroutine\n-    def async_update(self):\n+    async def async_update(self):\n         \"\"\"Get the latest data from EBox and update the state.\"\"\"\n-        yield from self.ebox_data.async_update()\n+        await self.ebox_data.async_update()\n         if self.type in self.ebox_data.data:\n             self._state = round(self.ebox_data.data[self.type], 2)\n \n", "comment": "Nothin is checking the return value. Please just write `return` (also below)", "ids": [308638, "5e7b5eb7c9f950ce6274374fd1886f8debf15694", "0c712d002d69666d74abd3cb1c4abf52455f8619"], "repo": "home-assistant-core", "ghid": 14183, "old": "         \"\"\"Icon to use in the frontend, if any.\"\"\"\n         return self._icon\n-    @asyncio.coroutine\n-    def async_update(self):\n         \"\"\"Get the latest data from EBox and update the state.\"\"\"\n-        yield from self.ebox_data.async_update()\n         if self.type in self.ebox_data.data:\n             self._state = round(self.ebox_data.data[self.type], 2)", "new": "         \"\"\"Icon to use in the frontend, if any.\"\"\"\n         return self._icon\n+    async def async_update(self):\n         \"\"\"Get the latest data from EBox and update the state.\"\"\"\n+        await self.ebox_data.async_update()\n         if self.type in self.ebox_data.data:\n             self._state = round(self.ebox_data.data[self.type], 2)", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -116,28 +114,33 @@ def icon(self):\n         \"\"\"Icon to use in the frontend, if any.\"\"\"\n         return self._icon\n \n-    def update(self):\n+    @asyncio.coroutine\n+    def async_update(self):\n         \"\"\"Get the latest data from EBox and update the state.\"\"\"\n-        self.ebox_data.update()\n+        yield from self.ebox_data.async_update()\n         if self.type in self.ebox_data.data:\n             self._state = round(self.ebox_data.data[self.type], 2)\n \n \n class EBoxData(object):\n     \"\"\"Get data from Ebox.\"\"\"\n \n-    def __init__(self, username, password):\n+    def __init__(self, username, password, httpsession):\n         \"\"\"Initialize the data object.\"\"\"\n         from pyebox import EboxClient\n-        self.client = EboxClient(username, password, REQUESTS_TIMEOUT)\n+        self.client = EboxClient(username, password,\n+                                 REQUESTS_TIMEOUT, httpsession)\n         self.data = {}\n \n-    def update(self):\n+    @Throttle(MIN_TIME_BETWEEN_UPDATES)\n+    async def async_update(self):\n         \"\"\"Get the latest data from Ebox.\"\"\"\n         from pyebox.client import PyEboxError\n         try:\n-            self.client.fetch_data()\n+            await self.client.fetch_data()\n         except PyEboxError as exp:\n             _LOGGER.error(\"Error on receive last EBox data: %s\", exp)\n-            return\n+            return False", "msg": "Nothin is checking the return value. Please just write `return` (also below)"}
{"old_hunk": "@@ -95,6 +96,14 @@ def __init__(self, node, network):\n         dispatcher.connect(\n             self.network_scene_activated, ZWaveNetwork.SIGNAL_SCENE_EVENT)\n \n+    @property\n+    def unique_id(self):\n+        \"\"\"Unique ID of Z-wave node.\"\"\"\n+        return '{}-{}-{}'.format(\n+            self.node_id,\n+            slugify(self._manufacturer_name),", "hunk": "@@ -99,10 +99,7 @@ class ZWaveNodeEntity(ZWaveBaseEntity):\n     @property\n     def unique_id(self):\n         \"\"\"Unique ID of Z-wave node.\"\"\"\n-        return '{}-{}-{}'.format(\n-            self.node_id,\n-            slugify(self._manufacturer_name),\n-            slugify(self._product_name))\n+        return self._unique_id\n \n     def network_node_changed(self, node=None, value=None, args=None):\n         \"\"\"Handle a changed node on the network.\"\"\"\n", "comment": "Don't manufacturer name and product name need to be fetched and thus it might be that a device gets a new unique ID after restart? Also, why wouldn't node id be enough ? And what about the devices like the 5-in-1 sensor, do they share a node id ?", "ids": [308758, "f15693b769f002e40ba7b3b7c5f1ef2fccbf515a", "0b01e3742da8c95a8e52f53d64061d566d959f39"], "repo": "home-assistant-core", "ghid": 14201, "old": "     @property\n     def unique_id(self):\n         \"\"\"Unique ID of Z-wave node.\"\"\"\n-        return '{}-{}-{}'.format(\n-            self.node_id,\n-            slugify(self._manufacturer_name),\n-            slugify(self._product_name))\n     def network_node_changed(self, node=None, value=None, args=None):\n         \"\"\"Handle a changed node on the network.\"\"\"", "new": "     @property\n     def unique_id(self):\n         \"\"\"Unique ID of Z-wave node.\"\"\"\n+        return self._unique_id\n     def network_node_changed(self, node=None, value=None, args=None):\n         \"\"\"Handle a changed node on the network.\"\"\"", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -95,6 +96,14 @@ def __init__(self, node, network):\n         dispatcher.connect(\n             self.network_scene_activated, ZWaveNetwork.SIGNAL_SCENE_EVENT)\n \n+    @property\n+    def unique_id(self):\n+        \"\"\"Unique ID of Z-wave node.\"\"\"\n+        return '{}-{}-{}'.format(\n+            self.node_id,\n+            slugify(self._manufacturer_name),", "msg": "Don't manufacturer name and product name need to be fetched and thus it might be that a device gets a new unique ID after restart? Also, why wouldn't node id be enough ? And what about the devices like the 5-in-1 sensor, do they share a node id ?"}
{"old_hunk": "@@ -75,8 +77,14 @@\n             \"Config entry failed since one deCONZ instance already exists\")\n         return False\n \n+    @callback\n+    def async_add_device_callback(device_type, device):\n+        \"\"\"Called when a new device has been created in deCONZ.\"\"\"\n+        async_dispatcher_send(hass, DOMAIN + '_new_' + device_type, device)", "hunk": "@@ -80,7 +80,8 @@ async def async_setup_entry(hass, config_entry):\n     @callback\n     def async_add_device_callback(device_type, device):\n         \"\"\"Called when a new device has been created in deCONZ.\"\"\"\n-        async_dispatcher_send(hass, DOMAIN + '_new_' + device_type, device)\n+        async_dispatcher_send(\n+            hass, 'deconz_new_{}'.format(device_type), device)\n \n     session = aiohttp_client.async_get_clientsession(hass)\n     deconz = DeconzSession(hass.loop, session, **config_entry.data,\n", "comment": "Don't use `+` for string concatenation, always use the string format function `\"{}_new_{}\".format(DOMAIN, device_type)`. Also, why not hardcode DOMAIN   ?", "ids": [308854, "45156ff9a332b0d34285536b46b471aeed01a094", "0057b99a624bc03b87acde0257f55a11acd4fc79"], "repo": "home-assistant-core", "ghid": 14221, "old": "     @callback\n     def async_add_device_callback(device_type, device):\n         \"\"\"Called when a new device has been created in deCONZ.\"\"\"\n-        async_dispatcher_send(hass, DOMAIN + '_new_' + device_type, device)\n     session = aiohttp_client.async_get_clientsession(hass)\n     deconz = DeconzSession(hass.loop, session, **config_entry.data,", "new": "     @callback\n     def async_add_device_callback(device_type, device):\n         \"\"\"Called when a new device has been created in deCONZ.\"\"\"\n+        async_dispatcher_send(\n+            hass, 'deconz_new_{}'.format(device_type), device)\n     session = aiohttp_client.async_get_clientsession(hass)\n     deconz = DeconzSession(hass.loop, session, **config_entry.data,", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -75,8 +77,14 @@\n             \"Config entry failed since one deCONZ instance already exists\")\n         return False\n \n+    @callback\n+    def async_add_device_callback(device_type, device):\n+        \"\"\"Called when a new device has been created in deCONZ.\"\"\"\n+        async_dispatcher_send(hass, DOMAIN + '_new_' + device_type, device)", "msg": "Don't use `+` for string concatenation, always use the string format function `\"{}_new_{}\".format(DOMAIN, device_type)`. Also, why not hardcode DOMAIN   ?"}
{"old_hunk": "@@ -26,10 +27,17 @@\n     entities = []\n \n     for sensor in sensors.values():\n-        if sensor and sensor.type in DECONZ_BINARY_SENSOR:\n+        if sensor.type in DECONZ_BINARY_SENSOR:\n             entities.append(DeconzBinarySensor(sensor))\n     async_add_devices(entities, True)\n \n+    async def async_new_sensor(sensor):", "hunk": "@@ -31,12 +31,13 @@ async def async_setup_entry(hass, config_entry, async_add_devices):\n             entities.append(DeconzBinarySensor(sensor))\n     async_add_devices(entities, True)\n \n-    async def async_new_sensor(sensor):\n+    @callback\n+    def async_new_sensor(sensor):\n         \"\"\"Called when a new sensor device has been added to deCONZ.\"\"\"\n         if sensor.type in DECONZ_BINARY_SENSOR:\n             async_add_devices(DeconzBinarySensor(sensor), True)\n     async_dispatcher_connect(\n-        hass, DATA_DECONZ + '_new_sensor', async_new_sensor)\n+        hass, 'deconz_new_sensor', async_new_sensor)\n \n \n class DeconzBinarySensor(BinarySensorDevice):\n", "comment": "No need to make `async`, just make it a normal function and mark it as `@callback`", "ids": [308853, "45156ff9a332b0d34285536b46b471aeed01a094", "0057b99a624bc03b87acde0257f55a11acd4fc79"], "repo": "home-assistant-core", "ghid": 14221, "old": "             entities.append(DeconzBinarySensor(sensor))\n     async_add_devices(entities, True)\n-    async def async_new_sensor(sensor):\n         \"\"\"Called when a new sensor device has been added to deCONZ.\"\"\"\n         if sensor.type in DECONZ_BINARY_SENSOR:\n             async_add_devices(DeconzBinarySensor(sensor), True)\n     async_dispatcher_connect(\n-        hass, DATA_DECONZ + '_new_sensor', async_new_sensor)\n class DeconzBinarySensor(BinarySensorDevice):", "new": "             entities.append(DeconzBinarySensor(sensor))\n     async_add_devices(entities, True)\n+    @callback\n+    def async_new_sensor(sensor):\n         \"\"\"Called when a new sensor device has been added to deCONZ.\"\"\"\n         if sensor.type in DECONZ_BINARY_SENSOR:\n             async_add_devices(DeconzBinarySensor(sensor), True)\n     async_dispatcher_connect(\n+        hass, 'deconz_new_sensor', async_new_sensor)\n class DeconzBinarySensor(BinarySensorDevice):", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -26,10 +27,17 @@\n     entities = []\n \n     for sensor in sensors.values():\n-        if sensor and sensor.type in DECONZ_BINARY_SENSOR:\n+        if sensor.type in DECONZ_BINARY_SENSOR:\n             entities.append(DeconzBinarySensor(sensor))\n     async_add_devices(entities, True)\n \n+    async def async_new_sensor(sensor):", "msg": "No need to make `async`, just make it a normal function and mark it as `@callback`"}
{"old_hunk": "@@ -75,6 +75,7 @@ def __init__(self, node, network):\n         super().__init__()\n         from openzwave.network import ZWaveNetwork\n         from pydispatch import dispatcher\n+        self.component = None", "hunk": "@@ -75,7 +75,6 @@ class ZWaveNodeEntity(ZWaveBaseEntity):\n         super().__init__()\n         from openzwave.network import ZWaveNetwork\n         from pydispatch import dispatcher\n-        self.component = None\n         self._network = network\n         self.node = node\n         self.node_id = self.node.node_id\n", "comment": "This is not necessary. Entities already have access to the platform that contains them at `self.platform`.", "ids": [308937, "5740f3d6949ef12a12f086f42611f5438b9f41c4", "fd42d073ca06d5843b535c94c5a6803c141e6cd2"], "repo": "home-assistant-core", "ghid": 14251, "old": "         super().__init__()\n         from openzwave.network import ZWaveNetwork\n         from pydispatch import dispatcher\n-        self.component = None\n         self._network = network\n         self.node = node\n         self.node_id = self.node.node_id", "new": "         super().__init__()\n         from openzwave.network import ZWaveNetwork\n         from pydispatch import dispatcher\n         self._network = network\n         self.node = node\n         self.node_id = self.node.node_id", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -75,6 +75,7 @@ def __init__(self, node, network):\n         super().__init__()\n         from openzwave.network import ZWaveNetwork\n         from pydispatch import dispatcher\n+        self.component = None", "msg": "This is not necessary. Entities already have access to the platform that contains them at `self.platform`."}
{"old_hunk": "@@ -61,7 +61,7 @@ def get_device(node, values, node_config, **kwargs):\n def brightness_state(value):\n     \"\"\"Return the brightness and state.\"\"\"\n     if value.data > 0:\n-        return round((value.data / 99) * 255, 0), STATE_ON\n+        return int((value.data / 99) * 255), STATE_ON", "hunk": "@@ -61,7 +61,7 @@ def get_device(node, values, node_config, **kwargs):\n def brightness_state(value):\n     \"\"\"Return the brightness and state.\"\"\"\n     if value.data > 0:\n-        return int((value.data / 99) * 255), STATE_ON\n+        return round((value.data / 99) * 255), STATE_ON\n     return 0, STATE_OFF\n \n \n", "comment": "use `round((value.data / 99) * 255)` instead. It will return an it but will round instead of truncate", "ids": [308969, "dbbbdd64e3f1cc55871d50b5d26f6ddd2f1723c9", "3b044ed4ce42f1d157567d525f7b4afa1481b0ab"], "repo": "home-assistant-core", "ghid": 14261, "old": " def brightness_state(value):\n     \"\"\"Return the brightness and state.\"\"\"\n     if value.data > 0:\n-        return int((value.data / 99) * 255), STATE_ON\n     return 0, STATE_OFF", "new": " def brightness_state(value):\n     \"\"\"Return the brightness and state.\"\"\"\n     if value.data > 0:\n+        return round((value.data / 99) * 255), STATE_ON\n     return 0, STATE_OFF", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -61,7 +61,7 @@ def get_device(node, values, node_config, **kwargs):\n def brightness_state(value):\n     \"\"\"Return the brightness and state.\"\"\"\n     if value.data > 0:\n-        return round((value.data / 99) * 255, 0), STATE_ON\n+        return int((value.data / 99) * 255), STATE_ON", "msg": "use `round((value.data / 99) * 255)` instead. It will return an it but will round instead of truncate"}
{"old_hunk": "@@ -92,6 +93,13 @@ def supported_features(self):\n         \"\"\"Flag supported features.\"\"\"\n         return SUPPORT_OPEN | SUPPORT_CLOSE\n \n+    @property\n+    def state_attributes(self):\n+        \"\"\"Return the state attributes.\"\"\"\n+        data = super().state_attributes\n+        data[ATTR_TEMPERATURE] = self._temperature", "hunk": "@@ -93,13 +93,6 @@ class MyGogogate2Device(CoverDevice):\n         \"\"\"Flag supported features.\"\"\"\n         return SUPPORT_OPEN | SUPPORT_CLOSE\n \n-    @property\n-    def state_attributes(self):\n-        \"\"\"Return the state attributes.\"\"\"\n-        data = super().state_attributes\n-        data[ATTR_TEMPERATURE] = self._temperature\n-        return data\n-\n     @property\n     def available(self):\n         \"\"\"Could the device be accessed during the last update call.\"\"\"\n", "comment": "This should not be added as an attribute but instead be made into it's own sensor. This means that you will need to greate a gogogate2 component to handle the connection and then setup the cover and sensor platforms.", "ids": [309195, "75fcf07b00886674cb7da45cc3e76e7c11d72ae1", "7635b6fab37146c7b2f9299356a2cbc768cde8ad"], "repo": "home-assistant-core", "ghid": 14294, "old": "         \"\"\"Flag supported features.\"\"\"\n         return SUPPORT_OPEN | SUPPORT_CLOSE\n-    @property\n-    def state_attributes(self):\n-        \"\"\"Return the state attributes.\"\"\"\n-        data = super().state_attributes\n-        data[ATTR_TEMPERATURE] = self._temperature\n-        return data\n-\n     @property\n     def available(self):\n         \"\"\"Could the device be accessed during the last update call.\"\"\"", "new": "         \"\"\"Flag supported features.\"\"\"\n         return SUPPORT_OPEN | SUPPORT_CLOSE\n     @property\n     def available(self):\n         \"\"\"Could the device be accessed during the last update call.\"\"\"", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -92,6 +93,13 @@ def supported_features(self):\n         \"\"\"Flag supported features.\"\"\"\n         return SUPPORT_OPEN | SUPPORT_CLOSE\n \n+    @property\n+    def state_attributes(self):\n+        \"\"\"Return the state attributes.\"\"\"\n+        data = super().state_attributes\n+        data[ATTR_TEMPERATURE] = self._temperature", "msg": "This should not be added as an attribute but instead be made into it's own sensor. This means that you will need to greate a gogogate2 component to handle the connection and then setup the cover and sensor platforms."}
{"old_hunk": "@@ -133,3 +145,90 @@ def async_update(self):\n \n         # process image data\n         yield from self.async_process_image(image.content)\n+\n+\n+class ImageProcessingFaceEntity(ImageProcessingEntity):\n+    \"\"\"Base entity class for face image processing.\"\"\"\n+\n+    def __init__(self):\n+        \"\"\"Initialize base face identify/verify entity.\"\"\"\n+        self.faces = []\n+        self.total_faces = 0\n+\n+    @property\n+    def state(self):\n+        \"\"\"Return the state of the entity.\"\"\"\n+        confidence = 0\n+        state = STATE_UNKNOWN", "hunk": "@@ -159,7 +159,7 @@ class ImageProcessingFaceEntity(ImageProcessingEntity):\n     def state(self):\n         \"\"\"Return the state of the entity.\"\"\"\n         confidence = 0\n-        state = STATE_UNKNOWN\n+        state = None\n \n         # No confidence support\n         if not self.confidence:\n", "comment": "Use `None` for unknown state.", "ids": [309222, "7bdec335bbaf00c43e38c10283a9022c74b65be0", "8cc8457c1715248a9ef7ce3e8e7bd84e586f1a41"], "repo": "home-assistant-core", "ghid": 14296, "old": "     def state(self):\n         \"\"\"Return the state of the entity.\"\"\"\n         confidence = 0\n-        state = STATE_UNKNOWN\n         # No confidence support\n         if not self.confidence:", "new": "     def state(self):\n         \"\"\"Return the state of the entity.\"\"\"\n         confidence = 0\n+        state = None\n         # No confidence support\n         if not self.confidence:", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -133,3 +145,90 @@ def async_update(self):\n \n         # process image data\n         yield from self.async_process_image(image.content)\n+\n+\n+class ImageProcessingFaceEntity(ImageProcessingEntity):\n+    \"\"\"Base entity class for face image processing.\"\"\"\n+\n+    def __init__(self):\n+        \"\"\"Initialize base face identify/verify entity.\"\"\"\n+        self.faces = []\n+        self.total_faces = 0\n+\n+    @property\n+    def state(self):\n+        \"\"\"Return the state of the entity.\"\"\"\n+        confidence = 0\n+        state = STATE_UNKNOWN", "msg": "Use `None` for unknown state."}
{"old_hunk": "@@ -17,13 +17,18 @@\n from homeassistant.util import color as color_util\n from homeassistant.util.color import \\\n     color_temperature_mired_to_kelvin as mired_to_kelvin\n+from homeassistant.util.json import load_json, save_json\n \n REQUIREMENTS = ['nanoleaf==0.4.1']\n \n _LOGGER = logging.getLogger(__name__)\n \n DEFAULT_NAME = 'Aurora'\n \n+DATA_NANOLEAF_AURORA = 'nanoleaf_aurora'\n+\n+CONFIG_FILE = 'nanoleaf_aurora.conf'", "hunk": "@@ -27,7 +27,7 @@ DEFAULT_NAME = 'Aurora'\n \n DATA_NANOLEAF_AURORA = 'nanoleaf_aurora'\n \n-CONFIG_FILE = 'nanoleaf_aurora.conf'\n+CONFIG_FILE = '.nanoleaf_aurora.conf'\n \n ICON = 'mdi:triangle-outline'\n \n", "comment": "If the config file is not supposed to be edited by users, we want it to be hidden, ie start with a dot.", "ids": [309250, "59257394f9d13e8290041d2f3470541ea814722f", "1cb9f7ad0faa850690d1a1b1259efc6e76e87a42"], "repo": "home-assistant-core", "ghid": 14301, "old": " DATA_NANOLEAF_AURORA = 'nanoleaf_aurora'\n-CONFIG_FILE = 'nanoleaf_aurora.conf'\n ICON = 'mdi:triangle-outline'", "new": " DATA_NANOLEAF_AURORA = 'nanoleaf_aurora'\n+CONFIG_FILE = '.nanoleaf_aurora.conf'\n ICON = 'mdi:triangle-outline'", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -17,13 +17,18 @@\n from homeassistant.util import color as color_util\n from homeassistant.util.color import \\\n     color_temperature_mired_to_kelvin as mired_to_kelvin\n+from homeassistant.util.json import load_json, save_json\n \n REQUIREMENTS = ['nanoleaf==0.4.1']\n \n _LOGGER = logging.getLogger(__name__)\n \n DEFAULT_NAME = 'Aurora'\n \n+DATA_NANOLEAF_AURORA = 'nanoleaf_aurora'\n+\n+CONFIG_FILE = 'nanoleaf_aurora.conf'", "msg": "If the config file is not supposed to be edited by users, we want it to be hidden, ie start with a dot."}
{"old_hunk": "@@ -37,6 +37,8 @@\n CONF_COLOR_TEMP_COMMAND_TOPIC = 'color_temp_command_topic'\n CONF_COLOR_TEMP_STATE_TOPIC = 'color_temp_state_topic'\n CONF_COLOR_TEMP_VALUE_TEMPLATE = 'color_temp_value_template'\n+CONF_COLOR_TEMP_MIREDS_MIN = 'color_temp_mireds_min'\n+CONF_COLOR_TEMP_MIREDS_MAX = 'color_temp_mireds_max'", "hunk": "@@ -37,8 +37,8 @@ CONF_BRIGHTNESS_VALUE_TEMPLATE = 'brightness_value_template'\n CONF_COLOR_TEMP_COMMAND_TOPIC = 'color_temp_command_topic'\n CONF_COLOR_TEMP_STATE_TOPIC = 'color_temp_state_topic'\n CONF_COLOR_TEMP_VALUE_TEMPLATE = 'color_temp_value_template'\n-CONF_COLOR_TEMP_MIREDS_MIN = 'color_temp_mireds_min'\n-CONF_COLOR_TEMP_MIREDS_MAX = 'color_temp_mireds_max'\n+CONF_COLOR_TEMP_MIN_MIREDS = 'color_temp_min_mireds'\n+CONF_COLOR_TEMP_MAX_MIREDS = 'color_temp_max_mireds'\n CONF_EFFECT_COMMAND_TOPIC = 'effect_command_topic'\n CONF_EFFECT_LIST = 'effect_list'\n CONF_EFFECT_STATE_TOPIC = 'effect_state_topic'\n", "comment": "I think we should call these options `color_temp_min_mireds` and `color_temp_max_mireds` as those are the names used throughout Home Assistant AFAIK.", "ids": [309308, "14856c41ebcfdffa93d0ff7b76cca751f0745abd", "7b477e8d9fe8b1ddb25785d97c89a3056ceb236c"], "repo": "home-assistant-core", "ghid": 14308, "old": " CONF_COLOR_TEMP_COMMAND_TOPIC = 'color_temp_command_topic'\n CONF_COLOR_TEMP_STATE_TOPIC = 'color_temp_state_topic'\n CONF_COLOR_TEMP_VALUE_TEMPLATE = 'color_temp_value_template'\n-CONF_COLOR_TEMP_MIREDS_MIN = 'color_temp_mireds_min'\n-CONF_COLOR_TEMP_MIREDS_MAX = 'color_temp_mireds_max'\n CONF_EFFECT_COMMAND_TOPIC = 'effect_command_topic'\n CONF_EFFECT_LIST = 'effect_list'\n CONF_EFFECT_STATE_TOPIC = 'effect_state_topic'", "new": " CONF_COLOR_TEMP_COMMAND_TOPIC = 'color_temp_command_topic'\n CONF_COLOR_TEMP_STATE_TOPIC = 'color_temp_state_topic'\n CONF_COLOR_TEMP_VALUE_TEMPLATE = 'color_temp_value_template'\n+CONF_COLOR_TEMP_MIN_MIREDS = 'color_temp_min_mireds'\n+CONF_COLOR_TEMP_MAX_MIREDS = 'color_temp_max_mireds'\n CONF_EFFECT_COMMAND_TOPIC = 'effect_command_topic'\n CONF_EFFECT_LIST = 'effect_list'\n CONF_EFFECT_STATE_TOPIC = 'effect_state_topic'", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -37,6 +37,8 @@\n CONF_COLOR_TEMP_COMMAND_TOPIC = 'color_temp_command_topic'\n CONF_COLOR_TEMP_STATE_TOPIC = 'color_temp_state_topic'\n CONF_COLOR_TEMP_VALUE_TEMPLATE = 'color_temp_value_template'\n+CONF_COLOR_TEMP_MIREDS_MIN = 'color_temp_mireds_min'\n+CONF_COLOR_TEMP_MIREDS_MAX = 'color_temp_mireds_max'", "msg": "I think we should call these options `color_temp_min_mireds` and `color_temp_max_mireds` as those are the names used throughout Home Assistant AFAIK."}
{"old_hunk": "@@ -275,10 +320,41 @@ def rgb_received(topic, payload, qos):\n         if self._topic[CONF_RGB_COMMAND_TOPIC] is not None:\n             self._hs = (0, 0)\n \n+        @callback\n+        def hue_received(topic, payload, qos):\n+            \"\"\"Handle new MQTT messages for hue.\"\"\"\n+            hue_value = float(templates[CONF_HUE](payload))\n+            percent_hue = hue_value / self._hue_scale\n+            self._hs = (int(percent_hue * 360), self._hs[1])\n+            self.async_schedule_update_ha_state()\n+\n+        if self._topic[CONF_HUE_STATE_TOPIC] is not None:\n+            yield from mqtt.async_subscribe(\n+                self.hass, self._topic[CONF_HUE_STATE_TOPIC],\n+                hue_received, self._qos)\n+            self._hs = (0, 0)\n+        if self._topic[CONF_HUE_COMMAND_TOPIC] is not None:\n+            self._hs = (0, 0)\n+\n+        @callback\n+        def saturation_received(topic, payload, qos):\n+            \"\"\"Handle new MQTT messages for saturation.\"\"\"\n+            saturation_value = float(templates[CONF_SATURATION](payload))\n+            self._hs = (self._hs[0], int(saturation_value))\n+            self.async_schedule_update_ha_state()\n+\n+        if self._topic[CONF_SATURATION_STATE_TOPIC] is not None:\n+            yield from mqtt.async_subscribe(\n+                self.hass, self._topic[CONF_SATURATION_STATE_TOPIC],\n+                saturation_received, self._qos)\n+            self._hs = (0, 0)\n+        if self._topic[CONF_SATURATION_COMMAND_TOPIC] is not None:\n+            self._hs = (0, 0)\n+\n         @callback\n         def color_temp_received(topic, payload, qos):\n             \"\"\"Handle new MQTT messages for color temperature.\"\"\"\n-            self._color_temp = int(templates[CONF_COLOR_TEMP](payload))\n+            self._color_temp = int(float(templates[CONF_COLOR_TEMP](payload)))", "hunk": "@@ -323,9 +323,8 @@ class MqttLight(MqttAvailability, Light):\n         @callback\n         def hue_received(topic, payload, qos):\n             \"\"\"Handle new MQTT messages for hue.\"\"\"\n-            hue_value = float(templates[CONF_HUE](payload))\n-            percent_hue = hue_value / self._hue_scale\n-            self._hs = (int(percent_hue * 360), self._hs[1])\n+            hue_value = int(templates[CONF_HUE_VALUE_TEMPLATE](payload))\n+            self._hs = (hue_value, self._hs[1])\n             self.async_schedule_update_ha_state()\n \n         if self._topic[CONF_HUE_STATE_TOPIC] is not None:\n", "comment": "? If we don't use floating point numbers internally, why accept floating point numbers? It would IMO just cause confusion because some users would expect us to handle color temp output in floating point to numbers too.", "ids": [309311, "14856c41ebcfdffa93d0ff7b76cca751f0745abd", "7b477e8d9fe8b1ddb25785d97c89a3056ceb236c"], "repo": "home-assistant-core", "ghid": 14308, "old": "         @callback\n         def hue_received(topic, payload, qos):\n             \"\"\"Handle new MQTT messages for hue.\"\"\"\n-            hue_value = float(templates[CONF_HUE](payload))\n-            percent_hue = hue_value / self._hue_scale\n-            self._hs = (int(percent_hue * 360), self._hs[1])\n             self.async_schedule_update_ha_state()\n         if self._topic[CONF_HUE_STATE_TOPIC] is not None:", "new": "         @callback\n         def hue_received(topic, payload, qos):\n             \"\"\"Handle new MQTT messages for hue.\"\"\"\n+            hue_value = int(templates[CONF_HUE_VALUE_TEMPLATE](payload))\n+            self._hs = (hue_value, self._hs[1])\n             self.async_schedule_update_ha_state()\n         if self._topic[CONF_HUE_STATE_TOPIC] is not None:", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -275,10 +320,41 @@ def rgb_received(topic, payload, qos):\n         if self._topic[CONF_RGB_COMMAND_TOPIC] is not None:\n             self._hs = (0, 0)\n \n+        @callback\n+        def hue_received(topic, payload, qos):\n+            \"\"\"Handle new MQTT messages for hue.\"\"\"\n+            hue_value = float(templates[CONF_HUE](payload))\n+            percent_hue = hue_value / self._hue_scale\n+            self._hs = (int(percent_hue * 360), self._hs[1])\n+            self.async_schedule_update_ha_state()\n+\n+        if self._topic[CONF_HUE_STATE_TOPIC] is not None:\n+            yield from mqtt.async_subscribe(\n+                self.hass, self._topic[CONF_HUE_STATE_TOPIC],\n+                hue_received, self._qos)\n+            self._hs = (0, 0)\n+        if self._topic[CONF_HUE_COMMAND_TOPIC] is not None:\n+            self._hs = (0, 0)\n+\n+        @callback\n+        def saturation_received(topic, payload, qos):\n+            \"\"\"Handle new MQTT messages for saturation.\"\"\"\n+            saturation_value = float(templates[CONF_SATURATION](payload))\n+            self._hs = (self._hs[0], int(saturation_value))\n+            self.async_schedule_update_ha_state()\n+\n+        if self._topic[CONF_SATURATION_STATE_TOPIC] is not None:\n+            yield from mqtt.async_subscribe(\n+                self.hass, self._topic[CONF_SATURATION_STATE_TOPIC],\n+                saturation_received, self._qos)\n+            self._hs = (0, 0)\n+        if self._topic[CONF_SATURATION_COMMAND_TOPIC] is not None:\n+            self._hs = (0, 0)\n+\n         @callback\n         def color_temp_received(topic, payload, qos):\n             \"\"\"Handle new MQTT messages for color temperature.\"\"\"\n-            self._color_temp = int(templates[CONF_COLOR_TEMP](payload))\n+            self._color_temp = int(float(templates[CONF_COLOR_TEMP](payload)))", "msg": "? If we don't use floating point numbers internally, why accept floating point numbers? It would IMO just cause confusion because some users would expect us to handle color temp output in floating point to numbers too."}
{"old_hunk": "@@ -81,5 +81,9 @@ def stop_cover(self, **kwargs):\n             self.apply_action('setPosition', 'secured')\n         elif self.tahoma_device.type == 'rts:BlindRTSComponent':\n             self.apply_action('my')\n+        elif self.tahoma_device.type == 'io:RollerShutterUnoIOComponent':", "hunk": "@@ -79,11 +79,8 @@ class TahomaCover(TahomaDevice, CoverDevice):\n         if self.tahoma_device.type == \\\n            'io:RollerShutterWithLowSpeedManagementIOComponent':\n             self.apply_action('setPosition', 'secured')\n-        elif self.tahoma_device.type == 'rts:BlindRTSComponent':\n-            self.apply_action('my')\n-        elif self.tahoma_device.type == 'io:RollerShutterUnoIOComponent':\n-            self.apply_action('my')\n-        elif self.tahoma_device.type == 'io:ExteriorVenetianBlindIOComponent':\n+        elif self.tahoma_device.type == 'rts:BlindRTSComponent' or \\\n+             self.tahoma_device.type == 'io:ExteriorVenetianBlindIOComponent':\n             self.apply_action('my')\n         else:\n-            self.apply_action('stopIdentify')\n+            self.apply_action('stopIdentify')\n\\ No newline at end of file\n", "comment": "Since it's the same command, please use a single if check: ```python elif self.tahoma_device.type in ('bla', 'bli', 'blub'): ```", "ids": [309333, "da604334d7b865d1c273f2738dc4d7c823870802", "280462df2cad787326d596e784f51af894778ffc"], "repo": "home-assistant-core", "ghid": 14321, "old": "         if self.tahoma_device.type == \\\n            'io:RollerShutterWithLowSpeedManagementIOComponent':\n             self.apply_action('setPosition', 'secured')\n-        elif self.tahoma_device.type == 'rts:BlindRTSComponent':\n-            self.apply_action('my')\n-        elif self.tahoma_device.type == 'io:RollerShutterUnoIOComponent':\n-            self.apply_action('my')\n-        elif self.tahoma_device.type == 'io:ExteriorVenetianBlindIOComponent':\n             self.apply_action('my')\n         else:\n-            self.apply_action('stopIdentify')\n\\ No newline at end of file", "new": "         if self.tahoma_device.type == \\\n            'io:RollerShutterWithLowSpeedManagementIOComponent':\n             self.apply_action('setPosition', 'secured')\n+        elif self.tahoma_device.type == 'rts:BlindRTSComponent' or \\\n+             self.tahoma_device.type == 'io:ExteriorVenetianBlindIOComponent':\n             self.apply_action('my')\n         else:\n+            self.apply_action('stopIdentify')\n\\ No newline at end of file", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -81,5 +81,9 @@ def stop_cover(self, **kwargs):\n             self.apply_action('setPosition', 'secured')\n         elif self.tahoma_device.type == 'rts:BlindRTSComponent':\n             self.apply_action('my')\n+        elif self.tahoma_device.type == 'io:RollerShutterUnoIOComponent':", "msg": "Since it's the same command, please use a single if check: ```python elif self.tahoma_device.type in ('bla', 'bli', 'blub'): ```"}
{"old_hunk": "@@ -8,6 +8,7 @@\n from concurrent import futures\n from contextlib import suppress\n from functools import partial\n+import hmac", "hunk": "@@ -8,7 +8,6 @@ import asyncio\n from concurrent import futures\n from contextlib import suppress\n from functools import partial\n-import hmac\n import json\n import logging\n \n", "comment": "'hmac' imported but unused", "ids": [309472, "8d712954fac7800a3aed33615bfad5463e8cf1a5", "5907f5e8785c8d29de56b1b4b00d97481bb07217"], "repo": "home-assistant-core", "ghid": 14339, "old": " from concurrent import futures\n from contextlib import suppress\n from functools import partial\n-import hmac\n import json\n import logging", "new": " from concurrent import futures\n from contextlib import suppress\n from functools import partial\n import json\n import logging", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -8,6 +8,7 @@\n from concurrent import futures\n from contextlib import suppress\n from functools import partial\n+import hmac", "msg": "'hmac' imported but unused"}
{"old_hunk": "@@ -102,8 +102,8 @@ def unit_of_measurement(self):\n     @property\n     def state(self):\n         \"\"\"Return the state of the entity.\"\"\"\n-        if self._state == 'unknown':\n-            return 'unknown'\n+        if self._state == STATE_UNKNOWN:", "hunk": "@@ -102,7 +102,7 @@ class TemperatureSensor(Sensor):\n     @property\n     def state(self):\n         \"\"\"Return the state of the entity.\"\"\"\n-        if self._state == STATE_UNKNOWN:\n+        if self._state is None:\n             return None\n         celsius = round(float(self._state) / 100, 1)\n         return convert_temperature(\n", "comment": "We shouldn't use `STATE_UNKNOWN` at all in the platforms or components. Probably change this in the zha component, that seems to set the initial state. Initial state should be `None`.", "ids": [309688, "f237d889470baf8543fed15d2fc946b1d75625d5", "053ac639bde90db496e9be41b9dd86b514fae061"], "repo": "home-assistant-core", "ghid": 14389, "old": "     @property\n     def state(self):\n         \"\"\"Return the state of the entity.\"\"\"\n-        if self._state == STATE_UNKNOWN:\n             return None\n         celsius = round(float(self._state) / 100, 1)\n         return convert_temperature(", "new": "     @property\n     def state(self):\n         \"\"\"Return the state of the entity.\"\"\"\n+        if self._state is None:\n             return None\n         celsius = round(float(self._state) / 100, 1)\n         return convert_temperature(", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -102,8 +102,8 @@ def unit_of_measurement(self):\n     @property\n     def state(self):\n         \"\"\"Return the state of the entity.\"\"\"\n-        if self._state == 'unknown':\n-            return 'unknown'\n+        if self._state == STATE_UNKNOWN:", "msg": "We shouldn't use `STATE_UNKNOWN` at all in the platforms or components. Probably change this in the zha component, that seems to set the initial state. Initial state should be `None`."}
{"old_hunk": "@@ -250,10 +253,13 @@ def brightness_received(topic, payload, qos):\n             self.async_schedule_update_ha_state()\n \n         if self._topic[CONF_BRIGHTNESS_STATE_TOPIC] is not None:\n-            yield from mqtt.async_subscribe(\n+            await mqtt.async_subscribe(\n                 self.hass, self._topic[CONF_BRIGHTNESS_STATE_TOPIC],\n                 brightness_received, self._qos)\n             self._brightness = 255\n+        elif self._optimistic_brightness and last_state\\\n+                and last_state.attributes.get('brightness'):", "hunk": "@@ -258,8 +258,8 @@ class MqttLight(MqttAvailability, Light):\n                 brightness_received, self._qos)\n             self._brightness = 255\n         elif self._optimistic_brightness and last_state\\\n-                and last_state.attributes.get('brightness'):\n-            self._brightness = last_state.attributes.get('brightness')\n+                and last_state.attributes.get(ATTR_BRIGHTNESS):\n+            self._brightness = last_state.attributes.get(ATTR_BRIGHTNESS)\n         elif self._topic[CONF_BRIGHTNESS_COMMAND_TOPIC] is not None:\n             self._brightness = 255\n         else:\n", "comment": "Please use `ATTR_BRIGHTNESS` from `light/__init__.py`. And on other places where a const which is defined in `light/__init__.py` make sense.", "ids": [309713, "fedf15e9224ba23c8ce52ac171feb2e9ecca8e3e", "dd2a30d49cc0ed0132003d3ea4f35fd39290b0df"], "repo": "home-assistant-core", "ghid": 14401, "old": "                 brightness_received, self._qos)\n             self._brightness = 255\n         elif self._optimistic_brightness and last_state\\\n-                and last_state.attributes.get('brightness'):\n-            self._brightness = last_state.attributes.get('brightness')\n         elif self._topic[CONF_BRIGHTNESS_COMMAND_TOPIC] is not None:\n             self._brightness = 255\n         else:", "new": "                 brightness_received, self._qos)\n             self._brightness = 255\n         elif self._optimistic_brightness and last_state\\\n+                and last_state.attributes.get(ATTR_BRIGHTNESS):\n+            self._brightness = last_state.attributes.get(ATTR_BRIGHTNESS)\n         elif self._topic[CONF_BRIGHTNESS_COMMAND_TOPIC] is not None:\n             self._brightness = 255\n         else:", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -250,10 +253,13 @@ def brightness_received(topic, payload, qos):\n             self.async_schedule_update_ha_state()\n \n         if self._topic[CONF_BRIGHTNESS_STATE_TOPIC] is not None:\n-            yield from mqtt.async_subscribe(\n+            await mqtt.async_subscribe(\n                 self.hass, self._topic[CONF_BRIGHTNESS_STATE_TOPIC],\n                 brightness_received, self._qos)\n             self._brightness = 255\n+        elif self._optimistic_brightness and last_state\\\n+                and last_state.attributes.get('brightness'):", "msg": "Please use `ATTR_BRIGHTNESS` from `light/__init__.py`. And on other places where a const which is defined in `light/__init__.py` make sense."}
{"old_hunk": "@@ -129,6 +143,8 @@ def update(self):\n \n         if self.variable == 'operation_mode':\n             self._state = getattr(self.device, \"mode\")\n+        elif self.variable == 'eta':\n+            self._state = getattr(self.device, 'eta_begin')", "hunk": "@@ -141,10 +141,9 @@ class NestBasicSensor(NestSensor):\n         \"\"\"Retrieve latest state.\"\"\"\n         self._unit = SENSOR_UNITS.get(self.variable, None)\n \n-        if self.variable == 'operation_mode':\n-            self._state = getattr(self.device, \"mode\")\n-        elif self.variable == 'eta':\n-            self._state = getattr(self.device, 'eta_begin')\n+        if self.variable in VARIABLE_NAME_MAPPING:\n+            self._state = getattr(self.device,\n+                                  VARIABLE_NAME_MAPPING[self.variable])\n         else:\n             self._state = getattr(self.device, self.variable)\n \n", "comment": "I know that you are just following the existing code, but maybe these string should be moved to constants at least harmonize the use of ' and \"", "ids": [309728, "484cf7b6bb712776a95d0c2ce79860de2eb16e8c", "64bf54e3df490ea67b351f78ec056fdc18ec83b0"], "repo": "home-assistant-core", "ghid": 14406, "old": "         \"\"\"Retrieve latest state.\"\"\"\n         self._unit = SENSOR_UNITS.get(self.variable, None)\n-        if self.variable == 'operation_mode':\n-            self._state = getattr(self.device, \"mode\")\n-        elif self.variable == 'eta':\n-            self._state = getattr(self.device, 'eta_begin')\n         else:\n             self._state = getattr(self.device, self.variable)", "new": "         \"\"\"Retrieve latest state.\"\"\"\n         self._unit = SENSOR_UNITS.get(self.variable, None)\n+        if self.variable in VARIABLE_NAME_MAPPING:\n+            self._state = getattr(self.device,\n+                                  VARIABLE_NAME_MAPPING[self.variable])\n         else:\n             self._state = getattr(self.device, self.variable)", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -129,6 +143,8 @@ def update(self):\n \n         if self.variable == 'operation_mode':\n             self._state = getattr(self.device, \"mode\")\n+        elif self.variable == 'eta':\n+            self._state = getattr(self.device, 'eta_begin')", "msg": "I know that you are just following the existing code, but maybe these string should be moved to constants at least harmonize the use of ' and \""}
{"old_hunk": "@@ -0,0 +1,183 @@\n+\"\"\"\n+Support for the elan.\n+For more details about this platform, please refer to the documentation at\n+https://home-assistant.io/components/xxxx/\n+\"\"\"\n+import asyncio\n+import aiohttp\n+import logging\n+import voluptuous as vol\n+\n+from homeassistant.core import callback\n+from homeassistant.const import ATTR_BATTERY_LEVEL\n+from homeassistant.components.light import (\n+    ATTR_BRIGHTNESS, ATTR_HS_COLOR,\n+    SUPPORT_BRIGHTNESS, SUPPORT_COLOR, Light)\n+from homeassistant.components.light import  PLATFORM_SCHEMA\n+from homeassistant.util import color as color_util\n+from homeassistant.helpers import config_validation as cv\n+\n+#REQUIREMENTS = ['pyelan']\n+\n+_LOGGER = logging.getLogger(__name__)\n+\n+#MIN_TIME_BETWEEN_SCANS = timedelta(seconds=10)\n+#MIN_TIME_BETWEEN_FORCED_SCANS = timedelta(milliseconds=100)\n+\n+# Validation of the user's configuration\n+PLATFORM_SCHEMA = PLATFORM_SCHEMA.extend({\n+    vol.Required('url'): cv.string,\n+})\n+\n+\n+@asyncio.coroutine\n+def async_setup_platform(hass, config, async_add_devices, discovery_info=None):\n+    \"\"\"Set up the elan Light platform.\"\"\"\n+    if discovery_info is None:\n+        return\n+\n+    url = discovery_info['url']\n+\n+    session = aiohttp.ClientSession()", "hunk": "@@ -11,11 +11,11 @@ import voluptuous as vol\n from homeassistant.core import callback\n from homeassistant.const import ATTR_BATTERY_LEVEL\n from homeassistant.components.light import (\n-    ATTR_BRIGHTNESS, ATTR_HS_COLOR,\n-    SUPPORT_BRIGHTNESS, SUPPORT_COLOR, Light)\n-from homeassistant.components.light import  PLATFORM_SCHEMA\n+    ATTR_BRIGHTNESS, ATTR_HS_COLOR, SUPPORT_BRIGHTNESS, SUPPORT_COLOR, Light)\n+from homeassistant.components.light import PLATFORM_SCHEMA\n from homeassistant.util import color as color_util\n from homeassistant.helpers import config_validation as cv\n+from homeassistant.helpers.aiohttp_client import async_get_clientsession\n \n #REQUIREMENTS = ['pyelan']\n \n", "comment": "Don't create new aiohttp sessions. Use the home assistant helper function, `hass.helpers.aiohttp_client.async_get_clientsession`, to get the session.", "ids": [309758, "e38c1762c0db15c867c7b403be50fc77517db1ae", "96878eb26326cac549f427e9249c271ab98843cf"], "repo": "home-assistant-core", "ghid": 14409, "old": " from homeassistant.core import callback\n from homeassistant.const import ATTR_BATTERY_LEVEL\n from homeassistant.components.light import (\n-    ATTR_BRIGHTNESS, ATTR_HS_COLOR,\n-    SUPPORT_BRIGHTNESS, SUPPORT_COLOR, Light)\n-from homeassistant.components.light import  PLATFORM_SCHEMA\n from homeassistant.util import color as color_util\n from homeassistant.helpers import config_validation as cv\n #REQUIREMENTS = ['pyelan']", "new": " from homeassistant.core import callback\n from homeassistant.const import ATTR_BATTERY_LEVEL\n from homeassistant.components.light import (\n+    ATTR_BRIGHTNESS, ATTR_HS_COLOR, SUPPORT_BRIGHTNESS, SUPPORT_COLOR, Light)\n+from homeassistant.components.light import PLATFORM_SCHEMA\n from homeassistant.util import color as color_util\n from homeassistant.helpers import config_validation as cv\n+from homeassistant.helpers.aiohttp_client import async_get_clientsession\n #REQUIREMENTS = ['pyelan']", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -0,0 +1,183 @@\n+\"\"\"\n+Support for the elan.\n+For more details about this platform, please refer to the documentation at\n+https://home-assistant.io/components/xxxx/\n+\"\"\"\n+import asyncio\n+import aiohttp\n+import logging\n+import voluptuous as vol\n+\n+from homeassistant.core import callback\n+from homeassistant.const import ATTR_BATTERY_LEVEL\n+from homeassistant.components.light import (\n+    ATTR_BRIGHTNESS, ATTR_HS_COLOR,\n+    SUPPORT_BRIGHTNESS, SUPPORT_COLOR, Light)\n+from homeassistant.components.light import  PLATFORM_SCHEMA\n+from homeassistant.util import color as color_util\n+from homeassistant.helpers import config_validation as cv\n+\n+#REQUIREMENTS = ['pyelan']\n+\n+_LOGGER = logging.getLogger(__name__)\n+\n+#MIN_TIME_BETWEEN_SCANS = timedelta(seconds=10)\n+#MIN_TIME_BETWEEN_FORCED_SCANS = timedelta(milliseconds=100)\n+\n+# Validation of the user's configuration\n+PLATFORM_SCHEMA = PLATFORM_SCHEMA.extend({\n+    vol.Required('url'): cv.string,\n+})\n+\n+\n+@asyncio.coroutine\n+def async_setup_platform(hass, config, async_add_devices, discovery_info=None):\n+    \"\"\"Set up the elan Light platform.\"\"\"\n+    if discovery_info is None:\n+        return\n+\n+    url = discovery_info['url']\n+\n+    session = aiohttp.ClientSession()", "msg": "Don't create new aiohttp sessions. Use the home assistant helper function, `hass.helpers.aiohttp_client.async_get_clientsession`, to get the session."}
{"old_hunk": "@@ -23,11 +23,13 @@\n \n CONF_LIFETIME = \"lifetime\"\n CONF_CYCLES = \"cycles\"\n+CONF_PRIORITY = \"priority\"\n \n PLATFORM_SCHEMA = PLATFORM_SCHEMA.extend({\n     vol.Optional(CONF_ICON, default=\"i555\"): cv.string,\n     vol.Optional(CONF_LIFETIME, default=10): cv.positive_int,\n     vol.Optional(CONF_CYCLES, default=1): cv.positive_int,\n+    vol.Optional(CONF_PRIORITY, default=\"warning\"): cv.string,", "hunk": "@@ -25,11 +25,14 @@ CONF_LIFETIME = \"lifetime\"\n CONF_CYCLES = \"cycles\"\n CONF_PRIORITY = \"priority\"\n \n+AVAILABLE_PRIORITIES = [\"info\", \"warning\", \"critical\"]\n+\n PLATFORM_SCHEMA = PLATFORM_SCHEMA.extend({\n     vol.Optional(CONF_ICON, default=\"i555\"): cv.string,\n     vol.Optional(CONF_LIFETIME, default=10): cv.positive_int,\n     vol.Optional(CONF_CYCLES, default=1): cv.positive_int,\n-    vol.Optional(CONF_PRIORITY, default=\"warning\"): cv.string,\n+    vol.Optional(CONF_PRIORITY, default=\"warning\"):\n+        vol.In(AVAILABLE_PRIORITIES)\n })\n \n \n", "comment": "```py AVAILABLE_PRIORITIES = [\"info\", \"warning\", \"critical\"] ... PLATFORM_SCHEMA = PLATFORM_SCHEMA.extend({ ... vol.Optional(CONF_PRIORITY, default=\"warning\"): vol.In(AVAILABLE_PRIORITIES), }) ```", "ids": [309799, "94ec9a27aec7cdd895df89140b3100f188e85b24", "e0f6e94badcaa28d9c58e50d24ba10d31c69fbbe"], "repo": "home-assistant-core", "ghid": 14414, "old": " CONF_CYCLES = \"cycles\"\n CONF_PRIORITY = \"priority\"\n PLATFORM_SCHEMA = PLATFORM_SCHEMA.extend({\n     vol.Optional(CONF_ICON, default=\"i555\"): cv.string,\n     vol.Optional(CONF_LIFETIME, default=10): cv.positive_int,\n     vol.Optional(CONF_CYCLES, default=1): cv.positive_int,\n-    vol.Optional(CONF_PRIORITY, default=\"warning\"): cv.string,\n })", "new": " CONF_CYCLES = \"cycles\"\n CONF_PRIORITY = \"priority\"\n+AVAILABLE_PRIORITIES = [\"info\", \"warning\", \"critical\"]\n+\n PLATFORM_SCHEMA = PLATFORM_SCHEMA.extend({\n     vol.Optional(CONF_ICON, default=\"i555\"): cv.string,\n     vol.Optional(CONF_LIFETIME, default=10): cv.positive_int,\n     vol.Optional(CONF_CYCLES, default=1): cv.positive_int,\n+    vol.Optional(CONF_PRIORITY, default=\"warning\"):\n+        vol.In(AVAILABLE_PRIORITIES)\n })", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -23,11 +23,13 @@\n \n CONF_LIFETIME = \"lifetime\"\n CONF_CYCLES = \"cycles\"\n+CONF_PRIORITY = \"priority\"\n \n PLATFORM_SCHEMA = PLATFORM_SCHEMA.extend({\n     vol.Optional(CONF_ICON, default=\"i555\"): cv.string,\n     vol.Optional(CONF_LIFETIME, default=10): cv.positive_int,\n     vol.Optional(CONF_CYCLES, default=1): cv.positive_int,\n+    vol.Optional(CONF_PRIORITY, default=\"warning\"): cv.string,", "msg": "```py AVAILABLE_PRIORITIES = [\"info\", \"warning\", \"critical\"] ... PLATFORM_SCHEMA = PLATFORM_SCHEMA.extend({ ... vol.Optional(CONF_PRIORITY, default=\"warning\"): vol.In(AVAILABLE_PRIORITIES), }) ```"}
{"old_hunk": "@@ -68,22 +79,14 @@ def icon(self):\n         charging_state = vehicle_state.charging_status in \\\n             [ChargingState.CHARGING]\n \n-        if self._attribute == 'mileage':\n-            return 'mdi:speedometer'\n-        elif self._attribute in (\n-                'remaining_range_total', 'remaining_range_electric',\n-                'remaining_range_fuel', 'max_range_electric'):\n-            return 'mdi:ruler'\n-        elif self._attribute == 'remaining_fuel':\n-            return 'mdi:gas-station'\n-        elif self._attribute == 'charging_time_remaining':\n-            return 'mdi:update'\n-        elif self._attribute == 'charging_status':\n-            return 'mdi:battery-charging'\n-        elif self._attribute == 'charging_level_hv':\n+        for key, value in ATTR_TO_HA.items():", "hunk": "@@ -79,14 +79,12 @@ class BMWConnectedDriveSensor(Entity):\n         charging_state = vehicle_state.charging_status in \\\n             [ChargingState.CHARGING]\n \n-        for key, value in ATTR_TO_HA.items():\n-            if self._attribute == key:\n-                return value[0]\n         if self._attribute == 'charging_level_hv':\n             return icon_for_battery_level(\n                 battery_level=vehicle_state.charging_level_hv,\n                 charging=charging_state)\n-        return None\n+        icon, _ = ATTR_TO_HA.get(self._attribute, [None, None])\n+        return icon\n \n     @property\n     def state(self):\n", "comment": "```py if self._attribute == 'charging_level_hv': return icon_for_battery_level(...) icon, _ = ATTR_TO_HA.get(self._attribute, [None, None]) return icon ```", "ids": [309851, "c683b10a2776f539710eac14b27175c8e6abe651", "059d1fe69f70d6898ccd8ae3a05fb7e2e713da5c"], "repo": "home-assistant-core", "ghid": 14424, "old": "         charging_state = vehicle_state.charging_status in \\\n             [ChargingState.CHARGING]\n-        for key, value in ATTR_TO_HA.items():\n-            if self._attribute == key:\n-                return value[0]\n         if self._attribute == 'charging_level_hv':\n             return icon_for_battery_level(\n                 battery_level=vehicle_state.charging_level_hv,\n                 charging=charging_state)\n-        return None\n     @property\n     def state(self):", "new": "         charging_state = vehicle_state.charging_status in \\\n             [ChargingState.CHARGING]\n         if self._attribute == 'charging_level_hv':\n             return icon_for_battery_level(\n                 battery_level=vehicle_state.charging_level_hv,\n                 charging=charging_state)\n+        icon, _ = ATTR_TO_HA.get(self._attribute, [None, None])\n+        return icon\n     @property\n     def state(self):", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -68,22 +79,14 @@ def icon(self):\n         charging_state = vehicle_state.charging_status in \\\n             [ChargingState.CHARGING]\n \n-        if self._attribute == 'mileage':\n-            return 'mdi:speedometer'\n-        elif self._attribute in (\n-                'remaining_range_total', 'remaining_range_electric',\n-                'remaining_range_fuel', 'max_range_electric'):\n-            return 'mdi:ruler'\n-        elif self._attribute == 'remaining_fuel':\n-            return 'mdi:gas-station'\n-        elif self._attribute == 'charging_time_remaining':\n-            return 'mdi:update'\n-        elif self._attribute == 'charging_status':\n-            return 'mdi:battery-charging'\n-        elif self._attribute == 'charging_level_hv':\n+        for key, value in ATTR_TO_HA.items():", "msg": "```py if self._attribute == 'charging_level_hv': return icon_for_battery_level(...) icon, _ = ATTR_TO_HA.get(self._attribute, [None, None]) return icon ```"}
{"old_hunk": "@@ -1,5 +1,5 @@\n \"\"\"\n-Adds support for generic thermostat units.\n+Based on generic thermostat units. Added a separate AC entity.", "hunk": "@@ -1,5 +1,5 @@\n \"\"\"\n-Based on generic thermostat units. Added a separate AC entity.\n+Adds support for generic thermostat units.\n \n For more details about this platform, please refer to the documentation at\n https://home-assistant.io/components/climate.generic_thermostat/\n", "comment": "This looks like a changelog now. Need rephrasing.", "ids": [309864, "eee2b64c93fa5a2cf8bfeab55825dea0bea29a99", "2f74ffcf81e22a9c038f2591dc008b7938b96d92"], "repo": "home-assistant-core", "ghid": 14427, "old": " \"\"\"\n-Based on generic thermostat units. Added a separate AC entity.\n For more details about this platform, please refer to the documentation at\n https://home-assistant.io/components/climate.generic_thermostat/", "new": " \"\"\"\n+Adds support for generic thermostat units.\n For more details about this platform, please refer to the documentation at\n https://home-assistant.io/components/climate.generic_thermostat/", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -1,5 +1,5 @@\n \"\"\"\n-Adds support for generic thermostat units.\n+Based on generic thermostat units. Added a separate AC entity.", "msg": "This looks like a changelog now. Need rephrasing."}
{"old_hunk": "@@ -36,6 +36,7 @@\n CONF_GATEWAYS = 'gateways'\n CONF_INTERFACE = 'interface'\n CONF_KEY = 'key'\n+CONF_DISABLED = 'disabled'", "hunk": "@@ -36,7 +36,7 @@ CONF_DISCOVERY_RETRY = 'discovery_retry'\n CONF_GATEWAYS = 'gateways'\n CONF_INTERFACE = 'interface'\n CONF_KEY = 'key'\n-CONF_DISABLED = 'disabled'\n+CONF_DISABLE = 'disable'\n \n DOMAIN = 'xiaomi_aqara'\n \n", "comment": "Could you rename the setting from \"disabled\" to \"disable\"?", "ids": [309886, "51d0e62ab329b836844b4309643d85cd21ae4fa8", "20e269f21621041f3f2d95f4afa6e2f13a0e79d7"], "repo": "home-assistant-core", "ghid": 14428, "old": " CONF_GATEWAYS = 'gateways'\n CONF_INTERFACE = 'interface'\n CONF_KEY = 'key'\n-CONF_DISABLED = 'disabled'\n DOMAIN = 'xiaomi_aqara'", "new": " CONF_GATEWAYS = 'gateways'\n CONF_INTERFACE = 'interface'\n CONF_KEY = 'key'\n+CONF_DISABLE = 'disable'\n DOMAIN = 'xiaomi_aqara'", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -36,6 +36,7 @@\n CONF_GATEWAYS = 'gateways'\n CONF_INTERFACE = 'interface'\n CONF_KEY = 'key'\n+CONF_DISABLED = 'disabled'", "msg": "Could you rename the setting from \"disabled\" to \"disable\"?"}
{"old_hunk": "@@ -0,0 +1,60 @@\n+\"\"\"\n+Family Hub camera for Samsung Refrigerators.\n+\n+For more details about this platform, please refer to the documentation\n+https://home-assistant.io/components/camera.familyhub/\n+\"\"\"\n+import logging\n+import voluptuous as vol\n+\n+import homeassistant.helpers.config_validation as cv\n+from homeassistant.components.sensor import PLATFORM_SCHEMA\n+from homeassistant.const import (CONF_NAME)\n+from homeassistant.components.camera import Camera\n+from homeassistant.helpers.aiohttp_client import async_get_clientsession\n+\n+_LOGGER = logging.getLogger(__name__)\n+\n+REQUIREMENTS = ['pyfamilyhublocal==0.0.2']\n+\n+CONF_IP = 'address'", "hunk": "@@ -9,7 +9,7 @@ import voluptuous as vol\n \n import homeassistant.helpers.config_validation as cv\n from homeassistant.components.sensor import PLATFORM_SCHEMA\n-from homeassistant.const import (CONF_NAME)\n+from homeassistant.const import (CONF_NAME, CONF_IP_ADDRESS)\n from homeassistant.components.camera import Camera\n from homeassistant.helpers.aiohttp_client import async_get_clientsession\n \n", "comment": "We have `CONF_IP_ADDRESS` in `const.py`. Please use that.", "ids": [310056, "05ab976092b33bbc224e09eea4a947d7e200c2f2", "945185498e7b3e510cdc9f8e2b740b91d7ed4dc4"], "repo": "home-assistant-core", "ghid": 14458, "old": " import homeassistant.helpers.config_validation as cv\n from homeassistant.components.sensor import PLATFORM_SCHEMA\n-from homeassistant.const import (CONF_NAME)\n from homeassistant.components.camera import Camera\n from homeassistant.helpers.aiohttp_client import async_get_clientsession", "new": " import homeassistant.helpers.config_validation as cv\n from homeassistant.components.sensor import PLATFORM_SCHEMA\n+from homeassistant.const import (CONF_NAME, CONF_IP_ADDRESS)\n from homeassistant.components.camera import Camera\n from homeassistant.helpers.aiohttp_client import async_get_clientsession", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -0,0 +1,60 @@\n+\"\"\"\n+Family Hub camera for Samsung Refrigerators.\n+\n+For more details about this platform, please refer to the documentation\n+https://home-assistant.io/components/camera.familyhub/\n+\"\"\"\n+import logging\n+import voluptuous as vol\n+\n+import homeassistant.helpers.config_validation as cv\n+from homeassistant.components.sensor import PLATFORM_SCHEMA\n+from homeassistant.const import (CONF_NAME)\n+from homeassistant.components.camera import Camera\n+from homeassistant.helpers.aiohttp_client import async_get_clientsession\n+\n+_LOGGER = logging.getLogger(__name__)\n+\n+REQUIREMENTS = ['pyfamilyhublocal==0.0.2']\n+\n+CONF_IP = 'address'", "msg": "We have `CONF_IP_ADDRESS` in `const.py`. Please use that."}
{"old_hunk": "@@ -175,8 +180,10 @@ def turn_on(self, **kwargs):\n \n     def turn_off(self, **kwargs):\n         \"\"\"Turn the device off.\"\"\"\n+        one_or_zero = 1 if self.invert else 0", "hunk": "@@ -180,9 +180,9 @@ class ArestSwitchPin(ArestSwitchBase):\n \n     def turn_off(self, **kwargs):\n         \"\"\"Turn the device off.\"\"\"\n-        one_or_zero = 1 if self.invert else 0\n+        turn_off_payload = int(self.invert)\n         request = requests.get(\n-            '{}/digital/{}/{}'.format(self._resource, self._pin, one_or_zero),\n+            '{}/digital/{}/{}'.format(self._resource, self._pin, turn_off_payload),\n             timeout=10)\n         if request.status_code == 200:\n             self._state = False\n", "comment": "I'm not happy about the naming. `turn_off_payload = int(not invert)`?", "ids": [310078, "18db43f371334e598979208da3b1496c463ca610", "2afd4949b6dbd870e0378a2fa209a708109388a9"], "repo": "home-assistant-core", "ghid": 14467, "old": "     def turn_off(self, **kwargs):\n         \"\"\"Turn the device off.\"\"\"\n-        one_or_zero = 1 if self.invert else 0\n         request = requests.get(\n-            '{}/digital/{}/{}'.format(self._resource, self._pin, one_or_zero),\n             timeout=10)\n         if request.status_code == 200:\n             self._state = False", "new": "     def turn_off(self, **kwargs):\n         \"\"\"Turn the device off.\"\"\"\n+        turn_off_payload = int(self.invert)\n         request = requests.get(\n+            '{}/digital/{}/{}'.format(self._resource, self._pin, turn_off_payload),\n             timeout=10)\n         if request.status_code == 200:\n             self._state = False", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -175,8 +180,10 @@ def turn_on(self, **kwargs):\n \n     def turn_off(self, **kwargs):\n         \"\"\"Turn the device off.\"\"\"\n+        one_or_zero = 1 if self.invert else 0", "msg": "I'm not happy about the naming. `turn_off_payload = int(not invert)`?"}
{"old_hunk": "@@ -18,11 +18,13 @@\n \n CONF_FUNCTIONS = 'functions'\n CONF_PINS = 'pins'\n+CONF_INVERT = 'invert'\n \n DEFAULT_NAME = 'aREST switch'\n \n PIN_FUNCTION_SCHEMA = vol.Schema({\n     vol.Optional(CONF_NAME): cv.string,\n+    vol.Optional(CONF_INVERT): cv.boolean,", "hunk": "@@ -24,7 +24,7 @@ DEFAULT_NAME = 'aREST switch'\n \n PIN_FUNCTION_SCHEMA = vol.Schema({\n     vol.Optional(CONF_NAME): cv.string,\n-    vol.Optional(CONF_INVERT): cv.boolean,\n+    vol.Optional(CONF_INVERT, default=False): cv.boolean,\n })\n \n PLATFORM_SCHEMA = PLATFORM_SCHEMA.extend({\n", "comment": "Please apply the default (False) here.", "ids": [310077, "18db43f371334e598979208da3b1496c463ca610", "2afd4949b6dbd870e0378a2fa209a708109388a9"], "repo": "home-assistant-core", "ghid": 14467, "old": " PIN_FUNCTION_SCHEMA = vol.Schema({\n     vol.Optional(CONF_NAME): cv.string,\n-    vol.Optional(CONF_INVERT): cv.boolean,\n })\n PLATFORM_SCHEMA = PLATFORM_SCHEMA.extend({", "new": " PIN_FUNCTION_SCHEMA = vol.Schema({\n     vol.Optional(CONF_NAME): cv.string,\n+    vol.Optional(CONF_INVERT, default=False): cv.boolean,\n })\n PLATFORM_SCHEMA = PLATFORM_SCHEMA.extend({", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -18,11 +18,13 @@\n \n CONF_FUNCTIONS = 'functions'\n CONF_PINS = 'pins'\n+CONF_INVERT = 'invert'\n \n DEFAULT_NAME = 'aREST switch'\n \n PIN_FUNCTION_SCHEMA = vol.Schema({\n     vol.Optional(CONF_NAME): cv.string,\n+    vol.Optional(CONF_INVERT): cv.boolean,", "msg": "Please apply the default (False) here."}
{"old_hunk": "@@ -242,7 +248,7 @@ def turn_on(self, **kwargs):\n             self._bulb.setPresetPattern(EFFECT_MAP[effect], 50)\n \n         # white change only\n-        elif white is not None:\n+        if white is not None:", "hunk": "@@ -247,10 +247,6 @@ class FluxLight(Light):\n         elif effect in EFFECT_MAP:\n             self._bulb.setPresetPattern(EFFECT_MAP[effect], 50)\n \n-        # white change only\n-        if white is not None:\n-            self._bulb.setWarmWhite255(white)\n-\n     def turn_off(self, **kwargs):\n         \"\"\"Turn the specified or all lights off.\"\"\"\n         self._bulb.turnOff()\n", "comment": "The changes from `elif`s to `if`s will make it do four color change calls to the bulb if `rgb`, `brightness`, `effect` and `white` are all set. Even with just `rgb` and `brightness`, it'll still go through the whole \"fetch rgb\" block. This method should only ever call a single `setRgb`, from the looks of it. Simply returning after each setRgb would be one way to go here.", "ids": [310122, "4238fa58fb5c4bcb018b25d91669ca4b91801215", "0515a9d22e661189f48b3edcd068c726253e817f"], "repo": "home-assistant-core", "ghid": 14476, "old": "         elif effect in EFFECT_MAP:\n             self._bulb.setPresetPattern(EFFECT_MAP[effect], 50)\n-        # white change only\n-        if white is not None:\n-            self._bulb.setWarmWhite255(white)\n-\n     def turn_off(self, **kwargs):\n         \"\"\"Turn the specified or all lights off.\"\"\"\n         self._bulb.turnOff()", "new": "         elif effect in EFFECT_MAP:\n             self._bulb.setPresetPattern(EFFECT_MAP[effect], 50)\n     def turn_off(self, **kwargs):\n         \"\"\"Turn the specified or all lights off.\"\"\"\n         self._bulb.turnOff()", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -242,7 +248,7 @@ def turn_on(self, **kwargs):\n             self._bulb.setPresetPattern(EFFECT_MAP[effect], 50)\n \n         # white change only\n-        elif white is not None:\n+        if white is not None:", "msg": "The changes from `elif`s to `if`s will make it do four color change calls to the bulb if `rgb`, `brightness`, `effect` and `white` are all set. Even with just `rgb` and `brightness`, it'll still go through the whole \"fetch rgb\" block. This method should only ever call a single `setRgb`, from the looks of it. Simply returning after each setRgb would be one way to go here."}
{"old_hunk": "@@ -75,6 +76,7 @@\n \n ENDPOINT_ROOT = '/api/konnected'\n UPDATE_ENDPOINT = (ENDPOINT_ROOT + r'/device/{device_id:[a-zA-Z0-9]+}')\n+SIGNAL_SENSOR_UPDATE = 'konnected.sensor_updated'", "hunk": "@@ -76,7 +76,7 @@ DEPENDENCIES = ['http', 'discovery']\n \n ENDPOINT_ROOT = '/api/konnected'\n UPDATE_ENDPOINT = (ENDPOINT_ROOT + r'/device/{device_id:[a-zA-Z0-9]+}')\n-SIGNAL_SENSOR_UPDATE = 'konnected.sensor_updated'\n+SIGNAL_SENSOR_UPDATE = 'konnected.{}.update'\n \n \n async def async_setup(hass, config):\n", "comment": "Have the signal key take a parameter in the form of the entity_id. Then you create unique signals per entity and the dispatcher will be more effective. ```py SIGNAL_ENTITY_UPDATE = 'update_konnected_{}' ```", "ids": [310207, "a901081452554307ccde8716a97f7cd2c5288773", "63490c363439412af8ef780fa4cf8854591496d2"], "repo": "home-assistant-core", "ghid": 14491, "old": " ENDPOINT_ROOT = '/api/konnected'\n UPDATE_ENDPOINT = (ENDPOINT_ROOT + r'/device/{device_id:[a-zA-Z0-9]+}')\n-SIGNAL_SENSOR_UPDATE = 'konnected.sensor_updated'\n async def async_setup(hass, config):", "new": " ENDPOINT_ROOT = '/api/konnected'\n UPDATE_ENDPOINT = (ENDPOINT_ROOT + r'/device/{device_id:[a-zA-Z0-9]+}')\n+SIGNAL_SENSOR_UPDATE = 'konnected.{}.update'\n async def async_setup(hass, config):", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -75,6 +76,7 @@\n \n ENDPOINT_ROOT = '/api/konnected'\n UPDATE_ENDPOINT = (ENDPOINT_ROOT + r'/device/{device_id:[a-zA-Z0-9]+}')\n+SIGNAL_SENSOR_UPDATE = 'konnected.sensor_updated'", "msg": "Have the signal key take a parameter in the form of the entity_id. Then you create unique signals per entity and the dispatcher will be more effective. ```py SIGNAL_ENTITY_UPDATE = 'update_konnected_{}' ```"}
{"old_hunk": "@@ -60,25 +64,29 @@ def __init__(self, hass, config: ConfigType, see) -> None:\n             self.success_init = True\n \n         except InvalidUser:\n-            _LOGGER.error('You have specified invalid login credentials')\n+            _LOGGER.error(\"You have specified invalid login credentials\")\n             self.success_init = False\n \n     def _update_info(self, now=None):\n         for person in self.service.get_all_people():\n-            dev_id = 'google_maps_{0}'.format(slugify(person.id))\n+            try:\n+                dev_id = 'google_maps_{0}'.format(person.id)\n+            except TypeError:\n+                _LOGGER.error(\"No location(s) shared with this account\")", "hunk": "@@ -72,7 +72,7 @@ class GoogleMapsScanner(object):\n             try:\n                 dev_id = 'google_maps_{0}'.format(person.id)\n             except TypeError:\n-                _LOGGER.error(\"No location(s) shared with this account\")\n+                _LOGGER.warning(\"No location(s) shared with this account\")\n                 return\n \n             attrs = {\n", "comment": "It's possible to disable the location sharing temporary/easily? If yes, I would use the log level \"warn\" here.", "ids": [310251, "133ef4662623c399889ba727435573cb81261ae7", "a9b5b800ac74c5258bb84702db64e74cad262d63"], "repo": "home-assistant-core", "ghid": 14511, "old": "             try:\n                 dev_id = 'google_maps_{0}'.format(person.id)\n             except TypeError:\n-                _LOGGER.error(\"No location(s) shared with this account\")\n                 return\n             attrs = {", "new": "             try:\n                 dev_id = 'google_maps_{0}'.format(person.id)\n             except TypeError:\n+                _LOGGER.warning(\"No location(s) shared with this account\")\n                 return\n             attrs = {", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -60,25 +64,29 @@ def __init__(self, hass, config: ConfigType, see) -> None:\n             self.success_init = True\n \n         except InvalidUser:\n-            _LOGGER.error('You have specified invalid login credentials')\n+            _LOGGER.error(\"You have specified invalid login credentials\")\n             self.success_init = False\n \n     def _update_info(self, now=None):\n         for person in self.service.get_all_people():\n-            dev_id = 'google_maps_{0}'.format(slugify(person.id))\n+            try:\n+                dev_id = 'google_maps_{0}'.format(person.id)\n+            except TypeError:\n+                _LOGGER.error(\"No location(s) shared with this account\")", "msg": "It's possible to disable the location sharing temporary/easily? If yes, I would use the log level \"warn\" here."}
{"old_hunk": "@@ -14,12 +14,20 @@\n DEPENDENCIES = ['dyson']\n \n SENSOR_UNITS = {\n-    'air_quality': 'level',\n-    'dust': 'level',\n+    'air_quality': '',", "hunk": "@@ -10,7 +10,7 @@\n     <h1>Error 503 Backend is unhealthy</h1>\n     <p>Backend is unhealthy</p>\n     <h3>Guru Mediation:</h3>\n-    <p>Details: cache-sea4465-SEA 1645536512 271769901</p>\n+    <p>Details: cache-sea4432-SEA 1645536512 2205795317</p>\n     <hr>\n     <p>Varnish cache server</p>\n   </body>\n", "comment": "I assume `None` is preferred instead of an empty string.", "ids": [310360, "e9616c099e64e354bd0d57d534d4fe4d1f52dff1", "eeb7a89760b6d8eab94900b6095bda9d9271eb53"], "repo": "home-assistant-core", "ghid": 14550, "old": "     <h1>Error 503 Backend is unhealthy</h1>\n     <p>Backend is unhealthy</p>\n     <h3>Guru Mediation:</h3>\n-    <p>Details: cache-sea4465-SEA 1645536512 271769901</p>\n     <hr>\n     <p>Varnish cache server</p>\n   </body>", "new": "     <h1>Error 503 Backend is unhealthy</h1>\n     <p>Backend is unhealthy</p>\n     <h3>Guru Mediation:</h3>\n+    <p>Details: cache-sea4432-SEA 1645536512 2205795317</p>\n     <hr>\n     <p>Varnish cache server</p>\n   </body>", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -14,12 +14,20 @@\n DEPENDENCIES = ['dyson']\n \n SENSOR_UNITS = {\n-    'air_quality': 'level',\n-    'dust': 'level',\n+    'air_quality': '',", "msg": "I assume `None` is preferred instead of an empty string."}
{"old_hunk": "@@ -143,3 +146,20 @@ def state(self):\n             return None\n \n         return round(float(self._state))\n+\n+\n+class IlluminanceMeasurementSensor(Sensor):\n+    \"\"\"ZHA lux sensor.\"\"\"\n+", "hunk": "@@ -159,7 +159,5 @@ class IlluminanceMeasurementSensor(Sensor):\n     @property\n     def state(self):\n         \"\"\"Return the state of the entity.\"\"\"\n-        if self._state is None:\n-            return None\n \n         return self._state\n", "comment": "No value attribute here?", "ids": [310451, "29dec6bf7bbd35cc27ac20e2c55d77cf66bdfe59", "03dd75ff7fe982b107d1be3be38df050191a3d25"], "repo": "home-assistant-core", "ghid": 14563, "old": "     @property\n     def state(self):\n         \"\"\"Return the state of the entity.\"\"\"\n-        if self._state is None:\n-            return None\n         return self._state", "new": "     @property\n     def state(self):\n         \"\"\"Return the state of the entity.\"\"\"\n         return self._state", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -143,3 +146,20 @@ def state(self):\n             return None\n \n         return round(float(self._state))\n+\n+\n+class IlluminanceMeasurementSensor(Sensor):\n+    \"\"\"ZHA lux sensor.\"\"\"\n+", "msg": "No value attribute here?"}
{"old_hunk": "@@ -209,11 +215,12 @@ def unit_of_measurement(self):\n     def device_state_attributes(self):\n         \"\"\"Return the sensor attributes.\"\"\"\n         attr = dict()\n-        attr[ATTR_STATE] = self.opp_state()\n+        attr[ATTR_STATE] = self.display_state() + \" (\" + \\\n+            self._data.status[KEY_STATUS] + \")\"", "hunk": "@@ -215,8 +215,8 @@ class NUTSensor(Entity):\n     def device_state_attributes(self):\n         \"\"\"Return the sensor attributes.\"\"\"\n         attr = dict()\n-        attr[ATTR_STATE] = self.display_state() + \" (\" + \\\n-            self._data.status[KEY_STATUS] + \")\"\n+        attr[ATTR_STATE] = \"{} ({})\".format(self.display_state(),\n+                                            self._data.status[KEY_STATUS])\n         return attr\n \n     def display_state(self):\n", "comment": "Please use `format()` instead of concats (`+`)", "ids": [310460, "dc8c63bc68e0f6eba9940e9a5cacd92b00c3cd0b", "2033b265d4b2ccafd1f8be88fcf53f8cd6ad5fd3"], "repo": "home-assistant-core", "ghid": 14570, "old": "     def device_state_attributes(self):\n         \"\"\"Return the sensor attributes.\"\"\"\n         attr = dict()\n-        attr[ATTR_STATE] = self.display_state() + \" (\" + \\\n-            self._data.status[KEY_STATUS] + \")\"\n         return attr\n     def display_state(self):", "new": "     def device_state_attributes(self):\n         \"\"\"Return the sensor attributes.\"\"\"\n         attr = dict()\n+        attr[ATTR_STATE] = \"{} ({})\".format(self.display_state(),\n+                                            self._data.status[KEY_STATUS])\n         return attr\n     def display_state(self):", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -209,11 +215,12 @@ def unit_of_measurement(self):\n     def device_state_attributes(self):\n         \"\"\"Return the sensor attributes.\"\"\"\n         attr = dict()\n-        attr[ATTR_STATE] = self.opp_state()\n+        attr[ATTR_STATE] = self.display_state() + \" (\" + \\\n+            self._data.status[KEY_STATUS] + \")\"", "msg": "Please use `format()` instead of concats (`+`)"}
{"old_hunk": "@@ -29,6 +29,9 @@\n \n CONFIG_FILE = '.nanoleaf_aurora.conf'\n \n+NANOLEAF_AURORA_MIN_MIRED = 154\n+NANOLEAF_AURORA_MAX_MIRED = 833", "hunk": "@@ -29,9 +29,6 @@ DATA_NANOLEAF_AURORA = 'nanoleaf_aurora'\n \n CONFIG_FILE = '.nanoleaf_aurora.conf'\n \n-NANOLEAF_AURORA_MIN_MIRED = 154\n-NANOLEAF_AURORA_MAX_MIRED = 833\n-\n ICON = 'mdi:triangle-outline'\n \n SUPPORT_AURORA = (SUPPORT_BRIGHTNESS | SUPPORT_COLOR_TEMP | SUPPORT_EFFECT |\n", "comment": "The name of the const is a bit verbose and is used only once. In my opinion you could put the values direct to the methods.", "ids": [310467, "e44fe3a53163557687a89218812cfcd321fb4dea", "8e5d40a033dd92f59287c378d5a66a70bd5cb0f4"], "repo": "home-assistant-core", "ghid": 14571, "old": " CONFIG_FILE = '.nanoleaf_aurora.conf'\n-NANOLEAF_AURORA_MIN_MIRED = 154\n-NANOLEAF_AURORA_MAX_MIRED = 833\n-\n ICON = 'mdi:triangle-outline'\n SUPPORT_AURORA = (SUPPORT_BRIGHTNESS | SUPPORT_COLOR_TEMP | SUPPORT_EFFECT |", "new": " CONFIG_FILE = '.nanoleaf_aurora.conf'\n ICON = 'mdi:triangle-outline'\n SUPPORT_AURORA = (SUPPORT_BRIGHTNESS | SUPPORT_COLOR_TEMP | SUPPORT_EFFECT |", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -29,6 +29,9 @@\n \n CONFIG_FILE = '.nanoleaf_aurora.conf'\n \n+NANOLEAF_AURORA_MIN_MIRED = 154\n+NANOLEAF_AURORA_MAX_MIRED = 833", "msg": "The name of the const is a bit verbose and is used only once. In my opinion you could put the values direct to the methods."}
{"old_hunk": "@@ -69,6 +70,11 @@ def __init__(self, myq, device):\n         self._name = device['name']\n         self._status = STATE_CLOSED\n \n+    @property\n+    def device_class(self):\n+        \"\"\"Poll for state.\"\"\"", "hunk": "@@ -72,8 +72,8 @@ class MyQDevice(CoverDevice):\n \n     @property\n     def device_class(self):\n-        \"\"\"Poll for state.\"\"\"\n-        return DEFAULT_DEVICE_CLASS\n+        \"\"\"Define this cover as a garage door.\"\"\"\n+        return 'garage'\n \n     @property\n     def should_poll(self):\n", "comment": "Please update this docstring.", "ids": [310607, "c82ba5ca8729f184f46d71a7e05f9c75998b4fa7", "21101a5605ea909f9393d19c07c6ca0ace94c962"], "repo": "home-assistant-core", "ghid": 14602, "old": "     @property\n     def device_class(self):\n-        \"\"\"Poll for state.\"\"\"\n-        return DEFAULT_DEVICE_CLASS\n     @property\n     def should_poll(self):", "new": "     @property\n     def device_class(self):\n+        \"\"\"Define this cover as a garage door.\"\"\"\n+        return 'garage'\n     @property\n     def should_poll(self):", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -69,6 +70,11 @@ def __init__(self, myq, device):\n         self._name = device['name']\n         self._status = STATE_CLOSED\n \n+    @property\n+    def device_class(self):\n+        \"\"\"Poll for state.\"\"\"", "msg": "Please update this docstring."}
{"old_hunk": "@@ -17,6 +17,7 @@\n \n _LOGGER = logging.getLogger(__name__)\n \n+DEFAULT_DEVICE_CLASS = 'garage'", "hunk": "@@ -17,7 +17,6 @@ REQUIREMENTS = ['pymyq==0.0.8']\n \n _LOGGER = logging.getLogger(__name__)\n \n-DEFAULT_DEVICE_CLASS = 'garage'\n DEFAULT_NAME = 'myq'\n \n NOTIFICATION_ID = 'myq_notification'\n", "comment": "Please move the value into the function. The const is used only once and the name (DEFAULT_) isn't straight.", "ids": [310608, "c82ba5ca8729f184f46d71a7e05f9c75998b4fa7", "21101a5605ea909f9393d19c07c6ca0ace94c962"], "repo": "home-assistant-core", "ghid": 14602, "old": " _LOGGER = logging.getLogger(__name__)\n-DEFAULT_DEVICE_CLASS = 'garage'\n DEFAULT_NAME = 'myq'\n NOTIFICATION_ID = 'myq_notification'", "new": " _LOGGER = logging.getLogger(__name__)\n DEFAULT_NAME = 'myq'\n NOTIFICATION_ID = 'myq_notification'", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -17,6 +17,7 @@\n \n _LOGGER = logging.getLogger(__name__)\n \n+DEFAULT_DEVICE_CLASS = 'garage'", "msg": "Please move the value into the function. The const is used only once and the name (DEFAULT_) isn't straight."}
{"old_hunk": "@@ -0,0 +1,136 @@\n+\"\"\"\n+Support for 1-Wire environment switches.\n+\n+For more details about this platform, please refer to the documentation at\n+https://home-assistant.io/components/switch.onewire/\n+\"\"\"\n+import os\n+import logging\n+from glob import glob\n+\n+import voluptuous as vol\n+\n+import homeassistant.helpers.config_validation as cv\n+from homeassistant.components.switch import SwitchDevice\n+from homeassistant.helpers.config_validation import PLATFORM_SCHEMA  # noqa\n+from homeassistant.const import DEVICE_DEFAULT_NAME\n+\n+_LOGGER = logging.getLogger(__name__)\n+\n+CONF_MOUNT_DIR = 'mount_dir'\n+CONF_NAMES = 'names'\n+\n+DEFAULT_MOUNT_DIR = '/sys/bus/w1/devices/'\n+DEVICE_SWITCHES = {'05': {'A': 'PIO'},\n+                   '12': {'A': 'PIO.A',\n+                          'B': 'PIO.B'},\n+                   '29': {'0': 'PIO.0',\n+                          '1': 'PIO.1',\n+                          '2': 'PIO.2',\n+                          '3': 'PIO.3',\n+                          '4': 'PIO.4',\n+                          '5': 'PIO.5',\n+                          '6': 'PIO.6',\n+                          '7': 'PIO.7'},\n+                   '3A': {'A': 'PIO.A',\n+                          'B': 'PIO.B'}}\n+\n+DEVICE_SWITCH_ON = \"1\"\n+DEVICE_SWITCH_OFF = \"0\"\n+\n+PLATFORM_SCHEMA = PLATFORM_SCHEMA.extend({\n+    vol.Optional(CONF_NAMES): {cv.string: cv.string},\n+    vol.Optional(CONF_MOUNT_DIR, default=DEFAULT_MOUNT_DIR): cv.string,\n+})\n+\n+\n+# pylint: disable=unused-argument\n+def setup_platform(hass, config, add_devices, discovery_info=None):\n+    \"\"\"Set up the one wire Switches.\"\"\"\n+    base_dir = config.get(CONF_MOUNT_DIR)\n+    devs = []\n+    device_names = {}\n+    if 'names' in config:\n+        if isinstance(config['names'], dict):\n+            device_names = config['names']\n+\n+    for family_file_path in glob(os.path.join(base_dir, '*', 'family')):\n+        with open(family_file_path, \"r\") as family_file:\n+            family = family_file.read()\n+        if family in DEVICE_SWITCHES:\n+            switch_id = os.path.split(\n+                os.path.split(family_file_path)[0])[1]\n+            device_files = {}", "hunk": "@@ -60,7 +60,6 @@ def setup_platform(hass, config, add_devices, discovery_info=None):\n         if family in DEVICE_SWITCHES:\n             switch_id = os.path.split(\n                 os.path.split(family_file_path)[0])[1]\n-            device_files = {}\n             for switch_key, switch_value in DEVICE_SWITCHES[family].items():\n                 switch_id_complete = switch_id + \"_\" + switch_key\n                 device_file = os.path.join(\n", "comment": "local variable 'device_files' is assigned to but never used", "ids": [310650, "9d957b37649401934d89c599aefb89a081e97d03", "f6efba55263110f1fbcca3b0f54aa04515583051"], "repo": "home-assistant-core", "ghid": 14616, "old": "         if family in DEVICE_SWITCHES:\n             switch_id = os.path.split(\n                 os.path.split(family_file_path)[0])[1]\n-            device_files = {}\n             for switch_key, switch_value in DEVICE_SWITCHES[family].items():\n                 switch_id_complete = switch_id + \"_\" + switch_key\n                 device_file = os.path.join(", "new": "         if family in DEVICE_SWITCHES:\n             switch_id = os.path.split(\n                 os.path.split(family_file_path)[0])[1]\n             for switch_key, switch_value in DEVICE_SWITCHES[family].items():\n                 switch_id_complete = switch_id + \"_\" + switch_key\n                 device_file = os.path.join(", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -0,0 +1,136 @@\n+\"\"\"\n+Support for 1-Wire environment switches.\n+\n+For more details about this platform, please refer to the documentation at\n+https://home-assistant.io/components/switch.onewire/\n+\"\"\"\n+import os\n+import logging\n+from glob import glob\n+\n+import voluptuous as vol\n+\n+import homeassistant.helpers.config_validation as cv\n+from homeassistant.components.switch import SwitchDevice\n+from homeassistant.helpers.config_validation import PLATFORM_SCHEMA  # noqa\n+from homeassistant.const import DEVICE_DEFAULT_NAME\n+\n+_LOGGER = logging.getLogger(__name__)\n+\n+CONF_MOUNT_DIR = 'mount_dir'\n+CONF_NAMES = 'names'\n+\n+DEFAULT_MOUNT_DIR = '/sys/bus/w1/devices/'\n+DEVICE_SWITCHES = {'05': {'A': 'PIO'},\n+                   '12': {'A': 'PIO.A',\n+                          'B': 'PIO.B'},\n+                   '29': {'0': 'PIO.0',\n+                          '1': 'PIO.1',\n+                          '2': 'PIO.2',\n+                          '3': 'PIO.3',\n+                          '4': 'PIO.4',\n+                          '5': 'PIO.5',\n+                          '6': 'PIO.6',\n+                          '7': 'PIO.7'},\n+                   '3A': {'A': 'PIO.A',\n+                          'B': 'PIO.B'}}\n+\n+DEVICE_SWITCH_ON = \"1\"\n+DEVICE_SWITCH_OFF = \"0\"\n+\n+PLATFORM_SCHEMA = PLATFORM_SCHEMA.extend({\n+    vol.Optional(CONF_NAMES): {cv.string: cv.string},\n+    vol.Optional(CONF_MOUNT_DIR, default=DEFAULT_MOUNT_DIR): cv.string,\n+})\n+\n+\n+# pylint: disable=unused-argument\n+def setup_platform(hass, config, add_devices, discovery_info=None):\n+    \"\"\"Set up the one wire Switches.\"\"\"\n+    base_dir = config.get(CONF_MOUNT_DIR)\n+    devs = []\n+    device_names = {}\n+    if 'names' in config:\n+        if isinstance(config['names'], dict):\n+            device_names = config['names']\n+\n+    for family_file_path in glob(os.path.join(base_dir, '*', 'family')):\n+        with open(family_file_path, \"r\") as family_file:\n+            family = family_file.read()\n+        if family in DEVICE_SWITCHES:\n+            switch_id = os.path.split(\n+                os.path.split(family_file_path)[0])[1]\n+            device_files = {}", "msg": "local variable 'device_files' is assigned to but never used"}
{"old_hunk": "@@ -104,6 +104,10 @@ def setup_platform(hass, config, add_devices, discovery_info=None):\n                                                    activity_zone)]\n     add_devices(sensors, True)\n \n+    for sensor in sensors:\n+        hass.bus.listen(EVENT_NEST_UPDATE,", "hunk": "@@ -105,8 +105,8 @@ def setup_platform(hass, config, add_devices, discovery_info=None):\n     add_devices(sensors, True)\n \n     for sensor in sensors:\n-        hass.bus.listen(EVENT_NEST_UPDATE,\n-                        sensor.async_nest_update_event_handler)\n+        async_dispatcher_connect(hass, SIGNAL_NEST_UPDATE,\n+                                 sensor.async_nest_update_callback)\n \n \n class NestBinarySensor(NestSensor, BinarySensorDevice):\n", "comment": "Use our dispatch helper instead, to communicate between component and its platforms.", "ids": [310823, "69b8f9ea13bbe624d6f3227e3ed642c2b5b2f7ba", "ff4bb8978631075622cc9b4dbb7bed678f545fcb"], "repo": "home-assistant-core", "ghid": 14656, "old": "     add_devices(sensors, True)\n     for sensor in sensors:\n-        hass.bus.listen(EVENT_NEST_UPDATE,\n-                        sensor.async_nest_update_event_handler)\n class NestBinarySensor(NestSensor, BinarySensorDevice):", "new": "     add_devices(sensors, True)\n     for sensor in sensors:\n+        async_dispatcher_connect(hass, SIGNAL_NEST_UPDATE,\n+                                 sensor.async_nest_update_callback)\n class NestBinarySensor(NestSensor, BinarySensorDevice):", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -104,6 +104,10 @@ def setup_platform(hass, config, add_devices, discovery_info=None):\n                                                    activity_zone)]\n     add_devices(sensors, True)\n \n+    for sensor in sensors:\n+        hass.bus.listen(EVENT_NEST_UPDATE,", "msg": "Use our dispatch helper instead, to communicate between component and its platforms."}
{"old_hunk": "@@ -51,23 +55,44 @@\n }, extra=vol.ALLOW_EXTRA)\n \n \n-def request_configuration(nest, hass, config):\n+async def async_nest_update_event_broker(hass, nest):\n+    \"\"\"\n+    Fire a EVENT_NEST_UPDATE event when nest stream API received data.\n+\n+    nest.update_event.wait will block the thread in most of time,\n+    so specific an executor to save default thread pool.\n+    \"\"\"\n+    _LOGGER.debug(\"listening nest.update_event\")\n+    with ThreadPoolExecutor(max_workers=1) as executor:", "hunk": "@@ -57,7 +57,7 @@ CONFIG_SCHEMA = vol.Schema({\n \n async def async_nest_update_event_broker(hass, nest):\n     \"\"\"\n-    Fire a EVENT_NEST_UPDATE event when nest stream API received data.\n+    Dispatch SIGNAL_NEST_UPDATE to devices when nest stream API received data.\n \n     nest.update_event.wait will block the thread in most of time,\n     so specific an executor to save default thread pool.\n", "comment": "I don't think we should do this. Can't python-nest allow a callback to be registered? The callback should be called by python-nest when a state update is needed. Otherwise waiting for the event should be done by a coroutine asynchronously. But the current event is a `threading.Event` so I don't think that will work as it is now.", "ids": [310831, "69b8f9ea13bbe624d6f3227e3ed642c2b5b2f7ba", "ff4bb8978631075622cc9b4dbb7bed678f545fcb"], "repo": "home-assistant-core", "ghid": 14656, "old": " async def async_nest_update_event_broker(hass, nest):\n     \"\"\"\n-    Fire a EVENT_NEST_UPDATE event when nest stream API received data.\n     nest.update_event.wait will block the thread in most of time,\n     so specific an executor to save default thread pool.", "new": " async def async_nest_update_event_broker(hass, nest):\n     \"\"\"\n+    Dispatch SIGNAL_NEST_UPDATE to devices when nest stream API received data.\n     nest.update_event.wait will block the thread in most of time,\n     so specific an executor to save default thread pool.", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -51,23 +55,44 @@\n }, extra=vol.ALLOW_EXTRA)\n \n \n-def request_configuration(nest, hass, config):\n+async def async_nest_update_event_broker(hass, nest):\n+    \"\"\"\n+    Fire a EVENT_NEST_UPDATE event when nest stream API received data.\n+\n+    nest.update_event.wait will block the thread in most of time,\n+    so specific an executor to save default thread pool.\n+    \"\"\"\n+    _LOGGER.debug(\"listening nest.update_event\")\n+    with ThreadPoolExecutor(max_workers=1) as executor:", "msg": "I don't think we should do this. Can't python-nest allow a callback to be registered? The callback should be called by python-nest when a state update is needed. Otherwise waiting for the event should be done by a coroutine asynchronously. But the current event is a `threading.Event` so I don't think that will work as it is now."}
{"old_hunk": "@@ -8,9 +8,9 @@\n import logging\n \n from homeassistant.components.binary_sensor import (BinarySensorDevice)\n+from homeassistant.components.nest import DATA_NEST, EVENT_NEST_UPDATE\n from homeassistant.components.sensor.nest import NestSensor", "hunk": "@@ -8,9 +8,10 @@ from itertools import chain\n import logging\n \n from homeassistant.components.binary_sensor import (BinarySensorDevice)\n-from homeassistant.components.nest import DATA_NEST, EVENT_NEST_UPDATE\n+from homeassistant.components.nest import DATA_NEST, SIGNAL_NEST_UPDATE\n from homeassistant.components.sensor.nest import NestSensor\n from homeassistant.const import CONF_MONITORED_CONDITIONS\n+from homeassistant.helpers.dispatcher import async_dispatcher_connect\n \n DEPENDENCIES = ['nest']\n \n", "comment": "It's not allowed to depend on another platform from a platform. You can define a common base nest entity class instead in the nest component and import that into the nest platforms.", "ids": [310830, "69b8f9ea13bbe624d6f3227e3ed642c2b5b2f7ba", "ff4bb8978631075622cc9b4dbb7bed678f545fcb"], "repo": "home-assistant-core", "ghid": 14656, "old": " import logging\n from homeassistant.components.binary_sensor import (BinarySensorDevice)\n-from homeassistant.components.nest import DATA_NEST, EVENT_NEST_UPDATE\n from homeassistant.components.sensor.nest import NestSensor\n from homeassistant.const import CONF_MONITORED_CONDITIONS\n DEPENDENCIES = ['nest']", "new": " import logging\n from homeassistant.components.binary_sensor import (BinarySensorDevice)\n+from homeassistant.components.nest import DATA_NEST, SIGNAL_NEST_UPDATE\n from homeassistant.components.sensor.nest import NestSensor\n from homeassistant.const import CONF_MONITORED_CONDITIONS\n+from homeassistant.helpers.dispatcher import async_dispatcher_connect\n DEPENDENCIES = ['nest']", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -8,9 +8,9 @@\n import logging\n \n from homeassistant.components.binary_sensor import (BinarySensorDevice)\n+from homeassistant.components.nest import DATA_NEST, EVENT_NEST_UPDATE\n from homeassistant.components.sensor.nest import NestSensor", "msg": "It's not allowed to depend on another platform from a platform. You can define a common base nest entity class instead in the nest component and import that into the nest platforms."}
{"old_hunk": "@@ -141,6 +149,36 @@ def update(self):\n             dt.start_of_local_day() + timedelta(days=1)\n         )\n \n+        # Handle event list for local calendar", "hunk": "@@ -149,7 +149,8 @@ class WebDavCalendarData(object):\n             dt.start_of_local_day() + timedelta(days=1)\n         )\n \n-        # Handle event list for local calendar\n+        # Get event list from the current calendar\n+        # Should we let users chose nb day before/after today ?\n         event_list = self.calendar.date_search(\n             dt.start_of_local_day() - timedelta(days=10),\n             dt.start_of_local_day() + timedelta(days=31)\n", "comment": "Why is local calendar things inside the caldav platform?", "ids": [310966, "b2a6c82b9581b60b03b30250bb95e33c1ac79b6a", "79dd01624b5c6edde147e33730bfad0568e41254"], "repo": "home-assistant-core", "ghid": 14702, "old": "             dt.start_of_local_day() + timedelta(days=1)\n         )\n-        # Handle event list for local calendar\n         event_list = self.calendar.date_search(\n             dt.start_of_local_day() - timedelta(days=10),\n             dt.start_of_local_day() + timedelta(days=31)", "new": "             dt.start_of_local_day() + timedelta(days=1)\n         )\n+        # Get event list from the current calendar\n+        # Should we let users chose nb day before/after today ?\n         event_list = self.calendar.date_search(\n             dt.start_of_local_day() - timedelta(days=10),\n             dt.start_of_local_day() + timedelta(days=31)", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -141,6 +149,36 @@ def update(self):\n             dt.start_of_local_day() + timedelta(days=1)\n         )\n \n+        # Handle event list for local calendar", "msg": "Why is local calendar things inside the caldav platform?"}
{"old_hunk": "@@ -26,6 +26,7 @@\n CONF_CUSTOM_CALENDARS = 'custom_calendars'\n CONF_CALENDAR = 'calendar'\n CONF_SEARCH = 'search'\n+CONF_COLOR = 'color'", "hunk": "@@ -26,7 +26,6 @@ CONF_CALENDARS = 'calendars'\n CONF_CUSTOM_CALENDARS = 'custom_calendars'\n CONF_CALENDAR = 'calendar'\n CONF_SEARCH = 'search'\n-CONF_COLOR = 'color'\n \n PLATFORM_SCHEMA = PLATFORM_SCHEMA.extend({\n     # pylint: disable=no-value-for-parameter\n", "comment": "Let's keep color out of this PR", "ids": [310965, "b2a6c82b9581b60b03b30250bb95e33c1ac79b6a", "79dd01624b5c6edde147e33730bfad0568e41254"], "repo": "home-assistant-core", "ghid": 14702, "old": " CONF_CUSTOM_CALENDARS = 'custom_calendars'\n CONF_CALENDAR = 'calendar'\n CONF_SEARCH = 'search'\n-CONF_COLOR = 'color'\n PLATFORM_SCHEMA = PLATFORM_SCHEMA.extend({\n     # pylint: disable=no-value-for-parameter", "new": " CONF_CUSTOM_CALENDARS = 'custom_calendars'\n CONF_CALENDAR = 'calendar'\n CONF_SEARCH = 'search'\n PLATFORM_SCHEMA = PLATFORM_SCHEMA.extend({\n     # pylint: disable=no-value-for-parameter", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -26,6 +26,7 @@\n CONF_CUSTOM_CALENDARS = 'custom_calendars'\n CONF_CALENDAR = 'calendar'\n CONF_SEARCH = 'search'\n+CONF_COLOR = 'color'", "msg": "Let's keep color out of this PR"}
{"old_hunk": "@@ -135,6 +163,8 @@ def cleanup(self):\n \n     def update(self):\n         \"\"\"Search for the next event.\"\"\"\n+        self._event_list = self.data.event_list", "hunk": "@@ -163,8 +163,6 @@ class CalendarEventDevice(Entity):\n \n     def update(self):\n         \"\"\"Search for the next event.\"\"\"\n-        self._event_list = self.data.event_list\n-\n         if not self.data or not self.data.update():\n             # update cached, don't do anything\n             return\n", "comment": "We should not cache event list in memory. We should generate them on the fly and then return from API.", "ids": [310964, "b2a6c82b9581b60b03b30250bb95e33c1ac79b6a", "79dd01624b5c6edde147e33730bfad0568e41254"], "repo": "home-assistant-core", "ghid": 14702, "old": "     def update(self):\n         \"\"\"Search for the next event.\"\"\"\n-        self._event_list = self.data.event_list\n-\n         if not self.data or not self.data.update():\n             # update cached, don't do anything\n             return", "new": "     def update(self):\n         \"\"\"Search for the next event.\"\"\"\n         if not self.data or not self.data.update():\n             # update cached, don't do anything\n             return", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -135,6 +163,8 @@ def cleanup(self):\n \n     def update(self):\n         \"\"\"Search for the next event.\"\"\"\n+        self._event_list = self.data.event_list", "msg": "We should not cache event list in memory. We should generate them on the fly and then return from API."}
{"old_hunk": "@@ -99,3 +100,8 @@ def update(self):\n \n         self.event = new_event\n         return True\n+\n+    @property\n+    def event_list(self):\n+        \"\"\"Return calendar event list.\"\"\"\n+        return self._event_list", "hunk": "@@ -92,11 +92,13 @@ class GoogleCalendarData(object):\n             if (not self.ignore_availability\n                     and 'transparency' in item.keys()):\n                 if item['transparency'] == 'opaque':\n-                    new_event = item\n-                    break\n+                    self._event_list.append(item)\n+                    if new_event is None:\n+                        new_event = item\n             else:\n-                new_event = item\n-                break\n+                self._event_list.append(item)\n+                if new_event is None:\n+                    new_event = item\n \n         self.event = new_event\n         return True\n", "comment": "This is always empty ?", "ids": [310967, "b2a6c82b9581b60b03b30250bb95e33c1ac79b6a", "79dd01624b5c6edde147e33730bfad0568e41254"], "repo": "home-assistant-core", "ghid": 14702, "old": "             if (not self.ignore_availability\n                     and 'transparency' in item.keys()):\n                 if item['transparency'] == 'opaque':\n-                    new_event = item\n-                    break\n             else:\n-                new_event = item\n-                break\n         self.event = new_event\n         return True", "new": "             if (not self.ignore_availability\n                     and 'transparency' in item.keys()):\n                 if item['transparency'] == 'opaque':\n+                    self._event_list.append(item)\n+                    if new_event is None:\n+                        new_event = item\n             else:\n+                self._event_list.append(item)\n+                if new_event is None:\n+                    new_event = item\n         self.event = new_event\n         return True", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -99,3 +100,8 @@ def update(self):\n \n         self.event = new_event\n         return True\n+\n+    @property\n+    def event_list(self):\n+        \"\"\"Return calendar event list.\"\"\"\n+        return self._event_list", "msg": "This is always empty ?"}
{"old_hunk": "@@ -43,6 +47,11 @@\n }, extra=vol.ALLOW_EXTRA)\n \n \n+def set_default_level(hass, level):", "hunk": "@@ -47,11 +47,6 @@ CONFIG_SCHEMA = vol.Schema({\n }, extra=vol.ALLOW_EXTRA)\n \n \n-def set_default_level(hass, level):\n-    \"\"\"Set default log level for components.\"\"\"\n-    hass.services.call(DOMAIN, SERVICE_SET_DEFAULT_LEVEL, level)\n-\n-\n def set_level(hass, logs):\n     \"\"\"Set log level for components.\"\"\"\n     hass.services.call(DOMAIN, SERVICE_SET_LEVEL, logs)\n", "comment": "No need to add a function. Won't be used by other components.", "ids": [311029, "c7ea470c3f0ad37725075c78984bf69603d21e1e", "c061c0d0562003f19743896dfb15e121efa79b83"], "repo": "home-assistant-core", "ghid": 14703, "old": " }, extra=vol.ALLOW_EXTRA)\n-def set_default_level(hass, level):\n-    \"\"\"Set default log level for components.\"\"\"\n-    hass.services.call(DOMAIN, SERVICE_SET_DEFAULT_LEVEL, level)\n-\n-\n def set_level(hass, logs):\n     \"\"\"Set log level for components.\"\"\"\n     hass.services.call(DOMAIN, SERVICE_SET_LEVEL, logs)", "new": " }, extra=vol.ALLOW_EXTRA)\n def set_level(hass, logs):\n     \"\"\"Set log level for components.\"\"\"\n     hass.services.call(DOMAIN, SERVICE_SET_LEVEL, logs)", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -43,6 +47,11 @@\n }, extra=vol.ALLOW_EXTRA)\n \n \n+def set_default_level(hass, level):", "msg": "No need to add a function. Won't be used by other components."}
{"old_hunk": "@@ -103,6 +109,11 @@ def set_log_levels(logpoints):\n             )\n         )\n \n+    # Set default log severity\n+    set_default_log_level('DEBUG')\n+    if LOGGER_DEFAULT in config.get(DOMAIN):", "hunk": "@@ -110,9 +110,10 @@ async def async_setup(hass, config):\n         )\n \n     # Set default log severity\n-    set_default_log_level('DEBUG')\n     if LOGGER_DEFAULT in config.get(DOMAIN):\n         set_default_log_level(config.get(DOMAIN)[LOGGER_DEFAULT])\n+    else:\n+        set_default_log_level('DEBUG')\n \n     logger = logging.getLogger('')\n     logger.setLevel(logging.NOTSET)\n", "comment": "This makes no sense, why call `set_default_log_level` twice, use if else", "ids": [311027, "c7ea470c3f0ad37725075c78984bf69603d21e1e", "c061c0d0562003f19743896dfb15e121efa79b83"], "repo": "home-assistant-core", "ghid": 14703, "old": "         )\n     # Set default log severity\n-    set_default_log_level('DEBUG')\n     if LOGGER_DEFAULT in config.get(DOMAIN):\n         set_default_log_level(config.get(DOMAIN)[LOGGER_DEFAULT])\n     logger = logging.getLogger('')\n     logger.setLevel(logging.NOTSET)", "new": "         )\n     # Set default log severity\n     if LOGGER_DEFAULT in config.get(DOMAIN):\n         set_default_log_level(config.get(DOMAIN)[LOGGER_DEFAULT])\n+    else:\n+        set_default_log_level('DEBUG')\n     logger = logging.getLogger('')\n     logger.setLevel(logging.NOTSET)", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -103,6 +109,11 @@ def set_log_levels(logpoints):\n             )\n         )\n \n+    # Set default log severity\n+    set_default_log_level('DEBUG')\n+    if LOGGER_DEFAULT in config.get(DOMAIN):", "msg": "This makes no sense, why call `set_default_log_level` twice, use if else"}
{"old_hunk": "@@ -247,10 +257,20 @@ def turn_on(self, **kwargs):\n         if rgb is None:\n             rgb = self._bulb.getRgb()\n \n-        self._bulb.setRgb(*tuple(rgb), brightness=brightness)\n+        if white is None and self._mode == MODE_RGBW:\n+            white = self.white_value\n \n-        if white is not None:\n-            self._bulb.setWarmWhite255(white)\n+        # handle W only mode (use brightness instead of white value)\n+        if self._mode == MODE_WHITE:\n+            self._bulb.setRgbw(0, 0, 0, w=brightness)\n+\n+        # handle RGBW mode\n+        elif self._mode == MODE_RGBW:\n+            self._bulb.setRgbw(*tuple(rgb), w=white, brightness=brightness)\n+\n+        # handle RGB mode\n+        else:\n+            self._bulb.setRgb(*tuple(rgb), brightness=brightness)", "hunk": "@@ -272,6 +272,9 @@ class FluxLight(Light):\n         else:\n             self._bulb.setRgb(*tuple(rgb), brightness=brightness)\n \n+        if not self.is_on:\n+            self._bulb.turnOn()\n+\n     def turn_off(self, **kwargs):\n         \"\"\"Turn the specified or all lights off.\"\"\"\n         self._bulb.turnOff()\n", "comment": "Could you confirm that calling turn_on(rgb=[5,0,0], brighness=None) will really set the RGB color to [5,0,0] and not to [255,0,0] or anything else deformed by the previous brightness settings you re-use on line 254 ?", "ids": [311050, "5877753089f3d1d6827f3f97670fe50b5c9dc232", "0ca1c4053220fb082670904d64cd57f9b5d40fdd"], "repo": "home-assistant-core", "ghid": 14713, "old": "         else:\n             self._bulb.setRgb(*tuple(rgb), brightness=brightness)\n     def turn_off(self, **kwargs):\n         \"\"\"Turn the specified or all lights off.\"\"\"\n         self._bulb.turnOff()", "new": "         else:\n             self._bulb.setRgb(*tuple(rgb), brightness=brightness)\n+        if not self.is_on:\n+            self._bulb.turnOn()\n+\n     def turn_off(self, **kwargs):\n         \"\"\"Turn the specified or all lights off.\"\"\"\n         self._bulb.turnOff()", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -247,10 +257,20 @@ def turn_on(self, **kwargs):\n         if rgb is None:\n             rgb = self._bulb.getRgb()\n \n-        self._bulb.setRgb(*tuple(rgb), brightness=brightness)\n+        if white is None and self._mode == MODE_RGBW:\n+            white = self.white_value\n \n-        if white is not None:\n-            self._bulb.setWarmWhite255(white)\n+        # handle W only mode (use brightness instead of white value)\n+        if self._mode == MODE_WHITE:\n+            self._bulb.setRgbw(0, 0, 0, w=brightness)\n+\n+        # handle RGBW mode\n+        elif self._mode == MODE_RGBW:\n+            self._bulb.setRgbw(*tuple(rgb), w=white, brightness=brightness)\n+\n+        # handle RGB mode\n+        else:\n+            self._bulb.setRgb(*tuple(rgb), brightness=brightness)", "msg": "Could you confirm that calling turn_on(rgb=[5,0,0], brighness=None) will really set the RGB color to [5,0,0] and not to [255,0,0] or anything else deformed by the previous brightness settings you re-use on line 254 ?"}
{"old_hunk": "@@ -9,9 +9,9 @@\n from homeassistant.components import climate\n from homeassistant.const import STATE_OFF, STATE_UNAVAILABLE\n from homeassistant.components.climate import (\n-               SUPPORT_OPERATION_MODE, SUPPORT_TARGET_TEMPERATURE,\n-               SUPPORT_FAN_MODE, SUPPORT_SWING_MODE, SUPPORT_HOLD_MODE,\n-               SUPPORT_AWAY_MODE, SUPPORT_AUX_HEAT)\n+    SUPPORT_OPERATION_MODE, SUPPORT_TARGET_TEMPERATURE,\n+    SUPPORT_FAN_MODE, SUPPORT_SWING_MODE, SUPPORT_HOLD_MODE,\n+    SUPPORT_AWAY_MODE, SUPPORT_AUX_HEAT, DEFAULT_MAX_TEMP, DEFAULT_MIN_TEMP)", "hunk": "@@ -9,9 +9,9 @@ from homeassistant.setup import setup_component\n from homeassistant.components import climate\n from homeassistant.const import STATE_OFF, STATE_UNAVAILABLE\n from homeassistant.components.climate import (\n-    SUPPORT_OPERATION_MODE, SUPPORT_TARGET_TEMPERATURE,\n-    SUPPORT_FAN_MODE, SUPPORT_SWING_MODE, SUPPORT_HOLD_MODE,\n-    SUPPORT_AWAY_MODE, SUPPORT_AUX_HEAT, DEFAULT_MAX_TEMP, DEFAULT_MIN_TEMP)\n+               SUPPORT_OPERATION_MODE, SUPPORT_TARGET_TEMPERATURE,\n+               SUPPORT_FAN_MODE, SUPPORT_SWING_MODE, SUPPORT_HOLD_MODE,\n+               SUPPORT_AWAY_MODE, SUPPORT_AUX_HEAT)\n from tests.common import (get_test_home_assistant, mock_mqtt_component,\n                           fire_mqtt_message, mock_component)\n \n", "comment": "I think changes in this file are not necessary for this PR", "ids": [311098, "cc264f415e746173aff8cbb182b8ddc81ddebb8d", "753fe8279ba019d376eeb1caafcd8640b4d29f98"], "repo": "home-assistant-core", "ghid": 14721, "old": " from homeassistant.components import climate\n from homeassistant.const import STATE_OFF, STATE_UNAVAILABLE\n from homeassistant.components.climate import (\n-    SUPPORT_OPERATION_MODE, SUPPORT_TARGET_TEMPERATURE,\n-    SUPPORT_FAN_MODE, SUPPORT_SWING_MODE, SUPPORT_HOLD_MODE,\n-    SUPPORT_AWAY_MODE, SUPPORT_AUX_HEAT, DEFAULT_MAX_TEMP, DEFAULT_MIN_TEMP)\n from tests.common import (get_test_home_assistant, mock_mqtt_component,\n                           fire_mqtt_message, mock_component)", "new": " from homeassistant.components import climate\n from homeassistant.const import STATE_OFF, STATE_UNAVAILABLE\n from homeassistant.components.climate import (\n+               SUPPORT_OPERATION_MODE, SUPPORT_TARGET_TEMPERATURE,\n+               SUPPORT_FAN_MODE, SUPPORT_SWING_MODE, SUPPORT_HOLD_MODE,\n+               SUPPORT_AWAY_MODE, SUPPORT_AUX_HEAT)\n from tests.common import (get_test_home_assistant, mock_mqtt_component,\n                           fire_mqtt_message, mock_component)", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -9,9 +9,9 @@\n from homeassistant.components import climate\n from homeassistant.const import STATE_OFF, STATE_UNAVAILABLE\n from homeassistant.components.climate import (\n-               SUPPORT_OPERATION_MODE, SUPPORT_TARGET_TEMPERATURE,\n-               SUPPORT_FAN_MODE, SUPPORT_SWING_MODE, SUPPORT_HOLD_MODE,\n-               SUPPORT_AWAY_MODE, SUPPORT_AUX_HEAT)\n+    SUPPORT_OPERATION_MODE, SUPPORT_TARGET_TEMPERATURE,\n+    SUPPORT_FAN_MODE, SUPPORT_SWING_MODE, SUPPORT_HOLD_MODE,\n+    SUPPORT_AWAY_MODE, SUPPORT_AUX_HEAT, DEFAULT_MAX_TEMP, DEFAULT_MIN_TEMP)", "msg": "I think changes in this file are not necessary for this PR"}
{"old_hunk": "@@ -268,7 +267,8 @@ def min_temp(self):\n         if self._min_temp:\n             return self._min_temp\n \n-        return DEFAULT_MIN_TEMP\n+        # get default temp from super class\n+        return ClimateDevice.min_temp.fget(self)", "hunk": "@@ -268,7 +268,7 @@ class GenericThermostat(ClimateDevice):\n             return self._min_temp\n \n         # get default temp from super class\n-        return ClimateDevice.min_temp.fget(self)\n+        return super().min_temp\n \n     @property\n     def max_temp(self):\n", "comment": "Why can't you call `super().min_temp` like you do in the other platforms?", "ids": [311113, "299d47505e25bdba63fbb5a87e0258f70d88d62a", "bd6bb2334d1fc8c4739381456a88c99d77e72d9d"], "repo": "home-assistant-core", "ghid": 14730, "old": "             return self._min_temp\n         # get default temp from super class\n-        return ClimateDevice.min_temp.fget(self)\n     @property\n     def max_temp(self):", "new": "             return self._min_temp\n         # get default temp from super class\n+        return super().min_temp\n     @property\n     def max_temp(self):", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -268,7 +267,8 @@ def min_temp(self):\n         if self._min_temp:\n             return self._min_temp\n \n-        return DEFAULT_MIN_TEMP\n+        # get default temp from super class\n+        return ClimateDevice.min_temp.fget(self)", "msg": "Why can't you call `super().min_temp` like you do in the other platforms?"}
{"old_hunk": "@@ -191,6 +192,14 @@ def current_humidity(self):\n         \"\"\"Return the current humidity.\"\"\"\n         return self._measurements['humidity']\n \n+    @property\n+    def current_battery(self):\n+        \"\"\"Return the current battery voltage.\"\"\"\n+        if 'batteryVoltage' in self._measurements:\n+            return self._measurements['batteryVoltage']\n+        else:\n+            return None", "hunk": "@@ -195,10 +195,7 @@ class SensiboClimate(ClimateDevice):\n     @property\n     def current_battery(self):\n         \"\"\"Return the current battery voltage.\"\"\"\n-        if 'batteryVoltage' in self._measurements:\n-            return self._measurements['batteryVoltage']\n-        else:\n-            return None\n+        return self._measurements.get('batteryVoltage')\n \n     @property\n     def current_temperature(self):\n", "comment": "Using `return self._measurements.get('batteryVoltage')` is enough.", "ids": [311127, "eef87cc9f3dbaa6ba7b12ee962d56051e0741c75", "14ea6d9bd5f8a026875e96cdb6917883b6fe27f6"], "repo": "home-assistant-core", "ghid": 14735, "old": "     @property\n     def current_battery(self):\n         \"\"\"Return the current battery voltage.\"\"\"\n-        if 'batteryVoltage' in self._measurements:\n-            return self._measurements['batteryVoltage']\n-        else:\n-            return None\n     @property\n     def current_temperature(self):", "new": "     @property\n     def current_battery(self):\n         \"\"\"Return the current battery voltage.\"\"\"\n+        return self._measurements.get('batteryVoltage')\n     @property\n     def current_temperature(self):", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -191,6 +192,14 @@ def current_humidity(self):\n         \"\"\"Return the current humidity.\"\"\"\n         return self._measurements['humidity']\n \n+    @property\n+    def current_battery(self):\n+        \"\"\"Return the current battery voltage.\"\"\"\n+        if 'batteryVoltage' in self._measurements:\n+            return self._measurements['batteryVoltage']\n+        else:\n+            return None", "msg": "Using `return self._measurements.get('batteryVoltage')` is enough."}
{"old_hunk": "@@ -105,14 +104,19 @@ def setup_platform(hass, config, add_devices, discovery_info=None):\n     add_devices(sensors, True)\n \n \n-class NestBinarySensor(NestSensor, BinarySensorDevice):\n+class NestBinarySensor(NestSensorDevice, BinarySensorDevice):\n     \"\"\"Represents a Nest binary sensor.\"\"\"\n \n     @property\n     def is_on(self):\n         \"\"\"Return true if the binary sensor is on.\"\"\"\n         return self._state\n \n+    @property\n+    def device_class(self):\n+        \"\"\"Return the device class of the binary sensor.\"\"\"\n+        return _VALID_BINARY_SENSOR_TYPES.get(self.variable, None)", "hunk": "@@ -115,7 +115,7 @@ class NestBinarySensor(NestSensorDevice, BinarySensorDevice):\n     @property\n     def device_class(self):\n         \"\"\"Return the device class of the binary sensor.\"\"\"\n-        return _VALID_BINARY_SENSOR_TYPES.get(self.variable, None)\n+        return _VALID_BINARY_SENSOR_TYPES.get(self.variable)\n \n     def update(self):\n         \"\"\"Retrieve latest state.\"\"\"\n", "comment": "`None` is the default returned value of `dict.get(key)` when the key is missing in the dict.", "ids": [311141, "2d6942b99183b1ad26b3ccfda73519019f94ed3c", "242e4ef31e4c99f5def385a09a71c7186bc53089"], "repo": "home-assistant-core", "ghid": 14746, "old": "     @property\n     def device_class(self):\n         \"\"\"Return the device class of the binary sensor.\"\"\"\n-        return _VALID_BINARY_SENSOR_TYPES.get(self.variable, None)\n     def update(self):\n         \"\"\"Retrieve latest state.\"\"\"", "new": "     @property\n     def device_class(self):\n         \"\"\"Return the device class of the binary sensor.\"\"\"\n+        return _VALID_BINARY_SENSOR_TYPES.get(self.variable)\n     def update(self):\n         \"\"\"Retrieve latest state.\"\"\"", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -105,14 +104,19 @@ def setup_platform(hass, config, add_devices, discovery_info=None):\n     add_devices(sensors, True)\n \n \n-class NestBinarySensor(NestSensor, BinarySensorDevice):\n+class NestBinarySensor(NestSensorDevice, BinarySensorDevice):\n     \"\"\"Represents a Nest binary sensor.\"\"\"\n \n     @property\n     def is_on(self):\n         \"\"\"Return true if the binary sensor is on.\"\"\"\n         return self._state\n \n+    @property\n+    def device_class(self):\n+        \"\"\"Return the device class of the binary sensor.\"\"\"\n+        return _VALID_BINARY_SENSOR_TYPES.get(self.variable, None)", "msg": "`None` is the default returned value of `dict.get(key)` when the key is missing in the dict."}
{"old_hunk": "@@ -65,7 +64,7 @@ def setup_platform(hass, config, add_devices, discovery_info=None):\n \n     # Add all available binary sensors if no Nest binary sensor config is set\n     if discovery_info == {}:\n-        conditions = _VALID_BINARY_SENSOR_TYPES\n+        conditions = _VALID_BINARY_SENSOR_TYPES.keys()", "hunk": "@@ -64,7 +64,7 @@ def setup_platform(hass, config, add_devices, discovery_info=None):\n \n     # Add all available binary sensors if no Nest binary sensor config is set\n     if discovery_info == {}:\n-        conditions = _VALID_BINARY_SENSOR_TYPES.keys()\n+        conditions = _VALID_BINARY_SENSOR_TYPES\n     else:\n         conditions = discovery_info.get(CONF_MONITORED_CONDITIONS, {})\n \n", "comment": "This change isn't needed.", "ids": [311140, "2d6942b99183b1ad26b3ccfda73519019f94ed3c", "21f4dc62f0b43414acc7296c2335cb8b9f30472d"], "repo": "home-assistant-core", "ghid": 14746, "old": "     # Add all available binary sensors if no Nest binary sensor config is set\n     if discovery_info == {}:\n-        conditions = _VALID_BINARY_SENSOR_TYPES.keys()\n     else:\n         conditions = discovery_info.get(CONF_MONITORED_CONDITIONS, {})", "new": "     # Add all available binary sensors if no Nest binary sensor config is set\n     if discovery_info == {}:\n+        conditions = _VALID_BINARY_SENSOR_TYPES\n     else:\n         conditions = discovery_info.get(CONF_MONITORED_CONDITIONS, {})", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -65,7 +64,7 @@ def setup_platform(hass, config, add_devices, discovery_info=None):\n \n     # Add all available binary sensors if no Nest binary sensor config is set\n     if discovery_info == {}:\n-        conditions = _VALID_BINARY_SENSOR_TYPES\n+        conditions = _VALID_BINARY_SENSOR_TYPES.keys()", "msg": "This change isn't needed."}
{"old_hunk": "@@ -44,16 +45,21 @@ def get(self, request):\n     @asyncio.coroutine\n     def _handle(self, hass, data):\n         \"\"\"Handle requests to the myStrom endpoint.\"\"\"\n-        button_action = list(data.keys())[0]\n-        button_id = data[button_action]\n-        entity_id = '{}.{}_{}'.format(DOMAIN, button_id, button_action)\n-\n-        if button_action not in ['single', 'double', 'long', 'touch']:\n+        button_action = None\n+        parameter_list = list(data.keys())\n+        for parameter in parameter_list:", "hunk": "@@ -45,12 +45,9 @@ class MyStromView(HomeAssistantView):\n     @asyncio.coroutine\n     def _handle(self, hass, data):\n         \"\"\"Handle requests to the myStrom endpoint.\"\"\"\n-        button_action = None\n-        parameter_list = list(data.keys())\n-        for parameter in parameter_list:\n-            if parameter in self.supported_actions:\n-                button_action = parameter\n-                break\n+        button_action = next((\n+            parameter for parameter in data\n+            if parameter in self.supported_actions), None)\n \n         if button_action is None:\n             _LOGGER.error(\n", "comment": "```py button_action = next(( parameter for parameter in data if parameter in self.supported_actions), None) ```", "ids": [311430, "da2c47a7bf15c30ca92c1f5aa462c6867150f8b3", "fdf1599a596a1ce3ea3a6f0d47c79e6a9f05bbc6"], "repo": "home-assistant-core", "ghid": 14815, "old": "     @asyncio.coroutine\n     def _handle(self, hass, data):\n         \"\"\"Handle requests to the myStrom endpoint.\"\"\"\n-        button_action = None\n-        parameter_list = list(data.keys())\n-        for parameter in parameter_list:\n-            if parameter in self.supported_actions:\n-                button_action = parameter\n-                break\n         if button_action is None:\n             _LOGGER.error(", "new": "     @asyncio.coroutine\n     def _handle(self, hass, data):\n         \"\"\"Handle requests to the myStrom endpoint.\"\"\"\n+        button_action = next((\n+            parameter for parameter in data\n+            if parameter in self.supported_actions), None)\n         if button_action is None:\n             _LOGGER.error(", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -44,16 +45,21 @@ def get(self, request):\n     @asyncio.coroutine\n     def _handle(self, hass, data):\n         \"\"\"Handle requests to the myStrom endpoint.\"\"\"\n-        button_action = list(data.keys())[0]\n-        button_id = data[button_action]\n-        entity_id = '{}.{}_{}'.format(DOMAIN, button_id, button_action)\n-\n-        if button_action not in ['single', 'double', 'long', 'touch']:\n+        button_action = None\n+        parameter_list = list(data.keys())\n+        for parameter in parameter_list:", "msg": "```py button_action = next(( parameter for parameter in data if parameter in self.supported_actions), None) ```"}
{"old_hunk": "@@ -135,7 +144,7 @@ def brand(self):\n     @property\n     def should_poll(self):\n         \"\"\"Camera should poll periodically.\"\"\"\n-        return True\n+        return False", "hunk": "@@ -141,11 +141,6 @@ class ArloCam(Camera):\n         \"\"\"Return the camera brand.\"\"\"\n         return DEFAULT_BRAND\n \n-    @property\n-    def should_poll(self):\n-        \"\"\"Camera should poll periodically.\"\"\"\n-        return False\n-\n     @property\n     def motion_detection_enabled(self):\n         \"\"\"Return the camera motion detection status.\"\"\"\n", "comment": "This is the default for cameras.", "ids": [311444, "da74c936aab3cc4e4b9407fb38cb37cb12828875", "377a9b7782652a882dd41655e0ece8d1e7f3eacf"], "repo": "home-assistant-core", "ghid": 14823, "old": "         \"\"\"Return the camera brand.\"\"\"\n         return DEFAULT_BRAND\n-    @property\n-    def should_poll(self):\n-        \"\"\"Camera should poll periodically.\"\"\"\n-        return False\n-\n     @property\n     def motion_detection_enabled(self):\n         \"\"\"Return the camera motion detection status.\"\"\"", "new": "         \"\"\"Return the camera brand.\"\"\"\n         return DEFAULT_BRAND\n     @property\n     def motion_detection_enabled(self):\n         \"\"\"Return the camera motion detection status.\"\"\"", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -135,7 +144,7 @@ def brand(self):\n     @property\n     def should_poll(self):\n         \"\"\"Camera should poll periodically.\"\"\"\n-        return True\n+        return False", "msg": "This is the default for cameras."}
{"old_hunk": "@@ -145,7 +154,7 @@ def motion_detection_enabled(self):\n     def set_base_station_mode(self, mode):\n         \"\"\"Set the mode in the base station.\"\"\"\n         # Get the list of base stations identified by library\n-        base_stations = self.hass.data[DATA_ARLO].base_stations\n+        base_stations = self.hass.data.get(DATA_ARLO).hub.base_stations", "hunk": "@@ -154,7 +154,7 @@ class ArloCam(Camera):\n     def set_base_station_mode(self, mode):\n         \"\"\"Set the mode in the base station.\"\"\"\n         # Get the list of base stations identified by library\n-        base_stations = self.hass.data.get(DATA_ARLO).hub.base_stations\n+        base_stations = self.hass.data[DATA_ARLO].base_stations\n \n         # Some Arlo cameras does not have base station\n         # So check if there is base station detected first\n", "comment": "Don't change to `dict.get`. We know that the key will be in the dict.", "ids": [311443, "da74c936aab3cc4e4b9407fb38cb37cb12828875", "377a9b7782652a882dd41655e0ece8d1e7f3eacf"], "repo": "home-assistant-core", "ghid": 14823, "old": "     def set_base_station_mode(self, mode):\n         \"\"\"Set the mode in the base station.\"\"\"\n         # Get the list of base stations identified by library\n-        base_stations = self.hass.data.get(DATA_ARLO).hub.base_stations\n         # Some Arlo cameras does not have base station\n         # So check if there is base station detected first", "new": "     def set_base_station_mode(self, mode):\n         \"\"\"Set the mode in the base station.\"\"\"\n         # Get the list of base stations identified by library\n+        base_stations = self.hass.data[DATA_ARLO].base_stations\n         # Some Arlo cameras does not have base station\n         # So check if there is base station detected first", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -145,7 +154,7 @@ def motion_detection_enabled(self):\n     def set_base_station_mode(self, mode):\n         \"\"\"Set the mode in the base station.\"\"\"\n         # Get the list of base stations identified by library\n-        base_stations = self.hass.data[DATA_ARLO].base_stations\n+        base_stations = self.hass.data.get(DATA_ARLO).hub.base_stations", "msg": "Don't change to `dict.get`. We know that the key will be in the dict."}
{"old_hunk": "@@ -55,4 +75,25 @@ def setup(hass, config):\n             title=NOTIFICATION_TITLE,\n             notification_id=NOTIFICATION_ID)\n         return False\n+\n+    def hub_refresh(event_time):\n+        \"\"\"Call ArloHub to refresh information.\"\"\"\n+        _LOGGER.info(\"Updating Arlo Hub component\")\n+        hass.data[DATA_ARLO].hub.update(update_cameras=True,\n+                                        update_base_station=True)\n+        dispatcher_send(hass, SIGNAL_UPDATE_ARLO)\n+\n+    # register service\n+    hass.services.register(DOMAIN, 'update', hub_refresh)\n+\n+    # register scan interval for ArloHub\n+    track_time_interval(hass, hub_refresh, scan_interval)\n     return True\n+\n+\n+class ArloHub(object):\n+    \"\"\"Representation of the base Arlo hub component.\"\"\"\n+\n+    def __init__(self, hub):\n+        \"\"\"Initialize the entity.\"\"\"\n+        self.hub = hub", "hunk": "@@ -89,11 +89,3 @@ def setup(hass, config):\n     # register scan interval for ArloHub\n     track_time_interval(hass, hub_refresh, scan_interval)\n     return True\n-\n-\n-class ArloHub(object):\n-    \"\"\"Representation of the base Arlo hub component.\"\"\"\n-\n-    def __init__(self, hub):\n-        \"\"\"Initialize the entity.\"\"\"\n-        self.hub = hub\n", "comment": "Why store the hub on another instance if it's just that attribute and no methods needed?", "ids": [311436, "da74c936aab3cc4e4b9407fb38cb37cb12828875", "377a9b7782652a882dd41655e0ece8d1e7f3eacf"], "repo": "home-assistant-core", "ghid": 14823, "old": "     # register scan interval for ArloHub\n     track_time_interval(hass, hub_refresh, scan_interval)\n     return True\n-\n-\n-class ArloHub(object):\n-    \"\"\"Representation of the base Arlo hub component.\"\"\"\n-\n-    def __init__(self, hub):\n-        \"\"\"Initialize the entity.\"\"\"\n-        self.hub = hub", "new": "     # register scan interval for ArloHub\n     track_time_interval(hass, hub_refresh, scan_interval)\n     return True", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -55,4 +75,25 @@ def setup(hass, config):\n             title=NOTIFICATION_TITLE,\n             notification_id=NOTIFICATION_ID)\n         return False\n+\n+    def hub_refresh(event_time):\n+        \"\"\"Call ArloHub to refresh information.\"\"\"\n+        _LOGGER.info(\"Updating Arlo Hub component\")\n+        hass.data[DATA_ARLO].hub.update(update_cameras=True,\n+                                        update_base_station=True)\n+        dispatcher_send(hass, SIGNAL_UPDATE_ARLO)\n+\n+    # register service\n+    hass.services.register(DOMAIN, 'update', hub_refresh)\n+\n+    # register scan interval for ArloHub\n+    track_time_interval(hass, hub_refresh, scan_interval)\n     return True\n+\n+\n+class ArloHub(object):\n+    \"\"\"Representation of the base Arlo hub component.\"\"\"\n+\n+    def __init__(self, hub):\n+        \"\"\"Initialize the entity.\"\"\"\n+        self.hub = hub", "msg": "Why store the hub on another instance if it's just that attribute and no methods needed?"}
{"old_hunk": "@@ -69,25 +68,29 @@\n \n     hass.data.setdefault(DOMAIN, {})\n     accesspoints = config.get(DOMAIN, [])\n+\n+    # Load homematicip_cloud even not accesspoints are defined\n+    if accesspoints == [{}]:\n+        return True\n+\n     for conf in accesspoints:\n+        apid = conf[CONF_ACCESSPOINT].replace('-', '').upper()", "hunk": "@@ -69,10 +69,6 @@ async def async_setup(hass, config):\n     hass.data.setdefault(DOMAIN, {})\n     accesspoints = config.get(DOMAIN, [])\n \n-    # Load homematicip_cloud even not accesspoints are defined\n-    if accesspoints == [{}]:\n-        return True\n-\n     for conf in accesspoints:\n         apid = conf[CONF_ACCESSPOINT].replace('-', '').upper()\n         if apid in hass.data[DOMAIN]:\n", "comment": "Why do we need to do this string replace everywhere for this key?", "ids": [311548, "96ca168e2eb974f31476f46f1155efcfdabf04c2", "43e4eb1f62fc6ef22ee78773de0168e25ae1c2f8"], "repo": "home-assistant-core", "ghid": 14861, "old": "     hass.data.setdefault(DOMAIN, {})\n     accesspoints = config.get(DOMAIN, [])\n-    # Load homematicip_cloud even not accesspoints are defined\n-    if accesspoints == [{}]:\n-        return True\n-\n     for conf in accesspoints:\n         apid = conf[CONF_ACCESSPOINT].replace('-', '').upper()\n         if apid in hass.data[DOMAIN]:", "new": "     hass.data.setdefault(DOMAIN, {})\n     accesspoints = config.get(DOMAIN, [])\n     for conf in accesspoints:\n         apid = conf[CONF_ACCESSPOINT].replace('-', '').upper()\n         if apid in hass.data[DOMAIN]:", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -69,25 +68,29 @@\n \n     hass.data.setdefault(DOMAIN, {})\n     accesspoints = config.get(DOMAIN, [])\n+\n+    # Load homematicip_cloud even not accesspoints are defined\n+    if accesspoints == [{}]:\n+        return True\n+\n     for conf in accesspoints:\n+        apid = conf[CONF_ACCESSPOINT].replace('-', '').upper()", "msg": "Why do we need to do this string replace everywhere for this key?"}
{"old_hunk": "@@ -17,12 +17,14 @@\n from homeassistant.helpers.entity import Entity\n from homeassistant.core import callback\n \n+from .const import DOMAIN, CONF_ACCESSPOINT, CONF_AUTHTOKEN, CONF_NAME\n+# Loading the config flow file will register the flow\n+from .config_flow import HomematicipCloudFlowHandler", "hunk": "@@ -19,7 +19,7 @@ from homeassistant.core import callback\n \n from .const import DOMAIN, CONF_ACCESSPOINT, CONF_AUTHTOKEN, CONF_NAME\n # Loading the config flow file will register the flow\n-from .config_flow import HomematicipCloudFlowHandler\n+from .config_flow import HomematicipCloudFlowHandler    # noqa: F401\n \n REQUIREMENTS = ['homematicip==0.9.4']\n \n", "comment": "'.config_flow.HomematicipCloudFlowHandler' imported but unused", "ids": [311546, "96ca168e2eb974f31476f46f1155efcfdabf04c2", "596c6970d1013b484af761441497ab63f3332407"], "repo": "home-assistant-core", "ghid": 14861, "old": " from .const import DOMAIN, CONF_ACCESSPOINT, CONF_AUTHTOKEN, CONF_NAME\n # Loading the config flow file will register the flow\n-from .config_flow import HomematicipCloudFlowHandler\n REQUIREMENTS = ['homematicip==0.9.4']", "new": " from .const import DOMAIN, CONF_ACCESSPOINT, CONF_AUTHTOKEN, CONF_NAME\n # Loading the config flow file will register the flow\n+from .config_flow import HomematicipCloudFlowHandler    # noqa: F401\n REQUIREMENTS = ['homematicip==0.9.4']", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -17,12 +17,14 @@\n from homeassistant.helpers.entity import Entity\n from homeassistant.core import callback\n \n+from .const import DOMAIN, CONF_ACCESSPOINT, CONF_AUTHTOKEN, CONF_NAME\n+# Loading the config flow file will register the flow\n+from .config_flow import HomematicipCloudFlowHandler", "msg": "'.config_flow.HomematicipCloudFlowHandler' imported but unused"}
{"old_hunk": "@@ -0,0 +1,87 @@\n+\"\"\"Config flow to configure HomematicIP Cloud.\"\"\"\n+import json\n+\n+import voluptuous as vol\n+\n+from homeassistant import config_entries, data_entry_flow\n+\n+from .const import DOMAIN, _LOGGER, CONF_ACCESSPOINT, CONF_PIN, CONF_NAME\n+\n+\n+@config_entries.HANDLERS.register(DOMAIN)\n+class HomematicipCloudFlowHandler(data_entry_flow.FlowHandler):\n+    \"\"\"Config flow HomematicIP Cloud.\"\"\"\n+\n+    VERSION = 1\n+\n+    def __init__(self):\n+        \"\"\"Initialize HomematicIP Cloud config flow.\"\"\"\n+        self.hmip_auth = None\n+        self.hmip_apid = None\n+        self.hmip_name = None\n+\n+    async def async_step_init(self, user_input=None):\n+        \"\"\"Handle a flow start.\"\"\"\n+        from homematicip.home import Home\n+        from homematicip.auth import Auth\n+\n+        errors = {}\n+\n+        if user_input is not None:\n+            if user_input[CONF_ACCESSPOINT]:\n+                self.hmip_apid = user_input[CONF_ACCESSPOINT]\n+                self.hmip_name = user_input[CONF_NAME]\n+                _LOGGER.info(\"Create new authtoken for %s\", self.hmip_apid)\n+\n+                # Create new authtoken for the accesspoint\n+                # Threaded version needs to be migrated to async\n+                home = Home()\n+                home.init(self.hmip_apid)\n+                self.hmip_auth = Auth(home)\n+                if user_input[CONF_PIN]:\n+                    self.hmip_auth.pin = user_input[CONF_PIN]\n+                res = self.hmip_auth.connectionRequest(self.hmip_apid,\n+                                                       'HomeAssistant')\n+\n+                # Connection established\n+                if res.status_code == 200:\n+                    _LOGGER.info(\"Connection established\")\n+                    return await self.async_step_link()\n+\n+                error_code = json.loads(res.text)['errorCode']\n+                if error_code == 'INVALID_PIN':\n+                    errors['base'] = 'invalid_pin'", "hunk": "@@ -50,7 +50,7 @@ class HomematicipCloudFlowHandler(data_entry_flow.FlowHandler):\n \n                 error_code = json.loads(res.text)['errorCode']\n                 if error_code == 'INVALID_PIN':\n-                    errors['base'] = 'invalid_pin'\n+                    errors[CONF_PIN] = 'invalid_pin'\n                 else:\n                     errors['base'] = 'register_failed'\n \n", "comment": "If the error concerns a specific form key, use the same key in `errors` as for the form. ```py errors[CONF_PIN] = 'invalid_pin' ``` This will make the error message show next to the concerned form slot.", "ids": [311549, "96ca168e2eb974f31476f46f1155efcfdabf04c2", "89adf3fa8db959d521c4f2276a6d33f638931431"], "repo": "home-assistant-core", "ghid": 14861, "old": "                 error_code = json.loads(res.text)['errorCode']\n                 if error_code == 'INVALID_PIN':\n-                    errors['base'] = 'invalid_pin'\n                 else:\n                     errors['base'] = 'register_failed'", "new": "                 error_code = json.loads(res.text)['errorCode']\n                 if error_code == 'INVALID_PIN':\n+                    errors[CONF_PIN] = 'invalid_pin'\n                 else:\n                     errors['base'] = 'register_failed'", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -0,0 +1,87 @@\n+\"\"\"Config flow to configure HomematicIP Cloud.\"\"\"\n+import json\n+\n+import voluptuous as vol\n+\n+from homeassistant import config_entries, data_entry_flow\n+\n+from .const import DOMAIN, _LOGGER, CONF_ACCESSPOINT, CONF_PIN, CONF_NAME\n+\n+\n+@config_entries.HANDLERS.register(DOMAIN)\n+class HomematicipCloudFlowHandler(data_entry_flow.FlowHandler):\n+    \"\"\"Config flow HomematicIP Cloud.\"\"\"\n+\n+    VERSION = 1\n+\n+    def __init__(self):\n+        \"\"\"Initialize HomematicIP Cloud config flow.\"\"\"\n+        self.hmip_auth = None\n+        self.hmip_apid = None\n+        self.hmip_name = None\n+\n+    async def async_step_init(self, user_input=None):\n+        \"\"\"Handle a flow start.\"\"\"\n+        from homematicip.home import Home\n+        from homematicip.auth import Auth\n+\n+        errors = {}\n+\n+        if user_input is not None:\n+            if user_input[CONF_ACCESSPOINT]:\n+                self.hmip_apid = user_input[CONF_ACCESSPOINT]\n+                self.hmip_name = user_input[CONF_NAME]\n+                _LOGGER.info(\"Create new authtoken for %s\", self.hmip_apid)\n+\n+                # Create new authtoken for the accesspoint\n+                # Threaded version needs to be migrated to async\n+                home = Home()\n+                home.init(self.hmip_apid)\n+                self.hmip_auth = Auth(home)\n+                if user_input[CONF_PIN]:\n+                    self.hmip_auth.pin = user_input[CONF_PIN]\n+                res = self.hmip_auth.connectionRequest(self.hmip_apid,\n+                                                       'HomeAssistant')\n+\n+                # Connection established\n+                if res.status_code == 200:\n+                    _LOGGER.info(\"Connection established\")\n+                    return await self.async_step_link()\n+\n+                error_code = json.loads(res.text)['errorCode']\n+                if error_code == 'INVALID_PIN':\n+                    errors['base'] = 'invalid_pin'", "msg": "If the error concerns a specific form key, use the same key in `errors` as for the form. ```py errors[CONF_PIN] = 'invalid_pin' ``` This will make the error message show next to the concerned form slot."}
{"old_hunk": "@@ -31,15 +33,12 @@\n     'climate',\n ]\n \n-CONF_NAME = 'name'\n-CONF_ACCESSPOINT = 'accesspoint'\n-CONF_AUTHTOKEN = 'authtoken'\n-\n+# All parameter are optional in order to support config entry loading", "hunk": "@@ -33,12 +33,11 @@ COMPONENTS = [\n     'climate',\n ]\n \n-# All parameter are optional in order to support config entry loading\n CONFIG_SCHEMA = vol.Schema({\n     vol.Optional(DOMAIN, default=[]): vol.All(cv.ensure_list, [vol.Schema({\n         vol.Optional(CONF_NAME): vol.Any(cv.string),\n-        vol.Optional(CONF_ACCESSPOINT): cv.string,\n-        vol.Optional(CONF_AUTHTOKEN): cv.string,\n+        vol.Required(CONF_ACCESSPOINT): cv.string,\n+        vol.Required(CONF_AUTHTOKEN): cv.string,\n     })]),\n }, extra=vol.ALLOW_EXTRA)\n \n", "comment": "Config entries doesn't use the config schema by default, so we shouldn't change this. It doesn't make sense to allow a config without necessary items. Instead check if the config dict for the DOMAIN is empty. If it's empty we know the component isn't being setup via normal bootstrap but from entry.", "ids": [311547, "96ca168e2eb974f31476f46f1155efcfdabf04c2", "43e4eb1f62fc6ef22ee78773de0168e25ae1c2f8"], "repo": "home-assistant-core", "ghid": 14861, "old": "     'climate',\n ]\n-# All parameter are optional in order to support config entry loading\n CONFIG_SCHEMA = vol.Schema({\n     vol.Optional(DOMAIN, default=[]): vol.All(cv.ensure_list, [vol.Schema({\n         vol.Optional(CONF_NAME): vol.Any(cv.string),\n-        vol.Optional(CONF_ACCESSPOINT): cv.string,\n-        vol.Optional(CONF_AUTHTOKEN): cv.string,\n     })]),\n }, extra=vol.ALLOW_EXTRA)", "new": "     'climate',\n ]\n CONFIG_SCHEMA = vol.Schema({\n     vol.Optional(DOMAIN, default=[]): vol.All(cv.ensure_list, [vol.Schema({\n         vol.Optional(CONF_NAME): vol.Any(cv.string),\n+        vol.Required(CONF_ACCESSPOINT): cv.string,\n+        vol.Required(CONF_AUTHTOKEN): cv.string,\n     })]),\n }, extra=vol.ALLOW_EXTRA)", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -31,15 +33,12 @@\n     'climate',\n ]\n \n-CONF_NAME = 'name'\n-CONF_ACCESSPOINT = 'accesspoint'\n-CONF_AUTHTOKEN = 'authtoken'\n-\n+# All parameter are optional in order to support config entry loading", "msg": "Config entries doesn't use the config schema by default, so we shouldn't change this. It doesn't make sense to allow a config without necessary items. Instead check if the config dict for the DOMAIN is empty. If it's empty we know the component isn't being setup via normal bootstrap but from entry."}
{"old_hunk": "@@ -11,31 +11,37 @@\n \n from homeassistant.components.weather import (\n     ATTR_FORECAST_CONDITION, ATTR_FORECAST_PRECIPITATION, ATTR_FORECAST_TEMP,\n-    ATTR_FORECAST_TIME, PLATFORM_SCHEMA, WeatherEntity)\n+    ATTR_FORECAST_TEMP_LOW, ATTR_FORECAST_TIME, PLATFORM_SCHEMA, WeatherEntity)\n from homeassistant.const import (\n-    CONF_API_KEY, CONF_LATITUDE, CONF_LONGITUDE, CONF_NAME, STATE_UNKNOWN,\n-    TEMP_CELSIUS)\n+    CONF_API_KEY, CONF_MODE, CONF_LATITUDE, CONF_LONGITUDE, CONF_NAME,\n+    STATE_UNKNOWN, TEMP_CELSIUS)\n import homeassistant.helpers.config_validation as cv\n from homeassistant.util import Throttle\n \n REQUIREMENTS = ['pyowm==2.8.0']\n \n _LOGGER = logging.getLogger(__name__)\n \n-ATTRIBUTION = 'Data provided by OpenWeatherMap'\n+ATTR_FORECAST_WIND_SPEED = 'wind_speed'\n+ATTR_FORECAST_WIND_BEARING = 'wind_bearing'\n+\n+ATTRIBUTION_DAILY = \"Weather data provided by OpenWeatherMap\"\n+ATTRIBUTION_HOURLY = \"Data provided by OpenWeatherMap\"", "hunk": "@@ -13,8 +13,8 @@ from homeassistant.components.weather import (\n     ATTR_FORECAST_CONDITION, ATTR_FORECAST_PRECIPITATION, ATTR_FORECAST_TEMP,\n     ATTR_FORECAST_TEMP_LOW, ATTR_FORECAST_TIME, PLATFORM_SCHEMA, WeatherEntity)\n from homeassistant.const import (\n-    CONF_API_KEY, CONF_MODE, CONF_LATITUDE, CONF_LONGITUDE, CONF_NAME,\n-    STATE_UNKNOWN, TEMP_CELSIUS)\n+    CONF_API_KEY, TEMP_CELSIUS, CONF_LATITUDE, CONF_LONGITUDE, CONF_MODE,\n+    CONF_NAME, STATE_UNKNOWN)\n import homeassistant.helpers.config_validation as cv\n from homeassistant.util import Throttle\n \n", "comment": "One attribution is enough.", "ids": [311688, "b98aac239c67b542bbceff9aeb97380ac8c20ca8", "78fb110e842f04c77bee02e69d4be5750c399f9c"], "repo": "home-assistant-core", "ghid": 14875, "old": "     ATTR_FORECAST_CONDITION, ATTR_FORECAST_PRECIPITATION, ATTR_FORECAST_TEMP,\n     ATTR_FORECAST_TEMP_LOW, ATTR_FORECAST_TIME, PLATFORM_SCHEMA, WeatherEntity)\n from homeassistant.const import (\n-    CONF_API_KEY, CONF_MODE, CONF_LATITUDE, CONF_LONGITUDE, CONF_NAME,\n-    STATE_UNKNOWN, TEMP_CELSIUS)\n import homeassistant.helpers.config_validation as cv\n from homeassistant.util import Throttle", "new": "     ATTR_FORECAST_CONDITION, ATTR_FORECAST_PRECIPITATION, ATTR_FORECAST_TEMP,\n     ATTR_FORECAST_TEMP_LOW, ATTR_FORECAST_TIME, PLATFORM_SCHEMA, WeatherEntity)\n from homeassistant.const import (\n+    CONF_API_KEY, TEMP_CELSIUS, CONF_LATITUDE, CONF_LONGITUDE, CONF_MODE,\n+    CONF_NAME, STATE_UNKNOWN)\n import homeassistant.helpers.config_validation as cv\n from homeassistant.util import Throttle", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -11,31 +11,37 @@\n \n from homeassistant.components.weather import (\n     ATTR_FORECAST_CONDITION, ATTR_FORECAST_PRECIPITATION, ATTR_FORECAST_TEMP,\n-    ATTR_FORECAST_TIME, PLATFORM_SCHEMA, WeatherEntity)\n+    ATTR_FORECAST_TEMP_LOW, ATTR_FORECAST_TIME, PLATFORM_SCHEMA, WeatherEntity)\n from homeassistant.const import (\n-    CONF_API_KEY, CONF_LATITUDE, CONF_LONGITUDE, CONF_NAME, STATE_UNKNOWN,\n-    TEMP_CELSIUS)\n+    CONF_API_KEY, CONF_MODE, CONF_LATITUDE, CONF_LONGITUDE, CONF_NAME,\n+    STATE_UNKNOWN, TEMP_CELSIUS)\n import homeassistant.helpers.config_validation as cv\n from homeassistant.util import Throttle\n \n REQUIREMENTS = ['pyowm==2.8.0']\n \n _LOGGER = logging.getLogger(__name__)\n \n-ATTRIBUTION = 'Data provided by OpenWeatherMap'\n+ATTR_FORECAST_WIND_SPEED = 'wind_speed'\n+ATTR_FORECAST_WIND_BEARING = 'wind_bearing'\n+\n+ATTRIBUTION_DAILY = \"Weather data provided by OpenWeatherMap\"\n+ATTRIBUTION_HOURLY = \"Data provided by OpenWeatherMap\"", "msg": "One attribution is enough."}
{"old_hunk": "@@ -147,6 +160,24 @@ def __init__(self, receiver):\n         self._frequency = self._receiver.frequency\n         self._station = self._receiver.station\n \n+        self._sound_mode_support = sound_mode_support\n+        if sound_mode_support:\n+            self._sound_mode = self._receiver.sound_mode\n+            self._sound_mode_raw = self._receiver.sound_mode_raw\n+            if sound_mode_dict is None:", "hunk": "@@ -164,11 +164,7 @@ class DenonDevice(MediaPlayerDevice):\n         if sound_mode_support:\n             self._sound_mode = self._receiver.sound_mode\n             self._sound_mode_raw = self._receiver.sound_mode_raw\n-            if sound_mode_dict is None:\n-                self._sound_mode_list = self._receiver.sound_mode_list\n-            else:\n-                self._receiver.set_sound_mode_dict(sound_mode_dict)\n-                self._sound_mode_list = list(sound_mode_dict)\n+            self._sound_mode_list = self._receiver.sound_mode_list\n         else:\n             self._sound_mode = None\n             self._sound_mode_raw = None\n", "comment": "If we can fetch it from the device, we should not allow it to be configured. Integrations should only represent the raw information that we receive from the device.", "ids": [311792, "7d2d249365732d20ecd81ee40a1095aef4056142", "554884c25fb8dd06cbaeb0f4c737f4d9c5f07200"], "repo": "home-assistant-core", "ghid": 14910, "old": "         if sound_mode_support:\n             self._sound_mode = self._receiver.sound_mode\n             self._sound_mode_raw = self._receiver.sound_mode_raw\n-            if sound_mode_dict is None:\n-                self._sound_mode_list = self._receiver.sound_mode_list\n-            else:\n-                self._receiver.set_sound_mode_dict(sound_mode_dict)\n-                self._sound_mode_list = list(sound_mode_dict)\n         else:\n             self._sound_mode = None\n             self._sound_mode_raw = None", "new": "         if sound_mode_support:\n             self._sound_mode = self._receiver.sound_mode\n             self._sound_mode_raw = self._receiver.sound_mode_raw\n+            self._sound_mode_list = self._receiver.sound_mode_list\n         else:\n             self._sound_mode = None\n             self._sound_mode_raw = None", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -147,6 +160,24 @@ def __init__(self, receiver):\n         self._frequency = self._receiver.frequency\n         self._station = self._receiver.station\n \n+        self._sound_mode_support = sound_mode_support\n+        if sound_mode_support:\n+            self._sound_mode = self._receiver.sound_mode\n+            self._sound_mode_raw = self._receiver.sound_mode_raw\n+            if sound_mode_dict is None:", "msg": "If we can fetch it from the device, we should not allow it to be configured. Integrations should only represent the raw information that we receive from the device."}
{"old_hunk": "@@ -64,13 +88,86 @@ def send_message(self, message='', **kwargs):\n         if not isinstance(targets, list):\n             targets = [targets]\n \n+        file = {}\n+\n+        if file_data is not None:\n+            filename = self.load_file(\n+                url=file_data.get(ATTR_FILE_URL),\n+                local_path=file_data.get(ATTR_FILE_PATH),\n+                username=file_data.get(ATTR_FILE_USERNAME),\n+                password=file_data.get(ATTR_FILE_PASSWORD),\n+                auth=file_data.get(ATTR_FILE_AUTH))\n+\n+            if filename is not None:\n+                file = (filename, open(filename, \"rb\"), \"image/jpeg\")", "hunk": "@@ -99,7 +99,8 @@ class PushoverNotificationService(BaseNotificationService):\n                 auth=file_data.get(ATTR_FILE_AUTH))\n \n             if filename is not None:\n-                file = (filename, open(filename, \"rb\"), \"image/jpeg\")\n+                file = (filename, open(\n+                    filename, \"rb\"), mimetypes.guess_type(filename))\n \n         for target in targets:\n             if target is not None:\n", "comment": "Is this hard-coding (limiting) the mime type to jpeg? Are other image mime types supported?", "ids": [311825, "bd3fc1bcd15ddd7562170e06e9c4c344d7d3ed1f", "c9263412d46322bdc118720fc93b57ac82157397"], "repo": "home-assistant-core", "ghid": 14915, "old": "                 auth=file_data.get(ATTR_FILE_AUTH))\n             if filename is not None:\n-                file = (filename, open(filename, \"rb\"), \"image/jpeg\")\n         for target in targets:\n             if target is not None:", "new": "                 auth=file_data.get(ATTR_FILE_AUTH))\n             if filename is not None:\n+                file = (filename, open(\n+                    filename, \"rb\"), mimetypes.guess_type(filename))\n         for target in targets:\n             if target is not None:", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -64,13 +88,86 @@ def send_message(self, message='', **kwargs):\n         if not isinstance(targets, list):\n             targets = [targets]\n \n+        file = {}\n+\n+        if file_data is not None:\n+            filename = self.load_file(\n+                url=file_data.get(ATTR_FILE_URL),\n+                local_path=file_data.get(ATTR_FILE_PATH),\n+                username=file_data.get(ATTR_FILE_USERNAME),\n+                password=file_data.get(ATTR_FILE_PASSWORD),\n+                auth=file_data.get(ATTR_FILE_AUTH))\n+\n+            if filename is not None:\n+                file = (filename, open(filename, \"rb\"), \"image/jpeg\")", "msg": "Is this hard-coding (limiting) the mime type to jpeg? Are other image mime types supported?"}
{"old_hunk": "@@ -77,6 +77,22 @@ def test_invalid_device_class(self):\n         state = self.hass.states.get('binary_sensor.test')\n         self.assertIsNone(state)\n \n+    def test_unique_id(self):\n+        \"\"\"Test unique id option only creates one sensor per unique_id.\"\"\"\n+        assert setup_component(self.hass, binary_sensor.DOMAIN, {\n+            binary_sensor.DOMAIN: [{\n+                'platform': 'mqtt',\n+                'name': 'Test 1',\n+                'state_topic': 'test-topic',\n+                'unique_id': 'TOTALLY_UNIQUE'\n+            }, {\n+                'platform': 'mqtt',\n+                'name': 'Test 2',\n+                'state_topic': 'test-topic',\n+                'unique_id': 'TOTALLY_UNIQUE'\n+            }]\n+        })\n+", "hunk": "@@ -92,6 +92,9 @@ class TestSensorMQTT(unittest.TestCase):\n                 'unique_id': 'TOTALLY_UNIQUE'\n             }]\n         })\n+        fire_mqtt_message(self.hass, 'test-topic', 'payload')\n+        self.hass.block_till_done()\n+        assert len(self.hass.states.all()) == 1\n \n     def test_availability_without_topic(self):\n         \"\"\"Test availability without defined availability topic.\"\"\"\n", "comment": "We should test the unique id functionality by firing an mqtt message. See sensor mqtt tests.", "ids": [311899, "cdf5de097ab7c10263881a79de643d584d514bf7", "8ec6d9f1370ba0321cc2180fdd073c455901fdf1"], "repo": "home-assistant-core", "ghid": 14929, "old": "                 'unique_id': 'TOTALLY_UNIQUE'\n             }]\n         })\n     def test_availability_without_topic(self):\n         \"\"\"Test availability without defined availability topic.\"\"\"", "new": "                 'unique_id': 'TOTALLY_UNIQUE'\n             }]\n         })\n+        fire_mqtt_message(self.hass, 'test-topic', 'payload')\n+        self.hass.block_till_done()\n+        assert len(self.hass.states.all()) == 1\n     def test_availability_without_topic(self):\n         \"\"\"Test availability without defined availability topic.\"\"\"", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -77,6 +77,22 @@ def test_invalid_device_class(self):\n         state = self.hass.states.get('binary_sensor.test')\n         self.assertIsNone(state)\n \n+    def test_unique_id(self):\n+        \"\"\"Test unique id option only creates one sensor per unique_id.\"\"\"\n+        assert setup_component(self.hass, binary_sensor.DOMAIN, {\n+            binary_sensor.DOMAIN: [{\n+                'platform': 'mqtt',\n+                'name': 'Test 1',\n+                'state_topic': 'test-topic',\n+                'unique_id': 'TOTALLY_UNIQUE'\n+            }, {\n+                'platform': 'mqtt',\n+                'name': 'Test 2',\n+                'state_topic': 'test-topic',\n+                'unique_id': 'TOTALLY_UNIQUE'\n+            }]\n+        })\n+", "msg": "We should test the unique id functionality by firing an mqtt message. See sensor mqtt tests."}
{"old_hunk": "@@ -173,127 +206,88 @@ def state(self):\n         \"\"\"Return the state.\"\"\"\n         return self._state\n \n-\n-class AirPollutionLevelSensor(AirVisualBaseSensor):\n-    \"\"\"Define a sensor to measure air pollution level.\"\"\"\n-\n-    @property\n-    def unique_id(self):\n-        \"\"\"Return a unique, HASS-friendly identifier for this entity.\"\"\"\n-        return '{0}_pollution_level'.format(self._entity_id)\n-\n-    def update(self):\n-        \"\"\"Update the status of the sensor.\"\"\"\n-        self.data.update()\n-\n-        aqi = self.data.pollution_info.get('aqi{0}'.format(self._locale))\n-        try:\n-            [level] = [\n-                i for i in POLLUTANT_LEVEL_MAPPING\n-                if i['minimum'] <= aqi <= i['maximum']\n-            ]\n-            self._state = level.get('label')\n-        except TypeError:\n-            self._state = None\n-        except ValueError:\n-            self._state = None\n-\n-\n-class AirQualityIndexSensor(AirVisualBaseSensor):\n-    \"\"\"Define a sensor to measure AQI.\"\"\"\n-\n     @property\n     def unique_id(self):\n         \"\"\"Return a unique, HASS-friendly identifier for this entity.\"\"\"\n-        return '{0}_aqi'.format(self._entity_id)\n+        return '{0}_{1}_{2}'.format(\n+            self._location_id, self._locale, self._type)\n \n     @property\n     def unit_of_measurement(self):\n         \"\"\"Return the unit the value is expressed in.\"\"\"\n-        return 'AQI'\n-\n-    def update(self):\n-        \"\"\"Update the status of the sensor.\"\"\"\n-        self.data.update()\n-\n-        self._state = self.data.pollution_info.get(\n-            'aqi{0}'.format(self._locale))\n-\n-\n-class MainPollutantSensor(AirVisualBaseSensor):\n-    \"\"\"Define a sensor to the main pollutant of an area.\"\"\"\n-\n-    def __init__(self, data, name, icon, locale, entity_id):\n-        \"\"\"Initialize the sensor.\"\"\"\n-        super().__init__(data, name, icon, locale, entity_id)\n-        self._symbol = None\n-        self._unit = None\n-\n-    @property\n-    def unique_id(self):\n-        \"\"\"Return a unique, HASS-friendly identifier for this entity.\"\"\"\n-        return '{0}_main_pollutant'.format(self._entity_id)\n+        return self._unit\n \n-    def update(self):\n-        \"\"\"Update the status of the sensor.\"\"\"\n-        self.data.update()\n+    async def async_update(self):\n+        \"\"\"Update the sensor.\"\"\"\n+        await self.airvisual.async_update()\n+        data = self.airvisual.pollution_info\n \n-        symbol = self.data.pollution_info.get('main{0}'.format(self._locale))\n-        pollution_info = POLLUTANT_MAPPING.get(symbol, {})\n-        self._state = pollution_info.get('label')\n-        self._unit = pollution_info.get('unit')\n-        self._symbol = symbol\n+        if not data:\n+            return\n \n-        self._attrs.update({\n-            ATTR_POLLUTANT_SYMBOL: self._symbol,\n-            ATTR_POLLUTANT_UNIT: self._unit\n-        })\n+        if self._type == SENSOR_TYPE_LEVEL:\n+            aqi = data['aqi{0}'.format(self._locale)]\n+            [level] = [\n+                i for i in POLLUTANT_LEVEL_MAPPING\n+                if i['minimum'] <= aqi <= i['maximum']\n+            ]\n+            self._state = level['label']\n+        elif self._type == SENSOR_TYPE_AQI:\n+            self._state = data['aqi{0}'.format(self._locale)]\n+        elif self._type == SENSOR_TYPE_POLLUTANT:\n+            symbol = data['main{0}'.format(self._locale)]\n+            self._state = POLLUTANT_MAPPING[symbol]['label']\n+            self._attrs.update({\n+                ATTR_POLLUTANT_SYMBOL: symbol,\n+                ATTR_POLLUTANT_UNIT: POLLUTANT_MAPPING[symbol]['unit']\n+            })\n \n \n class AirVisualData(object):\n     \"\"\"Define an object to hold sensor data.\"\"\"\n \n     def __init__(self, client, **kwargs):\n-        \"\"\"Initialize the AirVisual data element.\"\"\"\n+        \"\"\"Initialize.\"\"\"\n         self._client = client\n-        self.attrs = {}\n-        self.pollution_info = None\n-\n         self.city = kwargs.get(CONF_CITY)\n-        self.state = kwargs.get(CONF_STATE)\n         self.country = kwargs.get(CONF_COUNTRY)\n-\n         self.latitude = kwargs.get(CONF_LATITUDE)\n         self.longitude = kwargs.get(CONF_LONGITUDE)\n-        self._radius = kwargs.get(CONF_RADIUS)\n-\n+        self.pollution_info = {}\n         self.show_on_map = kwargs.get(CONF_SHOW_ON_MAP)\n+        self.state = kwargs.get(CONF_STATE)\n+\n+        self.async_update = Throttle(\n+            kwargs[CONF_SCAN_INTERVAL])(self._async_update)\n \n-    @Throttle(MIN_TIME_BETWEEN_UPDATES)\n-    def update(self):\n-        \"\"\"Update with new AirVisual data.\"\"\"\n-        from pyairvisual.exceptions import HTTPError\n+    async def _async_update(self):\n+        \"\"\"Update AirVisual data.\"\"\"\n+        from pyairvisual.errors import AirVisualError\n \n         try:\n             if self.city and self.state and self.country:\n-                resp = self._client.city(\n-                    self.city, self.state, self.country).get('data')\n-                self.longitude, self.latitude = resp.get('location').get(\n-                    'coordinates')\n+                resp = await self._client.data.city(\n+                    self.city, self.state, self.country)\n+                self.longitude, self.latitude = resp['location']['coordinates']\n             else:\n-                resp = self._client.nearest_city(\n-                    self.latitude, self.longitude, self._radius).get('data')\n+                resp = await self._client.data.nearest_city(\n+                    self.latitude, self.longitude)\n+\n             _LOGGER.debug(\"New data retrieved: %s\", resp)\n \n-            self.pollution_info = resp.get('current', {}).get('pollution', {})\n-\n-            self.attrs = {\n-                ATTR_CITY: resp.get('city'),\n-                ATTR_REGION: resp.get('state'),\n-                ATTR_COUNTRY: resp.get('country')\n-            }\n-        except HTTPError as exc_info:\n-            _LOGGER.error(\"Unable to retrieve data on this location: %s\",\n-                          self.__dict__)\n-            _LOGGER.debug(exc_info)\n+            self.pollution_info = resp['current']['pollution']\n+        except AirVisualError as err:\n+            if self.city and self.state and self.country:\n+                location = (self.city, self.state, self.country)\n+            else:\n+                location = (self.latitude, self.longitude)\n+\n+            _LOGGER.error(\n+                \"Can't retrieve data for location: %s (%s)\", location,\n+                err)\n+            self.city = None", "hunk": "@@ -285,9 +285,4 @@ class AirVisualData(object):\n             _LOGGER.error(\n                 \"Can't retrieve data for location: %s (%s)\", location,\n                 err)\n-            self.city = None\n-            self.country = None\n-            self.latitude = None\n-            self.longitude = None\n             self.pollution_info = {}\n-            self.state = None\n", "comment": "Why would you reset all these things ? If something is misconfigured, we should not just try to get the nearest city. We should just not update and mark as available=False.", "ids": [312066, "fa1b88382c6c5542f12b5ee9b62c01d59c62fa1b", "e34bb21795f14f844318968bff3b23360af5ea57"], "repo": "home-assistant-core", "ghid": 14943, "old": "             _LOGGER.error(\n                 \"Can't retrieve data for location: %s (%s)\", location,\n                 err)\n-            self.city = None\n-            self.country = None\n-            self.latitude = None\n-            self.longitude = None\n             self.pollution_info = {}\n-            self.state = None", "new": "             _LOGGER.error(\n                 \"Can't retrieve data for location: %s (%s)\", location,\n                 err)\n             self.pollution_info = {}", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -173,127 +206,88 @@ def state(self):\n         \"\"\"Return the state.\"\"\"\n         return self._state\n \n-\n-class AirPollutionLevelSensor(AirVisualBaseSensor):\n-    \"\"\"Define a sensor to measure air pollution level.\"\"\"\n-\n-    @property\n-    def unique_id(self):\n-        \"\"\"Return a unique, HASS-friendly identifier for this entity.\"\"\"\n-        return '{0}_pollution_level'.format(self._entity_id)\n-\n-    def update(self):\n-        \"\"\"Update the status of the sensor.\"\"\"\n-        self.data.update()\n-\n-        aqi = self.data.pollution_info.get('aqi{0}'.format(self._locale))\n-        try:\n-            [level] = [\n-                i for i in POLLUTANT_LEVEL_MAPPING\n-                if i['minimum'] <= aqi <= i['maximum']\n-            ]\n-            self._state = level.get('label')\n-        except TypeError:\n-            self._state = None\n-        except ValueError:\n-            self._state = None\n-\n-\n-class AirQualityIndexSensor(AirVisualBaseSensor):\n-    \"\"\"Define a sensor to measure AQI.\"\"\"\n-\n     @property\n     def unique_id(self):\n         \"\"\"Return a unique, HASS-friendly identifier for this entity.\"\"\"\n-        return '{0}_aqi'.format(self._entity_id)\n+        return '{0}_{1}_{2}'.format(\n+            self._location_id, self._locale, self._type)\n \n     @property\n     def unit_of_measurement(self):\n         \"\"\"Return the unit the value is expressed in.\"\"\"\n-        return 'AQI'\n-\n-    def update(self):\n-        \"\"\"Update the status of the sensor.\"\"\"\n-        self.data.update()\n-\n-        self._state = self.data.pollution_info.get(\n-            'aqi{0}'.format(self._locale))\n-\n-\n-class MainPollutantSensor(AirVisualBaseSensor):\n-    \"\"\"Define a sensor to the main pollutant of an area.\"\"\"\n-\n-    def __init__(self, data, name, icon, locale, entity_id):\n-        \"\"\"Initialize the sensor.\"\"\"\n-        super().__init__(data, name, icon, locale, entity_id)\n-        self._symbol = None\n-        self._unit = None\n-\n-    @property\n-    def unique_id(self):\n-        \"\"\"Return a unique, HASS-friendly identifier for this entity.\"\"\"\n-        return '{0}_main_pollutant'.format(self._entity_id)\n+        return self._unit\n \n-    def update(self):\n-        \"\"\"Update the status of the sensor.\"\"\"\n-        self.data.update()\n+    async def async_update(self):\n+        \"\"\"Update the sensor.\"\"\"\n+        await self.airvisual.async_update()\n+        data = self.airvisual.pollution_info\n \n-        symbol = self.data.pollution_info.get('main{0}'.format(self._locale))\n-        pollution_info = POLLUTANT_MAPPING.get(symbol, {})\n-        self._state = pollution_info.get('label')\n-        self._unit = pollution_info.get('unit')\n-        self._symbol = symbol\n+        if not data:\n+            return\n \n-        self._attrs.update({\n-            ATTR_POLLUTANT_SYMBOL: self._symbol,\n-            ATTR_POLLUTANT_UNIT: self._unit\n-        })\n+        if self._type == SENSOR_TYPE_LEVEL:\n+            aqi = data['aqi{0}'.format(self._locale)]\n+            [level] = [\n+                i for i in POLLUTANT_LEVEL_MAPPING\n+                if i['minimum'] <= aqi <= i['maximum']\n+            ]\n+            self._state = level['label']\n+        elif self._type == SENSOR_TYPE_AQI:\n+            self._state = data['aqi{0}'.format(self._locale)]\n+        elif self._type == SENSOR_TYPE_POLLUTANT:\n+            symbol = data['main{0}'.format(self._locale)]\n+            self._state = POLLUTANT_MAPPING[symbol]['label']\n+            self._attrs.update({\n+                ATTR_POLLUTANT_SYMBOL: symbol,\n+                ATTR_POLLUTANT_UNIT: POLLUTANT_MAPPING[symbol]['unit']\n+            })\n \n \n class AirVisualData(object):\n     \"\"\"Define an object to hold sensor data.\"\"\"\n \n     def __init__(self, client, **kwargs):\n-        \"\"\"Initialize the AirVisual data element.\"\"\"\n+        \"\"\"Initialize.\"\"\"\n         self._client = client\n-        self.attrs = {}\n-        self.pollution_info = None\n-\n         self.city = kwargs.get(CONF_CITY)\n-        self.state = kwargs.get(CONF_STATE)\n         self.country = kwargs.get(CONF_COUNTRY)\n-\n         self.latitude = kwargs.get(CONF_LATITUDE)\n         self.longitude = kwargs.get(CONF_LONGITUDE)\n-        self._radius = kwargs.get(CONF_RADIUS)\n-\n+        self.pollution_info = {}\n         self.show_on_map = kwargs.get(CONF_SHOW_ON_MAP)\n+        self.state = kwargs.get(CONF_STATE)\n+\n+        self.async_update = Throttle(\n+            kwargs[CONF_SCAN_INTERVAL])(self._async_update)\n \n-    @Throttle(MIN_TIME_BETWEEN_UPDATES)\n-    def update(self):\n-        \"\"\"Update with new AirVisual data.\"\"\"\n-        from pyairvisual.exceptions import HTTPError\n+    async def _async_update(self):\n+        \"\"\"Update AirVisual data.\"\"\"\n+        from pyairvisual.errors import AirVisualError\n \n         try:\n             if self.city and self.state and self.country:\n-                resp = self._client.city(\n-                    self.city, self.state, self.country).get('data')\n-                self.longitude, self.latitude = resp.get('location').get(\n-                    'coordinates')\n+                resp = await self._client.data.city(\n+                    self.city, self.state, self.country)\n+                self.longitude, self.latitude = resp['location']['coordinates']\n             else:\n-                resp = self._client.nearest_city(\n-                    self.latitude, self.longitude, self._radius).get('data')\n+                resp = await self._client.data.nearest_city(\n+                    self.latitude, self.longitude)\n+\n             _LOGGER.debug(\"New data retrieved: %s\", resp)\n \n-            self.pollution_info = resp.get('current', {}).get('pollution', {})\n-\n-            self.attrs = {\n-                ATTR_CITY: resp.get('city'),\n-                ATTR_REGION: resp.get('state'),\n-                ATTR_COUNTRY: resp.get('country')\n-            }\n-        except HTTPError as exc_info:\n-            _LOGGER.error(\"Unable to retrieve data on this location: %s\",\n-                          self.__dict__)\n-            _LOGGER.debug(exc_info)\n+            self.pollution_info = resp['current']['pollution']\n+        except AirVisualError as err:\n+            if self.city and self.state and self.country:\n+                location = (self.city, self.state, self.country)\n+            else:\n+                location = (self.latitude, self.longitude)\n+\n+            _LOGGER.error(\n+                \"Can't retrieve data for location: %s (%s)\", location,\n+                err)\n+            self.city = None", "msg": "Why would you reset all these things ? If something is misconfigured, we should not just try to get the nearest city. We should just not update and mark as available=False."}
{"old_hunk": "@@ -128,8 +129,8 @@ def async_snapshot(hass, filename, entity_id=None):\n     raise HomeAssistantError('Unable to get image')\n \n \n-@asyncio.coroutine\n-def async_setup(hass, config):\n+@bind_hass", "hunk": "@@ -129,7 +129,6 @@ async def async_get_image(hass, entity_id, timeout=10):\n     raise HomeAssistantError('Unable to get image')\n \n \n-@bind_hass\n async def async_setup(hass, config):\n     \"\"\"Set up the camera component.\"\"\"\n     component = hass.data[DOMAIN] = \\\n", "comment": "Setup should not have bind Hass. Bind Hass is only needed for helper methods that other components might call, like get image", "ids": [312251, "13ba7c814eb56eab5939277edfc87ef963839fb1", "1eb5640badac8fcb0e1b10fc428ae165ac33d894"], "repo": "home-assistant-core", "ghid": 14990, "old": "     raise HomeAssistantError('Unable to get image')\n-@bind_hass\n async def async_setup(hass, config):\n     \"\"\"Set up the camera component.\"\"\"\n     component = hass.data[DOMAIN] = \\", "new": "     raise HomeAssistantError('Unable to get image')\n async def async_setup(hass, config):\n     \"\"\"Set up the camera component.\"\"\"\n     component = hass.data[DOMAIN] = \\", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -128,8 +129,8 @@ def async_snapshot(hass, filename, entity_id=None):\n     raise HomeAssistantError('Unable to get image')\n \n \n-@asyncio.coroutine\n-def async_setup(hass, config):\n+@bind_hass", "msg": "Setup should not have bind Hass. Bind Hass is only needed for helper methods that other components might call, like get image"}
{"old_hunk": "@@ -125,23 +135,33 @@ def message_received(topic, payload, qos):\n                 self._expiration_trigger = async_track_point_in_utc_time(\n                     self.hass, self.value_is_expired, expiration_at)\n \n-            if self._json_attributes:\n-                self._attributes = {}\n+            if self._json_attributes or self._json_template_attributes:\n+                print('parsing json!')\n                 try:\n                     json_dict = json.loads(payload)\n-                    if isinstance(json_dict, dict):\n-                        attrs = {k: json_dict[k] for k in\n-                                 self._json_attributes & json_dict.keys()}\n-                        self._attributes = attrs\n-                    else:\n+                    if not isinstance(json_dict, dict):\n                         _LOGGER.warning(\"JSON result was not a dictionary\")\n+                        json_dict = None\n+\n                 except ValueError:\n                     _LOGGER.warning(\"MQTT payload could not be parsed as JSON\")\n                     _LOGGER.debug(\"Erroneous JSON: %s\", payload)\n+                self._attributes = {}\n+\n+            if self._json_attributes and json_dict:\n+                print('enumerating json attributes!')\n+                for key in self._json_attributes:\n+                    self._attributes[key] = json_dict[key]\n+\n+            if self._json_template_attributes and json_dict:\n+                print('enumerating json template attributes!')\n+                for key, value in self._json_template_attributes.items():\n+                    self._attributes[key] = self._json_template_attributes[key].async_render_with_possible_json_value(payload, self._state)", "hunk": "@@ -155,8 +155,10 @@ class MqttSensor(MqttAvailability, Entity):\n \n             if self._json_template_attributes and json_dict:\n                 print('enumerating json template attributes!')\n-                for key, value in self._json_template_attributes.items():\n-                    self._attributes[key] = self._json_template_attributes[key].async_render_with_possible_json_value(payload, self._state)\n+                for key, template in self._json_template_attributes.items():\n+                    result = template.async_render_with_possible_json_value(\n+                        payload, self._state)\n+                    self._attributes[key] = result\n \n             if self._template is not None:\n                 payload = self._template.async_render_with_possible_json_value(\n", "comment": "line too long (139 > 79 characters)", "ids": [312258, "25e6541df4305d4b93363c70bb75bc5aa1f19e36", "7cd760092094b1bf9eb2407ee06ebd46dd6510ca"], "repo": "home-assistant-core", "ghid": 14992, "old": "             if self._json_template_attributes and json_dict:\n                 print('enumerating json template attributes!')\n-                for key, value in self._json_template_attributes.items():\n-                    self._attributes[key] = self._json_template_attributes[key].async_render_with_possible_json_value(payload, self._state)\n             if self._template is not None:\n                 payload = self._template.async_render_with_possible_json_value(", "new": "             if self._json_template_attributes and json_dict:\n                 print('enumerating json template attributes!')\n+                for key, template in self._json_template_attributes.items():\n+                    result = template.async_render_with_possible_json_value(\n+                        payload, self._state)\n+                    self._attributes[key] = result\n             if self._template is not None:\n                 payload = self._template.async_render_with_possible_json_value(", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -125,23 +135,33 @@ def message_received(topic, payload, qos):\n                 self._expiration_trigger = async_track_point_in_utc_time(\n                     self.hass, self.value_is_expired, expiration_at)\n \n-            if self._json_attributes:\n-                self._attributes = {}\n+            if self._json_attributes or self._json_template_attributes:\n+                print('parsing json!')\n                 try:\n                     json_dict = json.loads(payload)\n-                    if isinstance(json_dict, dict):\n-                        attrs = {k: json_dict[k] for k in\n-                                 self._json_attributes & json_dict.keys()}\n-                        self._attributes = attrs\n-                    else:\n+                    if not isinstance(json_dict, dict):\n                         _LOGGER.warning(\"JSON result was not a dictionary\")\n+                        json_dict = None\n+\n                 except ValueError:\n                     _LOGGER.warning(\"MQTT payload could not be parsed as JSON\")\n                     _LOGGER.debug(\"Erroneous JSON: %s\", payload)\n+                self._attributes = {}\n+\n+            if self._json_attributes and json_dict:\n+                print('enumerating json attributes!')\n+                for key in self._json_attributes:\n+                    self._attributes[key] = json_dict[key]\n+\n+            if self._json_template_attributes and json_dict:\n+                print('enumerating json template attributes!')\n+                for key, value in self._json_template_attributes.items():\n+                    self._attributes[key] = self._json_template_attributes[key].async_render_with_possible_json_value(payload, self._state)", "msg": "line too long (139 > 79 characters)"}
{"old_hunk": "@@ -25,7 +25,7 @@\n from homeassistant.loader import get_component, get_platform\n from homeassistant.util.yaml import load_yaml, SECRET_YAML\n import homeassistant.helpers.config_validation as cv\n-from homeassistant.util import dt as date_util, location as loc_util\n+from homeassistant.util import dt as date_util, location as loc_util, package", "hunk": "@@ -25,7 +25,7 @@ from homeassistant.exceptions import HomeAssistantError\n from homeassistant.loader import get_component, get_platform\n from homeassistant.util.yaml import load_yaml, SECRET_YAML\n import homeassistant.helpers.config_validation as cv\n-from homeassistant.util import dt as date_util, location as loc_util, package\n+from homeassistant.util import dt as date_util, location as loc_util\n from homeassistant.util.unit_system import IMPERIAL_SYSTEM, METRIC_SYSTEM\n from homeassistant.helpers.entity_values import EntityValues\n from homeassistant.helpers import config_per_platform, extract_domain_configs\n", "comment": "'homeassistant.util.package' imported but unused", "ids": [312269, "b0165fb3ca6b4c1d3a7e9e4522cb88e9605f9f4b", "ca81ec407c4462169083c61275d89cfcf9db56e3"], "repo": "home-assistant-core", "ghid": 14993, "old": " from homeassistant.loader import get_component, get_platform\n from homeassistant.util.yaml import load_yaml, SECRET_YAML\n import homeassistant.helpers.config_validation as cv\n-from homeassistant.util import dt as date_util, location as loc_util, package\n from homeassistant.util.unit_system import IMPERIAL_SYSTEM, METRIC_SYSTEM\n from homeassistant.helpers.entity_values import EntityValues\n from homeassistant.helpers import config_per_platform, extract_domain_configs", "new": " from homeassistant.loader import get_component, get_platform\n from homeassistant.util.yaml import load_yaml, SECRET_YAML\n import homeassistant.helpers.config_validation as cv\n+from homeassistant.util import dt as date_util, location as loc_util\n from homeassistant.util.unit_system import IMPERIAL_SYSTEM, METRIC_SYSTEM\n from homeassistant.helpers.entity_values import EntityValues\n from homeassistant.helpers import config_per_platform, extract_domain_configs", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -25,7 +25,7 @@\n from homeassistant.loader import get_component, get_platform\n from homeassistant.util.yaml import load_yaml, SECRET_YAML\n import homeassistant.helpers.config_validation as cv\n-from homeassistant.util import dt as date_util, location as loc_util\n+from homeassistant.util import dt as date_util, location as loc_util, package", "msg": "'homeassistant.util.package' imported but unused"}
{"old_hunk": "@@ -10,20 +10,25 @@\n import requests\n import voluptuous as vol\n \n-from homeassistant.const import ATTR_NAME\n+from homeassistant.const import (\n+    ATTR_ENTITY_ID, ATTR_NAME)\n from homeassistant.core import split_entity_id\n import homeassistant.helpers.config_validation as cv\n from homeassistant.components.image_processing import (\n     PLATFORM_SCHEMA, ImageProcessingFaceEntity, ATTR_CONFIDENCE, CONF_SOURCE,\n-    CONF_ENTITY_ID, CONF_NAME)\n+    CONF_ENTITY_ID, CONF_NAME, DOMAIN)\n from homeassistant.const import (CONF_IP_ADDRESS, CONF_PORT)\n \n _LOGGER = logging.getLogger(__name__)\n \n ATTR_BOUNDING_BOX = 'bounding_box'\n+ATTR_CLASSIFIER = 'classifier'\n ATTR_IMAGE_ID = 'image_id'\n ATTR_MATCHED = 'matched'\n CLASSIFIER = 'facebox'\n+EVENT_CLASSIFIER_TEACH = 'image_processing.teach_classifier'\n+FILE_PATH = 'file_path'\n+SERVICE_FACEBOX_TEACH_FACE = 'facebox_teach_face'", "hunk": "@@ -26,9 +26,10 @@ ATTR_CLASSIFIER = 'classifier'\n ATTR_IMAGE_ID = 'image_id'\n ATTR_MATCHED = 'matched'\n CLASSIFIER = 'facebox'\n+DATA_FACEBOX = 'facebox_classifiers'\n EVENT_CLASSIFIER_TEACH = 'image_processing.teach_classifier'\n FILE_PATH = 'file_path'\n-SERVICE_FACEBOX_TEACH_FACE = 'facebox_teach_face'\n+SERVICE_TEACH_FACE = 'facebox_teach_face'\n TIMEOUT = 9\n \n \n", "comment": "Simplify constant name to `SERVICE_TEACH_FACE`. Don't change the string though.", "ids": [312285, "0179d37038a3c427619c21ed6b30eb6031c72085", "863ebd6a403959347aa279dd1a1bfa297625aaf0"], "repo": "home-assistant-core", "ghid": 14998, "old": " ATTR_IMAGE_ID = 'image_id'\n ATTR_MATCHED = 'matched'\n CLASSIFIER = 'facebox'\n EVENT_CLASSIFIER_TEACH = 'image_processing.teach_classifier'\n FILE_PATH = 'file_path'\n-SERVICE_FACEBOX_TEACH_FACE = 'facebox_teach_face'\n TIMEOUT = 9", "new": " ATTR_IMAGE_ID = 'image_id'\n ATTR_MATCHED = 'matched'\n CLASSIFIER = 'facebox'\n+DATA_FACEBOX = 'facebox_classifiers'\n EVENT_CLASSIFIER_TEACH = 'image_processing.teach_classifier'\n FILE_PATH = 'file_path'\n+SERVICE_TEACH_FACE = 'facebox_teach_face'\n TIMEOUT = 9", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -10,20 +10,25 @@\n import requests\n import voluptuous as vol\n \n-from homeassistant.const import ATTR_NAME\n+from homeassistant.const import (\n+    ATTR_ENTITY_ID, ATTR_NAME)\n from homeassistant.core import split_entity_id\n import homeassistant.helpers.config_validation as cv\n from homeassistant.components.image_processing import (\n     PLATFORM_SCHEMA, ImageProcessingFaceEntity, ATTR_CONFIDENCE, CONF_SOURCE,\n-    CONF_ENTITY_ID, CONF_NAME)\n+    CONF_ENTITY_ID, CONF_NAME, DOMAIN)\n from homeassistant.const import (CONF_IP_ADDRESS, CONF_PORT)\n \n _LOGGER = logging.getLogger(__name__)\n \n ATTR_BOUNDING_BOX = 'bounding_box'\n+ATTR_CLASSIFIER = 'classifier'\n ATTR_IMAGE_ID = 'image_id'\n ATTR_MATCHED = 'matched'\n CLASSIFIER = 'facebox'\n+EVENT_CLASSIFIER_TEACH = 'image_processing.teach_classifier'\n+FILE_PATH = 'file_path'\n+SERVICE_FACEBOX_TEACH_FACE = 'facebox_teach_face'", "msg": "Simplify constant name to `SERVICE_TEACH_FACE`. Don't change the string though."}
{"old_hunk": "@@ -178,6 +179,7 @@\n             vol.Optional(CONF_PATH, default=DEFAULT_PATH): cv.string,\n             vol.Optional(CONF_RESOLVENAMES, default=DEFAULT_RESOLVENAMES):\n                 vol.In(CONF_RESOLVENAMES_OPTIONS),\n+            vol.Optional(CONF_JSONPORT): cv.string,", "hunk": "@@ -179,7 +179,7 @@ CONFIG_SCHEMA = vol.Schema({\n             vol.Optional(CONF_PATH, default=DEFAULT_PATH): cv.string,\n             vol.Optional(CONF_RESOLVENAMES, default=DEFAULT_RESOLVENAMES):\n                 vol.In(CONF_RESOLVENAMES_OPTIONS),\n-            vol.Optional(CONF_JSONPORT): cv.string,\n+            vol.Optional(CONF_JSONPORT, default=DEFAULT_JSONPORT): cv.string,\n             vol.Optional(CONF_USERNAME, default=DEFAULT_USERNAME): cv.string,\n             vol.Optional(CONF_PASSWORD, default=DEFAULT_PASSWORD): cv.string,\n             vol.Optional(CONF_CALLBACK_IP): cv.string,\n", "comment": "Here is where the default port should be set. Not having it here will require everybody to update their configuration.yaml. So you have to define a global `DEFAULT_JSON_PORT` at the top of the file in case there's no global for port 80 set already somewhere else as a constant to use.", "ids": [312372, "bc77cd1e3a8c2e62719d464630fa9c6ed0d38dd7", "b69bb7e04635ebd0f8d178f1b502141f2d831f3c"], "repo": "home-assistant-core", "ghid": 15029, "old": "             vol.Optional(CONF_PATH, default=DEFAULT_PATH): cv.string,\n             vol.Optional(CONF_RESOLVENAMES, default=DEFAULT_RESOLVENAMES):\n                 vol.In(CONF_RESOLVENAMES_OPTIONS),\n-            vol.Optional(CONF_JSONPORT): cv.string,\n             vol.Optional(CONF_USERNAME, default=DEFAULT_USERNAME): cv.string,\n             vol.Optional(CONF_PASSWORD, default=DEFAULT_PASSWORD): cv.string,\n             vol.Optional(CONF_CALLBACK_IP): cv.string,", "new": "             vol.Optional(CONF_PATH, default=DEFAULT_PATH): cv.string,\n             vol.Optional(CONF_RESOLVENAMES, default=DEFAULT_RESOLVENAMES):\n                 vol.In(CONF_RESOLVENAMES_OPTIONS),\n+            vol.Optional(CONF_JSONPORT, default=DEFAULT_JSONPORT): cv.string,\n             vol.Optional(CONF_USERNAME, default=DEFAULT_USERNAME): cv.string,\n             vol.Optional(CONF_PASSWORD, default=DEFAULT_PASSWORD): cv.string,\n             vol.Optional(CONF_CALLBACK_IP): cv.string,", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -178,6 +179,7 @@\n             vol.Optional(CONF_PATH, default=DEFAULT_PATH): cv.string,\n             vol.Optional(CONF_RESOLVENAMES, default=DEFAULT_RESOLVENAMES):\n                 vol.In(CONF_RESOLVENAMES_OPTIONS),\n+            vol.Optional(CONF_JSONPORT): cv.string,", "msg": "Here is where the default port should be set. Not having it here will require everybody to update their configuration.yaml. So you have to define a global `DEFAULT_JSON_PORT` at the top of the file in case there's no global for port 80 set already somewhere else as a constant to use."}
{"old_hunk": "@@ -43,6 +45,7 @@\n })\n \n \n+# pylint: disable=unused-argument", "hunk": "@@ -45,7 +45,6 @@ PLATFORM_SCHEMA = PLATFORM_SCHEMA.extend({\n })\n \n \n-# pylint: disable=unused-argument\n def setup_platform(hass, config, add_devices, discovery_info=None):\n     \"\"\"Set up the FireTV platform.\"\"\"\n     name = config.get(CONF_NAME)\n", "comment": "I think we can remove this. Others looks fine", "ids": [312387, "9541bc07699d978860fce54a830d2da593d70039", "a88035d7616480419c499a53b54e5af6810560de"], "repo": "home-assistant-core", "ghid": 15033, "old": " })\n-# pylint: disable=unused-argument\n def setup_platform(hass, config, add_devices, discovery_info=None):\n     \"\"\"Set up the FireTV platform.\"\"\"\n     name = config.get(CONF_NAME)", "new": " })\n def setup_platform(hass, config, add_devices, discovery_info=None):\n     \"\"\"Set up the FireTV platform.\"\"\"\n     name = config.get(CONF_NAME)", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -43,6 +45,7 @@\n })\n \n \n+# pylint: disable=unused-argument", "msg": "I think we can remove this. Others looks fine"}
{"old_hunk": "@@ -124,13 +153,31 @@ def update(self):\n         elif self._sensor_type == 'battery_level':\n             try:\n                 self._state = self._data.battery_level\n-            except TypeError:\n+            except (AttributeError, TypeError):", "hunk": "@@ -153,13 +153,13 @@ class ArloSensor(Entity):\n         elif self._sensor_type == 'battery_level':\n             try:\n                 self._state = self._data.battery_level\n-            except (AttributeError, TypeError):\n+            except TypeError:\n                 self._state = None\n \n         elif self._sensor_type == 'signal_strength':\n             try:\n                 self._state = self._data.signal_strength\n-            except (AttributeError, TypeError):\n+            except TypeError:\n                 self._state = None\n \n         elif self._sensor_type == 'temperature':\n", "comment": "Why would there be an `AttributeError`?", "ids": [312507, "ac179caf113e3aed36c22400e4a875f7568516d3", "fe532ed5b731b4c2c7971c83957b442c3c50514a"], "repo": "home-assistant-core", "ghid": 15074, "old": "         elif self._sensor_type == 'battery_level':\n             try:\n                 self._state = self._data.battery_level\n-            except (AttributeError, TypeError):\n                 self._state = None\n         elif self._sensor_type == 'signal_strength':\n             try:\n                 self._state = self._data.signal_strength\n-            except (AttributeError, TypeError):\n                 self._state = None\n         elif self._sensor_type == 'temperature':", "new": "         elif self._sensor_type == 'battery_level':\n             try:\n                 self._state = self._data.battery_level\n+            except TypeError:\n                 self._state = None\n         elif self._sensor_type == 'signal_strength':\n             try:\n                 self._state = self._data.signal_strength\n+            except TypeError:\n                 self._state = None\n         elif self._sensor_type == 'temperature':", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -124,13 +153,31 @@ def update(self):\n         elif self._sensor_type == 'battery_level':\n             try:\n                 self._state = self._data.battery_level\n-            except TypeError:\n+            except (AttributeError, TypeError):", "msg": "Why would there be an `AttributeError`?"}
{"old_hunk": "@@ -81,6 +102,7 @@ def name(self):\n     @callback\n     def _update_callback(self):\n         \"\"\"Call update method.\"\"\"\n+        _LOGGER.debug('update')", "hunk": "@@ -102,7 +102,6 @@ class ArloSensor(Entity):\n     @callback\n     def _update_callback(self):\n         \"\"\"Call update method.\"\"\"\n-        _LOGGER.debug('update')\n         self.async_schedule_update_ha_state(True)\n \n     @property\n", "comment": "Remove this. The state update will be logged by the core at info level.", "ids": [312519, "597904a214e18eb7c6ba83ef9c08a94f39617297", "046c62be8c6276e8201e4e737f19af189fbb14ec"], "repo": "home-assistant-core", "ghid": 15074, "old": "     @callback\n     def _update_callback(self):\n         \"\"\"Call update method.\"\"\"\n-        _LOGGER.debug('update')\n         self.async_schedule_update_ha_state(True)\n     @property", "new": "     @callback\n     def _update_callback(self):\n         \"\"\"Call update method.\"\"\"\n         self.async_schedule_update_ha_state(True)\n     @property", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -81,6 +102,7 @@ def name(self):\n     @callback\n     def _update_callback(self):\n         \"\"\"Call update method.\"\"\"\n+        _LOGGER.debug('update')", "msg": "Remove this. The state update will be logged by the core at info level."}
{"old_hunk": "@@ -55,7 +55,7 @@ def test_only_valid_components(mock_load_platform, hass, mqtt_mock, caplog):\n     mock_load_platform.return_value = mock_coro()\n     yield from async_start(hass, 'homeassistant', {})\n \n-    async_fire_mqtt_message(hass, 'homeassistant/climate/bla/config', '{}')\n+    async_fire_mqtt_message(hass, 'homeassistant/lock/bla/config', '{}')", "hunk": "@@ -57,7 +57,7 @@ def test_only_valid_components(mock_load_platform, hass, mqtt_mock, caplog):\n \n     async_fire_mqtt_message(hass, 'homeassistant/lock/bla/config', '{}')\n     yield from hass.async_block_till_done()\n-    assert 'Component climate is not supported' in caplog.text\n+    assert 'Component lock is not supported' in caplog.text\n     assert not mock_load_platform.called\n \n \n", "comment": "Please read the rest of the test and fix it accordingly.", "ids": [312677, "4974ac8904a798fb61605906977c74931075e271", "d9e4b2b4f0f5333553a52894c6e4e68f1fd25eed"], "repo": "home-assistant-core", "ghid": 15085, "old": "     async_fire_mqtt_message(hass, 'homeassistant/lock/bla/config', '{}')\n     yield from hass.async_block_till_done()\n-    assert 'Component climate is not supported' in caplog.text\n     assert not mock_load_platform.called", "new": "     async_fire_mqtt_message(hass, 'homeassistant/lock/bla/config', '{}')\n     yield from hass.async_block_till_done()\n+    assert 'Component lock is not supported' in caplog.text\n     assert not mock_load_platform.called", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -55,7 +55,7 @@ def test_only_valid_components(mock_load_platform, hass, mqtt_mock, caplog):\n     mock_load_platform.return_value = mock_coro()\n     yield from async_start(hass, 'homeassistant', {})\n \n-    async_fire_mqtt_message(hass, 'homeassistant/climate/bla/config', '{}')\n+    async_fire_mqtt_message(hass, 'homeassistant/lock/bla/config', '{}')", "msg": "Please read the rest of the test and fix it accordingly."}
{"old_hunk": "@@ -65,10 +64,8 @@ def get_device_name(self, device):\n         return self.last_results.get(device)\n \n     def update_info(self):\n-        \"\"\"Ensure the information from the BT Home Hub 5 is up to date.\n-        Return boolean if scanning successful.\n-        \"\"\"\n-\n+        \"\"\"Ensure the information from the BT Home Hub 5 is up to date.\"\"\"\n+        # Return boolean if scanning successful.", "hunk": "@@ -65,7 +65,6 @@ class BTHomeHub5DeviceScanner(DeviceScanner):\n \n     def update_info(self):\n         \"\"\"Ensure the information from the BT Home Hub 5 is up to date.\"\"\"\n-        # Return boolean if scanning successful.\n         import bthomehub5_devicelist\n \n         _LOGGER.info(\"Scanning\")\n", "comment": "We don't return a boolean anymore.", "ids": [312716, "3aa41ef65fef15d826e365506f88d7ba62315d8c", "a598a496bc22243988dcf4b8edd28faed572ae0a"], "repo": "home-assistant-core", "ghid": 15096, "old": "     def update_info(self):\n         \"\"\"Ensure the information from the BT Home Hub 5 is up to date.\"\"\"\n-        # Return boolean if scanning successful.\n         import bthomehub5_devicelist\n         _LOGGER.info(\"Scanning\")", "new": "     def update_info(self):\n         \"\"\"Ensure the information from the BT Home Hub 5 is up to date.\"\"\"\n         import bthomehub5_devicelist\n         _LOGGER.info(\"Scanning\")", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -65,10 +64,8 @@ def get_device_name(self, device):\n         return self.last_results.get(device)\n \n     def update_info(self):\n-        \"\"\"Ensure the information from the BT Home Hub 5 is up to date.\n-        Return boolean if scanning successful.\n-        \"\"\"\n-\n+        \"\"\"Ensure the information from the BT Home Hub 5 is up to date.\"\"\"\n+        # Return boolean if scanning successful.", "msg": "We don't return a boolean anymore."}
{"old_hunk": "@@ -1,32 +1,33 @@\n \"\"\"\n Support for BT Home Hub 5.\n-\n For more details about this platform, please refer to the documentation at\n https://home-assistant.io/components/device_tracker.bt_home_hub_5/\n \"\"\"\n+\n import logging\n import re\n-import xml.etree.ElementTree as ET\n-import json\n-from urllib.parse import unquote\n \n import requests\n-import voluptuous as vol\n+from html_table_extractor import HTMLParser", "hunk": "@@ -8,7 +8,7 @@ import logging\n import re\n \n import requests\n-from html_table_extractor import HTMLParser\n+from html_table_parser_python3 import HTMLTableParser\n \n import homeassistant.helpers.config_validation as cv\n import voluptuous as vol\n", "comment": "'html_table_extractor.HTMLParser' imported but unused", "ids": [312705, "cfd9ea479501fa47c03b259c2d21ff865c475659", "9cced7d5ffd1f9d5c2ad4299d25ff0fd6365df80"], "repo": "home-assistant-core", "ghid": 15096, "old": " import re\n import requests\n-from html_table_extractor import HTMLParser\n import homeassistant.helpers.config_validation as cv\n import voluptuous as vol", "new": " import re\n import requests\n+from html_table_parser_python3 import HTMLTableParser\n import homeassistant.helpers.config_validation as cv\n import voluptuous as vol", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -1,32 +1,33 @@\n \"\"\"\n Support for BT Home Hub 5.\n-\n For more details about this platform, please refer to the documentation at\n https://home-assistant.io/components/device_tracker.bt_home_hub_5/\n \"\"\"\n+\n import logging\n import re\n-import xml.etree.ElementTree as ET\n-import json\n-from urllib.parse import unquote\n \n import requests\n-import voluptuous as vol\n+from html_table_extractor import HTMLParser", "msg": "'html_table_extractor.HTMLParser' imported but unused"}
{"old_hunk": "@@ -80,7 +83,18 @@ def get(self, config):\n     host = lte_config[CONF_HOST]\n     password = lte_config[CONF_PASSWORD]\n \n-    eternalegypt = eternalegypt.LB2120(host, password)\n-    lte_data = LTEData(eternalegypt)\n+    websession = async_create_clientsession(\n+        hass, cookie_jar=aiohttp.CookieJar(unsafe=True))", "hunk": "@@ -83,8 +83,7 @@ async def _setup_lte(hass, lte_config):\n     host = lte_config[CONF_HOST]\n     password = lte_config[CONF_PASSWORD]\n \n-    websession = async_create_clientsession(\n-        hass, cookie_jar=aiohttp.CookieJar(unsafe=True))\n+    websession = hass.data[DATA_KEY].websession\n \n     modem = eternalegypt.Modem(hostname=host, websession=websession)\n     await modem.login(password=password)\n", "comment": "You create for every device a new session. Can you not share the session over all devices?", "ids": [313148, "b6f5d2f4fdf25bf48b54da5f55721c2c6b3a7792", "6689e8ccb49aaa1804a009bafcddb27667e99a68"], "repo": "home-assistant-core", "ghid": 15180, "old": "     host = lte_config[CONF_HOST]\n     password = lte_config[CONF_PASSWORD]\n-    websession = async_create_clientsession(\n-        hass, cookie_jar=aiohttp.CookieJar(unsafe=True))\n     modem = eternalegypt.Modem(hostname=host, websession=websession)\n     await modem.login(password=password)", "new": "     host = lte_config[CONF_HOST]\n     password = lte_config[CONF_PASSWORD]\n+    websession = hass.data[DATA_KEY].websession\n     modem = eternalegypt.Modem(hostname=host, websession=websession)\n     await modem.login(password=password)", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -80,7 +83,18 @@ def get(self, config):\n     host = lte_config[CONF_HOST]\n     password = lte_config[CONF_PASSWORD]\n \n-    eternalegypt = eternalegypt.LB2120(host, password)\n-    lte_data = LTEData(eternalegypt)\n+    websession = async_create_clientsession(\n+        hass, cookie_jar=aiohttp.CookieJar(unsafe=True))", "msg": "You create for every device a new session. Can you not share the session over all devices?"}
{"old_hunk": "@@ -303,6 +303,9 @@\n # Temperature attribute\n ATTR_TEMPERATURE = 'temperature'\n \n+# Current Temperature attribute", "hunk": "@@ -303,9 +303,6 @@ ATTR_DEVICE_CLASS = 'device_class'\n # Temperature attribute\n ATTR_TEMPERATURE = 'temperature'\n \n-# Current Temperature attribute\n-ATTR_CURRENT_TEMPERATURE = 'current_temperature'\n-\n # #### UNITS OF MEASUREMENT ####\n # Temperature units\n TEMP_CELSIUS = '°C'\n", "comment": "Please don't add this to main const but import it from climate component.", "ids": [313329, "766cabf119f3b9fe0acb2a7114095f83c49cc5b8", "3ac0d4af4cfe8f33e370167e78b47fef02bcfa87"], "repo": "home-assistant-core", "ghid": 15232, "old": " # Temperature attribute\n ATTR_TEMPERATURE = 'temperature'\n-# Current Temperature attribute\n-ATTR_CURRENT_TEMPERATURE = 'current_temperature'\n-\n # #### UNITS OF MEASUREMENT ####\n # Temperature units\n TEMP_CELSIUS = '°C'", "new": " # Temperature attribute\n ATTR_TEMPERATURE = 'temperature'\n # #### UNITS OF MEASUREMENT ####\n # Temperature units\n TEMP_CELSIUS = '°C'", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -303,6 +303,9 @@\n # Temperature attribute\n ATTR_TEMPERATURE = 'temperature'\n \n+# Current Temperature attribute", "msg": "Please don't add this to main const but import it from climate component."}
{"old_hunk": "@@ -71,7 +74,9 @@\n     vol.Required(CONF_ZONE_NAME): cv.string,\n     vol.Optional(CONF_ZONE_TYPE,\n                  default=DEFAULT_ZONE_TYPE): vol.Any(DEVICE_CLASSES_SCHEMA),\n-    vol.Optional(CONF_ZONE_RFID): cv.string})\n+    vol.Optional(CONF_ZONE_RFID): cv.string,\n+    vol.Optional(CONF_RELAY_ADDR): cv.byte,\n+    vol.Optional(CONF_RELAY_CHAN): cv.byte})", "hunk": "@@ -75,8 +75,10 @@ ZONE_SCHEMA = vol.Schema({\n     vol.Optional(CONF_ZONE_TYPE,\n                  default=DEFAULT_ZONE_TYPE): vol.Any(DEVICE_CLASSES_SCHEMA),\n     vol.Optional(CONF_ZONE_RFID): cv.string,\n-    vol.Optional(CONF_RELAY_ADDR): cv.byte,\n-    vol.Optional(CONF_RELAY_CHAN): cv.byte})\n+    vol.Inclusive(CONF_RELAY_ADDR, 'relaylocation',\n+                  'Relay address and channel must exist together'): cv.byte,\n+    vol.Inclusive(CONF_RELAY_CHAN, 'relaylocation',\n+                  'Relay address and channel must exist together'): cv.byte})\n \n CONFIG_SCHEMA = vol.Schema({\n     DOMAIN: vol.Schema({\n", "comment": "Is this required for the relay address to be effective? If so, mark both items with `vol.Inclusive`.", "ids": [313343, "497e5da688927feeece7ae0e9808f9690c8613e7", "0087aeaca49f7aa8d5c17436f2cc865eaeaeaf2d"], "repo": "home-assistant-core", "ghid": 15242, "old": "     vol.Optional(CONF_ZONE_TYPE,\n                  default=DEFAULT_ZONE_TYPE): vol.Any(DEVICE_CLASSES_SCHEMA),\n     vol.Optional(CONF_ZONE_RFID): cv.string,\n-    vol.Optional(CONF_RELAY_ADDR): cv.byte,\n-    vol.Optional(CONF_RELAY_CHAN): cv.byte})\n CONFIG_SCHEMA = vol.Schema({\n     DOMAIN: vol.Schema({", "new": "     vol.Optional(CONF_ZONE_TYPE,\n                  default=DEFAULT_ZONE_TYPE): vol.Any(DEVICE_CLASSES_SCHEMA),\n     vol.Optional(CONF_ZONE_RFID): cv.string,\n+    vol.Inclusive(CONF_RELAY_ADDR, 'relaylocation',\n+                  'Relay address and channel must exist together'): cv.byte,\n+    vol.Inclusive(CONF_RELAY_CHAN, 'relaylocation',\n+                  'Relay address and channel must exist together'): cv.byte})\n CONFIG_SCHEMA = vol.Schema({\n     DOMAIN: vol.Schema({", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -71,7 +74,9 @@\n     vol.Required(CONF_ZONE_NAME): cv.string,\n     vol.Optional(CONF_ZONE_TYPE,\n                  default=DEFAULT_ZONE_TYPE): vol.Any(DEVICE_CLASSES_SCHEMA),\n-    vol.Optional(CONF_ZONE_RFID): cv.string})\n+    vol.Optional(CONF_ZONE_RFID): cv.string,\n+    vol.Optional(CONF_RELAY_ADDR): cv.byte,\n+    vol.Optional(CONF_RELAY_CHAN): cv.byte})", "msg": "Is this required for the relay address to be effective? If so, mark both items with `vol.Inclusive`."}
{"old_hunk": "@@ -36,7 +36,7 @@\n     vol.Required(CONF_MODEL): vol.Any(MODEL_YI,\n                                       MODEL_XIAOFANG),\n     vol.Optional(CONF_PORT, default=DEFAULT_PORT): cv.string,\n-    vol.Optional(CONF_PATH, default=DEFAULT_PATH): cv.string,\n+    vol.Required(CONF_PATH, default=DEFAULT_PATH): cv.string,", "hunk": "@@ -36,7 +36,7 @@ PLATFORM_SCHEMA = PLATFORM_SCHEMA.extend({\n     vol.Required(CONF_MODEL): vol.Any(MODEL_YI,\n                                       MODEL_XIAOFANG),\n     vol.Optional(CONF_PORT, default=DEFAULT_PORT): cv.string,\n-    vol.Required(CONF_PATH, default=DEFAULT_PATH): cv.string,\n+    vol.Required(CONF_PATH): cv.string,\n     vol.Optional(CONF_USERNAME, default=DEFAULT_USERNAME): cv.string,\n     vol.Required(CONF_PASSWORD): cv.string,\n     vol.Optional(CONF_FFMPEG_ARGUMENTS): cv.string\n", "comment": "Making this required, should remove the default. It would make no sense to keep it.", "ids": [313409, "899090001a5a641229542acd9b54c825a1e5de86", "f36d62bd596d2eb807fd5dbd695bb976b9012b35"], "repo": "home-assistant-core", "ghid": 15265, "old": "     vol.Required(CONF_MODEL): vol.Any(MODEL_YI,\n                                       MODEL_XIAOFANG),\n     vol.Optional(CONF_PORT, default=DEFAULT_PORT): cv.string,\n-    vol.Required(CONF_PATH, default=DEFAULT_PATH): cv.string,\n     vol.Optional(CONF_USERNAME, default=DEFAULT_USERNAME): cv.string,\n     vol.Required(CONF_PASSWORD): cv.string,\n     vol.Optional(CONF_FFMPEG_ARGUMENTS): cv.string", "new": "     vol.Required(CONF_MODEL): vol.Any(MODEL_YI,\n                                       MODEL_XIAOFANG),\n     vol.Optional(CONF_PORT, default=DEFAULT_PORT): cv.string,\n+    vol.Required(CONF_PATH): cv.string,\n     vol.Optional(CONF_USERNAME, default=DEFAULT_USERNAME): cv.string,\n     vol.Required(CONF_PASSWORD): cv.string,\n     vol.Optional(CONF_FFMPEG_ARGUMENTS): cv.string", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -36,7 +36,7 @@\n     vol.Required(CONF_MODEL): vol.Any(MODEL_YI,\n                                       MODEL_XIAOFANG),\n     vol.Optional(CONF_PORT, default=DEFAULT_PORT): cv.string,\n-    vol.Optional(CONF_PATH, default=DEFAULT_PATH): cv.string,\n+    vol.Required(CONF_PATH, default=DEFAULT_PATH): cv.string,", "msg": "Making this required, should remove the default. It would make no sense to keep it."}
{"old_hunk": "@@ -23,6 +23,8 @@\n DEFAULT_TIMEOUT = 10\n DOMAIN = 'zoneminder'\n \n+SET_RUN_STATE = 'set_run_state'", "hunk": "@@ -23,7 +23,7 @@ DEFAULT_SSL = False\n DEFAULT_TIMEOUT = 10\n DOMAIN = 'zoneminder'\n \n-SET_RUN_STATE = 'set_run_state'\n+SERVICE_SET_RUN_STATE = 'set_run_state'\n \n LOGIN_RETRIES = 2\n \n", "comment": "Prefix the constant name with `SERVICE_`.", "ids": [313582, "6fec74204801a99c495b87d1e9272f24f26cec4d", "6976ba62f50f7a0c9081da9f7eac38fffdb0d401"], "repo": "home-assistant-core", "ghid": 15324, "old": " DEFAULT_TIMEOUT = 10\n DOMAIN = 'zoneminder'\n-SET_RUN_STATE = 'set_run_state'\n LOGIN_RETRIES = 2", "new": " DEFAULT_TIMEOUT = 10\n DOMAIN = 'zoneminder'\n+SERVICE_SET_RUN_STATE = 'set_run_state'\n LOGIN_RETRIES = 2", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -23,6 +23,8 @@\n DEFAULT_TIMEOUT = 10\n DOMAIN = 'zoneminder'\n \n+SET_RUN_STATE = 'set_run_state'", "msg": "Prefix the constant name with `SERVICE_`."}
{"old_hunk": "@@ -140,3 +141,29 @@ def update(self):\n             self._state = event['results'][str(self._monitor_id)]\n         except (TypeError, KeyError):\n             self._state = '0'\n+\n+\n+class ZMSensorRunState(Entity):\n+    \"\"\"Get the ZoneMinder run state.\"\"\"\n+\n+    def __init__(self):\n+        \"\"\"Initialize run state sensor.\"\"\"\n+        self._state = None\n+\n+    @property\n+    def name(self):\n+        \"\"\"Return the name of the sensor.\"\"\"\n+        return 'Run State'\n+\n+    @property\n+    def state(self):\n+        \"\"\"Return the state of the sensor.\"\"\"\n+        return self._state\n+\n+    def update(self):\n+        \"\"\"Update the sensor.\"\"\"\n+        state_name = zoneminder.get_active_state()\n+        if state_name is None:\n+            self._state = STATE_UNKNOWN", "hunk": "@@ -164,6 +164,6 @@ class ZMSensorRunState(Entity):\n         \"\"\"Update the sensor.\"\"\"\n         state_name = zoneminder.get_active_state()\n         if state_name is None:\n-            self._state = STATE_UNKNOWN\n+            self._state = None\n         else:\n             self._state = state_name\n", "comment": "Don't use `STATE_UNKNOWN`. Set state to `None` to represent unknown state.", "ids": [313581, "6fec74204801a99c495b87d1e9272f24f26cec4d", "6976ba62f50f7a0c9081da9f7eac38fffdb0d401"], "repo": "home-assistant-core", "ghid": 15324, "old": "         \"\"\"Update the sensor.\"\"\"\n         state_name = zoneminder.get_active_state()\n         if state_name is None:\n-            self._state = STATE_UNKNOWN\n         else:\n             self._state = state_name", "new": "         \"\"\"Update the sensor.\"\"\"\n         state_name = zoneminder.get_active_state()\n         if state_name is None:\n+            self._state = None\n         else:\n             self._state = state_name", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -140,3 +141,29 @@ def update(self):\n             self._state = event['results'][str(self._monitor_id)]\n         except (TypeError, KeyError):\n             self._state = '0'\n+\n+\n+class ZMSensorRunState(Entity):\n+    \"\"\"Get the ZoneMinder run state.\"\"\"\n+\n+    def __init__(self):\n+        \"\"\"Initialize run state sensor.\"\"\"\n+        self._state = None\n+\n+    @property\n+    def name(self):\n+        \"\"\"Return the name of the sensor.\"\"\"\n+        return 'Run State'\n+\n+    @property\n+    def state(self):\n+        \"\"\"Return the state of the sensor.\"\"\"\n+        return self._state\n+\n+    def update(self):\n+        \"\"\"Update the sensor.\"\"\"\n+        state_name = zoneminder.get_active_state()\n+        if state_name is None:\n+            self._state = STATE_UNKNOWN", "msg": "Don't use `STATE_UNKNOWN`. Set state to `None` to represent unknown state."}
{"old_hunk": "@@ -45,7 +45,9 @@\n                           config[CONF_BUFFER_SIZE],\n                           config[CONF_TIMEOUT])]\n \n-    hass.http.register_view(CameraPushReceiver(cameras,\n+    component = hass.data.get(DOMAIN)", "hunk": "@@ -41,13 +41,14 @@ PLATFORM_SCHEMA = PLATFORM_SCHEMA.extend({\n async def async_setup_platform(hass, config, async_add_devices,\n                                discovery_info=None):\n     \"\"\"Set up the Push Camera platform.\"\"\"\n+    if PUSH_CAMERA_DATA not in hass.data:\n+        hass.data[PUSH_CAMERA_DATA] = {}\n+\n     cameras = [PushCamera(config[CONF_NAME],\n                           config[CONF_BUFFER_SIZE],\n                           config[CONF_TIMEOUT])]\n \n-    component = hass.data.get(DOMAIN)\n-\n-    hass.http.register_view(CameraPushReceiver(component,\n+    hass.http.register_view(CameraPushReceiver(hass,\n                                                config[CONF_IMAGE_FIELD]))\n \n     async_add_devices(cameras)\n", "comment": "You're not allowed to do this. You have to keep track of your own entities yourself. This is an internal thing of the camera component.", "ids": [313647, "9a2c46e55bef30152bed48d00b62f970490746da", "07ba00480ba4dc7addcd9399ccf8c4e02f57990e"], "repo": "home-assistant-core", "ghid": 15334, "old": " async def async_setup_platform(hass, config, async_add_devices,\n                                discovery_info=None):\n     \"\"\"Set up the Push Camera platform.\"\"\"\n     cameras = [PushCamera(config[CONF_NAME],\n                           config[CONF_BUFFER_SIZE],\n                           config[CONF_TIMEOUT])]\n-    component = hass.data.get(DOMAIN)\n-\n-    hass.http.register_view(CameraPushReceiver(component,\n                                                config[CONF_IMAGE_FIELD]))\n     async_add_devices(cameras)", "new": " async def async_setup_platform(hass, config, async_add_devices,\n                                discovery_info=None):\n     \"\"\"Set up the Push Camera platform.\"\"\"\n+    if PUSH_CAMERA_DATA not in hass.data:\n+        hass.data[PUSH_CAMERA_DATA] = {}\n+\n     cameras = [PushCamera(config[CONF_NAME],\n                           config[CONF_BUFFER_SIZE],\n                           config[CONF_TIMEOUT])]\n+    hass.http.register_view(CameraPushReceiver(hass,\n                                                config[CONF_IMAGE_FIELD]))\n     async_add_devices(cameras)", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -45,7 +45,9 @@\n                           config[CONF_BUFFER_SIZE],\n                           config[CONF_TIMEOUT])]\n \n-    hass.http.register_view(CameraPushReceiver(cameras,\n+    component = hass.data.get(DOMAIN)", "msg": "You're not allowed to do this. You have to keep track of your own entities yourself. This is an internal thing of the camera component."}
{"old_hunk": "@@ -46,6 +60,45 @@ def get_scanner(hass, config):\n \n \n class TplinkDeviceScanner(DeviceScanner):\n+    \"\"\"Queries the router for connected devices.\"\"\"\n+\n+    def __init__(self, config):\n+        \"\"\"Initialize the scanner.\"\"\"\n+        self.host = config[CONF_HOST]\n+        self.password = config[CONF_PASSWORD]\n+        self.username = config[CONF_USERNAME]\n+\n+        self.last_results = {}\n+        self.success_init = self._update_info()\n+\n+    def scan_devices(self):\n+        \"\"\"Scan for new devices and return a list with found device IDs.\"\"\"\n+        self._update_info()\n+        return self.last_results.keys()\n+\n+    def get_device_name(self, device):\n+        \"\"\"Get the name of the device.\"\"\"\n+        return self.last_results.get(device)\n+\n+    def _update_info(self):\n+        \"\"\"Ensure the information from the TP-Link router is up to date.\n+\n+        Return boolean if scanning successful.\n+        \"\"\"\n+        from tplink.tplink import TpLinkClient\n+        _LOGGER.info(\"Loading wireless clients...\")\n+        client = TpLinkClient(", "hunk": "@@ -64,9 +64,12 @@ class TplinkDeviceScanner(DeviceScanner):\n \n     def __init__(self, config):\n         \"\"\"Initialize the scanner.\"\"\"\n+        from tplink.tplink import TpLinkClient\n         self.host = config[CONF_HOST]\n         self.password = config[CONF_PASSWORD]\n         self.username = config[CONF_USERNAME]\n+        self.tplink_client = TpLinkClient(\n+            self.password, host=self.host, username=self.username)\n \n         self.last_results = {}\n         self.success_init = self._update_info()\n", "comment": "Do we need to create a new client on each update?", "ids": [313858, "1dc8989c630f018d8d98050d375b682fd58b7028", "dcff6b5ed191950230cd8c5efd4f16ebf8109f94"], "repo": "home-assistant-core", "ghid": 15356, "old": "     def __init__(self, config):\n         \"\"\"Initialize the scanner.\"\"\"\n         self.host = config[CONF_HOST]\n         self.password = config[CONF_PASSWORD]\n         self.username = config[CONF_USERNAME]\n         self.last_results = {}\n         self.success_init = self._update_info()", "new": "     def __init__(self, config):\n         \"\"\"Initialize the scanner.\"\"\"\n+        from tplink.tplink import TpLinkClient\n         self.host = config[CONF_HOST]\n         self.password = config[CONF_PASSWORD]\n         self.username = config[CONF_USERNAME]\n+        self.tplink_client = TpLinkClient(\n+            self.password, host=self.host, username=self.username)\n         self.last_results = {}\n         self.success_init = self._update_info()", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -46,6 +60,45 @@ def get_scanner(hass, config):\n \n \n class TplinkDeviceScanner(DeviceScanner):\n+    \"\"\"Queries the router for connected devices.\"\"\"\n+\n+    def __init__(self, config):\n+        \"\"\"Initialize the scanner.\"\"\"\n+        self.host = config[CONF_HOST]\n+        self.password = config[CONF_PASSWORD]\n+        self.username = config[CONF_USERNAME]\n+\n+        self.last_results = {}\n+        self.success_init = self._update_info()\n+\n+    def scan_devices(self):\n+        \"\"\"Scan for new devices and return a list with found device IDs.\"\"\"\n+        self._update_info()\n+        return self.last_results.keys()\n+\n+    def get_device_name(self, device):\n+        \"\"\"Get the name of the device.\"\"\"\n+        return self.last_results.get(device)\n+\n+    def _update_info(self):\n+        \"\"\"Ensure the information from the TP-Link router is up to date.\n+\n+        Return boolean if scanning successful.\n+        \"\"\"\n+        from tplink.tplink import TpLinkClient\n+        _LOGGER.info(\"Loading wireless clients...\")\n+        client = TpLinkClient(", "msg": "Do we need to create a new client on each update?"}
{"old_hunk": "@@ -0,0 +1,104 @@\n+\"\"\"Helpers to resolve client ID/secret.\"\"\"\n+from ipaddress import ip_address\n+from urllib.parse import urlparse\n+\n+\n+def verify_redirect_uri(client_id, redirect_uri):\n+    \"\"\"Verify that the client and redirect uri match.\"\"\"\n+    try:\n+        client_id_parts = _parse_client_id(client_id)\n+    except ValueError as err:\n+        return False\n+\n+    redirect_parts = _parse_url(redirect_uri)\n+\n+    # IndieAuth 4.2.2 allows for redirect_uri to be on different domain\n+    # but needs to be specified in link tag when fetching `client_id`.\n+    # This is not implemented.\n+\n+    # Verify redirect url and client url have same scheme and domain.\n+    return (\n+        client_id_parts.scheme == redirect_parts.scheme and\n+        client_id_parts.netloc == redirect_parts.netloc\n+    )\n+\n+\n+def verify_client_id(client_id):\n+    \"\"\"Verify that the client id is valid.\"\"\"\n+    try:\n+        _parse_client_id(client_id)\n+        return True\n+    except ValueError:\n+        return False\n+\n+\n+def _parse_url(url):\n+    \"\"\"Parse a url in parts and canonicalize according to IndieAuth.\"\"\"\n+    parts = urlparse(url)\n+\n+    # Canonicalize a url according to IndieAuth 3.2.\n+\n+    # SHOULD convert the hostname to lowercase\n+    parts = parts._replace(netloc=parts.netloc.lower())\n+\n+    # If a URL with no path component is ever encountered,\n+    # it MUST be treated as if it had the path /.\n+    if parts.path == '':\n+        parts = parts._replace(path='/')\n+\n+    return parts\n+\n+\n+def _parse_client_id(client_id):\n+    \"\"\"Test if client id is a valid URL according to IndieAuth section 3.2.\n+\n+    https://indieauth.spec.indieweb.org/#client-identifier\n+    \"\"\"\n+    parts = _parse_url(client_id)\n+\n+    # Client identifier URLs\n+    # MUST have either an https or http scheme\n+    if parts.scheme not in ('http', 'https'):\n+        raise ValueError()\n+\n+    # MUST contain a path component\n+    # Handled by url canonicalization.\n+\n+    # MUST NOT contain single-dot or double-dot path segments\n+    if any(segment in ('.', '..') for segment in parts.path.split('/')):\n+        raise ValueError(\n+            'Client ID cannot contain single-dot or double-dot path segments')\n+\n+    # MUST NOT contain a fragment component\n+    if parts.fragment != '':\n+        raise ValueError('Client ID cannot contain a fragment')\n+\n+    # MUST NOT contain a username or password component\n+    if parts.username is not None:\n+        raise ValueError('Client ID cannot contain username')\n+\n+    if parts.password is not None:\n+        raise ValueError('Client ID cannot contain password')\n+\n+    # MAY contain a port", "hunk": "@@ -10,7 +10,7 @@\n     <h1>Error 503 Backend is unhealthy</h1>\n     <p>Backend is unhealthy</p>\n     <h3>Guru Mediation:</h3>\n-    <p>Details: cache-sea4459-SEA 1645537005 3894949523</p>\n+    <p>Details: cache-sea4462-SEA 1645537005 1639447967</p>\n     <hr>\n     <p>Varnish cache server</p>\n   </body>\n", "comment": "EDIT: I was wrong. There is no issue. It has two paragraph in spec very close, `3.1 User Profile URL` and `3.2 Client Identifier`, the only different between these two are `User Profile URL` do not allow port and ip address, `Client Identifier` allow port and 127.0.0.1 I confused those two.", "ids": [314048, "7aba140e1d8c36ab25a8cbace95f2584c21bd3af", "02f8f79fd1f409b83f266c00b05441a2a8ab098b"], "repo": "home-assistant-core", "ghid": 15369, "old": "     <h1>Error 503 Backend is unhealthy</h1>\n     <p>Backend is unhealthy</p>\n     <h3>Guru Mediation:</h3>\n-    <p>Details: cache-sea4459-SEA 1645537005 3894949523</p>\n     <hr>\n     <p>Varnish cache server</p>\n   </body>", "new": "     <h1>Error 503 Backend is unhealthy</h1>\n     <p>Backend is unhealthy</p>\n     <h3>Guru Mediation:</h3>\n+    <p>Details: cache-sea4462-SEA 1645537005 1639447967</p>\n     <hr>\n     <p>Varnish cache server</p>\n   </body>", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -0,0 +1,104 @@\n+\"\"\"Helpers to resolve client ID/secret.\"\"\"\n+from ipaddress import ip_address\n+from urllib.parse import urlparse\n+\n+\n+def verify_redirect_uri(client_id, redirect_uri):\n+    \"\"\"Verify that the client and redirect uri match.\"\"\"\n+    try:\n+        client_id_parts = _parse_client_id(client_id)\n+    except ValueError as err:\n+        return False\n+\n+    redirect_parts = _parse_url(redirect_uri)\n+\n+    # IndieAuth 4.2.2 allows for redirect_uri to be on different domain\n+    # but needs to be specified in link tag when fetching `client_id`.\n+    # This is not implemented.\n+\n+    # Verify redirect url and client url have same scheme and domain.\n+    return (\n+        client_id_parts.scheme == redirect_parts.scheme and\n+        client_id_parts.netloc == redirect_parts.netloc\n+    )\n+\n+\n+def verify_client_id(client_id):\n+    \"\"\"Verify that the client id is valid.\"\"\"\n+    try:\n+        _parse_client_id(client_id)\n+        return True\n+    except ValueError:\n+        return False\n+\n+\n+def _parse_url(url):\n+    \"\"\"Parse a url in parts and canonicalize according to IndieAuth.\"\"\"\n+    parts = urlparse(url)\n+\n+    # Canonicalize a url according to IndieAuth 3.2.\n+\n+    # SHOULD convert the hostname to lowercase\n+    parts = parts._replace(netloc=parts.netloc.lower())\n+\n+    # If a URL with no path component is ever encountered,\n+    # it MUST be treated as if it had the path /.\n+    if parts.path == '':\n+        parts = parts._replace(path='/')\n+\n+    return parts\n+\n+\n+def _parse_client_id(client_id):\n+    \"\"\"Test if client id is a valid URL according to IndieAuth section 3.2.\n+\n+    https://indieauth.spec.indieweb.org/#client-identifier\n+    \"\"\"\n+    parts = _parse_url(client_id)\n+\n+    # Client identifier URLs\n+    # MUST have either an https or http scheme\n+    if parts.scheme not in ('http', 'https'):\n+        raise ValueError()\n+\n+    # MUST contain a path component\n+    # Handled by url canonicalization.\n+\n+    # MUST NOT contain single-dot or double-dot path segments\n+    if any(segment in ('.', '..') for segment in parts.path.split('/')):\n+        raise ValueError(\n+            'Client ID cannot contain single-dot or double-dot path segments')\n+\n+    # MUST NOT contain a fragment component\n+    if parts.fragment != '':\n+        raise ValueError('Client ID cannot contain a fragment')\n+\n+    # MUST NOT contain a username or password component\n+    if parts.username is not None:\n+        raise ValueError('Client ID cannot contain username')\n+\n+    if parts.password is not None:\n+        raise ValueError('Client ID cannot contain password')\n+\n+    # MAY contain a port", "msg": "EDIT: I was wrong. There is no issue. It has two paragraph in spec very close, `3.1 User Profile URL` and `3.2 Client Identifier`, the only different between these two are `User Profile URL` do not allow port and ip address, `Client Identifier` allow port and 127.0.0.1 I confused those two."}
{"old_hunk": "@@ -196,3 +197,51 @@ def get(self, request, node_id):\n                                       'label': value.label,\n                                       'length': len(value.data)}\n         return self.json(usercodes)\n+\n+\n+class ZWaveProtectionView(HomeAssistantView):\n+    \"\"\"View for the protection commandclass of a node.\"\"\"\n+\n+    url = r\"/api/zwave/protection/{node_id:\\d+}\"\n+    name = \"api:zwave:protection\"\n+\n+    @ha.callback\n+    def get(self, request, node_id):\n+        \"\"\"Retrieve the protection commandclass options of node.\"\"\"\n+        nodeid = int(node_id)\n+        hass = request.app['hass']\n+        network = hass.data.get(const.DATA_NETWORK)\n+        node = network.nodes.get(nodeid)\n+        if node is None:\n+            return self.json_message('Node not found', HTTP_NOT_FOUND)\n+        protection_options = {}\n+        if not node.has_command_class(const.COMMAND_CLASS_PROTECTION):\n+            return self.json(protection_options)\n+        protections = node.get_protections()\n+        protection_options = {\n+            'value_id': '{0:d}'.format(list(protections.keys())[0]),", "hunk": "@@ -219,7 +219,7 @@ class ZWaveProtectionView(HomeAssistantView):\n             return self.json(protection_options)\n         protections = node.get_protections()\n         protection_options = {\n-            'value_id': '{0:d}'.format(list(protections.keys())[0]),\n+            'value_id': '{0:d}'.format(list(protections)[0]),\n             'selected': node.get_protection_item(list(protections.keys())[0]),\n             'options': node.get_protection_items(list(protections.keys())[0])}\n         return self.json(protection_options)\n", "comment": "I don't think we need the `.keys()`. Passing a dict into a list copy will automatically make a list out of the keys.", "ids": [314156, "48659b1767f064cdcdb0ecf0d7255bf8624ed3dd", "25f3445a30ee4b989064c82fc4fbfe52c6c33d36"], "repo": "home-assistant-core", "ghid": 15390, "old": "             return self.json(protection_options)\n         protections = node.get_protections()\n         protection_options = {\n-            'value_id': '{0:d}'.format(list(protections.keys())[0]),\n             'selected': node.get_protection_item(list(protections.keys())[0]),\n             'options': node.get_protection_items(list(protections.keys())[0])}\n         return self.json(protection_options)", "new": "             return self.json(protection_options)\n         protections = node.get_protections()\n         protection_options = {\n+            'value_id': '{0:d}'.format(list(protections)[0]),\n             'selected': node.get_protection_item(list(protections.keys())[0]),\n             'options': node.get_protection_items(list(protections.keys())[0])}\n         return self.json(protection_options)", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -196,3 +197,51 @@ def get(self, request, node_id):\n                                       'label': value.label,\n                                       'length': len(value.data)}\n         return self.json(usercodes)\n+\n+\n+class ZWaveProtectionView(HomeAssistantView):\n+    \"\"\"View for the protection commandclass of a node.\"\"\"\n+\n+    url = r\"/api/zwave/protection/{node_id:\\d+}\"\n+    name = \"api:zwave:protection\"\n+\n+    @ha.callback\n+    def get(self, request, node_id):\n+        \"\"\"Retrieve the protection commandclass options of node.\"\"\"\n+        nodeid = int(node_id)\n+        hass = request.app['hass']\n+        network = hass.data.get(const.DATA_NETWORK)\n+        node = network.nodes.get(nodeid)\n+        if node is None:\n+            return self.json_message('Node not found', HTTP_NOT_FOUND)\n+        protection_options = {}\n+        if not node.has_command_class(const.COMMAND_CLASS_PROTECTION):\n+            return self.json(protection_options)\n+        protections = node.get_protections()\n+        protection_options = {\n+            'value_id': '{0:d}'.format(list(protections.keys())[0]),", "msg": "I don't think we need the `.keys()`. Passing a dict into a list copy will automatically make a list out of the keys."}
{"old_hunk": "@@ -0,0 +1,134 @@\n+\"\"\"\n+Support for Tuya Smart devices.\n+\n+For more details about this component, please refer to the documentation at\n+https://home-assistant.io/components/tuya/\n+\"\"\"\n+from datetime import timedelta\n+import logging\n+import time\n+import voluptuous as vol\n+\n+import homeassistant.helpers.config_validation as cv\n+from homeassistant.const import (CONF_USERNAME, CONF_PASSWORD)\n+from homeassistant.helpers import discovery\n+from homeassistant.helpers.entity import Entity\n+from homeassistant.helpers.event import track_time_interval\n+\n+REQUIREMENTS = ['tuyapy==0.1.0']\n+\n+_LOGGER = logging.getLogger(__name__)\n+\n+CONF_COUNTRYCODE = 'country_code'\n+\n+DOMAIN = 'tuya'\n+DATA_TUYA = 'data_tuya'\n+\n+SERVICE_FORCE_UPDATE = 'force_update'\n+SERVICE_PULL_DEVICES = 'pull_devices'\n+\n+CONFIG_SCHEMA = vol.Schema({\n+    DOMAIN: vol.Schema({\n+        vol.Required(CONF_PASSWORD): cv.string,\n+        vol.Required(CONF_USERNAME): cv.string,\n+        vol.Required(CONF_COUNTRYCODE): cv.string\n+    })\n+}, extra=vol.ALLOW_EXTRA)\n+\n+TUYA_COMPONENT = ['light']\n+\n+\n+def setup(hass, config):\n+    \"\"\"Set up Tuya Component.\"\"\"\n+    from tuyapy import TuyaApi\n+\n+    tuya = TuyaApi()\n+    username = config[DOMAIN][CONF_USERNAME]\n+    password = config[DOMAIN][CONF_PASSWORD]\n+    country_code = config[DOMAIN][CONF_COUNTRYCODE]\n+\n+    hass.data[DATA_TUYA] = tuya\n+\n+    tuya.init(username, password, country_code)\n+\n+    hass.data[DOMAIN] = {\n+        'dev_ids': [],\n+        'entities': {}\n+    }\n+\n+    dev_types = tuya.get_devTypes()\n+    for dev_type in dev_types:\n+        discovery.load_platform(hass, dev_type, DOMAIN, {}, config)", "hunk": "@@ -10,7 +10,7 @@\n     <h1>Error 503 Backend is unhealthy</h1>\n     <p>Backend is unhealthy</p>\n     <h3>Guru Mediation:</h3>\n-    <p>Details: cache-sea4436-SEA 1645537038 1020790792</p>\n+    <p>Details: cache-sea4427-SEA 1645537038 885008312</p>\n     <hr>\n     <p>Varnish cache server</p>\n   </body>\n", "comment": "Why does the tuya library know about the home assistant component types? That's very weird. Add a mapping, ie dictionary, between the tuya device types and home assistant components, and use that here.", "ids": [314185, "6dfb75fe29615614db216ace8c4f938c32a43418", "36750e56069ec8281d8b085b2b443a98198036c2"], "repo": "home-assistant-core", "ghid": 15399, "old": "     <h1>Error 503 Backend is unhealthy</h1>\n     <p>Backend is unhealthy</p>\n     <h3>Guru Mediation:</h3>\n-    <p>Details: cache-sea4436-SEA 1645537038 1020790792</p>\n     <hr>\n     <p>Varnish cache server</p>\n   </body>", "new": "     <h1>Error 503 Backend is unhealthy</h1>\n     <p>Backend is unhealthy</p>\n     <h3>Guru Mediation:</h3>\n+    <p>Details: cache-sea4427-SEA 1645537038 885008312</p>\n     <hr>\n     <p>Varnish cache server</p>\n   </body>", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -0,0 +1,134 @@\n+\"\"\"\n+Support for Tuya Smart devices.\n+\n+For more details about this component, please refer to the documentation at\n+https://home-assistant.io/components/tuya/\n+\"\"\"\n+from datetime import timedelta\n+import logging\n+import time\n+import voluptuous as vol\n+\n+import homeassistant.helpers.config_validation as cv\n+from homeassistant.const import (CONF_USERNAME, CONF_PASSWORD)\n+from homeassistant.helpers import discovery\n+from homeassistant.helpers.entity import Entity\n+from homeassistant.helpers.event import track_time_interval\n+\n+REQUIREMENTS = ['tuyapy==0.1.0']\n+\n+_LOGGER = logging.getLogger(__name__)\n+\n+CONF_COUNTRYCODE = 'country_code'\n+\n+DOMAIN = 'tuya'\n+DATA_TUYA = 'data_tuya'\n+\n+SERVICE_FORCE_UPDATE = 'force_update'\n+SERVICE_PULL_DEVICES = 'pull_devices'\n+\n+CONFIG_SCHEMA = vol.Schema({\n+    DOMAIN: vol.Schema({\n+        vol.Required(CONF_PASSWORD): cv.string,\n+        vol.Required(CONF_USERNAME): cv.string,\n+        vol.Required(CONF_COUNTRYCODE): cv.string\n+    })\n+}, extra=vol.ALLOW_EXTRA)\n+\n+TUYA_COMPONENT = ['light']\n+\n+\n+def setup(hass, config):\n+    \"\"\"Set up Tuya Component.\"\"\"\n+    from tuyapy import TuyaApi\n+\n+    tuya = TuyaApi()\n+    username = config[DOMAIN][CONF_USERNAME]\n+    password = config[DOMAIN][CONF_PASSWORD]\n+    country_code = config[DOMAIN][CONF_COUNTRYCODE]\n+\n+    hass.data[DATA_TUYA] = tuya\n+\n+    tuya.init(username, password, country_code)\n+\n+    hass.data[DOMAIN] = {\n+        'dev_ids': [],\n+        'entities': {}\n+    }\n+\n+    dev_types = tuya.get_devTypes()\n+    for dev_type in dev_types:\n+        discovery.load_platform(hass, dev_type, DOMAIN, {}, config)", "msg": "Why does the tuya library know about the home assistant component types? That's very weird. Add a mapping, ie dictionary, between the tuya device types and home assistant components, and use that here."}
{"old_hunk": "@@ -0,0 +1,134 @@\n+\"\"\"\n+Support for Tuya Smart devices.\n+\n+For more details about this component, please refer to the documentation at\n+https://home-assistant.io/components/tuya/\n+\"\"\"\n+from datetime import timedelta\n+import logging\n+import time\n+import voluptuous as vol\n+\n+import homeassistant.helpers.config_validation as cv\n+from homeassistant.const import (CONF_USERNAME, CONF_PASSWORD)\n+from homeassistant.helpers import discovery\n+from homeassistant.helpers.entity import Entity\n+from homeassistant.helpers.event import track_time_interval\n+\n+REQUIREMENTS = ['tuyapy==0.1.0']\n+\n+_LOGGER = logging.getLogger(__name__)\n+\n+CONF_COUNTRYCODE = 'country_code'\n+\n+DOMAIN = 'tuya'\n+DATA_TUYA = 'data_tuya'\n+\n+SERVICE_FORCE_UPDATE = 'force_update'\n+SERVICE_PULL_DEVICES = 'pull_devices'\n+\n+CONFIG_SCHEMA = vol.Schema({\n+    DOMAIN: vol.Schema({\n+        vol.Required(CONF_PASSWORD): cv.string,\n+        vol.Required(CONF_USERNAME): cv.string,\n+        vol.Required(CONF_COUNTRYCODE): cv.string\n+    })\n+}, extra=vol.ALLOW_EXTRA)\n+\n+TUYA_COMPONENT = ['light']\n+\n+\n+def setup(hass, config):\n+    \"\"\"Set up Tuya Component.\"\"\"\n+    from tuyapy import TuyaApi\n+\n+    tuya = TuyaApi()\n+    username = config[DOMAIN][CONF_USERNAME]\n+    password = config[DOMAIN][CONF_PASSWORD]\n+    country_code = config[DOMAIN][CONF_COUNTRYCODE]\n+\n+    hass.data[DATA_TUYA] = tuya\n+\n+    tuya.init(username, password, country_code)\n+\n+    hass.data[DOMAIN] = {\n+        'dev_ids': [],\n+        'entities': {}\n+    }\n+\n+    dev_types = tuya.get_devTypes()\n+    for dev_type in dev_types:\n+        discovery.load_platform(hass, dev_type, DOMAIN, {}, config)\n+\n+    def poll_devices_update(event_time):\n+        \"\"\"Check if accesstoken is expired and pull device list from server.\"\"\"\n+        devices = tuya.poll_devices_update()\n+        dev_types = tuya.get_devTypes()\n+\n+        if devices is None:\n+            return None\n+        for dev_type in dev_types:\n+            discovery.load_platform(hass, dev_type, DOMAIN, {}, config)\n+        newlist_ids = []\n+        for device in devices:\n+            newlist_ids.append(device.get('id'))\n+        for entity_list in hass.data[DOMAIN]['entities'].values():\n+            for entity in entity_list:\n+                if entity.object_id not in newlist_ids:\n+                    hass.add_job(entity.async_remove())\n+                    entity_list.remove(entity)\n+                    hass.data[DOMAIN]['dev_ids'].remove(entity.object_id)\n+\n+    track_time_interval(hass, poll_devices_update, timedelta(minutes=5))\n+\n+    hass.services.register(DOMAIN, SERVICE_PULL_DEVICES, poll_devices_update)\n+\n+    def force_update(call):\n+        \"\"\"Force all devices to pull data.\"\"\"\n+        _LOGGER.info(\"Refreshing Device Data From Tuya\")\n+        for entity_list in hass.data[DOMAIN]['entities'].values():\n+            for entity in entity_list:\n+                time.sleep(0.5)", "hunk": "@@ -10,7 +10,7 @@\n     <h1>Error 503 Backend is unhealthy</h1>\n     <p>Backend is unhealthy</p>\n     <h3>Guru Mediation:</h3>\n-    <p>Details: cache-sea4436-SEA 1645537039 1020799077</p>\n+    <p>Details: cache-sea4427-SEA 1645537039 885016388</p>\n     <hr>\n     <p>Varnish cache server</p>\n   </body>\n", "comment": "Sleeping is not allowed. Sync sleep would exhaust our thread pool. If we really need to sleep it should be done async.", "ids": [314188, "6dfb75fe29615614db216ace8c4f938c32a43418", "36750e56069ec8281d8b085b2b443a98198036c2"], "repo": "home-assistant-core", "ghid": 15399, "old": "     <h1>Error 503 Backend is unhealthy</h1>\n     <p>Backend is unhealthy</p>\n     <h3>Guru Mediation:</h3>\n-    <p>Details: cache-sea4436-SEA 1645537039 1020799077</p>\n     <hr>\n     <p>Varnish cache server</p>\n   </body>", "new": "     <h1>Error 503 Backend is unhealthy</h1>\n     <p>Backend is unhealthy</p>\n     <h3>Guru Mediation:</h3>\n+    <p>Details: cache-sea4427-SEA 1645537039 885016388</p>\n     <hr>\n     <p>Varnish cache server</p>\n   </body>", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -0,0 +1,134 @@\n+\"\"\"\n+Support for Tuya Smart devices.\n+\n+For more details about this component, please refer to the documentation at\n+https://home-assistant.io/components/tuya/\n+\"\"\"\n+from datetime import timedelta\n+import logging\n+import time\n+import voluptuous as vol\n+\n+import homeassistant.helpers.config_validation as cv\n+from homeassistant.const import (CONF_USERNAME, CONF_PASSWORD)\n+from homeassistant.helpers import discovery\n+from homeassistant.helpers.entity import Entity\n+from homeassistant.helpers.event import track_time_interval\n+\n+REQUIREMENTS = ['tuyapy==0.1.0']\n+\n+_LOGGER = logging.getLogger(__name__)\n+\n+CONF_COUNTRYCODE = 'country_code'\n+\n+DOMAIN = 'tuya'\n+DATA_TUYA = 'data_tuya'\n+\n+SERVICE_FORCE_UPDATE = 'force_update'\n+SERVICE_PULL_DEVICES = 'pull_devices'\n+\n+CONFIG_SCHEMA = vol.Schema({\n+    DOMAIN: vol.Schema({\n+        vol.Required(CONF_PASSWORD): cv.string,\n+        vol.Required(CONF_USERNAME): cv.string,\n+        vol.Required(CONF_COUNTRYCODE): cv.string\n+    })\n+}, extra=vol.ALLOW_EXTRA)\n+\n+TUYA_COMPONENT = ['light']\n+\n+\n+def setup(hass, config):\n+    \"\"\"Set up Tuya Component.\"\"\"\n+    from tuyapy import TuyaApi\n+\n+    tuya = TuyaApi()\n+    username = config[DOMAIN][CONF_USERNAME]\n+    password = config[DOMAIN][CONF_PASSWORD]\n+    country_code = config[DOMAIN][CONF_COUNTRYCODE]\n+\n+    hass.data[DATA_TUYA] = tuya\n+\n+    tuya.init(username, password, country_code)\n+\n+    hass.data[DOMAIN] = {\n+        'dev_ids': [],\n+        'entities': {}\n+    }\n+\n+    dev_types = tuya.get_devTypes()\n+    for dev_type in dev_types:\n+        discovery.load_platform(hass, dev_type, DOMAIN, {}, config)\n+\n+    def poll_devices_update(event_time):\n+        \"\"\"Check if accesstoken is expired and pull device list from server.\"\"\"\n+        devices = tuya.poll_devices_update()\n+        dev_types = tuya.get_devTypes()\n+\n+        if devices is None:\n+            return None\n+        for dev_type in dev_types:\n+            discovery.load_platform(hass, dev_type, DOMAIN, {}, config)\n+        newlist_ids = []\n+        for device in devices:\n+            newlist_ids.append(device.get('id'))\n+        for entity_list in hass.data[DOMAIN]['entities'].values():\n+            for entity in entity_list:\n+                if entity.object_id not in newlist_ids:\n+                    hass.add_job(entity.async_remove())\n+                    entity_list.remove(entity)\n+                    hass.data[DOMAIN]['dev_ids'].remove(entity.object_id)\n+\n+    track_time_interval(hass, poll_devices_update, timedelta(minutes=5))\n+\n+    hass.services.register(DOMAIN, SERVICE_PULL_DEVICES, poll_devices_update)\n+\n+    def force_update(call):\n+        \"\"\"Force all devices to pull data.\"\"\"\n+        _LOGGER.info(\"Refreshing Device Data From Tuya\")\n+        for entity_list in hass.data[DOMAIN]['entities'].values():\n+            for entity in entity_list:\n+                time.sleep(0.5)", "msg": "Sleeping is not allowed. Sync sleep would exhaust our thread pool. If we really need to sleep it should be done async."}
{"old_hunk": "@@ -0,0 +1,134 @@\n+\"\"\"\n+Support for Tuya Smart devices.\n+\n+For more details about this component, please refer to the documentation at\n+https://home-assistant.io/components/tuya/\n+\"\"\"\n+from datetime import timedelta\n+import logging\n+import time\n+import voluptuous as vol\n+\n+import homeassistant.helpers.config_validation as cv\n+from homeassistant.const import (CONF_USERNAME, CONF_PASSWORD)\n+from homeassistant.helpers import discovery\n+from homeassistant.helpers.entity import Entity\n+from homeassistant.helpers.event import track_time_interval\n+\n+REQUIREMENTS = ['tuyapy==0.1.0']\n+\n+_LOGGER = logging.getLogger(__name__)\n+\n+CONF_COUNTRYCODE = 'country_code'\n+\n+DOMAIN = 'tuya'\n+DATA_TUYA = 'data_tuya'\n+\n+SERVICE_FORCE_UPDATE = 'force_update'\n+SERVICE_PULL_DEVICES = 'pull_devices'\n+\n+CONFIG_SCHEMA = vol.Schema({\n+    DOMAIN: vol.Schema({\n+        vol.Required(CONF_PASSWORD): cv.string,\n+        vol.Required(CONF_USERNAME): cv.string,\n+        vol.Required(CONF_COUNTRYCODE): cv.string\n+    })\n+}, extra=vol.ALLOW_EXTRA)\n+\n+TUYA_COMPONENT = ['light']\n+\n+\n+def setup(hass, config):\n+    \"\"\"Set up Tuya Component.\"\"\"\n+    from tuyapy import TuyaApi\n+\n+    tuya = TuyaApi()\n+    username = config[DOMAIN][CONF_USERNAME]\n+    password = config[DOMAIN][CONF_PASSWORD]\n+    country_code = config[DOMAIN][CONF_COUNTRYCODE]\n+\n+    hass.data[DATA_TUYA] = tuya\n+\n+    tuya.init(username, password, country_code)\n+\n+    hass.data[DOMAIN] = {\n+        'dev_ids': [],\n+        'entities': {}\n+    }\n+\n+    dev_types = tuya.get_devTypes()\n+    for dev_type in dev_types:\n+        discovery.load_platform(hass, dev_type, DOMAIN, {}, config)\n+\n+    def poll_devices_update(event_time):\n+        \"\"\"Check if accesstoken is expired and pull device list from server.\"\"\"\n+        devices = tuya.poll_devices_update()\n+        dev_types = tuya.get_devTypes()\n+\n+        if devices is None:\n+            return None\n+        for dev_type in dev_types:\n+            discovery.load_platform(hass, dev_type, DOMAIN, {}, config)\n+        newlist_ids = []\n+        for device in devices:\n+            newlist_ids.append(device.get('id'))\n+        for entity_list in hass.data[DOMAIN]['entities'].values():\n+            for entity in entity_list:\n+                if entity.object_id not in newlist_ids:\n+                    hass.add_job(entity.async_remove())", "hunk": "@@ -10,7 +10,7 @@\n     <h1>Error 503 Backend is unhealthy</h1>\n     <p>Backend is unhealthy</p>\n     <h3>Guru Mediation:</h3>\n-    <p>Details: cache-sea4436-SEA 1645537038 1020793412</p>\n+    <p>Details: cache-sea4427-SEA 1645537038 885011025</p>\n     <hr>\n     <p>Varnish cache server</p>\n   </body>\n", "comment": "This needs to be changed so that we're not accessing the entity directly from this component. Access should be done from the platform of the entity. Use the dispatch helper to send a signal from here to the correct platform and entity_id.", "ids": [314186, "6dfb75fe29615614db216ace8c4f938c32a43418", "36750e56069ec8281d8b085b2b443a98198036c2"], "repo": "home-assistant-core", "ghid": 15399, "old": "     <h1>Error 503 Backend is unhealthy</h1>\n     <p>Backend is unhealthy</p>\n     <h3>Guru Mediation:</h3>\n-    <p>Details: cache-sea4436-SEA 1645537038 1020793412</p>\n     <hr>\n     <p>Varnish cache server</p>\n   </body>", "new": "     <h1>Error 503 Backend is unhealthy</h1>\n     <p>Backend is unhealthy</p>\n     <h3>Guru Mediation:</h3>\n+    <p>Details: cache-sea4427-SEA 1645537038 885011025</p>\n     <hr>\n     <p>Varnish cache server</p>\n   </body>", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -0,0 +1,134 @@\n+\"\"\"\n+Support for Tuya Smart devices.\n+\n+For more details about this component, please refer to the documentation at\n+https://home-assistant.io/components/tuya/\n+\"\"\"\n+from datetime import timedelta\n+import logging\n+import time\n+import voluptuous as vol\n+\n+import homeassistant.helpers.config_validation as cv\n+from homeassistant.const import (CONF_USERNAME, CONF_PASSWORD)\n+from homeassistant.helpers import discovery\n+from homeassistant.helpers.entity import Entity\n+from homeassistant.helpers.event import track_time_interval\n+\n+REQUIREMENTS = ['tuyapy==0.1.0']\n+\n+_LOGGER = logging.getLogger(__name__)\n+\n+CONF_COUNTRYCODE = 'country_code'\n+\n+DOMAIN = 'tuya'\n+DATA_TUYA = 'data_tuya'\n+\n+SERVICE_FORCE_UPDATE = 'force_update'\n+SERVICE_PULL_DEVICES = 'pull_devices'\n+\n+CONFIG_SCHEMA = vol.Schema({\n+    DOMAIN: vol.Schema({\n+        vol.Required(CONF_PASSWORD): cv.string,\n+        vol.Required(CONF_USERNAME): cv.string,\n+        vol.Required(CONF_COUNTRYCODE): cv.string\n+    })\n+}, extra=vol.ALLOW_EXTRA)\n+\n+TUYA_COMPONENT = ['light']\n+\n+\n+def setup(hass, config):\n+    \"\"\"Set up Tuya Component.\"\"\"\n+    from tuyapy import TuyaApi\n+\n+    tuya = TuyaApi()\n+    username = config[DOMAIN][CONF_USERNAME]\n+    password = config[DOMAIN][CONF_PASSWORD]\n+    country_code = config[DOMAIN][CONF_COUNTRYCODE]\n+\n+    hass.data[DATA_TUYA] = tuya\n+\n+    tuya.init(username, password, country_code)\n+\n+    hass.data[DOMAIN] = {\n+        'dev_ids': [],\n+        'entities': {}\n+    }\n+\n+    dev_types = tuya.get_devTypes()\n+    for dev_type in dev_types:\n+        discovery.load_platform(hass, dev_type, DOMAIN, {}, config)\n+\n+    def poll_devices_update(event_time):\n+        \"\"\"Check if accesstoken is expired and pull device list from server.\"\"\"\n+        devices = tuya.poll_devices_update()\n+        dev_types = tuya.get_devTypes()\n+\n+        if devices is None:\n+            return None\n+        for dev_type in dev_types:\n+            discovery.load_platform(hass, dev_type, DOMAIN, {}, config)\n+        newlist_ids = []\n+        for device in devices:\n+            newlist_ids.append(device.get('id'))\n+        for entity_list in hass.data[DOMAIN]['entities'].values():\n+            for entity in entity_list:\n+                if entity.object_id not in newlist_ids:\n+                    hass.add_job(entity.async_remove())", "msg": "This needs to be changed so that we're not accessing the entity directly from this component. Access should be done from the platform of the entity. Use the dispatch helper to send a signal from here to the correct platform and entity_id."}
{"old_hunk": "@@ -0,0 +1,134 @@\n+\"\"\"\n+Support for Tuya Smart devices.\n+\n+For more details about this component, please refer to the documentation at\n+https://home-assistant.io/components/tuya/\n+\"\"\"\n+from datetime import timedelta\n+import logging\n+import time\n+import voluptuous as vol\n+\n+import homeassistant.helpers.config_validation as cv\n+from homeassistant.const import (CONF_USERNAME, CONF_PASSWORD)\n+from homeassistant.helpers import discovery\n+from homeassistant.helpers.entity import Entity\n+from homeassistant.helpers.event import track_time_interval\n+\n+REQUIREMENTS = ['tuyapy==0.1.0']\n+\n+_LOGGER = logging.getLogger(__name__)\n+\n+CONF_COUNTRYCODE = 'country_code'\n+\n+DOMAIN = 'tuya'\n+DATA_TUYA = 'data_tuya'\n+\n+SERVICE_FORCE_UPDATE = 'force_update'\n+SERVICE_PULL_DEVICES = 'pull_devices'\n+\n+CONFIG_SCHEMA = vol.Schema({\n+    DOMAIN: vol.Schema({\n+        vol.Required(CONF_PASSWORD): cv.string,\n+        vol.Required(CONF_USERNAME): cv.string,\n+        vol.Required(CONF_COUNTRYCODE): cv.string\n+    })\n+}, extra=vol.ALLOW_EXTRA)\n+\n+TUYA_COMPONENT = ['light']\n+\n+\n+def setup(hass, config):\n+    \"\"\"Set up Tuya Component.\"\"\"\n+    from tuyapy import TuyaApi\n+\n+    tuya = TuyaApi()\n+    username = config[DOMAIN][CONF_USERNAME]\n+    password = config[DOMAIN][CONF_PASSWORD]\n+    country_code = config[DOMAIN][CONF_COUNTRYCODE]\n+\n+    hass.data[DATA_TUYA] = tuya\n+\n+    tuya.init(username, password, country_code)\n+\n+    hass.data[DOMAIN] = {\n+        'dev_ids': [],\n+        'entities': {}", "hunk": "@@ -10,7 +10,7 @@\n     <h1>Error 503 Backend is unhealthy</h1>\n     <p>Backend is unhealthy</p>\n     <h3>Guru Mediation:</h3>\n-    <p>Details: cache-sea4436-SEA 1645537037 1020787942</p>\n+    <p>Details: cache-sea4427-SEA 1645537037 885005355</p>\n     <hr>\n     <p>Varnish cache server</p>\n   </body>\n", "comment": "Entities should not be accessed outside the platform that created them. It's ok to store them in hass.data but not under a key that is known and accessible to other modules. It's ok to store the entity_ids of created entities under a shared key in hass.data. Eg create a dict the maps the device ids to the entity_ids.", "ids": [314184, "6dfb75fe29615614db216ace8c4f938c32a43418", "36750e56069ec8281d8b085b2b443a98198036c2"], "repo": "home-assistant-core", "ghid": 15399, "old": "     <h1>Error 503 Backend is unhealthy</h1>\n     <p>Backend is unhealthy</p>\n     <h3>Guru Mediation:</h3>\n-    <p>Details: cache-sea4436-SEA 1645537037 1020787942</p>\n     <hr>\n     <p>Varnish cache server</p>\n   </body>", "new": "     <h1>Error 503 Backend is unhealthy</h1>\n     <p>Backend is unhealthy</p>\n     <h3>Guru Mediation:</h3>\n+    <p>Details: cache-sea4427-SEA 1645537037 885005355</p>\n     <hr>\n     <p>Varnish cache server</p>\n   </body>", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -0,0 +1,134 @@\n+\"\"\"\n+Support for Tuya Smart devices.\n+\n+For more details about this component, please refer to the documentation at\n+https://home-assistant.io/components/tuya/\n+\"\"\"\n+from datetime import timedelta\n+import logging\n+import time\n+import voluptuous as vol\n+\n+import homeassistant.helpers.config_validation as cv\n+from homeassistant.const import (CONF_USERNAME, CONF_PASSWORD)\n+from homeassistant.helpers import discovery\n+from homeassistant.helpers.entity import Entity\n+from homeassistant.helpers.event import track_time_interval\n+\n+REQUIREMENTS = ['tuyapy==0.1.0']\n+\n+_LOGGER = logging.getLogger(__name__)\n+\n+CONF_COUNTRYCODE = 'country_code'\n+\n+DOMAIN = 'tuya'\n+DATA_TUYA = 'data_tuya'\n+\n+SERVICE_FORCE_UPDATE = 'force_update'\n+SERVICE_PULL_DEVICES = 'pull_devices'\n+\n+CONFIG_SCHEMA = vol.Schema({\n+    DOMAIN: vol.Schema({\n+        vol.Required(CONF_PASSWORD): cv.string,\n+        vol.Required(CONF_USERNAME): cv.string,\n+        vol.Required(CONF_COUNTRYCODE): cv.string\n+    })\n+}, extra=vol.ALLOW_EXTRA)\n+\n+TUYA_COMPONENT = ['light']\n+\n+\n+def setup(hass, config):\n+    \"\"\"Set up Tuya Component.\"\"\"\n+    from tuyapy import TuyaApi\n+\n+    tuya = TuyaApi()\n+    username = config[DOMAIN][CONF_USERNAME]\n+    password = config[DOMAIN][CONF_PASSWORD]\n+    country_code = config[DOMAIN][CONF_COUNTRYCODE]\n+\n+    hass.data[DATA_TUYA] = tuya\n+\n+    tuya.init(username, password, country_code)\n+\n+    hass.data[DOMAIN] = {\n+        'dev_ids': [],\n+        'entities': {}", "msg": "Entities should not be accessed outside the platform that created them. It's ok to store them in hass.data but not under a key that is known and accessible to other modules. It's ok to store the entity_ids of created entities under a shared key in hass.data. Eg create a dict the maps the device ids to the entity_ids."}
{"old_hunk": "@@ -328,6 +328,10 @@ def turn_on(self, **kwargs):\n         elif ATTR_HS_COLOR in kwargs:\n             self._hs = kwargs[ATTR_HS_COLOR]\n \n+        if ATTR_HS_COLOR in kwargs:\n+            # white LED must be off in order for color to work\n+            self._white = 0", "hunk": "@@ -324,11 +324,8 @@ class ZwaveColorLight(ZwaveDimmer):\n                 else:\n                     self._ct = TEMP_COLD_HASS\n                     rgbw = '#00000000ff'\n-\n         elif ATTR_HS_COLOR in kwargs:\n             self._hs = kwargs[ATTR_HS_COLOR]\n-\n-        if ATTR_HS_COLOR in kwargs:\n             # white LED must be off in order for color to work\n             self._white = 0\n \n", "comment": "Assuming that some devices _do_ allow setting of all the individual RGBW values, It might make sense to only do this zeroing if no `ATTR_WHITE_VALUE` is present?", "ids": [314234, "bda223a51e367a28f5c8670daf0b14e7c28cc29e", "ef0e08734ecb3c013a958b660c6a73f70a6749cf"], "repo": "home-assistant-core", "ghid": 15412, "old": "                 else:\n                     self._ct = TEMP_COLD_HASS\n                     rgbw = '#00000000ff'\n-\n         elif ATTR_HS_COLOR in kwargs:\n             self._hs = kwargs[ATTR_HS_COLOR]\n-\n-        if ATTR_HS_COLOR in kwargs:\n             # white LED must be off in order for color to work\n             self._white = 0", "new": "                 else:\n                     self._ct = TEMP_COLD_HASS\n                     rgbw = '#00000000ff'\n         elif ATTR_HS_COLOR in kwargs:\n             self._hs = kwargs[ATTR_HS_COLOR]\n             # white LED must be off in order for color to work\n             self._white = 0", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -328,6 +328,10 @@ def turn_on(self, **kwargs):\n         elif ATTR_HS_COLOR in kwargs:\n             self._hs = kwargs[ATTR_HS_COLOR]\n \n+        if ATTR_HS_COLOR in kwargs:\n+            # white LED must be off in order for color to work\n+            self._white = 0", "msg": "Assuming that some devices _do_ allow setting of all the individual RGBW values, It might make sense to only do this zeroing if no `ATTR_WHITE_VALUE` is present?"}
{"old_hunk": "@@ -27,11 +27,15 @@\n \n SCAN_INTERVAL = timedelta(seconds=60)\n \n+CONF_COMMAND_TIMEOUT ='command_timeout'\n+DEFAULT_TIMEOUT ='15'", "hunk": "@@ -28,7 +28,7 @@ DEFAULT_NAME = 'Command Sensor'\n SCAN_INTERVAL = timedelta(seconds=60)\n \n CONF_COMMAND_TIMEOUT ='command_timeout'\n-DEFAULT_TIMEOUT ='15'\n+DEFAULT_TIMEOUT = 15\n \n PLATFORM_SCHEMA = PLATFORM_SCHEMA.extend({\n     vol.Required(CONF_COMMAND): cv.string,\n", "comment": "This should be an int not a string.", "ids": [314386, "60a81d5eb457b0de916f1f8f2e4e78af8e2d17ef", "fb9e0c5e5663be4cd7222613b7c4c204b5b65f56"], "repo": "home-assistant-core", "ghid": 15442, "old": " SCAN_INTERVAL = timedelta(seconds=60)\n CONF_COMMAND_TIMEOUT ='command_timeout'\n-DEFAULT_TIMEOUT ='15'\n PLATFORM_SCHEMA = PLATFORM_SCHEMA.extend({\n     vol.Required(CONF_COMMAND): cv.string,", "new": " SCAN_INTERVAL = timedelta(seconds=60)\n CONF_COMMAND_TIMEOUT ='command_timeout'\n+DEFAULT_TIMEOUT = 15\n PLATFORM_SCHEMA = PLATFORM_SCHEMA.extend({\n     vol.Required(CONF_COMMAND): cv.string,", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -27,11 +27,15 @@\n \n SCAN_INTERVAL = timedelta(seconds=60)\n \n+CONF_COMMAND_TIMEOUT ='command_timeout'\n+DEFAULT_TIMEOUT ='15'", "msg": "This should be an int not a string."}
{"old_hunk": "@@ -33,7 +33,8 @@\n SERVICE_PULL_DEVICES = 'pull_devices'\n \n TUYA_TYPE_TO_HA = {\n-    'switch': 'switch'\n+    'switch': 'switch',", "hunk": "@@ -33,8 +33,8 @@ SERVICE_FORCE_UPDATE = 'force_update'\n SERVICE_PULL_DEVICES = 'pull_devices'\n \n TUYA_TYPE_TO_HA = {\n-    'switch': 'switch',\n     'light': 'light',\n+    'switch': 'switch',\n }\n \n CONFIG_SCHEMA = vol.Schema({\n", "comment": "Please sort :abcd:.", "ids": [314393, "c668da6fa44f1e187cb6d0047d80750dc849f5f0", "73f776e5aed2beeb631343fde96488b3ebf87ca6"], "repo": "home-assistant-core", "ghid": 15444, "old": " SERVICE_PULL_DEVICES = 'pull_devices'\n TUYA_TYPE_TO_HA = {\n-    'switch': 'switch',\n     'light': 'light',\n }\n CONFIG_SCHEMA = vol.Schema({", "new": " SERVICE_PULL_DEVICES = 'pull_devices'\n TUYA_TYPE_TO_HA = {\n     'light': 'light',\n+    'switch': 'switch',\n }\n CONFIG_SCHEMA = vol.Schema({", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -33,7 +33,8 @@\n SERVICE_PULL_DEVICES = 'pull_devices'\n \n TUYA_TYPE_TO_HA = {\n-    'switch': 'switch'\n+    'switch': 'switch',", "msg": "Please sort :abcd:."}
{"old_hunk": "@@ -129,13 +130,18 @@ def object_id(self):\n         \"\"\"Return Tuya device id.\"\"\"\n         return self.tuya.object_id()\n \n+    @property\n+    def unique_id(self):\n+        \"\"\"Return a unique ID.\"\"\"\n+        return 'tuya.' + self.tuya.object_id()", "hunk": "@@ -133,7 +133,7 @@ class TuyaDevice(Entity):\n     @property\n     def unique_id(self):\n         \"\"\"Return a unique ID.\"\"\"\n-        return 'tuya.' + self.tuya.object_id()\n+        return 'tuya.{}'.format(self.tuya.object_id())\n \n     @property\n     def name(self):\n", "comment": "Please use string formatting. ```py return 'tuya.{}'.format(self.tuya.object_id()) ```", "ids": [314392, "c668da6fa44f1e187cb6d0047d80750dc849f5f0", "73f776e5aed2beeb631343fde96488b3ebf87ca6"], "repo": "home-assistant-core", "ghid": 15444, "old": "     @property\n     def unique_id(self):\n         \"\"\"Return a unique ID.\"\"\"\n-        return 'tuya.' + self.tuya.object_id()\n     @property\n     def name(self):", "new": "     @property\n     def unique_id(self):\n         \"\"\"Return a unique ID.\"\"\"\n+        return 'tuya.{}'.format(self.tuya.object_id())\n     @property\n     def name(self):", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -129,13 +130,18 @@ def object_id(self):\n         \"\"\"Return Tuya device id.\"\"\"\n         return self.tuya.object_id()\n \n+    @property\n+    def unique_id(self):\n+        \"\"\"Return a unique ID.\"\"\"\n+        return 'tuya.' + self.tuya.object_id()", "msg": "Please use string formatting. ```py return 'tuya.{}'.format(self.tuya.object_id()) ```"}
{"old_hunk": "@@ -100,7 +104,12 @@ def state(self):\n     @property\n     def state_attributes(self):", "hunk": "@@ -102,7 +102,7 @@ class MVGLiveSensor(Entity):\n         return self._state\n \n     @property\n-    def state_attributes(self):\n+    def device_state_attributes(self):\n         \"\"\"Return the state attributes.\"\"\"\n         dep = self.data.departures\n         if not dep:\n", "comment": "This should be `device_state_attributes`.", "ids": [314407, "03280f39c4fbdea7cb3ec5746761325bc2c7c94d", "523067249d5f29e60416b1708b04d12eb3b4628f"], "repo": "home-assistant-core", "ghid": 15454, "old": "         return self._state\n     @property\n-    def state_attributes(self):\n         \"\"\"Return the state attributes.\"\"\"\n         dep = self.data.departures\n         if not dep:", "new": "         return self._state\n     @property\n+    def device_state_attributes(self):\n         \"\"\"Return the state attributes.\"\"\"\n         dep = self.data.departures\n         if not dep:", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -100,7 +104,12 @@ def state(self):\n     @property\n     def state_attributes(self):", "msg": "This should be `device_state_attributes`."}
{"old_hunk": "@@ -62,10 +62,16 @@ def available(self):\n         \"\"\"Device available.\"\"\"\n         return not self._device.unreach\n \n+    @property\n+    def icon(self):\n+        \"\"\"Return the icon.\"\"\"\n+        if hasattr(self._device, 'lowBat') and self._device.lowBat:\n+            return 'mdi:battery-outline'\n+", "hunk": "@@ -67,6 +67,7 @@ class HomematicipGenericDevice(Entity):\n         \"\"\"Return the icon.\"\"\"\n         if hasattr(self._device, 'lowBat') and self._device.lowBat:\n             return 'mdi:battery-outline'\n+        return None\n \n     @property\n     def device_state_attributes(self):\n", "comment": "Return `None`, if this case is not true.", "ids": [314516, "0b8b5fb02aa99fbd67414b1e053662549a30f059", "75b703b31bf06717cf60a6c347cb0f134dd0b3ce"], "repo": "home-assistant-core", "ghid": 15475, "old": "         \"\"\"Return the icon.\"\"\"\n         if hasattr(self._device, 'lowBat') and self._device.lowBat:\n             return 'mdi:battery-outline'\n     @property\n     def device_state_attributes(self):", "new": "         \"\"\"Return the icon.\"\"\"\n         if hasattr(self._device, 'lowBat') and self._device.lowBat:\n             return 'mdi:battery-outline'\n+        return None\n     @property\n     def device_state_attributes(self):", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -62,10 +62,16 @@ def available(self):\n         \"\"\"Device available.\"\"\"\n         return not self._device.unreach\n \n+    @property\n+    def icon(self):\n+        \"\"\"Return the icon.\"\"\"\n+        if hasattr(self._device, 'lowBat') and self._device.lowBat:\n+            return 'mdi:battery-outline'\n+", "msg": "Return `None`, if this case is not true."}
{"old_hunk": "@@ -0,0 +1,137 @@\n+\"\"\"Test the onboarding views.\"\"\"\n+import asyncio\n+from unittest.mock import patch\n+\n+import pytest\n+\n+from homeassistant.setup import async_setup_component\n+from homeassistant.components import onboarding\n+from homeassistant.components.onboarding import views\n+\n+from tests.common import register_auth_provider\n+\n+from . import mock_storage\n+\n+\n+@pytest.fixture(autouse=True)\n+def auth_active(hass):\n+    \"\"\"Ensure auth is always active.\"\"\"\n+    hass.loop.run_until_complete(register_auth_provider(hass, {\n+        'type': 'homeassistant'\n+    }))\n+\n+\n+async def test_onboarding_progress(hass, hass_storage, aiohttp_client):\n+    \"\"\"Test fetching progress.\"\"\"\n+    mock_storage(hass_storage, {\n+        'done': ['hello']\n+    })\n+\n+    assert await async_setup_component(hass, 'onboarding', {})\n+    client = await aiohttp_client(hass.http.app)\n+\n+    with patch.object(views, 'STEPS', ['hello', 'world']):\n+        resp = await client.get('/api/onboarding')\n+\n+    assert resp.status == 200\n+    data = await resp.json()\n+    assert len(data) == 2\n+    assert data[0] == {\n+        'step': 'hello',\n+        'done': True\n+    }\n+    assert data[1] == {\n+        'step': 'world',\n+        'done': False\n+    }\n+\n+\n+async def test_onboarding_user_already_done(hass, hass_storage,\n+                                            aiohttp_client):\n+    \"\"\"Test creating a new user when user step already done.\"\"\"\n+    mock_storage(hass_storage, {\n+        'done': [views.STEP_USER]\n+    })\n+\n+    with patch.object(onboarding, 'STEPS', ['hello', 'world']):\n+        assert await async_setup_component(hass, 'onboarding', {})\n+\n+    client = await aiohttp_client(hass.http.app)\n+\n+    resp = await client.post('/api/onboarding/users', json={\n+        'name': 'Test Name',\n+        'username': 'test-user',\n+        'password': 'test-pass',\n+    })\n+\n+    assert resp.status == 403\n+\n+\n+async def test_onboarding_user(hass, hass_storage, aiohttp_client):\n+    \"\"\"Test creating a new user.\"\"\"\n+    mock_storage(hass_storage, {\n+        'done': ['hello']\n+    })\n+\n+    assert await async_setup_component(hass, 'onboarding', {})\n+\n+    client = await aiohttp_client(hass.http.app)\n+\n+    resp = await client.post('/api/onboarding/users', json={\n+        'name': 'Test Name',\n+        'username': 'test-user',\n+        'password': 'test-pass',\n+    })\n+\n+    assert resp.status == 200\n+    users = await hass.auth.async_get_users()\n+    assert len(users) == 1\n+    user = users[0]\n+    assert user.name == 'Test Name'\n+    assert len(user.credentials) == 1\n+    assert user.credentials[0].data['username'] == 'test-user'\n+\n+\n+async def test_onboarding_user_invalid_name(hass, hass_storage,\n+                                            aiohttp_client):\n+    \"\"\"Test not providing name.\"\"\"\n+    mock_storage(hass_storage, {\n+        'done': ['hello']\n+    })\n+\n+    assert await async_setup_component(hass, 'onboarding', {})\n+\n+    client = await aiohttp_client(hass.http.app)\n+\n+    resp = await client.post('/api/onboarding/users', json={\n+        'username': 'test-user',\n+        'password': 'test-pass',\n+    })\n+\n+    assert resp.status == 400\n+\n+\n+async def test_onboarding_user_race(hass, hass_storage, aiohttp_client):\n+    \"\"\"Test race condition on creating new user.\"\"\"\n+    mock_storage(hass_storage, {\n+        'done': ['hello']\n+    })\n+\n+    assert await async_setup_component(hass, 'onboarding', {})\n+\n+    client = await aiohttp_client(hass.http.app)\n+\n+    resp1 = client.post('/api/onboarding/users', json={\n+        'name': 'Test 1',\n+        'username': '1-user',\n+        'password': '1-pass',\n+    })\n+    resp2 = client.post('/api/onboarding/users', json={\n+        'name': 'Test 1',\n+        'username': '1-user',", "hunk": "@@ -127,9 +127,9 @@ async def test_onboarding_user_race(hass, hass_storage, aiohttp_client):\n         'password': '1-pass',\n     })\n     resp2 = client.post('/api/onboarding/users', json={\n-        'name': 'Test 1',\n-        'username': '1-user',\n-        'password': '1-pass',\n+        'name': 'Test 2',\n+        'username': '2-user',\n+        'password': '2-pass',\n     })\n \n     res1, res2 = await asyncio.gather(resp1, resp2)\n", "comment": "Maybe add a scenario to use different username to make sure the error not caused by duplicate username", "ids": [314559, "29a98a111addc25ab472431b1885528d0795d521", "9551f3aaadb17369616e51a229eb35ac799b9d48"], "repo": "home-assistant-core", "ghid": 15492, "old": "         'password': '1-pass',\n     })\n     resp2 = client.post('/api/onboarding/users', json={\n-        'name': 'Test 1',\n-        'username': '1-user',\n-        'password': '1-pass',\n     })\n     res1, res2 = await asyncio.gather(resp1, resp2)", "new": "         'password': '1-pass',\n     })\n     resp2 = client.post('/api/onboarding/users', json={\n+        'name': 'Test 2',\n+        'username': '2-user',\n+        'password': '2-pass',\n     })\n     res1, res2 = await asyncio.gather(resp1, resp2)", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -0,0 +1,137 @@\n+\"\"\"Test the onboarding views.\"\"\"\n+import asyncio\n+from unittest.mock import patch\n+\n+import pytest\n+\n+from homeassistant.setup import async_setup_component\n+from homeassistant.components import onboarding\n+from homeassistant.components.onboarding import views\n+\n+from tests.common import register_auth_provider\n+\n+from . import mock_storage\n+\n+\n+@pytest.fixture(autouse=True)\n+def auth_active(hass):\n+    \"\"\"Ensure auth is always active.\"\"\"\n+    hass.loop.run_until_complete(register_auth_provider(hass, {\n+        'type': 'homeassistant'\n+    }))\n+\n+\n+async def test_onboarding_progress(hass, hass_storage, aiohttp_client):\n+    \"\"\"Test fetching progress.\"\"\"\n+    mock_storage(hass_storage, {\n+        'done': ['hello']\n+    })\n+\n+    assert await async_setup_component(hass, 'onboarding', {})\n+    client = await aiohttp_client(hass.http.app)\n+\n+    with patch.object(views, 'STEPS', ['hello', 'world']):\n+        resp = await client.get('/api/onboarding')\n+\n+    assert resp.status == 200\n+    data = await resp.json()\n+    assert len(data) == 2\n+    assert data[0] == {\n+        'step': 'hello',\n+        'done': True\n+    }\n+    assert data[1] == {\n+        'step': 'world',\n+        'done': False\n+    }\n+\n+\n+async def test_onboarding_user_already_done(hass, hass_storage,\n+                                            aiohttp_client):\n+    \"\"\"Test creating a new user when user step already done.\"\"\"\n+    mock_storage(hass_storage, {\n+        'done': [views.STEP_USER]\n+    })\n+\n+    with patch.object(onboarding, 'STEPS', ['hello', 'world']):\n+        assert await async_setup_component(hass, 'onboarding', {})\n+\n+    client = await aiohttp_client(hass.http.app)\n+\n+    resp = await client.post('/api/onboarding/users', json={\n+        'name': 'Test Name',\n+        'username': 'test-user',\n+        'password': 'test-pass',\n+    })\n+\n+    assert resp.status == 403\n+\n+\n+async def test_onboarding_user(hass, hass_storage, aiohttp_client):\n+    \"\"\"Test creating a new user.\"\"\"\n+    mock_storage(hass_storage, {\n+        'done': ['hello']\n+    })\n+\n+    assert await async_setup_component(hass, 'onboarding', {})\n+\n+    client = await aiohttp_client(hass.http.app)\n+\n+    resp = await client.post('/api/onboarding/users', json={\n+        'name': 'Test Name',\n+        'username': 'test-user',\n+        'password': 'test-pass',\n+    })\n+\n+    assert resp.status == 200\n+    users = await hass.auth.async_get_users()\n+    assert len(users) == 1\n+    user = users[0]\n+    assert user.name == 'Test Name'\n+    assert len(user.credentials) == 1\n+    assert user.credentials[0].data['username'] == 'test-user'\n+\n+\n+async def test_onboarding_user_invalid_name(hass, hass_storage,\n+                                            aiohttp_client):\n+    \"\"\"Test not providing name.\"\"\"\n+    mock_storage(hass_storage, {\n+        'done': ['hello']\n+    })\n+\n+    assert await async_setup_component(hass, 'onboarding', {})\n+\n+    client = await aiohttp_client(hass.http.app)\n+\n+    resp = await client.post('/api/onboarding/users', json={\n+        'username': 'test-user',\n+        'password': 'test-pass',\n+    })\n+\n+    assert resp.status == 400\n+\n+\n+async def test_onboarding_user_race(hass, hass_storage, aiohttp_client):\n+    \"\"\"Test race condition on creating new user.\"\"\"\n+    mock_storage(hass_storage, {\n+        'done': ['hello']\n+    })\n+\n+    assert await async_setup_component(hass, 'onboarding', {})\n+\n+    client = await aiohttp_client(hass.http.app)\n+\n+    resp1 = client.post('/api/onboarding/users', json={\n+        'name': 'Test 1',\n+        'username': '1-user',\n+        'password': '1-pass',\n+    })\n+    resp2 = client.post('/api/onboarding/users', json={\n+        'name': 'Test 1',\n+        'username': '1-user',", "msg": "Maybe add a scenario to use different username to make sure the error not caused by duplicate username"}
{"old_hunk": "@@ -346,7 +346,11 @@ class SetIntentHandler(intent.IntentHandler):\n         update_tasks = []\n         for light in target_lights:\n             if service.service == SERVICE_TURN_ON:\n-                await light.async_turn_on(**params)\n+                pars = params.copy()", "hunk": "@@ -346,8 +346,9 @@ async def async_setup(hass, config):\n         update_tasks = []\n         for light in target_lights:\n             if service.service == SERVICE_TURN_ON:\n-                pars = params.copy()\n+                pars = params\n                 if not pars:\n+                    pars = params.copy()\n                     pars[ATTR_PROFILE] = Profiles.get_default(light.entity_id)\n                     preprocess_turn_on_alternatives(pars)\n                 await light.async_turn_on(**pars)\n", "comment": "We should put the copy inside `if not pars:` to avoid unnecessary copying.", "ids": [314572, "a860e85c7aaa0beefa0e00a4b724a64f1c66d0f1", "7b9d524197766c70567a330abe52323c678a2e5a"], "repo": "home-assistant-core", "ghid": 15493, "old": "         update_tasks = []\n         for light in target_lights:\n             if service.service == SERVICE_TURN_ON:\n-                pars = params.copy()\n                 if not pars:\n                     pars[ATTR_PROFILE] = Profiles.get_default(light.entity_id)\n                     preprocess_turn_on_alternatives(pars)\n                 await light.async_turn_on(**pars)", "new": "         update_tasks = []\n         for light in target_lights:\n             if service.service == SERVICE_TURN_ON:\n+                pars = params\n                 if not pars:\n+                    pars = params.copy()\n                     pars[ATTR_PROFILE] = Profiles.get_default(light.entity_id)\n                     preprocess_turn_on_alternatives(pars)\n                 await light.async_turn_on(**pars)", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -346,7 +346,11 @@ class SetIntentHandler(intent.IntentHandler):\n         update_tasks = []\n         for light in target_lights:\n             if service.service == SERVICE_TURN_ON:\n-                await light.async_turn_on(**params)\n+                pars = params.copy()", "msg": "We should put the copy inside `if not pars:` to avoid unnecessary copying."}
{"old_hunk": "@@ -59,6 +59,17 @@ def __init__(self, tuya):\n         \"\"\"Init climate device.\"\"\"\n         super().__init__(tuya)\n         self.entity_id = ENTITY_ID_FORMAT.format(tuya.object_id())\n+        self.operations = []\n+\n+    async def async_added_to_hass(self):\n+        \"\"\"Create operation list when add to hass.\"\"\"\n+        super().async_added_to_hass()", "hunk": "@@ -63,7 +63,7 @@ class TuyaClimateDevice(TuyaDevice, ClimateDevice):\n \n     async def async_added_to_hass(self):\n         \"\"\"Create operation list when add to hass.\"\"\"\n-        super().async_added_to_hass()\n+        await super().async_added_to_hass()\n         modes = self.tuya.operation_list()\n         if modes is None:\n             return\n", "comment": "This is a coroutine so it needs to be awaited.", "ids": [314617, "5b5d28d73774d1992330b95f90df399991c2e573", "19ff552c33786d824490cbf74363a09f191949c9"], "repo": "home-assistant-core", "ghid": 15500, "old": "     async def async_added_to_hass(self):\n         \"\"\"Create operation list when add to hass.\"\"\"\n-        super().async_added_to_hass()\n         modes = self.tuya.operation_list()\n         if modes is None:\n             return", "new": "     async def async_added_to_hass(self):\n         \"\"\"Create operation list when add to hass.\"\"\"\n+        await super().async_added_to_hass()\n         modes = self.tuya.operation_list()\n         if modes is None:\n             return", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -59,6 +59,17 @@ def __init__(self, tuya):\n         \"\"\"Init climate device.\"\"\"\n         super().__init__(tuya)\n         self.entity_id = ENTITY_ID_FORMAT.format(tuya.object_id())\n+        self.operations = []\n+\n+    async def async_added_to_hass(self):\n+        \"\"\"Create operation list when add to hass.\"\"\"\n+        super().async_added_to_hass()", "msg": "This is a coroutine so it needs to be awaited."}
{"old_hunk": "@@ -54,4 +56,6 @@ def device_class(self):\n     @property\n     def is_on(self):\n         \"\"\"Return the boolean response if the node is on.\"\"\"\n-        return bool(self._insteon_device_state.value)\n+        on_val = bool(self._insteon_device_state.value)\n+        return (on_val if self._insteon_device_state.name != 'lightSensor' else", "hunk": "@@ -57,5 +57,8 @@ class InsteonPLMBinarySensor(InsteonPLMEntity, BinarySensorDevice):\n     def is_on(self):\n         \"\"\"Return the boolean response if the node is on.\"\"\"\n         on_val = bool(self._insteon_device_state.value)\n-        return (on_val if self._insteon_device_state.name != 'lightSensor' else\n-                not on_val)\n+\n+        if self._insteon_device_state.name == 'lightSensor':\n+            return not on_val\n+        else:\n+            return on_val\n", "comment": "Please convert to a normal if else if an inline if-statement is spanning two lines.", "ids": [314699, "3f6b11adefdd01308cb97831757ace8782bbcf5f", "3364ffa34001679d7275b4d45b422fae53c60c30"], "repo": "home-assistant-core", "ghid": 15523, "old": "     def is_on(self):\n         \"\"\"Return the boolean response if the node is on.\"\"\"\n         on_val = bool(self._insteon_device_state.value)\n-        return (on_val if self._insteon_device_state.name != 'lightSensor' else\n-                not on_val)", "new": "     def is_on(self):\n         \"\"\"Return the boolean response if the node is on.\"\"\"\n         on_val = bool(self._insteon_device_state.value)\n+\n+        if self._insteon_device_state.name == 'lightSensor':\n+            return not on_val\n+        else:\n+            return on_val", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -54,4 +56,6 @@ def device_class(self):\n     @property\n     def is_on(self):\n         \"\"\"Return the boolean response if the node is on.\"\"\"\n-        return bool(self._insteon_device_state.value)\n+        on_val = bool(self._insteon_device_state.value)\n+        return (on_val if self._insteon_device_state.name != 'lightSensor' else", "msg": "Please convert to a normal if else if an inline if-statement is spanning two lines."}
{"old_hunk": "@@ -199,6 +199,14 @@ class HassAuthProvider(AuthProvider):\n             # Can happen if somehow we didn't clean up a credential\n             pass\n \n+    async def async_change_password(self, username, new_password):", "hunk": "@@ -199,14 +199,6 @@ class HassAuthProvider(AuthProvider):\n             # Can happen if somehow we didn't clean up a credential\n             pass\n \n-    async def async_change_password(self, username, new_password):\n-        \"\"\"Helper to change a user's password.\"\"\"\n-        if self.data is None:\n-            await self.async_initialize()\n-\n-        await self.hass.async_add_executor_job(\n-            self.data.change_password, username, new_password)\n-\n \n class LoginFlow(data_entry_flow.FlowHandler):\n     \"\"\"Handler for the login flow.\"\"\"\n", "comment": "No need for this, just call straight the data object", "ids": [314723, "8735b681815d9cf184ebc98c53f5272b3583e7bf", "a519049fbcb84f972d4b5df44c068a144e93aa0d"], "repo": "home-assistant-core", "ghid": 15527, "old": "             # Can happen if somehow we didn't clean up a credential\n             pass\n-    async def async_change_password(self, username, new_password):\n-        \"\"\"Helper to change a user's password.\"\"\"\n-        if self.data is None:\n-            await self.async_initialize()\n-\n-        await self.hass.async_add_executor_job(\n-            self.data.change_password, username, new_password)\n-\n class LoginFlow(data_entry_flow.FlowHandler):\n     \"\"\"Handler for the login flow.\"\"\"", "new": "             # Can happen if somehow we didn't clean up a credential\n             pass\n class LoginFlow(data_entry_flow.FlowHandler):\n     \"\"\"Handler for the login flow.\"\"\"", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -199,6 +199,14 @@ class HassAuthProvider(AuthProvider):\n             # Can happen if somehow we didn't clean up a credential\n             pass\n \n+    async def async_change_password(self, username, new_password):", "msg": "No need for this, just call straight the data object"}
{"old_hunk": "@@ -0,0 +1,83 @@\n+\"\"\"\n+Support for Switchmate.\n+\n+For more details about this platform, please refer to the documentation at\n+https://home-assistant.io/components/switch.switchmate/\n+\"\"\"\n+import logging\n+from datetime import timedelta\n+\n+import voluptuous as vol\n+\n+import homeassistant.helpers.config_validation as cv\n+from homeassistant.components.switch import SwitchDevice, PLATFORM_SCHEMA\n+from homeassistant.const import CONF_NAME, CONF_MAC\n+from homeassistant.exceptions import PlatformNotReady\n+\n+REQUIREMENTS = ['bluepy==1.1.4']\n+\n+_LOGGER = logging.getLogger(__name__)\n+\n+DEFAULT_NAME = 'Switchmate'\n+HANDLE = 0x2e\n+ON_KEY = b'\\x00'\n+OFF_KEY = b'\\x01'\n+\n+SCAN_INTERVAL = timedelta(minutes=30)\n+\n+PLATFORM_SCHEMA = PLATFORM_SCHEMA.extend({\n+    vol.Required(CONF_MAC): cv.string,\n+    vol.Optional(CONF_NAME, default=DEFAULT_NAME): cv.string,\n+})\n+\n+\n+def setup_platform(hass, config, add_devices, discovery_info=None) -> None:\n+    \"\"\"Perform the setup for Switchmate devices.\"\"\"\n+    name = config.get(CONF_NAME)\n+    mac_addr = config.get(CONF_MAC)\n+    add_devices([Switchmate(mac_addr, name)], True)\n+\n+\n+class Switchmate(SwitchDevice):\n+    \"\"\"Representation of a Switchmate.\"\"\"\n+\n+    def __init__(self, mac, name) -> None:\n+        \"\"\"Initialize the Switchmate.\"\"\"\n+        # pylint: disable=import-error\n+        import bluepy\n+        self._state = False\n+        self._name = name\n+        self._mac = mac\n+        try:\n+            self._device = bluepy.btle.Peripheral(self._mac,\n+                                                  bluepy.btle.ADDR_TYPE_RANDOM)\n+        except bluepy.btle.BTLEException:\n+            _LOGGER.error(\"Failed to setup switchmate\", exc_info=True)\n+            raise PlatformNotReady()\n+\n+    @property\n+    def unique_id(self) -> str:\n+        \"\"\"Return a unique, HASS-friendly identifier for this entity.\"\"\"\n+        return '{0}'.format(self._mac.replace(':', ''))", "hunk": "@@ -58,7 +58,7 @@ class Switchmate(SwitchDevice):\n     @property\n     def unique_id(self) -> str:\n         \"\"\"Return a unique, HASS-friendly identifier for this entity.\"\"\"\n-        return '{0}'.format(self._mac.replace(':', ''))\n+        return self._mac.replace(':', '')\n \n     @property\n     def name(self) -> str:\n", "comment": "String formatting can be removed. Just do the replace.", "ids": [314740, "454d86e810d6040fae4229d429c3ac93f01d6f97", "8d7faf7e6033db45a183d179fabe354d76d38bd4"], "repo": "home-assistant-core", "ghid": 15535, "old": "     @property\n     def unique_id(self) -> str:\n         \"\"\"Return a unique, HASS-friendly identifier for this entity.\"\"\"\n-        return '{0}'.format(self._mac.replace(':', ''))\n     @property\n     def name(self) -> str:", "new": "     @property\n     def unique_id(self) -> str:\n         \"\"\"Return a unique, HASS-friendly identifier for this entity.\"\"\"\n+        return self._mac.replace(':', '')\n     @property\n     def name(self) -> str:", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -0,0 +1,83 @@\n+\"\"\"\n+Support for Switchmate.\n+\n+For more details about this platform, please refer to the documentation at\n+https://home-assistant.io/components/switch.switchmate/\n+\"\"\"\n+import logging\n+from datetime import timedelta\n+\n+import voluptuous as vol\n+\n+import homeassistant.helpers.config_validation as cv\n+from homeassistant.components.switch import SwitchDevice, PLATFORM_SCHEMA\n+from homeassistant.const import CONF_NAME, CONF_MAC\n+from homeassistant.exceptions import PlatformNotReady\n+\n+REQUIREMENTS = ['bluepy==1.1.4']\n+\n+_LOGGER = logging.getLogger(__name__)\n+\n+DEFAULT_NAME = 'Switchmate'\n+HANDLE = 0x2e\n+ON_KEY = b'\\x00'\n+OFF_KEY = b'\\x01'\n+\n+SCAN_INTERVAL = timedelta(minutes=30)\n+\n+PLATFORM_SCHEMA = PLATFORM_SCHEMA.extend({\n+    vol.Required(CONF_MAC): cv.string,\n+    vol.Optional(CONF_NAME, default=DEFAULT_NAME): cv.string,\n+})\n+\n+\n+def setup_platform(hass, config, add_devices, discovery_info=None) -> None:\n+    \"\"\"Perform the setup for Switchmate devices.\"\"\"\n+    name = config.get(CONF_NAME)\n+    mac_addr = config.get(CONF_MAC)\n+    add_devices([Switchmate(mac_addr, name)], True)\n+\n+\n+class Switchmate(SwitchDevice):\n+    \"\"\"Representation of a Switchmate.\"\"\"\n+\n+    def __init__(self, mac, name) -> None:\n+        \"\"\"Initialize the Switchmate.\"\"\"\n+        # pylint: disable=import-error\n+        import bluepy\n+        self._state = False\n+        self._name = name\n+        self._mac = mac\n+        try:\n+            self._device = bluepy.btle.Peripheral(self._mac,\n+                                                  bluepy.btle.ADDR_TYPE_RANDOM)\n+        except bluepy.btle.BTLEException:\n+            _LOGGER.error(\"Failed to setup switchmate\", exc_info=True)\n+            raise PlatformNotReady()\n+\n+    @property\n+    def unique_id(self) -> str:\n+        \"\"\"Return a unique, HASS-friendly identifier for this entity.\"\"\"\n+        return '{0}'.format(self._mac.replace(':', ''))", "msg": "String formatting can be removed. Just do the replace."}
{"old_hunk": "@@ -9,23 +9,23 @@\n \n import voluptuous as vol\n \n-import homeassistant.components.alarm_control_panel as alarm\n-from homeassistant.components.alarm_control_panel import PLATFORM_SCHEMA\n+from homeassistant.components.alarm_control_panel import (\n+    PLATFORM_SCHEMA, AlarmControlPanel)\n from homeassistant.const import (\n     CONF_CODE, CONF_NAME, CONF_PASSWORD, CONF_USERNAME,\n-    EVENT_HOMEASSISTANT_STOP, STATE_ALARM_ARMED_AWAY, STATE_ALARM_ARMED_HOME,\n+    STATE_ALARM_ARMED_AWAY, STATE_ALARM_ARMED_HOME,\n     STATE_ALARM_DISARMED, STATE_UNKNOWN)\n import homeassistant.helpers.config_validation as cv\n \n-REQUIREMENTS = ['simplisafe-python==1.0.5']\n+REQUIREMENTS = ['simplisafe-python==2.0.1']\n \n _LOGGER = logging.getLogger(__name__)\n \n DEFAULT_NAME = 'SimpliSafe'\n DOMAIN = 'simplisafe'", "hunk": "@@ -17,12 +17,11 @@ from homeassistant.const import (\n     STATE_ALARM_DISARMED, STATE_UNKNOWN)\n import homeassistant.helpers.config_validation as cv\n \n-REQUIREMENTS = ['simplisafe-python==2.0.1']\n+REQUIREMENTS = ['simplisafe-python==2.0.2']\n \n _LOGGER = logging.getLogger(__name__)\n \n DEFAULT_NAME = 'SimpliSafe'\n-DOMAIN = 'simplisafe'\n \n ATTR_ALARM_ACTIVE = \"alarm_active\"\n ATTR_TEMPERATURE = \"temperature\"\n", "comment": "This should be removed. It's not used anymore and it was incorrect before. The domain of this platform is `'alarm_control_panel'`.", "ids": [314751, "3e34490b3708a9faf993a7332d5e603f4d5500d4", "fd92c2e6f4ea0260f69c1605922795b31b55ed3d"], "repo": "home-assistant-core", "ghid": 15542, "old": "     STATE_ALARM_DISARMED, STATE_UNKNOWN)\n import homeassistant.helpers.config_validation as cv\n-REQUIREMENTS = ['simplisafe-python==2.0.1']\n _LOGGER = logging.getLogger(__name__)\n DEFAULT_NAME = 'SimpliSafe'\n-DOMAIN = 'simplisafe'\n ATTR_ALARM_ACTIVE = \"alarm_active\"\n ATTR_TEMPERATURE = \"temperature\"", "new": "     STATE_ALARM_DISARMED, STATE_UNKNOWN)\n import homeassistant.helpers.config_validation as cv\n+REQUIREMENTS = ['simplisafe-python==2.0.2']\n _LOGGER = logging.getLogger(__name__)\n DEFAULT_NAME = 'SimpliSafe'\n ATTR_ALARM_ACTIVE = \"alarm_active\"\n ATTR_TEMPERATURE = \"temperature\"", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -9,23 +9,23 @@\n \n import voluptuous as vol\n \n-import homeassistant.components.alarm_control_panel as alarm\n-from homeassistant.components.alarm_control_panel import PLATFORM_SCHEMA\n+from homeassistant.components.alarm_control_panel import (\n+    PLATFORM_SCHEMA, AlarmControlPanel)\n from homeassistant.const import (\n     CONF_CODE, CONF_NAME, CONF_PASSWORD, CONF_USERNAME,\n-    EVENT_HOMEASSISTANT_STOP, STATE_ALARM_ARMED_AWAY, STATE_ALARM_ARMED_HOME,\n+    STATE_ALARM_ARMED_AWAY, STATE_ALARM_ARMED_HOME,\n     STATE_ALARM_DISARMED, STATE_UNKNOWN)\n import homeassistant.helpers.config_validation as cv\n \n-REQUIREMENTS = ['simplisafe-python==1.0.5']\n+REQUIREMENTS = ['simplisafe-python==2.0.1']\n \n _LOGGER = logging.getLogger(__name__)\n \n DEFAULT_NAME = 'SimpliSafe'\n DOMAIN = 'simplisafe'", "msg": "This should be removed. It's not used anymore and it was incorrect before. The domain of this platform is `'alarm_control_panel'`."}
{"old_hunk": "@@ -481,9 +481,10 @@ def async_update(self, *_):\n \n         _LOGGER.debug(\"Buienradar parsed data: %s\", result)\n         if result.get(SUCCESS) is not True:\n-            _LOGGER.warning(\"Unable to parse data from Buienradar.\"\n-                            \"(Msg: %s)\",\n-                            result.get(MESSAGE),)\n+            if (int(datetime.now().strftime('%H'))>0):", "hunk": "@@ -481,7 +481,7 @@ class BrData(object):\n \n         _LOGGER.debug(\"Buienradar parsed data: %s\", result)\n         if result.get(SUCCESS) is not True:\n-            if (int(datetime.now().strftime('%H'))>0):\n+            if (int(datetime.now().strftime('%H')) > 0):\n                 _LOGGER.warning(\"Unable to parse data from Buienradar.\"\n                                 \"(Msg: %s)\",\n                                 result.get(MESSAGE),)\n", "comment": "undefined name 'datetime'<br>missing whitespace around operator", "ids": [314778, "734be70cd8a9ab0c72fdb1d41e0266074d273e73", "f936ad24fc0efc516899b81b741684073a2b89fe"], "repo": "home-assistant-core", "ghid": 15555, "old": "         _LOGGER.debug(\"Buienradar parsed data: %s\", result)\n         if result.get(SUCCESS) is not True:\n-            if (int(datetime.now().strftime('%H'))>0):\n                 _LOGGER.warning(\"Unable to parse data from Buienradar.\"\n                                 \"(Msg: %s)\",\n                                 result.get(MESSAGE),)", "new": "         _LOGGER.debug(\"Buienradar parsed data: %s\", result)\n         if result.get(SUCCESS) is not True:\n+            if (int(datetime.now().strftime('%H')) > 0):\n                 _LOGGER.warning(\"Unable to parse data from Buienradar.\"\n                                 \"(Msg: %s)\",\n                                 result.get(MESSAGE),)", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -481,9 +481,10 @@ def async_update(self, *_):\n \n         _LOGGER.debug(\"Buienradar parsed data: %s\", result)\n         if result.get(SUCCESS) is not True:\n-            _LOGGER.warning(\"Unable to parse data from Buienradar.\"\n-                            \"(Msg: %s)\",\n-                            result.get(MESSAGE),)\n+            if (int(datetime.now().strftime('%H'))>0):", "msg": "undefined name 'datetime'<br>missing whitespace around operator"}
{"old_hunk": "@@ -412,4 +412,6 @@ def websocket_current_user(hass, connection, msg):\n         'id': user.id,\n         'name': user.name,\n         'is_owner': user.is_owner,\n+        'credentials': [{'auth_provider_type': c.auth_provider_type,\n+                         'id': c.id} for c in user.credentials]", "hunk": "@@ -412,6 +412,6 @@ def websocket_current_user(hass, connection, msg):\n         'id': user.id,\n         'name': user.name,\n         'is_owner': user.is_owner,\n-        'credentials': [{'auth_provider_type': c.auth_provider_type,\n-                         'id': c.id} for c in user.credentials]\n+        'allow_change_password': any(c.auth_provider_type == 'homeassistant'\n+                                     for c in user.credentials)\n     }))\n", "comment": "Any usage for c.id?", "ids": [314786, "54ba5486bfd03f9c553e59055a69dfe0796320bf", "5d39a3e4aa3f4595ad9e3b969f0e3036ac24a4d7"], "repo": "home-assistant-core", "ghid": 15558, "old": "         'id': user.id,\n         'name': user.name,\n         'is_owner': user.is_owner,\n-        'credentials': [{'auth_provider_type': c.auth_provider_type,\n-                         'id': c.id} for c in user.credentials]\n     }))", "new": "         'id': user.id,\n         'name': user.name,\n         'is_owner': user.is_owner,\n+        'allow_change_password': any(c.auth_provider_type == 'homeassistant'\n+                                     for c in user.credentials)\n     }))", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -412,4 +412,6 @@ def websocket_current_user(hass, connection, msg):\n         'id': user.id,\n         'name': user.name,\n         'is_owner': user.is_owner,\n+        'credentials': [{'auth_provider_type': c.auth_provider_type,\n+                         'id': c.id} for c in user.credentials]", "msg": "Any usage for c.id?"}
{"old_hunk": "@@ -195,5 +197,9 @@ def get_features(self):\n             self._supported_features += SUPPORT_BRIGHTNESS\n         if self.smartbulb.is_variable_color_temp:\n             self._supported_features += SUPPORT_COLOR_TEMP\n+            self._min_mireds = kelvin_to_mired(\n+                self.smartbulb.valid_temperature_range[0])", "hunk": "@@ -198,8 +198,8 @@ class TPLinkSmartBulb(Light):\n         if self.smartbulb.is_variable_color_temp:\n             self._supported_features += SUPPORT_COLOR_TEMP\n             self._min_mireds = kelvin_to_mired(\n-                self.smartbulb.valid_temperature_range[0])\n+                self.smartbulb.valid_temperature_range[1])\n             self._max_mireds = kelvin_to_mired(\n-                    self.smartbulb.valid_temperature_range[1])\n+                    self.smartbulb.valid_temperature_range[0])\n         if self.smartbulb.is_color:\n             self._supported_features += SUPPORT_COLOR\n", "comment": "You've swapped 0 and 1. It used to be min mireds was `range[1]`", "ids": [314805, "4717a1118b8d8349fc29cfb4f89982b911691f86", "e6fa4b5839521a84fc01cb79cc0442c9e046bd26"], "repo": "home-assistant-core", "ghid": 15571, "old": "         if self.smartbulb.is_variable_color_temp:\n             self._supported_features += SUPPORT_COLOR_TEMP\n             self._min_mireds = kelvin_to_mired(\n-                self.smartbulb.valid_temperature_range[0])\n             self._max_mireds = kelvin_to_mired(\n-                    self.smartbulb.valid_temperature_range[1])\n         if self.smartbulb.is_color:\n             self._supported_features += SUPPORT_COLOR", "new": "         if self.smartbulb.is_variable_color_temp:\n             self._supported_features += SUPPORT_COLOR_TEMP\n             self._min_mireds = kelvin_to_mired(\n+                self.smartbulb.valid_temperature_range[1])\n             self._max_mireds = kelvin_to_mired(\n+                    self.smartbulb.valid_temperature_range[0])\n         if self.smartbulb.is_color:\n             self._supported_features += SUPPORT_COLOR", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -195,5 +197,9 @@ def get_features(self):\n             self._supported_features += SUPPORT_BRIGHTNESS\n         if self.smartbulb.is_variable_color_temp:\n             self._supported_features += SUPPORT_COLOR_TEMP\n+            self._min_mireds = kelvin_to_mired(\n+                self.smartbulb.valid_temperature_range[0])", "msg": "You've swapped 0 and 1. It used to be min mireds was `range[1]`"}
{"old_hunk": "@@ -135,7 +135,7 @@ def handle_async_mjpeg_stream(self, request):\n         websession = async_get_clientsession(self.hass)\n         stream_coro = websession.get(self._mjpeg_url, auth=self._auth)\n \n-        yield from async_aiohttp_proxy_web(self.hass, request, stream_coro)\n+        return yield from async_aiohttp_proxy_web(self.hass, request, stream_coro)", "hunk": "@@ -135,7 +135,7 @@ class MjpegCamera(Camera):\n         websession = async_get_clientsession(self.hass)\n         stream_coro = websession.get(self._mjpeg_url, auth=self._auth)\n \n-        return yield from async_aiohttp_proxy_web(self.hass, request, stream_coro)\n+        return await async_aiohttp_proxy_web(self.hass, request, stream_coro)\n \n     @property\n     def name(self):\n", "comment": "SyntaxError: invalid syntax<br>line too long (82 > 79 characters)", "ids": [314868, "caf5168990e73b1f7d7462ede6484dd60ab7fbc0", "ac40e711e310243bac169dd5635741fd7de43fe8"], "repo": "home-assistant-core", "ghid": 15577, "old": "         websession = async_get_clientsession(self.hass)\n         stream_coro = websession.get(self._mjpeg_url, auth=self._auth)\n-        return yield from async_aiohttp_proxy_web(self.hass, request, stream_coro)\n     @property\n     def name(self):", "new": "         websession = async_get_clientsession(self.hass)\n         stream_coro = websession.get(self._mjpeg_url, auth=self._auth)\n+        return await async_aiohttp_proxy_web(self.hass, request, stream_coro)\n     @property\n     def name(self):", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -135,7 +135,7 @@ def handle_async_mjpeg_stream(self, request):\n         websession = async_get_clientsession(self.hass)\n         stream_coro = websession.get(self._mjpeg_url, auth=self._auth)\n \n-        yield from async_aiohttp_proxy_web(self.hass, request, stream_coro)\n+        return yield from async_aiohttp_proxy_web(self.hass, request, stream_coro)", "msg": "SyntaxError: invalid syntax<br>line too long (82 > 79 characters)"}
{"old_hunk": "@@ -0,0 +1,45 @@\n+\"\"\"\n+Support for the Tuya scene.\n+\n+For more details about this platform, please refer to the documentation at\n+https://home-assistant.io/components/scene.tuya/\n+\"\"\"\n+from homeassistant.components.scene import Scene, DOMAIN\n+from homeassistant.components.tuya import DATA_TUYA, TuyaDevice\n+\n+DEPENDENCIES = ['tuya']\n+\n+ENTITY_ID_FORMAT = DOMAIN + '.{}'\n+\n+\n+def setup_platform(hass, config, add_devices, discovery_info=None):\n+    \"\"\"Set up Tuya scenes.\"\"\"\n+    if discovery_info is None:\n+        return\n+    tuya = hass.data[DATA_TUYA]\n+    dev_ids = discovery_info.get('dev_ids')\n+    devices = []\n+    for dev_id in dev_ids:\n+        device = tuya.get_device_by_id(dev_id)\n+        if device is None:\n+            continue\n+        devices.append(TuyaScene(device))\n+    add_devices(devices)\n+\n+\n+class TuyaScene(TuyaDevice, Scene):\n+    \"\"\"Tuya Scene.\"\"\"\n+\n+    def __init__(self, tuya):\n+        \"\"\"Init Tuya scene.\"\"\"\n+        super().__init__(tuya)\n+        self.entity_id = ENTITY_ID_FORMAT.format(tuya.object_id())\n+\n+    def activate(self):\n+        \"\"\"Activate the scene.\"\"\"\n+        self.tuya.activate()\n+\n+    @property\n+    def should_poll(self):\n+        \"\"\"Scene has no data.\"\"\"\n+        return False", "hunk": "@@ -38,8 +38,3 @@ class TuyaScene(TuyaDevice, Scene):\n     def activate(self):\n         \"\"\"Activate the scene.\"\"\"\n         self.tuya.activate()\n-\n-    @property\n-    def should_poll(self):\n-        \"\"\"Scene has no data.\"\"\"\n-        return False\n", "comment": "This is the default for scene entities.", "ids": [314885, "e03cd97b990c8b9c1273a82476adcc97dc1da9a5", "42fbaae8d0e28b3b2fee0d6df087ea2e150aa269"], "repo": "home-assistant-core", "ghid": 15587, "old": "     def activate(self):\n         \"\"\"Activate the scene.\"\"\"\n         self.tuya.activate()\n-\n-    @property\n-    def should_poll(self):\n-        \"\"\"Scene has no data.\"\"\"\n-        return False", "new": "     def activate(self):\n         \"\"\"Activate the scene.\"\"\"\n         self.tuya.activate()", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -0,0 +1,45 @@\n+\"\"\"\n+Support for the Tuya scene.\n+\n+For more details about this platform, please refer to the documentation at\n+https://home-assistant.io/components/scene.tuya/\n+\"\"\"\n+from homeassistant.components.scene import Scene, DOMAIN\n+from homeassistant.components.tuya import DATA_TUYA, TuyaDevice\n+\n+DEPENDENCIES = ['tuya']\n+\n+ENTITY_ID_FORMAT = DOMAIN + '.{}'\n+\n+\n+def setup_platform(hass, config, add_devices, discovery_info=None):\n+    \"\"\"Set up Tuya scenes.\"\"\"\n+    if discovery_info is None:\n+        return\n+    tuya = hass.data[DATA_TUYA]\n+    dev_ids = discovery_info.get('dev_ids')\n+    devices = []\n+    for dev_id in dev_ids:\n+        device = tuya.get_device_by_id(dev_id)\n+        if device is None:\n+            continue\n+        devices.append(TuyaScene(device))\n+    add_devices(devices)\n+\n+\n+class TuyaScene(TuyaDevice, Scene):\n+    \"\"\"Tuya Scene.\"\"\"\n+\n+    def __init__(self, tuya):\n+        \"\"\"Init Tuya scene.\"\"\"\n+        super().__init__(tuya)\n+        self.entity_id = ENTITY_ID_FORMAT.format(tuya.object_id())\n+\n+    def activate(self):\n+        \"\"\"Activate the scene.\"\"\"\n+        self.tuya.activate()\n+\n+    @property\n+    def should_poll(self):\n+        \"\"\"Scene has no data.\"\"\"\n+        return False", "msg": "This is the default for scene entities."}
{"old_hunk": "@@ -86,3 +86,21 @@ def is_on(self):\n         if self._device.sabotage:\n             return True\n         return self._device.motionDetected\n+\n+\n+class HomematicipSmokeDetector(HomematicipGenericDevice, BinarySensorDevice):\n+    \"\"\"MomematicIP smoke detector.\"\"\"\n+\n+    def __init__(self, home, device):", "hunk": "@@ -91,10 +91,6 @@ class HomematicipMotionDetector(HomematicipGenericDevice, BinarySensorDevice):\n class HomematicipSmokeDetector(HomematicipGenericDevice, BinarySensorDevice):\n     \"\"\"MomematicIP smoke detector.\"\"\"\n \n-    def __init__(self, home, device):\n-        \"\"\"Initialize the smoke detector.\"\"\"\n-        super().__init__(home, device)\n-\n     @property\n     def device_class(self):\n         \"\"\"Return the class of this sensor.\"\"\"\n", "comment": "This isn't needed if just passing through the arguments to the parent.", "ids": [314904, "1ffa4ea7570206fc93aea480bf0987df42fe4c3d", "50fbeddfa1d04e1be1769787cc6227a788cf68b3"], "repo": "home-assistant-core", "ghid": 15621, "old": " class HomematicipSmokeDetector(HomematicipGenericDevice, BinarySensorDevice):\n     \"\"\"MomematicIP smoke detector.\"\"\"\n-    def __init__(self, home, device):\n-        \"\"\"Initialize the smoke detector.\"\"\"\n-        super().__init__(home, device)\n-\n     @property\n     def device_class(self):\n         \"\"\"Return the class of this sensor.\"\"\"", "new": " class HomematicipSmokeDetector(HomematicipGenericDevice, BinarySensorDevice):\n     \"\"\"MomematicIP smoke detector.\"\"\"\n     @property\n     def device_class(self):\n         \"\"\"Return the class of this sensor.\"\"\"", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -86,3 +86,21 @@ def is_on(self):\n         if self._device.sabotage:\n             return True\n         return self._device.motionDetected\n+\n+\n+class HomematicipSmokeDetector(HomematicipGenericDevice, BinarySensorDevice):\n+    \"\"\"MomematicIP smoke detector.\"\"\"\n+\n+    def __init__(self, home, device):", "msg": "This isn't needed if just passing through the arguments to the parent."}
{"old_hunk": "@@ -855,14 +855,11 @@ def __init__(self, hass: HomeAssistant) -> None:\n         self._hass = hass\n         self._async_unsub_call_event = None  # type: Optional[CALLBACK_TYPE]\n \n-        def _gen_unique_id() -> Iterator[str]:\n-            cur_id = 1\n-            while True:\n-                yield '{}-{}'.format(id(self), cur_id)\n-                cur_id += 1\n-\n-        gen = _gen_unique_id()\n-        self._generate_unique_id = lambda: next(gen)\n+    def _generate_unique_id(self) -> Iterator[str]:", "hunk": "@@ -855,12 +855,6 @@ class ServiceRegistry:\n         self._hass = hass\n         self._async_unsub_call_event = None  # type: Optional[CALLBACK_TYPE]\n \n-    def _generate_unique_id(self) -> Iterator[str]:\n-        cur_id = 1\n-        while True:\n-            yield '{}-{}'.format(id(self), cur_id)\n-            cur_id += 1\n-\n     @property\n     def services(self) -> Dict[str, Dict[str, Service]]:\n         \"\"\"Return dictionary with per domain a list of available services.\"\"\"\n", "comment": "This is not the same.", "ids": [314995, "268fc2be2090598245336b0275c16217a370c11b", "4e5b55929c1e40302ac5546aaad7505cbd882bae"], "repo": "home-assistant-core", "ghid": 15661, "old": "         self._hass = hass\n         self._async_unsub_call_event = None  # type: Optional[CALLBACK_TYPE]\n-    def _generate_unique_id(self) -> Iterator[str]:\n-        cur_id = 1\n-        while True:\n-            yield '{}-{}'.format(id(self), cur_id)\n-            cur_id += 1\n-\n     @property\n     def services(self) -> Dict[str, Dict[str, Service]]:\n         \"\"\"Return dictionary with per domain a list of available services.\"\"\"", "new": "         self._hass = hass\n         self._async_unsub_call_event = None  # type: Optional[CALLBACK_TYPE]\n     @property\n     def services(self) -> Dict[str, Dict[str, Service]]:\n         \"\"\"Return dictionary with per domain a list of available services.\"\"\"", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -855,14 +855,11 @@ def __init__(self, hass: HomeAssistant) -> None:\n         self._hass = hass\n         self._async_unsub_call_event = None  # type: Optional[CALLBACK_TYPE]\n \n-        def _gen_unique_id() -> Iterator[str]:\n-            cur_id = 1\n-            while True:\n-                yield '{}-{}'.format(id(self), cur_id)\n-                cur_id += 1\n-\n-        gen = _gen_unique_id()\n-        self._generate_unique_id = lambda: next(gen)\n+    def _generate_unique_id(self) -> Iterator[str]:", "msg": "This is not the same."}
{"old_hunk": "@@ -998,44 +1034,43 @@ def call(self, domain: str, service: str,\n \n         This method is a coroutine.\n         \"\"\"\n-        call_id = self._generate_unique_id()\n-\n+        context = context or Context()\n         event_data = {\n             ATTR_DOMAIN: domain.lower(),\n             ATTR_SERVICE: service.lower(),\n             ATTR_SERVICE_DATA: service_data,\n-            ATTR_SERVICE_CALL_ID: call_id,\n         }\n \n-        if blocking:\n-            fut = asyncio.Future(loop=self._hass.loop)  # type: asyncio.Future\n+        if not blocking:\n+            self._hass.bus.async_fire(\n+                EVENT_CALL_SERVICE, event_data, EventOrigin.local, context)\n+            return None\n \n-            @callback\n-            def service_executed(event: Event) -> None:\n-                \"\"\"Handle an executed service.\"\"\"\n-                if event.data[ATTR_SERVICE_CALL_ID] == call_id:\n-                    fut.set_result(True)\n+        fut = asyncio.Future(loop=self._hass.loop)  # type: asyncio.Future", "hunk": "@@ -1046,7 +1046,7 @@ class ServiceRegistry:\n                 EVENT_CALL_SERVICE, event_data, EventOrigin.local, context)\n             return None\n \n-        fut = asyncio.Future(loop=self._hass.loop)  # type: asyncio.Future\n+        fut = asyncio.Future()  # type: asyncio.Future\n \n         @callback\n         def service_executed(event: Event) -> None:\n", "comment": "Passing loop isn't needed on Python 3.5.3.", "ids": [315060, "8bf26ca9dc6c2325eca9b8bf03378193998934b2", "f976ab2bdfe77c854d3741230aa9a8fe9c8b76db"], "repo": "home-assistant-core", "ghid": 15674, "old": "                 EVENT_CALL_SERVICE, event_data, EventOrigin.local, context)\n             return None\n-        fut = asyncio.Future(loop=self._hass.loop)  # type: asyncio.Future\n         @callback\n         def service_executed(event: Event) -> None:", "new": "                 EVENT_CALL_SERVICE, event_data, EventOrigin.local, context)\n             return None\n+        fut = asyncio.Future()  # type: asyncio.Future\n         @callback\n         def service_executed(event: Event) -> None:", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -998,44 +1034,43 @@ def call(self, domain: str, service: str,\n \n         This method is a coroutine.\n         \"\"\"\n-        call_id = self._generate_unique_id()\n-\n+        context = context or Context()\n         event_data = {\n             ATTR_DOMAIN: domain.lower(),\n             ATTR_SERVICE: service.lower(),\n             ATTR_SERVICE_DATA: service_data,\n-            ATTR_SERVICE_CALL_ID: call_id,\n         }\n \n-        if blocking:\n-            fut = asyncio.Future(loop=self._hass.loop)  # type: asyncio.Future\n+        if not blocking:\n+            self._hass.bus.async_fire(\n+                EVENT_CALL_SERVICE, event_data, EventOrigin.local, context)\n+            return None\n \n-            @callback\n-            def service_executed(event: Event) -> None:\n-                \"\"\"Handle an executed service.\"\"\"\n-                if event.data[ATTR_SERVICE_CALL_ID] == call_id:\n-                    fut.set_result(True)\n+        fut = asyncio.Future(loop=self._hass.loop)  # type: asyncio.Future", "msg": "Passing loop isn't needed on Python 3.5.3."}
{"old_hunk": "@@ -27,6 +28,7 @@\n \n SCAN_INTERVAL = timedelta(seconds=60)\n \n+CONF_JSON_ATTRS = 'json_attributes'", "hunk": "@@ -28,7 +28,7 @@ DEFAULT_NAME = 'Command Sensor'\n \n SCAN_INTERVAL = timedelta(seconds=60)\n \n-CONF_JSON_ATTRS = 'json_attributes'\n+CONF_JSON_ATTRIBUTES = 'json_attributes'\n CONF_COMMAND_TIMEOUT = 'command_timeout'\n DEFAULT_TIMEOUT = 15\n \n", "comment": "I would use full name CONF_JSON_ATTRIBUTES", "ids": [315090, "991552a67f70e20f813084f7f689688ebe6e2b7e", "26da77811a5b69fa9f0976a2d8088851e8a3685f"], "repo": "home-assistant-core", "ghid": 15679, "old": " SCAN_INTERVAL = timedelta(seconds=60)\n-CONF_JSON_ATTRS = 'json_attributes'\n CONF_COMMAND_TIMEOUT = 'command_timeout'\n DEFAULT_TIMEOUT = 15", "new": " SCAN_INTERVAL = timedelta(seconds=60)\n+CONF_JSON_ATTRIBUTES = 'json_attributes'\n CONF_COMMAND_TIMEOUT = 'command_timeout'\n DEFAULT_TIMEOUT = 15", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -27,6 +28,7 @@\n \n SCAN_INTERVAL = timedelta(seconds=60)\n \n+CONF_JSON_ATTRS = 'json_attributes'", "msg": "I would use full name CONF_JSON_ATTRIBUTES"}
{"old_hunk": "@@ -76,7 +78,34 @@ def brand(self):\n         \"\"\"Return the brand of the camera.\"\"\"\n         return NEST_BRAND\n \n-    # This doesn't seem to be getting called regularly, for some reason\n+    @property\n+    def supported_features(self):\n+        \"\"\"Nest Cam support turn on and off.\"\"\"\n+        return SUPPORT_ON_OFF\n+\n+    @property\n+    def is_on(self):\n+        \"\"\"Return true if on.\"\"\"\n+        return self._online and self._is_streaming\n+\n+    def turn_off(self):\n+        \"\"\"Turn off camera.\"\"\"\n+        _LOGGER.debug('Turn off camera %s', self._name)\n+        # Calling Nest API in is_streaming setter\n+        self.device.is_streaming = False\n+        self.schedule_update_ha_state(True)\n+\n+    def turn_on(self):\n+        \"\"\"Turn on camera.\"\"\"\n+        if not self._online:\n+            raise HomeAssistantError('Camera {} is offline.'", "hunk": "@@ -98,8 +98,8 @@ class NestCamera(Camera):\n     def turn_on(self):\n         \"\"\"Turn on camera.\"\"\"\n         if not self._online:\n-            raise HomeAssistantError('Camera {} is offline.'\n-                                     .format(self._name))\n+            _LOGGER.error('Camera %s is offline.', self._name)\n+            return\n \n         _LOGGER.debug('Turn on camera %s', self._name)\n         # Calling Nest API in is_streaming setter\n", "comment": "We usually don't raise within entity actuator methods. Log error and return is normal.", "ids": [315097, "6921917c4bc669351e093d961081d1ec8001a289", "9f6ebd29bc5842e2e14e3df33de8de28c0158602"], "repo": "home-assistant-core", "ghid": 15681, "old": "     def turn_on(self):\n         \"\"\"Turn on camera.\"\"\"\n         if not self._online:\n-            raise HomeAssistantError('Camera {} is offline.'\n-                                     .format(self._name))\n         _LOGGER.debug('Turn on camera %s', self._name)\n         # Calling Nest API in is_streaming setter", "new": "     def turn_on(self):\n         \"\"\"Turn on camera.\"\"\"\n         if not self._online:\n+            _LOGGER.error('Camera %s is offline.', self._name)\n+            return\n         _LOGGER.debug('Turn on camera %s', self._name)\n         # Calling Nest API in is_streaming setter", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -76,7 +78,34 @@ def brand(self):\n         \"\"\"Return the brand of the camera.\"\"\"\n         return NEST_BRAND\n \n-    # This doesn't seem to be getting called regularly, for some reason\n+    @property\n+    def supported_features(self):\n+        \"\"\"Nest Cam support turn on and off.\"\"\"\n+        return SUPPORT_ON_OFF\n+\n+    @property\n+    def is_on(self):\n+        \"\"\"Return true if on.\"\"\"\n+        return self._online and self._is_streaming\n+\n+    def turn_off(self):\n+        \"\"\"Turn off camera.\"\"\"\n+        _LOGGER.debug('Turn off camera %s', self._name)\n+        # Calling Nest API in is_streaming setter\n+        self.device.is_streaming = False\n+        self.schedule_update_ha_state(True)\n+\n+    def turn_on(self):\n+        \"\"\"Turn on camera.\"\"\"\n+        if not self._online:\n+            raise HomeAssistantError('Camera {} is offline.'", "msg": "We usually don't raise within entity actuator methods. Log error and return is normal."}
{"old_hunk": "@@ -76,7 +77,34 @@ def brand(self):\n         \"\"\"Return the brand of the camera.\"\"\"\n         return NEST_BRAND\n \n-    # This doesn't seem to be getting called regularly, for some reason\n+    @property\n+    def supported_features(self):\n+        \"\"\"Nest Cam support turn on and off.\"\"\"\n+        return SUPPORT_ON_OFF\n+\n+    @property\n+    def is_on(self):\n+        \"\"\"Return true if on.\"\"\"\n+        return self._online and self._is_streaming\n+\n+    def turn_off(self):\n+        \"\"\"Turn off camera.\"\"\"\n+        _LOGGER.debug('Turn off camera %s', self._name)\n+        # Calling Nest API in is_streaming setter\n+        self.device.is_streaming = False\n+        self.schedule_update_ha_state(True)", "hunk": "@@ -90,9 +90,10 @@ class NestCamera(Camera):\n     def turn_off(self):\n         \"\"\"Turn off camera.\"\"\"\n         _LOGGER.debug('Turn off camera %s', self._name)\n-        # Calling Nest API in is_streaming setter\n+        # Calling Nest API in is_streaming setter.\n+        # device.is_streaming would not immediately change until the process\n+        # finished in Nest Cam.\n         self.device.is_streaming = False\n-        self.schedule_update_ha_state(True)\n \n     def turn_on(self):\n         \"\"\"Turn on camera.\"\"\"\n", "comment": "Since this is a polling entity, this line isn't needed.", "ids": [315098, "9f6ebd29bc5842e2e14e3df33de8de28c0158602", "5da4780a6c855dfb94612f1ceebbdac52c99d15f"], "repo": "home-assistant-core", "ghid": 15681, "old": "     def turn_off(self):\n         \"\"\"Turn off camera.\"\"\"\n         _LOGGER.debug('Turn off camera %s', self._name)\n-        # Calling Nest API in is_streaming setter\n         self.device.is_streaming = False\n-        self.schedule_update_ha_state(True)\n     def turn_on(self):\n         \"\"\"Turn on camera.\"\"\"", "new": "     def turn_off(self):\n         \"\"\"Turn off camera.\"\"\"\n         _LOGGER.debug('Turn off camera %s', self._name)\n+        # Calling Nest API in is_streaming setter.\n+        # device.is_streaming would not immediately change until the process\n+        # finished in Nest Cam.\n         self.device.is_streaming = False\n     def turn_on(self):\n         \"\"\"Turn on camera.\"\"\"", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -76,7 +77,34 @@ def brand(self):\n         \"\"\"Return the brand of the camera.\"\"\"\n         return NEST_BRAND\n \n-    # This doesn't seem to be getting called regularly, for some reason\n+    @property\n+    def supported_features(self):\n+        \"\"\"Nest Cam support turn on and off.\"\"\"\n+        return SUPPORT_ON_OFF\n+\n+    @property\n+    def is_on(self):\n+        \"\"\"Return true if on.\"\"\"\n+        return self._online and self._is_streaming\n+\n+    def turn_off(self):\n+        \"\"\"Turn off camera.\"\"\"\n+        _LOGGER.debug('Turn off camera %s', self._name)\n+        # Calling Nest API in is_streaming setter\n+        self.device.is_streaming = False\n+        self.schedule_update_ha_state(True)", "msg": "Since this is a polling entity, this line isn't needed."}
{"old_hunk": "@@ -125,6 +131,9 @@ def get_device_name(self, device):\n \n     def get_extra_attributes(self, device):\n         \"\"\"Return the extra attributes of the device.\"\"\"\n-        client = self._clients.get(device, {})\n-        _LOGGER.debug(\"Device mac %s attributes %s\", device, client)\n-        return client\n+        if (self._extra_attributes):", "hunk": "@@ -131,9 +131,9 @@ class UnifiScanner(DeviceScanner):\n \n     def get_extra_attributes(self, device):\n         \"\"\"Return the extra attributes of the device.\"\"\"\n-        if (self._extra_attributes):\n-            client = self._clients.get(device, {})\n-            _LOGGER.debug(\"Device mac %s attributes %s\", device, client)\n-            return client\n-        else:\n-            return dict()\n+        if not self._extra_attributes:\n+            return {}\n+\n+        client = self._clients.get(device, {})\n+        _LOGGER.debug(\"Device mac %s attributes %s\", device, client)\n+        return client\n", "comment": "We prefer guard clause ``` if not self._extra_attributes: return {} bla bla ```", "ids": [315218, "3821a12b7ae6d65f8e08758689fe6003164feb6e", "c8b7795dde722d2cbcfaa0ecbf5c7a169aae706b"], "repo": "home-assistant-core", "ghid": 15711, "old": "     def get_extra_attributes(self, device):\n         \"\"\"Return the extra attributes of the device.\"\"\"\n-        if (self._extra_attributes):\n-            client = self._clients.get(device, {})\n-            _LOGGER.debug(\"Device mac %s attributes %s\", device, client)\n-            return client\n-        else:\n-            return dict()", "new": "     def get_extra_attributes(self, device):\n         \"\"\"Return the extra attributes of the device.\"\"\"\n+        if not self._extra_attributes:\n+            return {}\n+\n+        client = self._clients.get(device, {})\n+        _LOGGER.debug(\"Device mac %s attributes %s\", device, client)\n+        return client", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -125,6 +131,9 @@ def get_device_name(self, device):\n \n     def get_extra_attributes(self, device):\n         \"\"\"Return the extra attributes of the device.\"\"\"\n-        client = self._clients.get(device, {})\n-        _LOGGER.debug(\"Device mac %s attributes %s\", device, client)\n-        return client\n+        if (self._extra_attributes):", "msg": "We prefer guard clause ``` if not self._extra_attributes: return {} bla bla ```"}
{"old_hunk": "@@ -1,9 +1,11 @@\n \"\"\"Test the Dyson fan component.\"\"\"\n+import asyncio\n import unittest\n from unittest import mock\n \n from homeassistant.components.dyson import DYSON_DEVICES\n-from homeassistant.components.fan import dyson\n+from homeassistant.components.fan import (dyson, ATTR_SPEED, ATTR_SPEED_LIST,", "hunk": "@@ -5,7 +5,7 @@ from unittest import mock\n \n from homeassistant.components.dyson import DYSON_DEVICES\n from homeassistant.components.fan import (dyson, ATTR_SPEED, ATTR_SPEED_LIST,\n-                                          ATTR_OSCILLATING, ATTR_DIRECTION)\n+                                          ATTR_OSCILLATING)\n from tests.common import get_test_home_assistant\n from libpurecoollink.const import FanSpeed, FanMode, NightMode, Oscillation\n from libpurecoollink.dyson_pure_state import DysonPureCoolState\n", "comment": "'homeassistant.components.fan.ATTR_DIRECTION' imported but unused", "ids": [315235, "e0bb523264df9ab3bcf1f236d6875ea9249ff66b", "629f609ca85762d17c6beb192bd5aa23bcb32bd1"], "repo": "home-assistant-core", "ghid": 15716, "old": " from homeassistant.components.dyson import DYSON_DEVICES\n from homeassistant.components.fan import (dyson, ATTR_SPEED, ATTR_SPEED_LIST,\n-                                          ATTR_OSCILLATING, ATTR_DIRECTION)\n from tests.common import get_test_home_assistant\n from libpurecoollink.const import FanSpeed, FanMode, NightMode, Oscillation\n from libpurecoollink.dyson_pure_state import DysonPureCoolState", "new": " from homeassistant.components.dyson import DYSON_DEVICES\n from homeassistant.components.fan import (dyson, ATTR_SPEED, ATTR_SPEED_LIST,\n+                                          ATTR_OSCILLATING)\n from tests.common import get_test_home_assistant\n from libpurecoollink.const import FanSpeed, FanMode, NightMode, Oscillation\n from libpurecoollink.dyson_pure_state import DysonPureCoolState", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -1,9 +1,11 @@\n \"\"\"Test the Dyson fan component.\"\"\"\n+import asyncio\n import unittest\n from unittest import mock\n \n from homeassistant.components.dyson import DYSON_DEVICES\n-from homeassistant.components.fan import dyson\n+from homeassistant.components.fan import (dyson, ATTR_SPEED, ATTR_SPEED_LIST,", "msg": "'homeassistant.components.fan.ATTR_DIRECTION' imported but unused"}
{"old_hunk": "@@ -131,7 +131,7 @@ def humidity(self):\n     @property\n     def wind_speed(self):\n         \"\"\"Return the wind speed.\"\"\"\n-        return self.data.get_wind().get('speed')\n+        return self.data.get_wind().get('speed') * 3.6", "hunk": "@@ -131,7 +131,7 @@ class OpenWeatherMapWeather(WeatherEntity):\n     @property\n     def wind_speed(self):\n         \"\"\"Return the wind speed.\"\"\"\n-        return self.data.get_wind().get('speed') * 3.6\n+        return round(self.data.get_wind().get('speed') * 3.6, 2)\n \n     @property\n     def wind_bearing(self):\n", "comment": "Do we need to round this off?", "ids": [315292, "06dd2726925f3d3467a9515b016bad917202273a", "8bcba6a89d5a71ed746e8dc800e6e93104c41af4"], "repo": "home-assistant-core", "ghid": 15740, "old": "     @property\n     def wind_speed(self):\n         \"\"\"Return the wind speed.\"\"\"\n-        return self.data.get_wind().get('speed') * 3.6\n     @property\n     def wind_bearing(self):", "new": "     @property\n     def wind_speed(self):\n         \"\"\"Return the wind speed.\"\"\"\n+        return round(self.data.get_wind().get('speed') * 3.6, 2)\n     @property\n     def wind_bearing(self):", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -131,7 +131,7 @@ def humidity(self):\n     @property\n     def wind_speed(self):\n         \"\"\"Return the wind speed.\"\"\"\n-        return self.data.get_wind().get('speed')\n+        return self.data.get_wind().get('speed') * 3.6", "msg": "Do we need to round this off?"}
{"old_hunk": "@@ -102,19 +108,23 @@ def name(self):\n \n             await ftp.quit()\n \n+            if not self._is_on:", "hunk": "@@ -107,10 +107,7 @@ class YiCamera(Camera):\n                 return None\n \n             await ftp.quit()\n-\n-            if not self._is_on:\n-                self._is_on = True\n-\n+            self._is_on = True\n             return 'ftp://{0}:{1}@{2}:{3}{4}/{5}/{6}'.format(\n                 self.user, self.passwd, self.host, self.port, self.path,\n                 latest_dir, videos[-1])\n", "comment": "Missed this, if is not necessary for me.", "ids": [315419, "d62406ba92299fc6651471d31006122cd601d84c", "067815db390231f7fac7b52902876db8354859c8"], "repo": "home-assistant-core", "ghid": 15754, "old": "                 return None\n             await ftp.quit()\n-\n-            if not self._is_on:\n-                self._is_on = True\n-\n             return 'ftp://{0}:{1}@{2}:{3}{4}/{5}/{6}'.format(\n                 self.user, self.passwd, self.host, self.port, self.path,\n                 latest_dir, videos[-1])", "new": "                 return None\n             await ftp.quit()\n+            self._is_on = True\n             return 'ftp://{0}:{1}@{2}:{3}{4}/{5}/{6}'.format(\n                 self.user, self.passwd, self.host, self.port, self.path,\n                 latest_dir, videos[-1])", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -102,19 +108,23 @@ def name(self):\n \n             await ftp.quit()\n \n+            if not self._is_on:", "msg": "Missed this, if is not necessary for me."}
{"old_hunk": "@@ -102,19 +108,25 @@ def name(self):\n \n             await ftp.quit()\n \n+            if not self._is_on:\n+                self._is_on = True\n+\n             return 'ftp://{0}:{1}@{2}:{3}{4}/{5}/{6}'.format(\n                 self.user, self.passwd, self.host, self.port, self.path,\n                 latest_dir, videos[-1])\n         except (ConnectionRefusedError, StatusCodeError) as err:\n             _LOGGER.error('Error while fetching video: %s', err)\n+\n+            if self._is_on:", "hunk": "@@ -116,9 +116,7 @@ class YiCamera(Camera):\n                 latest_dir, videos[-1])\n         except (ConnectionRefusedError, StatusCodeError) as err:\n             _LOGGER.error('Error while fetching video: %s', err)\n-\n-            if self._is_on:\n-                self._is_on = False\n+            self._is_on = False\n             return None\n \n     async def async_camera_image(self):\n", "comment": "if is unnecessary", "ids": [315416, "6e88631cdbcda0111c0dc2f335c27626f15a3704", "d62406ba92299fc6651471d31006122cd601d84c"], "repo": "home-assistant-core", "ghid": 15754, "old": "                 latest_dir, videos[-1])\n         except (ConnectionRefusedError, StatusCodeError) as err:\n             _LOGGER.error('Error while fetching video: %s', err)\n-\n-            if self._is_on:\n-                self._is_on = False\n             return None\n     async def async_camera_image(self):", "new": "                 latest_dir, videos[-1])\n         except (ConnectionRefusedError, StatusCodeError) as err:\n             _LOGGER.error('Error while fetching video: %s', err)\n+            self._is_on = False\n             return None\n     async def async_camera_image(self):", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -102,19 +108,25 @@ def name(self):\n \n             await ftp.quit()\n \n+            if not self._is_on:\n+                self._is_on = True\n+\n             return 'ftp://{0}:{1}@{2}:{3}{4}/{5}/{6}'.format(\n                 self.user, self.passwd, self.host, self.port, self.path,\n                 latest_dir, videos[-1])\n         except (ConnectionRefusedError, StatusCodeError) as err:\n             _LOGGER.error('Error while fetching video: %s', err)\n+\n+            if self._is_on:", "msg": "if is unnecessary"}
{"old_hunk": "@@ -36,6 +36,9 @@ def component_translation_file(hass, component, language):\n         name = component\n \n     module = get_component(hass, component)\n+    if module is None:", "hunk": "@@ -36,9 +36,6 @@ def component_translation_file(hass, component, language):\n         name = component\n \n     module = get_component(hass, component)\n-    if module is None:\n-        _LOGGER.warning('component %s is not loaded', component)\n-        return\n     component_path = path.dirname(module.__file__)\n \n     # If loading translations for the package root, (__init__.py), the\n", "comment": "This makes no sense. `get_component` is not about things not being setup. It's about loading the component from file, either from your custom components dir or from built-in.", "ids": [315490, "723175656d171e1c12dc665c2c38a860fcadb5c8", "d656077de4ca02ac2b47f4c2e8a0d9e91ab129d3"], "repo": "home-assistant-core", "ghid": 15765, "old": "         name = component\n     module = get_component(hass, component)\n-    if module is None:\n-        _LOGGER.warning('component %s is not loaded', component)\n-        return\n     component_path = path.dirname(module.__file__)\n     # If loading translations for the package root, (__init__.py), the", "new": "         name = component\n     module = get_component(hass, component)\n     component_path = path.dirname(module.__file__)\n     # If loading translations for the package root, (__init__.py), the", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -36,6 +36,9 @@ def component_translation_file(hass, component, language):\n         name = component\n \n     module = get_component(hass, component)\n+    if module is None:", "msg": "This makes no sense. `get_component` is not about things not being setup. It's about loading the component from file, either from your custom components dir or from built-in."}
{"old_hunk": "@@ -23,6 +23,8 @@ def load_json(filename: str, default: Union[List, Dict, None] = None) \\\n \n     Defaults to returning empty dict if file is not found.\n     \"\"\"\n+    if filename is None:", "hunk": "@@ -23,8 +23,6 @@ def load_json(filename: str, default: Union[List, Dict, None] = None) \\\n \n     Defaults to returning empty dict if file is not found.\n     \"\"\"\n-    if filename is None:\n-        return {}\n     try:\n         with open(filename, encoding='utf-8') as fdesc:\n             return json.loads(fdesc.read())  # type: ignore\n", "comment": "This is also not good. We should fail. This code should never be called if filename is None. We should write code that doesn't handle \"bad\" inputs and tries to fix them. Raise errors instead. The moment we fix them, that function will need to keep fixing it forever, even if other things get added to the function that would allow the previously bad input. The previously bad input expects to be \"changed\" or else bugs in other code happen.", "ids": [315491, "723175656d171e1c12dc665c2c38a860fcadb5c8", "d656077de4ca02ac2b47f4c2e8a0d9e91ab129d3"], "repo": "home-assistant-core", "ghid": 15765, "old": "     Defaults to returning empty dict if file is not found.\n     \"\"\"\n-    if filename is None:\n-        return {}\n     try:\n         with open(filename, encoding='utf-8') as fdesc:\n             return json.loads(fdesc.read())  # type: ignore", "new": "     Defaults to returning empty dict if file is not found.\n     \"\"\"\n     try:\n         with open(filename, encoding='utf-8') as fdesc:\n             return json.loads(fdesc.read())  # type: ignore", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -23,6 +23,8 @@ def load_json(filename: str, default: Union[List, Dict, None] = None) \\\n \n     Defaults to returning empty dict if file is not found.\n     \"\"\"\n+    if filename is None:", "msg": "This is also not good. We should fail. This code should never be called if filename is None. We should write code that doesn't handle \"bad\" inputs and tries to fix them. Raise errors instead. The moment we fix them, that function will need to keep fixing it forever, even if other things get added to the function that would allow the previously bad input. The previously bad input expects to be \"changed\" or else bugs in other code happen."}
{"old_hunk": "@@ -0,0 +1,113 @@\n+\"\"\"\n+This platform provides sensors for OpenUV data.\n+\n+For more details about this platform, please refer to the documentation at\n+https://home-assistant.io/components/sensor.openuv/\n+\"\"\"\n+import logging\n+\n+from homeassistant.const import CONF_MONITORED_CONDITIONS\n+from homeassistant.core import callback\n+from homeassistant.helpers.dispatcher import async_dispatcher_connect\n+from homeassistant.components.openuv import (\n+    DATA_UV, DOMAIN, SENSOR_UPDATE_TOPIC, SENSORS, TYPE_CURRENT_OZONE_INDEX,\n+    TYPE_CURRENT_UV_INDEX, TYPE_MAX_UV_INDEX, TYPE_SAFE_EXPOSURE_TIME_1,\n+    TYPE_SAFE_EXPOSURE_TIME_2, TYPE_SAFE_EXPOSURE_TIME_3,\n+    TYPE_SAFE_EXPOSURE_TIME_4, TYPE_SAFE_EXPOSURE_TIME_5,\n+    TYPE_SAFE_EXPOSURE_TIME_6, OpenUvEntity)\n+from homeassistant.util.dt import as_local, parse_datetime\n+\n+DEPENDENCIES = ['openuv']\n+_LOGGER = logging.getLogger(__name__)\n+\n+ATTR_MAX_UV_TIME = 'time'\n+\n+\n+async def async_setup_platform(\n+        hass, config, async_add_devices, discovery_info=None):\n+    \"\"\"Set up the OpenUV binary sensor platform.\"\"\"\n+    if discovery_info is None:\n+        return\n+\n+    openuv = hass.data[DOMAIN]\n+\n+    sensors = []\n+    for sensor_type in discovery_info[CONF_MONITORED_CONDITIONS]:\n+        name, icon, unit = SENSORS[sensor_type]\n+        sensors.append(OpenUvSensor(openuv, sensor_type, name, icon, unit))\n+\n+    async_add_devices(sensors, True)\n+\n+\n+class OpenUvSensor(OpenUvEntity):\n+    \"\"\"Define a binary sensor for OpenUV.\"\"\"\n+\n+    def __init__(self, openuv, sensor_type, name, icon, unit):\n+        \"\"\"Initialize the sensor.\"\"\"\n+        super().__init__(openuv)\n+\n+        self._icon = icon\n+        self._latitude = openuv.client.latitude\n+        self._longitude = openuv.client.longitude\n+        self._name = name\n+        self._sensor_type = sensor_type\n+        self._state = None\n+        self._unit = unit\n+\n+    @property\n+    def icon(self):\n+        \"\"\"Return the icon.\"\"\"\n+        return self._icon\n+\n+    @property\n+    def should_poll(self):\n+        \"\"\"Disable polling.\"\"\"\n+        return False\n+\n+    @property\n+    def state(self):\n+        \"\"\"Return the status of the sensor.\"\"\"\n+        return self._state\n+\n+    @property\n+    def unique_id(self) -> str:\n+        \"\"\"Return a unique, HASS-friendly identifier for this entity.\"\"\"\n+        return '{0}_{1}_{2}'.format(\n+            self._latitude, self._longitude, self._sensor_type)\n+\n+    @property\n+    def unit_of_measurement(self):\n+        \"\"\"Return the unit the value is expressed in.\"\"\"\n+        return self._unit\n+\n+    @callback\n+    def _update_data(self):\n+        \"\"\"Update the state.\"\"\"\n+        self.async_schedule_update_ha_state(True)\n+\n+    async def async_added_to_hass(self):\n+        \"\"\"Register callbacks.\"\"\"\n+        async_dispatcher_connect(\n+            self.hass, SENSOR_UPDATE_TOPIC, self._update_data)\n+\n+    async def async_update(self):\n+        \"\"\"Update the state.\"\"\"\n+        data = self.openuv.data[DATA_UV]['result']\n+        if self._sensor_type == TYPE_CURRENT_OZONE_INDEX:\n+            self._state = data['ozone']\n+        elif self._sensor_type == TYPE_CURRENT_UV_INDEX:\n+            self._state = data['uv']\n+        elif self._sensor_type == TYPE_MAX_UV_INDEX:\n+            self._state = data['uv_max']\n+            self._attrs.update({\n+                ATTR_MAX_UV_TIME: as_local(\n+                    parse_datetime(data['uv_max_time']))\n+            })\n+        elif self._sensor_type in (TYPE_SAFE_EXPOSURE_TIME_1,\n+                                   TYPE_SAFE_EXPOSURE_TIME_2,\n+                                   TYPE_SAFE_EXPOSURE_TIME_3,\n+                                   TYPE_SAFE_EXPOSURE_TIME_4,\n+                                   TYPE_SAFE_EXPOSURE_TIME_5,\n+                                   TYPE_SAFE_EXPOSURE_TIME_6):\n+            kind = self._sensor_type[-1]", "hunk": "@@ -10,7 +10,7 @@ from homeassistant.const import CONF_MONITORED_CONDITIONS\n from homeassistant.core import callback\n from homeassistant.helpers.dispatcher import async_dispatcher_connect\n from homeassistant.components.openuv import (\n-    DATA_UV, DOMAIN, SENSOR_UPDATE_TOPIC, SENSORS, TYPE_CURRENT_OZONE_INDEX,\n+    DATA_UV, DOMAIN, SENSORS, TOPIC_UPDATE, TYPE_CURRENT_OZONE_INDEX,\n     TYPE_CURRENT_UV_INDEX, TYPE_MAX_UV_INDEX, TYPE_SAFE_EXPOSURE_TIME_1,\n     TYPE_SAFE_EXPOSURE_TIME_2, TYPE_SAFE_EXPOSURE_TIME_3,\n     TYPE_SAFE_EXPOSURE_TIME_4, TYPE_SAFE_EXPOSURE_TIME_5,\n", "comment": "Depending on the string value of a constant defined in home assistant for interaction with another API like this, could be considered error prone. It would be safer to decouple the APIs using a dictionary.", "ids": [315502, "5a08ec34e8ca5e29ea7b05b5488bacf20557d347", "28cc21952b008ed862b2cb2f131d9554b614bc22"], "repo": "home-assistant-core", "ghid": 15769, "old": " from homeassistant.core import callback\n from homeassistant.helpers.dispatcher import async_dispatcher_connect\n from homeassistant.components.openuv import (\n-    DATA_UV, DOMAIN, SENSOR_UPDATE_TOPIC, SENSORS, TYPE_CURRENT_OZONE_INDEX,\n     TYPE_CURRENT_UV_INDEX, TYPE_MAX_UV_INDEX, TYPE_SAFE_EXPOSURE_TIME_1,\n     TYPE_SAFE_EXPOSURE_TIME_2, TYPE_SAFE_EXPOSURE_TIME_3,\n     TYPE_SAFE_EXPOSURE_TIME_4, TYPE_SAFE_EXPOSURE_TIME_5,", "new": " from homeassistant.core import callback\n from homeassistant.helpers.dispatcher import async_dispatcher_connect\n from homeassistant.components.openuv import (\n+    DATA_UV, DOMAIN, SENSORS, TOPIC_UPDATE, TYPE_CURRENT_OZONE_INDEX,\n     TYPE_CURRENT_UV_INDEX, TYPE_MAX_UV_INDEX, TYPE_SAFE_EXPOSURE_TIME_1,\n     TYPE_SAFE_EXPOSURE_TIME_2, TYPE_SAFE_EXPOSURE_TIME_3,\n     TYPE_SAFE_EXPOSURE_TIME_4, TYPE_SAFE_EXPOSURE_TIME_5,", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -0,0 +1,113 @@\n+\"\"\"\n+This platform provides sensors for OpenUV data.\n+\n+For more details about this platform, please refer to the documentation at\n+https://home-assistant.io/components/sensor.openuv/\n+\"\"\"\n+import logging\n+\n+from homeassistant.const import CONF_MONITORED_CONDITIONS\n+from homeassistant.core import callback\n+from homeassistant.helpers.dispatcher import async_dispatcher_connect\n+from homeassistant.components.openuv import (\n+    DATA_UV, DOMAIN, SENSOR_UPDATE_TOPIC, SENSORS, TYPE_CURRENT_OZONE_INDEX,\n+    TYPE_CURRENT_UV_INDEX, TYPE_MAX_UV_INDEX, TYPE_SAFE_EXPOSURE_TIME_1,\n+    TYPE_SAFE_EXPOSURE_TIME_2, TYPE_SAFE_EXPOSURE_TIME_3,\n+    TYPE_SAFE_EXPOSURE_TIME_4, TYPE_SAFE_EXPOSURE_TIME_5,\n+    TYPE_SAFE_EXPOSURE_TIME_6, OpenUvEntity)\n+from homeassistant.util.dt import as_local, parse_datetime\n+\n+DEPENDENCIES = ['openuv']\n+_LOGGER = logging.getLogger(__name__)\n+\n+ATTR_MAX_UV_TIME = 'time'\n+\n+\n+async def async_setup_platform(\n+        hass, config, async_add_devices, discovery_info=None):\n+    \"\"\"Set up the OpenUV binary sensor platform.\"\"\"\n+    if discovery_info is None:\n+        return\n+\n+    openuv = hass.data[DOMAIN]\n+\n+    sensors = []\n+    for sensor_type in discovery_info[CONF_MONITORED_CONDITIONS]:\n+        name, icon, unit = SENSORS[sensor_type]\n+        sensors.append(OpenUvSensor(openuv, sensor_type, name, icon, unit))\n+\n+    async_add_devices(sensors, True)\n+\n+\n+class OpenUvSensor(OpenUvEntity):\n+    \"\"\"Define a binary sensor for OpenUV.\"\"\"\n+\n+    def __init__(self, openuv, sensor_type, name, icon, unit):\n+        \"\"\"Initialize the sensor.\"\"\"\n+        super().__init__(openuv)\n+\n+        self._icon = icon\n+        self._latitude = openuv.client.latitude\n+        self._longitude = openuv.client.longitude\n+        self._name = name\n+        self._sensor_type = sensor_type\n+        self._state = None\n+        self._unit = unit\n+\n+    @property\n+    def icon(self):\n+        \"\"\"Return the icon.\"\"\"\n+        return self._icon\n+\n+    @property\n+    def should_poll(self):\n+        \"\"\"Disable polling.\"\"\"\n+        return False\n+\n+    @property\n+    def state(self):\n+        \"\"\"Return the status of the sensor.\"\"\"\n+        return self._state\n+\n+    @property\n+    def unique_id(self) -> str:\n+        \"\"\"Return a unique, HASS-friendly identifier for this entity.\"\"\"\n+        return '{0}_{1}_{2}'.format(\n+            self._latitude, self._longitude, self._sensor_type)\n+\n+    @property\n+    def unit_of_measurement(self):\n+        \"\"\"Return the unit the value is expressed in.\"\"\"\n+        return self._unit\n+\n+    @callback\n+    def _update_data(self):\n+        \"\"\"Update the state.\"\"\"\n+        self.async_schedule_update_ha_state(True)\n+\n+    async def async_added_to_hass(self):\n+        \"\"\"Register callbacks.\"\"\"\n+        async_dispatcher_connect(\n+            self.hass, SENSOR_UPDATE_TOPIC, self._update_data)\n+\n+    async def async_update(self):\n+        \"\"\"Update the state.\"\"\"\n+        data = self.openuv.data[DATA_UV]['result']\n+        if self._sensor_type == TYPE_CURRENT_OZONE_INDEX:\n+            self._state = data['ozone']\n+        elif self._sensor_type == TYPE_CURRENT_UV_INDEX:\n+            self._state = data['uv']\n+        elif self._sensor_type == TYPE_MAX_UV_INDEX:\n+            self._state = data['uv_max']\n+            self._attrs.update({\n+                ATTR_MAX_UV_TIME: as_local(\n+                    parse_datetime(data['uv_max_time']))\n+            })\n+        elif self._sensor_type in (TYPE_SAFE_EXPOSURE_TIME_1,\n+                                   TYPE_SAFE_EXPOSURE_TIME_2,\n+                                   TYPE_SAFE_EXPOSURE_TIME_3,\n+                                   TYPE_SAFE_EXPOSURE_TIME_4,\n+                                   TYPE_SAFE_EXPOSURE_TIME_5,\n+                                   TYPE_SAFE_EXPOSURE_TIME_6):\n+            kind = self._sensor_type[-1]", "msg": "Depending on the string value of a constant defined in home assistant for interaction with another API like this, could be considered error prone. It would be safer to decouple the APIs using a dictionary."}
{"old_hunk": "@@ -0,0 +1,182 @@\n+\"\"\"\n+Support for data from openuv.io.\n+\n+For more details about this component, please refer to the documentation at\n+https://home-assistant.io/components/openuv/\n+\"\"\"\n+import logging\n+from datetime import timedelta\n+\n+import voluptuous as vol\n+\n+from homeassistant.const import (\n+    ATTR_ATTRIBUTION, CONF_API_KEY, CONF_BINARY_SENSORS, CONF_ELEVATION,\n+    CONF_LATITUDE, CONF_LONGITUDE, CONF_MONITORED_CONDITIONS,\n+    CONF_SCAN_INTERVAL, CONF_SENSORS)\n+from homeassistant.helpers import (\n+    aiohttp_client, config_validation as cv, discovery)\n+from homeassistant.helpers.dispatcher import async_dispatcher_send\n+from homeassistant.helpers.entity import Entity\n+from homeassistant.helpers.event import async_track_time_interval\n+\n+REQUIREMENTS = ['pyopenuv==1.0.1']\n+_LOGGER = logging.getLogger(__name__)\n+\n+DOMAIN = 'openuv'\n+\n+DATA_PROTECTION_WINDOW = 'protection_window'\n+DATA_UV = 'uv'\n+\n+DEFAULT_ATTRIBUTION = 'Data provided by OpenUV'\n+DEFAULT_SCAN_INTERVAL = timedelta(minutes=30)\n+\n+NOTIFICATION_ID = 'openuv_notification'\n+NOTIFICATION_TITLE = 'OpenUV Component Setup'\n+\n+SENSOR_UPDATE_TOPIC = '{0}_data_update'.format(DOMAIN)\n+\n+TYPE_CURRENT_OZONE_INDEX = 'current_ozone_index'\n+TYPE_CURRENT_UV_INDEX = 'current_uv_index'\n+TYPE_MAX_UV_INDEX = 'max_uv_index'\n+TYPE_PROTECTION_WINDOW = 'uv_protection_window'\n+TYPE_SAFE_EXPOSURE_TIME_1 = 'safe_exposure_time_type_1'\n+TYPE_SAFE_EXPOSURE_TIME_2 = 'safe_exposure_time_type_2'\n+TYPE_SAFE_EXPOSURE_TIME_3 = 'safe_exposure_time_type_3'\n+TYPE_SAFE_EXPOSURE_TIME_4 = 'safe_exposure_time_type_4'\n+TYPE_SAFE_EXPOSURE_TIME_5 = 'safe_exposure_time_type_5'\n+TYPE_SAFE_EXPOSURE_TIME_6 = 'safe_exposure_time_type_6'\n+\n+BINARY_SENSORS = {\n+    TYPE_PROTECTION_WINDOW: ('Protection Window', 'mdi:sunglasses')\n+}\n+\n+BINARY_SENSOR_SCHEMA = vol.Schema({\n+    vol.Optional(CONF_MONITORED_CONDITIONS, default=list(BINARY_SENSORS)):\n+        vol.All(cv.ensure_list, [vol.In(BINARY_SENSORS)])\n+})\n+\n+SENSORS = {\n+    TYPE_CURRENT_OZONE_INDEX: (\n+        'Current Ozone Index', 'mdi:vector-triangle', 'index'),\n+    TYPE_CURRENT_UV_INDEX: ('Current UV Index', 'mdi:weather-sunny', 'index'),\n+    TYPE_MAX_UV_INDEX: ('Max UV Index', 'mdi:weather-sunny', 'index'),\n+    TYPE_SAFE_EXPOSURE_TIME_1: (\n+        'Skin Type 1 Safe Exposure Time', 'mdi:timer', 'minutes'),\n+    TYPE_SAFE_EXPOSURE_TIME_2: (\n+        'Skin Type 2 Safe Exposure Time', 'mdi:timer', 'minutes'),\n+    TYPE_SAFE_EXPOSURE_TIME_3: (\n+        'Skin Type 3 Safe Exposure Time', 'mdi:timer', 'minutes'),\n+    TYPE_SAFE_EXPOSURE_TIME_4: (\n+        'Skin Type 4 Safe Exposure Time', 'mdi:timer', 'minutes'),\n+    TYPE_SAFE_EXPOSURE_TIME_5: (\n+        'Skin Type 5 Safe Exposure Time', 'mdi:timer', 'minutes'),\n+    TYPE_SAFE_EXPOSURE_TIME_6: (\n+        'Skin Type 6 Safe Exposure Time', 'mdi:timer', 'minutes'),\n+}\n+\n+SENSOR_SCHEMA = vol.Schema({\n+    vol.Optional(CONF_MONITORED_CONDITIONS, default=list(SENSORS)):\n+        vol.All(cv.ensure_list, [vol.In(SENSORS)])\n+})\n+\n+CONFIG_SCHEMA = vol.Schema({\n+    DOMAIN: vol.Schema({\n+        vol.Required(CONF_API_KEY): cv.string,\n+        vol.Optional(CONF_ELEVATION): float,\n+        vol.Optional(CONF_LATITUDE): cv.latitude,\n+        vol.Optional(CONF_LONGITUDE): cv.longitude,\n+        vol.Optional(CONF_BINARY_SENSORS, default={}): BINARY_SENSOR_SCHEMA,", "hunk": "@@ -33,7 +33,7 @@ DEFAULT_SCAN_INTERVAL = timedelta(minutes=30)\n NOTIFICATION_ID = 'openuv_notification'\n NOTIFICATION_TITLE = 'OpenUV Component Setup'\n \n-SENSOR_UPDATE_TOPIC = '{0}_data_update'.format(DOMAIN)\n+TOPIC_UPDATE = '{0}_data_update'.format(DOMAIN)\n \n TYPE_CURRENT_OZONE_INDEX = 'current_ozone_index'\n TYPE_CURRENT_UV_INDEX = 'current_uv_index'\n", "comment": "Is the default value evaluation nested in voluptuous so that the empty dict is populated with the default conditions?", "ids": [315501, "5a08ec34e8ca5e29ea7b05b5488bacf20557d347", "28cc21952b008ed862b2cb2f131d9554b614bc22"], "repo": "home-assistant-core", "ghid": 15769, "old": " NOTIFICATION_ID = 'openuv_notification'\n NOTIFICATION_TITLE = 'OpenUV Component Setup'\n-SENSOR_UPDATE_TOPIC = '{0}_data_update'.format(DOMAIN)\n TYPE_CURRENT_OZONE_INDEX = 'current_ozone_index'\n TYPE_CURRENT_UV_INDEX = 'current_uv_index'", "new": " NOTIFICATION_ID = 'openuv_notification'\n NOTIFICATION_TITLE = 'OpenUV Component Setup'\n+TOPIC_UPDATE = '{0}_data_update'.format(DOMAIN)\n TYPE_CURRENT_OZONE_INDEX = 'current_ozone_index'\n TYPE_CURRENT_UV_INDEX = 'current_uv_index'", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -0,0 +1,182 @@\n+\"\"\"\n+Support for data from openuv.io.\n+\n+For more details about this component, please refer to the documentation at\n+https://home-assistant.io/components/openuv/\n+\"\"\"\n+import logging\n+from datetime import timedelta\n+\n+import voluptuous as vol\n+\n+from homeassistant.const import (\n+    ATTR_ATTRIBUTION, CONF_API_KEY, CONF_BINARY_SENSORS, CONF_ELEVATION,\n+    CONF_LATITUDE, CONF_LONGITUDE, CONF_MONITORED_CONDITIONS,\n+    CONF_SCAN_INTERVAL, CONF_SENSORS)\n+from homeassistant.helpers import (\n+    aiohttp_client, config_validation as cv, discovery)\n+from homeassistant.helpers.dispatcher import async_dispatcher_send\n+from homeassistant.helpers.entity import Entity\n+from homeassistant.helpers.event import async_track_time_interval\n+\n+REQUIREMENTS = ['pyopenuv==1.0.1']\n+_LOGGER = logging.getLogger(__name__)\n+\n+DOMAIN = 'openuv'\n+\n+DATA_PROTECTION_WINDOW = 'protection_window'\n+DATA_UV = 'uv'\n+\n+DEFAULT_ATTRIBUTION = 'Data provided by OpenUV'\n+DEFAULT_SCAN_INTERVAL = timedelta(minutes=30)\n+\n+NOTIFICATION_ID = 'openuv_notification'\n+NOTIFICATION_TITLE = 'OpenUV Component Setup'\n+\n+SENSOR_UPDATE_TOPIC = '{0}_data_update'.format(DOMAIN)\n+\n+TYPE_CURRENT_OZONE_INDEX = 'current_ozone_index'\n+TYPE_CURRENT_UV_INDEX = 'current_uv_index'\n+TYPE_MAX_UV_INDEX = 'max_uv_index'\n+TYPE_PROTECTION_WINDOW = 'uv_protection_window'\n+TYPE_SAFE_EXPOSURE_TIME_1 = 'safe_exposure_time_type_1'\n+TYPE_SAFE_EXPOSURE_TIME_2 = 'safe_exposure_time_type_2'\n+TYPE_SAFE_EXPOSURE_TIME_3 = 'safe_exposure_time_type_3'\n+TYPE_SAFE_EXPOSURE_TIME_4 = 'safe_exposure_time_type_4'\n+TYPE_SAFE_EXPOSURE_TIME_5 = 'safe_exposure_time_type_5'\n+TYPE_SAFE_EXPOSURE_TIME_6 = 'safe_exposure_time_type_6'\n+\n+BINARY_SENSORS = {\n+    TYPE_PROTECTION_WINDOW: ('Protection Window', 'mdi:sunglasses')\n+}\n+\n+BINARY_SENSOR_SCHEMA = vol.Schema({\n+    vol.Optional(CONF_MONITORED_CONDITIONS, default=list(BINARY_SENSORS)):\n+        vol.All(cv.ensure_list, [vol.In(BINARY_SENSORS)])\n+})\n+\n+SENSORS = {\n+    TYPE_CURRENT_OZONE_INDEX: (\n+        'Current Ozone Index', 'mdi:vector-triangle', 'index'),\n+    TYPE_CURRENT_UV_INDEX: ('Current UV Index', 'mdi:weather-sunny', 'index'),\n+    TYPE_MAX_UV_INDEX: ('Max UV Index', 'mdi:weather-sunny', 'index'),\n+    TYPE_SAFE_EXPOSURE_TIME_1: (\n+        'Skin Type 1 Safe Exposure Time', 'mdi:timer', 'minutes'),\n+    TYPE_SAFE_EXPOSURE_TIME_2: (\n+        'Skin Type 2 Safe Exposure Time', 'mdi:timer', 'minutes'),\n+    TYPE_SAFE_EXPOSURE_TIME_3: (\n+        'Skin Type 3 Safe Exposure Time', 'mdi:timer', 'minutes'),\n+    TYPE_SAFE_EXPOSURE_TIME_4: (\n+        'Skin Type 4 Safe Exposure Time', 'mdi:timer', 'minutes'),\n+    TYPE_SAFE_EXPOSURE_TIME_5: (\n+        'Skin Type 5 Safe Exposure Time', 'mdi:timer', 'minutes'),\n+    TYPE_SAFE_EXPOSURE_TIME_6: (\n+        'Skin Type 6 Safe Exposure Time', 'mdi:timer', 'minutes'),\n+}\n+\n+SENSOR_SCHEMA = vol.Schema({\n+    vol.Optional(CONF_MONITORED_CONDITIONS, default=list(SENSORS)):\n+        vol.All(cv.ensure_list, [vol.In(SENSORS)])\n+})\n+\n+CONFIG_SCHEMA = vol.Schema({\n+    DOMAIN: vol.Schema({\n+        vol.Required(CONF_API_KEY): cv.string,\n+        vol.Optional(CONF_ELEVATION): float,\n+        vol.Optional(CONF_LATITUDE): cv.latitude,\n+        vol.Optional(CONF_LONGITUDE): cv.longitude,\n+        vol.Optional(CONF_BINARY_SENSORS, default={}): BINARY_SENSOR_SCHEMA,", "msg": "Is the default value evaluation nested in voluptuous so that the empty dict is populated with the default conditions?"}
{"old_hunk": "@@ -0,0 +1,88 @@\n+\"\"\"\n+Support for RitAssist Platform.\n+\n+For more details about this platform, please refer to the documentation at\n+https://home-assistant.io/components/device_tracker.ritassist/\n+\"\"\"\n+import logging\n+\n+import requests\n+import voluptuous as vol\n+\n+import homeassistant.helpers.config_validation as cv\n+from homeassistant.components.device_tracker import PLATFORM_SCHEMA\n+from homeassistant.const import CONF_USERNAME, CONF_PASSWORD\n+from homeassistant.helpers.event import track_utc_time_change\n+\n+REQUIREMENTS = ['ritassist==0.5']\n+\n+_LOGGER = logging.getLogger(__name__)\n+\n+CONF_CLIENT_ID = 'client_id'\n+CONF_CLIENT_SECRET = 'client_secret'\n+CONF_INCLUDE = 'include'\n+\n+PLATFORM_SCHEMA = PLATFORM_SCHEMA.extend({\n+    vol.Required(CONF_USERNAME): cv.string,\n+    vol.Required(CONF_PASSWORD): cv.string,\n+    vol.Required(CONF_CLIENT_ID): cv.string,\n+    vol.Required(CONF_CLIENT_SECRET): cv.string,\n+    vol.Optional(CONF_INCLUDE, default=[]):\n+        vol.All(cv.ensure_list, [cv.string])\n+})\n+\n+\n+def setup_scanner(hass, config: dict, see, discovery_info=None):\n+    \"\"\"Set up the DeviceScanner and check if login is valid.\"\"\"\n+    scanner = RitAssistDeviceScanner(config, see)\n+    if not scanner.login(hass):\n+        _LOGGER.error('RitAssist authentication failed')\n+        return False\n+    return True\n+\n+\n+class RitAssistDeviceScanner:\n+    \"\"\"Define a scanner for the RitAssist platform.\"\"\"\n+\n+    def __init__(self, config, see):\n+        \"\"\"Initialize RitAssistDeviceScanner.\"\"\"\n+        from ritassist import API\n+\n+        self._include = config.get(CONF_INCLUDE)\n+        self._see = see\n+\n+        self._api = API(config.get(CONF_CLIENT_ID),\n+                        config.get(CONF_CLIENT_SECRET),\n+                        config.get(CONF_USERNAME),\n+                        config.get(CONF_PASSWORD))\n+\n+    def setup(self, hass):\n+        \"\"\"Setup a timer and start gathering devices.\"\"\"\n+        self._refresh()\n+        track_utc_time_change(hass,\n+                              lambda now: self._refresh(),\n+                              second=range(0, 60, 30))\n+\n+    def login(self, hass):\n+        \"\"\"Perform a login on the RitAssist API.\"\"\"\n+        if self._api.login():\n+            self.setup(hass)\n+            return True\n+        else:", "hunk": "@@ -68,8 +68,7 @@ class RitAssistDeviceScanner:\n         if self._api.login():\n             self.setup(hass)\n             return True\n-        else:\n-            return False\n+        return False\n \n     def _refresh(self) -> None:\n         \"\"\"Refresh device information from the platform.\"\"\"\n", "comment": "The `else` isn't needed. Just return False.", "ids": [315552, "7671f8b86c82fe9480db8ef4bf02feadd8a7d044", "15aad5b1133407ac33a40da6d7cdfccb8189880c"], "repo": "home-assistant-core", "ghid": 15780, "old": "         if self._api.login():\n             self.setup(hass)\n             return True\n-        else:\n-            return False\n     def _refresh(self) -> None:\n         \"\"\"Refresh device information from the platform.\"\"\"", "new": "         if self._api.login():\n             self.setup(hass)\n             return True\n+        return False\n     def _refresh(self) -> None:\n         \"\"\"Refresh device information from the platform.\"\"\"", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -0,0 +1,88 @@\n+\"\"\"\n+Support for RitAssist Platform.\n+\n+For more details about this platform, please refer to the documentation at\n+https://home-assistant.io/components/device_tracker.ritassist/\n+\"\"\"\n+import logging\n+\n+import requests\n+import voluptuous as vol\n+\n+import homeassistant.helpers.config_validation as cv\n+from homeassistant.components.device_tracker import PLATFORM_SCHEMA\n+from homeassistant.const import CONF_USERNAME, CONF_PASSWORD\n+from homeassistant.helpers.event import track_utc_time_change\n+\n+REQUIREMENTS = ['ritassist==0.5']\n+\n+_LOGGER = logging.getLogger(__name__)\n+\n+CONF_CLIENT_ID = 'client_id'\n+CONF_CLIENT_SECRET = 'client_secret'\n+CONF_INCLUDE = 'include'\n+\n+PLATFORM_SCHEMA = PLATFORM_SCHEMA.extend({\n+    vol.Required(CONF_USERNAME): cv.string,\n+    vol.Required(CONF_PASSWORD): cv.string,\n+    vol.Required(CONF_CLIENT_ID): cv.string,\n+    vol.Required(CONF_CLIENT_SECRET): cv.string,\n+    vol.Optional(CONF_INCLUDE, default=[]):\n+        vol.All(cv.ensure_list, [cv.string])\n+})\n+\n+\n+def setup_scanner(hass, config: dict, see, discovery_info=None):\n+    \"\"\"Set up the DeviceScanner and check if login is valid.\"\"\"\n+    scanner = RitAssistDeviceScanner(config, see)\n+    if not scanner.login(hass):\n+        _LOGGER.error('RitAssist authentication failed')\n+        return False\n+    return True\n+\n+\n+class RitAssistDeviceScanner:\n+    \"\"\"Define a scanner for the RitAssist platform.\"\"\"\n+\n+    def __init__(self, config, see):\n+        \"\"\"Initialize RitAssistDeviceScanner.\"\"\"\n+        from ritassist import API\n+\n+        self._include = config.get(CONF_INCLUDE)\n+        self._see = see\n+\n+        self._api = API(config.get(CONF_CLIENT_ID),\n+                        config.get(CONF_CLIENT_SECRET),\n+                        config.get(CONF_USERNAME),\n+                        config.get(CONF_PASSWORD))\n+\n+    def setup(self, hass):\n+        \"\"\"Setup a timer and start gathering devices.\"\"\"\n+        self._refresh()\n+        track_utc_time_change(hass,\n+                              lambda now: self._refresh(),\n+                              second=range(0, 60, 30))\n+\n+    def login(self, hass):\n+        \"\"\"Perform a login on the RitAssist API.\"\"\"\n+        if self._api.login():\n+            self.setup(hass)\n+            return True\n+        else:", "msg": "The `else` isn't needed. Just return False."}
{"old_hunk": "@@ -139,7 +139,13 @@ def icon(self):\n \n     def update(self):\n         \"\"\"Get the latest data, update state.\"\"\"\n-        self.update_sensor()\n+        from requests import ReadTimeout", "hunk": "@@ -139,11 +139,10 @@ class Sense(Entity):\n \n     def update(self):\n         \"\"\"Get the latest data, update state.\"\"\"\n-        from requests import ReadTimeout\n-        from websocket import WebSocketTimeoutException\n+        from sense_energy import SenseAPITimeoutException\n         try:\n             self.update_sensor()\n-        except (WebSocketTimeoutException, ReadTimeout):\n+        except SenseAPITimeoutException:\n             _LOGGER.error(\"Timeout retrieving data\")\n             return\n \n", "comment": "I don't think you should introduce exceptions from heretofore un-explicitly-used libraries. The `sense_energy` library should provide its own exception type and HASS should deal with _that_ instead.", "ids": [315565, "cb7b27303d38f8fcacca787222f0a9f4c1c9957b", "3fa4d0df3c6bca11b35b4064fabf3a7ed299eec9"], "repo": "home-assistant-core", "ghid": 15789, "old": "     def update(self):\n         \"\"\"Get the latest data, update state.\"\"\"\n-        from requests import ReadTimeout\n-        from websocket import WebSocketTimeoutException\n         try:\n             self.update_sensor()\n-        except (WebSocketTimeoutException, ReadTimeout):\n             _LOGGER.error(\"Timeout retrieving data\")\n             return", "new": "     def update(self):\n         \"\"\"Get the latest data, update state.\"\"\"\n+        from sense_energy import SenseAPITimeoutException\n         try:\n             self.update_sensor()\n+        except SenseAPITimeoutException:\n             _LOGGER.error(\"Timeout retrieving data\")\n             return", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -139,7 +139,13 @@ def icon(self):\n \n     def update(self):\n         \"\"\"Get the latest data, update state.\"\"\"\n-        self.update_sensor()\n+        from requests import ReadTimeout", "msg": "I don't think you should introduce exceptions from heretofore un-explicitly-used libraries. The `sense_energy` library should provide its own exception type and HASS should deal with _that_ instead."}
{"old_hunk": "@@ -139,7 +141,11 @@ def icon(self):\n \n     def update(self):\n         \"\"\"Get the latest data, update state.\"\"\"\n-        self.update_sensor()\n+        try:\n+            self.update_sensor()\n+        except (WebSocketTimeoutException, ReadTimeout):", "hunk": "@@ -141,6 +141,8 @@ class Sense(Entity):\n \n     def update(self):\n         \"\"\"Get the latest data, update state.\"\"\"\n+        from requests import ReadTimeout\n+        from websocket import WebSocketTimeoutException\n         try:\n             self.update_sensor()\n         except (WebSocketTimeoutException, ReadTimeout):\n", "comment": "undefined name 'WebSocketTimeoutException'<br>undefined name 'ReadTimeout'", "ids": [315562, "5cff9dfa8a2b0b8d3f790eef9cf1545ec3f6806f", "cb7b27303d38f8fcacca787222f0a9f4c1c9957b"], "repo": "home-assistant-core", "ghid": 15789, "old": "     def update(self):\n         \"\"\"Get the latest data, update state.\"\"\"\n         try:\n             self.update_sensor()\n         except (WebSocketTimeoutException, ReadTimeout):", "new": "     def update(self):\n         \"\"\"Get the latest data, update state.\"\"\"\n+        from requests import ReadTimeout\n+        from websocket import WebSocketTimeoutException\n         try:\n             self.update_sensor()\n         except (WebSocketTimeoutException, ReadTimeout):", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -139,7 +141,11 @@ def icon(self):\n \n     def update(self):\n         \"\"\"Get the latest data, update state.\"\"\"\n-        self.update_sensor()\n+        try:\n+            self.update_sensor()\n+        except (WebSocketTimeoutException, ReadTimeout):", "msg": "undefined name 'WebSocketTimeoutException'<br>undefined name 'ReadTimeout'"}
{"old_hunk": "@@ -168,7 +169,7 @@ def async_handle_turn_service(service):\n     def async_handle_core_service(call):\n         \"\"\"Service handler for handling core services.\"\"\"\n         if call.service == SERVICE_HOMEASSISTANT_STOP:\n-            hass.async_create_task(hass.async_stop())\n+            async_call_later(hass, 0, hass.async_stop())", "hunk": "@@ -169,7 +169,7 @@ def async_setup(hass: ha.HomeAssistant, config: dict) -> Awaitable[bool]:\n     def async_handle_core_service(call):\n         \"\"\"Service handler for handling core services.\"\"\"\n         if call.service == SERVICE_HOMEASSISTANT_STOP:\n-            async_call_later(hass, 0, hass.async_stop())\n+            hass.async_create_task(hass.async_stop())\n             return\n \n         try:\n", "comment": "Home Assistant is a layered system. It's weird to change behavior in the core because a few layers on top is reporting the wrong result. The only thing that `async_call_later` does is to delay the call between very little time to up to a second (as we don't know when next time changed event will fire). We're actually working on removing time changed event and replace it with `hass.loop.call_soon`. What is the HTTP message that is causing the error and could we update `<ha-call-service-button>` on the frontend to catch it correctly instead?", "ids": [315679, "3dd3be6857b8ec8ed4767f1a32bf3c1f0710275f", "c50dc3796cb36bec9f29c2d1b22ec5a1babd5e84"], "repo": "home-assistant-core", "ghid": 15803, "old": "     def async_handle_core_service(call):\n         \"\"\"Service handler for handling core services.\"\"\"\n         if call.service == SERVICE_HOMEASSISTANT_STOP:\n-            async_call_later(hass, 0, hass.async_stop())\n             return\n         try:", "new": "     def async_handle_core_service(call):\n         \"\"\"Service handler for handling core services.\"\"\"\n         if call.service == SERVICE_HOMEASSISTANT_STOP:\n+            hass.async_create_task(hass.async_stop())\n             return\n         try:", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -168,7 +169,7 @@ def async_handle_turn_service(service):\n     def async_handle_core_service(call):\n         \"\"\"Service handler for handling core services.\"\"\"\n         if call.service == SERVICE_HOMEASSISTANT_STOP:\n-            hass.async_create_task(hass.async_stop())\n+            async_call_later(hass, 0, hass.async_stop())", "msg": "Home Assistant is a layered system. It's weird to change behavior in the core because a few layers on top is reporting the wrong result. The only thing that `async_call_later` does is to delay the call between very little time to up to a second (as we don't know when next time changed event will fire). We're actually working on removing time changed event and replace it with `hass.loop.call_soon`. What is the HTTP message that is causing the error and could we update `<ha-call-service-button>` on the frontend to catch it correctly instead?"}
{"old_hunk": "@@ -151,8 +153,13 @@ def __init__(self, flow_mgr):\n         else:\n             handler = data['handler']\n \n+        if handler[0] == 'trusted_networks':", "hunk": "@@ -152,8 +152,7 @@ class LoginFlowIndexView(HomeAssistantView):\n             handler = data['handler']\n \n         try:\n-            result = await self._flow_mgr.async_init(\n-                handler, context={'ip_address': request[KEY_REAL_IP]})\n+            result = await self._flow_mgr.async_init(handler, context={})\n         except data_entry_flow.UnknownHandler:\n             return self.json_message('Invalid handler specified', 404)\n         except data_entry_flow.UnknownStep:\n", "comment": "This is not good. By doing this, we're starting to conflate concerns of the auth provider with the generic abstraction. It means the abstraction is not good and that needs to be changed. I don't know right now how we should evolve it, maybe we need to decouple the manager from the flow, allowing the manager to pass in an extra auth context.", "ids": [315693, "b7fc5cfe8fadeba31b56171055535ca9207d4430", "979fd57b4325f9e5bc9112fe722d2f503b03c312"], "repo": "home-assistant-core", "ghid": 15812, "old": "             handler = data['handler']\n         try:\n-            result = await self._flow_mgr.async_init(\n-                handler, context={'ip_address': request[KEY_REAL_IP]})\n         except data_entry_flow.UnknownHandler:\n             return self.json_message('Invalid handler specified', 404)\n         except data_entry_flow.UnknownStep:", "new": "             handler = data['handler']\n         try:\n+            result = await self._flow_mgr.async_init(handler, context={})\n         except data_entry_flow.UnknownHandler:\n             return self.json_message('Invalid handler specified', 404)\n         except data_entry_flow.UnknownStep:", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -151,8 +153,13 @@ def __init__(self, flow_mgr):\n         else:\n             handler = data['handler']\n \n+        if handler[0] == 'trusted_networks':", "msg": "This is not good. By doing this, we're starting to conflate concerns of the auth provider with the generic abstraction. It means the abstraction is not good and that needs to be changed. I don't know right now how we should evolve it, maybe we need to decouple the manager from the flow, allowing the manager to pass in an extra auth context."}
{"old_hunk": "@@ -93,12 +98,40 @@ def target_temperature_step(self):\n \n         return 1\n \n+    @property\n+    def device_state_attributes(self):\n+        \"\"\"Show Device Attributes.\"\"\"\n+        attributes = {\n+            'currently_active': self._zone['isCurrentlyActive']\n+        }\n+        return attributes\n+\n     @property\n     def current_operation(self):\n         \"\"\"Return current operation ie. heat, cool, idle.\"\"\"\n+        mode = self._ember.get_zone_mode(self._zone_name)\n+        return self.map_mode_eph_hass(mode)\n+\n+    @property\n+    def operation_list(self):\n+        \"\"\"Return the supported operations.\"\"\"\n+        return [STATE_AUTO, STATE_ALL_DAY, STATE_HEAT, STATE_OFF]", "hunk": "@@ -115,7 +115,7 @@ class EphEmberThermostat(ClimateDevice):\n     @property\n     def operation_list(self):\n         \"\"\"Return the supported operations.\"\"\"\n-        return [STATE_AUTO, STATE_ALL_DAY, STATE_HEAT, STATE_OFF]\n+        return OPERATION_LIST\n \n     def set_operation_mode(self, operation_mode):\n         \"\"\"Set the operation mode.\"\"\"\n", "comment": "Move this to a module level constant and return that here.", "ids": [315768, "8a814cae226d674a3f0cb952abddf188ac82e232", "e6135d78907214c96bad85e3e5c6a65c1a05b32c"], "repo": "home-assistant-core", "ghid": 15820, "old": "     @property\n     def operation_list(self):\n         \"\"\"Return the supported operations.\"\"\"\n-        return [STATE_AUTO, STATE_ALL_DAY, STATE_HEAT, STATE_OFF]\n     def set_operation_mode(self, operation_mode):\n         \"\"\"Set the operation mode.\"\"\"", "new": "     @property\n     def operation_list(self):\n         \"\"\"Return the supported operations.\"\"\"\n+        return OPERATION_LIST\n     def set_operation_mode(self, operation_mode):\n         \"\"\"Set the operation mode.\"\"\"", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -93,12 +98,40 @@ def target_temperature_step(self):\n \n         return 1\n \n+    @property\n+    def device_state_attributes(self):\n+        \"\"\"Show Device Attributes.\"\"\"\n+        attributes = {\n+            'currently_active': self._zone['isCurrentlyActive']\n+        }\n+        return attributes\n+\n     @property\n     def current_operation(self):\n         \"\"\"Return current operation ie. heat, cool, idle.\"\"\"\n+        mode = self._ember.get_zone_mode(self._zone_name)\n+        return self.map_mode_eph_hass(mode)\n+\n+    @property\n+    def operation_list(self):\n+        \"\"\"Return the supported operations.\"\"\"\n+        return [STATE_AUTO, STATE_ALL_DAY, STATE_HEAT, STATE_OFF]", "msg": "Move this to a module level constant and return that here."}
{"old_hunk": "@@ -108,6 +108,7 @@\n CONF_PENDING_TIME = 'pending_time'\n CONF_PIN = 'pin'\n CONF_PLATFORM = 'platform'\n+CONF_PROCEED = 'proceed'", "hunk": "@@ -108,7 +108,6 @@ CONF_PAYLOAD_ON = 'payload_on'\n CONF_PENDING_TIME = 'pending_time'\n CONF_PIN = 'pin'\n CONF_PLATFORM = 'platform'\n-CONF_PROCEED = 'proceed'\n CONF_PORT = 'port'\n CONF_PREFIX = 'prefix'\n CONF_PROFILE_NAME = 'profile_name'\n", "comment": "`proceed` is not self-explain to me. How about `continue_on_timeout` And it should resident in `helpers/script.py` together with `CONF_WAIT_TEMPLATE`.", "ids": [315800, "91ce7af8b18ed2bbdff9df283d3a1725ca5e91c8", "1a56021250e3efe18bb6090c54273da7fe5df73b"], "repo": "home-assistant-core", "ghid": 15836, "old": " CONF_PENDING_TIME = 'pending_time'\n CONF_PIN = 'pin'\n CONF_PLATFORM = 'platform'\n-CONF_PROCEED = 'proceed'\n CONF_PORT = 'port'\n CONF_PREFIX = 'prefix'\n CONF_PROFILE_NAME = 'profile_name'", "new": " CONF_PENDING_TIME = 'pending_time'\n CONF_PIN = 'pin'\n CONF_PLATFORM = 'platform'\n CONF_PORT = 'port'\n CONF_PREFIX = 'prefix'\n CONF_PROFILE_NAME = 'profile_name'", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -108,6 +108,7 @@\n CONF_PENDING_TIME = 'pending_time'\n CONF_PIN = 'pin'\n CONF_PLATFORM = 'platform'\n+CONF_PROCEED = 'proceed'", "msg": "`proceed` is not self-explain to me. How about `continue_on_timeout` And it should resident in `helpers/script.py` together with `CONF_WAIT_TEMPLATE`."}
{"old_hunk": "@@ -41,6 +41,8 @@\n         cv.boolean, cv.isfile),\n     vol.Optional(CONF_DETECTION_TIME, default=DEFAULT_DETECTION_TIME): vol.All(\n         cv.time_period, cv.positive_timedelta),\n+    vol.Optional(CONF_MONITORED_CONDITIONS):\n+        vol.All(cv.ensure_list, [cv.string]),", "hunk": "@@ -42,7 +42,7 @@ PLATFORM_SCHEMA = PLATFORM_SCHEMA.extend({\n     vol.Optional(CONF_DETECTION_TIME, default=DEFAULT_DETECTION_TIME): vol.All(\n         cv.time_period, cv.positive_timedelta),\n     vol.Optional(CONF_MONITORED_CONDITIONS):\n-        vol.All(cv.ensure_list, [cv.string]),\n+        vol.All(cv.ensure_list, [vol.In(AVAILABLE_ATTRS)]),\n     vol.Optional(CONF_SSID_FILTER): vol.All(cv.ensure_list, [cv.string])\n })\n \n", "comment": "There should be a list of accepted conditions that we validate against here. Please look at sensor platforms that use monitored conditions for examples.", "ids": [315859, "e07ba58ad7368141bec164a01270c24837754b8e", "eba02381475b96759018ee0d9700e90a488fa40a"], "repo": "home-assistant-core", "ghid": 15888, "old": "     vol.Optional(CONF_DETECTION_TIME, default=DEFAULT_DETECTION_TIME): vol.All(\n         cv.time_period, cv.positive_timedelta),\n     vol.Optional(CONF_MONITORED_CONDITIONS):\n-        vol.All(cv.ensure_list, [cv.string]),\n     vol.Optional(CONF_SSID_FILTER): vol.All(cv.ensure_list, [cv.string])\n })", "new": "     vol.Optional(CONF_DETECTION_TIME, default=DEFAULT_DETECTION_TIME): vol.All(\n         cv.time_period, cv.positive_timedelta),\n     vol.Optional(CONF_MONITORED_CONDITIONS):\n+        vol.All(cv.ensure_list, [vol.In(AVAILABLE_ATTRS)]),\n     vol.Optional(CONF_SSID_FILTER): vol.All(cv.ensure_list, [cv.string])\n })", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -41,6 +41,8 @@\n         cv.boolean, cv.isfile),\n     vol.Optional(CONF_DETECTION_TIME, default=DEFAULT_DETECTION_TIME): vol.All(\n         cv.time_period, cv.positive_timedelta),\n+    vol.Optional(CONF_MONITORED_CONDITIONS):\n+        vol.All(cv.ensure_list, [cv.string]),", "msg": "There should be a list of accepted conditions that we validate against here. Please look at sensor platforms that use monitored conditions for examples."}
{"old_hunk": "@@ -122,11 +124,12 @@ class FlowHandler:\n     flow_id = None\n     hass = None\n     handler = None\n-    source = None\n     cur_step = None\n+    context = None\n \n     # Set by _async_create_flow callback\n     init_step = 'init'\n+    source = None", "hunk": "@@ -129,7 +129,6 @@ class FlowHandler:\n \n     # Set by _async_create_flow callback\n     init_step = 'init'\n-    source = None\n \n     # Set by developer\n     VERSION = 1\n", "comment": "Remove this since it's in context now?", "ids": [315884, "5778867b441f0f8a2c623c01e0ae78a7694e2d24", "64c820dc4b070347dfcdda1a120e79a94e0741af"], "repo": "home-assistant-core", "ghid": 15914, "old": "     # Set by _async_create_flow callback\n     init_step = 'init'\n-    source = None\n     # Set by developer\n     VERSION = 1", "new": "     # Set by _async_create_flow callback\n     init_step = 'init'\n     # Set by developer\n     VERSION = 1", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -122,11 +124,12 @@ class FlowHandler:\n     flow_id = None\n     hass = None\n     handler = None\n-    source = None\n     cur_step = None\n+    context = None\n \n     # Set by _async_create_flow callback\n     init_step = 'init'\n+    source = None", "msg": "Remove this since it's in context now?"}
{"old_hunk": "@@ -47,6 +47,7 @@ def async_progress(self) -> List[Dict]:\n             'flow_id': flow.flow_id,\n             'handler': flow.handler,\n             'source': flow.source,\n+            'context': flow.context,", "hunk": "@@ -46,7 +46,6 @@ class FlowManager:\n         return [{\n             'flow_id': flow.flow_id,\n             'handler': flow.handler,\n-            'source': flow.source,\n             'context': flow.context,\n         } for flow in self._progress.values()]\n \n", "comment": "Why expose this?", "ids": [315883, "5778867b441f0f8a2c623c01e0ae78a7694e2d24", "64c820dc4b070347dfcdda1a120e79a94e0741af"], "repo": "home-assistant-core", "ghid": 15914, "old": "         return [{\n             'flow_id': flow.flow_id,\n             'handler': flow.handler,\n-            'source': flow.source,\n             'context': flow.context,\n         } for flow in self._progress.values()]", "new": "         return [{\n             'flow_id': flow.flow_id,\n             'handler': flow.handler,\n             'context': flow.context,\n         } for flow in self._progress.values()]", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -47,6 +47,7 @@ def async_progress(self) -> List[Dict]:\n             'flow_id': flow.flow_id,\n             'handler': flow.handler,\n             'source': flow.source,\n+            'context': flow.context,", "msg": "Why expose this?"}
{"old_hunk": "@@ -39,7 +69,23 @@ def device_class(self):\n \n     def turn_on(self, **kwargs):\n         \"\"\"Send the on command.\"\"\"\n-        self.toggle()\n+        _LOGGER.debug(\"Turn on: %s\", self._name)\n+        if self.tahoma_device.type == 'rts:GarageDoor4TRTSComponent':\n+            self.toggle()\n+        else:\n+            self.apply_action('on')\n+            self._skip_update = True\n+            self._state = STATE_ON\n+\n+    def turn_off(self, **kwargs):\n+        \"\"\"Send the off command.\"\"\"\n+        _LOGGER.debug(\"Turn off: %s\", self._name)\n+        if self.tahoma_device.type == 'rts:GarageDoor4TRTSComponent':\n+            return\n+        else:", "hunk": "@@ -82,10 +82,10 @@ class TahomaSwitch(TahomaDevice, SwitchDevice):\n         _LOGGER.debug(\"Turn off: %s\", self._name)\n         if self.tahoma_device.type == 'rts:GarageDoor4TRTSComponent':\n             return\n-        else:\n-            self.apply_action('off')\n-            self._skip_update = True\n-            self._state = STATE_OFF\n+\n+        self.apply_action('off')\n+        self._skip_update = True\n+        self._state = STATE_OFF\n \n     def toggle(self, **kwargs):\n         \"\"\"Click the switch.\"\"\"\n", "comment": "This `else` can be removed since there's a return statement above.", "ids": [315910, "546b3d651ddc99c9635d52558738729a2b44ca62", "abb5463a42cda8386cc2ff67f77fee889523d043"], "repo": "home-assistant-core", "ghid": 15925, "old": "         _LOGGER.debug(\"Turn off: %s\", self._name)\n         if self.tahoma_device.type == 'rts:GarageDoor4TRTSComponent':\n             return\n-        else:\n-            self.apply_action('off')\n-            self._skip_update = True\n-            self._state = STATE_OFF\n     def toggle(self, **kwargs):\n         \"\"\"Click the switch.\"\"\"", "new": "         _LOGGER.debug(\"Turn off: %s\", self._name)\n         if self.tahoma_device.type == 'rts:GarageDoor4TRTSComponent':\n             return\n+\n+        self.apply_action('off')\n+        self._skip_update = True\n+        self._state = STATE_OFF\n     def toggle(self, **kwargs):\n         \"\"\"Click the switch.\"\"\"", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -39,7 +69,23 @@ def device_class(self):\n \n     def turn_on(self, **kwargs):\n         \"\"\"Send the on command.\"\"\"\n-        self.toggle()\n+        _LOGGER.debug(\"Turn on: %s\", self._name)\n+        if self.tahoma_device.type == 'rts:GarageDoor4TRTSComponent':\n+            self.toggle()\n+        else:\n+            self.apply_action('on')\n+            self._skip_update = True\n+            self._state = STATE_ON\n+\n+    def turn_off(self, **kwargs):\n+        \"\"\"Send the off command.\"\"\"\n+        _LOGGER.debug(\"Turn off: %s\", self._name)\n+        if self.tahoma_device.type == 'rts:GarageDoor4TRTSComponent':\n+            return\n+        else:", "msg": "This `else` can be removed since there's a return statement above."}
{"old_hunk": "@@ -259,16 +272,23 @@ def source_list(self):\n         \"\"\"List of available input sources.\"\"\"\n         return sorted(self._source_list.keys())\n \n+    @property\n+    def channel_list(self):\n+        \"\"\"List of available channels.\"\"\"\n+        return sorted(self._channel_list.values())", "hunk": "@@ -275,7 +275,7 @@ class LgWebOSDevice(MediaPlayerDevice):\n     @property\n     def channel_list(self):\n         \"\"\"List of available channels.\"\"\"\n-        return sorted(self._channel_list.values())\n+        return self._channel_list\n \n     @property\n     def media_content_type(self):\n", "comment": "Please sort the channel_list once!", "ids": [316017, "a199366de2708a88b6142ce998eaec7bef92c270", "90de66aa8ab2d3317fcc2737e50cd986655a1a6c"], "repo": "home-assistant-core", "ghid": 15941, "old": "     @property\n     def channel_list(self):\n         \"\"\"List of available channels.\"\"\"\n-        return sorted(self._channel_list.values())\n     @property\n     def media_content_type(self):", "new": "     @property\n     def channel_list(self):\n         \"\"\"List of available channels.\"\"\"\n+        return self._channel_list\n     @property\n     def media_content_type(self):", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -259,16 +272,23 @@ def source_list(self):\n         \"\"\"List of available input sources.\"\"\"\n         return sorted(self._source_list.keys())\n \n+    @property\n+    def channel_list(self):\n+        \"\"\"List of available channels.\"\"\"\n+        return sorted(self._channel_list.values())", "msg": "Please sort the channel_list once!"}
{"old_hunk": "@@ -52,17 +52,17 @@\n     vol.Required(CONF_DESTINATION): cv.string,\n     vol.Required(CONF_ORIGIN): cv.string,\n     vol.Optional(CONF_NAME): cv.string,\n-    vol.Optional(CONF_TRAVEL_MODE): vol.In(TRAVEL_MODE),\n+    vol.Optional(CONF_TRAVEL_MODE): cv.string,\n     vol.Optional(CONF_OPTIONS, default={CONF_MODE: 'driving'}): vol.All(\n         dict, vol.Schema({\n-            vol.Optional(CONF_MODE, default='driving'): vol.In(TRAVEL_MODE),\n+            vol.Optional(CONF_MODE, default='driving'): cv.string,\n             vol.Optional('language'): vol.In(ALL_LANGUAGES),\n             vol.Optional('avoid'): vol.In(AVOID),\n             vol.Optional('units'): vol.In(UNITS),\n             vol.Exclusive('arrival_time', 'time'): cv.string,\n             vol.Exclusive('departure_time', 'time'): cv.string,\n             vol.Optional('traffic_model'): vol.In(TRAVEL_MODEL),\n-            vol.Optional('transit_mode'): vol.In(TRANSPORT_TYPE),\n+            vol.Optional('transit_mode'): cv.string,", "hunk": "@@ -62,7 +62,7 @@ PLATFORM_SCHEMA = PLATFORM_SCHEMA.extend({\n             vol.Exclusive('arrival_time', 'time'): cv.string,\n             vol.Exclusive('departure_time', 'time'): cv.string,\n             vol.Optional('traffic_model'): vol.In(TRAVEL_MODEL),\n-            vol.Optional('transit_mode'): cv.string,\n+            vol.Optional('transit_mode'): vol.In(TRANSPORT_TYPE),\n             vol.Optional('transit_routing_preference'): vol.In(TRANSIT_PREFS)\n         }))\n })\n", "comment": "This is a bad approach, we should never overload values to be either an entity or a value.", "ids": [316031, "c04309279ac9eca9b8e4f6c1f2fcf760e5eadc1c", "296f30b27acd9f535f3bb8ca6ad644466ff73d43"], "repo": "home-assistant-core", "ghid": 15943, "old": "             vol.Exclusive('arrival_time', 'time'): cv.string,\n             vol.Exclusive('departure_time', 'time'): cv.string,\n             vol.Optional('traffic_model'): vol.In(TRAVEL_MODEL),\n-            vol.Optional('transit_mode'): cv.string,\n             vol.Optional('transit_routing_preference'): vol.In(TRANSIT_PREFS)\n         }))\n })", "new": "             vol.Exclusive('arrival_time', 'time'): cv.string,\n             vol.Exclusive('departure_time', 'time'): cv.string,\n             vol.Optional('traffic_model'): vol.In(TRAVEL_MODEL),\n+            vol.Optional('transit_mode'): vol.In(TRANSPORT_TYPE),\n             vol.Optional('transit_routing_preference'): vol.In(TRANSIT_PREFS)\n         }))\n })", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -52,17 +52,17 @@\n     vol.Required(CONF_DESTINATION): cv.string,\n     vol.Required(CONF_ORIGIN): cv.string,\n     vol.Optional(CONF_NAME): cv.string,\n-    vol.Optional(CONF_TRAVEL_MODE): vol.In(TRAVEL_MODE),\n+    vol.Optional(CONF_TRAVEL_MODE): cv.string,\n     vol.Optional(CONF_OPTIONS, default={CONF_MODE: 'driving'}): vol.All(\n         dict, vol.Schema({\n-            vol.Optional(CONF_MODE, default='driving'): vol.In(TRAVEL_MODE),\n+            vol.Optional(CONF_MODE, default='driving'): cv.string,\n             vol.Optional('language'): vol.In(ALL_LANGUAGES),\n             vol.Optional('avoid'): vol.In(AVOID),\n             vol.Optional('units'): vol.In(UNITS),\n             vol.Exclusive('arrival_time', 'time'): cv.string,\n             vol.Exclusive('departure_time', 'time'): cv.string,\n             vol.Optional('traffic_model'): vol.In(TRAVEL_MODEL),\n-            vol.Optional('transit_mode'): vol.In(TRANSPORT_TYPE),\n+            vol.Optional('transit_mode'): cv.string,", "msg": "This is a bad approach, we should never overload values to be either an entity or a value."}
{"old_hunk": "@@ -740,6 +740,11 @@ def async_added_to_hass(self):\n         \"\"\"Load data init callbacks.\"\"\"\n         yield from self.hass.async_add_job(self.link_homematic)\n \n+    @property\n+    def unique_id(self):\n+        \"\"\"Return unique ID. HomeMatic entity IDs are unique by default.\"\"\"\n+        return self._name", "hunk": "@@ -743,7 +743,9 @@ class HMDevice(Entity):\n     @property\n     def unique_id(self):\n         \"\"\"Return unique ID. HomeMatic entity IDs are unique by default.\"\"\"\n-        return self._name\n+        if self._address in self._name:\n+            return self._name.replace(\" \", \"_\")\n+        return None\n \n     @property\n     def should_poll(self):\n", "comment": "Please clarify if the user can make this non unique or if this will always be constant (unique). Reading this PR description and the linked issue, it's not clear to me what you mean might change due to setting friendly name (using the resolve names function).", "ids": [316086, "3da5486fe70be180e5a0b8b1f1c9ad7ce45b2066", "69a5672a50ae21a99498f258259cd667b894c670"], "repo": "home-assistant-core", "ghid": 15950, "old": "     @property\n     def unique_id(self):\n         \"\"\"Return unique ID. HomeMatic entity IDs are unique by default.\"\"\"\n-        return self._name\n     @property\n     def should_poll(self):", "new": "     @property\n     def unique_id(self):\n         \"\"\"Return unique ID. HomeMatic entity IDs are unique by default.\"\"\"\n+        if self._address in self._name:\n+            return self._name.replace(\" \", \"_\")\n+        return None\n     @property\n     def should_poll(self):", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -740,6 +740,11 @@ def async_added_to_hass(self):\n         \"\"\"Load data init callbacks.\"\"\"\n         yield from self.hass.async_add_job(self.link_homematic)\n \n+    @property\n+    def unique_id(self):\n+        \"\"\"Return unique ID. HomeMatic entity IDs are unique by default.\"\"\"\n+        return self._name", "msg": "Please clarify if the user can make this non unique or if this will always be constant (unique). Reading this PR description and the linked issue, it's not clear to me what you mean might change due to setting friendly name (using the resolve names function)."}
{"old_hunk": "@@ -79,6 +79,11 @@ def supported_features(self):\n         \"\"\"Flag supported features.\"\"\"\n         return SUPPORTED_FEATURES\n \n+    @property\n+    def unique_id(self):\n+        \"\"\"Return a unique ID.\"\"\"\n+        return f'{self._device_id}-{self._number}'", "hunk": "@@ -82,7 +82,7 @@ class AladdinDevice(CoverDevice):\n     @property\n     def unique_id(self):\n         \"\"\"Return a unique ID.\"\"\"\n-        return f'{self._device_id}-{self._number}'\n+        return '{}-{}'.format(self._device_id, self._number)\n \n     @property\n     def name(self):\n", "comment": "f strings are only Python 3.6+. Please use new style string formatting instead.", "ids": [316313, "9ea01cab17de572960be68d071bd262767718c42", "a2c862dfe96316a61cfb500cb4cf5dfd5c54a764"], "repo": "home-assistant-core", "ghid": 15986, "old": "     @property\n     def unique_id(self):\n         \"\"\"Return a unique ID.\"\"\"\n-        return f'{self._device_id}-{self._number}'\n     @property\n     def name(self):", "new": "     @property\n     def unique_id(self):\n         \"\"\"Return a unique ID.\"\"\"\n+        return '{}-{}'.format(self._device_id, self._number)\n     @property\n     def name(self):", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -79,6 +79,11 @@ def supported_features(self):\n         \"\"\"Flag supported features.\"\"\"\n         return SUPPORTED_FEATURES\n \n+    @property\n+    def unique_id(self):\n+        \"\"\"Return a unique ID.\"\"\"\n+        return f'{self._device_id}-{self._number}'", "msg": "f strings are only Python 3.6+. Please use new style string formatting instead."}
{"old_hunk": "@@ -20,13 +20,15 @@\n \n DOMAIN = 'bmw_connected_drive'\n CONF_REGION = 'region'\n+CONF_ENABLE_SERVICES = 'services'", "hunk": "@@ -20,7 +20,7 @@ _LOGGER = logging.getLogger(__name__)\n \n DOMAIN = 'bmw_connected_drive'\n CONF_REGION = 'region'\n-CONF_ENABLE_SERVICES = 'services'\n+CONF_READ_ONLY = 'read_only'\n ATTR_VIN = 'vin'\n \n ACCOUNT_SCHEMA = vol.Schema({\n", "comment": "Can you rename the option to `read_only` and default it to `False`", "ids": [316339, "2cd788f9719d0933d565f5bf85032fa157b8b144", "5938bdb1ef485de59288772a71345a2bb17ca6f0"], "repo": "home-assistant-core", "ghid": 15993, "old": " DOMAIN = 'bmw_connected_drive'\n CONF_REGION = 'region'\n-CONF_ENABLE_SERVICES = 'services'\n ATTR_VIN = 'vin'\n ACCOUNT_SCHEMA = vol.Schema({", "new": " DOMAIN = 'bmw_connected_drive'\n CONF_REGION = 'region'\n+CONF_READ_ONLY = 'read_only'\n ATTR_VIN = 'vin'\n ACCOUNT_SCHEMA = vol.Schema({", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -20,13 +20,15 @@\n \n DOMAIN = 'bmw_connected_drive'\n CONF_REGION = 'region'\n+CONF_ENABLE_SERVICES = 'services'", "msg": "Can you rename the option to `read_only` and default it to `False`"}
{"old_hunk": "@@ -82,8 +84,9 @@ def setup_account(account_config: dict, hass, name: str) \\\n     username = account_config[CONF_USERNAME]\n     password = account_config[CONF_PASSWORD]\n     region = account_config[CONF_REGION]\n+    enable_services = account_config[CONF_ENABLE_SERVICES]\n     _LOGGER.debug('Adding new account %s', name)\n-    cd_account = BMWConnectedDriveAccount(username, password, region, name)\n+    cd_account = BMWConnectedDriveAccount(username, password, region, name,enable_services)", "hunk": "@@ -86,7 +86,7 @@ def setup_account(account_config: dict, hass, name: str) \\\n     region = account_config[CONF_REGION]\n     enable_services = account_config[CONF_ENABLE_SERVICES]\n     _LOGGER.debug('Adding new account %s', name)\n-    cd_account = BMWConnectedDriveAccount(username, password, region, name,enable_services)\n+    cd_account = BMWConnectedDriveAccount(username, password, region, name, enable_services)\n \n     def execute_service(call):\n         \"\"\"Execute a service for a vehicle.\n", "comment": "missing whitespace after ','<br>line too long (91 > 79 characters)", "ids": [316335, "ca772e0df5b715b0a878ab2dfc4c90a50e99acc6", "8be3971775e9c8e46dd879c94f807a7e6a278318"], "repo": "home-assistant-core", "ghid": 15993, "old": "     region = account_config[CONF_REGION]\n     enable_services = account_config[CONF_ENABLE_SERVICES]\n     _LOGGER.debug('Adding new account %s', name)\n-    cd_account = BMWConnectedDriveAccount(username, password, region, name,enable_services)\n     def execute_service(call):\n         \"\"\"Execute a service for a vehicle.", "new": "     region = account_config[CONF_REGION]\n     enable_services = account_config[CONF_ENABLE_SERVICES]\n     _LOGGER.debug('Adding new account %s', name)\n+    cd_account = BMWConnectedDriveAccount(username, password, region, name, enable_services)\n     def execute_service(call):\n         \"\"\"Execute a service for a vehicle.", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -82,8 +84,9 @@ def setup_account(account_config: dict, hass, name: str) \\\n     username = account_config[CONF_USERNAME]\n     password = account_config[CONF_PASSWORD]\n     region = account_config[CONF_REGION]\n+    enable_services = account_config[CONF_ENABLE_SERVICES]\n     _LOGGER.debug('Adding new account %s', name)\n-    cd_account = BMWConnectedDriveAccount(username, password, region, name)\n+    cd_account = BMWConnectedDriveAccount(username, password, region, name,enable_services)", "msg": "missing whitespace after ','<br>line too long (91 > 79 characters)"}
{"old_hunk": "@@ -147,17 +140,17 @@ def battery_level(self):\n         return self._state['details']['charge']\n \n     @property\n-    def status(self):\n+    def state(self):\n         \"\"\"Return the status of the vacuum cleaner.\"\"\"\n-        return self._status_state\n+        return self._clean_state\n \n     @property\n     def state_attributes(self):", "hunk": "@@ -145,7 +145,7 @@ class NeatoConnectedVacuum(StateVacuumDevice):\n         return self._clean_state\n \n     @property\n-    def state_attributes(self):\n+    def device_state_attributes(self):\n         \"\"\"Return the state attributes of the vacuum cleaner.\"\"\"\n         data = {}\n \n", "comment": "Please also fix this old code and use `device_state_attributes` here instead. State attributes will be updated with `device_state_attributes` in the base entity.", "ids": [316416, "d631db4ea4dd2591a2b6b4e20bf804ac2fb5e78e", "077d4440943d695305149b195786a66477d2fea9"], "repo": "home-assistant-core", "ghid": 16035, "old": "         return self._clean_state\n     @property\n-    def state_attributes(self):\n         \"\"\"Return the state attributes of the vacuum cleaner.\"\"\"\n         data = {}", "new": "         return self._clean_state\n     @property\n+    def device_state_attributes(self):\n         \"\"\"Return the state attributes of the vacuum cleaner.\"\"\"\n         data = {}", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -147,17 +140,17 @@ def battery_level(self):\n         return self._state['details']['charge']\n \n     @property\n-    def status(self):\n+    def state(self):\n         \"\"\"Return the status of the vacuum cleaner.\"\"\"\n-        return self._status_state\n+        return self._clean_state\n \n     @property\n     def state_attributes(self):", "msg": "Please also fix this old code and use `device_state_attributes` here instead. State attributes will be updated with `device_state_attributes` in the base entity."}
{"old_hunk": "@@ -0,0 +1,107 @@\n+\"\"\"Config flow to configure Google Hangouts.\"\"\"\n+import voluptuous as vol\n+\n+from homeassistant import config_entries, data_entry_flow\n+from homeassistant.const import CONF_EMAIL, CONF_PASSWORD\n+from homeassistant.core import callback\n+\n+from .const import CONF_2FA, CONF_REFRESH_TOKEN\n+from .const import DOMAIN as HANGOUTS_DOMAIN\n+\n+\n+@callback\n+def configured_hangouts(hass):\n+    \"\"\"Return the configures Google Hangouts Account.\"\"\"\n+    entries = hass.config_entries.async_entries(HANGOUTS_DOMAIN)\n+    if entries:\n+        return entries[0]\n+    return None\n+\n+\n+@config_entries.HANDLERS.register(HANGOUTS_DOMAIN)\n+class HangoutsFlowHandler(data_entry_flow.FlowHandler):\n+    \"\"\"Config flow Google Hangouts.\"\"\"\n+\n+    VERSION = 1\n+\n+    def __init__(self):\n+        \"\"\"Initialize Google Hangouts config flow.\"\"\"\n+        self._credentials = None\n+        self._refresh_token = None\n+\n+    async def async_step_user(self, user_input=None):\n+        \"\"\"Handle a flow start.\"\"\"\n+        errors = {}\n+\n+        if configured_hangouts(self.hass) is not None:\n+            return self.async_abort(reason=\"already_configured\")\n+\n+        if user_input is not None:\n+            from hangups import get_auth\n+            from .hangups_utils import (HangoutsCredentials,\n+                                        HangoutsRefreshToken,\n+                                        GoogleAuthError, Google2FAError)\n+            self._credentials = HangoutsCredentials(user_input[CONF_EMAIL],\n+                                                    user_input[CONF_PASSWORD])\n+            self._refresh_token = HangoutsRefreshToken(None)\n+            try:\n+                await self.hass.async_add_executor_job(get_auth,\n+                                                       self._credentials,\n+                                                       self._refresh_token)\n+\n+                return await self.async_step_final()\n+            except GoogleAuthError as err:\n+                if err.__class__ is Google2FAError:", "hunk": "@@ -51,7 +51,7 @@ class HangoutsFlowHandler(data_entry_flow.FlowHandler):\n \n                 return await self.async_step_final()\n             except GoogleAuthError as err:\n-                if err.__class__ is Google2FAError:\n+                if isinstance(err, Google2FAError):\n                     return await self.async_step_2fa()\n                 msg = str(err)\n                 if msg == 'Unknown verification code input':\n", "comment": "`if isinstance(err, Google2FAError):`", "ids": [316470, "26337dc77d92e7b9d1957f9431c8345c26212f52", "42cafef5eb4def18458c428cffec0408092a76af"], "repo": "home-assistant-core", "ghid": 16049, "old": "                 return await self.async_step_final()\n             except GoogleAuthError as err:\n-                if err.__class__ is Google2FAError:\n                     return await self.async_step_2fa()\n                 msg = str(err)\n                 if msg == 'Unknown verification code input':", "new": "                 return await self.async_step_final()\n             except GoogleAuthError as err:\n+                if isinstance(err, Google2FAError):\n                     return await self.async_step_2fa()\n                 msg = str(err)\n                 if msg == 'Unknown verification code input':", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -0,0 +1,107 @@\n+\"\"\"Config flow to configure Google Hangouts.\"\"\"\n+import voluptuous as vol\n+\n+from homeassistant import config_entries, data_entry_flow\n+from homeassistant.const import CONF_EMAIL, CONF_PASSWORD\n+from homeassistant.core import callback\n+\n+from .const import CONF_2FA, CONF_REFRESH_TOKEN\n+from .const import DOMAIN as HANGOUTS_DOMAIN\n+\n+\n+@callback\n+def configured_hangouts(hass):\n+    \"\"\"Return the configures Google Hangouts Account.\"\"\"\n+    entries = hass.config_entries.async_entries(HANGOUTS_DOMAIN)\n+    if entries:\n+        return entries[0]\n+    return None\n+\n+\n+@config_entries.HANDLERS.register(HANGOUTS_DOMAIN)\n+class HangoutsFlowHandler(data_entry_flow.FlowHandler):\n+    \"\"\"Config flow Google Hangouts.\"\"\"\n+\n+    VERSION = 1\n+\n+    def __init__(self):\n+        \"\"\"Initialize Google Hangouts config flow.\"\"\"\n+        self._credentials = None\n+        self._refresh_token = None\n+\n+    async def async_step_user(self, user_input=None):\n+        \"\"\"Handle a flow start.\"\"\"\n+        errors = {}\n+\n+        if configured_hangouts(self.hass) is not None:\n+            return self.async_abort(reason=\"already_configured\")\n+\n+        if user_input is not None:\n+            from hangups import get_auth\n+            from .hangups_utils import (HangoutsCredentials,\n+                                        HangoutsRefreshToken,\n+                                        GoogleAuthError, Google2FAError)\n+            self._credentials = HangoutsCredentials(user_input[CONF_EMAIL],\n+                                                    user_input[CONF_PASSWORD])\n+            self._refresh_token = HangoutsRefreshToken(None)\n+            try:\n+                await self.hass.async_add_executor_job(get_auth,\n+                                                       self._credentials,\n+                                                       self._refresh_token)\n+\n+                return await self.async_step_final()\n+            except GoogleAuthError as err:\n+                if err.__class__ is Google2FAError:", "msg": "`if isinstance(err, Google2FAError):`"}
{"old_hunk": "@@ -0,0 +1,76 @@\n+\"\"\"\n+Hangouts notification service.\n+\n+For more details about this platform, please refer to the documentation at\n+https://home-assistant.io/components/notify.hangouts/\n+\"\"\"\n+import logging\n+\n+import voluptuous as vol\n+\n+import homeassistant.helpers.config_validation as cv\n+from homeassistant.components.notify import (ATTR_TARGET, PLATFORM_SCHEMA,\n+                                             NOTIFY_SERVICE_SCHEMA,\n+                                             BaseNotificationService,\n+                                             ATTR_MESSAGE)\n+\n+_LOGGER = logging.getLogger(__name__)\n+\n+CONF_DEFAULT_CONVERSATIONS = 'default_conversations'\n+CONF_CONVERSATION_ID = 'id'\n+CONF_CONVERSATION_NAME = 'name'\n+\n+DOMAIN = 'hangouts'\n+DEPENDENCIES = [DOMAIN]\n+\n+TARGETS_SCHEMA = vol.All(\n+    vol.Schema({\n+        vol.Exclusive(CONF_CONVERSATION_ID, 'id'): cv.string,\n+        vol.Exclusive(CONF_CONVERSATION_NAME, 'id'): cv.string", "hunk": "@@ -25,8 +25,8 @@ DEPENDENCIES = [DOMAIN]\n \n TARGETS_SCHEMA = vol.All(\n     vol.Schema({\n-        vol.Exclusive(CONF_CONVERSATION_ID, 'id'): cv.string,\n-        vol.Exclusive(CONF_CONVERSATION_NAME, 'id'): cv.string\n+        vol.Exclusive(CONF_CONVERSATION_ID, 'id or name'): cv.string,\n+        vol.Exclusive(CONF_CONVERSATION_NAME, 'id or name'): cv.string\n     }),\n     cv.has_at_least_one_key(CONF_CONVERSATION_ID, CONF_CONVERSATION_NAME)\n )\n", "comment": "Is 'id' the correct 'group of exclusion'?", "ids": [316432, "bcad6b0eeefc9b28a21e4fe9e55b76be0b41945b", "7fad198496e34c2b037fc6c7b99557e796aa3008"], "repo": "home-assistant-core", "ghid": 16049, "old": " TARGETS_SCHEMA = vol.All(\n     vol.Schema({\n-        vol.Exclusive(CONF_CONVERSATION_ID, 'id'): cv.string,\n-        vol.Exclusive(CONF_CONVERSATION_NAME, 'id'): cv.string\n     }),\n     cv.has_at_least_one_key(CONF_CONVERSATION_ID, CONF_CONVERSATION_NAME)\n )", "new": " TARGETS_SCHEMA = vol.All(\n     vol.Schema({\n+        vol.Exclusive(CONF_CONVERSATION_ID, 'id or name'): cv.string,\n+        vol.Exclusive(CONF_CONVERSATION_NAME, 'id or name'): cv.string\n     }),\n     cv.has_at_least_one_key(CONF_CONVERSATION_ID, CONF_CONVERSATION_NAME)\n )", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -0,0 +1,76 @@\n+\"\"\"\n+Hangouts notification service.\n+\n+For more details about this platform, please refer to the documentation at\n+https://home-assistant.io/components/notify.hangouts/\n+\"\"\"\n+import logging\n+\n+import voluptuous as vol\n+\n+import homeassistant.helpers.config_validation as cv\n+from homeassistant.components.notify import (ATTR_TARGET, PLATFORM_SCHEMA,\n+                                             NOTIFY_SERVICE_SCHEMA,\n+                                             BaseNotificationService,\n+                                             ATTR_MESSAGE)\n+\n+_LOGGER = logging.getLogger(__name__)\n+\n+CONF_DEFAULT_CONVERSATIONS = 'default_conversations'\n+CONF_CONVERSATION_ID = 'id'\n+CONF_CONVERSATION_NAME = 'name'\n+\n+DOMAIN = 'hangouts'\n+DEPENDENCIES = [DOMAIN]\n+\n+TARGETS_SCHEMA = vol.All(\n+    vol.Schema({\n+        vol.Exclusive(CONF_CONVERSATION_ID, 'id'): cv.string,\n+        vol.Exclusive(CONF_CONVERSATION_NAME, 'id'): cv.string", "msg": "Is 'id' the correct 'group of exclusion'?"}
{"old_hunk": "@@ -0,0 +1,86 @@\n+\"\"\"\n+The hangouts bot component.\n+\n+For more details about this platform, please refer to the documentation at\n+https://home-assistant.io/components/matrix/\n+\"\"\"\n+import logging\n+\n+from homeassistant import config_entries\n+from homeassistant.const import EVENT_HOMEASSISTANT_STOP\n+\n+from .const import (DOMAIN,", "hunk": "@@ -2,7 +2,7 @@\n The hangouts bot component.\n \n For more details about this platform, please refer to the documentation at\n-https://home-assistant.io/components/matrix/\n+https://home-assistant.io/components/hangouts/\n \"\"\"\n import logging\n \n", "comment": "'.const.CONF_CONVERSATIONS' imported but unused<br>'.const.CONF_WORD' imported but unused<br>'.const.CONF_EXPRESSION' imported but unused<br>'.const.CONFIG_SCHEMA' imported but unused<br>'.const.EVENT_HANGOUTS_COMMAND' imported but unused<br>'.const.EVENT_HANGOUTS_USERS_CHANGED' imported but unused", "ids": [316447, "6e42cccc808e81194c436aefb6b9cc737aaee910", "bc70c48f30094f7d21780a2e011744dcd0325fb2"], "repo": "home-assistant-core", "ghid": 16049, "old": " The hangouts bot component.\n For more details about this platform, please refer to the documentation at\n-https://home-assistant.io/components/matrix/\n \"\"\"\n import logging", "new": " The hangouts bot component.\n For more details about this platform, please refer to the documentation at\n+https://home-assistant.io/components/hangouts/\n \"\"\"\n import logging", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -0,0 +1,86 @@\n+\"\"\"\n+The hangouts bot component.\n+\n+For more details about this platform, please refer to the documentation at\n+https://home-assistant.io/components/matrix/\n+\"\"\"\n+import logging\n+\n+from homeassistant import config_entries\n+from homeassistant.const import EVENT_HOMEASSISTANT_STOP\n+\n+from .const import (DOMAIN,", "msg": "'.const.CONF_CONVERSATIONS' imported but unused<br>'.const.CONF_WORD' imported but unused<br>'.const.CONF_EXPRESSION' imported but unused<br>'.const.CONFIG_SCHEMA' imported but unused<br>'.const.EVENT_HANGOUTS_COMMAND' imported but unused<br>'.const.EVENT_HANGOUTS_USERS_CHANGED' imported but unused"}
{"old_hunk": "@@ -1,23 +1,34 @@\n \"\"\"Schema migration helpers.\"\"\"\n import logging\n+import os\n \n from .util import session_scope\n \n _LOGGER = logging.getLogger(__name__)\n+PROGRESS_FILE = '.migration_progress'\n \n \n def migrate_schema(instance):\n     \"\"\"Check if the schema needs to be upgraded.\"\"\"\n     from .models import SchemaChanges, SCHEMA_VERSION\n \n+    progress_path = instance.hass.config.path(PROGRESS_FILE)\n+\n     with session_scope(session=instance.get_session()) as session:\n         res = session.query(SchemaChanges).order_by(\n             SchemaChanges.change_id.desc()).first()\n         current_version = getattr(res, 'schema_version', None)\n \n         if current_version == SCHEMA_VERSION:\n+            # Clean up if old migration left file\n+            if os.path.isfile(progress_path):\n+                _LOGGER.warning(\"Found existing migration file, cleaning up\")\n+                os.remove(instance.hass.config.path(PROGRESS_FILE))\n             return\n \n+        with open(progress_path, 'w'):\n+            pass\n+\n         _LOGGER.debug(\"Database requires upgrade. Schema version: %s\",", "hunk": "@@ -29,8 +29,8 @@ def migrate_schema(instance):\n         with open(progress_path, 'w'):\n             pass\n \n-        _LOGGER.debug(\"Database requires upgrade. Schema version: %s\",\n-                      current_version)\n+        _LOGGER.warning(\"Database requires upgrade. Schema version: %s\",\n+                        current_version)\n \n         if current_version is None:\n             current_version = _inspect_schema_version(instance.engine, session)\n", "comment": "Can we change this message to warning?", "ids": [316535, "f5805f92e93fe9dafd9e264cd09a8fafc7392ae2", "9a6714a2dc1b02884f4b8eb9f5c6f43f6932b344"], "repo": "home-assistant-core", "ghid": 16061, "old": "         with open(progress_path, 'w'):\n             pass\n-        _LOGGER.debug(\"Database requires upgrade. Schema version: %s\",\n-                      current_version)\n         if current_version is None:\n             current_version = _inspect_schema_version(instance.engine, session)", "new": "         with open(progress_path, 'w'):\n             pass\n+        _LOGGER.warning(\"Database requires upgrade. Schema version: %s\",\n+                        current_version)\n         if current_version is None:\n             current_version = _inspect_schema_version(instance.engine, session)", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -1,23 +1,34 @@\n \"\"\"Schema migration helpers.\"\"\"\n import logging\n+import os\n \n from .util import session_scope\n \n _LOGGER = logging.getLogger(__name__)\n+PROGRESS_FILE = '.migration_progress'\n \n \n def migrate_schema(instance):\n     \"\"\"Check if the schema needs to be upgraded.\"\"\"\n     from .models import SchemaChanges, SCHEMA_VERSION\n \n+    progress_path = instance.hass.config.path(PROGRESS_FILE)\n+\n     with session_scope(session=instance.get_session()) as session:\n         res = session.query(SchemaChanges).order_by(\n             SchemaChanges.change_id.desc()).first()\n         current_version = getattr(res, 'schema_version', None)\n \n         if current_version == SCHEMA_VERSION:\n+            # Clean up if old migration left file\n+            if os.path.isfile(progress_path):\n+                _LOGGER.warning(\"Found existing migration file, cleaning up\")\n+                os.remove(instance.hass.config.path(PROGRESS_FILE))\n             return\n \n+        with open(progress_path, 'w'):\n+            pass\n+\n         _LOGGER.debug(\"Database requires upgrade. Schema version: %s\",", "msg": "Can we change this message to warning?"}
{"old_hunk": "@@ -1,23 +1,33 @@\n \"\"\"Schema migration helpers.\"\"\"\n import logging\n+import os\n \n from .util import session_scope\n \n _LOGGER = logging.getLogger(__name__)\n+PROGRESS_FILE = '.migration_progress'\n \n \n def migrate_schema(instance):\n     \"\"\"Check if the schema needs to be upgraded.\"\"\"\n     from .models import SchemaChanges, SCHEMA_VERSION\n \n+    progress_path = instance.hass.config.path(PROGRESS_FILE)\n+\n     with session_scope(session=instance.get_session()) as session:\n         res = session.query(SchemaChanges).order_by(\n             SchemaChanges.change_id.desc()).first()\n         current_version = getattr(res, 'schema_version', None)\n \n         if current_version == SCHEMA_VERSION:\n+            # Clean up if old migration left file\n+            if os.path.isfile(progress_path):\n+                os.remove(instance.hass.config.path(PROGRESS_FILE))", "hunk": "@@ -22,6 +22,7 @@ def migrate_schema(instance):\n         if current_version == SCHEMA_VERSION:\n             # Clean up if old migration left file\n             if os.path.isfile(progress_path):\n+                _LOGGER.warning(\"Found existing migration file, cleaning up\")\n                 os.remove(instance.hass.config.path(PROGRESS_FILE))\n             return\n \n", "comment": "we should add a warning", "ids": [316534, "457fc0c7c198cf91f7bf7312749ad289b47653d2", "f5805f92e93fe9dafd9e264cd09a8fafc7392ae2"], "repo": "home-assistant-core", "ghid": 16061, "old": "         if current_version == SCHEMA_VERSION:\n             # Clean up if old migration left file\n             if os.path.isfile(progress_path):\n                 os.remove(instance.hass.config.path(PROGRESS_FILE))\n             return", "new": "         if current_version == SCHEMA_VERSION:\n             # Clean up if old migration left file\n             if os.path.isfile(progress_path):\n+                _LOGGER.warning(\"Found existing migration file, cleaning up\")\n                 os.remove(instance.hass.config.path(PROGRESS_FILE))\n             return", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -1,23 +1,33 @@\n \"\"\"Schema migration helpers.\"\"\"\n import logging\n+import os\n \n from .util import session_scope\n \n _LOGGER = logging.getLogger(__name__)\n+PROGRESS_FILE = '.migration_progress'\n \n \n def migrate_schema(instance):\n     \"\"\"Check if the schema needs to be upgraded.\"\"\"\n     from .models import SchemaChanges, SCHEMA_VERSION\n \n+    progress_path = instance.hass.config.path(PROGRESS_FILE)\n+\n     with session_scope(session=instance.get_session()) as session:\n         res = session.query(SchemaChanges).order_by(\n             SchemaChanges.change_id.desc()).first()\n         current_version = getattr(res, 'schema_version', None)\n \n         if current_version == SCHEMA_VERSION:\n+            # Clean up if old migration left file\n+            if os.path.isfile(progress_path):\n+                os.remove(instance.hass.config.path(PROGRESS_FILE))", "msg": "we should add a warning"}
{"old_hunk": "@@ -758,3 +763,48 @@ def async_notify_setup_error(\n \n     persistent_notification.async_create(\n         hass, message, 'Invalid config', 'invalid_config')\n+\n+\n+def _secure_git(hass: HomeAssistant) -> None:\n+    \"\"\"Make sure the git repo has Home Assistant security features enabled.\"\"\"\n+    ignore_path = hass.config.path('.gitignore')\n+\n+    try:\n+        with open(ignore_path, 'rt') as file:", "hunk": "@@ -775,10 +775,7 @@ def _secure_git(hass: HomeAssistant) -> None:\n     except FileNotFoundError:\n         ignore_entries = set()\n \n-    to_add = [entry for entry in (\n-        'secrets.yaml',\n-        '.storage',\n-    ) if entry not in ignore_entries]\n+    to_add = [entry for entry in GIT_IGNORE if entry not in ignore_entries]\n \n     if not to_add:\n         return\n", "comment": "Maybe not use the reserved word `file`?", "ids": [316554, "09d818bba2ccf959d486382276d496d621f6bd39", "6bc02d5c9bab8ba79368ca6df266d39045d92f23"], "repo": "home-assistant-core", "ghid": 16073, "old": "     except FileNotFoundError:\n         ignore_entries = set()\n-    to_add = [entry for entry in (\n-        'secrets.yaml',\n-        '.storage',\n-    ) if entry not in ignore_entries]\n     if not to_add:\n         return", "new": "     except FileNotFoundError:\n         ignore_entries = set()\n+    to_add = [entry for entry in GIT_IGNORE if entry not in ignore_entries]\n     if not to_add:\n         return", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -758,3 +763,48 @@ def async_notify_setup_error(\n \n     persistent_notification.async_create(\n         hass, message, 'Invalid config', 'invalid_config')\n+\n+\n+def _secure_git(hass: HomeAssistant) -> None:\n+    \"\"\"Make sure the git repo has Home Assistant security features enabled.\"\"\"\n+    ignore_path = hass.config.path('.gitignore')\n+\n+    try:\n+        with open(ignore_path, 'rt') as file:", "msg": "Maybe not use the reserved word `file`?"}
{"old_hunk": "@@ -51,14 +49,15 @@\n     vol.Optional(CONF_NAME, default=DEFAULT_NAME): cv.string,\n     vol.Optional(CONF_MEDIAN, default=DEFAULT_MEDIAN): cv.positive_int,\n     vol.Optional(CONF_FORCE_UPDATE, default=DEFAULT_FORCE_UPDATE): cv.boolean,\n-    vol.Optional(CONF_TIMEOUT, default=DEFAULT_TIMEOUT): cv.positive_int,\n-    vol.Optional(CONF_RETRIES, default=DEFAULT_RETRIES): cv.positive_int,\n-    vol.Optional(CONF_CACHE, default=DEFAULT_UPDATE_INTERVAL): cv.positive_int,\n     vol.Optional(CONF_ADAPTER, default=DEFAULT_ADAPTER): cv.string,\n+    vol.Optional(CONF_SCAN_INTERVAL, default=DEFAULT_SCAN_INTERVAL):\n+        cv.time_period,\n })\n \n \n-def setup_platform(hass, config, add_entities, discovery_info=None):\n+@asyncio.coroutine", "hunk": "@@ -55,8 +55,7 @@ PLATFORM_SCHEMA = PLATFORM_SCHEMA.extend({\n })\n \n \n-@asyncio.coroutine\n-def async_setup_platform(hass, config, async_add_entities,\n+async def async_setup_platform(hass, config, async_add_entities,\n                          discovery_info=None):\n     \"\"\"Set up the MiFlora sensor.\"\"\"\n     from miflora import miflora_poller\n", "comment": "Use Python 3.5 async syntax,`async def`, instead of the coroutine decorator.", "ids": [316571, "7ce3cea7f40a70d410ded6f1793abe296944610d", "d8b44e2c563c447c8d28088ccbd4950f2d12f800"], "repo": "home-assistant-core", "ghid": 16085, "old": " })\n-@asyncio.coroutine\n-def async_setup_platform(hass, config, async_add_entities,\n                          discovery_info=None):\n     \"\"\"Set up the MiFlora sensor.\"\"\"\n     from miflora import miflora_poller", "new": " })\n+async def async_setup_platform(hass, config, async_add_entities,\n                          discovery_info=None):\n     \"\"\"Set up the MiFlora sensor.\"\"\"\n     from miflora import miflora_poller", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -51,14 +49,15 @@\n     vol.Optional(CONF_NAME, default=DEFAULT_NAME): cv.string,\n     vol.Optional(CONF_MEDIAN, default=DEFAULT_MEDIAN): cv.positive_int,\n     vol.Optional(CONF_FORCE_UPDATE, default=DEFAULT_FORCE_UPDATE): cv.boolean,\n-    vol.Optional(CONF_TIMEOUT, default=DEFAULT_TIMEOUT): cv.positive_int,\n-    vol.Optional(CONF_RETRIES, default=DEFAULT_RETRIES): cv.positive_int,\n-    vol.Optional(CONF_CACHE, default=DEFAULT_UPDATE_INTERVAL): cv.positive_int,\n     vol.Optional(CONF_ADAPTER, default=DEFAULT_ADAPTER): cv.string,\n+    vol.Optional(CONF_SCAN_INTERVAL, default=DEFAULT_SCAN_INTERVAL):\n+        cv.time_period,\n })\n \n \n-def setup_platform(hass, config, add_entities, discovery_info=None):\n+@asyncio.coroutine", "msg": "Use Python 3.5 async syntax,`async def`, instead of the coroutine decorator."}
{"old_hunk": "@@ -42,6 +43,7 @@\n         vol.Optional(CONF_CONFIG): dict,\n         vol.Optional(CONF_WEBCOMPONENT_PATH): cv.isfile,\n         vol.Optional(CONF_JS_URL): cv.string,\n+        vol.Optional(CONF_JS_IS_MODULE): cv.boolean,", "hunk": "@@ -43,7 +43,7 @@ CONFIG_SCHEMA = vol.Schema({\n         vol.Optional(CONF_CONFIG): dict,\n         vol.Optional(CONF_WEBCOMPONENT_PATH): cv.isfile,\n         vol.Optional(CONF_JS_URL): cv.string,\n-        vol.Optional(CONF_JS_IS_MODULE): cv.boolean,\n+        vol.Optional(CONF_MODULE_URL): cv.string,\n         vol.Optional(CONF_EMBED_IFRAME,\n                      default=DEFAULT_EMBED_IFRAME): cv.boolean,\n         vol.Optional(CONF_TRUST_EXTERNAL_SCRIPT,\n", "comment": "I am fine with the functionality but I am not a big fan of this config option. I would prefer if you add a new option ` CONF_MODULE_URL` instead.", "ids": [316601, "f3a46f4754916ef8a406a50c35a9e07327833b9d", "54292c5cd3411ff3836cc20d77e141dc8a35cb84"], "repo": "home-assistant-core", "ghid": 16096, "old": "         vol.Optional(CONF_CONFIG): dict,\n         vol.Optional(CONF_WEBCOMPONENT_PATH): cv.isfile,\n         vol.Optional(CONF_JS_URL): cv.string,\n-        vol.Optional(CONF_JS_IS_MODULE): cv.boolean,\n         vol.Optional(CONF_EMBED_IFRAME,\n                      default=DEFAULT_EMBED_IFRAME): cv.boolean,\n         vol.Optional(CONF_TRUST_EXTERNAL_SCRIPT,", "new": "         vol.Optional(CONF_CONFIG): dict,\n         vol.Optional(CONF_WEBCOMPONENT_PATH): cv.isfile,\n         vol.Optional(CONF_JS_URL): cv.string,\n+        vol.Optional(CONF_MODULE_URL): cv.string,\n         vol.Optional(CONF_EMBED_IFRAME,\n                      default=DEFAULT_EMBED_IFRAME): cv.boolean,\n         vol.Optional(CONF_TRUST_EXTERNAL_SCRIPT,", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -42,6 +43,7 @@\n         vol.Optional(CONF_CONFIG): dict,\n         vol.Optional(CONF_WEBCOMPONENT_PATH): cv.isfile,\n         vol.Optional(CONF_JS_URL): cv.string,\n+        vol.Optional(CONF_JS_IS_MODULE): cv.boolean,", "msg": "I am fine with the functionality but I am not a big fan of this config option. I would prefer if you add a new option ` CONF_MODULE_URL` instead."}
{"old_hunk": "@@ -256,20 +256,24 @@ def async_create_access_token(self,\n         return await auth_provider.async_credential_flow(context)\n \n     async def _async_finish_login_flow(\n-            self, context: Optional[Dict], result: Dict[str, Any]) \\\n-            -> Optional[Union[models.User, models.Credentials]]:\n+            self, flow: data_entry_flow.FlowHandler, result: Dict[str, Any]) \\\n+            -> Dict[str, Any]:\n         \"\"\"Return a user as result of login flow.\"\"\"\n         if result['type'] != data_entry_flow.RESULT_TYPE_CREATE_ENTRY:\n-            return None\n+            result.pop('result', None)", "hunk": "@@ -260,7 +260,6 @@ class AuthManager:\n             -> Dict[str, Any]:\n         \"\"\"Return a user as result of login flow.\"\"\"\n         if result['type'] != data_entry_flow.RESULT_TYPE_CREATE_ENTRY:\n-            result.pop('result', None)\n             return result\n \n         auth_provider = self._providers[result['handler']]\n", "comment": "There can't be a `result` entry in the ABORT or SHOW_FORM results?", "ids": [316612, "93bfffcc4aae485ff8145d08252d36ed1773221e", "1e236cbeebc9bc034de058ed9fa03ba89bc1b9c8"], "repo": "home-assistant-core", "ghid": 16100, "old": "             -> Dict[str, Any]:\n         \"\"\"Return a user as result of login flow.\"\"\"\n         if result['type'] != data_entry_flow.RESULT_TYPE_CREATE_ENTRY:\n-            result.pop('result', None)\n             return result\n         auth_provider = self._providers[result['handler']]", "new": "             -> Dict[str, Any]:\n         \"\"\"Return a user as result of login flow.\"\"\"\n         if result['type'] != data_entry_flow.RESULT_TYPE_CREATE_ENTRY:\n             return result\n         auth_provider = self._providers[result['handler']]", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -256,20 +256,24 @@ def async_create_access_token(self,\n         return await auth_provider.async_credential_flow(context)\n \n     async def _async_finish_login_flow(\n-            self, context: Optional[Dict], result: Dict[str, Any]) \\\n-            -> Optional[Union[models.User, models.Credentials]]:\n+            self, flow: data_entry_flow.FlowHandler, result: Dict[str, Any]) \\\n+            -> Dict[str, Any]:\n         \"\"\"Return a user as result of login flow.\"\"\"\n         if result['type'] != data_entry_flow.RESULT_TYPE_CREATE_ENTRY:\n-            return None\n+            result.pop('result', None)", "msg": "There can't be a `result` entry in the ABORT or SHOW_FORM results?"}
{"old_hunk": "@@ -0,0 +1,20 @@\n+\"\"\"\n+Support for INSTEON PowerLinc Modem.\n+\n+For more details about this component, please refer to the documentation at\n+https://home-assistant.io/components/insteon_plm/\n+\"\"\"\n+import asyncio\n+import logging\n+\n+_LOGGER = logging.getLogger(__name__)\n+\n+\n+@asyncio.coroutine\n+def async_setup(hass, config):\n+    \"\"\"Setup the insteon_plm component.\"\"\"\n+    _LOGGER.warning('The insteon_plm comonent has been replaced by '", "hunk": "@@ -17,4 +17,10 @@ def async_setup(hass, config):\n                     'the insteon component')\n     _LOGGER.warning('Please see https://home-assistant.io/components/insteon')\n \n+    hass.components.persistent_notification.create(\n+    'The insteon_plm has been replaced by the insteon component.<br />'\n+    'Please see https://home-assistant.io/components/insteon',\n+    title='insteon_local Component Deactivated',\n+    notification_id='insteon_plm')\n+\n     return False\n", "comment": "You could make it a persistent notification for more visibility.", "ids": [316620, "6fef39b78b744c0432954bc5ae06f5282ace849d", "03fd4f40b4bfcbbaf4433b070965782ccfd1ead1"], "repo": "home-assistant-core", "ghid": 16102, "old": "                     'the insteon component')\n     _LOGGER.warning('Please see https://home-assistant.io/components/insteon')\n     return False", "new": "                     'the insteon component')\n     _LOGGER.warning('Please see https://home-assistant.io/components/insteon')\n+    hass.components.persistent_notification.create(\n+    'The insteon_plm has been replaced by the insteon component.<br />'\n+    'Please see https://home-assistant.io/components/insteon',\n+    title='insteon_local Component Deactivated',\n+    notification_id='insteon_plm')\n+\n     return False", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -0,0 +1,20 @@\n+\"\"\"\n+Support for INSTEON PowerLinc Modem.\n+\n+For more details about this component, please refer to the documentation at\n+https://home-assistant.io/components/insteon_plm/\n+\"\"\"\n+import asyncio\n+import logging\n+\n+_LOGGER = logging.getLogger(__name__)\n+\n+\n+@asyncio.coroutine\n+def async_setup(hass, config):\n+    \"\"\"Setup the insteon_plm component.\"\"\"\n+    _LOGGER.warning('The insteon_plm comonent has been replaced by '", "msg": "You could make it a persistent notification for more visibility."}
{"old_hunk": "@@ -80,10 +81,12 @@ def async_create_credentials(self, data: Dict[str, str]) -> Credentials:\n \n     # Implement by extending class\n \n-    async def async_credential_flow(\n-            self, context: Optional[Dict]) -> data_entry_flow.FlowHandler:\n-        \"\"\"Return the data flow for logging in with auth provider.\"\"\"\n-        raise NotImplementedError\n+    async def async_login_flow(self, context: Optional[Dict]) -> 'LoginFlow':\n+        \"\"\"Return the data flow for logging in with auth provider.\n+\n+         Auth provider should extend LoginFlow\n+        \"\"\"\n+        return LoginFlow(self)", "hunk": "@@ -84,9 +84,9 @@ class AuthProvider:\n     async def async_login_flow(self, context: Optional[Dict]) -> 'LoginFlow':\n         \"\"\"Return the data flow for logging in with auth provider.\n \n-         Auth provider should extend LoginFlow\n+        Auth provider should extend LoginFlow and return an instance.\n         \"\"\"\n-        return LoginFlow(self)\n+        raise NotImplementedError\n \n     async def async_get_or_create_credentials(\n             self, flow_result: Dict[str, str]) -> Credentials:\n", "comment": "This should remain `NotImplementedError` as `LoginFlow` is an abstract class?", "ids": [316639, "b839129029c5a50b1d4924d0b0012d36325ef234", "5778cdc4a2878307d9926fe6ad53fb29b9eb04dd"], "repo": "home-assistant-core", "ghid": 16104, "old": "     async def async_login_flow(self, context: Optional[Dict]) -> 'LoginFlow':\n         \"\"\"Return the data flow for logging in with auth provider.\n-         Auth provider should extend LoginFlow\n         \"\"\"\n-        return LoginFlow(self)\n     async def async_get_or_create_credentials(\n             self, flow_result: Dict[str, str]) -> Credentials:", "new": "     async def async_login_flow(self, context: Optional[Dict]) -> 'LoginFlow':\n         \"\"\"Return the data flow for logging in with auth provider.\n+        Auth provider should extend LoginFlow and return an instance.\n         \"\"\"\n+        raise NotImplementedError\n     async def async_get_or_create_credentials(\n             self, flow_result: Dict[str, str]) -> Credentials:", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -80,10 +81,12 @@ def async_create_credentials(self, data: Dict[str, str]) -> Credentials:\n \n     # Implement by extending class\n \n-    async def async_credential_flow(\n-            self, context: Optional[Dict]) -> data_entry_flow.FlowHandler:\n-        \"\"\"Return the data flow for logging in with auth provider.\"\"\"\n-        raise NotImplementedError\n+    async def async_login_flow(self, context: Optional[Dict]) -> 'LoginFlow':\n+        \"\"\"Return the data flow for logging in with auth provider.\n+\n+         Auth provider should extend LoginFlow\n+        \"\"\"\n+        return LoginFlow(self)", "msg": "This should remain `NotImplementedError` as `LoginFlow` is an abstract class?"}
{"old_hunk": "@@ -0,0 +1,47 @@\n+\"\"\"\n+Demo platform that has a couple of fake sensors.\n+\n+For more details about this platform, please refer to the documentation\n+https://home-assistant.io/components/demo/\n+\"\"\"\n+import logging\n+\n+from homeassistant.const import (\n+        TEMP_CELSIUS, DEVICE_CLASS_TEMPERATURE)\n+from homeassistant.components.velbus import (\n+        DOMAIN as VELBUS_DOMAIN, VelbusEntity)\n+\n+_LOGGER = logging.getLogger(__name__)\n+\n+DEPENDENCIES = ['velbus']\n+\n+\n+async def async_setup_platform(hass, config, async_add_devices,\n+                               discovery_info=None):\n+    \"\"\"Set up the Velbus temp sensor platform.\"\"\"\n+    if discovery_info is None:\n+        return\n+    sensors = []\n+    for sensor in discovery_info:\n+        module = hass.data[VELBUS_DOMAIN].get_module(sensor[0])\n+        channel = sensor[1]\n+        sensors.append(VelbusTempSensor(module, channel))\n+    async_add_devices(sensors, update_before_add=False)", "hunk": "@@ -10,7 +10,7 @@\n     <h1>Error 503 Backend is unhealthy</h1>\n     <p>Backend is unhealthy</p>\n     <h3>Guru Mediation:</h3>\n-    <p>Details: cache-sea4456-SEA 1645537488 1189437420</p>\n+    <p>Details: cache-sea4453-SEA 1645537488 716611415</p>\n     <hr>\n     <p>Varnish cache server</p>\n   </body>\n", "comment": "`False` is the default for `update_before_add`.", "ids": [317169, "d20c514f29f613ad29efa5efd78cccb7ddf1414f", "39cbe2bb38b66633353e57c5415615e69d4a89e2"], "repo": "home-assistant-core", "ghid": 16203, "old": "     <h1>Error 503 Backend is unhealthy</h1>\n     <p>Backend is unhealthy</p>\n     <h3>Guru Mediation:</h3>\n-    <p>Details: cache-sea4456-SEA 1645537488 1189437420</p>\n     <hr>\n     <p>Varnish cache server</p>\n   </body>", "new": "     <h1>Error 503 Backend is unhealthy</h1>\n     <p>Backend is unhealthy</p>\n     <h3>Guru Mediation:</h3>\n+    <p>Details: cache-sea4453-SEA 1645537488 716611415</p>\n     <hr>\n     <p>Varnish cache server</p>\n   </body>", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -0,0 +1,47 @@\n+\"\"\"\n+Demo platform that has a couple of fake sensors.\n+\n+For more details about this platform, please refer to the documentation\n+https://home-assistant.io/components/demo/\n+\"\"\"\n+import logging\n+\n+from homeassistant.const import (\n+        TEMP_CELSIUS, DEVICE_CLASS_TEMPERATURE)\n+from homeassistant.components.velbus import (\n+        DOMAIN as VELBUS_DOMAIN, VelbusEntity)\n+\n+_LOGGER = logging.getLogger(__name__)\n+\n+DEPENDENCIES = ['velbus']\n+\n+\n+async def async_setup_platform(hass, config, async_add_devices,\n+                               discovery_info=None):\n+    \"\"\"Set up the Velbus temp sensor platform.\"\"\"\n+    if discovery_info is None:\n+        return\n+    sensors = []\n+    for sensor in discovery_info:\n+        module = hass.data[VELBUS_DOMAIN].get_module(sensor[0])\n+        channel = sensor[1]\n+        sensors.append(VelbusTempSensor(module, channel))\n+    async_add_devices(sensors, update_before_add=False)", "msg": "`False` is the default for `update_before_add`."}
{"old_hunk": "@@ -0,0 +1,47 @@\n+\"\"\"\n+Velbus sensors.\n+\n+For more details about this platform, please refer to the documentation\n+https://home-assistant.io/components/velbus/", "hunk": "@@ -10,7 +10,7 @@\n     <h1>Error 503 Backend is unhealthy</h1>\n     <p>Backend is unhealthy</p>\n     <h3>Guru Mediation:</h3>\n-    <p>Details: cache-sea4453-SEA 1645537490 716624405</p>\n+    <p>Details: cache-sea4461-SEA 1645537490 4084106275</p>\n     <hr>\n     <p>Varnish cache server</p>\n   </body>\n", "comment": "Url doesn't have correct format. Look at other platforms for the correct format.", "ids": [317173, "39cbe2bb38b66633353e57c5415615e69d4a89e2", "dc09cbee0bb90716d9eb93d4fcd6495616db91b3"], "repo": "home-assistant-core", "ghid": 16203, "old": "     <h1>Error 503 Backend is unhealthy</h1>\n     <p>Backend is unhealthy</p>\n     <h3>Guru Mediation:</h3>\n-    <p>Details: cache-sea4453-SEA 1645537490 716624405</p>\n     <hr>\n     <p>Varnish cache server</p>\n   </body>", "new": "     <h1>Error 503 Backend is unhealthy</h1>\n     <p>Backend is unhealthy</p>\n     <h3>Guru Mediation:</h3>\n+    <p>Details: cache-sea4461-SEA 1645537490 4084106275</p>\n     <hr>\n     <p>Varnish cache server</p>\n   </body>", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -0,0 +1,47 @@\n+\"\"\"\n+Velbus sensors.\n+\n+For more details about this platform, please refer to the documentation\n+https://home-assistant.io/components/velbus/", "msg": "Url doesn't have correct format. Look at other platforms for the correct format."}
{"old_hunk": "@@ -4,71 +4,56 @@\n For more details about this platform, please refer to the documentation at\n https://home-assistant.io/components/alarm_control_panel.spc/\n \"\"\"\n-import asyncio\n import logging\n \n import homeassistant.components.alarm_control_panel as alarm\n from homeassistant.components.spc import (\n-    ATTR_DISCOVER_AREAS, DATA_API, DATA_REGISTRY, SpcWebGateway)\n+    ATTR_DISCOVER_AREAS, DATA_API, DATA_REGISTRY)\n from homeassistant.const import (\n-    STATE_ALARM_ARMED_AWAY, STATE_ALARM_ARMED_HOME, STATE_ALARM_DISARMED,\n-    STATE_UNKNOWN)\n+    STATE_ALARM_ARMED_AWAY, STATE_ALARM_ARMED_HOME, STATE_ALARM_ARMED_NIGHT,\n+    STATE_ALARM_DISARMED, STATE_ALARM_TRIGGERED, STATE_UNKNOWN)\n \n _LOGGER = logging.getLogger(__name__)\n \n-SPC_AREA_MODE_TO_STATE = {\n-    '0': STATE_ALARM_DISARMED,\n-    '1': STATE_ALARM_ARMED_HOME,\n-    '3': STATE_ALARM_ARMED_AWAY,\n-}\n \n-\n-def _get_alarm_state(spc_mode):\n+def _get_alarm_state(area):\n     \"\"\"Get the alarm state.\"\"\"\n-    return SPC_AREA_MODE_TO_STATE.get(spc_mode, STATE_UNKNOWN)\n+    from pyspcwebgw.const import AreaMode\n+\n+    if area.verified_alarm:\n+        return STATE_ALARM_TRIGGERED\n \n+    mode_to_state = {\n+        AreaMode.UNSET: STATE_ALARM_DISARMED,\n+        AreaMode.PART_SET_A: STATE_ALARM_ARMED_HOME,\n+        AreaMode.PART_SET_B: STATE_ALARM_ARMED_NIGHT,\n+        AreaMode.FULL_SET: STATE_ALARM_ARMED_AWAY,\n+    }\n+    return mode_to_state.get(area.mode, STATE_UNKNOWN)", "hunk": "@@ -10,7 +10,7 @@\n     <h1>Error 503 Backend is unhealthy</h1>\n     <p>Backend is unhealthy</p>\n     <h3>Guru Mediation:</h3>\n-    <p>Details: cache-sea4483-SEA 1645537495 655356299</p>\n+    <p>Details: cache-sea4451-SEA 1645537495 3437094610</p>\n     <hr>\n     <p>Varnish cache server</p>\n   </body>\n", "comment": "Don't use `STATE_UNKNOWN`, use `None`.", "ids": [317189, "dd3b4932c9c6d88ec01673f9c4d31a2c2e9ffb72", "9c586ed52e4405a81bbcff8e6819fd530dc54567"], "repo": "home-assistant-core", "ghid": 16214, "old": "     <h1>Error 503 Backend is unhealthy</h1>\n     <p>Backend is unhealthy</p>\n     <h3>Guru Mediation:</h3>\n-    <p>Details: cache-sea4483-SEA 1645537495 655356299</p>\n     <hr>\n     <p>Varnish cache server</p>\n   </body>", "new": "     <h1>Error 503 Backend is unhealthy</h1>\n     <p>Backend is unhealthy</p>\n     <h3>Guru Mediation:</h3>\n+    <p>Details: cache-sea4451-SEA 1645537495 3437094610</p>\n     <hr>\n     <p>Varnish cache server</p>\n   </body>", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -4,71 +4,56 @@\n For more details about this platform, please refer to the documentation at\n https://home-assistant.io/components/alarm_control_panel.spc/\n \"\"\"\n-import asyncio\n import logging\n \n import homeassistant.components.alarm_control_panel as alarm\n from homeassistant.components.spc import (\n-    ATTR_DISCOVER_AREAS, DATA_API, DATA_REGISTRY, SpcWebGateway)\n+    ATTR_DISCOVER_AREAS, DATA_API, DATA_REGISTRY)\n from homeassistant.const import (\n-    STATE_ALARM_ARMED_AWAY, STATE_ALARM_ARMED_HOME, STATE_ALARM_DISARMED,\n-    STATE_UNKNOWN)\n+    STATE_ALARM_ARMED_AWAY, STATE_ALARM_ARMED_HOME, STATE_ALARM_ARMED_NIGHT,\n+    STATE_ALARM_DISARMED, STATE_ALARM_TRIGGERED, STATE_UNKNOWN)\n \n _LOGGER = logging.getLogger(__name__)\n \n-SPC_AREA_MODE_TO_STATE = {\n-    '0': STATE_ALARM_DISARMED,\n-    '1': STATE_ALARM_ARMED_HOME,\n-    '3': STATE_ALARM_ARMED_AWAY,\n-}\n \n-\n-def _get_alarm_state(spc_mode):\n+def _get_alarm_state(area):\n     \"\"\"Get the alarm state.\"\"\"\n-    return SPC_AREA_MODE_TO_STATE.get(spc_mode, STATE_UNKNOWN)\n+    from pyspcwebgw.const import AreaMode\n+\n+    if area.verified_alarm:\n+        return STATE_ALARM_TRIGGERED\n \n+    mode_to_state = {\n+        AreaMode.UNSET: STATE_ALARM_DISARMED,\n+        AreaMode.PART_SET_A: STATE_ALARM_ARMED_HOME,\n+        AreaMode.PART_SET_B: STATE_ALARM_ARMED_NIGHT,\n+        AreaMode.FULL_SET: STATE_ALARM_ARMED_AWAY,\n+    }\n+    return mode_to_state.get(area.mode, STATE_UNKNOWN)", "msg": "Don't use `STATE_UNKNOWN`, use `None`."}
{"old_hunk": "@@ -4,71 +4,63 @@\n For more details about this platform, please refer to the documentation at\n https://home-assistant.io/components/alarm_control_panel.spc/\n \"\"\"\n-import asyncio\n import logging\n \n import homeassistant.components.alarm_control_panel as alarm\n+from homeassistant.helpers.dispatcher import async_dispatcher_connect\n from homeassistant.components.spc import (\n-    ATTR_DISCOVER_AREAS, DATA_API, DATA_REGISTRY, SpcWebGateway)\n+    ATTR_DISCOVER_AREAS, DATA_API, SIGNAL_UPDATE_ALARM)\n from homeassistant.const import (\n-    STATE_ALARM_ARMED_AWAY, STATE_ALARM_ARMED_HOME, STATE_ALARM_DISARMED,\n-    STATE_UNKNOWN)\n+    STATE_ALARM_ARMED_AWAY, STATE_ALARM_ARMED_HOME, STATE_ALARM_ARMED_NIGHT,\n+    STATE_ALARM_DISARMED, STATE_ALARM_TRIGGERED)\n \n _LOGGER = logging.getLogger(__name__)\n \n-SPC_AREA_MODE_TO_STATE = {\n-    '0': STATE_ALARM_DISARMED,\n-    '1': STATE_ALARM_ARMED_HOME,\n-    '3': STATE_ALARM_ARMED_AWAY,\n-}\n \n-\n-def _get_alarm_state(spc_mode):\n+def _get_alarm_state(area):\n     \"\"\"Get the alarm state.\"\"\"\n-    return SPC_AREA_MODE_TO_STATE.get(spc_mode, STATE_UNKNOWN)\n+    from pyspcwebgw.const import AreaMode\n+\n+    if area.verified_alarm:\n+        return STATE_ALARM_TRIGGERED\n+\n+    mode_to_state = {\n+        AreaMode.UNSET: STATE_ALARM_DISARMED,\n+        AreaMode.PART_SET_A: STATE_ALARM_ARMED_HOME,\n+        AreaMode.PART_SET_B: STATE_ALARM_ARMED_NIGHT,\n+        AreaMode.FULL_SET: STATE_ALARM_ARMED_AWAY,\n+    }\n+    return mode_to_state.get(area.mode, None)\n \n \n-@asyncio.coroutine\n-def async_setup_platform(hass, config, async_add_entities,\n-                         discovery_info=None):\n+async def async_setup_platform(hass, config, async_add_entities,\n+                               discovery_info=None):\n     \"\"\"Set up the SPC alarm control panel platform.\"\"\"\n     if (discovery_info is None or\n             discovery_info[ATTR_DISCOVER_AREAS] is None):\n         return\n \n-    api = hass.data[DATA_API]\n-    devices = [SpcAlarm(api, area)\n-               for area in discovery_info[ATTR_DISCOVER_AREAS]]\n-\n-    async_add_entities(devices)\n+    async_add_entities([SpcAlarm(area=area, api=hass.data[DATA_API])\n+                        for area in discovery_info[ATTR_DISCOVER_AREAS]])\n \n \n class SpcAlarm(alarm.AlarmControlPanel):\n     \"\"\"Representation of the SPC alarm panel.\"\"\"\n \n-    def __init__(self, api, area):\n+    def __init__(self, area, api):\n         \"\"\"Initialize the SPC alarm panel.\"\"\"\n-        self._area_id = area['id']\n-        self._name = area['name']\n-        self._state = _get_alarm_state(area['mode'])\n-        if self._state == STATE_ALARM_DISARMED:\n-            self._changed_by = area.get('last_unset_user_name', 'unknown')\n-        else:\n-            self._changed_by = area.get('last_set_user_name', 'unknown')\n+        self._area = area\n         self._api = api\n \n-    @asyncio.coroutine\n-    def async_added_to_hass(self):\n+    async def async_added_to_hass(self):\n         \"\"\"Call for adding new entities.\"\"\"\n-        self.hass.data[DATA_REGISTRY].register_alarm_device(\n-            self._area_id, self)\n+        async def async_update_callback():", "hunk": "@@ -10,7 +10,7 @@\n     <h1>Error 503 Backend is unhealthy</h1>\n     <p>Backend is unhealthy</p>\n     <h3>Guru Mediation:</h3>\n-    <p>Details: cache-sea4451-SEA 1645537499 3437124268</p>\n+    <p>Details: cache-sea4447-SEA 1645537499 2490126793</p>\n     <hr>\n     <p>Varnish cache server</p>\n   </body>\n", "comment": "Make this a callback instead of a coroutine by decorating the function with `@callback` imported from core.py.", "ids": [317195, "9c586ed52e4405a81bbcff8e6819fd530dc54567", "0359593db13929eed1feed0e9eea59cdebc67e5d"], "repo": "home-assistant-core", "ghid": 16214, "old": "     <h1>Error 503 Backend is unhealthy</h1>\n     <p>Backend is unhealthy</p>\n     <h3>Guru Mediation:</h3>\n-    <p>Details: cache-sea4451-SEA 1645537499 3437124268</p>\n     <hr>\n     <p>Varnish cache server</p>\n   </body>", "new": "     <h1>Error 503 Backend is unhealthy</h1>\n     <p>Backend is unhealthy</p>\n     <h3>Guru Mediation:</h3>\n+    <p>Details: cache-sea4447-SEA 1645537499 2490126793</p>\n     <hr>\n     <p>Varnish cache server</p>\n   </body>", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -4,71 +4,63 @@\n For more details about this platform, please refer to the documentation at\n https://home-assistant.io/components/alarm_control_panel.spc/\n \"\"\"\n-import asyncio\n import logging\n \n import homeassistant.components.alarm_control_panel as alarm\n+from homeassistant.helpers.dispatcher import async_dispatcher_connect\n from homeassistant.components.spc import (\n-    ATTR_DISCOVER_AREAS, DATA_API, DATA_REGISTRY, SpcWebGateway)\n+    ATTR_DISCOVER_AREAS, DATA_API, SIGNAL_UPDATE_ALARM)\n from homeassistant.const import (\n-    STATE_ALARM_ARMED_AWAY, STATE_ALARM_ARMED_HOME, STATE_ALARM_DISARMED,\n-    STATE_UNKNOWN)\n+    STATE_ALARM_ARMED_AWAY, STATE_ALARM_ARMED_HOME, STATE_ALARM_ARMED_NIGHT,\n+    STATE_ALARM_DISARMED, STATE_ALARM_TRIGGERED)\n \n _LOGGER = logging.getLogger(__name__)\n \n-SPC_AREA_MODE_TO_STATE = {\n-    '0': STATE_ALARM_DISARMED,\n-    '1': STATE_ALARM_ARMED_HOME,\n-    '3': STATE_ALARM_ARMED_AWAY,\n-}\n \n-\n-def _get_alarm_state(spc_mode):\n+def _get_alarm_state(area):\n     \"\"\"Get the alarm state.\"\"\"\n-    return SPC_AREA_MODE_TO_STATE.get(spc_mode, STATE_UNKNOWN)\n+    from pyspcwebgw.const import AreaMode\n+\n+    if area.verified_alarm:\n+        return STATE_ALARM_TRIGGERED\n+\n+    mode_to_state = {\n+        AreaMode.UNSET: STATE_ALARM_DISARMED,\n+        AreaMode.PART_SET_A: STATE_ALARM_ARMED_HOME,\n+        AreaMode.PART_SET_B: STATE_ALARM_ARMED_NIGHT,\n+        AreaMode.FULL_SET: STATE_ALARM_ARMED_AWAY,\n+    }\n+    return mode_to_state.get(area.mode, None)\n \n \n-@asyncio.coroutine\n-def async_setup_platform(hass, config, async_add_entities,\n-                         discovery_info=None):\n+async def async_setup_platform(hass, config, async_add_entities,\n+                               discovery_info=None):\n     \"\"\"Set up the SPC alarm control panel platform.\"\"\"\n     if (discovery_info is None or\n             discovery_info[ATTR_DISCOVER_AREAS] is None):\n         return\n \n-    api = hass.data[DATA_API]\n-    devices = [SpcAlarm(api, area)\n-               for area in discovery_info[ATTR_DISCOVER_AREAS]]\n-\n-    async_add_entities(devices)\n+    async_add_entities([SpcAlarm(area=area, api=hass.data[DATA_API])\n+                        for area in discovery_info[ATTR_DISCOVER_AREAS]])\n \n \n class SpcAlarm(alarm.AlarmControlPanel):\n     \"\"\"Representation of the SPC alarm panel.\"\"\"\n \n-    def __init__(self, api, area):\n+    def __init__(self, area, api):\n         \"\"\"Initialize the SPC alarm panel.\"\"\"\n-        self._area_id = area['id']\n-        self._name = area['name']\n-        self._state = _get_alarm_state(area['mode'])\n-        if self._state == STATE_ALARM_DISARMED:\n-            self._changed_by = area.get('last_unset_user_name', 'unknown')\n-        else:\n-            self._changed_by = area.get('last_set_user_name', 'unknown')\n+        self._area = area\n         self._api = api\n \n-    @asyncio.coroutine\n-    def async_added_to_hass(self):\n+    async def async_added_to_hass(self):\n         \"\"\"Call for adding new entities.\"\"\"\n-        self.hass.data[DATA_REGISTRY].register_alarm_device(\n-            self._area_id, self)\n+        async def async_update_callback():", "msg": "Make this a callback instead of a coroutine by decorating the function with `@callback` imported from core.py."}
{"old_hunk": "@@ -4,71 +4,63 @@\n For more details about this platform, please refer to the documentation at\n https://home-assistant.io/components/alarm_control_panel.spc/\n \"\"\"\n-import asyncio\n import logging\n \n import homeassistant.components.alarm_control_panel as alarm\n+from homeassistant.helpers.dispatcher import async_dispatcher_connect\n from homeassistant.components.spc import (\n-    ATTR_DISCOVER_AREAS, DATA_API, DATA_REGISTRY, SpcWebGateway)\n+    ATTR_DISCOVER_AREAS, DATA_API, SIGNAL_UPDATE_ALARM)\n from homeassistant.const import (\n-    STATE_ALARM_ARMED_AWAY, STATE_ALARM_ARMED_HOME, STATE_ALARM_DISARMED,\n-    STATE_UNKNOWN)\n+    STATE_ALARM_ARMED_AWAY, STATE_ALARM_ARMED_HOME, STATE_ALARM_ARMED_NIGHT,\n+    STATE_ALARM_DISARMED, STATE_ALARM_TRIGGERED)\n \n _LOGGER = logging.getLogger(__name__)\n \n-SPC_AREA_MODE_TO_STATE = {\n-    '0': STATE_ALARM_DISARMED,\n-    '1': STATE_ALARM_ARMED_HOME,\n-    '3': STATE_ALARM_ARMED_AWAY,\n-}\n \n-\n-def _get_alarm_state(spc_mode):\n+def _get_alarm_state(area):\n     \"\"\"Get the alarm state.\"\"\"\n-    return SPC_AREA_MODE_TO_STATE.get(spc_mode, STATE_UNKNOWN)\n+    from pyspcwebgw.const import AreaMode\n+\n+    if area.verified_alarm:\n+        return STATE_ALARM_TRIGGERED\n+\n+    mode_to_state = {\n+        AreaMode.UNSET: STATE_ALARM_DISARMED,\n+        AreaMode.PART_SET_A: STATE_ALARM_ARMED_HOME,\n+        AreaMode.PART_SET_B: STATE_ALARM_ARMED_NIGHT,\n+        AreaMode.FULL_SET: STATE_ALARM_ARMED_AWAY,\n+    }\n+    return mode_to_state.get(area.mode, None)", "hunk": "@@ -10,7 +10,7 @@\n     <h1>Error 503 Backend is unhealthy</h1>\n     <p>Backend is unhealthy</p>\n     <h3>Guru Mediation:</h3>\n-    <p>Details: cache-sea4451-SEA 1645537498 3437120907</p>\n+    <p>Details: cache-sea4447-SEA 1645537498 2490123297</p>\n     <hr>\n     <p>Varnish cache server</p>\n   </body>\n", "comment": "`None` is already the default value returned if the key is missing in the dict.", "ids": [317194, "9c586ed52e4405a81bbcff8e6819fd530dc54567", "0359593db13929eed1feed0e9eea59cdebc67e5d"], "repo": "home-assistant-core", "ghid": 16214, "old": "     <h1>Error 503 Backend is unhealthy</h1>\n     <p>Backend is unhealthy</p>\n     <h3>Guru Mediation:</h3>\n-    <p>Details: cache-sea4451-SEA 1645537498 3437120907</p>\n     <hr>\n     <p>Varnish cache server</p>\n   </body>", "new": "     <h1>Error 503 Backend is unhealthy</h1>\n     <p>Backend is unhealthy</p>\n     <h3>Guru Mediation:</h3>\n+    <p>Details: cache-sea4447-SEA 1645537498 2490123297</p>\n     <hr>\n     <p>Varnish cache server</p>\n   </body>", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -4,71 +4,63 @@\n For more details about this platform, please refer to the documentation at\n https://home-assistant.io/components/alarm_control_panel.spc/\n \"\"\"\n-import asyncio\n import logging\n \n import homeassistant.components.alarm_control_panel as alarm\n+from homeassistant.helpers.dispatcher import async_dispatcher_connect\n from homeassistant.components.spc import (\n-    ATTR_DISCOVER_AREAS, DATA_API, DATA_REGISTRY, SpcWebGateway)\n+    ATTR_DISCOVER_AREAS, DATA_API, SIGNAL_UPDATE_ALARM)\n from homeassistant.const import (\n-    STATE_ALARM_ARMED_AWAY, STATE_ALARM_ARMED_HOME, STATE_ALARM_DISARMED,\n-    STATE_UNKNOWN)\n+    STATE_ALARM_ARMED_AWAY, STATE_ALARM_ARMED_HOME, STATE_ALARM_ARMED_NIGHT,\n+    STATE_ALARM_DISARMED, STATE_ALARM_TRIGGERED)\n \n _LOGGER = logging.getLogger(__name__)\n \n-SPC_AREA_MODE_TO_STATE = {\n-    '0': STATE_ALARM_DISARMED,\n-    '1': STATE_ALARM_ARMED_HOME,\n-    '3': STATE_ALARM_ARMED_AWAY,\n-}\n \n-\n-def _get_alarm_state(spc_mode):\n+def _get_alarm_state(area):\n     \"\"\"Get the alarm state.\"\"\"\n-    return SPC_AREA_MODE_TO_STATE.get(spc_mode, STATE_UNKNOWN)\n+    from pyspcwebgw.const import AreaMode\n+\n+    if area.verified_alarm:\n+        return STATE_ALARM_TRIGGERED\n+\n+    mode_to_state = {\n+        AreaMode.UNSET: STATE_ALARM_DISARMED,\n+        AreaMode.PART_SET_A: STATE_ALARM_ARMED_HOME,\n+        AreaMode.PART_SET_B: STATE_ALARM_ARMED_NIGHT,\n+        AreaMode.FULL_SET: STATE_ALARM_ARMED_AWAY,\n+    }\n+    return mode_to_state.get(area.mode, None)", "msg": "`None` is already the default value returned if the key is missing in the dict."}
{"old_hunk": "@@ -42,244 +36,43 @@\n }, extra=vol.ALLOW_EXTRA)\n \n \n-@asyncio.coroutine\n-def async_setup(hass, config):\n-    \"\"\"Set up the SPC platform.\"\"\"\n-    hass.data[DATA_REGISTRY] = SpcRegistry()\n-\n-    api = SpcWebGateway(hass,\n-                        config[DOMAIN].get(CONF_API_URL),\n-                        config[DOMAIN].get(CONF_WS_URL))\n-\n-    hass.data[DATA_API] = api\n-\n-    # add sensor devices for each zone (typically motion/fire/door sensors)\n-    zones = yield from api.get_zones()\n-    if zones:\n-        hass.async_create_task(discovery.async_load_platform(\n-            hass, 'binary_sensor', DOMAIN,\n-            {ATTR_DISCOVER_DEVICES: zones}, config))\n-\n-    # create a separate alarm panel for each area\n-    areas = yield from api.get_areas()\n-    if areas:\n-        hass.async_create_task(discovery.async_load_platform(\n-            hass, 'alarm_control_panel', DOMAIN,\n-            {ATTR_DISCOVER_AREAS: areas}, config))\n-\n-    # start listening for incoming events over websocket\n-    api.start_listener(_async_process_message, hass.data[DATA_REGISTRY])\n-\n-    return True\n-\n-\n-@asyncio.coroutine\n-def _async_process_message(sia_message, spc_registry):\n-    spc_id = sia_message['sia_address']\n-    sia_code = sia_message['sia_code']\n-\n-    # BA - Burglary Alarm\n-    # CG - Close Area\n-    # NL - Perimeter Armed\n-    # OG - Open Area\n-    # ZO - Zone Open\n-    # ZC - Zone Close\n-    # ZX - Zone Short\n-    # ZD - Zone Disconnected\n-\n-    extra = {}\n-\n-    if sia_code in ('BA', 'CG', 'NL', 'OG'):\n-        # change in area status, notify alarm panel device\n-        device = spc_registry.get_alarm_device(spc_id)\n-        data = sia_message['description'].split('¦')\n-        if len(data) == 3:\n-            extra['changed_by'] = data[1]\n-    else:\n-        # Change in zone status, notify sensor device\n-        device = spc_registry.get_sensor_device(spc_id)\n-\n-    sia_code_to_state_map = {\n-        'BA': STATE_ALARM_TRIGGERED,\n-        'CG': STATE_ALARM_ARMED_AWAY,\n-        'NL': STATE_ALARM_ARMED_HOME,\n-        'OG': STATE_ALARM_DISARMED,\n-        'ZO': STATE_ON,\n-        'ZC': STATE_OFF,\n-        'ZX': STATE_UNKNOWN,\n-        'ZD': STATE_UNAVAILABLE,\n-    }\n-\n-    new_state = sia_code_to_state_map.get(sia_code, None)\n-\n-    if new_state and not device:\n-        _LOGGER.warning(\n-            \"No device mapping found for SPC area/zone id %s\", spc_id)\n-    elif new_state:\n-        yield from device.async_update_from_spc(new_state, extra)\n-\n+async def async_setup(hass, config):\n+    \"\"\"Set up the SPC component.\"\"\"\n+    from pyspcwebgw import SpcWebGateway\n \n-class SpcRegistry:\n-    \"\"\"Maintain mappings between SPC zones/areas and HA entities.\"\"\"\n+    async def async_upate_callback(spc_object):\n+        from pyspcwebgw.area import Area\n+        from pyspcwebgw.zone import Zone\n \n-    def __init__(self):\n-        \"\"\"Initialize the registry.\"\"\"\n-        self._zone_id_to_sensor_map = {}\n-        self._area_id_to_alarm_map = {}\n+        if isinstance(spc_object, Area):\n+            dispatcher_send(hass, SIGNAL_UPDATE_ALARM.format(spc_object.id))", "hunk": "@@ -45,9 +45,11 @@ async def async_setup(hass, config):\n         from pyspcwebgw.zone import Zone\n \n         if isinstance(spc_object, Area):\n-            dispatcher_send(hass, SIGNAL_UPDATE_ALARM.format(spc_object.id))\n+            async_dispatcher_send(hass,\n+                                  SIGNAL_UPDATE_ALARM.format(spc_object.id))\n         elif isinstance(spc_object, Zone):\n-            dispatcher_send(hass, SIGNAL_UPDATE_SENSOR.format(spc_object.id))\n+            async_dispatcher_send(hass,\n+                                  SIGNAL_UPDATE_SENSOR.format(spc_object.id))\n \n     session = aiohttp_client.async_get_clientsession(hass)\n \n", "comment": "Use `async_dispatcher_send`, since this is an async context.", "ids": [317198, "9c586ed52e4405a81bbcff8e6819fd530dc54567", "0359593db13929eed1feed0e9eea59cdebc67e5d"], "repo": "home-assistant-core", "ghid": 16214, "old": "         from pyspcwebgw.zone import Zone\n         if isinstance(spc_object, Area):\n-            dispatcher_send(hass, SIGNAL_UPDATE_ALARM.format(spc_object.id))\n         elif isinstance(spc_object, Zone):\n-            dispatcher_send(hass, SIGNAL_UPDATE_SENSOR.format(spc_object.id))\n     session = aiohttp_client.async_get_clientsession(hass)", "new": "         from pyspcwebgw.zone import Zone\n         if isinstance(spc_object, Area):\n+            async_dispatcher_send(hass,\n+                                  SIGNAL_UPDATE_ALARM.format(spc_object.id))\n         elif isinstance(spc_object, Zone):\n+            async_dispatcher_send(hass,\n+                                  SIGNAL_UPDATE_SENSOR.format(spc_object.id))\n     session = aiohttp_client.async_get_clientsession(hass)", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -42,244 +36,43 @@\n }, extra=vol.ALLOW_EXTRA)\n \n \n-@asyncio.coroutine\n-def async_setup(hass, config):\n-    \"\"\"Set up the SPC platform.\"\"\"\n-    hass.data[DATA_REGISTRY] = SpcRegistry()\n-\n-    api = SpcWebGateway(hass,\n-                        config[DOMAIN].get(CONF_API_URL),\n-                        config[DOMAIN].get(CONF_WS_URL))\n-\n-    hass.data[DATA_API] = api\n-\n-    # add sensor devices for each zone (typically motion/fire/door sensors)\n-    zones = yield from api.get_zones()\n-    if zones:\n-        hass.async_create_task(discovery.async_load_platform(\n-            hass, 'binary_sensor', DOMAIN,\n-            {ATTR_DISCOVER_DEVICES: zones}, config))\n-\n-    # create a separate alarm panel for each area\n-    areas = yield from api.get_areas()\n-    if areas:\n-        hass.async_create_task(discovery.async_load_platform(\n-            hass, 'alarm_control_panel', DOMAIN,\n-            {ATTR_DISCOVER_AREAS: areas}, config))\n-\n-    # start listening for incoming events over websocket\n-    api.start_listener(_async_process_message, hass.data[DATA_REGISTRY])\n-\n-    return True\n-\n-\n-@asyncio.coroutine\n-def _async_process_message(sia_message, spc_registry):\n-    spc_id = sia_message['sia_address']\n-    sia_code = sia_message['sia_code']\n-\n-    # BA - Burglary Alarm\n-    # CG - Close Area\n-    # NL - Perimeter Armed\n-    # OG - Open Area\n-    # ZO - Zone Open\n-    # ZC - Zone Close\n-    # ZX - Zone Short\n-    # ZD - Zone Disconnected\n-\n-    extra = {}\n-\n-    if sia_code in ('BA', 'CG', 'NL', 'OG'):\n-        # change in area status, notify alarm panel device\n-        device = spc_registry.get_alarm_device(spc_id)\n-        data = sia_message['description'].split('¦')\n-        if len(data) == 3:\n-            extra['changed_by'] = data[1]\n-    else:\n-        # Change in zone status, notify sensor device\n-        device = spc_registry.get_sensor_device(spc_id)\n-\n-    sia_code_to_state_map = {\n-        'BA': STATE_ALARM_TRIGGERED,\n-        'CG': STATE_ALARM_ARMED_AWAY,\n-        'NL': STATE_ALARM_ARMED_HOME,\n-        'OG': STATE_ALARM_DISARMED,\n-        'ZO': STATE_ON,\n-        'ZC': STATE_OFF,\n-        'ZX': STATE_UNKNOWN,\n-        'ZD': STATE_UNAVAILABLE,\n-    }\n-\n-    new_state = sia_code_to_state_map.get(sia_code, None)\n-\n-    if new_state and not device:\n-        _LOGGER.warning(\n-            \"No device mapping found for SPC area/zone id %s\", spc_id)\n-    elif new_state:\n-        yield from device.async_update_from_spc(new_state, extra)\n-\n+async def async_setup(hass, config):\n+    \"\"\"Set up the SPC component.\"\"\"\n+    from pyspcwebgw import SpcWebGateway\n \n-class SpcRegistry:\n-    \"\"\"Maintain mappings between SPC zones/areas and HA entities.\"\"\"\n+    async def async_upate_callback(spc_object):\n+        from pyspcwebgw.area import Area\n+        from pyspcwebgw.zone import Zone\n \n-    def __init__(self):\n-        \"\"\"Initialize the registry.\"\"\"\n-        self._zone_id_to_sensor_map = {}\n-        self._area_id_to_alarm_map = {}\n+        if isinstance(spc_object, Area):\n+            dispatcher_send(hass, SIGNAL_UPDATE_ALARM.format(spc_object.id))", "msg": "Use `async_dispatcher_send`, since this is an async context."}
{"old_hunk": "@@ -14,10 +14,11 @@\n \n from .config_flow import configured_hangouts\n from .const import (", "hunk": "@@ -11,6 +11,7 @@ import voluptuous as vol\n from homeassistant import config_entries\n from homeassistant.const import EVENT_HOMEASSISTANT_STOP\n from homeassistant.helpers import dispatcher\n+import homeassistant.helpers.config_validation as cv\n \n from .config_flow import configured_hangouts\n from .const import (\n", "comment": "You need to import `CONFIG_SCHEMA` for Home Assistant to pick it up.", "ids": [317228, "37cf77699bf711032b485056288ac22a713127db", "a419aff39d2483bc16551984ab8b8738e00b260e"], "repo": "home-assistant-core", "ghid": 16220, "old": " from homeassistant import config_entries\n from homeassistant.const import EVENT_HOMEASSISTANT_STOP\n from homeassistant.helpers import dispatcher\n from .config_flow import configured_hangouts\n from .const import (", "new": " from homeassistant import config_entries\n from homeassistant.const import EVENT_HOMEASSISTANT_STOP\n from homeassistant.helpers import dispatcher\n+import homeassistant.helpers.config_validation as cv\n from .config_flow import configured_hangouts\n from .const import (", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -14,10 +14,11 @@\n \n from .config_flow import configured_hangouts\n from .const import (", "msg": "You need to import `CONFIG_SCHEMA` for Home Assistant to pick it up."}
{"old_hunk": "@@ -22,6 +22,8 @@\n     vol.Required(CONF_TOKEN): cv.string\n })\n \n+ATTR_IMAGES = 'images'", "hunk": "@@ -23,7 +23,6 @@ PLATFORM_SCHEMA = PLATFORM_SCHEMA.extend({\n })\n \n ATTR_IMAGES = 'images'\n-ATTR_DATA = 'data'\n \n def get_service(hass, config, discovery_info=None):\n     \"\"\"Get the Discord notification service.\"\"\"\n", "comment": "What string constant do we use in other notify platforms for image attachment?", "ids": [317289, "0fba7904c8f775d45e7bcfe9ad51e5a08795e0e5", "960b2e41c96400409f3d4023f45fefab261c087c"], "repo": "home-assistant-core", "ghid": 16248, "old": " })\n ATTR_IMAGES = 'images'\n-ATTR_DATA = 'data'\n def get_service(hass, config, discovery_info=None):\n     \"\"\"Get the Discord notification service.\"\"\"", "new": " })\n ATTR_IMAGES = 'images'\n def get_service(hass, config, discovery_info=None):\n     \"\"\"Get the Discord notification service.\"\"\"", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -22,6 +22,8 @@\n     vol.Required(CONF_TOKEN): cv.string\n })\n \n+ATTR_IMAGES = 'images'", "msg": "What string constant do we use in other notify platforms for image attachment?"}
{"old_hunk": "@@ -119,12 +122,13 @@ def _auto_config_settings(self):\n \n         return self.async_show_form(\n             step_id='user',\n-            data_schema=vol.Schema({\n-                vol.Required('name'): vol.In(names),\n-                vol.Optional('sensors', default=False): bool,\n-                vol.Optional('port_forward', default=False): bool,\n-            })\n-        )\n+            data_schema=vol.Schema(\n+                OrderedDict([\n+                    (vol.Required('name'), vol.In(names)),\n+                    (vol.Optional('enable_sensors', default=False), bool),", "hunk": "@@ -125,8 +125,8 @@ class UpnpFlowHandler(data_entry_flow.FlowHandler):\n             data_schema=vol.Schema(\n                 OrderedDict([\n                     (vol.Required('name'), vol.In(names)),\n-                    (vol.Optional('enable_sensors', default=False), bool),\n-                    (vol.Optional('enable_port_mapping', default=False), bool),\n+                    (vol.Optional('enable_sensors'), bool),\n+                    (vol.Optional('enable_port_mapping'), bool),\n                 ])\n             ))\n \n", "comment": "Do we support default values here?", "ids": [317457, "d732f8eca2201e0ad8ade2abb282f3507647de1e", "3cb20c7b4d074c39ad04fb01fb7f07f0eb77faea"], "repo": "home-assistant-core", "ghid": 16300, "old": "             data_schema=vol.Schema(\n                 OrderedDict([\n                     (vol.Required('name'), vol.In(names)),\n-                    (vol.Optional('enable_sensors', default=False), bool),\n-                    (vol.Optional('enable_port_mapping', default=False), bool),\n                 ])\n             ))", "new": "             data_schema=vol.Schema(\n                 OrderedDict([\n                     (vol.Required('name'), vol.In(names)),\n+                    (vol.Optional('enable_sensors'), bool),\n+                    (vol.Optional('enable_port_mapping'), bool),\n                 ])\n             ))", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -119,12 +122,13 @@ def _auto_config_settings(self):\n \n         return self.async_show_form(\n             step_id='user',\n-            data_schema=vol.Schema({\n-                vol.Required('name'): vol.In(names),\n-                vol.Optional('sensors', default=False): bool,\n-                vol.Optional('port_forward', default=False): bool,\n-            })\n-        )\n+            data_schema=vol.Schema(\n+                OrderedDict([\n+                    (vol.Required('name'), vol.In(names)),\n+                    (vol.Optional('enable_sensors', default=False), bool),", "msg": "Do we support default values here?"}
{"old_hunk": "@@ -0,0 +1,226 @@\n+\"\"\"\n+Will open a port in your router for Home Assistant and provide statistics.\n+\n+For more details about this component, please refer to the documentation at\n+https://home-assistant.io/components/upnp/\n+\"\"\"\n+\n+\n+import asyncio\n+from ipaddress import IPv4Address\n+from ipaddress import ip_address\n+\n+import aiohttp\n+import voluptuous as vol\n+\n+from homeassistant.config_entries import ConfigEntry\n+from homeassistant.const import EVENT_HOMEASSISTANT_STOP\n+from homeassistant.exceptions import PlatformNotReady\n+from homeassistant.helpers import config_validation as cv\n+from homeassistant.helpers import discovery\n+from homeassistant.helpers.aiohttp_client import async_get_clientsession\n+from homeassistant.helpers.typing import ConfigType, HomeAssistantType\n+from homeassistant.util import get_local_ip\n+from homeassistant.components.discovery import DOMAIN as DISCOVERY_DOMAIN\n+\n+from .const import CONF_ENABLE_PORT_MAPPING, CONF_ENABLE_SENSORS\n+from .const import DOMAIN\n+from .const import LOGGER as _LOGGER\n+import homeassistant.components.igd.config_flow  # register the handler", "hunk": "@@ -143,7 +143,7 @@ async def async_setup(hass: HomeAssistantType, config: ConfigType):\n \n     # ensure sane config\n     if DOMAIN not in config:\n-        return False\n+        return True\n \n     if DISCOVERY_DOMAIN not in config:\n         _LOGGER.warning('IGD needs discovery, please enable it')\n", "comment": "'homeassistant.components.igd.config_flow' imported but unused", "ids": [317422, "38318ed15ff3886c7e734cd7c922261367cad91e", "c9e34e236da147d206ce66ec5d181269e622212d"], "repo": "home-assistant-core", "ghid": 16300, "old": "     # ensure sane config\n     if DOMAIN not in config:\n-        return False\n     if DISCOVERY_DOMAIN not in config:\n         _LOGGER.warning('IGD needs discovery, please enable it')", "new": "     # ensure sane config\n     if DOMAIN not in config:\n+        return True\n     if DISCOVERY_DOMAIN not in config:\n         _LOGGER.warning('IGD needs discovery, please enable it')", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -0,0 +1,226 @@\n+\"\"\"\n+Will open a port in your router for Home Assistant and provide statistics.\n+\n+For more details about this component, please refer to the documentation at\n+https://home-assistant.io/components/upnp/\n+\"\"\"\n+\n+\n+import asyncio\n+from ipaddress import IPv4Address\n+from ipaddress import ip_address\n+\n+import aiohttp\n+import voluptuous as vol\n+\n+from homeassistant.config_entries import ConfigEntry\n+from homeassistant.const import EVENT_HOMEASSISTANT_STOP\n+from homeassistant.exceptions import PlatformNotReady\n+from homeassistant.helpers import config_validation as cv\n+from homeassistant.helpers import discovery\n+from homeassistant.helpers.aiohttp_client import async_get_clientsession\n+from homeassistant.helpers.typing import ConfigType, HomeAssistantType\n+from homeassistant.util import get_local_ip\n+from homeassistant.components.discovery import DOMAIN as DISCOVERY_DOMAIN\n+\n+from .const import CONF_ENABLE_PORT_MAPPING, CONF_ENABLE_SENSORS\n+from .const import DOMAIN\n+from .const import LOGGER as _LOGGER\n+import homeassistant.components.igd.config_flow  # register the handler", "msg": "'homeassistant.components.igd.config_flow' imported but unused"}
{"old_hunk": "@@ -0,0 +1,164 @@\n+\"\"\"\n+Support for IGD Sensors.\n+\n+For more details about this platform, please refer to the documentation at\n+https://home-assistant.io/components/sensor.igd/\n+\"\"\"\n+# pylint: disable=invalid-name\n+import logging\n+\n+from homeassistant.components import history\n+from homeassistant.components.igd import DOMAIN, UNITS\n+from homeassistant.helpers.entity import Entity\n+\n+\n+_LOGGER = logging.getLogger(__name__)\n+\n+DEPENDENCIES = ['igd', 'history']\n+\n+BYTES_RECEIVED = 'bytes_received'\n+BYTES_SENT = 'bytes_sent'\n+PACKETS_RECEIVED = 'packets_received'\n+PACKETS_SENT = 'packets_sent'\n+\n+# sensor_type: [friendly_name, convert_unit, icon]\n+SENSOR_TYPES = {\n+    BYTES_RECEIVED: ['bytes received', True, 'mdi:server-network', float],\n+    BYTES_SENT: ['bytes sent', True, 'mdi:server-network', float],\n+    PACKETS_RECEIVED: ['packets received', False, 'mdi:server-network', int],\n+    PACKETS_SENT: ['packets sent', False, 'mdi:server-network', int],\n+}\n+\n+OVERFLOW_AT = 2**32\n+\n+\n+async def async_setup_platform(hass, config, async_add_devices,\n+                               discovery_info=None):\n+    \"\"\"Set up the IGD sensors.\"\"\"\n+    if discovery_info is None:\n+        return\n+\n+    udn = discovery_info['udn']\n+    device = hass.data[DOMAIN]['devices'][udn]\n+    unit = discovery_info['unit']\n+    async_add_devices([\n+        IGDSensor(device, t, unit if SENSOR_TYPES[t][1] else '#')\n+        for t in SENSOR_TYPES])\n+\n+\n+class IGDSensor(Entity):\n+    \"\"\"Representation of a UPnP IGD sensor.\"\"\"\n+\n+    def __init__(self, device, sensor_type, unit=None):\n+        \"\"\"Initialize the IGD sensor.\"\"\"\n+        self._device = device\n+        self.type = sensor_type\n+        self.unit = unit\n+        self.unit_factor = UNITS[unit] if unit in UNITS else 1\n+        self._name = 'IGD {}'.format(SENSOR_TYPES[sensor_type][0])\n+        self._state = None\n+        self._last_value = None\n+\n+    @property\n+    def name(self):\n+        \"\"\"Return the name of the sensor.\"\"\"\n+        return self._name\n+\n+    @property\n+    def state(self):\n+        \"\"\"Return the state of the device.\"\"\"\n+        if self._state is None:\n+            return None\n+\n+        coercer = SENSOR_TYPES[self.type][3]\n+        if coercer == int:\n+            return format(self._state)\n+\n+        return format(self._state / self.unit_factor, '.1f')\n+\n+    @property\n+    def icon(self):\n+        \"\"\"Icon to use in the frontend, if any.\"\"\"\n+        return SENSOR_TYPES[self.type][2]\n+\n+    @property\n+    def unit_of_measurement(self):\n+        \"\"\"Return the unit of measurement of this entity, if any.\"\"\"\n+        return self.unit\n+\n+    async def async_update(self):\n+        \"\"\"Get the latest information from the IGD.\"\"\"\n+        new_value = 0\n+        if self.type == BYTES_RECEIVED:\n+            new_value = await self._device.async_get_total_bytes_received()\n+        elif self.type == BYTES_SENT:\n+            new_value = await self._device.async_get_total_bytes_sent()\n+        elif self.type == PACKETS_RECEIVED:\n+            new_value = await self._device.async_get_total_packets_received()\n+        elif self.type == PACKETS_SENT:\n+            new_value = await self._device.async_get_total_packets_sent()\n+\n+        self._handle_new_value(new_value)\n+\n+    @property\n+    def _last_state(self):\n+        \"\"\"Get the last state reported to hass.\"\"\"\n+        states = history.get_last_state_changes(self.hass, 2, self.entity_id)\n+        entity_states = [\n+            state for state in states[self.entity_id]\n+            if state.state != 'unknown']\n+        _LOGGER.debug('%s: entity_states: %s', self.entity_id, entity_states)\n+        if not entity_states:\n+            return None\n+\n+        return entity_states[0]\n+\n+    @property\n+    def _last_value_from_state(self):\n+        \"\"\"Get the last value reported to hass.\"\"\"\n+        last_state = self._last_state\n+        if not last_state:\n+            _LOGGER.debug('%s: No last state', self.entity_id)\n+            return None\n+\n+        coercer = SENSOR_TYPES[self.type][3]\n+        try:\n+            state = coercer(float(last_state.state)) * self.unit_factor\n+        except ValueError:\n+            state = coercer(0.0)\n+\n+        return state\n+\n+    def _handle_new_value(self, new_value):\n+        if self.entity_id is None:\n+            # don't know our entity ID yet, do nothing but store value\n+            self._last_value = new_value\n+            return\n+\n+        if self._last_value is None:\n+            self._last_value = new_value\n+\n+        if self._state is None:\n+            # try to get the state from history\n+            self._state = self._last_value_from_state or 0\n+\n+        _LOGGER.debug('%s: state: %s, last_value: %s',\n+                      self.entity_id, self._state, self._last_value)\n+\n+        # calculate new state", "hunk": "@@ -5,10 +5,10 @@ For more details about this platform, please refer to the documentation at\n https://home-assistant.io/components/sensor.igd/\n \"\"\"\n # pylint: disable=invalid-name\n+from datetime import datetime\n import logging\n \n-from homeassistant.components import history\n-from homeassistant.components.igd import DOMAIN, UNITS\n+from homeassistant.components.igd import DOMAIN\n from homeassistant.helpers.entity import Entity\n \n \n", "comment": "I would refrain from changing the raw values. Or at least make it optional.", "ids": [317433, "c9e34e236da147d206ce66ec5d181269e622212d", "50f63ed4c5f5fdd2ab247b4a7a2ef39d34d6a856"], "repo": "home-assistant-core", "ghid": 16300, "old": " https://home-assistant.io/components/sensor.igd/\n \"\"\"\n # pylint: disable=invalid-name\n import logging\n-from homeassistant.components import history\n-from homeassistant.components.igd import DOMAIN, UNITS\n from homeassistant.helpers.entity import Entity", "new": " https://home-assistant.io/components/sensor.igd/\n \"\"\"\n # pylint: disable=invalid-name\n+from datetime import datetime\n import logging\n+from homeassistant.components.igd import DOMAIN\n from homeassistant.helpers.entity import Entity", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -0,0 +1,164 @@\n+\"\"\"\n+Support for IGD Sensors.\n+\n+For more details about this platform, please refer to the documentation at\n+https://home-assistant.io/components/sensor.igd/\n+\"\"\"\n+# pylint: disable=invalid-name\n+import logging\n+\n+from homeassistant.components import history\n+from homeassistant.components.igd import DOMAIN, UNITS\n+from homeassistant.helpers.entity import Entity\n+\n+\n+_LOGGER = logging.getLogger(__name__)\n+\n+DEPENDENCIES = ['igd', 'history']\n+\n+BYTES_RECEIVED = 'bytes_received'\n+BYTES_SENT = 'bytes_sent'\n+PACKETS_RECEIVED = 'packets_received'\n+PACKETS_SENT = 'packets_sent'\n+\n+# sensor_type: [friendly_name, convert_unit, icon]\n+SENSOR_TYPES = {\n+    BYTES_RECEIVED: ['bytes received', True, 'mdi:server-network', float],\n+    BYTES_SENT: ['bytes sent', True, 'mdi:server-network', float],\n+    PACKETS_RECEIVED: ['packets received', False, 'mdi:server-network', int],\n+    PACKETS_SENT: ['packets sent', False, 'mdi:server-network', int],\n+}\n+\n+OVERFLOW_AT = 2**32\n+\n+\n+async def async_setup_platform(hass, config, async_add_devices,\n+                               discovery_info=None):\n+    \"\"\"Set up the IGD sensors.\"\"\"\n+    if discovery_info is None:\n+        return\n+\n+    udn = discovery_info['udn']\n+    device = hass.data[DOMAIN]['devices'][udn]\n+    unit = discovery_info['unit']\n+    async_add_devices([\n+        IGDSensor(device, t, unit if SENSOR_TYPES[t][1] else '#')\n+        for t in SENSOR_TYPES])\n+\n+\n+class IGDSensor(Entity):\n+    \"\"\"Representation of a UPnP IGD sensor.\"\"\"\n+\n+    def __init__(self, device, sensor_type, unit=None):\n+        \"\"\"Initialize the IGD sensor.\"\"\"\n+        self._device = device\n+        self.type = sensor_type\n+        self.unit = unit\n+        self.unit_factor = UNITS[unit] if unit in UNITS else 1\n+        self._name = 'IGD {}'.format(SENSOR_TYPES[sensor_type][0])\n+        self._state = None\n+        self._last_value = None\n+\n+    @property\n+    def name(self):\n+        \"\"\"Return the name of the sensor.\"\"\"\n+        return self._name\n+\n+    @property\n+    def state(self):\n+        \"\"\"Return the state of the device.\"\"\"\n+        if self._state is None:\n+            return None\n+\n+        coercer = SENSOR_TYPES[self.type][3]\n+        if coercer == int:\n+            return format(self._state)\n+\n+        return format(self._state / self.unit_factor, '.1f')\n+\n+    @property\n+    def icon(self):\n+        \"\"\"Icon to use in the frontend, if any.\"\"\"\n+        return SENSOR_TYPES[self.type][2]\n+\n+    @property\n+    def unit_of_measurement(self):\n+        \"\"\"Return the unit of measurement of this entity, if any.\"\"\"\n+        return self.unit\n+\n+    async def async_update(self):\n+        \"\"\"Get the latest information from the IGD.\"\"\"\n+        new_value = 0\n+        if self.type == BYTES_RECEIVED:\n+            new_value = await self._device.async_get_total_bytes_received()\n+        elif self.type == BYTES_SENT:\n+            new_value = await self._device.async_get_total_bytes_sent()\n+        elif self.type == PACKETS_RECEIVED:\n+            new_value = await self._device.async_get_total_packets_received()\n+        elif self.type == PACKETS_SENT:\n+            new_value = await self._device.async_get_total_packets_sent()\n+\n+        self._handle_new_value(new_value)\n+\n+    @property\n+    def _last_state(self):\n+        \"\"\"Get the last state reported to hass.\"\"\"\n+        states = history.get_last_state_changes(self.hass, 2, self.entity_id)\n+        entity_states = [\n+            state for state in states[self.entity_id]\n+            if state.state != 'unknown']\n+        _LOGGER.debug('%s: entity_states: %s', self.entity_id, entity_states)\n+        if not entity_states:\n+            return None\n+\n+        return entity_states[0]\n+\n+    @property\n+    def _last_value_from_state(self):\n+        \"\"\"Get the last value reported to hass.\"\"\"\n+        last_state = self._last_state\n+        if not last_state:\n+            _LOGGER.debug('%s: No last state', self.entity_id)\n+            return None\n+\n+        coercer = SENSOR_TYPES[self.type][3]\n+        try:\n+            state = coercer(float(last_state.state)) * self.unit_factor\n+        except ValueError:\n+            state = coercer(0.0)\n+\n+        return state\n+\n+    def _handle_new_value(self, new_value):\n+        if self.entity_id is None:\n+            # don't know our entity ID yet, do nothing but store value\n+            self._last_value = new_value\n+            return\n+\n+        if self._last_value is None:\n+            self._last_value = new_value\n+\n+        if self._state is None:\n+            # try to get the state from history\n+            self._state = self._last_value_from_state or 0\n+\n+        _LOGGER.debug('%s: state: %s, last_value: %s',\n+                      self.entity_id, self._state, self._last_value)\n+\n+        # calculate new state", "msg": "I would refrain from changing the raw values. Or at least make it optional."}
{"old_hunk": "@@ -1,7 +1,23 @@\n \"\"\"Constants for the IGD component.\"\"\"\n import logging\n \n+\n DOMAIN = 'igd'\n LOGGER = logging.getLogger('homeassistant.components.igd')\n CONF_ENABLE_PORT_MAPPING = 'port_forward'\n CONF_ENABLE_SENSORS = 'sensors'\n+CONF_UDN = 'udn'\n+CONF_SSDP_DESCRIPTION = 'ssdp_description'\n+\n+\n+def ensure_domain_data(hass):", "hunk": "@@ -8,16 +8,3 @@ CONF_ENABLE_PORT_MAPPING = 'port_forward'\n CONF_ENABLE_SENSORS = 'sensors'\n CONF_UDN = 'udn'\n CONF_SSDP_DESCRIPTION = 'ssdp_description'\n-\n-\n-def ensure_domain_data(hass):\n-    \"\"\"Ensure hass.data is filled properly.\"\"\"\n-    hass.data[DOMAIN] = hass.data.get(DOMAIN, {})\n-    hass.data[DOMAIN]['devices'] = hass.data[DOMAIN].get('devices', {})\n-    hass.data[DOMAIN]['sensors'] = hass.data[DOMAIN].get('sensors', {})\n-    hass.data[DOMAIN]['discovered'] = hass.data[DOMAIN].get('discovered', {})\n-    hass.data[DOMAIN]['auto_config'] = hass.data[DOMAIN].get('auto_config', {\n-        'active': False,\n-        'port_forward': False,\n-        'sensors': False,\n-    })\n", "comment": "This is not really a const ? Maybe rename to common.py ?", "ids": [317434, "8bec4a55d1de3475d7db1a1dd38e85a796d811a1", "a4a175440b23c85382abd59e1963b9e5e994ea29"], "repo": "home-assistant-core", "ghid": 16300, "old": " CONF_ENABLE_SENSORS = 'sensors'\n CONF_UDN = 'udn'\n CONF_SSDP_DESCRIPTION = 'ssdp_description'\n-\n-\n-def ensure_domain_data(hass):\n-    \"\"\"Ensure hass.data is filled properly.\"\"\"\n-    hass.data[DOMAIN] = hass.data.get(DOMAIN, {})\n-    hass.data[DOMAIN]['devices'] = hass.data[DOMAIN].get('devices', {})\n-    hass.data[DOMAIN]['sensors'] = hass.data[DOMAIN].get('sensors', {})\n-    hass.data[DOMAIN]['discovered'] = hass.data[DOMAIN].get('discovered', {})\n-    hass.data[DOMAIN]['auto_config'] = hass.data[DOMAIN].get('auto_config', {\n-        'active': False,\n-        'port_forward': False,\n-        'sensors': False,\n-    })", "new": " CONF_ENABLE_SENSORS = 'sensors'\n CONF_UDN = 'udn'\n CONF_SSDP_DESCRIPTION = 'ssdp_description'", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -1,7 +1,23 @@\n \"\"\"Constants for the IGD component.\"\"\"\n import logging\n \n+\n DOMAIN = 'igd'\n LOGGER = logging.getLogger('homeassistant.components.igd')\n CONF_ENABLE_PORT_MAPPING = 'port_forward'\n CONF_ENABLE_SENSORS = 'sensors'\n+CONF_UDN = 'udn'\n+CONF_SSDP_DESCRIPTION = 'ssdp_description'\n+\n+\n+def ensure_domain_data(hass):", "msg": "This is not really a const ? Maybe rename to common.py ?"}
{"old_hunk": "@@ -39,6 +43,7 @@\n     vol.Optional(CONF_TIMEOUT, default=timedelta(seconds=5)): vol.All(\n         cv.time_period, cv.positive_timedelta),\n     vol.Optional(CONF_IMAGE_FIELD, default='image'): cv.string,\n+    vol.Required(CONF_TOKEN): vol.All(cv.string, vol.Length(min=8)),", "hunk": "@@ -43,7 +43,7 @@ PLATFORM_SCHEMA = PLATFORM_SCHEMA.extend({\n     vol.Optional(CONF_TIMEOUT, default=timedelta(seconds=5)): vol.All(\n         cv.time_period, cv.positive_timedelta),\n     vol.Optional(CONF_IMAGE_FIELD, default='image'): cv.string,\n-    vol.Required(CONF_TOKEN): vol.All(cv.string, vol.Length(min=8)),\n+    vol.Optional(CONF_TOKEN): vol.All(cv.string, vol.Length(min=8)),\n })\n \n \n", "comment": "Since you accept bearer token, CONF_TOKEN should be optional.", "ids": [317641, "351fb72cb479c6bed5617c517e3578e265272050", "c57fab9cb8bcaef009f9e284ed6b901f0eb4ce94"], "repo": "home-assistant-core", "ghid": 16339, "old": "     vol.Optional(CONF_TIMEOUT, default=timedelta(seconds=5)): vol.All(\n         cv.time_period, cv.positive_timedelta),\n     vol.Optional(CONF_IMAGE_FIELD, default='image'): cv.string,\n-    vol.Required(CONF_TOKEN): vol.All(cv.string, vol.Length(min=8)),\n })", "new": "     vol.Optional(CONF_TIMEOUT, default=timedelta(seconds=5)): vol.All(\n         cv.time_period, cv.positive_timedelta),\n     vol.Optional(CONF_IMAGE_FIELD, default='image'): cv.string,\n+    vol.Optional(CONF_TOKEN): vol.All(cv.string, vol.Length(min=8)),\n })", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -39,6 +43,7 @@\n     vol.Optional(CONF_TIMEOUT, default=timedelta(seconds=5)): vol.All(\n         cv.time_period, cv.positive_timedelta),\n     vol.Optional(CONF_IMAGE_FIELD, default='image'): cv.string,\n+    vol.Required(CONF_TOKEN): vol.All(cv.string, vol.Length(min=8)),", "msg": "Since you accept bearer token, CONF_TOKEN should be optional."}
{"old_hunk": "@@ -75,8 +82,17 @@ def __init__(self, hass, image_field):\n \n         if _camera is None:\n             _LOGGER.error(\"Unknown %s\", entity_id)\n+            status = HTTP_NOT_FOUND if request[KEY_AUTHENTICATED]\\\n+                else HTTP_UNAUTHORIZED\n             return self.json_message('Unknown {}'.format(entity_id),\n-                                     HTTP_BAD_REQUEST)\n+                                     status)", "hunk": "@@ -91,8 +91,9 @@ class CameraPushReceiver(HomeAssistantView):\n                          request.query.get('token') == _camera.token)\n \n         if not authenticated:\n-            return self.json_message('Invalid token for {}'.format(entity_id),\n-                                     HTTP_UNAUTHORIZED)\n+            return self.json_message(\n+                'Invalid authorization credentials for {}'.format(entity_id),\n+                HTTP_UNAUTHORIZED)\n \n         try:\n             data = await request.post()\n", "comment": "Should not return 'Invalid token' message if HTTP_UNAUTHORIZED", "ids": [317640, "351fb72cb479c6bed5617c517e3578e265272050", "c57fab9cb8bcaef009f9e284ed6b901f0eb4ce94"], "repo": "home-assistant-core", "ghid": 16339, "old": "                          request.query.get('token') == _camera.token)\n         if not authenticated:\n-            return self.json_message('Invalid token for {}'.format(entity_id),\n-                                     HTTP_UNAUTHORIZED)\n         try:\n             data = await request.post()", "new": "                          request.query.get('token') == _camera.token)\n         if not authenticated:\n+            return self.json_message(\n+                'Invalid authorization credentials for {}'.format(entity_id),\n+                HTTP_UNAUTHORIZED)\n         try:\n             data = await request.post()", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -75,8 +82,17 @@ def __init__(self, hass, image_field):\n \n         if _camera is None:\n             _LOGGER.error(\"Unknown %s\", entity_id)\n+            status = HTTP_NOT_FOUND if request[KEY_AUTHENTICATED]\\\n+                else HTTP_UNAUTHORIZED\n             return self.json_message('Unknown {}'.format(entity_id),\n-                                     HTTP_BAD_REQUEST)\n+                                     status)", "msg": "Should not return 'Invalid token' message if HTTP_UNAUTHORIZED"}
{"old_hunk": "@@ -0,0 +1,130 @@\n+\"\"\"\n+Platform to retrieve Jewish calendar information for Home Assistant.\n+\n+For more details about this platform, please refer to the documentation at\n+https://home-assistant.io/components/sensor.jewish_calendar/\n+\"\"\"\n+import logging\n+from datetime import date as dt", "hunk": "@@ -5,7 +5,6 @@ For more details about this platform, please refer to the documentation at\n https://home-assistant.io/components/sensor.jewish_calendar/\n \"\"\"\n import logging\n-from datetime import date as dt\n \n import voluptuous as vol\n \n", "comment": "'datetime.date as dt' imported but unused", "ids": [317790, "47a688335a79eb438667bcf50e9e9e531cd38db6", "263d0cb972b524499aae5deeb5f08fb8e9a2baf9"], "repo": "home-assistant-core", "ghid": 16393, "old": " https://home-assistant.io/components/sensor.jewish_calendar/\n \"\"\"\n import logging\n-from datetime import date as dt\n import voluptuous as vol", "new": " https://home-assistant.io/components/sensor.jewish_calendar/\n \"\"\"\n import logging\n import voluptuous as vol", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -0,0 +1,130 @@\n+\"\"\"\n+Platform to retrieve Jewish calendar information for Home Assistant.\n+\n+For more details about this platform, please refer to the documentation at\n+https://home-assistant.io/components/sensor.jewish_calendar/\n+\"\"\"\n+import logging\n+from datetime import date as dt", "msg": "'datetime.date as dt' imported but unused"}
{"old_hunk": "@@ -63,6 +63,17 @@ def brightness_state(value):\n     return 0, STATE_OFF\n \n \n+def byte_to_zwave_brightness(value):\n+    \"\"\"\n+    Converts brightness in 0-255 scale to 0-99 scale.\n+\n+    `value` -- (int) Brightness byte value from 0-255.\n+    \"\"\"\n+    if value > 0:\n+        return max(1, int((value / 255) * 99))\n+    return 0", "hunk": "@@ -64,8 +64,7 @@ def brightness_state(value):\n \n \n def byte_to_zwave_brightness(value):\n-    \"\"\"\n-    Converts brightness in 0-255 scale to 0-99 scale.\n+    \"\"\"Converts brightness in 0-255 scale to 0-99 scale.\n \n     `value` -- (int) Brightness byte value from 0-255.\n     \"\"\"\n", "comment": "If the value is 0, shouldn't we just return `None` (there is no brightness) ?", "ids": [317826, "d9d2ee2037daf0c01ea83415a71f3c7fe0b5ef46", "9bb7ee1c53834799c0fa0553466657e4d3463182"], "repo": "home-assistant-core", "ghid": 16420, "old": " def byte_to_zwave_brightness(value):\n-    \"\"\"\n-    Converts brightness in 0-255 scale to 0-99 scale.\n     `value` -- (int) Brightness byte value from 0-255.\n     \"\"\"", "new": " def byte_to_zwave_brightness(value):\n+    \"\"\"Converts brightness in 0-255 scale to 0-99 scale.\n     `value` -- (int) Brightness byte value from 0-255.\n     \"\"\"", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -63,6 +63,17 @@ def brightness_state(value):\n     return 0, STATE_OFF\n \n \n+def byte_to_zwave_brightness(value):\n+    \"\"\"\n+    Converts brightness in 0-255 scale to 0-99 scale.\n+\n+    `value` -- (int) Brightness byte value from 0-255.\n+    \"\"\"\n+    if value > 0:\n+        return max(1, int((value / 255) * 99))\n+    return 0", "msg": "If the value is 0, shouldn't we just return `None` (there is no brightness) ?"}
{"old_hunk": "@@ -65,6 +65,11 @@ def setup_platform(hass, config, add_entities, discovery_info=None):\n                                             'dual_channel', hass, gateway))\n             elif model in ['cube', 'sensor_cube', 'sensor_cube.aqgl01']:\n                 devices.append(XiaomiCube(device, hass, gateway))\n+            elif model in ['vibration']:\n+                devices.append(XiaomiVibration(device, 'Vibration',\n+                                               'status', hass, gateway))\n+            else:\n+                _LOGGER.warning('Unmapped Device Model')", "hunk": "@@ -69,7 +69,8 @@ def setup_platform(hass, config, add_entities, discovery_info=None):\n                 devices.append(XiaomiVibration(device, 'Vibration',\n                                                'status', hass, gateway))\n             else:\n-                _LOGGER.warning('Unmapped Device Model')\n+                _LOGGER.warning('Unmapped Device Model %s', model)\n+\n     add_entities(devices)\n \n \n", "comment": "add the `model` to the warning message", "ids": [317845, "52b79068d7f559452c0655ad204e60bf1a6c675b", "e729548a21aee49a8548255047500a9551572d37"], "repo": "home-assistant-core", "ghid": 16422, "old": "                 devices.append(XiaomiVibration(device, 'Vibration',\n                                                'status', hass, gateway))\n             else:\n-                _LOGGER.warning('Unmapped Device Model')\n     add_entities(devices)", "new": "                 devices.append(XiaomiVibration(device, 'Vibration',\n                                                'status', hass, gateway))\n             else:\n+                _LOGGER.warning('Unmapped Device Model %s', model)\n+\n     add_entities(devices)", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -65,6 +65,11 @@ def setup_platform(hass, config, add_entities, discovery_info=None):\n                                             'dual_channel', hass, gateway))\n             elif model in ['cube', 'sensor_cube', 'sensor_cube.aqgl01']:\n                 devices.append(XiaomiCube(device, hass, gateway))\n+            elif model in ['vibration']:\n+                devices.append(XiaomiVibration(device, 'Vibration',\n+                                               'status', hass, gateway))\n+            else:\n+                _LOGGER.warning('Unmapped Device Model')", "msg": "add the `model` to the warning message"}
{"old_hunk": "@@ -72,15 +81,15 @@ def entity_picture(self):\n     # pylint: disable=no-member\n     def update(self):\n         \"\"\"Update device state.\"\"\"\n-        from twitch.api import v3 as twitch\n-        stream = twitch.streams.by_channel(self._channel).get('stream')\n+        id = self._client.users.translate_usernames_to_ids([self._channel])[0].id", "hunk": "@@ -81,7 +81,6 @@ class TwitchSensor(Entity):\n     # pylint: disable=no-member\n     def update(self):\n         \"\"\"Update device state.\"\"\"\n-        id = self._client.users.translate_usernames_to_ids([self._channel])[0].id\n         stream = self._client.streams.get_stream_by_user(self._id)\n         if stream:\n             self._game = stream.get('channel').get('game')\n", "comment": "local variable 'id' is assigned to but never used<br>line too long (81 > 79 characters)", "ids": [318182, "8de9a688b79ca14299534ce2c0c4472eb9ae6b9f", "86445ed828792aea2dad268cb80021736763871e"], "repo": "home-assistant-core", "ghid": 16428, "old": "     # pylint: disable=no-member\n     def update(self):\n         \"\"\"Update device state.\"\"\"\n-        id = self._client.users.translate_usernames_to_ids([self._channel])[0].id\n         stream = self._client.streams.get_stream_by_user(self._id)\n         if stream:\n             self._game = stream.get('channel').get('game')", "new": "     # pylint: disable=no-member\n     def update(self):\n         \"\"\"Update device state.\"\"\"\n         stream = self._client.streams.get_stream_by_user(self._id)\n         if stream:\n             self._game = stream.get('channel').get('game')", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -72,15 +81,15 @@ def entity_picture(self):\n     # pylint: disable=no-member\n     def update(self):\n         \"\"\"Update device state.\"\"\"\n-        from twitch.api import v3 as twitch\n-        stream = twitch.streams.by_channel(self._channel).get('stream')\n+        id = self._client.users.translate_usernames_to_ids([self._channel])[0].id", "msg": "local variable 'id' is assigned to but never used<br>line too long (81 > 79 characters)"}
{"old_hunk": "@@ -99,11 +99,17 @@ def async_script_delay(now):\n                 delay = action[CONF_DELAY]\n \n                 try:\n+                    import collections", "hunk": "@@ -99,13 +99,12 @@ class Script():\n                 delay = action[CONF_DELAY]\n \n                 try:\n-                    import collections\n                     if isinstance(delay, template.Template):\n                         delay = vol.All(\n                             cv.time_period,\n                             cv.positive_timedelta)(\n                                 delay.async_render(variables))\n-                    elif isinstance(delay, collections.Mapping):\n+                    elif isinstance(delay, dict):\n                         delay_data = {}\n                         delay_data.update(\n                             template.render_complex(delay, variables))\n", "comment": "We import library on top like PEB8", "ids": [318238, "be7a347155811920e503fb187fc2eae7e6969e93", "b0615171c3be6f15504a8ecb7daddc3ffc3b99a9"], "repo": "home-assistant-core", "ghid": 16442, "old": "                 delay = action[CONF_DELAY]\n                 try:\n-                    import collections\n                     if isinstance(delay, template.Template):\n                         delay = vol.All(\n                             cv.time_period,\n                             cv.positive_timedelta)(\n                                 delay.async_render(variables))\n-                    elif isinstance(delay, collections.Mapping):\n                         delay_data = {}\n                         delay_data.update(\n                             template.render_complex(delay, variables))", "new": "                 delay = action[CONF_DELAY]\n                 try:\n                     if isinstance(delay, template.Template):\n                         delay = vol.All(\n                             cv.time_period,\n                             cv.positive_timedelta)(\n                                 delay.async_render(variables))\n+                    elif isinstance(delay, dict):\n                         delay_data = {}\n                         delay_data.update(\n                             template.render_complex(delay, variables))", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -99,11 +99,17 @@ def async_script_delay(now):\n                 delay = action[CONF_DELAY]\n \n                 try:\n+                    import collections", "msg": "We import library on top like PEB8"}
{"old_hunk": "@@ -5,6 +5,8 @@\n https://home-assistant.io/components/alarmdecoder/\n \"\"\"\n import logging\n+import os\n+import OpenSSL", "hunk": "@@ -10,7 +10,7 @@\n     <h1>Error 503 Backend is unhealthy</h1>\n     <p>Backend is unhealthy</p>\n     <h3>Guru Mediation:</h3>\n-    <p>Details: cache-sea4427-SEA 1645537650 889616069</p>\n+    <p>Details: cache-sea4473-SEA 1645537650 2094392408</p>\n     <hr>\n     <p>Varnish cache server</p>\n   </body>\n", "comment": "'OpenSSL' imported but unused", "ids": [318408, "ed86511997dab2aca9a891fcb2e060119a1bdf00", "c202495d137fdafa15175f6eef24c20b0f08c2b4"], "repo": "home-assistant-core", "ghid": 16483, "old": "     <h1>Error 503 Backend is unhealthy</h1>\n     <p>Backend is unhealthy</p>\n     <h3>Guru Mediation:</h3>\n-    <p>Details: cache-sea4427-SEA 1645537650 889616069</p>\n     <hr>\n     <p>Varnish cache server</p>\n   </body>", "new": "     <h1>Error 503 Backend is unhealthy</h1>\n     <p>Backend is unhealthy</p>\n     <h3>Guru Mediation:</h3>\n+    <p>Details: cache-sea4473-SEA 1645537650 2094392408</p>\n     <hr>\n     <p>Varnish cache server</p>\n   </body>", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -5,6 +5,8 @@\n https://home-assistant.io/components/alarmdecoder/\n \"\"\"\n import logging\n+import os\n+import OpenSSL", "msg": "'OpenSSL' imported but unused"}
{"old_hunk": "@@ -4,12 +4,13 @@\n For more details about this component, please refer to the documentation at\n https://home-assistant.io/components/doorbird/\n \"\"\"\n-import logging\n \n import asyncio\n+import datetime\n+import logging\n import voluptuous as vol\n \n-from homeassistant.components.http import HomeAssistantView\n+from homeassistant.components.http import KEY_AUTHENTICATED, HomeAssistantView", "hunk": "@@ -10,7 +10,7 @@\n     <h1>Error 503 Backend is unhealthy</h1>\n     <p>Backend is unhealthy</p>\n     <h3>Guru Mediation:</h3>\n-    <p>Details: cache-sea4449-SEA 1645537661 1452861607</p>\n+    <p>Details: cache-sea4435-SEA 1645537661 586600745</p>\n     <hr>\n     <p>Varnish cache server</p>\n   </body>\n", "comment": "'homeassistant.components.http.KEY_AUTHENTICATED' imported but unused", "ids": [318481, "0cae83a7693e0a4cf17700874421ab60fd4bb4ca", "052c0f721cb065aa078d90f319ac01386f224079"], "repo": "home-assistant-core", "ghid": 16504, "old": "     <h1>Error 503 Backend is unhealthy</h1>\n     <p>Backend is unhealthy</p>\n     <h3>Guru Mediation:</h3>\n-    <p>Details: cache-sea4449-SEA 1645537661 1452861607</p>\n     <hr>\n     <p>Varnish cache server</p>\n   </body>", "new": "     <h1>Error 503 Backend is unhealthy</h1>\n     <p>Backend is unhealthy</p>\n     <h3>Guru Mediation:</h3>\n+    <p>Details: cache-sea4435-SEA 1645537661 586600745</p>\n     <hr>\n     <p>Varnish cache server</p>\n   </body>", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -4,12 +4,13 @@\n For more details about this component, please refer to the documentation at\n https://home-assistant.io/components/doorbird/\n \"\"\"\n-import logging\n \n import asyncio\n+import datetime\n+import logging\n import voluptuous as vol\n \n-from homeassistant.components.http import HomeAssistantView\n+from homeassistant.components.http import KEY_AUTHENTICATED, HomeAssistantView", "msg": "'homeassistant.components.http.KEY_AUTHENTICATED' imported but unused"}
{"old_hunk": "@@ -11,41 +11,50 @@\n import voluptuous as vol\n from aiohttp.hdrs import CONTENT_TYPE\n \n-from homeassistant.const import CONF_API_KEY, CONF_HOST, CONTENT_TYPE_JSON\n+from homeassistant.const import (CONF_API_KEY, CONF_HOST, CONTENT_TYPE_JSON,\n+                                 CONF_NAME, CONF_PORT)\n import homeassistant.helpers.config_validation as cv\n \n _LOGGER = logging.getLogger(__name__)\n \n DOMAIN = 'octoprint'\n CONF_NUMBER_OF_TOOLS = 'number_of_tools'\n CONF_BED = 'bed'\n+DEFAULT_NAME = 'OctoPrint'\n \n CONFIG_SCHEMA = vol.Schema({\n-    DOMAIN: vol.Schema({\n+    DOMAIN: vol.All(cv.ensure_list, [vol.Schema({\n         vol.Required(CONF_API_KEY): cv.string,\n         vol.Required(CONF_HOST): cv.string,\n+        vol.Optional(CONF_PORT, default=80): cv.positive_int,\n+        vol.Optional(CONF_NAME, default=DEFAULT_NAME): cv.string,\n         vol.Optional(CONF_NUMBER_OF_TOOLS, default=0): cv.positive_int,\n         vol.Optional(CONF_BED, default=False): cv.boolean\n-    }),\n+    })]),\n }, extra=vol.ALLOW_EXTRA)\n \n \n def setup(hass, config):\n     \"\"\"Set up the OctoPrint component.\"\"\"\n-    base_url = 'http://{}/api/'.format(config[DOMAIN][CONF_HOST])\n-    api_key = config[DOMAIN][CONF_API_KEY]\n-    number_of_tools = config[DOMAIN][CONF_NUMBER_OF_TOOLS]\n-    bed = config[DOMAIN][CONF_BED]\n-\n-    hass.data[DOMAIN] = {\"api\": None}\n-\n-    try:\n-        octoprint_api = OctoPrintAPI(base_url, api_key, bed, number_of_tools)\n-        hass.data[DOMAIN][\"api\"] = octoprint_api\n-        octoprint_api.get('printer')\n-        octoprint_api.get('job')\n-    except requests.exceptions.RequestException as conn_err:\n-        _LOGGER.error(\"Error setting up OctoPrint API: %r\", conn_err)\n+    printers = hass.data[DOMAIN] = {}\n+\n+    for printer in config[DOMAIN]:\n+        name = printer[CONF_NAME]\n+        if name in printers:", "hunk": "@@ -10,7 +10,7 @@\n     <h1>Error 503 Backend is unhealthy</h1>\n     <p>Backend is unhealthy</p>\n     <h3>Guru Mediation:</h3>\n-    <p>Details: cache-sea4459-SEA 1645537664 3899975060</p>\n+    <p>Details: cache-sea4477-SEA 1645537664 3631137552</p>\n     <hr>\n     <p>Varnish cache server</p>\n   </body>\n", "comment": "Move this check to the config schema. Take a look at the mysensors component for an example that validates unique file names in config.", "ids": [318494, "0fcca9ec4b7468b045f156369c87434ae4ffbda3", "a8691f0c0f8142376e0aa6ad5f3b9d657c196089"], "repo": "home-assistant-core", "ghid": 16519, "old": "     <h1>Error 503 Backend is unhealthy</h1>\n     <p>Backend is unhealthy</p>\n     <h3>Guru Mediation:</h3>\n-    <p>Details: cache-sea4459-SEA 1645537664 3899975060</p>\n     <hr>\n     <p>Varnish cache server</p>\n   </body>", "new": "     <h1>Error 503 Backend is unhealthy</h1>\n     <p>Backend is unhealthy</p>\n     <h3>Guru Mediation:</h3>\n+    <p>Details: cache-sea4477-SEA 1645537664 3631137552</p>\n     <hr>\n     <p>Varnish cache server</p>\n   </body>", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -11,41 +11,50 @@\n import voluptuous as vol\n from aiohttp.hdrs import CONTENT_TYPE\n \n-from homeassistant.const import CONF_API_KEY, CONF_HOST, CONTENT_TYPE_JSON\n+from homeassistant.const import (CONF_API_KEY, CONF_HOST, CONTENT_TYPE_JSON,\n+                                 CONF_NAME, CONF_PORT)\n import homeassistant.helpers.config_validation as cv\n \n _LOGGER = logging.getLogger(__name__)\n \n DOMAIN = 'octoprint'\n CONF_NUMBER_OF_TOOLS = 'number_of_tools'\n CONF_BED = 'bed'\n+DEFAULT_NAME = 'OctoPrint'\n \n CONFIG_SCHEMA = vol.Schema({\n-    DOMAIN: vol.Schema({\n+    DOMAIN: vol.All(cv.ensure_list, [vol.Schema({\n         vol.Required(CONF_API_KEY): cv.string,\n         vol.Required(CONF_HOST): cv.string,\n+        vol.Optional(CONF_PORT, default=80): cv.positive_int,\n+        vol.Optional(CONF_NAME, default=DEFAULT_NAME): cv.string,\n         vol.Optional(CONF_NUMBER_OF_TOOLS, default=0): cv.positive_int,\n         vol.Optional(CONF_BED, default=False): cv.boolean\n-    }),\n+    })]),\n }, extra=vol.ALLOW_EXTRA)\n \n \n def setup(hass, config):\n     \"\"\"Set up the OctoPrint component.\"\"\"\n-    base_url = 'http://{}/api/'.format(config[DOMAIN][CONF_HOST])\n-    api_key = config[DOMAIN][CONF_API_KEY]\n-    number_of_tools = config[DOMAIN][CONF_NUMBER_OF_TOOLS]\n-    bed = config[DOMAIN][CONF_BED]\n-\n-    hass.data[DOMAIN] = {\"api\": None}\n-\n-    try:\n-        octoprint_api = OctoPrintAPI(base_url, api_key, bed, number_of_tools)\n-        hass.data[DOMAIN][\"api\"] = octoprint_api\n-        octoprint_api.get('printer')\n-        octoprint_api.get('job')\n-    except requests.exceptions.RequestException as conn_err:\n-        _LOGGER.error(\"Error setting up OctoPrint API: %r\", conn_err)\n+    printers = hass.data[DOMAIN] = {}\n+\n+    for printer in config[DOMAIN]:\n+        name = printer[CONF_NAME]\n+        if name in printers:", "msg": "Move this check to the config schema. Take a look at the mysensors component for an example that validates unique file names in config."}
{"old_hunk": "@@ -11,41 +11,50 @@\n import voluptuous as vol\n from aiohttp.hdrs import CONTENT_TYPE\n \n-from homeassistant.const import CONF_API_KEY, CONF_HOST, CONTENT_TYPE_JSON\n+from homeassistant.const import (CONF_API_KEY, CONF_HOST, CONTENT_TYPE_JSON,\n+                                 CONF_NAME, CONF_PORT)\n import homeassistant.helpers.config_validation as cv\n \n _LOGGER = logging.getLogger(__name__)\n \n DOMAIN = 'octoprint'\n CONF_NUMBER_OF_TOOLS = 'number_of_tools'\n CONF_BED = 'bed'\n+DEFAULT_NAME = 'OctoPrint'\n \n CONFIG_SCHEMA = vol.Schema({\n-    DOMAIN: vol.Schema({\n+    DOMAIN: vol.All(cv.ensure_list, [vol.Schema({\n         vol.Required(CONF_API_KEY): cv.string,\n         vol.Required(CONF_HOST): cv.string,\n+        vol.Optional(CONF_PORT, default=80): cv.positive_int,\n+        vol.Optional(CONF_NAME, default=DEFAULT_NAME): cv.string,\n         vol.Optional(CONF_NUMBER_OF_TOOLS, default=0): cv.positive_int,\n         vol.Optional(CONF_BED, default=False): cv.boolean\n-    }),\n+    })]),\n }, extra=vol.ALLOW_EXTRA)\n \n \n def setup(hass, config):\n     \"\"\"Set up the OctoPrint component.\"\"\"\n-    base_url = 'http://{}/api/'.format(config[DOMAIN][CONF_HOST])\n-    api_key = config[DOMAIN][CONF_API_KEY]\n-    number_of_tools = config[DOMAIN][CONF_NUMBER_OF_TOOLS]\n-    bed = config[DOMAIN][CONF_BED]\n-\n-    hass.data[DOMAIN] = {\"api\": None}\n-\n-    try:\n-        octoprint_api = OctoPrintAPI(base_url, api_key, bed, number_of_tools)\n-        hass.data[DOMAIN][\"api\"] = octoprint_api\n-        octoprint_api.get('printer')\n-        octoprint_api.get('job')\n-    except requests.exceptions.RequestException as conn_err:\n-        _LOGGER.error(\"Error setting up OctoPrint API: %r\", conn_err)\n+    printers = hass.data[DOMAIN] = {}\n+\n+    for printer in config[DOMAIN]:\n+        name = printer[CONF_NAME]\n+        if name in printers:\n+            raise vol.Invalid('Printer names must be unique')\n+        base_url = 'http://{}:{}/api/'.format(printer[CONF_HOST],\n+                                              printer[CONF_PORT])\n+        api_key = printer[CONF_API_KEY]\n+        number_of_tools = printer[CONF_NUMBER_OF_TOOLS]\n+        bed = printer[CONF_BED]\n+        try:\n+            octoprint_api = OctoPrintAPI(base_url, api_key, bed,\n+                                         number_of_tools)\n+            printers[name] = octoprint_api", "hunk": "@@ -10,7 +10,7 @@\n     <h1>Error 503 Backend is unhealthy</h1>\n     <p>Backend is unhealthy</p>\n     <h3>Guru Mediation:</h3>\n-    <p>Details: cache-sea4459-SEA 1645537666 3899995973</p>\n+    <p>Details: cache-sea4477-SEA 1645537666 3631157605</p>\n     <hr>\n     <p>Varnish cache server</p>\n   </body>\n", "comment": "Isn't `base_url` unique? Maybe store the api under that key instead?", "ids": [318497, "0fcca9ec4b7468b045f156369c87434ae4ffbda3", "a8691f0c0f8142376e0aa6ad5f3b9d657c196089"], "repo": "home-assistant-core", "ghid": 16519, "old": "     <h1>Error 503 Backend is unhealthy</h1>\n     <p>Backend is unhealthy</p>\n     <h3>Guru Mediation:</h3>\n-    <p>Details: cache-sea4459-SEA 1645537666 3899995973</p>\n     <hr>\n     <p>Varnish cache server</p>\n   </body>", "new": "     <h1>Error 503 Backend is unhealthy</h1>\n     <p>Backend is unhealthy</p>\n     <h3>Guru Mediation:</h3>\n+    <p>Details: cache-sea4477-SEA 1645537666 3631157605</p>\n     <hr>\n     <p>Varnish cache server</p>\n   </body>", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -11,41 +11,50 @@\n import voluptuous as vol\n from aiohttp.hdrs import CONTENT_TYPE\n \n-from homeassistant.const import CONF_API_KEY, CONF_HOST, CONTENT_TYPE_JSON\n+from homeassistant.const import (CONF_API_KEY, CONF_HOST, CONTENT_TYPE_JSON,\n+                                 CONF_NAME, CONF_PORT)\n import homeassistant.helpers.config_validation as cv\n \n _LOGGER = logging.getLogger(__name__)\n \n DOMAIN = 'octoprint'\n CONF_NUMBER_OF_TOOLS = 'number_of_tools'\n CONF_BED = 'bed'\n+DEFAULT_NAME = 'OctoPrint'\n \n CONFIG_SCHEMA = vol.Schema({\n-    DOMAIN: vol.Schema({\n+    DOMAIN: vol.All(cv.ensure_list, [vol.Schema({\n         vol.Required(CONF_API_KEY): cv.string,\n         vol.Required(CONF_HOST): cv.string,\n+        vol.Optional(CONF_PORT, default=80): cv.positive_int,\n+        vol.Optional(CONF_NAME, default=DEFAULT_NAME): cv.string,\n         vol.Optional(CONF_NUMBER_OF_TOOLS, default=0): cv.positive_int,\n         vol.Optional(CONF_BED, default=False): cv.boolean\n-    }),\n+    })]),\n }, extra=vol.ALLOW_EXTRA)\n \n \n def setup(hass, config):\n     \"\"\"Set up the OctoPrint component.\"\"\"\n-    base_url = 'http://{}/api/'.format(config[DOMAIN][CONF_HOST])\n-    api_key = config[DOMAIN][CONF_API_KEY]\n-    number_of_tools = config[DOMAIN][CONF_NUMBER_OF_TOOLS]\n-    bed = config[DOMAIN][CONF_BED]\n-\n-    hass.data[DOMAIN] = {\"api\": None}\n-\n-    try:\n-        octoprint_api = OctoPrintAPI(base_url, api_key, bed, number_of_tools)\n-        hass.data[DOMAIN][\"api\"] = octoprint_api\n-        octoprint_api.get('printer')\n-        octoprint_api.get('job')\n-    except requests.exceptions.RequestException as conn_err:\n-        _LOGGER.error(\"Error setting up OctoPrint API: %r\", conn_err)\n+    printers = hass.data[DOMAIN] = {}\n+\n+    for printer in config[DOMAIN]:\n+        name = printer[CONF_NAME]\n+        if name in printers:\n+            raise vol.Invalid('Printer names must be unique')\n+        base_url = 'http://{}:{}/api/'.format(printer[CONF_HOST],\n+                                              printer[CONF_PORT])\n+        api_key = printer[CONF_API_KEY]\n+        number_of_tools = printer[CONF_NUMBER_OF_TOOLS]\n+        bed = printer[CONF_BED]\n+        try:\n+            octoprint_api = OctoPrintAPI(base_url, api_key, bed,\n+                                         number_of_tools)\n+            printers[name] = octoprint_api", "msg": "Isn't `base_url` unique? Maybe store the api under that key instead?"}
{"old_hunk": "@@ -11,41 +11,50 @@\n import voluptuous as vol\n from aiohttp.hdrs import CONTENT_TYPE\n \n-from homeassistant.const import CONF_API_KEY, CONF_HOST, CONTENT_TYPE_JSON\n+from homeassistant.const import (CONF_API_KEY, CONF_HOST, CONTENT_TYPE_JSON,\n+                                 CONF_NAME, CONF_PORT)\n import homeassistant.helpers.config_validation as cv\n \n _LOGGER = logging.getLogger(__name__)\n \n DOMAIN = 'octoprint'\n CONF_NUMBER_OF_TOOLS = 'number_of_tools'\n CONF_BED = 'bed'\n+DEFAULT_NAME = 'OctoPrint'\n \n CONFIG_SCHEMA = vol.Schema({\n-    DOMAIN: vol.Schema({\n+    DOMAIN: vol.All(cv.ensure_list, [vol.Schema({\n         vol.Required(CONF_API_KEY): cv.string,\n         vol.Required(CONF_HOST): cv.string,\n+        vol.Optional(CONF_PORT, default=80): cv.positive_int,\n+        vol.Optional(CONF_NAME, default=DEFAULT_NAME): cv.string,", "hunk": "@@ -10,7 +10,7 @@\n     <h1>Error 503 Backend is unhealthy</h1>\n     <p>Backend is unhealthy</p>\n     <h3>Guru Mediation:</h3>\n-    <p>Details: cache-sea4459-SEA 1645537666 3899992802</p>\n+    <p>Details: cache-sea4477-SEA 1645537666 3631154839</p>\n     <hr>\n     <p>Varnish cache server</p>\n   </body>\n", "comment": "Maybe it would be more natural to make the config a dictionary with the names as keys and value be another dictionary with the rest of the config options. Then the names would naturally be unique. Or skip the requirement on unique name and use base url as storage key instead.", "ids": [318496, "0fcca9ec4b7468b045f156369c87434ae4ffbda3", "a8691f0c0f8142376e0aa6ad5f3b9d657c196089"], "repo": "home-assistant-core", "ghid": 16519, "old": "     <h1>Error 503 Backend is unhealthy</h1>\n     <p>Backend is unhealthy</p>\n     <h3>Guru Mediation:</h3>\n-    <p>Details: cache-sea4459-SEA 1645537666 3899992802</p>\n     <hr>\n     <p>Varnish cache server</p>\n   </body>", "new": "     <h1>Error 503 Backend is unhealthy</h1>\n     <p>Backend is unhealthy</p>\n     <h3>Guru Mediation:</h3>\n+    <p>Details: cache-sea4477-SEA 1645537666 3631154839</p>\n     <hr>\n     <p>Varnish cache server</p>\n   </body>", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -11,41 +11,50 @@\n import voluptuous as vol\n from aiohttp.hdrs import CONTENT_TYPE\n \n-from homeassistant.const import CONF_API_KEY, CONF_HOST, CONTENT_TYPE_JSON\n+from homeassistant.const import (CONF_API_KEY, CONF_HOST, CONTENT_TYPE_JSON,\n+                                 CONF_NAME, CONF_PORT)\n import homeassistant.helpers.config_validation as cv\n \n _LOGGER = logging.getLogger(__name__)\n \n DOMAIN = 'octoprint'\n CONF_NUMBER_OF_TOOLS = 'number_of_tools'\n CONF_BED = 'bed'\n+DEFAULT_NAME = 'OctoPrint'\n \n CONFIG_SCHEMA = vol.Schema({\n-    DOMAIN: vol.Schema({\n+    DOMAIN: vol.All(cv.ensure_list, [vol.Schema({\n         vol.Required(CONF_API_KEY): cv.string,\n         vol.Required(CONF_HOST): cv.string,\n+        vol.Optional(CONF_PORT, default=80): cv.positive_int,\n+        vol.Optional(CONF_NAME, default=DEFAULT_NAME): cv.string,", "msg": "Maybe it would be more natural to make the config a dictionary with the names as keys and value be another dictionary with the rest of the config options. Then the names would naturally be unique. Or skip the requirement on unique name and use base url as storage key instead."}
{"old_hunk": "@@ -0,0 +1,98 @@\n+\"\"\"\n+Support for balance data via the Starling Bank API.\n+\n+For more details about this platform, please refer to the documentation at\n+https://www.home-assistant.io/components/sensor.starlingbank/\n+\"\"\"\n+import voluptuous as vol\n+\n+from homeassistant.helpers.entity import Entity\n+from homeassistant.components.sensor import PLATFORM_SCHEMA\n+from homeassistant.const import (\n+    CONF_ACCESS_TOKEN, CONF_NAME\n+)\n+import homeassistant.helpers.config_validation as cv\n+\n+REQUIREMENTS = ['starlingbank==1.1']\n+\n+BALANCE_TYPES = ['cleared_balance', 'effective_balance']\n+DEFAULT_ACCOUNT_NAME = 'Starling'\n+ICON = 'mdi:currency-gbp'\n+\n+CONF_ACCOUNTS = 'accounts'\n+CONF_BALANCE_TYPES = 'balance_types'\n+\n+PLATFORM_SCHEMA = PLATFORM_SCHEMA.extend(\n+    {\n+        vol.Required(CONF_ACCOUNTS): vol.All([{\n+            vol.Optional(CONF_NAME, default=DEFAULT_ACCOUNT_NAME): cv.string,\n+            vol.Required(CONF_ACCESS_TOKEN): cv.string,\n+            vol.Optional(\n+                CONF_BALANCE_TYPES, default=BALANCE_TYPES\n+                ): vol.All(\n+                    cv.ensure_list,  # Wraps value in a list if it is not one.\n+                    [vol.In(BALANCE_TYPES)]\n+                )\n+        }], cv.ensure_list)\n+    }\n+)\n+\n+\n+def setup_platform(hass, config, add_devices, discovery_info=None):\n+    \"\"\"Sensor platform setup.\"\"\"\n+    accounts = config.get(CONF_ACCOUNTS)\n+\n+    sensors = []\n+    for account in accounts:\n+        for balance_type in account[CONF_BALANCE_TYPES]:\n+            sensors.append(StarlingBalanceSensor(\n+                account[CONF_ACCESS_TOKEN],\n+                account[CONF_NAME],\n+                balance_type\n+            ))\n+    add_devices(sensors, True)  # True forces an update upon platform setup.\n+\n+\n+class StarlingBalanceSensor(Entity):\n+    \"\"\"Representation of a Starling balance sensor.\"\"\"\n+\n+    def __init__(self, token, account_name, balance_data_type):\n+        \"\"\"Initialize the sensor.\"\"\"\n+        from starlingbank import StarlingAccount\n+\n+        self._starling_account = StarlingAccount(token)", "hunk": "@@ -10,7 +10,7 @@\n     <h1>Error 503 Backend is unhealthy</h1>\n     <p>Backend is unhealthy</p>\n     <h3>Guru Mediation:</h3>\n-    <p>Details: cache-sea4482-SEA 1645537668 1952207351</p>\n+    <p>Details: cache-sea4435-SEA 1645537668 586658560</p>\n     <hr>\n     <p>Varnish cache server</p>\n   </body>\n", "comment": "Create this during the platform setup and pass it to `StarlingBalanceSensor(Entity)`.", "ids": [318530, "04c930a83968198a21648676a0714bbce37e6814", "232b0fa113bdb58a9aa02fd48a19b501a0a5b8d9"], "repo": "home-assistant-core", "ghid": 16522, "old": "     <h1>Error 503 Backend is unhealthy</h1>\n     <p>Backend is unhealthy</p>\n     <h3>Guru Mediation:</h3>\n-    <p>Details: cache-sea4482-SEA 1645537668 1952207351</p>\n     <hr>\n     <p>Varnish cache server</p>\n   </body>", "new": "     <h1>Error 503 Backend is unhealthy</h1>\n     <p>Backend is unhealthy</p>\n     <h3>Guru Mediation:</h3>\n+    <p>Details: cache-sea4435-SEA 1645537668 586658560</p>\n     <hr>\n     <p>Varnish cache server</p>\n   </body>", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -0,0 +1,98 @@\n+\"\"\"\n+Support for balance data via the Starling Bank API.\n+\n+For more details about this platform, please refer to the documentation at\n+https://www.home-assistant.io/components/sensor.starlingbank/\n+\"\"\"\n+import voluptuous as vol\n+\n+from homeassistant.helpers.entity import Entity\n+from homeassistant.components.sensor import PLATFORM_SCHEMA\n+from homeassistant.const import (\n+    CONF_ACCESS_TOKEN, CONF_NAME\n+)\n+import homeassistant.helpers.config_validation as cv\n+\n+REQUIREMENTS = ['starlingbank==1.1']\n+\n+BALANCE_TYPES = ['cleared_balance', 'effective_balance']\n+DEFAULT_ACCOUNT_NAME = 'Starling'\n+ICON = 'mdi:currency-gbp'\n+\n+CONF_ACCOUNTS = 'accounts'\n+CONF_BALANCE_TYPES = 'balance_types'\n+\n+PLATFORM_SCHEMA = PLATFORM_SCHEMA.extend(\n+    {\n+        vol.Required(CONF_ACCOUNTS): vol.All([{\n+            vol.Optional(CONF_NAME, default=DEFAULT_ACCOUNT_NAME): cv.string,\n+            vol.Required(CONF_ACCESS_TOKEN): cv.string,\n+            vol.Optional(\n+                CONF_BALANCE_TYPES, default=BALANCE_TYPES\n+                ): vol.All(\n+                    cv.ensure_list,  # Wraps value in a list if it is not one.\n+                    [vol.In(BALANCE_TYPES)]\n+                )\n+        }], cv.ensure_list)\n+    }\n+)\n+\n+\n+def setup_platform(hass, config, add_devices, discovery_info=None):\n+    \"\"\"Sensor platform setup.\"\"\"\n+    accounts = config.get(CONF_ACCOUNTS)\n+\n+    sensors = []\n+    for account in accounts:\n+        for balance_type in account[CONF_BALANCE_TYPES]:\n+            sensors.append(StarlingBalanceSensor(\n+                account[CONF_ACCESS_TOKEN],\n+                account[CONF_NAME],\n+                balance_type\n+            ))\n+    add_devices(sensors, True)  # True forces an update upon platform setup.\n+\n+\n+class StarlingBalanceSensor(Entity):\n+    \"\"\"Representation of a Starling balance sensor.\"\"\"\n+\n+    def __init__(self, token, account_name, balance_data_type):\n+        \"\"\"Initialize the sensor.\"\"\"\n+        from starlingbank import StarlingAccount\n+\n+        self._starling_account = StarlingAccount(token)", "msg": "Create this during the platform setup and pass it to `StarlingBalanceSensor(Entity)`."}
{"old_hunk": "@@ -0,0 +1,98 @@\n+\"\"\"\n+Support for balance data via the Starling Bank API.\n+\n+For more details about this platform, please refer to the documentation at\n+https://www.home-assistant.io/components/sensor.starlingbank/\n+\"\"\"\n+import voluptuous as vol\n+\n+from homeassistant.helpers.entity import Entity\n+from homeassistant.components.sensor import PLATFORM_SCHEMA\n+from homeassistant.const import (\n+    CONF_ACCESS_TOKEN, CONF_NAME\n+)\n+import homeassistant.helpers.config_validation as cv\n+\n+REQUIREMENTS = ['starlingbank==1.1']\n+\n+BALANCE_TYPES = ['cleared_balance', 'effective_balance']\n+DEFAULT_ACCOUNT_NAME = 'Starling'\n+ICON = 'mdi:currency-gbp'\n+\n+CONF_ACCOUNTS = 'accounts'\n+CONF_BALANCE_TYPES = 'balance_types'\n+\n+PLATFORM_SCHEMA = PLATFORM_SCHEMA.extend(\n+    {\n+        vol.Required(CONF_ACCOUNTS): vol.All([{\n+            vol.Optional(CONF_NAME, default=DEFAULT_ACCOUNT_NAME): cv.string,\n+            vol.Required(CONF_ACCESS_TOKEN): cv.string,\n+            vol.Optional(\n+                CONF_BALANCE_TYPES, default=BALANCE_TYPES\n+                ): vol.All(\n+                    cv.ensure_list,  # Wraps value in a list if it is not one.\n+                    [vol.In(BALANCE_TYPES)]\n+                )\n+        }], cv.ensure_list)\n+    }\n+)\n+\n+\n+def setup_platform(hass, config, add_devices, discovery_info=None):\n+    \"\"\"Sensor platform setup.\"\"\"\n+    accounts = config.get(CONF_ACCOUNTS)\n+\n+    sensors = []\n+    for account in accounts:\n+        for balance_type in account[CONF_BALANCE_TYPES]:\n+            sensors.append(StarlingBalanceSensor(\n+                account[CONF_ACCESS_TOKEN],\n+                account[CONF_NAME],\n+                balance_type\n+            ))\n+    add_devices(sensors, True)  # True forces an update upon platform setup.", "hunk": "@@ -10,7 +10,7 @@\n     <h1>Error 503 Backend is unhealthy</h1>\n     <p>Backend is unhealthy</p>\n     <h3>Guru Mediation:</h3>\n-    <p>Details: cache-sea4482-SEA 1645537668 1952204122</p>\n+    <p>Details: cache-sea4435-SEA 1645537668 586655474</p>\n     <hr>\n     <p>Varnish cache server</p>\n   </body>\n", "comment": "You should check that the Access token is valid during the setup of the platform. If not then fail and log the error. Otherwise will be users end up with a non-working platform in their instance.", "ids": [318528, "04c930a83968198a21648676a0714bbce37e6814", "232b0fa113bdb58a9aa02fd48a19b501a0a5b8d9"], "repo": "home-assistant-core", "ghid": 16522, "old": "     <h1>Error 503 Backend is unhealthy</h1>\n     <p>Backend is unhealthy</p>\n     <h3>Guru Mediation:</h3>\n-    <p>Details: cache-sea4482-SEA 1645537668 1952204122</p>\n     <hr>\n     <p>Varnish cache server</p>\n   </body>", "new": "     <h1>Error 503 Backend is unhealthy</h1>\n     <p>Backend is unhealthy</p>\n     <h3>Guru Mediation:</h3>\n+    <p>Details: cache-sea4435-SEA 1645537668 586655474</p>\n     <hr>\n     <p>Varnish cache server</p>\n   </body>", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -0,0 +1,98 @@\n+\"\"\"\n+Support for balance data via the Starling Bank API.\n+\n+For more details about this platform, please refer to the documentation at\n+https://www.home-assistant.io/components/sensor.starlingbank/\n+\"\"\"\n+import voluptuous as vol\n+\n+from homeassistant.helpers.entity import Entity\n+from homeassistant.components.sensor import PLATFORM_SCHEMA\n+from homeassistant.const import (\n+    CONF_ACCESS_TOKEN, CONF_NAME\n+)\n+import homeassistant.helpers.config_validation as cv\n+\n+REQUIREMENTS = ['starlingbank==1.1']\n+\n+BALANCE_TYPES = ['cleared_balance', 'effective_balance']\n+DEFAULT_ACCOUNT_NAME = 'Starling'\n+ICON = 'mdi:currency-gbp'\n+\n+CONF_ACCOUNTS = 'accounts'\n+CONF_BALANCE_TYPES = 'balance_types'\n+\n+PLATFORM_SCHEMA = PLATFORM_SCHEMA.extend(\n+    {\n+        vol.Required(CONF_ACCOUNTS): vol.All([{\n+            vol.Optional(CONF_NAME, default=DEFAULT_ACCOUNT_NAME): cv.string,\n+            vol.Required(CONF_ACCESS_TOKEN): cv.string,\n+            vol.Optional(\n+                CONF_BALANCE_TYPES, default=BALANCE_TYPES\n+                ): vol.All(\n+                    cv.ensure_list,  # Wraps value in a list if it is not one.\n+                    [vol.In(BALANCE_TYPES)]\n+                )\n+        }], cv.ensure_list)\n+    }\n+)\n+\n+\n+def setup_platform(hass, config, add_devices, discovery_info=None):\n+    \"\"\"Sensor platform setup.\"\"\"\n+    accounts = config.get(CONF_ACCOUNTS)\n+\n+    sensors = []\n+    for account in accounts:\n+        for balance_type in account[CONF_BALANCE_TYPES]:\n+            sensors.append(StarlingBalanceSensor(\n+                account[CONF_ACCESS_TOKEN],\n+                account[CONF_NAME],\n+                balance_type\n+            ))\n+    add_devices(sensors, True)  # True forces an update upon platform setup.", "msg": "You should check that the Access token is valid during the setup of the platform. If not then fail and log the error. Otherwise will be users end up with a non-working platform in their instance."}
{"old_hunk": "@@ -1042,10 +1043,13 @@ def call(self, domain: str, service: str,\n         This method is a coroutine.\n         \"\"\"\n         context = context or Context()\n+        self._call_id += 1\n+        call_id = self._call_id", "hunk": "@@ -1043,8 +1043,7 @@ class ServiceRegistry:\n         This method is a coroutine.\n         \"\"\"\n         context = context or Context()\n-        self._call_id += 1\n-        call_id = self._call_id\n+        call_id = uuid.uuid4().hex\n         event_data = {\n             ATTR_DOMAIN: domain.lower(),\n             ATTR_SERVICE: service.lower(),\n", "comment": "We should use uuid4. Because we can restore the context object and the caller ID is not equal anymore.", "ids": [318622, "e4618ef75df43ecac73b0485b75516a82d98912a", "edf431d028b53b30f8c4958339268a3279b5375b"], "repo": "home-assistant-core", "ghid": 16558, "old": "         This method is a coroutine.\n         \"\"\"\n         context = context or Context()\n-        self._call_id += 1\n-        call_id = self._call_id\n         event_data = {\n             ATTR_DOMAIN: domain.lower(),\n             ATTR_SERVICE: service.lower(),", "new": "         This method is a coroutine.\n         \"\"\"\n         context = context or Context()\n+        call_id = uuid.uuid4().hex\n         event_data = {\n             ATTR_DOMAIN: domain.lower(),\n             ATTR_SERVICE: service.lower(),", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -1042,10 +1043,13 @@ def call(self, domain: str, service: str,\n         This method is a coroutine.\n         \"\"\"\n         context = context or Context()\n+        self._call_id += 1\n+        call_id = self._call_id", "msg": "We should use uuid4. Because we can restore the context object and the caller ID is not equal anymore."}
{"old_hunk": "@@ -445,3 +468,41 @@ def websocket_create_long_lived_access_token(\n \n     hass.async_create_task(\n         async_create_long_lived_access_token(connection.user))\n+\n+\n+@websocket_api.ws_require_user()\n+@callback\n+def websocket_refresh_tokens(\n+        hass: HomeAssistant, connection: websocket_api.ActiveConnection, msg):\n+    \"\"\"Return metadata of users refresh tokens.\"\"\"\n+    connection.to_write.put_nowait(websocket_api.result_message(msg['id'], [{\n+        'id': refresh.id,\n+        'client_id': refresh.client_id,\n+        'client_name': refresh.client_name,\n+        'client_icon': refresh.client_icon,\n+        'type': refresh.token_type,\n+        'created_at': refresh.created_at,\n+    } for refresh in connection.user.refresh_tokens.values()]))\n+\n+\n+@websocket_api.ws_require_user()\n+@callback\n+def websocket_delete_refresh_token(\n+        hass: HomeAssistant, connection: websocket_api.ActiveConnection, msg):\n+    \"\"\"Handle a delete refresh token request.\"\"\"\n+    async def async_delete_refresh_token(user, refresh_token_id):\n+        \"\"\"Delete a refresh token.\"\"\"\n+        refresh_token = await hass.auth.async_get_refresh_token(", "hunk": "@@ -492,10 +492,9 @@ def websocket_delete_refresh_token(\n     \"\"\"Handle a delete refresh token request.\"\"\"\n     async def async_delete_refresh_token(user, refresh_token_id):\n         \"\"\"Delete a refresh token.\"\"\"\n-        refresh_token = await hass.auth.async_get_refresh_token(\n-            refresh_token_id)\n+        refresh_token = connection.user.refresh_tokens.get(refresh_token_id)\n \n-        if refresh_token is None or refresh_token.user != user:\n+        if refresh_token is None:\n             return websocket_api.error_message(\n                 msg['id'], 'invalid_token_id', 'Received invalid token')\n \n", "comment": "Can we do ```python refresh_token = connection.user.refresh_tokens.get(refresh_token_id) ```", "ids": [318625, "dd98e28bd56d468ebb910bc2abc49387c70286e1", "268215bc8811b84bf49d2bababd745f32a23df53"], "repo": "home-assistant-core", "ghid": 16559, "old": "     \"\"\"Handle a delete refresh token request.\"\"\"\n     async def async_delete_refresh_token(user, refresh_token_id):\n         \"\"\"Delete a refresh token.\"\"\"\n-        refresh_token = await hass.auth.async_get_refresh_token(\n-            refresh_token_id)\n-        if refresh_token is None or refresh_token.user != user:\n             return websocket_api.error_message(\n                 msg['id'], 'invalid_token_id', 'Received invalid token')", "new": "     \"\"\"Handle a delete refresh token request.\"\"\"\n     async def async_delete_refresh_token(user, refresh_token_id):\n         \"\"\"Delete a refresh token.\"\"\"\n+        refresh_token = connection.user.refresh_tokens.get(refresh_token_id)\n+        if refresh_token is None:\n             return websocket_api.error_message(\n                 msg['id'], 'invalid_token_id', 'Received invalid token')", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -445,3 +468,41 @@ def websocket_create_long_lived_access_token(\n \n     hass.async_create_task(\n         async_create_long_lived_access_token(connection.user))\n+\n+\n+@websocket_api.ws_require_user()\n+@callback\n+def websocket_refresh_tokens(\n+        hass: HomeAssistant, connection: websocket_api.ActiveConnection, msg):\n+    \"\"\"Return metadata of users refresh tokens.\"\"\"\n+    connection.to_write.put_nowait(websocket_api.result_message(msg['id'], [{\n+        'id': refresh.id,\n+        'client_id': refresh.client_id,\n+        'client_name': refresh.client_name,\n+        'client_icon': refresh.client_icon,\n+        'type': refresh.token_type,\n+        'created_at': refresh.created_at,\n+    } for refresh in connection.user.refresh_tokens.values()]))\n+\n+\n+@websocket_api.ws_require_user()\n+@callback\n+def websocket_delete_refresh_token(\n+        hass: HomeAssistant, connection: websocket_api.ActiveConnection, msg):\n+    \"\"\"Handle a delete refresh token request.\"\"\"\n+    async def async_delete_refresh_token(user, refresh_token_id):\n+        \"\"\"Delete a refresh token.\"\"\"\n+        refresh_token = await hass.auth.async_get_refresh_token(", "msg": "Can we do ```python refresh_token = connection.user.refresh_tokens.get(refresh_token_id) ```"}
{"old_hunk": "@@ -214,10 +220,48 @@ def _on_disconnect(self):\n                                                segment_type=hangouts_pb2.\n                                                SEGMENT_TYPE_LINE_BREAK))\n \n+        image_file = None\n+        if data and data.get('image') is not None:\n+            uri = data.get('image')\n+            validate = urlparse(uri)\n+            if validate.scheme:\n+                try:\n+                    websession = async_get_clientsession(self.hass)\n+                    with async_timeout.timeout(5, loop=self.hass.loop):\n+                        response = await websession.get(uri)", "hunk": "@@ -227,8 +227,7 @@ class HangoutsBot:\n             if validate.scheme:\n                 try:\n                     websession = async_get_clientsession(self.hass)\n-                    with async_timeout.timeout(5, loop=self.hass.loop):\n-                        response = await websession.get(uri)\n+                    async with websession.get(uri, timeout=5) as response:\n                         if response.status != 200:\n                             _LOGGER.error(\n                                 'Fetch image failed, %s, %s',\n", "comment": "That is the old style of timeout handling. Please use the new one with `async with websession.get(uri, timeout=5) as resp`", "ids": [318651, "c291f383336d07ef22b5e6bc9e42dac71940e661", "a244eb8b63e220f2b21a13e7ccb0b7d6e6d97a0a"], "repo": "home-assistant-core", "ghid": 16560, "old": "             if validate.scheme:\n                 try:\n                     websession = async_get_clientsession(self.hass)\n-                    with async_timeout.timeout(5, loop=self.hass.loop):\n-                        response = await websession.get(uri)\n                         if response.status != 200:\n                             _LOGGER.error(\n                                 'Fetch image failed, %s, %s',", "new": "             if validate.scheme:\n                 try:\n                     websession = async_get_clientsession(self.hass)\n+                    async with websession.get(uri, timeout=5) as response:\n                         if response.status != 200:\n                             _LOGGER.error(\n                                 'Fetch image failed, %s, %s',", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -214,10 +220,48 @@ def _on_disconnect(self):\n                                                segment_type=hangouts_pb2.\n                                                SEGMENT_TYPE_LINE_BREAK))\n \n+        image_file = None\n+        if data and data.get('image') is not None:\n+            uri = data.get('image')\n+            validate = urlparse(uri)\n+            if validate.scheme:\n+                try:\n+                    websession = async_get_clientsession(self.hass)\n+                    with async_timeout.timeout(5, loop=self.hass.loop):\n+                        response = await websession.get(uri)", "msg": "That is the old style of timeout handling. Please use the new one with `async with websession.get(uri, timeout=5) as resp`"}
{"old_hunk": "@@ -4,7 +4,6 @@\n For more details about this platform, please refer to the documentation at\n https://home-assistant.io/components/mailbox.asteriskvm/\n \"\"\"\n-import asyncio\n import logging\n \n from homeassistant.components.asterisk_mbox import DOMAIN", "hunk": "@@ -6,7 +6,7 @@ https://home-assistant.io/components/mailbox.asteriskvm/\n \"\"\"\n import logging\n \n-from homeassistant.components.asterisk_mbox import DOMAIN\n+from homeassistant.components.asterisk_mbox import DOMAIN as ASTERISK_DOMAIN\n from homeassistant.components.mailbox import (\n     CONTENT_TYPE_MPEG, Mailbox, StreamError)\n from homeassistant.core import callback\n", "comment": "Please import this as another name too.", "ids": [318833, "31b0f9749fc15ebed939941f94ac423290a2bc9e", "2f80578e3a3bb46477b0483e2fb15c97246f0ad7"], "repo": "home-assistant-core", "ghid": 16579, "old": " \"\"\"\n import logging\n-from homeassistant.components.asterisk_mbox import DOMAIN\n from homeassistant.components.mailbox import (\n     CONTENT_TYPE_MPEG, Mailbox, StreamError)\n from homeassistant.core import callback", "new": " \"\"\"\n import logging\n+from homeassistant.components.asterisk_mbox import DOMAIN as ASTERISK_DOMAIN\n from homeassistant.components.mailbox import (\n     CONTENT_TYPE_MPEG, Mailbox, StreamError)\n from homeassistant.core import callback", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -4,7 +4,6 @@\n For more details about this platform, please refer to the documentation at\n https://home-assistant.io/components/mailbox.asteriskvm/\n \"\"\"\n-import asyncio\n import logging\n \n from homeassistant.components.asterisk_mbox import DOMAIN", "msg": "Please import this as another name too."}
{"old_hunk": "@@ -0,0 +1,47 @@\n+\"\"\"HTTP views to interact with the device registry.\"\"\"\n+import voluptuous as vol\n+\n+from homeassistant.core import callback\n+from homeassistant.helpers.device_registry import async_get_registry\n+from homeassistant.components import websocket_api\n+\n+DEPENDENCIES = ['websocket_api']\n+\n+WS_TYPE_LIST = 'config/device_registry/list'\n+SCHEMA_WS_LIST = websocket_api.BASE_COMMAND_MESSAGE_SCHEMA.extend({\n+    vol.Required('type'): WS_TYPE_LIST,\n+})\n+\n+\n+async def async_setup(hass):\n+    \"\"\"Enable the Entity Registry views.\"\"\"\n+    hass.components.websocket_api.async_register_command(\n+        WS_TYPE_LIST, websocket_list_devices,\n+        SCHEMA_WS_LIST\n+    )\n+    return True\n+\n+\n+@callback\n+def websocket_list_devices(hass, connection, msg):\n+    \"\"\"Handle list devices command.\n+\n+    Async friendly.\n+    \"\"\"\n+    async def retrieve_entities():\n+        \"\"\"Get devices from registry.\"\"\"\n+        registry = await async_get_registry(hass)\n+        connection.send_message_outside(websocket_api.result_message(\n+            msg['id'], [{\n+                'config_entries': list(entry.config_entries),\n+                'connections': list(entry.connections),\n+                'identifiers': list(entry.identifiers),", "hunk": "@@ -35,7 +35,6 @@ def websocket_list_devices(hass, connection, msg):\n             msg['id'], [{\n                 'config_entries': list(entry.config_entries),\n                 'connections': list(entry.connections),\n-                'identifiers': list(entry.identifiers),\n                 'manufacturer': entry.manufacturer,\n                 'model': entry.model,\n                 'name': entry.name,\n", "comment": "Are connections and identifiers really relevant to expose to the Gui? Config entries I can understand to tie be able to tie them together in some hierarchy but the others are mostly internal usage.", "ids": [318863, "25d29f8cbe7aa632f5cea21e626bd96dac648035", "c56e839f5cb84f5582d7c4a8f198450a16ef1318"], "repo": "home-assistant-core", "ghid": 16597, "old": "             msg['id'], [{\n                 'config_entries': list(entry.config_entries),\n                 'connections': list(entry.connections),\n-                'identifiers': list(entry.identifiers),\n                 'manufacturer': entry.manufacturer,\n                 'model': entry.model,\n                 'name': entry.name,", "new": "             msg['id'], [{\n                 'config_entries': list(entry.config_entries),\n                 'connections': list(entry.connections),\n                 'manufacturer': entry.manufacturer,\n                 'model': entry.model,\n                 'name': entry.name,", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -0,0 +1,47 @@\n+\"\"\"HTTP views to interact with the device registry.\"\"\"\n+import voluptuous as vol\n+\n+from homeassistant.core import callback\n+from homeassistant.helpers.device_registry import async_get_registry\n+from homeassistant.components import websocket_api\n+\n+DEPENDENCIES = ['websocket_api']\n+\n+WS_TYPE_LIST = 'config/device_registry/list'\n+SCHEMA_WS_LIST = websocket_api.BASE_COMMAND_MESSAGE_SCHEMA.extend({\n+    vol.Required('type'): WS_TYPE_LIST,\n+})\n+\n+\n+async def async_setup(hass):\n+    \"\"\"Enable the Entity Registry views.\"\"\"\n+    hass.components.websocket_api.async_register_command(\n+        WS_TYPE_LIST, websocket_list_devices,\n+        SCHEMA_WS_LIST\n+    )\n+    return True\n+\n+\n+@callback\n+def websocket_list_devices(hass, connection, msg):\n+    \"\"\"Handle list devices command.\n+\n+    Async friendly.\n+    \"\"\"\n+    async def retrieve_entities():\n+        \"\"\"Get devices from registry.\"\"\"\n+        registry = await async_get_registry(hass)\n+        connection.send_message_outside(websocket_api.result_message(\n+            msg['id'], [{\n+                'config_entries': list(entry.config_entries),\n+                'connections': list(entry.connections),\n+                'identifiers': list(entry.identifiers),", "msg": "Are connections and identifiers really relevant to expose to the Gui? Config entries I can understand to tie be able to tie them together in some hierarchy but the others are mostly internal usage."}
{"old_hunk": "@@ -321,13 +351,24 @@ def block_till_done(self) -> None:\n \n     def stop(self) -> None:\n         \"\"\"Stop Home Assistant and shuts down all threads.\"\"\"\n+        if self.state == CoreState.not_running:  # just ignore\n+            return\n         fire_coroutine_threadsafe(self.async_stop(), self.loop)\n \n     async def async_stop(self, exit_code: int = 0) -> None:\n         \"\"\"Stop Home Assistant and shuts down all threads.\n \n         This method is a coroutine.\n         \"\"\"\n+        if self.state == CoreState.not_running:  # just ignore\n+            return\n+        if self.state == CoreState.stopping:\n+            _LOGGER.info(\"async_stop called twice: ignored\")", "hunk": "@@ -367,7 +367,7 @@ class HomeAssistant:\n             return\n         if self.state == CoreState.starting:\n             # This may not work\n-            _LOGGER.warn(\"async_stop called before startup is complete\")\n+            _LOGGER.warning(\"async_stop called before startup is complete\")\n \n         # stage 1\n         self.state = CoreState.stopping\n", "comment": "Should this be a warning?", "ids": [318970, "79161c620646b38a9af3e9c7c59887260c369a96", "1377fbda76ba4bf7d5773a1fda01b140db11735b"], "repo": "home-assistant-core", "ghid": 16617, "old": "             return\n         if self.state == CoreState.starting:\n             # This may not work\n-            _LOGGER.warn(\"async_stop called before startup is complete\")\n         # stage 1\n         self.state = CoreState.stopping", "new": "             return\n         if self.state == CoreState.starting:\n             # This may not work\n+            _LOGGER.warning(\"async_stop called before startup is complete\")\n         # stage 1\n         self.state = CoreState.stopping", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -321,13 +351,24 @@ def block_till_done(self) -> None:\n \n     def stop(self) -> None:\n         \"\"\"Stop Home Assistant and shuts down all threads.\"\"\"\n+        if self.state == CoreState.not_running:  # just ignore\n+            return\n         fire_coroutine_threadsafe(self.async_stop(), self.loop)\n \n     async def async_stop(self, exit_code: int = 0) -> None:\n         \"\"\"Stop Home Assistant and shuts down all threads.\n \n         This method is a coroutine.\n         \"\"\"\n+        if self.state == CoreState.not_running:  # just ignore\n+            return\n+        if self.state == CoreState.stopping:\n+            _LOGGER.info(\"async_stop called twice: ignored\")", "msg": "Should this be a warning?"}
{"old_hunk": "@@ -30,6 +30,15 @@\n }\n \n \n+try:", "hunk": "@@ -30,15 +30,6 @@ TBL_SLUGIFY = {\n }\n \n \n-try:\n-    asyncio_run = asyncio.run\n-except AttributeError:\n-    def asyncio_run(main, *, debug=False):\n-        loop = asyncio.new_event_loop()\n-        loop.set_debug(debug)\n-        return loop.run(main)\n-\n-\n def sanitize_filename(filename: str) -> str:\n     r\"\"\"Sanitize a filename by removing .. / and \\\\.\"\"\"\n     return RE_SANITIZE_FILENAME.sub(\"\", filename)\n", "comment": "Can we move this to `util.async_`", "ids": [318971, "79161c620646b38a9af3e9c7c59887260c369a96", "84ab2d9a1d160a3c9da7e47731cf27afad4051a5"], "repo": "home-assistant-core", "ghid": 16617, "old": " }\n-try:\n-    asyncio_run = asyncio.run\n-except AttributeError:\n-    def asyncio_run(main, *, debug=False):\n-        loop = asyncio.new_event_loop()\n-        loop.set_debug(debug)\n-        return loop.run(main)\n-\n-\n def sanitize_filename(filename: str) -> str:\n     r\"\"\"Sanitize a filename by removing .. / and \\\\.\"\"\"\n     return RE_SANITIZE_FILENAME.sub(\"\", filename)", "new": " }\n def sanitize_filename(filename: str) -> str:\n     r\"\"\"Sanitize a filename by removing .. / and \\\\.\"\"\"\n     return RE_SANITIZE_FILENAME.sub(\"\", filename)", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -30,6 +30,15 @@\n }\n \n \n+try:", "msg": "Can we move this to `util.async_`"}
{"old_hunk": "@@ -21,8 +21,6 @@\n \n def attempt_use_uvloop() -> None:\n     \"\"\"Attempt to use uvloop.\"\"\"\n-    import asyncio", "hunk": "@@ -21,11 +21,13 @@ from homeassistant.const import (\n \n def attempt_use_uvloop() -> None:\n     \"\"\"Attempt to use uvloop.\"\"\"\n+    import asyncio\n     try:\n         import uvloop\n-        asyncio.set_event_loop_policy(uvloop.EventLoopPolicy())\n     except ImportError:\n         pass\n+    else:\n+        asyncio.set_event_loop_policy(uvloop.EventLoopPolicy())\n \n \n def validate_python() -> None:\n", "comment": "Now the import is missing?", "ids": [318969, "79161c620646b38a9af3e9c7c59887260c369a96", "0f0d5c2e332e36274e5cae7843fe59f27de30e6c"], "repo": "home-assistant-core", "ghid": 16617, "old": " def attempt_use_uvloop() -> None:\n     \"\"\"Attempt to use uvloop.\"\"\"\n     try:\n         import uvloop\n-        asyncio.set_event_loop_policy(uvloop.EventLoopPolicy())\n     except ImportError:\n         pass\n def validate_python() -> None:", "new": " def attempt_use_uvloop() -> None:\n     \"\"\"Attempt to use uvloop.\"\"\"\n+    import asyncio\n     try:\n         import uvloop\n     except ImportError:\n         pass\n+    else:\n+        asyncio.set_event_loop_policy(uvloop.EventLoopPolicy())\n def validate_python() -> None:", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -21,8 +21,6 @@\n \n def attempt_use_uvloop() -> None:\n     \"\"\"Attempt to use uvloop.\"\"\"\n-    import asyncio", "msg": "Now the import is missing?"}
{"old_hunk": "@@ -0,0 +1,48 @@\n+\"\"\"Huawei LTE component tests.\"\"\"\n+import pytest\n+\n+from homeassistant.components import huawei_lte\n+\n+\n+@pytest.fixture(autouse=True)\n+def routerdata():\n+    \"\"\"Set up a router data for testing.\"\"\"\n+    rd = huawei_lte.RouterData(None)\n+    rd.device_information = dict(", "hunk": "@@ -8,10 +8,10 @@ from homeassistant.components import huawei_lte\n def routerdata():\n     \"\"\"Set up a router data for testing.\"\"\"\n     rd = huawei_lte.RouterData(None)\n-    rd.device_information = dict(\n-        SoftwareVersion=\"1.0\",\n-        nested=dict(foo=\"bar\"),\n-    )\n+    rd.device_information = {\n+        'SoftwareVersion': '1.0',\n+        'nested': {'foo': 'bar'},\n+    }\n     return rd\n \n \n", "comment": "We prefer to use `{key: value}` to define a dictionary.", "ids": [318998, "7afb5ca7eb700eda85b60c59d11956e8eab41d15", "8ce4ab8ce0d22370cae39fc0d6b6713186e4854e"], "repo": "home-assistant-core", "ghid": 16626, "old": " def routerdata():\n     \"\"\"Set up a router data for testing.\"\"\"\n     rd = huawei_lte.RouterData(None)\n-    rd.device_information = dict(\n-        SoftwareVersion=\"1.0\",\n-        nested=dict(foo=\"bar\"),\n-    )\n     return rd", "new": " def routerdata():\n     \"\"\"Set up a router data for testing.\"\"\"\n     rd = huawei_lte.RouterData(None)\n+    rd.device_information = {\n+        'SoftwareVersion': '1.0',\n+        'nested': {'foo': 'bar'},\n+    }\n     return rd", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -0,0 +1,48 @@\n+\"\"\"Huawei LTE component tests.\"\"\"\n+import pytest\n+\n+from homeassistant.components import huawei_lte\n+\n+\n+@pytest.fixture(autouse=True)\n+def routerdata():\n+    \"\"\"Set up a router data for testing.\"\"\"\n+    rd = huawei_lte.RouterData(None)\n+    rd.device_information = dict(", "msg": "We prefer to use `{key: value}` to define a dictionary."}
{"old_hunk": "@@ -91,3 +94,17 @@ def device_class(self):\n     def is_on(self):\n         \"\"\"Return true if smoke is detected.\"\"\"\n         return self._device.smokeDetectorAlarmType != STATE_SMOKE_OFF\n+\n+\n+class HomematicipWaterDetector(HomematicipGenericDevice, BinarySensorDevice):\n+    \"\"\"Representation of a HomematicIP Cloud water detector detector.\"\"\"", "hunk": "@@ -97,7 +97,7 @@ class HomematicipSmokeDetector(HomematicipGenericDevice, BinarySensorDevice):\n \n \n class HomematicipWaterDetector(HomematicipGenericDevice, BinarySensorDevice):\n-    \"\"\"Representation of a HomematicIP Cloud water detector detector.\"\"\"\n+    \"\"\"Representation of a HomematicIP Cloud water detector.\"\"\"\n \n     @property\n     def device_class(self):\n", "comment": "Should it be two \"detector\"?", "ids": [319045, "6a51e31b65c08cbf5b08625ffee92d98a1dff9cd", "12819616e9e8418b3eb5e0b48c35e8d28ccd84fa"], "repo": "home-assistant-core", "ghid": 16636, "old": " class HomematicipWaterDetector(HomematicipGenericDevice, BinarySensorDevice):\n-    \"\"\"Representation of a HomematicIP Cloud water detector detector.\"\"\"\n     @property\n     def device_class(self):", "new": " class HomematicipWaterDetector(HomematicipGenericDevice, BinarySensorDevice):\n+    \"\"\"Representation of a HomematicIP Cloud water detector.\"\"\"\n     @property\n     def device_class(self):", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -91,3 +94,17 @@ def device_class(self):\n     def is_on(self):\n         \"\"\"Return true if smoke is detected.\"\"\"\n         return self._device.smokeDetectorAlarmType != STATE_SMOKE_OFF\n+\n+\n+class HomematicipWaterDetector(HomematicipGenericDevice, BinarySensorDevice):\n+    \"\"\"Representation of a HomematicIP Cloud water detector detector.\"\"\"", "msg": "Should it be two \"detector\"?"}
{"old_hunk": "@@ -97,6 +98,11 @@ def is_closed(self):\n         \"\"\"Return true if cover is closed, else False.\"\"\"\n         return MYQ_TO_HASS[self._status] == STATE_CLOSED\n \n+    @property\n+    def is_open(self):", "hunk": "@@ -98,11 +98,6 @@ class MyQDevice(CoverDevice):\n         \"\"\"Return true if cover is closed, else False.\"\"\"\n         return MYQ_TO_HASS[self._status] == STATE_CLOSED\n \n-    @property\n-    def is_open(self):\n-        \"\"\"Return true if cover is open, else False.\"\"\"\n-        return MYQ_TO_HASS[self._status] == STATE_OPEN\n-\n     @property\n     def is_closing(self):\n         \"\"\"Return if the cover is closing or not.\"\"\"\n", "comment": "This property doesn't exist in the base class. Just remove this. It's enough to extend the dict above.", "ids": [319135, "36485c3fb9634a5bd4d67696a385c5339e3de0f1", "dc86a328ef8a2d625ae5b810f85ad5b238e0434e"], "repo": "home-assistant-core", "ghid": 16681, "old": "         \"\"\"Return true if cover is closed, else False.\"\"\"\n         return MYQ_TO_HASS[self._status] == STATE_CLOSED\n-    @property\n-    def is_open(self):\n-        \"\"\"Return true if cover is open, else False.\"\"\"\n-        return MYQ_TO_HASS[self._status] == STATE_OPEN\n-\n     @property\n     def is_closing(self):\n         \"\"\"Return if the cover is closing or not.\"\"\"", "new": "         \"\"\"Return true if cover is closed, else False.\"\"\"\n         return MYQ_TO_HASS[self._status] == STATE_CLOSED\n     @property\n     def is_closing(self):\n         \"\"\"Return if the cover is closing or not.\"\"\"", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -97,6 +98,11 @@ def is_closed(self):\n         \"\"\"Return true if cover is closed, else False.\"\"\"\n         return MYQ_TO_HASS[self._status] == STATE_CLOSED\n \n+    @property\n+    def is_open(self):", "msg": "This property doesn't exist in the base class. Just remove this. It's enough to extend the dict above."}
{"old_hunk": "@@ -240,6 +240,7 @@ def _data_to_save(self):\n                 'unique_id': entry.unique_id,\n                 'platform': entry.platform,\n                 'name': entry.name,\n+                'disabled_by': entry.disabled_by", "hunk": "@@ -240,7 +240,7 @@ class EntityRegistry:\n                 'unique_id': entry.unique_id,\n                 'platform': entry.platform,\n                 'name': entry.name,\n-                'disabled_by': entry.disabled_by\n+                'disabled_by': entry.disabled_by,\n             } for entry in self.entities.values()\n         ]\n \n", "comment": "Maybe add a trailing comma so the next addition can also be a single-line diff.", "ids": [319146, "890fc916309e063417c8d6380399d0460e495c03", "6d911b8833019a5cbe4b72d0a29781731ef2b6c2"], "repo": "home-assistant-core", "ghid": 16699, "old": "                 'unique_id': entry.unique_id,\n                 'platform': entry.platform,\n                 'name': entry.name,\n-                'disabled_by': entry.disabled_by\n             } for entry in self.entities.values()\n         ]", "new": "                 'unique_id': entry.unique_id,\n                 'platform': entry.platform,\n                 'name': entry.name,\n+                'disabled_by': entry.disabled_by,\n             } for entry in self.entities.values()\n         ]", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -240,6 +240,7 @@ def _data_to_save(self):\n                 'unique_id': entry.unique_id,\n                 'platform': entry.platform,\n                 'name': entry.name,\n+                'disabled_by': entry.disabled_by", "msg": "Maybe add a trailing comma so the next addition can also be a single-line diff."}
{"old_hunk": "@@ -36,7 +36,9 @@\n     from zigpy.zcl.clusters.smartenergy import Metering\n     from zigpy.zcl.clusters.homeautomation import ElectricalMeasurement\n     in_clusters = discovery_info['in_clusters']\n-    if RelativeHumidity.cluster_id in in_clusters:\n+    if 'sub_component' in discovery_info:\n+        sensor = globals()[discovery_info['sub_component']](**discovery_info)", "hunk": "@@ -37,7 +37,7 @@ async def make_sensor(discovery_info):\n     from zigpy.zcl.clusters.homeautomation import ElectricalMeasurement\n     in_clusters = discovery_info['in_clusters']\n     if 'sub_component' in discovery_info:\n-        sensor = globals()[discovery_info['sub_component']](**discovery_info)\n+        sensor = discovery_info['sub_component'](**discovery_info)\n     elif RelativeHumidity.cluster_id in in_clusters:\n         sensor = RelativeHumiditySensor(**discovery_info)\n     elif TemperatureMeasurement.cluster_id in in_clusters:\n", "comment": "Whatever it does, it's too much magic.", "ids": [319164, "b88ae837a9ebaa01445f1d6c92626b754e031dd9", "d5dabe1e2960f60320632fbb53016b205be2b65e"], "repo": "home-assistant-core", "ghid": 16714, "old": "     from zigpy.zcl.clusters.homeautomation import ElectricalMeasurement\n     in_clusters = discovery_info['in_clusters']\n     if 'sub_component' in discovery_info:\n-        sensor = globals()[discovery_info['sub_component']](**discovery_info)\n     elif RelativeHumidity.cluster_id in in_clusters:\n         sensor = RelativeHumiditySensor(**discovery_info)\n     elif TemperatureMeasurement.cluster_id in in_clusters:", "new": "     from zigpy.zcl.clusters.homeautomation import ElectricalMeasurement\n     in_clusters = discovery_info['in_clusters']\n     if 'sub_component' in discovery_info:\n+        sensor = discovery_info['sub_component'](**discovery_info)\n     elif RelativeHumidity.cluster_id in in_clusters:\n         sensor = RelativeHumiditySensor(**discovery_info)\n     elif TemperatureMeasurement.cluster_id in in_clusters:", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -36,7 +36,9 @@\n     from zigpy.zcl.clusters.smartenergy import Metering\n     from zigpy.zcl.clusters.homeautomation import ElectricalMeasurement\n     in_clusters = discovery_info['in_clusters']\n-    if RelativeHumidity.cluster_id in in_clusters:\n+    if 'sub_component' in discovery_info:\n+        sensor = globals()[discovery_info['sub_component']](**discovery_info)", "msg": "Whatever it does, it's too much magic."}
{"old_hunk": "@@ -330,3 +319,91 @@ def test_resend_confirm_view_unknown_error(mock_cognito, cloud_client):\n         'email': 'hello@bla.com',\n     })\n     assert req.status == 502\n+\n+\n+async def test_websocket_status(hass, hass_ws_client):\n+    \"\"\"Test querying the status.\"\"\"\n+    hass.data[DOMAIN].id_token = jwt.encode({\n+        'email': 'hello@home-assistant.io',\n+        'custom:sub-exp': '2018-01-03'\n+    }, 'test')\n+    hass.data[DOMAIN].iot.state = iot.STATE_CONNECTED\n+    client = await hass_ws_client(hass)\n+    await client.send_json({\n+        'id': 5,\n+        'type': 'cloud/status'\n+    })\n+    response = await client.receive_json()\n+    assert response['result'] == {\n+        'logged_in': True,\n+        'email': 'hello@home-assistant.io',\n+        'cloud': 'connected',\n+    }\n+\n+\n+async def test_websocket_status_not_logged_in(hass, hass_ws_client):\n+    \"\"\"Test querying the status.\"\"\"\n+    client = await hass_ws_client(hass)\n+    await client.send_json({\n+        'id': 5,\n+        'type': 'cloud/status'\n+    })\n+    response = await client.receive_json()\n+    assert response['result'] == {\n+        'logged_in': False,\n+        'cloud': 'disconnected'\n+    }\n+\n+\n+async def test_websocket_subscription(hass, hass_ws_client, aioclient_mock,\n+                                      mock_auth):\n+    \"\"\"Test querying the status.\"\"\"\n+    aioclient_mock.get(SUBSCRIPTION_INFO_URL, json={'return': 'value'})\n+    hass.data[DOMAIN].id_token = jwt.encode({\n+        'email': 'hello@home-assistant.io',\n+        'custom:sub-exp': '2018-01-03'\n+    }, 'test')\n+    client = await hass_ws_client(hass)\n+    await client.send_json({\n+        'id': 5,\n+        'type': 'cloud/subscription'\n+    })\n+    response = await client.receive_json()\n+\n+    assert response['result'] == {\n+        'return': 'value'\n+    }\n+\n+\n+async def test_websocket_subscription(hass, hass_ws_client, aioclient_mock,", "hunk": "@@ -375,8 +375,8 @@ async def test_websocket_subscription(hass, hass_ws_client, aioclient_mock,\n     }\n \n \n-async def test_websocket_subscription(hass, hass_ws_client, aioclient_mock,\n-                                      mock_auth):\n+async def test_websocket_subscription_fail(hass, hass_ws_client,\n+                                           aioclient_mock, mock_auth):\n     \"\"\"Test querying the status.\"\"\"\n     aioclient_mock.get(SUBSCRIPTION_INFO_URL, status=500)\n     hass.data[DOMAIN].id_token = jwt.encode({\n", "comment": "redefinition of unused 'test_websocket_subscription' from line 358", "ids": [319301, "9b0607fe933912023d457eb08f2b588e24b6605b", "22292f20abccc2be81987e7579f266f26f1110d2"], "repo": "home-assistant-core", "ghid": 16727, "old": "     }\n-async def test_websocket_subscription(hass, hass_ws_client, aioclient_mock,\n-                                      mock_auth):\n     \"\"\"Test querying the status.\"\"\"\n     aioclient_mock.get(SUBSCRIPTION_INFO_URL, status=500)\n     hass.data[DOMAIN].id_token = jwt.encode({", "new": "     }\n+async def test_websocket_subscription_fail(hass, hass_ws_client,\n+                                           aioclient_mock, mock_auth):\n     \"\"\"Test querying the status.\"\"\"\n     aioclient_mock.get(SUBSCRIPTION_INFO_URL, status=500)\n     hass.data[DOMAIN].id_token = jwt.encode({", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -330,3 +319,91 @@ def test_resend_confirm_view_unknown_error(mock_cognito, cloud_client):\n         'email': 'hello@bla.com',\n     })\n     assert req.status == 502\n+\n+\n+async def test_websocket_status(hass, hass_ws_client):\n+    \"\"\"Test querying the status.\"\"\"\n+    hass.data[DOMAIN].id_token = jwt.encode({\n+        'email': 'hello@home-assistant.io',\n+        'custom:sub-exp': '2018-01-03'\n+    }, 'test')\n+    hass.data[DOMAIN].iot.state = iot.STATE_CONNECTED\n+    client = await hass_ws_client(hass)\n+    await client.send_json({\n+        'id': 5,\n+        'type': 'cloud/status'\n+    })\n+    response = await client.receive_json()\n+    assert response['result'] == {\n+        'logged_in': True,\n+        'email': 'hello@home-assistant.io',\n+        'cloud': 'connected',\n+    }\n+\n+\n+async def test_websocket_status_not_logged_in(hass, hass_ws_client):\n+    \"\"\"Test querying the status.\"\"\"\n+    client = await hass_ws_client(hass)\n+    await client.send_json({\n+        'id': 5,\n+        'type': 'cloud/status'\n+    })\n+    response = await client.receive_json()\n+    assert response['result'] == {\n+        'logged_in': False,\n+        'cloud': 'disconnected'\n+    }\n+\n+\n+async def test_websocket_subscription(hass, hass_ws_client, aioclient_mock,\n+                                      mock_auth):\n+    \"\"\"Test querying the status.\"\"\"\n+    aioclient_mock.get(SUBSCRIPTION_INFO_URL, json={'return': 'value'})\n+    hass.data[DOMAIN].id_token = jwt.encode({\n+        'email': 'hello@home-assistant.io',\n+        'custom:sub-exp': '2018-01-03'\n+    }, 'test')\n+    client = await hass_ws_client(hass)\n+    await client.send_json({\n+        'id': 5,\n+        'type': 'cloud/subscription'\n+    })\n+    response = await client.receive_json()\n+\n+    assert response['result'] == {\n+        'return': 'value'\n+    }\n+\n+\n+async def test_websocket_subscription(hass, hass_ws_client, aioclient_mock,", "msg": "redefinition of unused 'test_websocket_subscription' from line 358"}
{"old_hunk": "@@ -0,0 +1,120 @@\n+\"\"\"\n+Support for AquaLogic switches.\n+\n+For more details about this platform, please refer to the documentation at\n+https://home-assistant.io/components/switch.aqualogic/\n+\"\"\"\n+import asyncio\n+import logging\n+\n+import voluptuous as vol\n+\n+import homeassistant.helpers.config_validation as cv\n+from homeassistant.core import callback\n+import homeassistant.components.aqualogic as aq\n+from homeassistant.components.switch import SwitchDevice, PLATFORM_SCHEMA\n+from homeassistant.const import (CONF_MONITORED_CONDITIONS)\n+\n+DEPENDENCIES = ['aqualogic']\n+\n+_LOGGER = logging.getLogger(__name__)\n+\n+SWITCH_TYPES = {\n+    'lights': 'Lights',\n+    'filter': 'Filter',\n+    'filter_low_speed': 'Filter Low Speed',\n+    'aux_1': 'Aux 1',\n+    'aux_2': 'Aux 2',\n+    'aux_3': 'Aux 3',\n+    'aux_4': 'Aux 4',\n+    'aux_5': 'Aux 5',\n+    'aux_6': 'Aux 6',\n+    'aux_7': 'Aux 7',\n+}\n+\n+PLATFORM_SCHEMA = PLATFORM_SCHEMA.extend({\n+    vol.Optional(CONF_MONITORED_CONDITIONS, default=list(SWITCH_TYPES)):\n+        vol.All(cv.ensure_list, [vol.In(SWITCH_TYPES)]),\n+})\n+\n+\n+@asyncio.coroutine\n+def async_setup_platform(hass, config, async_add_devices, discovery_info=None):\n+    \"\"\"Set up the switch platform.\"\"\"\n+    switches = []\n+\n+    component = hass.data[aq.DOMAIN]\n+    for switch_type in config.get(CONF_MONITORED_CONDITIONS):\n+        switches.append(AquaLogicSwitch(component, switch_type))\n+\n+    async_add_devices(switches)\n+\n+\n+class AquaLogicSwitch(SwitchDevice):\n+    \"\"\"Switch implementation for the AquaLogic component.\"\"\"\n+\n+    def __init__(self, component, switch_type):\n+        \"\"\"Initialize switch.\"\"\"\n+        from aqualogic.core import States\n+        self._component = component\n+        self._type = switch_type\n+        self._state_name = {\n+            'lights': States.LIGHTS,\n+            'filter': States.FILTER,\n+            'filter_low_speed': States.FILTER_LOW_SPEED,\n+            'aux_1': States.AUX_1,\n+            'aux_2': States.AUX_2,\n+            'aux_3': States.AUX_3,\n+            'aux_4': States.AUX_4,\n+            'aux_5': States.AUX_5,\n+            'aux_6': States.AUX_6,\n+            'aux_7': States.AUX_7\n+        }[switch_type]\n+\n+    @property\n+    def name(self):\n+        \"\"\"Return the name of the switch.\"\"\"\n+        return \"AquaLogic {}\".format(SWITCH_TYPES[self._type])\n+\n+    @property\n+    def should_poll(self):\n+        \"\"\"Return the polling state.\"\"\"\n+        return False\n+\n+    @property\n+    def is_on(self):\n+        \"\"\"Return true if device is on.\"\"\"\n+        panel = self._component.panel\n+        if panel is None:\n+            return False\n+        state = panel.get_state(self._state_name)\n+        return state\n+\n+    def turn_on(self, **kwargs):\n+        \"\"\"Turn the device on.\"\"\"\n+        if self.is_on:", "hunk": "@@ -10,7 +10,7 @@\n     <h1>Error 503 Backend is unhealthy</h1>\n     <p>Backend is unhealthy</p>\n     <h3>Guru Mediation:</h3>\n-    <p>Details: cache-sea4436-SEA 1645537808 1026399156</p>\n+    <p>Details: cache-sea4434-SEA 1645537808 1993474614</p>\n     <hr>\n     <p>Varnish cache server</p>\n   </body>\n", "comment": "We usually don't guard from this. If home assistant would have the wrong state the user would never be able to ask the device to change state. Is this necessary due to how the device works?", "ids": [319402, "59663a2c17c8d95a2ccdd26bba7f170ef1660e82", "7217ec1676771ec36096959b13eb5bf5b5659a5d"], "repo": "home-assistant-core", "ghid": 16763, "old": "     <h1>Error 503 Backend is unhealthy</h1>\n     <p>Backend is unhealthy</p>\n     <h3>Guru Mediation:</h3>\n-    <p>Details: cache-sea4436-SEA 1645537808 1026399156</p>\n     <hr>\n     <p>Varnish cache server</p>\n   </body>", "new": "     <h1>Error 503 Backend is unhealthy</h1>\n     <p>Backend is unhealthy</p>\n     <h3>Guru Mediation:</h3>\n+    <p>Details: cache-sea4434-SEA 1645537808 1993474614</p>\n     <hr>\n     <p>Varnish cache server</p>\n   </body>", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -0,0 +1,120 @@\n+\"\"\"\n+Support for AquaLogic switches.\n+\n+For more details about this platform, please refer to the documentation at\n+https://home-assistant.io/components/switch.aqualogic/\n+\"\"\"\n+import asyncio\n+import logging\n+\n+import voluptuous as vol\n+\n+import homeassistant.helpers.config_validation as cv\n+from homeassistant.core import callback\n+import homeassistant.components.aqualogic as aq\n+from homeassistant.components.switch import SwitchDevice, PLATFORM_SCHEMA\n+from homeassistant.const import (CONF_MONITORED_CONDITIONS)\n+\n+DEPENDENCIES = ['aqualogic']\n+\n+_LOGGER = logging.getLogger(__name__)\n+\n+SWITCH_TYPES = {\n+    'lights': 'Lights',\n+    'filter': 'Filter',\n+    'filter_low_speed': 'Filter Low Speed',\n+    'aux_1': 'Aux 1',\n+    'aux_2': 'Aux 2',\n+    'aux_3': 'Aux 3',\n+    'aux_4': 'Aux 4',\n+    'aux_5': 'Aux 5',\n+    'aux_6': 'Aux 6',\n+    'aux_7': 'Aux 7',\n+}\n+\n+PLATFORM_SCHEMA = PLATFORM_SCHEMA.extend({\n+    vol.Optional(CONF_MONITORED_CONDITIONS, default=list(SWITCH_TYPES)):\n+        vol.All(cv.ensure_list, [vol.In(SWITCH_TYPES)]),\n+})\n+\n+\n+@asyncio.coroutine\n+def async_setup_platform(hass, config, async_add_devices, discovery_info=None):\n+    \"\"\"Set up the switch platform.\"\"\"\n+    switches = []\n+\n+    component = hass.data[aq.DOMAIN]\n+    for switch_type in config.get(CONF_MONITORED_CONDITIONS):\n+        switches.append(AquaLogicSwitch(component, switch_type))\n+\n+    async_add_devices(switches)\n+\n+\n+class AquaLogicSwitch(SwitchDevice):\n+    \"\"\"Switch implementation for the AquaLogic component.\"\"\"\n+\n+    def __init__(self, component, switch_type):\n+        \"\"\"Initialize switch.\"\"\"\n+        from aqualogic.core import States\n+        self._component = component\n+        self._type = switch_type\n+        self._state_name = {\n+            'lights': States.LIGHTS,\n+            'filter': States.FILTER,\n+            'filter_low_speed': States.FILTER_LOW_SPEED,\n+            'aux_1': States.AUX_1,\n+            'aux_2': States.AUX_2,\n+            'aux_3': States.AUX_3,\n+            'aux_4': States.AUX_4,\n+            'aux_5': States.AUX_5,\n+            'aux_6': States.AUX_6,\n+            'aux_7': States.AUX_7\n+        }[switch_type]\n+\n+    @property\n+    def name(self):\n+        \"\"\"Return the name of the switch.\"\"\"\n+        return \"AquaLogic {}\".format(SWITCH_TYPES[self._type])\n+\n+    @property\n+    def should_poll(self):\n+        \"\"\"Return the polling state.\"\"\"\n+        return False\n+\n+    @property\n+    def is_on(self):\n+        \"\"\"Return true if device is on.\"\"\"\n+        panel = self._component.panel\n+        if panel is None:\n+            return False\n+        state = panel.get_state(self._state_name)\n+        return state\n+\n+    def turn_on(self, **kwargs):\n+        \"\"\"Turn the device on.\"\"\"\n+        if self.is_on:", "msg": "We usually don't guard from this. If home assistant would have the wrong state the user would never be able to ask the device to change state. Is this necessary due to how the device works?"}
{"old_hunk": "@@ -0,0 +1,113 @@\n+\"\"\"\n+Support for AquaLogic sensors.\n+\n+For more details about this platform, please refer to the documentation at\n+https://home-assistant.io/components/sensor.aqualogic/\n+\"\"\"\n+import asyncio\n+import logging\n+\n+import voluptuous as vol\n+\n+from homeassistant.components.sensor import PLATFORM_SCHEMA\n+from homeassistant.const import (CONF_MONITORED_CONDITIONS,\n+                                 TEMP_CELSIUS, TEMP_FAHRENHEIT)\n+from homeassistant.core import callback\n+from homeassistant.helpers.entity import Entity\n+import homeassistant.components.aqualogic as aq\n+import homeassistant.helpers.config_validation as cv\n+\n+_LOGGER = logging.getLogger(__name__)\n+\n+DEPENDENCIES = ['aqualogic']\n+\n+TEMP_UNITS = [TEMP_CELSIUS, TEMP_FAHRENHEIT]\n+PERCENT_UNITS = ['%', '%']\n+SALT_UNITS = ['g/L', 'PPM']\n+WATT_UNITS = ['W', 'W']\n+NO_UNITS = [None, None]\n+\n+# sensor_type [ description, unit, icon ]\n+# sensor_type corresponds to property names in aqualogic.core.AquaLogic\n+SENSOR_TYPES = {\n+    'air_temp': ['Air Temperature', TEMP_UNITS, 'mdi:thermometer'],\n+    'pool_temp': ['Pool Temperature', TEMP_UNITS, 'mdi:oil-temperature'],\n+    'spa_temp': ['Spa Temperature', TEMP_UNITS, 'mdi:oil-temperature'],\n+    'pool_chlorinator': ['Pool Chlorinator', PERCENT_UNITS, 'mdi:gauge'],\n+    'spa_chlorinator': ['Spa Chlorinator', PERCENT_UNITS, 'mdi:gauge'],\n+    'salt_level': ['Salt Level', SALT_UNITS, 'mdi:gauge'],\n+    'pump_speed': ['Pump Speed', PERCENT_UNITS, 'mdi:speedometer'],\n+    'pump_power': ['Pump Power', WATT_UNITS, 'mdi:gauge'],\n+    'status': ['Status', NO_UNITS, 'mdi:alert']\n+}\n+\n+PLATFORM_SCHEMA = PLATFORM_SCHEMA.extend({\n+    vol.Required(CONF_MONITORED_CONDITIONS, default=list(SENSOR_TYPES)):\n+        vol.All(cv.ensure_list, [vol.In(SENSOR_TYPES)])\n+})\n+\n+\n+@asyncio.coroutine\n+def async_setup_platform(hass, config, async_add_devices, discovery_info=None):\n+    \"\"\"Set up the sensor platform.\"\"\"\n+    sensors = []\n+\n+    component = hass.data[aq.DOMAIN]", "hunk": "@@ -10,7 +10,7 @@\n     <h1>Error 503 Backend is unhealthy</h1>\n     <p>Backend is unhealthy</p>\n     <h3>Guru Mediation:</h3>\n-    <p>Details: cache-sea4463-SEA 1645537806 3741867971</p>\n+    <p>Details: cache-sea4422-SEA 1645537806 1464207846</p>\n     <hr>\n     <p>Varnish cache server</p>\n   </body>\n", "comment": "Rename this to `processor` or similar.", "ids": [319397, "59663a2c17c8d95a2ccdd26bba7f170ef1660e82", "7217ec1676771ec36096959b13eb5bf5b5659a5d"], "repo": "home-assistant-core", "ghid": 16763, "old": "     <h1>Error 503 Backend is unhealthy</h1>\n     <p>Backend is unhealthy</p>\n     <h3>Guru Mediation:</h3>\n-    <p>Details: cache-sea4463-SEA 1645537806 3741867971</p>\n     <hr>\n     <p>Varnish cache server</p>\n   </body>", "new": "     <h1>Error 503 Backend is unhealthy</h1>\n     <p>Backend is unhealthy</p>\n     <h3>Guru Mediation:</h3>\n+    <p>Details: cache-sea4422-SEA 1645537806 1464207846</p>\n     <hr>\n     <p>Varnish cache server</p>\n   </body>", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -0,0 +1,113 @@\n+\"\"\"\n+Support for AquaLogic sensors.\n+\n+For more details about this platform, please refer to the documentation at\n+https://home-assistant.io/components/sensor.aqualogic/\n+\"\"\"\n+import asyncio\n+import logging\n+\n+import voluptuous as vol\n+\n+from homeassistant.components.sensor import PLATFORM_SCHEMA\n+from homeassistant.const import (CONF_MONITORED_CONDITIONS,\n+                                 TEMP_CELSIUS, TEMP_FAHRENHEIT)\n+from homeassistant.core import callback\n+from homeassistant.helpers.entity import Entity\n+import homeassistant.components.aqualogic as aq\n+import homeassistant.helpers.config_validation as cv\n+\n+_LOGGER = logging.getLogger(__name__)\n+\n+DEPENDENCIES = ['aqualogic']\n+\n+TEMP_UNITS = [TEMP_CELSIUS, TEMP_FAHRENHEIT]\n+PERCENT_UNITS = ['%', '%']\n+SALT_UNITS = ['g/L', 'PPM']\n+WATT_UNITS = ['W', 'W']\n+NO_UNITS = [None, None]\n+\n+# sensor_type [ description, unit, icon ]\n+# sensor_type corresponds to property names in aqualogic.core.AquaLogic\n+SENSOR_TYPES = {\n+    'air_temp': ['Air Temperature', TEMP_UNITS, 'mdi:thermometer'],\n+    'pool_temp': ['Pool Temperature', TEMP_UNITS, 'mdi:oil-temperature'],\n+    'spa_temp': ['Spa Temperature', TEMP_UNITS, 'mdi:oil-temperature'],\n+    'pool_chlorinator': ['Pool Chlorinator', PERCENT_UNITS, 'mdi:gauge'],\n+    'spa_chlorinator': ['Spa Chlorinator', PERCENT_UNITS, 'mdi:gauge'],\n+    'salt_level': ['Salt Level', SALT_UNITS, 'mdi:gauge'],\n+    'pump_speed': ['Pump Speed', PERCENT_UNITS, 'mdi:speedometer'],\n+    'pump_power': ['Pump Power', WATT_UNITS, 'mdi:gauge'],\n+    'status': ['Status', NO_UNITS, 'mdi:alert']\n+}\n+\n+PLATFORM_SCHEMA = PLATFORM_SCHEMA.extend({\n+    vol.Required(CONF_MONITORED_CONDITIONS, default=list(SENSOR_TYPES)):\n+        vol.All(cv.ensure_list, [vol.In(SENSOR_TYPES)])\n+})\n+\n+\n+@asyncio.coroutine\n+def async_setup_platform(hass, config, async_add_devices, discovery_info=None):\n+    \"\"\"Set up the sensor platform.\"\"\"\n+    sensors = []\n+\n+    component = hass.data[aq.DOMAIN]", "msg": "Rename this to `processor` or similar."}
{"old_hunk": "@@ -0,0 +1,97 @@\n+\"\"\"\n+Support for AquaLogic component.\n+\n+For more details about this platform, please refer to the documentation at\n+https://home-assistant.io/components/aqualogic/\n+\"\"\"\n+from datetime import timedelta\n+import logging\n+import time\n+import threading\n+\n+import voluptuous as vol\n+\n+from homeassistant.const import (CONF_HOST, CONF_PORT,\n+                                 EVENT_HOMEASSISTANT_START,\n+                                 EVENT_HOMEASSISTANT_STOP)\n+from homeassistant.helpers import config_validation as cv\n+\n+REQUIREMENTS = [\"aqualogic==1.0\"]\n+\n+_LOGGER = logging.getLogger(__name__)\n+\n+DOMAIN = \"aqualogic\"\n+UPDATE_TOPIC = DOMAIN + \"_update\"\n+CONF_UNIT = \"unit\"\n+RECONNECT_INTERVAL = timedelta(seconds=10)\n+\n+CONFIG_SCHEMA = vol.Schema({\n+    DOMAIN: vol.Schema({\n+        vol.Required(CONF_HOST): cv.string,\n+        vol.Required(CONF_PORT): cv.port\n+    }),\n+}, extra=vol.ALLOW_EXTRA)\n+\n+\n+def setup(hass, base_config):\n+    \"\"\"Set up AquaLogic platform.\"\"\"\n+    config = base_config.get(DOMAIN)\n+    host = config.get(CONF_HOST)\n+    port = config.get(CONF_PORT)\n+    hass.data[DOMAIN] = AquaLogicProcessor(hass, host, port)\n+    return True\n+\n+\n+class AquaLogicProcessor(threading.Thread):\n+    \"\"\"AquaLogic event processor thread.\"\"\"\n+\n+    def __init__(self, hass, host, port):\n+        \"\"\"Initialize the data object.\"\"\"\n+        super(AquaLogicProcessor, self).__init__(daemon=True)\n+        self._hass = hass\n+        self._host = host\n+        self._port = port\n+        self._shutdown = False\n+        self._panel = None\n+\n+        hass.bus.listen_once(EVENT_HOMEASSISTANT_START,\n+                             self.start_listen)\n+        hass.bus.listen_once(EVENT_HOMEASSISTANT_STOP,\n+                             self.shutdown)\n+        _LOGGER.debug(\"AquaLogicProcessor %s:%i initialized\",", "hunk": "@@ -10,7 +10,7 @@\n     <h1>Error 503 Backend is unhealthy</h1>\n     <p>Backend is unhealthy</p>\n     <h3>Guru Mediation:</h3>\n-    <p>Details: cache-sea4436-SEA 1645537806 1026381621</p>\n+    <p>Details: cache-sea4460-SEA 1645537806 1949150010</p>\n     <hr>\n     <p>Varnish cache server</p>\n   </body>\n", "comment": "We prefer to not have any side effects in init method. We could move the event subscription and logging to `setup` function.", "ids": [319396, "59663a2c17c8d95a2ccdd26bba7f170ef1660e82", "7217ec1676771ec36096959b13eb5bf5b5659a5d"], "repo": "home-assistant-core", "ghid": 16763, "old": "     <h1>Error 503 Backend is unhealthy</h1>\n     <p>Backend is unhealthy</p>\n     <h3>Guru Mediation:</h3>\n-    <p>Details: cache-sea4436-SEA 1645537806 1026381621</p>\n     <hr>\n     <p>Varnish cache server</p>\n   </body>", "new": "     <h1>Error 503 Backend is unhealthy</h1>\n     <p>Backend is unhealthy</p>\n     <h3>Guru Mediation:</h3>\n+    <p>Details: cache-sea4460-SEA 1645537806 1949150010</p>\n     <hr>\n     <p>Varnish cache server</p>\n   </body>", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -0,0 +1,97 @@\n+\"\"\"\n+Support for AquaLogic component.\n+\n+For more details about this platform, please refer to the documentation at\n+https://home-assistant.io/components/aqualogic/\n+\"\"\"\n+from datetime import timedelta\n+import logging\n+import time\n+import threading\n+\n+import voluptuous as vol\n+\n+from homeassistant.const import (CONF_HOST, CONF_PORT,\n+                                 EVENT_HOMEASSISTANT_START,\n+                                 EVENT_HOMEASSISTANT_STOP)\n+from homeassistant.helpers import config_validation as cv\n+\n+REQUIREMENTS = [\"aqualogic==1.0\"]\n+\n+_LOGGER = logging.getLogger(__name__)\n+\n+DOMAIN = \"aqualogic\"\n+UPDATE_TOPIC = DOMAIN + \"_update\"\n+CONF_UNIT = \"unit\"\n+RECONNECT_INTERVAL = timedelta(seconds=10)\n+\n+CONFIG_SCHEMA = vol.Schema({\n+    DOMAIN: vol.Schema({\n+        vol.Required(CONF_HOST): cv.string,\n+        vol.Required(CONF_PORT): cv.port\n+    }),\n+}, extra=vol.ALLOW_EXTRA)\n+\n+\n+def setup(hass, base_config):\n+    \"\"\"Set up AquaLogic platform.\"\"\"\n+    config = base_config.get(DOMAIN)\n+    host = config.get(CONF_HOST)\n+    port = config.get(CONF_PORT)\n+    hass.data[DOMAIN] = AquaLogicProcessor(hass, host, port)\n+    return True\n+\n+\n+class AquaLogicProcessor(threading.Thread):\n+    \"\"\"AquaLogic event processor thread.\"\"\"\n+\n+    def __init__(self, hass, host, port):\n+        \"\"\"Initialize the data object.\"\"\"\n+        super(AquaLogicProcessor, self).__init__(daemon=True)\n+        self._hass = hass\n+        self._host = host\n+        self._port = port\n+        self._shutdown = False\n+        self._panel = None\n+\n+        hass.bus.listen_once(EVENT_HOMEASSISTANT_START,\n+                             self.start_listen)\n+        hass.bus.listen_once(EVENT_HOMEASSISTANT_STOP,\n+                             self.shutdown)\n+        _LOGGER.debug(\"AquaLogicProcessor %s:%i initialized\",", "msg": "We prefer to not have any side effects in init method. We could move the event subscription and logging to `setup` function."}
{"old_hunk": "@@ -0,0 +1,110 @@\n+\"\"\"\n+Support for AquaLogic component.\n+\n+For more details about this platform, please refer to the documentation at\n+https://home-assistant.io/components/aqualogic/\n+\"\"\"\n+from datetime import timedelta\n+import logging\n+import time\n+import threading\n+import socket\n+\n+import voluptuous as vol\n+\n+from homeassistant.const import (CONF_HOST, CONF_PORT,\n+                                 EVENT_HOMEASSISTANT_START,\n+                                 EVENT_HOMEASSISTANT_STOP)\n+from homeassistant.helpers import config_validation as cv\n+\n+REQUIREMENTS = [\"aqualogic==0.11\", \"zope.event==4.3.0\"]\n+\n+_LOGGER = logging.getLogger(__name__)\n+\n+DOMAIN = \"aqualogic\"\n+UPDATE_TOPIC = DOMAIN + \"_update\"\n+CONF_UNIT = \"unit\"\n+RECONNECT_INTERVAL = timedelta(seconds=10)\n+\n+CONFIG_SCHEMA = vol.Schema({\n+    DOMAIN: vol.Schema({\n+        vol.Required(CONF_HOST): cv.string,\n+        vol.Required(CONF_PORT): cv.port\n+    }),\n+}, extra=vol.ALLOW_EXTRA)\n+\n+\n+def setup(hass, base_config):\n+    \"\"\"Set up AquaLogic platform.\"\"\"\n+    config = base_config.get(DOMAIN)\n+    host = config.get(CONF_HOST)\n+    port = config.get(CONF_PORT)\n+    hass.data[DOMAIN] = AquaLogicProcessor(hass, host, port)\n+    return True\n+\n+\n+class AquaLogicProcessor(threading.Thread):\n+    \"\"\"AquaLogic event processor thread.\"\"\"\n+\n+    def __init__(self, hass, host, port):\n+        \"\"\"Initialize the data object.\"\"\"\n+        super(AquaLogicProcessor, self).__init__(daemon=True)\n+        self._hass = hass\n+        self._host = host\n+        self._port = port\n+        self._shutdown = False\n+        self._panel = None\n+\n+        hass.bus.listen_once(EVENT_HOMEASSISTANT_START,\n+                             self.start_listen)\n+        hass.bus.listen_once(EVENT_HOMEASSISTANT_STOP,\n+                             self.shutdown)\n+        _LOGGER.debug(\"AquaLogicProcessor %s:%i initialized\",\n+                      self._host, self._port)\n+\n+    def start_listen(self, event):\n+        \"\"\"Start event-processing thread.\"\"\"\n+        _LOGGER.debug(\"Event processing thread started\")\n+        self.start()\n+\n+    def shutdown(self, event):\n+        \"\"\"Signal shutdown of processing event.\"\"\"\n+        _LOGGER.debug(\"Event processing signaled exit\")\n+        self._shutdown = True\n+\n+    def data_changed(self, panel):\n+        \"\"\"Aqualogic data changed callback.\"\"\"\n+        self._hass.helpers.dispatcher.dispatcher_send(UPDATE_TOPIC)\n+\n+    def run(self):\n+        \"\"\"Event thread.\"\"\"\n+        from aqualogic.core import AquaLogic\n+        import zope.event\n+\n+        zope.event.subscribers.append(self.data_changed)\n+        while True:\n+            try:\n+                sock = socket.create_connection((self._host, self._port), 10)", "hunk": "@@ -10,7 +10,7 @@\n     <h1>Error 503 Backend is unhealthy</h1>\n     <p>Backend is unhealthy</p>\n     <h3>Guru Mediation:</h3>\n-    <p>Details: cache-sea4460-SEA 1645537805 1949139496</p>\n+    <p>Details: cache-sea4436-SEA 1645537805 1026372722</p>\n     <hr>\n     <p>Varnish cache server</p>\n   </body>\n", "comment": "Please extract integration specific I/O and data parsing into a standalone library published on pypi.", "ids": [319393, "983ce868d979b9cb0b941a0b3fa29ba7cb3ba390", "59663a2c17c8d95a2ccdd26bba7f170ef1660e82"], "repo": "home-assistant-core", "ghid": 16763, "old": "     <h1>Error 503 Backend is unhealthy</h1>\n     <p>Backend is unhealthy</p>\n     <h3>Guru Mediation:</h3>\n-    <p>Details: cache-sea4460-SEA 1645537805 1949139496</p>\n     <hr>\n     <p>Varnish cache server</p>\n   </body>", "new": "     <h1>Error 503 Backend is unhealthy</h1>\n     <p>Backend is unhealthy</p>\n     <h3>Guru Mediation:</h3>\n+    <p>Details: cache-sea4436-SEA 1645537805 1026372722</p>\n     <hr>\n     <p>Varnish cache server</p>\n   </body>", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -0,0 +1,110 @@\n+\"\"\"\n+Support for AquaLogic component.\n+\n+For more details about this platform, please refer to the documentation at\n+https://home-assistant.io/components/aqualogic/\n+\"\"\"\n+from datetime import timedelta\n+import logging\n+import time\n+import threading\n+import socket\n+\n+import voluptuous as vol\n+\n+from homeassistant.const import (CONF_HOST, CONF_PORT,\n+                                 EVENT_HOMEASSISTANT_START,\n+                                 EVENT_HOMEASSISTANT_STOP)\n+from homeassistant.helpers import config_validation as cv\n+\n+REQUIREMENTS = [\"aqualogic==0.11\", \"zope.event==4.3.0\"]\n+\n+_LOGGER = logging.getLogger(__name__)\n+\n+DOMAIN = \"aqualogic\"\n+UPDATE_TOPIC = DOMAIN + \"_update\"\n+CONF_UNIT = \"unit\"\n+RECONNECT_INTERVAL = timedelta(seconds=10)\n+\n+CONFIG_SCHEMA = vol.Schema({\n+    DOMAIN: vol.Schema({\n+        vol.Required(CONF_HOST): cv.string,\n+        vol.Required(CONF_PORT): cv.port\n+    }),\n+}, extra=vol.ALLOW_EXTRA)\n+\n+\n+def setup(hass, base_config):\n+    \"\"\"Set up AquaLogic platform.\"\"\"\n+    config = base_config.get(DOMAIN)\n+    host = config.get(CONF_HOST)\n+    port = config.get(CONF_PORT)\n+    hass.data[DOMAIN] = AquaLogicProcessor(hass, host, port)\n+    return True\n+\n+\n+class AquaLogicProcessor(threading.Thread):\n+    \"\"\"AquaLogic event processor thread.\"\"\"\n+\n+    def __init__(self, hass, host, port):\n+        \"\"\"Initialize the data object.\"\"\"\n+        super(AquaLogicProcessor, self).__init__(daemon=True)\n+        self._hass = hass\n+        self._host = host\n+        self._port = port\n+        self._shutdown = False\n+        self._panel = None\n+\n+        hass.bus.listen_once(EVENT_HOMEASSISTANT_START,\n+                             self.start_listen)\n+        hass.bus.listen_once(EVENT_HOMEASSISTANT_STOP,\n+                             self.shutdown)\n+        _LOGGER.debug(\"AquaLogicProcessor %s:%i initialized\",\n+                      self._host, self._port)\n+\n+    def start_listen(self, event):\n+        \"\"\"Start event-processing thread.\"\"\"\n+        _LOGGER.debug(\"Event processing thread started\")\n+        self.start()\n+\n+    def shutdown(self, event):\n+        \"\"\"Signal shutdown of processing event.\"\"\"\n+        _LOGGER.debug(\"Event processing signaled exit\")\n+        self._shutdown = True\n+\n+    def data_changed(self, panel):\n+        \"\"\"Aqualogic data changed callback.\"\"\"\n+        self._hass.helpers.dispatcher.dispatcher_send(UPDATE_TOPIC)\n+\n+    def run(self):\n+        \"\"\"Event thread.\"\"\"\n+        from aqualogic.core import AquaLogic\n+        import zope.event\n+\n+        zope.event.subscribers.append(self.data_changed)\n+        while True:\n+            try:\n+                sock = socket.create_connection((self._host, self._port), 10)", "msg": "Please extract integration specific I/O and data parsing into a standalone library published on pypi."}
{"old_hunk": "@@ -0,0 +1,120 @@\n+\"\"\"\n+Support for AquaLogic switches.\n+\n+For more details about this platform, please refer to the documentation at\n+https://home-assistant.io/components/switch.aqualogic/\n+\"\"\"\n+import asyncio\n+import logging\n+\n+import voluptuous as vol\n+\n+import homeassistant.helpers.config_validation as cv\n+from homeassistant.core import callback\n+import homeassistant.components.aqualogic as aqualogic\n+from homeassistant.components.switch import SwitchDevice, PLATFORM_SCHEMA\n+from homeassistant.const import (\n+    CONF_MONITORED_CONDITIONS, ATTR_ATTRIBUTION)\n+\n+DEPENDENCIES = ['aqualogic']\n+\n+_LOGGER = logging.getLogger(__name__)\n+\n+SWITCH_TYPES = {\n+    'lights': 'Lights',\n+    'filter': 'Filter',\n+    'filter_low_speed': 'Filter Low Speed',\n+    'aux_1': 'Aux 1',\n+    'aux_2': 'Aux 2',\n+    'aux_3': 'Aux 3',\n+    'aux_4': 'Aux 4',\n+    'aux_5': 'Aux 5',\n+    'aux_6': 'Aux 6',\n+    'aux_7': 'Aux 7',\n+}\n+\n+PLATFORM_SCHEMA = PLATFORM_SCHEMA.extend({\n+    vol.Optional(CONF_MONITORED_CONDITIONS, default=list(SWITCH_TYPES)):\n+        vol.All(cv.ensure_list, [vol.In(SWITCH_TYPES)]),\n+})\n+\n+\n+@asyncio.coroutine\n+def async_setup_platform(hass, config, async_add_devices, discovery_info=None):\n+    \"\"\"Set up the switch platform.\"\"\"\n+    switches = []\n+\n+    component = hass.data[aqualogic.DOMAIN]\n+    for switch_type in config.get(CONF_MONITORED_CONDITIONS):\n+        switches.append(AquaLogicSwitch(component, switch_type))\n+\n+    async_add_devices(switches)\n+\n+\n+class AquaLogicSwitch(SwitchDevice):\n+    \"\"\"Switch implementation for the AquaLogic component.\"\"\"\n+\n+    def __init__(self, component, switch_type):\n+        \"\"\"Initialize switch.\"\"\"\n+        from aqualogic.core import AquaLogic, States\n+        import aqualogic.core", "hunk": "@@ -10,7 +10,7 @@\n     <h1>Error 503 Backend is unhealthy</h1>\n     <p>Backend is unhealthy</p>\n     <h3>Guru Mediation:</h3>\n-    <p>Details: cache-sea4479-SEA 1645537796 2987705209</p>\n+    <p>Details: cache-sea4477-SEA 1645537796 3632137449</p>\n     <hr>\n     <p>Varnish cache server</p>\n   </body>\n", "comment": "'aqualogic.core' imported but unused", "ids": [319371, "2f9de61728acfc5c2a516524331564e475ca0ae9", "be6649ff9e6359a410e5ccf74eaa914838377da9"], "repo": "home-assistant-core", "ghid": 16763, "old": "     <h1>Error 503 Backend is unhealthy</h1>\n     <p>Backend is unhealthy</p>\n     <h3>Guru Mediation:</h3>\n-    <p>Details: cache-sea4479-SEA 1645537796 2987705209</p>\n     <hr>\n     <p>Varnish cache server</p>\n   </body>", "new": "     <h1>Error 503 Backend is unhealthy</h1>\n     <p>Backend is unhealthy</p>\n     <h3>Guru Mediation:</h3>\n+    <p>Details: cache-sea4477-SEA 1645537796 3632137449</p>\n     <hr>\n     <p>Varnish cache server</p>\n   </body>", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -0,0 +1,120 @@\n+\"\"\"\n+Support for AquaLogic switches.\n+\n+For more details about this platform, please refer to the documentation at\n+https://home-assistant.io/components/switch.aqualogic/\n+\"\"\"\n+import asyncio\n+import logging\n+\n+import voluptuous as vol\n+\n+import homeassistant.helpers.config_validation as cv\n+from homeassistant.core import callback\n+import homeassistant.components.aqualogic as aqualogic\n+from homeassistant.components.switch import SwitchDevice, PLATFORM_SCHEMA\n+from homeassistant.const import (\n+    CONF_MONITORED_CONDITIONS, ATTR_ATTRIBUTION)\n+\n+DEPENDENCIES = ['aqualogic']\n+\n+_LOGGER = logging.getLogger(__name__)\n+\n+SWITCH_TYPES = {\n+    'lights': 'Lights',\n+    'filter': 'Filter',\n+    'filter_low_speed': 'Filter Low Speed',\n+    'aux_1': 'Aux 1',\n+    'aux_2': 'Aux 2',\n+    'aux_3': 'Aux 3',\n+    'aux_4': 'Aux 4',\n+    'aux_5': 'Aux 5',\n+    'aux_6': 'Aux 6',\n+    'aux_7': 'Aux 7',\n+}\n+\n+PLATFORM_SCHEMA = PLATFORM_SCHEMA.extend({\n+    vol.Optional(CONF_MONITORED_CONDITIONS, default=list(SWITCH_TYPES)):\n+        vol.All(cv.ensure_list, [vol.In(SWITCH_TYPES)]),\n+})\n+\n+\n+@asyncio.coroutine\n+def async_setup_platform(hass, config, async_add_devices, discovery_info=None):\n+    \"\"\"Set up the switch platform.\"\"\"\n+    switches = []\n+\n+    component = hass.data[aqualogic.DOMAIN]\n+    for switch_type in config.get(CONF_MONITORED_CONDITIONS):\n+        switches.append(AquaLogicSwitch(component, switch_type))\n+\n+    async_add_devices(switches)\n+\n+\n+class AquaLogicSwitch(SwitchDevice):\n+    \"\"\"Switch implementation for the AquaLogic component.\"\"\"\n+\n+    def __init__(self, component, switch_type):\n+        \"\"\"Initialize switch.\"\"\"\n+        from aqualogic.core import AquaLogic, States\n+        import aqualogic.core", "msg": "'aqualogic.core' imported but unused"}
{"old_hunk": "@@ -0,0 +1,130 @@\n+\"\"\"\n+Support for the Opple light.\n+\n+For more details about this platform, please refer to the documentation at\n+https://home-assistant.io/components/light.opple/\n+\"\"\"\n+\n+import logging\n+\n+import voluptuous as vol\n+\n+from homeassistant.components.light import (\n+    ATTR_BRIGHTNESS, ATTR_COLOR_TEMP, PLATFORM_SCHEMA, SUPPORT_BRIGHTNESS,\n+    SUPPORT_COLOR_TEMP, Light)\n+from homeassistant.const import CONF_HOST, CONF_NAME\n+import homeassistant.helpers.config_validation as cv\n+from homeassistant.util.color import \\\n+    color_temperature_kelvin_to_mired as kelvin_to_mired\n+from homeassistant.util.color import \\\n+    color_temperature_mired_to_kelvin as mired_to_kelvin\n+\n+REQUIREMENTS = ['pyoppleio==1.0.5']\n+\n+_LOGGER = logging.getLogger(__name__)\n+\n+PLATFORM_SCHEMA = PLATFORM_SCHEMA.extend({\n+    vol.Required(CONF_HOST): cv.string,\n+    vol.Optional(CONF_NAME, default=\"opple light\"): cv.string\n+})\n+\n+\n+def setup_platform(hass, config, add_devices, discovery_info=None):\n+    \"\"\"Set up Opple light platform.\"\"\"\n+    name = config.get('name')\n+    host = config.get('host')\n+    add_devices([OppleLight(name, host)])\n+\n+\n+class OppleLight(Light):\n+    \"\"\"Opple light device.\"\"\"\n+\n+    def __init__(self, name, host):\n+        \"\"\"Initialize an Opple light.\"\"\"\n+        from pyoppleio.OppleLightDevice import OppleLightDevice\n+        self._device = OppleLightDevice(host)\n+\n+        self._name = name\n+        self._is_on = None\n+        self._brightness = None\n+        self._color_temp = None\n+\n+        _LOGGER.debug(\"init light %s %s\", self._device.ip, self._device.mac)", "hunk": "@@ -10,7 +10,7 @@\n     <h1>Error 503 Backend is unhealthy</h1>\n     <p>Backend is unhealthy</p>\n     <h3>Guru Mediation:</h3>\n-    <p>Details: cache-sea4475-SEA 1645537810 45893716</p>\n+    <p>Details: cache-sea4475-SEA 1645537810 45894833</p>\n     <hr>\n     <p>Varnish cache server</p>\n   </body>\n", "comment": "Capitalize start word of log messages.", "ids": [319408, "a82eb247e247ce3117045ec23191337555105ae2", "55e9bc1bc90b643d9710dfdeb01354c843aca2d5"], "repo": "home-assistant-core", "ghid": 16765, "old": "     <h1>Error 503 Backend is unhealthy</h1>\n     <p>Backend is unhealthy</p>\n     <h3>Guru Mediation:</h3>\n-    <p>Details: cache-sea4475-SEA 1645537810 45893716</p>\n     <hr>\n     <p>Varnish cache server</p>\n   </body>", "new": "     <h1>Error 503 Backend is unhealthy</h1>\n     <p>Backend is unhealthy</p>\n     <h3>Guru Mediation:</h3>\n+    <p>Details: cache-sea4475-SEA 1645537810 45894833</p>\n     <hr>\n     <p>Varnish cache server</p>\n   </body>", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -0,0 +1,130 @@\n+\"\"\"\n+Support for the Opple light.\n+\n+For more details about this platform, please refer to the documentation at\n+https://home-assistant.io/components/light.opple/\n+\"\"\"\n+\n+import logging\n+\n+import voluptuous as vol\n+\n+from homeassistant.components.light import (\n+    ATTR_BRIGHTNESS, ATTR_COLOR_TEMP, PLATFORM_SCHEMA, SUPPORT_BRIGHTNESS,\n+    SUPPORT_COLOR_TEMP, Light)\n+from homeassistant.const import CONF_HOST, CONF_NAME\n+import homeassistant.helpers.config_validation as cv\n+from homeassistant.util.color import \\\n+    color_temperature_kelvin_to_mired as kelvin_to_mired\n+from homeassistant.util.color import \\\n+    color_temperature_mired_to_kelvin as mired_to_kelvin\n+\n+REQUIREMENTS = ['pyoppleio==1.0.5']\n+\n+_LOGGER = logging.getLogger(__name__)\n+\n+PLATFORM_SCHEMA = PLATFORM_SCHEMA.extend({\n+    vol.Required(CONF_HOST): cv.string,\n+    vol.Optional(CONF_NAME, default=\"opple light\"): cv.string\n+})\n+\n+\n+def setup_platform(hass, config, add_devices, discovery_info=None):\n+    \"\"\"Set up Opple light platform.\"\"\"\n+    name = config.get('name')\n+    host = config.get('host')\n+    add_devices([OppleLight(name, host)])\n+\n+\n+class OppleLight(Light):\n+    \"\"\"Opple light device.\"\"\"\n+\n+    def __init__(self, name, host):\n+        \"\"\"Initialize an Opple light.\"\"\"\n+        from pyoppleio.OppleLightDevice import OppleLightDevice\n+        self._device = OppleLightDevice(host)\n+\n+        self._name = name\n+        self._is_on = None\n+        self._brightness = None\n+        self._color_temp = None\n+\n+        _LOGGER.debug(\"init light %s %s\", self._device.ip, self._device.mac)", "msg": "Capitalize start word of log messages."}
{"old_hunk": "@@ -0,0 +1,130 @@\n+\"\"\"\n+Support for the Opple light.\n+\n+For more details about this platform, please refer to the documentation at\n+https://home-assistant.io/components/light.opple/\n+\"\"\"\n+\n+import logging\n+\n+import voluptuous as vol\n+\n+from homeassistant.components.light import (\n+    ATTR_BRIGHTNESS, ATTR_COLOR_TEMP, PLATFORM_SCHEMA, SUPPORT_BRIGHTNESS,\n+    SUPPORT_COLOR_TEMP, Light)\n+from homeassistant.const import CONF_HOST, CONF_NAME\n+import homeassistant.helpers.config_validation as cv\n+from homeassistant.util.color import \\\n+    color_temperature_kelvin_to_mired as kelvin_to_mired\n+from homeassistant.util.color import \\\n+    color_temperature_mired_to_kelvin as mired_to_kelvin\n+\n+REQUIREMENTS = ['pyoppleio==1.0.5']\n+\n+_LOGGER = logging.getLogger(__name__)\n+\n+PLATFORM_SCHEMA = PLATFORM_SCHEMA.extend({\n+    vol.Required(CONF_HOST): cv.string,\n+    vol.Optional(CONF_NAME, default=\"opple light\"): cv.string\n+})\n+\n+\n+def setup_platform(hass, config, add_devices, discovery_info=None):\n+    \"\"\"Set up Opple light platform.\"\"\"\n+    name = config.get('name')\n+    host = config.get('host')\n+    add_devices([OppleLight(name, host)])\n+\n+\n+class OppleLight(Light):\n+    \"\"\"Opple light device.\"\"\"\n+\n+    def __init__(self, name, host):\n+        \"\"\"Initialize an Opple light.\"\"\"\n+        from pyoppleio.OppleLightDevice import OppleLightDevice\n+        self._device = OppleLightDevice(host)\n+\n+        self._name = name\n+        self._is_on = None\n+        self._brightness = None\n+        self._color_temp = None\n+\n+        _LOGGER.debug(\"init light %s %s\", self._device.ip, self._device.mac)\n+\n+    @property\n+    def available(self):\n+        \"\"\"Return True if light is available.\"\"\"\n+        return self._device.is_online\n+\n+    @property\n+    def name(self):\n+        \"\"\"Return the display name of this light.\"\"\"\n+        return self._name\n+\n+    @property\n+    def is_on(self):\n+        \"\"\"Return true if light is on.\"\"\"\n+        return self._is_on\n+\n+    @property\n+    def brightness(self):\n+        \"\"\"Return the brightness of the light.\"\"\"\n+        return self._brightness\n+\n+    @property\n+    def color_temp(self):\n+        \"\"\"Return the color temperature of this light.\"\"\"\n+        return kelvin_to_mired(self._color_temp)\n+\n+    @property\n+    def min_mireds(self):\n+        \"\"\"Return minimum supported color temperature.\"\"\"\n+        return 175\n+\n+    @property\n+    def max_mireds(self):\n+        \"\"\"Return maximum supported color temperature.\"\"\"\n+        return 333\n+\n+    @property\n+    def supported_features(self):\n+        \"\"\"Flag supported features.\"\"\"\n+        return SUPPORT_BRIGHTNESS | SUPPORT_COLOR_TEMP\n+\n+    def turn_on(self, **kwargs):\n+        \"\"\"Instruct the light to turn on.\"\"\"\n+        _LOGGER.debug(\"Turn on light %s %s\", self._device.ip, kwargs)\n+        if not self.is_on:\n+            self._is_on = self._device.power_on = True\n+\n+        if ATTR_BRIGHTNESS in kwargs and \\\n+                self.brightness != kwargs[ATTR_BRIGHTNESS]:\n+            self._brightness = kwargs[ATTR_BRIGHTNESS]\n+            self._device.brightness = kwargs[ATTR_BRIGHTNESS]\n+\n+        if ATTR_COLOR_TEMP in kwargs and \\\n+                self.brightness != kwargs[ATTR_COLOR_TEMP]:\n+            color_temp = mired_to_kelvin(kwargs[ATTR_COLOR_TEMP])\n+            self._color_temp = self._device.color_temperature = color_temp\n+\n+    def turn_off(self, **kwargs):\n+        \"\"\"Instruct the light to turn off.\"\"\"\n+        self._device.power_on = False\n+        _LOGGER.debug(\"Turn off light %s\", self._device.ip)\n+\n+    def update(self):\n+        \"\"\"Synchronize state with light.\"\"\"\n+        self._device.update()\n+        self._is_on = self._device.power_on\n+        self._brightness = self._device.brightness\n+        self._color_temp = self._device.color_temperature\n+\n+        if not self.available:", "hunk": "@@ -10,7 +10,7 @@\n     <h1>Error 503 Backend is unhealthy</h1>\n     <p>Backend is unhealthy</p>\n     <h3>Guru Mediation:</h3>\n-    <p>Details: cache-sea4475-SEA 1645537811 45901469</p>\n+    <p>Details: cache-sea4475-SEA 1645537811 45902493</p>\n     <hr>\n     <p>Varnish cache server</p>\n   </body>\n", "comment": "Only log one message when the device changes availability state, not on each update if state is the same.", "ids": [319410, "a82eb247e247ce3117045ec23191337555105ae2", "55e9bc1bc90b643d9710dfdeb01354c843aca2d5"], "repo": "home-assistant-core", "ghid": 16765, "old": "     <h1>Error 503 Backend is unhealthy</h1>\n     <p>Backend is unhealthy</p>\n     <h3>Guru Mediation:</h3>\n-    <p>Details: cache-sea4475-SEA 1645537811 45901469</p>\n     <hr>\n     <p>Varnish cache server</p>\n   </body>", "new": "     <h1>Error 503 Backend is unhealthy</h1>\n     <p>Backend is unhealthy</p>\n     <h3>Guru Mediation:</h3>\n+    <p>Details: cache-sea4475-SEA 1645537811 45902493</p>\n     <hr>\n     <p>Varnish cache server</p>\n   </body>", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -0,0 +1,130 @@\n+\"\"\"\n+Support for the Opple light.\n+\n+For more details about this platform, please refer to the documentation at\n+https://home-assistant.io/components/light.opple/\n+\"\"\"\n+\n+import logging\n+\n+import voluptuous as vol\n+\n+from homeassistant.components.light import (\n+    ATTR_BRIGHTNESS, ATTR_COLOR_TEMP, PLATFORM_SCHEMA, SUPPORT_BRIGHTNESS,\n+    SUPPORT_COLOR_TEMP, Light)\n+from homeassistant.const import CONF_HOST, CONF_NAME\n+import homeassistant.helpers.config_validation as cv\n+from homeassistant.util.color import \\\n+    color_temperature_kelvin_to_mired as kelvin_to_mired\n+from homeassistant.util.color import \\\n+    color_temperature_mired_to_kelvin as mired_to_kelvin\n+\n+REQUIREMENTS = ['pyoppleio==1.0.5']\n+\n+_LOGGER = logging.getLogger(__name__)\n+\n+PLATFORM_SCHEMA = PLATFORM_SCHEMA.extend({\n+    vol.Required(CONF_HOST): cv.string,\n+    vol.Optional(CONF_NAME, default=\"opple light\"): cv.string\n+})\n+\n+\n+def setup_platform(hass, config, add_devices, discovery_info=None):\n+    \"\"\"Set up Opple light platform.\"\"\"\n+    name = config.get('name')\n+    host = config.get('host')\n+    add_devices([OppleLight(name, host)])\n+\n+\n+class OppleLight(Light):\n+    \"\"\"Opple light device.\"\"\"\n+\n+    def __init__(self, name, host):\n+        \"\"\"Initialize an Opple light.\"\"\"\n+        from pyoppleio.OppleLightDevice import OppleLightDevice\n+        self._device = OppleLightDevice(host)\n+\n+        self._name = name\n+        self._is_on = None\n+        self._brightness = None\n+        self._color_temp = None\n+\n+        _LOGGER.debug(\"init light %s %s\", self._device.ip, self._device.mac)\n+\n+    @property\n+    def available(self):\n+        \"\"\"Return True if light is available.\"\"\"\n+        return self._device.is_online\n+\n+    @property\n+    def name(self):\n+        \"\"\"Return the display name of this light.\"\"\"\n+        return self._name\n+\n+    @property\n+    def is_on(self):\n+        \"\"\"Return true if light is on.\"\"\"\n+        return self._is_on\n+\n+    @property\n+    def brightness(self):\n+        \"\"\"Return the brightness of the light.\"\"\"\n+        return self._brightness\n+\n+    @property\n+    def color_temp(self):\n+        \"\"\"Return the color temperature of this light.\"\"\"\n+        return kelvin_to_mired(self._color_temp)\n+\n+    @property\n+    def min_mireds(self):\n+        \"\"\"Return minimum supported color temperature.\"\"\"\n+        return 175\n+\n+    @property\n+    def max_mireds(self):\n+        \"\"\"Return maximum supported color temperature.\"\"\"\n+        return 333\n+\n+    @property\n+    def supported_features(self):\n+        \"\"\"Flag supported features.\"\"\"\n+        return SUPPORT_BRIGHTNESS | SUPPORT_COLOR_TEMP\n+\n+    def turn_on(self, **kwargs):\n+        \"\"\"Instruct the light to turn on.\"\"\"\n+        _LOGGER.debug(\"Turn on light %s %s\", self._device.ip, kwargs)\n+        if not self.is_on:\n+            self._is_on = self._device.power_on = True\n+\n+        if ATTR_BRIGHTNESS in kwargs and \\\n+                self.brightness != kwargs[ATTR_BRIGHTNESS]:\n+            self._brightness = kwargs[ATTR_BRIGHTNESS]\n+            self._device.brightness = kwargs[ATTR_BRIGHTNESS]\n+\n+        if ATTR_COLOR_TEMP in kwargs and \\\n+                self.brightness != kwargs[ATTR_COLOR_TEMP]:\n+            color_temp = mired_to_kelvin(kwargs[ATTR_COLOR_TEMP])\n+            self._color_temp = self._device.color_temperature = color_temp\n+\n+    def turn_off(self, **kwargs):\n+        \"\"\"Instruct the light to turn off.\"\"\"\n+        self._device.power_on = False\n+        _LOGGER.debug(\"Turn off light %s\", self._device.ip)\n+\n+    def update(self):\n+        \"\"\"Synchronize state with light.\"\"\"\n+        self._device.update()\n+        self._is_on = self._device.power_on\n+        self._brightness = self._device.brightness\n+        self._color_temp = self._device.color_temperature\n+\n+        if not self.available:", "msg": "Only log one message when the device changes availability state, not on each update if state is the same."}
{"old_hunk": "@@ -0,0 +1,130 @@\n+\"\"\"\n+Support for the Opple light.\n+\n+For more details about this platform, please refer to the documentation at\n+https://home-assistant.io/components/light.opple/\n+\"\"\"\n+\n+import logging\n+\n+import voluptuous as vol\n+\n+from homeassistant.components.light import (\n+    ATTR_BRIGHTNESS, ATTR_COLOR_TEMP, PLATFORM_SCHEMA, SUPPORT_BRIGHTNESS,\n+    SUPPORT_COLOR_TEMP, Light)\n+from homeassistant.const import CONF_HOST, CONF_NAME\n+import homeassistant.helpers.config_validation as cv\n+from homeassistant.util.color import \\\n+    color_temperature_kelvin_to_mired as kelvin_to_mired\n+from homeassistant.util.color import \\\n+    color_temperature_mired_to_kelvin as mired_to_kelvin\n+\n+REQUIREMENTS = ['pyoppleio==1.0.5']\n+\n+_LOGGER = logging.getLogger(__name__)\n+\n+PLATFORM_SCHEMA = PLATFORM_SCHEMA.extend({\n+    vol.Required(CONF_HOST): cv.string,\n+    vol.Optional(CONF_NAME, default=\"opple light\"): cv.string\n+})\n+\n+\n+def setup_platform(hass, config, add_devices, discovery_info=None):\n+    \"\"\"Set up Opple light platform.\"\"\"\n+    name = config.get('name')\n+    host = config.get('host')\n+    add_devices([OppleLight(name, host)])\n+\n+\n+class OppleLight(Light):\n+    \"\"\"Opple light device.\"\"\"\n+\n+    def __init__(self, name, host):\n+        \"\"\"Initialize an Opple light.\"\"\"\n+        from pyoppleio.OppleLightDevice import OppleLightDevice\n+        self._device = OppleLightDevice(host)\n+\n+        self._name = name\n+        self._is_on = None\n+        self._brightness = None\n+        self._color_temp = None\n+\n+        _LOGGER.debug(\"init light %s %s\", self._device.ip, self._device.mac)\n+\n+    @property\n+    def available(self):\n+        \"\"\"Return True if light is available.\"\"\"\n+        return self._device.is_online\n+\n+    @property\n+    def name(self):\n+        \"\"\"Return the display name of this light.\"\"\"\n+        return self._name\n+\n+    @property\n+    def is_on(self):\n+        \"\"\"Return true if light is on.\"\"\"\n+        return self._is_on\n+\n+    @property\n+    def brightness(self):\n+        \"\"\"Return the brightness of the light.\"\"\"\n+        return self._brightness\n+\n+    @property\n+    def color_temp(self):\n+        \"\"\"Return the color temperature of this light.\"\"\"\n+        return kelvin_to_mired(self._color_temp)\n+\n+    @property\n+    def min_mireds(self):\n+        \"\"\"Return minimum supported color temperature.\"\"\"\n+        return 175\n+\n+    @property\n+    def max_mireds(self):\n+        \"\"\"Return maximum supported color temperature.\"\"\"\n+        return 333\n+\n+    @property\n+    def supported_features(self):\n+        \"\"\"Flag supported features.\"\"\"\n+        return SUPPORT_BRIGHTNESS | SUPPORT_COLOR_TEMP\n+\n+    def turn_on(self, **kwargs):\n+        \"\"\"Instruct the light to turn on.\"\"\"\n+        _LOGGER.debug(\"Turn on light %s %s\", self._device.ip, kwargs)\n+        if not self.is_on:\n+            self._is_on = self._device.power_on = True", "hunk": "@@ -10,7 +10,7 @@\n     <h1>Error 503 Backend is unhealthy</h1>\n     <p>Backend is unhealthy</p>\n     <h3>Guru Mediation:</h3>\n-    <p>Details: cache-sea4449-SEA 1645537811 1453948297</p>\n+    <p>Details: cache-sea4475-SEA 1645537811 45898557</p>\n     <hr>\n     <p>Varnish cache server</p>\n   </body>\n", "comment": "Let `update` change state holding instance attributes. It will be called directly after the method call.", "ids": [319409, "a82eb247e247ce3117045ec23191337555105ae2", "55e9bc1bc90b643d9710dfdeb01354c843aca2d5"], "repo": "home-assistant-core", "ghid": 16765, "old": "     <h1>Error 503 Backend is unhealthy</h1>\n     <p>Backend is unhealthy</p>\n     <h3>Guru Mediation:</h3>\n-    <p>Details: cache-sea4449-SEA 1645537811 1453948297</p>\n     <hr>\n     <p>Varnish cache server</p>\n   </body>", "new": "     <h1>Error 503 Backend is unhealthy</h1>\n     <p>Backend is unhealthy</p>\n     <h3>Guru Mediation:</h3>\n+    <p>Details: cache-sea4475-SEA 1645537811 45898557</p>\n     <hr>\n     <p>Varnish cache server</p>\n   </body>", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -0,0 +1,130 @@\n+\"\"\"\n+Support for the Opple light.\n+\n+For more details about this platform, please refer to the documentation at\n+https://home-assistant.io/components/light.opple/\n+\"\"\"\n+\n+import logging\n+\n+import voluptuous as vol\n+\n+from homeassistant.components.light import (\n+    ATTR_BRIGHTNESS, ATTR_COLOR_TEMP, PLATFORM_SCHEMA, SUPPORT_BRIGHTNESS,\n+    SUPPORT_COLOR_TEMP, Light)\n+from homeassistant.const import CONF_HOST, CONF_NAME\n+import homeassistant.helpers.config_validation as cv\n+from homeassistant.util.color import \\\n+    color_temperature_kelvin_to_mired as kelvin_to_mired\n+from homeassistant.util.color import \\\n+    color_temperature_mired_to_kelvin as mired_to_kelvin\n+\n+REQUIREMENTS = ['pyoppleio==1.0.5']\n+\n+_LOGGER = logging.getLogger(__name__)\n+\n+PLATFORM_SCHEMA = PLATFORM_SCHEMA.extend({\n+    vol.Required(CONF_HOST): cv.string,\n+    vol.Optional(CONF_NAME, default=\"opple light\"): cv.string\n+})\n+\n+\n+def setup_platform(hass, config, add_devices, discovery_info=None):\n+    \"\"\"Set up Opple light platform.\"\"\"\n+    name = config.get('name')\n+    host = config.get('host')\n+    add_devices([OppleLight(name, host)])\n+\n+\n+class OppleLight(Light):\n+    \"\"\"Opple light device.\"\"\"\n+\n+    def __init__(self, name, host):\n+        \"\"\"Initialize an Opple light.\"\"\"\n+        from pyoppleio.OppleLightDevice import OppleLightDevice\n+        self._device = OppleLightDevice(host)\n+\n+        self._name = name\n+        self._is_on = None\n+        self._brightness = None\n+        self._color_temp = None\n+\n+        _LOGGER.debug(\"init light %s %s\", self._device.ip, self._device.mac)\n+\n+    @property\n+    def available(self):\n+        \"\"\"Return True if light is available.\"\"\"\n+        return self._device.is_online\n+\n+    @property\n+    def name(self):\n+        \"\"\"Return the display name of this light.\"\"\"\n+        return self._name\n+\n+    @property\n+    def is_on(self):\n+        \"\"\"Return true if light is on.\"\"\"\n+        return self._is_on\n+\n+    @property\n+    def brightness(self):\n+        \"\"\"Return the brightness of the light.\"\"\"\n+        return self._brightness\n+\n+    @property\n+    def color_temp(self):\n+        \"\"\"Return the color temperature of this light.\"\"\"\n+        return kelvin_to_mired(self._color_temp)\n+\n+    @property\n+    def min_mireds(self):\n+        \"\"\"Return minimum supported color temperature.\"\"\"\n+        return 175\n+\n+    @property\n+    def max_mireds(self):\n+        \"\"\"Return maximum supported color temperature.\"\"\"\n+        return 333\n+\n+    @property\n+    def supported_features(self):\n+        \"\"\"Flag supported features.\"\"\"\n+        return SUPPORT_BRIGHTNESS | SUPPORT_COLOR_TEMP\n+\n+    def turn_on(self, **kwargs):\n+        \"\"\"Instruct the light to turn on.\"\"\"\n+        _LOGGER.debug(\"Turn on light %s %s\", self._device.ip, kwargs)\n+        if not self.is_on:\n+            self._is_on = self._device.power_on = True", "msg": "Let `update` change state holding instance attributes. It will be called directly after the method call."}
{"old_hunk": "@@ -4,18 +4,29 @@\n For more details about this platform, please refer to the documentation at\n https://home-assistant.io/components/device_tracker.bbox/\n \"\"\"\n+import voluptuous as vol\n+\n+import homeassistant.helpers.config_validation as cv\n+from homeassistant.components.device_tracker import (", "hunk": "@@ -10,7 +10,7 @@\n     <h1>Error 503 Backend is unhealthy</h1>\n     <p>Backend is unhealthy</p>\n     <h3>Guru Mediation:</h3>\n-    <p>Details: cache-sea4478-SEA 1645537813 58623061</p>\n+    <p>Details: cache-sea4467-SEA 1645537813 4130222745</p>\n     <hr>\n     <p>Varnish cache server</p>\n   </body>\n", "comment": "'homeassistant.components.device_tracker.CONF_SCAN_INTERVAL' imported but unused<br>'homeassistant.components.device_tracker.DEFAULT_SCAN_INTERVAL' imported but unused", "ids": [319433, "c6379f0ab6f43ec9a399a4ecd8f27d3bb3c86983", "4391d031e7c4b31c0ced6c809baaa205590b08ef"], "repo": "home-assistant-core", "ghid": 16778, "old": "     <h1>Error 503 Backend is unhealthy</h1>\n     <p>Backend is unhealthy</p>\n     <h3>Guru Mediation:</h3>\n-    <p>Details: cache-sea4478-SEA 1645537813 58623061</p>\n     <hr>\n     <p>Varnish cache server</p>\n   </body>", "new": "     <h1>Error 503 Backend is unhealthy</h1>\n     <p>Backend is unhealthy</p>\n     <h3>Guru Mediation:</h3>\n+    <p>Details: cache-sea4467-SEA 1645537813 4130222745</p>\n     <hr>\n     <p>Varnish cache server</p>\n   </body>", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -4,18 +4,29 @@\n For more details about this platform, please refer to the documentation at\n https://home-assistant.io/components/device_tracker.bbox/\n \"\"\"\n+import voluptuous as vol\n+\n+import homeassistant.helpers.config_validation as cv\n+from homeassistant.components.device_tracker import (", "msg": "'homeassistant.components.device_tracker.CONF_SCAN_INTERVAL' imported but unused<br>'homeassistant.components.device_tracker.DEFAULT_SCAN_INTERVAL' imported but unused"}
{"old_hunk": "@@ -113,6 +103,14 @@ def __init__(self, poller, parameter, name, unit, force_update, median):\n         # Use median_count = 1 if no filtering is required.\n         self.median_count = median\n \n+    async def async_added_to_hass(self):\n+        \"\"\"Set initial state.\"\"\"\n+        @callback\n+        def on_startup():\n+            self.async_schedule_update_ha_state(True)\n+\n+        self.hass.bus.async_listen_once(EVENT_HOMEASSISTANT_START, on_startup)\n+", "hunk": "@@ -103,14 +103,6 @@ class MiFloraSensor(Entity):\n         # Use median_count = 1 if no filtering is required.\n         self.median_count = median\n \n-    async def async_added_to_hass(self):\n-        \"\"\"Set initial state.\"\"\"\n-        @callback\n-        def on_startup():\n-            self.async_schedule_update_ha_state(True)\n-\n-        self.hass.bus.async_listen_once(EVENT_HOMEASSISTANT_START, on_startup)\n-\n     @property\n     def name(self):\n         \"\"\"Return the name of the sensor.\"\"\"\n", "comment": "I think you don't need this code, because it's a polling device and the core call the update itself after some time. And if I understand that correct, time is not a problem only real time :D", "ids": [319461, "dbddd172bcb3a137dfac075805533ac573b96e04", "898156e999585b017955c33d9a237d13f7c31a18"], "repo": "home-assistant-core", "ghid": 16798, "old": "         # Use median_count = 1 if no filtering is required.\n         self.median_count = median\n-    async def async_added_to_hass(self):\n-        \"\"\"Set initial state.\"\"\"\n-        @callback\n-        def on_startup():\n-            self.async_schedule_update_ha_state(True)\n-\n-        self.hass.bus.async_listen_once(EVENT_HOMEASSISTANT_START, on_startup)\n-\n     @property\n     def name(self):\n         \"\"\"Return the name of the sensor.\"\"\"", "new": "         # Use median_count = 1 if no filtering is required.\n         self.median_count = median\n     @property\n     def name(self):\n         \"\"\"Return the name of the sensor.\"\"\"", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -113,6 +103,14 @@ def __init__(self, poller, parameter, name, unit, force_update, median):\n         # Use median_count = 1 if no filtering is required.\n         self.median_count = median\n \n+    async def async_added_to_hass(self):\n+        \"\"\"Set initial state.\"\"\"\n+        @callback\n+        def on_startup():\n+            self.async_schedule_update_ha_state(True)\n+\n+        self.hass.bus.async_listen_once(EVENT_HOMEASSISTANT_START, on_startup)\n+", "msg": "I think you don't need this code, because it's a polling device and the core call the update itself after some time. And if I understand that correct, time is not a problem only real time :D"}
{"old_hunk": "@@ -39,6 +39,8 @@\n STATUS_STOPPED = 2\n STATUS_WAIT = 3\n \n+MAX_DEVICES = 100", "hunk": "@@ -39,8 +39,6 @@ STATUS_RUNNING = 1\n STATUS_STOPPED = 2\n STATUS_WAIT = 3\n \n-MAX_DEVICES = 100\n-\n SWITCH_TYPES = {TYPE_OUTLET: 'Outlet',\n                 TYPE_SWITCH: 'Switch'}\n \n", "comment": "Just move this line between `TYPES` and `_LOGGER`.", "ids": [319540, "50f8324c7acd159902de0c32e232e4e460a29565", "9fa253513361237c7373a46ecacff6f2aa1b6334"], "repo": "home-assistant-core", "ghid": 16807, "old": " STATUS_STOPPED = 2\n STATUS_WAIT = 3\n-MAX_DEVICES = 100\n-\n SWITCH_TYPES = {TYPE_OUTLET: 'Outlet',\n                 TYPE_SWITCH: 'Switch'}", "new": " STATUS_STOPPED = 2\n STATUS_WAIT = 3\n SWITCH_TYPES = {TYPE_OUTLET: 'Outlet',\n                 TYPE_SWITCH: 'Switch'}", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -39,6 +39,8 @@\n STATUS_STOPPED = 2\n STATUS_WAIT = 3\n \n+MAX_DEVICES = 100", "msg": "Just move this line between `TYPES` and `_LOGGER`."}
{"old_hunk": "@@ -0,0 +1,72 @@\n+\"\"\"Tests for Tradfri setup.\"\"\"\n+from unittest.mock import patch\n+\n+from homeassistant.setup import async_setup_component\n+\n+from tests.common import MockConfigEntry\n+\n+\n+async def test_config_yaml_host_not_imported(hass):\n+    \"\"\"Test that we don't import a configured host.\"\"\"\n+    MockConfigEntry(\n+        domain='tradfri',\n+        data={'host': 'mock-host'}\n+    ).add_to_hass(hass)\n+\n+    with patch('homeassistant.components.tradfri.load_json',\n+               return_value={}), \\\n+            patch.object(hass.config_entries.flow, 'async_init') as mock_init:\n+        assert await async_setup_component(hass, 'tradfri', {\n+            'tradfri': {\n+                'host': 'mock-host'\n+            }\n+        })\n+\n+    assert len(mock_init.mock_calls) == 0\n+\n+\n+async def test_config_yaml_host_imported(hass):\n+    \"\"\"Test that we import a configured host.\"\"\"\n+    with patch('homeassistant.components.tradfri.load_json',\n+               return_value={}):\n+        assert await async_setup_component(hass, 'tradfri', {\n+            'tradfri': {\n+                'host': 'mock-host'\n+            }\n+        })\n+\n+    progress = hass.config_entries.flow.async_progress()\n+    assert len(progress) == 1\n+    assert progress[0]['handler'] == 'tradfri'\n+    assert progress[0]['context'] == {'source': 'import'}\n+\n+\n+async def test_config_json_host_not_imported(hass):\n+    \"\"\"Test that we don't import a configured host.\"\"\"\n+    MockConfigEntry(\n+        domain='tradfri',\n+        data={'host': 'mock-host'}\n+    ).add_to_hass(hass)\n+\n+    with patch('homeassistant.components.tradfri.load_json',\n+               return_value={'mock-host': {'key': 'some-info'}}), \\\n+            patch.object(hass.config_entries.flow, 'async_init') as mock_init:\n+        assert await async_setup_component(hass, 'tradfri', {\n+            'tradfri': {}\n+        })\n+\n+    assert len(mock_init.mock_calls) == 0\n+\n+\n+async def test_config_yaml_host_imported(hass):", "hunk": "@@ -58,7 +58,7 @@ async def test_config_json_host_not_imported(hass):\n     assert len(mock_init.mock_calls) == 0\n \n \n-async def test_config_yaml_host_imported(hass):\n+async def test_config_json_host_imported(hass):\n     \"\"\"Test that we import a configured host.\"\"\"\n     with patch('homeassistant.components.tradfri.load_json',\n                return_value={'mock-host': {'key': 'some-info'}}):\n", "comment": "redefinition of unused 'test_config_yaml_host_imported' from line 28", "ids": [319775, "39416a5b51a4fd7e9d408753bc2422f766a1ed8e", "458767d58e8ee51803e2084e7753bd0939e6b22e"], "repo": "home-assistant-core", "ghid": 16891, "old": "     assert len(mock_init.mock_calls) == 0\n-async def test_config_yaml_host_imported(hass):\n     \"\"\"Test that we import a configured host.\"\"\"\n     with patch('homeassistant.components.tradfri.load_json',\n                return_value={'mock-host': {'key': 'some-info'}}):", "new": "     assert len(mock_init.mock_calls) == 0\n+async def test_config_json_host_imported(hass):\n     \"\"\"Test that we import a configured host.\"\"\"\n     with patch('homeassistant.components.tradfri.load_json',\n                return_value={'mock-host': {'key': 'some-info'}}):", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -0,0 +1,72 @@\n+\"\"\"Tests for Tradfri setup.\"\"\"\n+from unittest.mock import patch\n+\n+from homeassistant.setup import async_setup_component\n+\n+from tests.common import MockConfigEntry\n+\n+\n+async def test_config_yaml_host_not_imported(hass):\n+    \"\"\"Test that we don't import a configured host.\"\"\"\n+    MockConfigEntry(\n+        domain='tradfri',\n+        data={'host': 'mock-host'}\n+    ).add_to_hass(hass)\n+\n+    with patch('homeassistant.components.tradfri.load_json',\n+               return_value={}), \\\n+            patch.object(hass.config_entries.flow, 'async_init') as mock_init:\n+        assert await async_setup_component(hass, 'tradfri', {\n+            'tradfri': {\n+                'host': 'mock-host'\n+            }\n+        })\n+\n+    assert len(mock_init.mock_calls) == 0\n+\n+\n+async def test_config_yaml_host_imported(hass):\n+    \"\"\"Test that we import a configured host.\"\"\"\n+    with patch('homeassistant.components.tradfri.load_json',\n+               return_value={}):\n+        assert await async_setup_component(hass, 'tradfri', {\n+            'tradfri': {\n+                'host': 'mock-host'\n+            }\n+        })\n+\n+    progress = hass.config_entries.flow.async_progress()\n+    assert len(progress) == 1\n+    assert progress[0]['handler'] == 'tradfri'\n+    assert progress[0]['context'] == {'source': 'import'}\n+\n+\n+async def test_config_json_host_not_imported(hass):\n+    \"\"\"Test that we don't import a configured host.\"\"\"\n+    MockConfigEntry(\n+        domain='tradfri',\n+        data={'host': 'mock-host'}\n+    ).add_to_hass(hass)\n+\n+    with patch('homeassistant.components.tradfri.load_json',\n+               return_value={'mock-host': {'key': 'some-info'}}), \\\n+            patch.object(hass.config_entries.flow, 'async_init') as mock_init:\n+        assert await async_setup_component(hass, 'tradfri', {\n+            'tradfri': {}\n+        })\n+\n+    assert len(mock_init.mock_calls) == 0\n+\n+\n+async def test_config_yaml_host_imported(hass):", "msg": "redefinition of unused 'test_config_yaml_host_imported' from line 28"}
{"old_hunk": "@@ -345,6 +346,12 @@ def network_complete():\n                      \"have been queried\")\n         hass.bus.fire(const.EVENT_NETWORK_COMPLETE)\n \n+    def network_complete_some_dead():\n+        \"\"\"Handle the querying of all nodes on network.\"\"\"\n+        _LOGGER.info(\"Z-Wave network is complete. All nodes on the network \"\n+                     \"have been queried, but some node ar mark dead\")", "hunk": "@@ -349,7 +349,7 @@ async def async_setup(hass, config):\n     def network_complete_some_dead():\n         \"\"\"Handle the querying of all nodes on network.\"\"\"\n         _LOGGER.info(\"Z-Wave network is complete. All nodes on the network \"\n-                     \"have been queried, but some node ar mark dead\")\n+                     \"have been queried, but some node are marked dead\")\n         hass.bus.fire(const.EVENT_NETWORK_COMPLETE_SOME_DEAD)\n \n     dispatcher.connect(\n", "comment": ".., but some nodes are marked as dead.", "ids": [319781, "3a80a6b0a501b4d2ced2c20dc5097f5ae077203c", "392981c0d106d82478427eb8919137c3ad63713d"], "repo": "home-assistant-core", "ghid": 16894, "old": "     def network_complete_some_dead():\n         \"\"\"Handle the querying of all nodes on network.\"\"\"\n         _LOGGER.info(\"Z-Wave network is complete. All nodes on the network \"\n-                     \"have been queried, but some node ar mark dead\")\n         hass.bus.fire(const.EVENT_NETWORK_COMPLETE_SOME_DEAD)\n     dispatcher.connect(", "new": "     def network_complete_some_dead():\n         \"\"\"Handle the querying of all nodes on network.\"\"\"\n         _LOGGER.info(\"Z-Wave network is complete. All nodes on the network \"\n+                     \"have been queried, but some node are marked dead\")\n         hass.bus.fire(const.EVENT_NETWORK_COMPLETE_SOME_DEAD)\n     dispatcher.connect(", "lang": "py", "y": 1, "proj": "home-assistant-core", "patch": "@@ -345,6 +346,12 @@ def network_complete():\n                      \"have been queried\")\n         hass.bus.fire(const.EVENT_NETWORK_COMPLETE)\n \n+    def network_complete_some_dead():\n+        \"\"\"Handle the querying of all nodes on network.\"\"\"\n+        _LOGGER.info(\"Z-Wave network is complete. All nodes on the network \"\n+                     \"have been queried, but some node ar mark dead\")", "msg": ".., but some nodes are marked as dead."}
