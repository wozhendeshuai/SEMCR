{"old_hunk": "@@ -872,6 +872,22 @@ func (a *apiServer) GetAuthTokenInTransaction(txnCtx *txnenv.TransactionContext,\n \t}, nil\n }\n \n+// GetPipelineAuthTokenInTransaction is an internal API used to create a pipeline token for a given pipeline.\n+// Not an RPC.", "hunk": "@@ -875,6 +875,10 @@ func (a *apiServer) GetAuthTokenInTransaction(txnCtx *txnenv.TransactionContext,\n // GetPipelineAuthTokenInTransaction is an internal API used to create a pipeline token for a given pipeline.\n // Not an RPC.\n func (a *apiServer) GetPipelineAuthTokenInTransaction(txnCtx *txnenv.TransactionContext, pipeline string) (string, error) {\n+\tif err := a.isActive(); err != nil {\n+\t\treturn \"\", err\n+\t}\n+\n \ttokenInfo := auth.TokenInfo{\n \t\tSource:  auth.TokenInfo_GET_TOKEN,\n \t\tSubject: auth.PipelinePrefix + pipeline,\n", "comment": "Maybe add an extra note about how it's insecure? Something like `does not authenticate the caller, and should only be used in code paths that have already authenticated the caller (currently just CreatePipeline)` or something?", "ids": [46193, "7f5d26fb87069ae603ffa1da4a0461d7f72fecf9", "0ca008e9813529d9a9c2289e1a2ead817707087b"], "repo": "pachyderm-pachyderm", "ghid": 5739, "old": " // GetPipelineAuthTokenInTransaction is an internal API used to create a pipeline token for a given pipeline.\n // Not an RPC.\n func (a *apiServer) GetPipelineAuthTokenInTransaction(txnCtx *txnenv.TransactionContext, pipeline string) (string, error) {\n \ttokenInfo := auth.TokenInfo{\n \t\tSource:  auth.TokenInfo_GET_TOKEN,\n \t\tSubject: auth.PipelinePrefix + pipeline,", "new": " // GetPipelineAuthTokenInTransaction is an internal API used to create a pipeline token for a given pipeline.\n // Not an RPC.\n func (a *apiServer) GetPipelineAuthTokenInTransaction(txnCtx *txnenv.TransactionContext, pipeline string) (string, error) {\n+\tif err := a.isActive(); err != nil {\n+\t\treturn \"\", err\n+\t}\n+\n \ttokenInfo := auth.TokenInfo{\n \t\tSource:  auth.TokenInfo_GET_TOKEN,\n \t\tSubject: auth.PipelinePrefix + pipeline,", "lang": "go", "y": 1, "proj": "pachyderm-pachyderm", "patch": "@@ -872,6 +872,22 @@ func (a *apiServer) GetAuthTokenInTransaction(txnCtx *txnenv.TransactionContext,\n \t}, nil\n }\n \n+// GetPipelineAuthTokenInTransaction is an internal API used to create a pipeline token for a given pipeline.\n+// Not an RPC.", "msg": "Maybe add an extra note about how it's insecure? Something like `does not authenticate the caller, and should only be used in code paths that have already authenticated the caller (currently just CreatePipeline)` or something?"}
{"old_hunk": "@@ -2492,37 +2492,79 @@ func (a *apiServer) listPipeline(pachClient *client.APIClient, request *pps.List\n \t\t// ensure field names and enum values match with --raw output\n \t\tenc = serde.NewJSONEncoder(&jsonBuffer, serde.WithOrigName(true))\n \t}\n-\treturn a.listPipelinePtr(pachClient, request.Pipeline, request.History,\n-\t\tfunc(name string, ptr *pps.EtcdPipelineInfo) error {\n-\t\t\tvar pipelineInfo *pps.PipelineInfo\n-\t\t\tvar err error\n-\t\t\tif request.AllowIncomplete {\n-\t\t\t\tpipelineInfo, err = ppsutil.GetPipelineInfoAllowIncomplete(pachClient, name, ptr)\n+\tfilterPipeline := func(pipelineInfo *pps.PipelineInfo) bool {\n+\t\tif jqCode != nil {\n+\t\t\tjsonBuffer.Reset()\n+\t\t\t// convert pipelineInfo to a map[string]interface{} for use with gojq\n+\t\t\tenc.EncodeProto(pipelineInfo)\n+\t\t\tvar pipelineInterface interface{}\n+\t\t\tjson.Unmarshal(jsonBuffer.Bytes(), &pipelineInterface)\n+\t\t\titer := jqCode.Run(pipelineInterface)\n+\t\t\t// treat either jq false-y value as rejection\n+\t\t\tif v, _ := iter.Next(); v == false || v == nil {\n+\t\t\t\treturn false\n+\t\t\t}\n+\t\t}\n+\t\treturn true\n+\t}\n+\t// the mess below is so we can lookup the PFS info for each pipeline concurrently.\n+\ttype etcdInfo struct {\n+\t\tname string\n+\t\tptr  *pps.EtcdPipelineInfo\n+\t}\n+\teg, ctx := errgroup.WithContext(pachClient.Ctx())\n+\tetcdInfos := make(chan etcdInfo)\n+\t// stream these out of etcd\n+\teg.Go(func() error {\n+\t\tdefer close(etcdInfos)\n+\t\treturn a.listPipelinePtr(pachClient, request.Pipeline, request.History, func(name string, ptr *pps.EtcdPipelineInfo) error {\n+\t\t\t// copy\n+\t\t\tdata, _ := ptr.Marshal()\n+\t\t\tptr2 := &pps.EtcdPipelineInfo{}\n+\t\t\tif err := ptr2.Unmarshal(data); err != nil {\n+\t\t\t\tpanic(err)\n+\t\t\t}\n+\t\t\tselect {\n+\t\t\tcase <-ctx.Done():\n+\t\t\t\treturn ctx.Err()\n+\t\t\tcase etcdInfos <- etcdInfo{name: name, ptr: ptr2}:\n+\t\t\t\treturn nil\n+\t\t\t}\n+\t\t})\n+\t})\n+\t// spin up goroutines to get the PFS info, and then use the mutex to call f all synchronized like.\n+\tvar mu sync.Mutex\n+\tfor i := 0; i < 20; i++ {\n+\t\teg.Go(func() error {\n+\t\t\tfor info := range etcdInfos {\n+\t\t\t\tpinfo, err := a.resolvePipelineInfo(pachClient, request.AllowIncomplete, info.name, info.ptr)\n \t\t\t\tif err != nil {\n \t\t\t\t\treturn err\n \t\t\t\t}\n-\t\t\t} else {\n-\t\t\t\tpipelineInfo, err = ppsutil.GetPipelineInfo(pachClient, name, ptr)\n-\t\t\t\tif err != nil {\n+\t\t\t\tif err := func() error {\n+\t\t\t\t\tmu.Lock()\n+\t\t\t\t\tdefer mu.Unlock()\n+\t\t\t\t\t// the filtering shares that buffer thing, and it's CPU bound so why not do it with the lock\n+\t\t\t\t\tif !filterPipeline(pinfo) {\n+\t\t\t\t\t\treturn nil\n+\t\t\t\t\t}\n+\t\t\t\t\treturn f(pinfo)", "hunk": "@@ -2534,6 +2534,7 @@ func (a *apiServer) listPipeline(pachClient *client.APIClient, request *pps.List\n \t})\n \t// spin up goroutines to get the PFS info, and then use the mutex to call f all synchronized like.\n \tvar mu sync.Mutex\n+\tvar fHasErrored bool\n \tfor i := 0; i < 20; i++ {\n \t\teg.Go(func() error {\n \t\t\tfor info := range etcdInfos {\n", "comment": "The only problem I see is that we may call `f` after it has already errored. Perhaps we should have a bool to bail out after acquiring `mu` if an error has occurred (or the `eg` has been canceled)?", "ids": [46231, "d967dd7612b7760c9f4e559023990e32ff12660f", "4a72f20927c4772579c6ab45e89deb7924ac0d15"], "repo": "pachyderm-pachyderm", "ghid": 5772, "old": " \t})\n \t// spin up goroutines to get the PFS info, and then use the mutex to call f all synchronized like.\n \tvar mu sync.Mutex\n \tfor i := 0; i < 20; i++ {\n \t\teg.Go(func() error {\n \t\t\tfor info := range etcdInfos {", "new": " \t})\n \t// spin up goroutines to get the PFS info, and then use the mutex to call f all synchronized like.\n \tvar mu sync.Mutex\n+\tvar fHasErrored bool\n \tfor i := 0; i < 20; i++ {\n \t\teg.Go(func() error {\n \t\t\tfor info := range etcdInfos {", "lang": "go", "y": 1, "proj": "pachyderm-pachyderm", "patch": "@@ -2492,37 +2492,79 @@ func (a *apiServer) listPipeline(pachClient *client.APIClient, request *pps.List\n \t\t// ensure field names and enum values match with --raw output\n \t\tenc = serde.NewJSONEncoder(&jsonBuffer, serde.WithOrigName(true))\n \t}\n-\treturn a.listPipelinePtr(pachClient, request.Pipeline, request.History,\n-\t\tfunc(name string, ptr *pps.EtcdPipelineInfo) error {\n-\t\t\tvar pipelineInfo *pps.PipelineInfo\n-\t\t\tvar err error\n-\t\t\tif request.AllowIncomplete {\n-\t\t\t\tpipelineInfo, err = ppsutil.GetPipelineInfoAllowIncomplete(pachClient, name, ptr)\n+\tfilterPipeline := func(pipelineInfo *pps.PipelineInfo) bool {\n+\t\tif jqCode != nil {\n+\t\t\tjsonBuffer.Reset()\n+\t\t\t// convert pipelineInfo to a map[string]interface{} for use with gojq\n+\t\t\tenc.EncodeProto(pipelineInfo)\n+\t\t\tvar pipelineInterface interface{}\n+\t\t\tjson.Unmarshal(jsonBuffer.Bytes(), &pipelineInterface)\n+\t\t\titer := jqCode.Run(pipelineInterface)\n+\t\t\t// treat either jq false-y value as rejection\n+\t\t\tif v, _ := iter.Next(); v == false || v == nil {\n+\t\t\t\treturn false\n+\t\t\t}\n+\t\t}\n+\t\treturn true\n+\t}\n+\t// the mess below is so we can lookup the PFS info for each pipeline concurrently.\n+\ttype etcdInfo struct {\n+\t\tname string\n+\t\tptr  *pps.EtcdPipelineInfo\n+\t}\n+\teg, ctx := errgroup.WithContext(pachClient.Ctx())\n+\tetcdInfos := make(chan etcdInfo)\n+\t// stream these out of etcd\n+\teg.Go(func() error {\n+\t\tdefer close(etcdInfos)\n+\t\treturn a.listPipelinePtr(pachClient, request.Pipeline, request.History, func(name string, ptr *pps.EtcdPipelineInfo) error {\n+\t\t\t// copy\n+\t\t\tdata, _ := ptr.Marshal()\n+\t\t\tptr2 := &pps.EtcdPipelineInfo{}\n+\t\t\tif err := ptr2.Unmarshal(data); err != nil {\n+\t\t\t\tpanic(err)\n+\t\t\t}\n+\t\t\tselect {\n+\t\t\tcase <-ctx.Done():\n+\t\t\t\treturn ctx.Err()\n+\t\t\tcase etcdInfos <- etcdInfo{name: name, ptr: ptr2}:\n+\t\t\t\treturn nil\n+\t\t\t}\n+\t\t})\n+\t})\n+\t// spin up goroutines to get the PFS info, and then use the mutex to call f all synchronized like.\n+\tvar mu sync.Mutex\n+\tfor i := 0; i < 20; i++ {\n+\t\teg.Go(func() error {\n+\t\t\tfor info := range etcdInfos {\n+\t\t\t\tpinfo, err := a.resolvePipelineInfo(pachClient, request.AllowIncomplete, info.name, info.ptr)\n \t\t\t\tif err != nil {\n \t\t\t\t\treturn err\n \t\t\t\t}\n-\t\t\t} else {\n-\t\t\t\tpipelineInfo, err = ppsutil.GetPipelineInfo(pachClient, name, ptr)\n-\t\t\t\tif err != nil {\n+\t\t\t\tif err := func() error {\n+\t\t\t\t\tmu.Lock()\n+\t\t\t\t\tdefer mu.Unlock()\n+\t\t\t\t\t// the filtering shares that buffer thing, and it's CPU bound so why not do it with the lock\n+\t\t\t\t\tif !filterPipeline(pinfo) {\n+\t\t\t\t\t\treturn nil\n+\t\t\t\t\t}\n+\t\t\t\t\treturn f(pinfo)", "msg": "The only problem I see is that we may call `f` after it has already errored. Perhaps we should have a bool to bail out after acquiring `mu` if an error has occurred (or the `eg` has been canceled)?"}
{"old_hunk": "@@ -870,6 +870,31 @@ func (a *apiServer) GetRoleBinding(ctx context.Context, req *auth.GetRoleBinding\n \treturn response, nil\n }\n \n+// GetRobotToken implements the protobuf auth.GetRobotToken RPC\n+func (a *apiServer) GetRobotToken(ctx context.Context, req *auth.GetRobotTokenRequest) (resp *auth.GetRobotTokenResponse, retErr error) {\n+\ta.LogReq(req)\n+\tdefer func(start time.Time) { a.LogResp(req, resp, retErr, time.Since(start)) }(time.Now())\n+\n+\tsubject := auth.RobotPrefix + req.Robot", "hunk": "@@ -875,7 +875,10 @@ func (a *apiServer) GetRobotToken(ctx context.Context, req *auth.GetRobotTokenRe\n \ta.LogReq(req)\n \tdefer func(start time.Time) { a.LogResp(req, resp, retErr, time.Since(start)) }(time.Now())\n \n-\tsubject := auth.RobotPrefix + req.Robot\n+\tsubject := req.Robot\n+\tif !strings.HasPrefix(subject, auth.RobotPrefix) {\n+\t\tsubject = auth.RobotPrefix + subject\n+\t}\n \n \ttokenInfo := auth.TokenInfo{\n \t\tSource:  auth.TokenInfo_GET_TOKEN,\n", "comment": "Would it make sense to canonicalize the subject? (e.g. in case a user accidentally sends `robot:foo` and then the token is for `robot:robot:foo` or something?)", "ids": [46272, "8305ce7e4646019f85dbab47f3faee8e6e2e1baa", "71b18bbe550fef8d10c57b499a2ea11a1f9d3190"], "repo": "pachyderm-pachyderm", "ghid": 5800, "old": " \ta.LogReq(req)\n \tdefer func(start time.Time) { a.LogResp(req, resp, retErr, time.Since(start)) }(time.Now())\n-\tsubject := auth.RobotPrefix + req.Robot\n \ttokenInfo := auth.TokenInfo{\n \t\tSource:  auth.TokenInfo_GET_TOKEN,", "new": " \ta.LogReq(req)\n \tdefer func(start time.Time) { a.LogResp(req, resp, retErr, time.Since(start)) }(time.Now())\n+\tsubject := req.Robot\n+\tif !strings.HasPrefix(subject, auth.RobotPrefix) {\n+\t\tsubject = auth.RobotPrefix + subject\n+\t}\n \ttokenInfo := auth.TokenInfo{\n \t\tSource:  auth.TokenInfo_GET_TOKEN,", "lang": "go", "y": 1, "proj": "pachyderm-pachyderm", "patch": "@@ -870,6 +870,31 @@ func (a *apiServer) GetRoleBinding(ctx context.Context, req *auth.GetRoleBinding\n \treturn response, nil\n }\n \n+// GetRobotToken implements the protobuf auth.GetRobotToken RPC\n+func (a *apiServer) GetRobotToken(ctx context.Context, req *auth.GetRobotTokenRequest) (resp *auth.GetRobotTokenResponse, retErr error) {\n+\ta.LogReq(req)\n+\tdefer func(start time.Time) { a.LogResp(req, resp, retErr, time.Since(start)) }(time.Now())\n+\n+\tsubject := auth.RobotPrefix + req.Robot", "msg": "Would it make sense to canonicalize the subject? (e.g. in case a user accidentally sends `robot:foo` and then the token is for `robot:robot:foo` or something?)"}
{"old_hunk": "@@ -875,7 +875,10 @@ func (a *apiServer) GetRobotToken(ctx context.Context, req *auth.GetRobotTokenRe\n \ta.LogReq(req)\n \tdefer func(start time.Time) { a.LogResp(req, resp, retErr, time.Since(start)) }(time.Now())\n \n-\tsubject := auth.RobotPrefix + req.Robot\n+\tsubject := req.Robot\n+\tif !strings.HasPrefix(subject, auth.RobotPrefix) {\n+\t\tsubject = auth.RobotPrefix + subject", "hunk": "@@ -875,11 +875,14 @@ func (a *apiServer) GetRobotToken(ctx context.Context, req *auth.GetRobotTokenRe\n \ta.LogReq(req)\n \tdefer func(start time.Time) { a.LogResp(req, resp, retErr, time.Since(start)) }(time.Now())\n \n-\tsubject := req.Robot\n-\tif !strings.HasPrefix(subject, auth.RobotPrefix) {\n-\t\tsubject = auth.RobotPrefix + subject\n+\t// If the user specified a redundant robot: prefix, strip it. Colons are not permitted in robot names.\n+\tsubject := strings.TrimPrefix(req.Robot, auth.RobotPrefix)\n+\tif strings.Contains(subject, \":\") {\n+\t\treturn nil, errors.New(\"robot names cannot contain colons (':')\")\n \t}\n \n+\tsubject = auth.RobotPrefix + subject\n+\n \ttokenInfo := auth.TokenInfo{\n \t\tSource:  auth.TokenInfo_GET_TOKEN,\n \t\tSubject: subject,\n", "comment": "would it make sense to error/trim if a user sends e.g. `robbot:foo`?", "ids": [46273, "71b18bbe550fef8d10c57b499a2ea11a1f9d3190", "5a6e314204bd8a8ee1f5edaec1cb68d94b7c4463"], "repo": "pachyderm-pachyderm", "ghid": 5800, "old": " \ta.LogReq(req)\n \tdefer func(start time.Time) { a.LogResp(req, resp, retErr, time.Since(start)) }(time.Now())\n-\tsubject := req.Robot\n-\tif !strings.HasPrefix(subject, auth.RobotPrefix) {\n-\t\tsubject = auth.RobotPrefix + subject\n \t}\n \ttokenInfo := auth.TokenInfo{\n \t\tSource:  auth.TokenInfo_GET_TOKEN,\n \t\tSubject: subject,", "new": " \ta.LogReq(req)\n \tdefer func(start time.Time) { a.LogResp(req, resp, retErr, time.Since(start)) }(time.Now())\n+\t// If the user specified a redundant robot: prefix, strip it. Colons are not permitted in robot names.\n+\tsubject := strings.TrimPrefix(req.Robot, auth.RobotPrefix)\n+\tif strings.Contains(subject, \":\") {\n+\t\treturn nil, errors.New(\"robot names cannot contain colons (':')\")\n \t}\n+\tsubject = auth.RobotPrefix + subject\n+\n \ttokenInfo := auth.TokenInfo{\n \t\tSource:  auth.TokenInfo_GET_TOKEN,\n \t\tSubject: subject,", "lang": "go", "y": 1, "proj": "pachyderm-pachyderm", "patch": "@@ -875,7 +875,10 @@ func (a *apiServer) GetRobotToken(ctx context.Context, req *auth.GetRobotTokenRe\n \ta.LogReq(req)\n \tdefer func(start time.Time) { a.LogResp(req, resp, retErr, time.Since(start)) }(time.Now())\n \n-\tsubject := auth.RobotPrefix + req.Robot\n+\tsubject := req.Robot\n+\tif !strings.HasPrefix(subject, auth.RobotPrefix) {\n+\t\tsubject = auth.RobotPrefix + subject", "msg": "would it make sense to error/trim if a user sends e.g. `robbot:foo`?"}
{"old_hunk": "@@ -557,8 +557,10 @@ func standardDeployCmds() []*cobra.Command {\n \t\t\t\topts.ExposeObjectAPI = true\n \n \t\t\t\t// Set the postgres and etcd nodeports explicitly for developers\n-\t\t\t\topts.PostgresOpts.Port = 32228\n-\t\t\t\topts.EtcdOpts.Port = 32379\n+\t\t\t\tif !enterpriseServer {\n+\t\t\t\t\topts.PostgresOpts.Port = 32228", "hunk": "@@ -558,8 +558,8 @@ func standardDeployCmds() []*cobra.Command {\n \n \t\t\t\t// Set the postgres and etcd nodeports explicitly for developers\n \t\t\t\tif !enterpriseServer {\n-\t\t\t\t\topts.PostgresOpts.Port = 32228\n-\t\t\t\t\topts.EtcdOpts.Port = 32379\n+\t\t\t\t\topts.PostgresOpts.Port = dbutil.DefaultPort\n+\t\t\t\t\topts.EtcdOpts.Port = etcdNodePort\n \t\t\t\t}\n \t\t\t}\n \n", "comment": "Aren't these set elsewhere? I think they should be in constants.", "ids": [46369, "db787d8f43ee61eeb365803171a33fda6bbbf482", "db655c120857d6a24b76d3f146d873fe5252004f"], "repo": "pachyderm-pachyderm", "ghid": 5834, "old": " \t\t\t\t// Set the postgres and etcd nodeports explicitly for developers\n \t\t\t\tif !enterpriseServer {\n-\t\t\t\t\topts.PostgresOpts.Port = 32228\n-\t\t\t\t\topts.EtcdOpts.Port = 32379\n \t\t\t\t}\n \t\t\t}", "new": " \t\t\t\t// Set the postgres and etcd nodeports explicitly for developers\n \t\t\t\tif !enterpriseServer {\n+\t\t\t\t\topts.PostgresOpts.Port = dbutil.DefaultPort\n+\t\t\t\t\topts.EtcdOpts.Port = etcdNodePort\n \t\t\t\t}\n \t\t\t}", "lang": "go", "y": 1, "proj": "pachyderm-pachyderm", "patch": "@@ -557,8 +557,10 @@ func standardDeployCmds() []*cobra.Command {\n \t\t\t\topts.ExposeObjectAPI = true\n \n \t\t\t\t// Set the postgres and etcd nodeports explicitly for developers\n-\t\t\t\topts.PostgresOpts.Port = 32228\n-\t\t\t\topts.EtcdOpts.Port = 32379\n+\t\t\t\tif !enterpriseServer {\n+\t\t\t\t\topts.PostgresOpts.Port = 32228", "msg": "Aren't these set elsewhere? I think they should be in constants."}
{"old_hunk": "@@ -453,6 +453,34 @@ func NewForTest() (*APIClient, error) {\n \treturn client, nil\n }\n \n+// NewEnterpriseClientForTest constructs a new APIClient for tests.\n+func NewEnterpriseClientForTest() (*APIClient, error) {", "hunk": "@@ -454,6 +454,7 @@ func NewForTest() (*APIClient, error) {\n }\n \n // NewEnterpriseClientForTest constructs a new APIClient for tests.\n+// TODO(actgardner): this should probably live in testutils and accept a testing.TB\n func NewEnterpriseClientForTest() (*APIClient, error) {\n \tcfg, err := config.Read(false, false)\n \tif err != nil {\n", "comment": "If this is only supposed to be used in tests, I like the pattern of depending on the interface `testing.TB` and then not returning an error and just using the require package to short-circuit on errors. We use the naming scheme `NewTest____(t testing.TB) _____` elsewhere.", "ids": [46367, "db787d8f43ee61eeb365803171a33fda6bbbf482", "db655c120857d6a24b76d3f146d873fe5252004f"], "repo": "pachyderm-pachyderm", "ghid": 5834, "old": " }\n // NewEnterpriseClientForTest constructs a new APIClient for tests.\n func NewEnterpriseClientForTest() (*APIClient, error) {\n \tcfg, err := config.Read(false, false)\n \tif err != nil {", "new": " }\n // NewEnterpriseClientForTest constructs a new APIClient for tests.\n+// TODO(actgardner): this should probably live in testutils and accept a testing.TB\n func NewEnterpriseClientForTest() (*APIClient, error) {\n \tcfg, err := config.Read(false, false)\n \tif err != nil {", "lang": "go", "y": 1, "proj": "pachyderm-pachyderm", "patch": "@@ -453,6 +453,34 @@ func NewForTest() (*APIClient, error) {\n \treturn client, nil\n }\n \n+// NewEnterpriseClientForTest constructs a new APIClient for tests.\n+func NewEnterpriseClientForTest() (*APIClient, error) {", "msg": "If this is only supposed to be used in tests, I like the pattern of depending on the interface `testing.TB` and then not returning an error and just using the require package to short-circuit on errors. We use the naming scheme `NewTest____(t testing.TB) _____` elsewhere."}
{"old_hunk": "@@ -1294,54 +1294,49 @@ func buildHelper(pc *pachdclient.APIClient, request *ppsclient.CreatePipelineReq\n \t}\n \n \t// insert the source code\n-\tpfc, err := pc.NewPutFileClient()\n-\tif err != nil {\n-\t\treturn errors.Wrapf(err, \"failed to construct put file client for source code in build step-enabled pipeline\")\n-\t}\n-\tif update {\n-\t\tif err = pfc.DeleteFile(buildPipelineName, \"source\", \"/\"); err != nil {\n-\t\t\treturn errors.Wrapf(err, \"failed to delete existing source code for build step-enabled pipeline\")\n-\t\t}\n-\t}\n-\tif err := filepath.Walk(buildPath, func(srcFilePath string, info os.FileInfo, _ error) (retErr error) {\n-\t\tif info == nil {\n-\t\t\treturn errors.Errorf(\"%q doesn't exist\", srcFilePath)\n-\t\t}\n-\t\tif info.IsDir() {\n-\t\t\treturn nil\n-\t\t}\n-\n-\t\tdestFilePath, err := filepath.Rel(buildPath, srcFilePath)\n-\t\tif err != nil {\n-\t\t\treturn errors.Wrapf(err, \"failed to discover relative path for %s\", srcFilePath)\n+\tif err := pc.WithModifyFileClient(buildPipelineName, \"source\", func(mfc pachdclient.ModifyFileClient) error {\n+\t\tif update {\n+\t\t\tif err := mfc.DeleteFile(\"/\"); err != nil {\n+\t\t\t\treturn errors.Wrapf(err, \"failed to delete existing source code for build step-enabled pipeline\")\n+\t\t\t}\n \t\t}\n-\t\tfor _, g := range ignores {\n-\t\t\tif g.Match(destFilePath) {\n+\t\treturn filepath.Walk(buildPath, func(srcFilePath string, info os.FileInfo, _ error) (retErr error) {\n+\t\t\tif info == nil {\n+\t\t\t\treturn errors.Errorf(\"%q doesn't exist\", srcFilePath)\n+\t\t\t}\n+\t\t\tif info.IsDir() {\n \t\t\t\treturn nil\n \t\t\t}\n-\t\t}\n \n-\t\tf, err := progress.Open(srcFilePath)\n-\t\tif err != nil {\n-\t\t\treturn errors.Wrapf(err, \"failed to open file %q for source code in build step-enabled pipeline\", srcFilePath)\n-\t\t}\n-\t\tdefer func() {\n-\t\t\tif err := f.Close(); err != nil && retErr == nil {\n-\t\t\t\tretErr = err\n+\t\t\tdestFilePath, err := filepath.Rel(buildPath, srcFilePath)\n+\t\t\tif err != nil {\n+\t\t\t\treturn errors.Wrapf(err, \"failed to discover relative path for %s\", srcFilePath)", "hunk": "@@ -1302,7 +1302,7 @@ func buildHelper(pc *pachdclient.APIClient, request *ppsclient.CreatePipelineReq\n \t\t}\n \t\treturn filepath.Walk(buildPath, func(srcFilePath string, info os.FileInfo, _ error) (retErr error) {\n \t\t\tif info == nil {\n-\t\t\t\treturn errors.Errorf(\"%q doesn't exist\", srcFilePath)\n+\t\t\t\treturn errors.Errorf(\"%s doesn't exist\", srcFilePath)\n \t\t\t}\n \t\t\tif info.IsDir() {\n \t\t\t\treturn nil\n", "comment": "both of these should be q or s", "ids": [46435, "48438e9129542faa655d7edc3ede42b1c11e5505", "05b68aae4879688cd8081bff13eabf5e5d9d7952"], "repo": "pachyderm-pachyderm", "ghid": 5851, "old": " \t\t}\n \t\treturn filepath.Walk(buildPath, func(srcFilePath string, info os.FileInfo, _ error) (retErr error) {\n \t\t\tif info == nil {\n-\t\t\t\treturn errors.Errorf(\"%q doesn't exist\", srcFilePath)\n \t\t\t}\n \t\t\tif info.IsDir() {\n \t\t\t\treturn nil", "new": " \t\t}\n \t\treturn filepath.Walk(buildPath, func(srcFilePath string, info os.FileInfo, _ error) (retErr error) {\n \t\t\tif info == nil {\n+\t\t\t\treturn errors.Errorf(\"%s doesn't exist\", srcFilePath)\n \t\t\t}\n \t\t\tif info.IsDir() {\n \t\t\t\treturn nil", "lang": "go", "y": 1, "proj": "pachyderm-pachyderm", "patch": "@@ -1294,54 +1294,49 @@ func buildHelper(pc *pachdclient.APIClient, request *ppsclient.CreatePipelineReq\n \t}\n \n \t// insert the source code\n-\tpfc, err := pc.NewPutFileClient()\n-\tif err != nil {\n-\t\treturn errors.Wrapf(err, \"failed to construct put file client for source code in build step-enabled pipeline\")\n-\t}\n-\tif update {\n-\t\tif err = pfc.DeleteFile(buildPipelineName, \"source\", \"/\"); err != nil {\n-\t\t\treturn errors.Wrapf(err, \"failed to delete existing source code for build step-enabled pipeline\")\n-\t\t}\n-\t}\n-\tif err := filepath.Walk(buildPath, func(srcFilePath string, info os.FileInfo, _ error) (retErr error) {\n-\t\tif info == nil {\n-\t\t\treturn errors.Errorf(\"%q doesn't exist\", srcFilePath)\n-\t\t}\n-\t\tif info.IsDir() {\n-\t\t\treturn nil\n-\t\t}\n-\n-\t\tdestFilePath, err := filepath.Rel(buildPath, srcFilePath)\n-\t\tif err != nil {\n-\t\t\treturn errors.Wrapf(err, \"failed to discover relative path for %s\", srcFilePath)\n+\tif err := pc.WithModifyFileClient(buildPipelineName, \"source\", func(mfc pachdclient.ModifyFileClient) error {\n+\t\tif update {\n+\t\t\tif err := mfc.DeleteFile(\"/\"); err != nil {\n+\t\t\t\treturn errors.Wrapf(err, \"failed to delete existing source code for build step-enabled pipeline\")\n+\t\t\t}\n \t\t}\n-\t\tfor _, g := range ignores {\n-\t\t\tif g.Match(destFilePath) {\n+\t\treturn filepath.Walk(buildPath, func(srcFilePath string, info os.FileInfo, _ error) (retErr error) {\n+\t\t\tif info == nil {\n+\t\t\t\treturn errors.Errorf(\"%q doesn't exist\", srcFilePath)\n+\t\t\t}\n+\t\t\tif info.IsDir() {\n \t\t\t\treturn nil\n \t\t\t}\n-\t\t}\n \n-\t\tf, err := progress.Open(srcFilePath)\n-\t\tif err != nil {\n-\t\t\treturn errors.Wrapf(err, \"failed to open file %q for source code in build step-enabled pipeline\", srcFilePath)\n-\t\t}\n-\t\tdefer func() {\n-\t\t\tif err := f.Close(); err != nil && retErr == nil {\n-\t\t\t\tretErr = err\n+\t\t\tdestFilePath, err := filepath.Rel(buildPath, srcFilePath)\n+\t\t\tif err != nil {\n+\t\t\t\treturn errors.Wrapf(err, \"failed to discover relative path for %s\", srcFilePath)", "msg": "both of these should be q or s"}
{"old_hunk": "@@ -174,35 +160,36 @@ func (mfc *modifyFileCore) maybeError(f func() error) (retErr error) {\n \treturn f()\n }\n \n-func (mfc *modifyFileCore) sendAppendFile(req *pfs.AppendFile) error {\n+func (mfc *modifyFileCore) sendPutFile(req *pfs.PutFile) error {\n \treturn mfc.client.Send(&pfs.ModifyFileRequest{\n-\t\tModification: &pfs.ModifyFileRequest_AppendFile{\n-\t\t\tAppendFile: req,\n+\t\tModification: &pfs.ModifyFileRequest_PutFile{\n+\t\t\tPutFile: req,\n \t\t},\n \t})\n }\n \n-// AppendFileTar appends a set of files from a tar stream.\n-func (mfc *modifyFileCore) AppendFileTar(overwrite bool, r io.Reader, tag ...string) error {\n+var errV1NotImplemented = errors.Errorf(\"V1 method not implemented\")", "hunk": "@@ -168,12 +168,6 @@ func (mfc *modifyFileCore) sendPutFile(req *pfs.PutFile) error {\n \t})\n }\n \n-var errV1NotImplemented = errors.Errorf(\"V1 method not implemented\")\n-\n-func (mfc *modifyFileCore) PutFileWriter(path string, opts ...PutFileOption) (io.WriteCloser, error) {\n-\treturn nil, errV1NotImplemented\n-}\n-\n func (mfc *modifyFileCore) PutFileTar(r io.Reader, opts ...PutFileOption) error {\n \treturn mfc.maybeError(func() error {\n \t\tpf := &pfs.PutFile{\n", "comment": "See above, I think we should just break the API. This was useful when v1 and v2 lived in the same codebase.", "ids": [46431, "48438e9129542faa655d7edc3ede42b1c11e5505", "05b68aae4879688cd8081bff13eabf5e5d9d7952"], "repo": "pachyderm-pachyderm", "ghid": 5851, "old": " \t})\n }\n-var errV1NotImplemented = errors.Errorf(\"V1 method not implemented\")\n-\n-func (mfc *modifyFileCore) PutFileWriter(path string, opts ...PutFileOption) (io.WriteCloser, error) {\n-\treturn nil, errV1NotImplemented\n-}\n-\n func (mfc *modifyFileCore) PutFileTar(r io.Reader, opts ...PutFileOption) error {\n \treturn mfc.maybeError(func() error {\n \t\tpf := &pfs.PutFile{", "new": " \t})\n }\n func (mfc *modifyFileCore) PutFileTar(r io.Reader, opts ...PutFileOption) error {\n \treturn mfc.maybeError(func() error {\n \t\tpf := &pfs.PutFile{", "lang": "go", "y": 1, "proj": "pachyderm-pachyderm", "patch": "@@ -174,35 +160,36 @@ func (mfc *modifyFileCore) maybeError(f func() error) (retErr error) {\n \treturn f()\n }\n \n-func (mfc *modifyFileCore) sendAppendFile(req *pfs.AppendFile) error {\n+func (mfc *modifyFileCore) sendPutFile(req *pfs.PutFile) error {\n \treturn mfc.client.Send(&pfs.ModifyFileRequest{\n-\t\tModification: &pfs.ModifyFileRequest_AppendFile{\n-\t\t\tAppendFile: req,\n+\t\tModification: &pfs.ModifyFileRequest_PutFile{\n+\t\t\tPutFile: req,\n \t\t},\n \t})\n }\n \n-// AppendFileTar appends a set of files from a tar stream.\n-func (mfc *modifyFileCore) AppendFileTar(overwrite bool, r io.Reader, tag ...string) error {\n+var errV1NotImplemented = errors.Errorf(\"V1 method not implemented\")", "msg": "See above, I think we should just break the API. This was useful when v1 and v2 lived in the same codebase."}
{"old_hunk": "@@ -230,9 +230,15 @@ func (a *apiServer) AddCluster(ctx context.Context, req *lc.AddClusterRequest) (\n \t}, nil\n }\n \n+func stripSecretFromRequest(req *lc.HeartbeatRequest) *lc.HeartbeatRequest {\n+\tr := *req\n+\tr.Secret = \"\"\n+\treturn &r\n+}\n+\n func (a *apiServer) Heartbeat(ctx context.Context, req *lc.HeartbeatRequest) (resp *lc.HeartbeatResponse, retErr error) {\n \ta.LogReq(nil)", "hunk": "@@ -237,8 +237,9 @@ func stripSecretFromRequest(req *lc.HeartbeatRequest) *lc.HeartbeatRequest {\n }\n \n func (a *apiServer) Heartbeat(ctx context.Context, req *lc.HeartbeatRequest) (resp *lc.HeartbeatResponse, retErr error) {\n-\ta.LogReq(nil)\n-\tdefer func(start time.Time) { a.pachLogger.Log(stripSecretFromRequest(req), nil, retErr, time.Since(start)) }(time.Now())\n+\tredactedRequest := stripSecretFromRequest(req)\n+\ta.LogReq(redactedRequest)\n+\tdefer func(start time.Time) { a.pachLogger.Log(redactedRequest, nil, retErr, time.Since(start)) }(time.Now())\n \n \tvar count int\n \tif err := a.env.GetDBClient().GetContext(ctx, &count, `SELECT COUNT(*) FROM license.clusters WHERE id=$1 and secret=$2`, req.Id, req.Secret); err != nil {\n", "comment": "Can you also update `LogReq` to log the request with the secret redacted?", "ids": [46522, "e2e83ddd26c6362beda9c2f51eb2038afe473a7b", "19adf0ce58bb07bf81c98c8239b5bbe9ffc4b27b"], "repo": "pachyderm-pachyderm", "ghid": 5873, "old": " }\n func (a *apiServer) Heartbeat(ctx context.Context, req *lc.HeartbeatRequest) (resp *lc.HeartbeatResponse, retErr error) {\n-\ta.LogReq(nil)\n-\tdefer func(start time.Time) { a.pachLogger.Log(stripSecretFromRequest(req), nil, retErr, time.Since(start)) }(time.Now())\n \tvar count int\n \tif err := a.env.GetDBClient().GetContext(ctx, &count, `SELECT COUNT(*) FROM license.clusters WHERE id=$1 and secret=$2`, req.Id, req.Secret); err != nil {", "new": " }\n func (a *apiServer) Heartbeat(ctx context.Context, req *lc.HeartbeatRequest) (resp *lc.HeartbeatResponse, retErr error) {\n+\tredactedRequest := stripSecretFromRequest(req)\n+\ta.LogReq(redactedRequest)\n+\tdefer func(start time.Time) { a.pachLogger.Log(redactedRequest, nil, retErr, time.Since(start)) }(time.Now())\n \tvar count int\n \tif err := a.env.GetDBClient().GetContext(ctx, &count, `SELECT COUNT(*) FROM license.clusters WHERE id=$1 and secret=$2`, req.Id, req.Secret); err != nil {", "lang": "go", "y": 1, "proj": "pachyderm-pachyderm", "patch": "@@ -230,9 +230,15 @@ func (a *apiServer) AddCluster(ctx context.Context, req *lc.AddClusterRequest) (\n \t}, nil\n }\n \n+func stripSecretFromRequest(req *lc.HeartbeatRequest) *lc.HeartbeatRequest {\n+\tr := *req\n+\tr.Secret = \"\"\n+\treturn &r\n+}\n+\n func (a *apiServer) Heartbeat(ctx context.Context, req *lc.HeartbeatRequest) (resp *lc.HeartbeatResponse, retErr error) {\n \ta.LogReq(nil)", "msg": "Can you also update `LogReq` to log the request with the secret redacted?"}
{"old_hunk": "@@ -1,51 +1,67 @@\n package auth\n \n import (\n+\t\"fmt\"\n \t\"github.com/pachyderm/pachyderm/v2/src/auth\"\n \t\"github.com/pachyderm/pachyderm/v2/src/client\"\n )\n \n-type authHandler func(*client.APIClient, string) error\n+// an authHandler can optionally return a username string that will be cached in the request's context\n+type authHandler func(*client.APIClient, string) (string, error)\n+\n+type ContextKey string\n+\n+const WhoAmIResultKey = ContextKey(\"WhoAmI\")", "hunk": "@@ -1,6 +1,7 @@\n package auth\n \n import (\n+\t\"context\"\n \t\"fmt\"\n \t\"github.com/pachyderm/pachyderm/v2/src/auth\"\n \t\"github.com/pachyderm/pachyderm/v2/src/client\"\n", "comment": "I think it makes sense to make these package-private, and expose a `GetWhoAmI(ctx)` method that accepts a context and returns the username if it's set? That way we know it can only be set in this package.", "ids": [46546, "5e4c67aa2d75e54c57f37c27161c18722f85ebf1", "70746ebaa13627c2363f05e6fc74103cd2c58360"], "repo": "pachyderm-pachyderm", "ghid": 5884, "old": " package auth\n import (\n \t\"fmt\"\n \t\"github.com/pachyderm/pachyderm/v2/src/auth\"\n \t\"github.com/pachyderm/pachyderm/v2/src/client\"", "new": " package auth\n import (\n+\t\"context\"\n \t\"fmt\"\n \t\"github.com/pachyderm/pachyderm/v2/src/auth\"\n \t\"github.com/pachyderm/pachyderm/v2/src/client\"", "lang": "go", "y": 1, "proj": "pachyderm-pachyderm", "patch": "@@ -1,51 +1,67 @@\n package auth\n \n import (\n+\t\"fmt\"\n \t\"github.com/pachyderm/pachyderm/v2/src/auth\"\n \t\"github.com/pachyderm/pachyderm/v2/src/client\"\n )\n \n-type authHandler func(*client.APIClient, string) error\n+// an authHandler can optionally return a username string that will be cached in the request's context\n+type authHandler func(*client.APIClient, string) (string, error)\n+\n+type ContextKey string\n+\n+const WhoAmIResultKey = ContextKey(\"WhoAmI\")", "msg": "I think it makes sense to make these package-private, and expose a `GetWhoAmI(ctx)` method that accepts a context and returns the username if it's set? That way we know it can only be set in this package."}
{"old_hunk": "@@ -65,6 +68,62 @@ type EtcdOpts struct {\n \tPort int32\n }\n \n+// WriteEtcdAssets generates all of the etcd-related parts of the kubernetes\n+// manifest according to the given options and writes it into the given encoder.\n+func WriteEtcdAssets(encoder serde.Encoder, opts *AssetOpts, objectStoreBackend Backend,\n+\tpersistentDiskBackend Backend, volumeSize int,\n+\thostPath string) error {\n+\tif opts.EtcdOpts.Nodes > 0 && opts.EtcdOpts.Volume != \"\" {\n+\t\treturn errors.Errorf(\"only one of --dynamic-etcd-nodes and --static-etcd-volume should be given, but not both\")\n+\t}\n+\n+\t// In the dynamic route, we create a storage class which dynamically\n+\t// provisions volumes, and run etcd as a stateful set.\n+\t// In the static route, we create a single volume, a single volume\n+\t// claim, and run etcd as a replication controller with a single node.\n+\tif persistentDiskBackend == LocalBackend {\n+\t\tif err := encoder.Encode(EtcdDeployment(opts, hostPath)); err != nil {\n+\t\t\treturn err\n+\t\t}\n+\t} else if opts.EtcdOpts.Nodes > 0 {\n+\t\t// Create a StorageClass, if the user didn't provide one.\n+\t\tif opts.EtcdOpts.StorageClassName == \"\" {\n+\t\t\tif sc := EtcdStorageClass(opts, persistentDiskBackend); sc != nil {\n+\t\t\t\tif err := encoder.Encode(sc); err != nil {\n+\t\t\t\t\treturn err\n+\t\t\t\t}\n+\t\t\t}\n+\t\t}\n+\t\tif err := encoder.Encode(EtcdHeadlessService(opts)); err != nil {\n+\t\t\treturn err\n+\t\t}\n+\t\tif err := encoder.Encode(EtcdStatefulSet(opts, persistentDiskBackend, volumeSize)); err != nil {\n+\t\t\treturn err\n+\t\t}\n+\t} else if opts.EtcdOpts.Volume != \"\" {\n+\t\tvolume, err := EtcdVolume(persistentDiskBackend, opts, hostPath, opts.EtcdOpts.Volume, volumeSize)\n+\t\tif err != nil {\n+\t\t\treturn err\n+\t\t}\n+\t\tif err = encoder.Encode(volume); err != nil {\n+\t\t\treturn err\n+\t\t}\n+\t\tif err = encoder.Encode(EtcdVolumeClaim(volumeSize, opts)); err != nil {\n+\t\t\treturn err\n+\t\t}\n+\t\tif err = encoder.Encode(EtcdDeployment(opts, \"\")); err != nil {\n+\t\t\treturn err\n+\t\t}\n+\t} else {\n+\t\treturn errors.Errorf(\"unless deploying locally, either --dynamic-etcd-nodes or --static-etcd-volume needs to be provided\")\n+\t}\n+\tif err := encoder.Encode(EtcdNodePortService(opts)); err != nil {", "hunk": "@@ -117,11 +117,7 @@ func WriteEtcdAssets(encoder serde.Encoder, opts *AssetOpts, objectStoreBackend\n \t} else {\n \t\treturn errors.Errorf(\"unless deploying locally, either --dynamic-etcd-nodes or --static-etcd-volume needs to be provided\")\n \t}\n-\tif err := encoder.Encode(EtcdNodePortService(opts)); err != nil {\n-\t\treturn err\n-\t}\n-\n-\treturn nil\n+\treturn encoder.Encode(EtcdNodePortService(opts))\n }\n \n // EtcdDeployment returns an etcd k8s Deployment.\n", "comment": "nit, you could just return the result of the last encode call", "ids": [46691, "81b08a4c7be557c7d545dbfdf1c8bce01c3b47d9", "f4d4b84258ed359bc1ad9d76af823228340af92c"], "repo": "pachyderm-pachyderm", "ghid": 5969, "old": " \t} else {\n \t\treturn errors.Errorf(\"unless deploying locally, either --dynamic-etcd-nodes or --static-etcd-volume needs to be provided\")\n \t}\n-\tif err := encoder.Encode(EtcdNodePortService(opts)); err != nil {\n-\t\treturn err\n-\t}\n-\n-\treturn nil\n }\n // EtcdDeployment returns an etcd k8s Deployment.", "new": " \t} else {\n \t\treturn errors.Errorf(\"unless deploying locally, either --dynamic-etcd-nodes or --static-etcd-volume needs to be provided\")\n \t}\n+\treturn encoder.Encode(EtcdNodePortService(opts))\n }\n // EtcdDeployment returns an etcd k8s Deployment.", "lang": "go", "y": 1, "proj": "pachyderm-pachyderm", "patch": "@@ -65,6 +68,62 @@ type EtcdOpts struct {\n \tPort int32\n }\n \n+// WriteEtcdAssets generates all of the etcd-related parts of the kubernetes\n+// manifest according to the given options and writes it into the given encoder.\n+func WriteEtcdAssets(encoder serde.Encoder, opts *AssetOpts, objectStoreBackend Backend,\n+\tpersistentDiskBackend Backend, volumeSize int,\n+\thostPath string) error {\n+\tif opts.EtcdOpts.Nodes > 0 && opts.EtcdOpts.Volume != \"\" {\n+\t\treturn errors.Errorf(\"only one of --dynamic-etcd-nodes and --static-etcd-volume should be given, but not both\")\n+\t}\n+\n+\t// In the dynamic route, we create a storage class which dynamically\n+\t// provisions volumes, and run etcd as a stateful set.\n+\t// In the static route, we create a single volume, a single volume\n+\t// claim, and run etcd as a replication controller with a single node.\n+\tif persistentDiskBackend == LocalBackend {\n+\t\tif err := encoder.Encode(EtcdDeployment(opts, hostPath)); err != nil {\n+\t\t\treturn err\n+\t\t}\n+\t} else if opts.EtcdOpts.Nodes > 0 {\n+\t\t// Create a StorageClass, if the user didn't provide one.\n+\t\tif opts.EtcdOpts.StorageClassName == \"\" {\n+\t\t\tif sc := EtcdStorageClass(opts, persistentDiskBackend); sc != nil {\n+\t\t\t\tif err := encoder.Encode(sc); err != nil {\n+\t\t\t\t\treturn err\n+\t\t\t\t}\n+\t\t\t}\n+\t\t}\n+\t\tif err := encoder.Encode(EtcdHeadlessService(opts)); err != nil {\n+\t\t\treturn err\n+\t\t}\n+\t\tif err := encoder.Encode(EtcdStatefulSet(opts, persistentDiskBackend, volumeSize)); err != nil {\n+\t\t\treturn err\n+\t\t}\n+\t} else if opts.EtcdOpts.Volume != \"\" {\n+\t\tvolume, err := EtcdVolume(persistentDiskBackend, opts, hostPath, opts.EtcdOpts.Volume, volumeSize)\n+\t\tif err != nil {\n+\t\t\treturn err\n+\t\t}\n+\t\tif err = encoder.Encode(volume); err != nil {\n+\t\t\treturn err\n+\t\t}\n+\t\tif err = encoder.Encode(EtcdVolumeClaim(volumeSize, opts)); err != nil {\n+\t\t\treturn err\n+\t\t}\n+\t\tif err = encoder.Encode(EtcdDeployment(opts, \"\")); err != nil {\n+\t\t\treturn err\n+\t\t}\n+\t} else {\n+\t\treturn errors.Errorf(\"unless deploying locally, either --dynamic-etcd-nodes or --static-etcd-volume needs to be provided\")\n+\t}\n+\tif err := encoder.Encode(EtcdNodePortService(opts)); err != nil {", "msg": "nit, you could just return the result of the last encode call"}
{"old_hunk": "@@ -51,6 +54,66 @@ type PostgresOpts struct {\n \tPort int32\n }\n \n+// WritePostgresAssets generates all of the postgres-related parts of the\n+// kubernetes manifest according to the given options and writes it into the\n+// given encoder.\n+func WritePostgresAssets(encoder serde.Encoder, opts *AssetOpts, objectStoreBackend Backend,\n+\tpersistentDiskBackend Backend, volumeSize int,\n+\thostPath string) error {\n+\tif err := encoder.Encode(PostgresInitConfigMap(opts)); err != nil {\n+\t\treturn err\n+\t}\n+\n+\t// In the dynamic route, we create a storage class which dynamically\n+\t// provisions volumes, and run postgres as a stateful set.\n+\t// In the static route, we create a single volume, a single volume\n+\t// claim, and run postgres as a replication controller with a single node.\n+\tif persistentDiskBackend == LocalBackend {\n+\t\tif err := encoder.Encode(PostgresDeployment(opts, hostPath)); err != nil {\n+\t\t\treturn err\n+\t\t}\n+\t} else if opts.PostgresOpts.Nodes > 0 {\n+\t\t// TODO: Add support for multiple Postgres pods?\n+\t\tif opts.PostgresOpts.Nodes > 1 {\n+\t\t\treturn errors.Errorf(\"--dynamic-postgres-nodes must be equal to 1\")\n+\t\t}\n+\t\t// Create a StorageClass, if the user didn't provide one.\n+\t\tif opts.PostgresOpts.StorageClassName == \"\" {\n+\t\t\tif sc := PostgresStorageClass(opts, persistentDiskBackend); sc != nil {\n+\t\t\t\tif err := encoder.Encode(sc); err != nil {\n+\t\t\t\t\treturn err\n+\t\t\t\t}\n+\t\t\t}\n+\t\t}\n+\t\tif err := encoder.Encode(PostgresHeadlessService(opts)); err != nil {\n+\t\t\treturn err\n+\t\t}\n+\t\tif err := encoder.Encode(PostgresStatefulSet(opts, persistentDiskBackend, volumeSize)); err != nil {\n+\t\t\treturn err\n+\t\t}\n+\t} else if opts.PostgresOpts.Volume != \"\" {\n+\t\tvolume, err := PostgresVolume(persistentDiskBackend, opts, hostPath, opts.PostgresOpts.Volume, volumeSize)\n+\t\tif err != nil {\n+\t\t\treturn err\n+\t\t}\n+\t\tif err = encoder.Encode(volume); err != nil {\n+\t\t\treturn err\n+\t\t}\n+\t\tif err = encoder.Encode(PostgresVolumeClaim(volumeSize, opts)); err != nil {\n+\t\t\treturn err\n+\t\t}\n+\t\tif err = encoder.Encode(PostgresDeployment(opts, \"\")); err != nil {\n+\t\t\treturn err\n+\t\t}\n+\t} else {\n+\t\treturn errors.Errorf(\"unless deploying locally, either --dynamic-postgres-nodes or --static-postgres-volume needs to be provided\")\n+\t}\n+\tif err := encoder.Encode(PostgresService(opts)); err != nil {", "hunk": "@@ -108,10 +108,7 @@ func WritePostgresAssets(encoder serde.Encoder, opts *AssetOpts, objectStoreBack\n \t} else {\n \t\treturn errors.Errorf(\"unless deploying locally, either --dynamic-postgres-nodes or --static-postgres-volume needs to be provided\")\n \t}\n-\tif err := encoder.Encode(PostgresService(opts)); err != nil {\n-\t\treturn err\n-\t}\n-\treturn nil\n+\treturn encoder.Encode(PostgresService(opts))\n }\n \n // PostgresDeployment generates a Deployment for the pachyderm postgres instance.\n", "comment": "similar to above, just return the result of the encode call.", "ids": [46692, "81b08a4c7be557c7d545dbfdf1c8bce01c3b47d9", "f4d4b84258ed359bc1ad9d76af823228340af92c"], "repo": "pachyderm-pachyderm", "ghid": 5969, "old": " \t} else {\n \t\treturn errors.Errorf(\"unless deploying locally, either --dynamic-postgres-nodes or --static-postgres-volume needs to be provided\")\n \t}\n-\tif err := encoder.Encode(PostgresService(opts)); err != nil {\n-\t\treturn err\n-\t}\n-\treturn nil\n }\n // PostgresDeployment generates a Deployment for the pachyderm postgres instance.", "new": " \t} else {\n \t\treturn errors.Errorf(\"unless deploying locally, either --dynamic-postgres-nodes or --static-postgres-volume needs to be provided\")\n \t}\n+\treturn encoder.Encode(PostgresService(opts))\n }\n // PostgresDeployment generates a Deployment for the pachyderm postgres instance.", "lang": "go", "y": 1, "proj": "pachyderm-pachyderm", "patch": "@@ -51,6 +54,66 @@ type PostgresOpts struct {\n \tPort int32\n }\n \n+// WritePostgresAssets generates all of the postgres-related parts of the\n+// kubernetes manifest according to the given options and writes it into the\n+// given encoder.\n+func WritePostgresAssets(encoder serde.Encoder, opts *AssetOpts, objectStoreBackend Backend,\n+\tpersistentDiskBackend Backend, volumeSize int,\n+\thostPath string) error {\n+\tif err := encoder.Encode(PostgresInitConfigMap(opts)); err != nil {\n+\t\treturn err\n+\t}\n+\n+\t// In the dynamic route, we create a storage class which dynamically\n+\t// provisions volumes, and run postgres as a stateful set.\n+\t// In the static route, we create a single volume, a single volume\n+\t// claim, and run postgres as a replication controller with a single node.\n+\tif persistentDiskBackend == LocalBackend {\n+\t\tif err := encoder.Encode(PostgresDeployment(opts, hostPath)); err != nil {\n+\t\t\treturn err\n+\t\t}\n+\t} else if opts.PostgresOpts.Nodes > 0 {\n+\t\t// TODO: Add support for multiple Postgres pods?\n+\t\tif opts.PostgresOpts.Nodes > 1 {\n+\t\t\treturn errors.Errorf(\"--dynamic-postgres-nodes must be equal to 1\")\n+\t\t}\n+\t\t// Create a StorageClass, if the user didn't provide one.\n+\t\tif opts.PostgresOpts.StorageClassName == \"\" {\n+\t\t\tif sc := PostgresStorageClass(opts, persistentDiskBackend); sc != nil {\n+\t\t\t\tif err := encoder.Encode(sc); err != nil {\n+\t\t\t\t\treturn err\n+\t\t\t\t}\n+\t\t\t}\n+\t\t}\n+\t\tif err := encoder.Encode(PostgresHeadlessService(opts)); err != nil {\n+\t\t\treturn err\n+\t\t}\n+\t\tif err := encoder.Encode(PostgresStatefulSet(opts, persistentDiskBackend, volumeSize)); err != nil {\n+\t\t\treturn err\n+\t\t}\n+\t} else if opts.PostgresOpts.Volume != \"\" {\n+\t\tvolume, err := PostgresVolume(persistentDiskBackend, opts, hostPath, opts.PostgresOpts.Volume, volumeSize)\n+\t\tif err != nil {\n+\t\t\treturn err\n+\t\t}\n+\t\tif err = encoder.Encode(volume); err != nil {\n+\t\t\treturn err\n+\t\t}\n+\t\tif err = encoder.Encode(PostgresVolumeClaim(volumeSize, opts)); err != nil {\n+\t\t\treturn err\n+\t\t}\n+\t\tif err = encoder.Encode(PostgresDeployment(opts, \"\")); err != nil {\n+\t\t\treturn err\n+\t\t}\n+\t} else {\n+\t\treturn errors.Errorf(\"unless deploying locally, either --dynamic-postgres-nodes or --static-postgres-volume needs to be provided\")\n+\t}\n+\tif err := encoder.Encode(PostgresService(opts)); err != nil {", "msg": "similar to above, just return the result of the encode call."}
{"old_hunk": "@@ -672,6 +683,13 @@ func (a *apiServer) CreateRoleBindingInTransaction(txnCtx *txnenv.TransactionCon\n \t// Call Create, this will raise an error if the role binding already exists.\n \tkey := resourceKey(resource)\n \troleBindings := a.roleBindings.ReadWrite(txnCtx.SqlTx)\n+\t// TODO(acohen4): what should the collision semantics be here?", "hunk": "@@ -683,12 +683,6 @@ func (a *apiServer) CreateRoleBindingInTransaction(txnCtx *txnenv.TransactionCon\n \t// Call Create, this will raise an error if the role binding already exists.\n \tkey := resourceKey(resource)\n \troleBindings := a.roleBindings.ReadWrite(txnCtx.SqlTx)\n-\t// TODO(acohen4): what should the collision semantics be here?\n-\tif b, err := a.GetRoleBindingInTransaction(txnCtx, &auth.GetRoleBindingRequest{Resource: resource}); err != nil {\n-\t\treturn err\n-\t} else if len(b.Binding.Entries) > 0 {\n-\t\treturn nil\n-\t}\n \n \tif err := roleBindings.Create(key, bindings); err != nil {\n \t\treturn err\n", "comment": "Is it necessary to add a read here? If the role binding key has a uniqueness constraint, whatever transaction commits second should fail and the first transaction will succeed at creating the repo.", "ids": [46744, "b4c2b4a872d5a8aea20fe487bbe2843e5437f795", "283fcb16e12532ec056d7a19ff88a7edd28ca910"], "repo": "pachyderm-pachyderm", "ghid": 5998, "old": " \t// Call Create, this will raise an error if the role binding already exists.\n \tkey := resourceKey(resource)\n \troleBindings := a.roleBindings.ReadWrite(txnCtx.SqlTx)\n-\t// TODO(acohen4): what should the collision semantics be here?\n-\tif b, err := a.GetRoleBindingInTransaction(txnCtx, &auth.GetRoleBindingRequest{Resource: resource}); err != nil {\n-\t\treturn err\n-\t} else if len(b.Binding.Entries) > 0 {\n-\t\treturn nil\n-\t}\n \tif err := roleBindings.Create(key, bindings); err != nil {\n \t\treturn err", "new": " \t// Call Create, this will raise an error if the role binding already exists.\n \tkey := resourceKey(resource)\n \troleBindings := a.roleBindings.ReadWrite(txnCtx.SqlTx)\n \tif err := roleBindings.Create(key, bindings); err != nil {\n \t\treturn err", "lang": "go", "y": 1, "proj": "pachyderm-pachyderm", "patch": "@@ -672,6 +683,13 @@ func (a *apiServer) CreateRoleBindingInTransaction(txnCtx *txnenv.TransactionCon\n \t// Call Create, this will raise an error if the role binding already exists.\n \tkey := resourceKey(resource)\n \troleBindings := a.roleBindings.ReadWrite(txnCtx.SqlTx)\n+\t// TODO(acohen4): what should the collision semantics be here?", "msg": "Is it necessary to add a read here? If the role binding key has a uniqueness constraint, whatever transaction commits second should fail and the first transaction will succeed at creating the repo."}
{"old_hunk": "@@ -258,7 +264,13 @@ func (a *apiServer) getClusterRoleBinding(ctx context.Context) (*auth.RoleBindin\n }\n \n func (a *apiServer) isActive(ctx context.Context) error {\n-\t_, err := a.getClusterRoleBinding(ctx)\n+\treturn a.txnEnv.WithReadContext(ctx, func(txnCtx *txnenv.TransactionContext) error {\n+\t\treturn a.isActiveInTransaction(txnCtx)", "hunk": "@@ -264,13 +264,7 @@ func (a *apiServer) getClusterRoleBindingInTransaction(txnCtx *txnenv.Transactio\n }\n \n func (a *apiServer) isActive(ctx context.Context) error {\n-\treturn a.txnEnv.WithReadContext(ctx, func(txnCtx *txnenv.TransactionContext) error {\n-\t\treturn a.isActiveInTransaction(txnCtx)\n-\t})\n-}\n-\n-func (a *apiServer) isActiveInTransaction(txnCtx *txnenv.TransactionContext) error {\n-\t_, err := a.getClusterRoleBindingInTransaction(txnCtx)\n+\t_, err := a.getClusterRoleBinding(ctx)\n \treturn err\n }\n \n", "comment": "Is it free to open a transaction if we don't actually use it? The happy path for `getClusterRoleBinding` is that it hits the in-memory cache, so I just want to make sure this isn't adding overhead in that case.", "ids": [46743, "b4c2b4a872d5a8aea20fe487bbe2843e5437f795", "283fcb16e12532ec056d7a19ff88a7edd28ca910"], "repo": "pachyderm-pachyderm", "ghid": 5998, "old": " }\n func (a *apiServer) isActive(ctx context.Context) error {\n-\treturn a.txnEnv.WithReadContext(ctx, func(txnCtx *txnenv.TransactionContext) error {\n-\t\treturn a.isActiveInTransaction(txnCtx)\n-\t})\n-}\n-\n-func (a *apiServer) isActiveInTransaction(txnCtx *txnenv.TransactionContext) error {\n-\t_, err := a.getClusterRoleBindingInTransaction(txnCtx)\n \treturn err\n }", "new": " }\n func (a *apiServer) isActive(ctx context.Context) error {\n+\t_, err := a.getClusterRoleBinding(ctx)\n \treturn err\n }", "lang": "go", "y": 1, "proj": "pachyderm-pachyderm", "patch": "@@ -258,7 +264,13 @@ func (a *apiServer) getClusterRoleBinding(ctx context.Context) (*auth.RoleBindin\n }\n \n func (a *apiServer) isActive(ctx context.Context) error {\n-\t_, err := a.getClusterRoleBinding(ctx)\n+\treturn a.txnEnv.WithReadContext(ctx, func(txnCtx *txnenv.TransactionContext) error {\n+\t\treturn a.isActiveInTransaction(txnCtx)", "msg": "Is it free to open a transaction if we don't actually use it? The happy path for `getClusterRoleBinding` is that it hits the in-memory cache, so I just want to make sure this isn't adding overhead in that case."}
{"old_hunk": "@@ -2481,7 +2481,6 @@ func TestRestoreAuthToken(t *testing.T) {\n \trequire.True(t, auth.IsErrExpiredToken(err))\n \n \t// restore a token with an expiration date in the future\n-\treq.Token.HashedToken = fmt.Sprintf(\"%x\", sha256.Sum256([]byte(\"expiring-token\")))", "hunk": "@@ -2481,6 +2481,7 @@ func TestRestoreAuthToken(t *testing.T) {\n \trequire.True(t, auth.IsErrExpiredToken(err))\n \n \t// restore a token with an expiration date in the future\n+\treq.Token.HashedToken = fmt.Sprintf(\"%x\", sha256.Sum256([]byte(\"expiring-token\")))\n \tfutureExpiration := time.Now().Add(10 * time.Minute)\n \treq.Token.Expiration = &futureExpiration\n \n", "comment": "Why is this being removed?", "ids": [46745, "b4c2b4a872d5a8aea20fe487bbe2843e5437f795", "283fcb16e12532ec056d7a19ff88a7edd28ca910"], "repo": "pachyderm-pachyderm", "ghid": 5998, "old": " \trequire.True(t, auth.IsErrExpiredToken(err))\n \t// restore a token with an expiration date in the future\n \tfutureExpiration := time.Now().Add(10 * time.Minute)\n \treq.Token.Expiration = &futureExpiration", "new": " \trequire.True(t, auth.IsErrExpiredToken(err))\n \t// restore a token with an expiration date in the future\n+\treq.Token.HashedToken = fmt.Sprintf(\"%x\", sha256.Sum256([]byte(\"expiring-token\")))\n \tfutureExpiration := time.Now().Add(10 * time.Minute)\n \treq.Token.Expiration = &futureExpiration", "lang": "go", "y": 1, "proj": "pachyderm-pachyderm", "patch": "@@ -2481,7 +2481,6 @@ func TestRestoreAuthToken(t *testing.T) {\n \trequire.True(t, auth.IsErrExpiredToken(err))\n \n \t// restore a token with an expiration date in the future\n-\treq.Token.HashedToken = fmt.Sprintf(\"%x\", sha256.Sum256([]byte(\"expiring-token\")))", "msg": "Why is this being removed?"}
{"old_hunk": "@@ -46,6 +50,8 @@ const (\n \t// GitHookPort is 655\n \t// Prometheus uses 656\n \n+\t// OidcPort is the port where OIDC ID Providers can send auth assertions", "hunk": "@@ -47,11 +47,6 @@ const (\n \t// accessed so we cache them.\n \tclusterRoleBindingKey = \"CLUSTER:\"\n \n-\t// GitHookPort is 655\n-\t// Prometheus uses 656\n-\n-\t// OidcPort is the port where OIDC ID Providers can send auth assertions\n-\tOidcPort = 657\n \t// the length of interval between expired auth token cleanups\n \tcleanupIntervalHours = 24\n )\n", "comment": "nit: the auth assertions line is not 100% accurate, it's more like \"the port where the OIDC server listens for redirects\". \"auth assertions\" is probably copy-paste from SAML.", "ids": [46747, "b4c2b4a872d5a8aea20fe487bbe2843e5437f795", "283fcb16e12532ec056d7a19ff88a7edd28ca910"], "repo": "pachyderm-pachyderm", "ghid": 5998, "old": " \t// accessed so we cache them.\n \tclusterRoleBindingKey = \"CLUSTER:\"\n-\t// GitHookPort is 655\n-\t// Prometheus uses 656\n-\n-\t// OidcPort is the port where OIDC ID Providers can send auth assertions\n-\tOidcPort = 657\n \t// the length of interval between expired auth token cleanups\n \tcleanupIntervalHours = 24\n )", "new": " \t// accessed so we cache them.\n \tclusterRoleBindingKey = \"CLUSTER:\"\n \t// the length of interval between expired auth token cleanups\n \tcleanupIntervalHours = 24\n )", "lang": "go", "y": 1, "proj": "pachyderm-pachyderm", "patch": "@@ -46,6 +50,8 @@ const (\n \t// GitHookPort is 655\n \t// Prometheus uses 656\n \n+\t// OidcPort is the port where OIDC ID Providers can send auth assertions", "msg": "nit: the auth assertions line is not 100% accurate, it's more like \"the port where the OIDC server listens for redirects\". \"auth assertions\" is probably copy-paste from SAML."}
{"old_hunk": "@@ -444,18 +444,15 @@ func (ds *datumStream) Next() error {\n \t\t\treturn io.EOF\n \t\t}\n \t\tds.meta = meta\n+\t\tds.id = common.DatumID(meta.Inputs)\n \t\treturn nil\n \tcase err := <-ds.errChan:\n \t\treturn err\n \t}\n }\n \n-func (ds *datumStream) Key() string {\n-\treturn common.DatumID(ds.meta.Inputs)\n-}\n-\n-func (ds *datumStream) Priority() int {\n-\treturn ds.priority\n+func (ds *datumStream) Compare(s stream.Stream) int {\n+\treturn strings.Compare(ds.id, s.(*datumStream).id)", "hunk": "@@ -451,8 +451,10 @@ func (ds *datumStream) Next() error {\n \t}\n }\n \n-func (ds *datumStream) Compare(s stream.Stream) int {\n-\treturn strings.Compare(ds.id, s.(*datumStream).id)\n+func compare(s1, s2 stream.Stream) int {\n+\tds1 := s1.(*datumStream)\n+\tds2 := s2.(*datumStream)\n+\treturn strings.Compare(ds1.id, ds2.id)\n }\n \n // NewIterator creates a new datum iterator.\n", "comment": "This looks like it could panic if it is given something that's not a datumStream. It's usually better to have a comparison function (like the sort package). Because it will actually be in a position to know that it will never be comparing non datumStreams. There is no way of knowing that from the context of this method.", "ids": [46767, "82da4ee9005df6872c2598ba2ddc6912d913f171", "351d80d776fcab9434794c6ccd555371a1d76f25"], "repo": "pachyderm-pachyderm", "ghid": 6001, "old": " \t}\n }\n-func (ds *datumStream) Compare(s stream.Stream) int {\n-\treturn strings.Compare(ds.id, s.(*datumStream).id)\n }\n // NewIterator creates a new datum iterator.", "new": " \t}\n }\n+func compare(s1, s2 stream.Stream) int {\n+\tds1 := s1.(*datumStream)\n+\tds2 := s2.(*datumStream)\n+\treturn strings.Compare(ds1.id, ds2.id)\n }\n // NewIterator creates a new datum iterator.", "lang": "go", "y": 1, "proj": "pachyderm-pachyderm", "patch": "@@ -444,18 +444,15 @@ func (ds *datumStream) Next() error {\n \t\t\treturn io.EOF\n \t\t}\n \t\tds.meta = meta\n+\t\tds.id = common.DatumID(meta.Inputs)\n \t\treturn nil\n \tcase err := <-ds.errChan:\n \t\treturn err\n \t}\n }\n \n-func (ds *datumStream) Key() string {\n-\treturn common.DatumID(ds.meta.Inputs)\n-}\n-\n-func (ds *datumStream) Priority() int {\n-\treturn ds.priority\n+func (ds *datumStream) Compare(s stream.Stream) int {\n+\treturn strings.Compare(ds.id, s.(*datumStream).id)", "msg": "This looks like it could panic if it is given something that's not a datumStream. It's usually better to have a comparison function (like the sort package). Because it will actually be in a position to know that it will never be comparing non datumStreams. There is no way of knowing that from the context of this method."}
{"old_hunk": "@@ -765,28 +739,23 @@ func (d *driver) makeCommit(\n // If a complete commit provenance is passed in it just uses that.\n // It accepts an STM so that it can be used in a transaction and avoids an\n // inconsistent call to d.inspectCommit()\n-func (d *driver) resolveCommitProvenance(stm col.STM, userCommitProvenance *pfs.CommitProvenance) (*pfs.CommitProvenance, error) {\n+func (d *driver) resolveCommitProvenance(stm col.STM, userCommitProvenance *pfs.CommitProvenance) (*pfs.CommitProvenance, *pfs.CommitInfo, error) {", "hunk": "@@ -761,7 +761,6 @@ func (d *driver) resolveCommitProvenance(stm col.STM, userCommitProvenance *pfs.\n // TODO: Need to block operations on the commit before kicking off the compaction / finishing the commit.\n // We are going to want to move the compaction to the read side, and just mark the commit as finished here.\n func (d *driver) finishCommit(txnCtx *txnenv.TransactionContext, commit *pfs.Commit, description string) error {\n-\tctx := txnCtx.Client.Ctx()\n \tcommitInfo, err := d.resolveCommit(txnCtx.Stm, commit)\n \tif err != nil {\n \t\treturn err\n", "comment": "This was already the case but it strikes me as a little bit weird that this mutates one of its arguments and then returns it rather than just mutating it and having the user use it? Is there a good reason to do that?", "ids": [46883, "9b6233391c38c8829777a439b14b2e8657100cd8", "8d138efa26cc0696c1f68ba1de64d215189b7b48"], "repo": "pachyderm-pachyderm", "ghid": 6066, "old": " // TODO: Need to block operations on the commit before kicking off the compaction / finishing the commit.\n // We are going to want to move the compaction to the read side, and just mark the commit as finished here.\n func (d *driver) finishCommit(txnCtx *txnenv.TransactionContext, commit *pfs.Commit, description string) error {\n-\tctx := txnCtx.Client.Ctx()\n \tcommitInfo, err := d.resolveCommit(txnCtx.Stm, commit)\n \tif err != nil {\n \t\treturn err", "new": " // TODO: Need to block operations on the commit before kicking off the compaction / finishing the commit.\n // We are going to want to move the compaction to the read side, and just mark the commit as finished here.\n func (d *driver) finishCommit(txnCtx *txnenv.TransactionContext, commit *pfs.Commit, description string) error {\n \tcommitInfo, err := d.resolveCommit(txnCtx.Stm, commit)\n \tif err != nil {\n \t\treturn err", "lang": "go", "y": 1, "proj": "pachyderm-pachyderm", "patch": "@@ -765,28 +739,23 @@ func (d *driver) makeCommit(\n // If a complete commit provenance is passed in it just uses that.\n // It accepts an STM so that it can be used in a transaction and avoids an\n // inconsistent call to d.inspectCommit()\n-func (d *driver) resolveCommitProvenance(stm col.STM, userCommitProvenance *pfs.CommitProvenance) (*pfs.CommitProvenance, error) {\n+func (d *driver) resolveCommitProvenance(stm col.STM, userCommitProvenance *pfs.CommitProvenance) (*pfs.CommitProvenance, *pfs.CommitInfo, error) {", "msg": "This was already the case but it strikes me as a little bit weird that this mutates one of its arguments and then returns it rather than just mutating it and having the user use it? Is there a good reason to do that?"}
{"old_hunk": "@@ -64,7 +60,39 @@ func CreateSets(dit Iterator, storageRoot string, setSpec *SetSpec, upload func(\n \t}); err != nil {\n \t\treturn err\n \t}\n-\treturn createSet(metas, storageRoot, upload)\n+\tif len(metas) > 0 {\n+\t\treturn createSet(metas, storageRoot, upload)\n+\t}\n+\treturn nil\n+}\n+\n+func shouldCreateSetFunc(setSpec *SetSpec) func(*Meta) bool {\n+\tswitch {\n+\tcase setSpec.Number > 0:\n+\t\tvar num int64\n+\t\treturn func(meta *Meta) bool {\n+\t\t\tnum++\n+\t\t\tif num >= setSpec.Number {\n+\t\t\t\tnum = 0\n+\t\t\t\treturn true\n+\t\t\t}\n+\t\t\treturn false\n+\t\t}\n+\tcase setSpec.SizeBytes > 0:\n+\t\tvar size int64\n+\t\treturn func(meta *Meta) bool {\n+\t\t\tfor _, input := range meta.Inputs {\n+\t\t\t\tsize += int64(input.FileInfo.SizeBytes)\n+\t\t\t}\n+\t\t\tif size >= setSpec.SizeBytes {\n+\t\t\t\tsize = 0\n+\t\t\t\treturn true\n+\t\t\t}\n+\t\t\treturn false\n+\t\t}\n+\tdefault:\n+\t\tpanic(\"must set a set spec field\")", "hunk": "@@ -91,7 +91,9 @@ func shouldCreateSetFunc(setSpec *SetSpec) func(*Meta) bool {\n \t\t\treturn false\n \t\t}\n \tdefault:\n-\t\tpanic(\"must set a set spec field\")\n+\t\treturn func(meta *Meta) bool {\n+\t\t\treturn true\n+\t\t}\n \t}\n }\n \n", "comment": "Do we really want to panic here? It seems like that will just put the worker in a crashloop that won't give any feedback to the user about why the pipelines isn't proceeding.", "ids": [46893, "24e63bc1c6ea872ee5e743e092e1cd0f92c8f452", "f2db90f6926a1f8349fdb7fd1adcc42607437b8a"], "repo": "pachyderm-pachyderm", "ghid": 6068, "old": " \t\t\treturn false\n \t\t}\n \tdefault:\n-\t\tpanic(\"must set a set spec field\")\n \t}\n }", "new": " \t\t\treturn false\n \t\t}\n \tdefault:\n+\t\treturn func(meta *Meta) bool {\n+\t\t\treturn true\n+\t\t}\n \t}\n }", "lang": "go", "y": 1, "proj": "pachyderm-pachyderm", "patch": "@@ -64,7 +60,39 @@ func CreateSets(dit Iterator, storageRoot string, setSpec *SetSpec, upload func(\n \t}); err != nil {\n \t\treturn err\n \t}\n-\treturn createSet(metas, storageRoot, upload)\n+\tif len(metas) > 0 {\n+\t\treturn createSet(metas, storageRoot, upload)\n+\t}\n+\treturn nil\n+}\n+\n+func shouldCreateSetFunc(setSpec *SetSpec) func(*Meta) bool {\n+\tswitch {\n+\tcase setSpec.Number > 0:\n+\t\tvar num int64\n+\t\treturn func(meta *Meta) bool {\n+\t\t\tnum++\n+\t\t\tif num >= setSpec.Number {\n+\t\t\t\tnum = 0\n+\t\t\t\treturn true\n+\t\t\t}\n+\t\t\treturn false\n+\t\t}\n+\tcase setSpec.SizeBytes > 0:\n+\t\tvar size int64\n+\t\treturn func(meta *Meta) bool {\n+\t\t\tfor _, input := range meta.Inputs {\n+\t\t\t\tsize += int64(input.FileInfo.SizeBytes)\n+\t\t\t}\n+\t\t\tif size >= setSpec.SizeBytes {\n+\t\t\t\tsize = 0\n+\t\t\t\treturn true\n+\t\t\t}\n+\t\t\treturn false\n+\t\t}\n+\tdefault:\n+\t\tpanic(\"must set a set spec field\")", "msg": "Do we really want to panic here? It seems like that will just put the worker in a crashloop that won't give any feedback to the user about why the pipelines isn't proceeding."}
{"old_hunk": "@@ -301,6 +305,58 @@ func (a *apiServer) getEnterpriseTokenState(ctx context.Context) (enterpriseclie\n \treturn resp.State, nil\n }\n \n+// expiredEnterpriseCheck enforces that if the cluster's enterprise token is\n+// expired, users cannot log in. The root token can be used to access the cluster.\n+func (a *apiServer) expiredEnterpriseCheck(ctx context.Context) error {\n+\tstate, err := a.getEnterpriseTokenState(ctx)\n+\tif err != nil {\n+\t\treturn errors.Wrapf(err, \"error confirming Pachyderm Enterprise token\")\n+\t}\n+\n+\tif state == enterpriseclient.State_ACTIVE {\n+\t\treturn nil\n+\t}\n+\n+\t// this call internally checks for an Active License\n+\t// and Authorized Users in case of a deactivated license\n+\tif _, err = a.getAuthenticatedUser(ctx); err != nil {", "hunk": "@@ -307,7 +307,7 @@ func (a *apiServer) getEnterpriseTokenState(ctx context.Context) (enterpriseclie\n \n // expiredEnterpriseCheck enforces that if the cluster's enterprise token is\n // expired, users cannot log in. The root token can be used to access the cluster.\n-func (a *apiServer) expiredEnterpriseCheck(ctx context.Context) error {\n+func (a *apiServer) expiredEnterpriseCheck(ctx context.Context, username string) error {\n \tstate, err := a.getEnterpriseTokenState(ctx)\n \tif err != nil {\n \t\treturn errors.Wrapf(err, \"error confirming Pachyderm Enterprise token\")\n", "comment": "I think `expiredEnterpriseCheck` is only called in `Authenticate`, where the user doesn't have an auth token. So `getAuthenticatedUser` will always return an error, but we don't check the type of error here. The test in oidc_test only tests that users _can't_ log in when the license is expired, but I think we want to allow admins to log in? Right now I don't think anyone can log in if the token is expired, even cluster admins.", "ids": [46913, "ada4aa3968cfae6700ba3d592148cb300e46fd1a", "bad9f48ed5f0238f0771830f55ebd793df2dbed1"], "repo": "pachyderm-pachyderm", "ghid": 6074, "old": " // expiredEnterpriseCheck enforces that if the cluster's enterprise token is\n // expired, users cannot log in. The root token can be used to access the cluster.\n-func (a *apiServer) expiredEnterpriseCheck(ctx context.Context) error {\n \tstate, err := a.getEnterpriseTokenState(ctx)\n \tif err != nil {\n \t\treturn errors.Wrapf(err, \"error confirming Pachyderm Enterprise token\")", "new": " // expiredEnterpriseCheck enforces that if the cluster's enterprise token is\n // expired, users cannot log in. The root token can be used to access the cluster.\n+func (a *apiServer) expiredEnterpriseCheck(ctx context.Context, username string) error {\n \tstate, err := a.getEnterpriseTokenState(ctx)\n \tif err != nil {\n \t\treturn errors.Wrapf(err, \"error confirming Pachyderm Enterprise token\")", "lang": "go", "y": 1, "proj": "pachyderm-pachyderm", "patch": "@@ -301,6 +305,58 @@ func (a *apiServer) getEnterpriseTokenState(ctx context.Context) (enterpriseclie\n \treturn resp.State, nil\n }\n \n+// expiredEnterpriseCheck enforces that if the cluster's enterprise token is\n+// expired, users cannot log in. The root token can be used to access the cluster.\n+func (a *apiServer) expiredEnterpriseCheck(ctx context.Context) error {\n+\tstate, err := a.getEnterpriseTokenState(ctx)\n+\tif err != nil {\n+\t\treturn errors.Wrapf(err, \"error confirming Pachyderm Enterprise token\")\n+\t}\n+\n+\tif state == enterpriseclient.State_ACTIVE {\n+\t\treturn nil\n+\t}\n+\n+\t// this call internally checks for an Active License\n+\t// and Authorized Users in case of a deactivated license\n+\tif _, err = a.getAuthenticatedUser(ctx); err != nil {", "msg": "I think `expiredEnterpriseCheck` is only called in `Authenticate`, where the user doesn't have an auth token. So `getAuthenticatedUser` will always return an error, but we don't check the type of error here. The test in oidc_test only tests that users _can't_ log in when the license is expired, but I think we want to allow admins to log in? Right now I don't think anyone can log in if the token is expired, even cluster admins."}
{"old_hunk": "@@ -0,0 +1,98 @@\n+package serviceenv\n+\n+import (\n+\t\"crypto/tls\"\n+\t\"net\"\n+\t\"net/http\"\n+\t\"sync\"\n+\t\"time\"\n+\n+\tlog \"github.com/sirupsen/logrus\"\n+\tutilnet \"k8s.io/apimachinery/pkg/util/net\"\n+)\n+\n+const cancellationsBeforeReset = 4\n+\n+type reconnectTransport struct {\n+\tsync.Mutex\n+\ttlsConfig     *tls.Config\n+\tunderlying    *http.Transport\n+\tinFlight      map[cancelKey]interface{}", "hunk": "@@ -67,7 +67,7 @@ func (t *reconnectTransport) CancelRequest(req *http.Request) {\n \t\t// Close all keep-alive connections in the current transport.\n \t\t// TOOD: this doesn't cancel the outstanding requests, we should track them and cancel them all here too.\n \t\tt.underlying.CloseIdleConnections()\n-\t\tfor inflightReq, _ := range t.inFlight {\n+\t\tfor inflightReq := range t.inFlight {\n \t\t\tt.underlying.CancelRequest(inflightReq.req)\n \t\t}\n \t\tt.cancellations = 0\n", "comment": "Prefer `map[cancelKey]struct{}` for sets. (And then `t.inFlight[cancelKey{req}] = struct{}{}` to indicate presence in the set.)", "ids": [46918, "f1c6f2b0b2d373bfdf2c18d6bd3aff5f8dfd1fe2", "59efe691414cb6789979d7e014857429025451be"], "repo": "pachyderm-pachyderm", "ghid": 6078, "old": " \t\t// Close all keep-alive connections in the current transport.\n \t\t// TOOD: this doesn't cancel the outstanding requests, we should track them and cancel them all here too.\n \t\tt.underlying.CloseIdleConnections()\n-\t\tfor inflightReq, _ := range t.inFlight {\n \t\t\tt.underlying.CancelRequest(inflightReq.req)\n \t\t}\n \t\tt.cancellations = 0", "new": " \t\t// Close all keep-alive connections in the current transport.\n \t\t// TOOD: this doesn't cancel the outstanding requests, we should track them and cancel them all here too.\n \t\tt.underlying.CloseIdleConnections()\n+\t\tfor inflightReq := range t.inFlight {\n \t\t\tt.underlying.CancelRequest(inflightReq.req)\n \t\t}\n \t\tt.cancellations = 0", "lang": "go", "y": 1, "proj": "pachyderm-pachyderm", "patch": "@@ -0,0 +1,98 @@\n+package serviceenv\n+\n+import (\n+\t\"crypto/tls\"\n+\t\"net\"\n+\t\"net/http\"\n+\t\"sync\"\n+\t\"time\"\n+\n+\tlog \"github.com/sirupsen/logrus\"\n+\tutilnet \"k8s.io/apimachinery/pkg/util/net\"\n+)\n+\n+const cancellationsBeforeReset = 4\n+\n+type reconnectTransport struct {\n+\tsync.Mutex\n+\ttlsConfig     *tls.Config\n+\tunderlying    *http.Transport\n+\tinFlight      map[cancelKey]interface{}", "msg": "Prefer `map[cancelKey]struct{}` for sets. (And then `t.inFlight[cancelKey{req}] = struct{}{}` to indicate presence in the set.)"}
{"old_hunk": "@@ -189,9 +189,9 @@ func NewCronInputOpts(name string, repo string, spec string, overwrite bool) *pp\n \t}\n }\n \n-// NewJobInput creates a pps.JobInput.\n-func NewJobInput(repoName string, branchName string, commitID string, glob string) *pps.JobInput {\n-\treturn &pps.JobInput{\n+// NewJobInput creates a pps.PipelineJobInput.\n+func NewJobInput(repoName string, branchName string, commitID string, glob string) *pps.PipelineJobInput {", "hunk": "@@ -189,9 +189,9 @@ func NewCronInputOpts(name string, repo string, spec string, overwrite bool) *pp\n \t}\n }\n \n-// NewJobInput creates a pps.PipelineJobInput.\n-func NewJobInput(repoName string, branchName string, commitID string, glob string) *pps.PipelineJobInput {\n-\treturn &pps.PipelineJobInput{\n+// NewJobInput creates a pps.JobInput.\n+func NewJobInput(repoName string, branchName string, commitID string, glob string) *pps.JobInput {\n+\treturn &pps.JobInput{\n \t\tCommit: NewCommit(repoName, branchName, commitID),\n \t\tGlob:   glob,\n \t}\n", "comment": "should this function name change as well? I guess it's unambiguous since the other is in pfs", "ids": [47039, "fee6a763ee7feccb72af16949285a7329e25cd36", "e40f635acbc35e7b1c61c3cc4ebaad8b916a9ed6"], "repo": "pachyderm-pachyderm", "ghid": 6149, "old": " \t}\n }\n-// NewJobInput creates a pps.PipelineJobInput.\n-func NewJobInput(repoName string, branchName string, commitID string, glob string) *pps.PipelineJobInput {\n-\treturn &pps.PipelineJobInput{\n \t\tCommit: NewCommit(repoName, branchName, commitID),\n \t\tGlob:   glob,\n \t}", "new": " \t}\n }\n+// NewJobInput creates a pps.JobInput.\n+func NewJobInput(repoName string, branchName string, commitID string, glob string) *pps.JobInput {\n+\treturn &pps.JobInput{\n \t\tCommit: NewCommit(repoName, branchName, commitID),\n \t\tGlob:   glob,\n \t}", "lang": "go", "y": 1, "proj": "pachyderm-pachyderm", "patch": "@@ -189,9 +189,9 @@ func NewCronInputOpts(name string, repo string, spec string, overwrite bool) *pp\n \t}\n }\n \n-// NewJobInput creates a pps.JobInput.\n-func NewJobInput(repoName string, branchName string, commitID string, glob string) *pps.JobInput {\n-\treturn &pps.JobInput{\n+// NewJobInput creates a pps.PipelineJobInput.\n+func NewJobInput(repoName string, branchName string, commitID string, glob string) *pps.PipelineJobInput {", "msg": "should this function name change as well? I guess it's unambiguous since the other is in pfs"}
{"old_hunk": "@@ -416,25 +416,27 @@ func (d *driver) renewFileset(ctx context.Context, id fileset.ID, ttl time.Durat\n \treturn err\n }\n \n-func (d *driver) addFileset(ctx context.Context, commit *pfs.Commit, filesetID fileset.ID) error {\n-\tcommitInfo, err := d.inspectCommit(ctx, commit, pfs.CommitState_STARTED)\n+func (d *driver) addFileset(txnCtx *txnenv.TransactionContext, commit *pfs.Commit, filesetID fileset.ID) error {\n+\tcommitInfo, err := d.resolveCommit(txnCtx.SqlTx, commit)\n \tif err != nil {\n \t\treturn err\n \t}\n \tif commitInfo.Finished != nil {\n \t\treturn pfsserver.ErrCommitFinished{commitInfo.Commit}\n \t}\n-\treturn d.commitStore.AddFileset(ctx, commitInfo.Commit, filesetID)\n+\treturn d.commitStore.AddFilesetTx(txnCtx.SqlTx, commitInfo.Commit, filesetID)\n }\n \n func (d *driver) getFileset(ctx context.Context, commit *pfs.Commit) (*fileset.ID, error) {\n \tcommitInfo, err := d.getCommit(ctx, commit)\n \tif err != nil {\n \t\treturn nil, err\n \t}\n+\t/* TODO: brendon: re-enable compaction", "hunk": "@@ -432,11 +432,9 @@ func (d *driver) getFileset(ctx context.Context, commit *pfs.Commit) (*fileset.I\n \tif err != nil {\n \t\treturn nil, err\n \t}\n-\t/* TODO: brendon: re-enable compaction\n \tif commitInfo.Finished != nil {\n \t\treturn d.getOrComputeTotal(ctx, commitInfo.Commit)\n \t}\n-\t*/\n \tvar ids []fileset.ID\n \tif commitInfo.ParentCommit != nil {\n \t\t// ¯\\_(ツ)_/¯\n", "comment": "This needs to be removed. Compaction is enabled in master.", "ids": [47058, "0c541c16fefd079b35b49da9d50d1e70b13cf353", "e40f635acbc35e7b1c61c3cc4ebaad8b916a9ed6"], "repo": "pachyderm-pachyderm", "ghid": 6151, "old": " \tif err != nil {\n \t\treturn nil, err\n \t}\n-\t/* TODO: brendon: re-enable compaction\n \tif commitInfo.Finished != nil {\n \t\treturn d.getOrComputeTotal(ctx, commitInfo.Commit)\n \t}\n-\t*/\n \tvar ids []fileset.ID\n \tif commitInfo.ParentCommit != nil {\n \t\t// ¯\\_(ツ)_/¯", "new": " \tif err != nil {\n \t\treturn nil, err\n \t}\n \tif commitInfo.Finished != nil {\n \t\treturn d.getOrComputeTotal(ctx, commitInfo.Commit)\n \t}\n \tvar ids []fileset.ID\n \tif commitInfo.ParentCommit != nil {\n \t\t// ¯\\_(ツ)_/¯", "lang": "go", "y": 1, "proj": "pachyderm-pachyderm", "patch": "@@ -416,25 +416,27 @@ func (d *driver) renewFileset(ctx context.Context, id fileset.ID, ttl time.Durat\n \treturn err\n }\n \n-func (d *driver) addFileset(ctx context.Context, commit *pfs.Commit, filesetID fileset.ID) error {\n-\tcommitInfo, err := d.inspectCommit(ctx, commit, pfs.CommitState_STARTED)\n+func (d *driver) addFileset(txnCtx *txnenv.TransactionContext, commit *pfs.Commit, filesetID fileset.ID) error {\n+\tcommitInfo, err := d.resolveCommit(txnCtx.SqlTx, commit)\n \tif err != nil {\n \t\treturn err\n \t}\n \tif commitInfo.Finished != nil {\n \t\treturn pfsserver.ErrCommitFinished{commitInfo.Commit}\n \t}\n-\treturn d.commitStore.AddFileset(ctx, commitInfo.Commit, filesetID)\n+\treturn d.commitStore.AddFilesetTx(txnCtx.SqlTx, commitInfo.Commit, filesetID)\n }\n \n func (d *driver) getFileset(ctx context.Context, commit *pfs.Commit) (*fileset.ID, error) {\n \tcommitInfo, err := d.getCommit(ctx, commit)\n \tif err != nil {\n \t\treturn nil, err\n \t}\n+\t/* TODO: brendon: re-enable compaction", "msg": "This needs to be removed. Compaction is enabled in master."}
{"old_hunk": "@@ -45,11 +45,15 @@ func NewCommitProvenance(repoName string, branchName string, commitID string) *p\n }\n \n // NewFile creates a pfs.File.\n-func NewFile(repoName string, branchName string, commitID string, path string) *pfs.File {\n-\treturn &pfs.File{\n+func NewFile(repoName string, branchName string, commitID string, path string, tag ...string) *pfs.File {", "hunk": "@@ -45,15 +45,11 @@ func NewCommitProvenance(repoName string, branchName string, commitID string) *p\n }\n \n // NewFile creates a pfs.File.\n-func NewFile(repoName string, branchName string, commitID string, path string, tag ...string) *pfs.File {\n-\tf := &pfs.File{\n+func NewFile(repoName string, branchName string, commitID string, path string) *pfs.File {\n+\treturn &pfs.File{\n \t\tCommit: NewCommit(repoName, branchName, commitID),\n \t\tPath:   path,\n \t}\n-\tif len(tag) > 0 {\n-\t\tf.Tag = tag[0]\n-\t}\n-\treturn f\n }\n \n // CreateRepo creates a new Repo object in pfs with the given name. Repos are\n", "comment": "This snuck back in.", "ids": [47092, "fe2d6b7bc781ae18099144e79d23fd1403fc16b5", "88586dec6aa8c0052ad1f32294e02560de6a3d28"], "repo": "pachyderm-pachyderm", "ghid": 6169, "old": " }\n // NewFile creates a pfs.File.\n-func NewFile(repoName string, branchName string, commitID string, path string, tag ...string) *pfs.File {\n-\tf := &pfs.File{\n \t\tCommit: NewCommit(repoName, branchName, commitID),\n \t\tPath:   path,\n \t}\n-\tif len(tag) > 0 {\n-\t\tf.Tag = tag[0]\n-\t}\n-\treturn f\n }\n // CreateRepo creates a new Repo object in pfs with the given name. Repos are", "new": " }\n // NewFile creates a pfs.File.\n+func NewFile(repoName string, branchName string, commitID string, path string) *pfs.File {\n+\treturn &pfs.File{\n \t\tCommit: NewCommit(repoName, branchName, commitID),\n \t\tPath:   path,\n \t}\n }\n // CreateRepo creates a new Repo object in pfs with the given name. Repos are", "lang": "go", "y": 1, "proj": "pachyderm-pachyderm", "patch": "@@ -45,11 +45,15 @@ func NewCommitProvenance(repoName string, branchName string, commitID string) *p\n }\n \n // NewFile creates a pfs.File.\n-func NewFile(repoName string, branchName string, commitID string, path string) *pfs.File {\n-\treturn &pfs.File{\n+func NewFile(repoName string, branchName string, commitID string, path string, tag ...string) *pfs.File {", "msg": "This snuck back in."}
{"old_hunk": "@@ -4016,7 +4016,6 @@ func TestManyLogs(t *testing.T) {\n \n func TestLokiLogs(t *testing.T) {\n \t// TODO(2.0 required): This test is taking too long to complete due to slow pipelines", "hunk": "@@ -4015,7 +4015,6 @@ func TestManyLogs(t *testing.T) {\n }\n \n func TestLokiLogs(t *testing.T) {\n-\t// TODO(2.0 required): This test is taking too long to complete due to slow pipelines\n \tif testing.Short() {\n \t\tt.Skip(\"Skipping integration tests in short mode\")\n \t}\n", "comment": "We can probably remove this if it is no longer flaky and we suspect the issue was a delay in Loki.", "ids": [47194, "9eeb69cbf00d756896044cf3e3a157337ab7da21", "1bdea24ffcb39912d8e33db8ff3d4384025b2379"], "repo": "pachyderm-pachyderm", "ghid": 6211, "old": " }\n func TestLokiLogs(t *testing.T) {\n-\t// TODO(2.0 required): This test is taking too long to complete due to slow pipelines\n \tif testing.Short() {\n \t\tt.Skip(\"Skipping integration tests in short mode\")\n \t}", "new": " }\n func TestLokiLogs(t *testing.T) {\n \tif testing.Short() {\n \t\tt.Skip(\"Skipping integration tests in short mode\")\n \t}", "lang": "go", "y": 1, "proj": "pachyderm-pachyderm", "patch": "@@ -4016,7 +4016,6 @@ func TestManyLogs(t *testing.T) {\n \n func TestLokiLogs(t *testing.T) {\n \t// TODO(2.0 required): This test is taking too long to complete due to slow pipelines", "msg": "We can probably remove this if it is no longer flaky and we suspect the issue was a delay in Loki."}
{"old_hunk": "@@ -187,7 +187,8 @@ func SetupPostgresCollections(ctx context.Context, sqlTx *sqlx.Tx, collections .\n \t\t\tindexFields = append(indexFields, \"'\"+name+\"'\")\n \t\t}\n \n-\t\t// TODO: create indexes on table\n+\t\tfmt.Printf(\"creating collection: %s\\n%v\\n\", col.table, columns)", "hunk": "@@ -187,8 +187,6 @@ func SetupPostgresCollections(ctx context.Context, sqlTx *sqlx.Tx, collections .\n \t\t\tindexFields = append(indexFields, \"'\"+name+\"'\")\n \t\t}\n \n-\t\tfmt.Printf(\"creating collection: %s\\n%v\\n\", col.table, columns)\n-\n \t\tcreateTable := fmt.Sprintf(\"create table if not exists collections.%s (%s);\", col.table, strings.Join(columns, \", \"))\n \t\tif _, err := sqlTx.Exec(createTable); err != nil {\n \t\t\treturn errors.EnsureStack(err)\n", "comment": "I assume this was for debugging, but maybe not a bad thing to leave", "ids": [47212, "fca191dee8502d76f97a2a7f5b3a225a9502aa3f", "b101cbd4d0856ba52bda4d32008fe299c105a638"], "repo": "pachyderm-pachyderm", "ghid": 6216, "old": " \t\t\tindexFields = append(indexFields, \"'\"+name+\"'\")\n \t\t}\n-\t\tfmt.Printf(\"creating collection: %s\\n%v\\n\", col.table, columns)\n-\n \t\tcreateTable := fmt.Sprintf(\"create table if not exists collections.%s (%s);\", col.table, strings.Join(columns, \", \"))\n \t\tif _, err := sqlTx.Exec(createTable); err != nil {\n \t\t\treturn errors.EnsureStack(err)", "new": " \t\t\tindexFields = append(indexFields, \"'\"+name+\"'\")\n \t\t}\n \t\tcreateTable := fmt.Sprintf(\"create table if not exists collections.%s (%s);\", col.table, strings.Join(columns, \", \"))\n \t\tif _, err := sqlTx.Exec(createTable); err != nil {\n \t\t\treturn errors.EnsureStack(err)", "lang": "go", "y": 1, "proj": "pachyderm-pachyderm", "patch": "@@ -187,7 +187,8 @@ func SetupPostgresCollections(ctx context.Context, sqlTx *sqlx.Tx, collections .\n \t\t\tindexFields = append(indexFields, \"'\"+name+\"'\")\n \t\t}\n \n-\t\t// TODO: create indexes on table\n+\t\tfmt.Printf(\"creating collection: %s\\n%v\\n\", col.table, columns)", "msg": "I assume this was for debugging, but maybe not a bad thing to leave"}
{"old_hunk": "@@ -130,7 +132,7 @@ func (jdi *JobDatumIterator) Iterate(cb func(*datum.Meta) error) error {\n \t\t\tif outputFilesetID, err = jdi.withDatumFileset(pachClient, func(outputSet *datum.Set) error {\n \t\t\t\treturn datum.Merge([]datum.Iterator{parentFilesetIterator, filesetIterator}, func(metas []*datum.Meta) error {\n \t\t\t\t\tif len(metas) == 1 {\n-\t\t\t\t\t\tif metas[0].PipelineJobID != jdi.pipelineJobID {\n+\t\t\t\t\t\tif !proto.Equal(metas[0].PipelineJob, jdi.pipelineJob) {", "hunk": "@@ -132,7 +132,7 @@ func (jdi *JobDatumIterator) Iterate(cb func(*datum.Meta) error) error {\n \t\t\tif outputFilesetID, err = jdi.withDatumFileset(pachClient, func(outputSet *datum.Set) error {\n \t\t\t\treturn datum.Merge([]datum.Iterator{parentFilesetIterator, filesetIterator}, func(metas []*datum.Meta) error {\n \t\t\t\t\tif len(metas) == 1 {\n-\t\t\t\t\t\tif !proto.Equal(metas[0].PipelineJob, jdi.pipelineJob) {\n+\t\t\t\t\t\tif metas[0].PipelineJobID != jdi.pipelineJobID {\n \t\t\t\t\t\t\treturn nil\n \t\t\t\t\t\t}\n \t\t\t\t\t\treturn outputSet.UploadMeta(metas[0], datum.WithPrefixIndex())\n", "comment": "will this ever actually be considering datum metadata from another pipeline? happy with comparing the protos regardless", "ids": [47276, "b1e618797b312f35e683d3f4816c76677583e6e6", "29de2151522840a3926dd285c89a77eed4b51589"], "repo": "pachyderm-pachyderm", "ghid": 6229, "old": " \t\t\tif outputFilesetID, err = jdi.withDatumFileset(pachClient, func(outputSet *datum.Set) error {\n \t\t\t\treturn datum.Merge([]datum.Iterator{parentFilesetIterator, filesetIterator}, func(metas []*datum.Meta) error {\n \t\t\t\t\tif len(metas) == 1 {\n-\t\t\t\t\t\tif !proto.Equal(metas[0].PipelineJob, jdi.pipelineJob) {\n \t\t\t\t\t\t\treturn nil\n \t\t\t\t\t\t}\n \t\t\t\t\t\treturn outputSet.UploadMeta(metas[0], datum.WithPrefixIndex())", "new": " \t\t\tif outputFilesetID, err = jdi.withDatumFileset(pachClient, func(outputSet *datum.Set) error {\n \t\t\t\treturn datum.Merge([]datum.Iterator{parentFilesetIterator, filesetIterator}, func(metas []*datum.Meta) error {\n \t\t\t\t\tif len(metas) == 1 {\n+\t\t\t\t\t\tif metas[0].PipelineJobID != jdi.pipelineJobID {\n \t\t\t\t\t\t\treturn nil\n \t\t\t\t\t\t}\n \t\t\t\t\t\treturn outputSet.UploadMeta(metas[0], datum.WithPrefixIndex())", "lang": "go", "y": 1, "proj": "pachyderm-pachyderm", "patch": "@@ -130,7 +132,7 @@ func (jdi *JobDatumIterator) Iterate(cb func(*datum.Meta) error) error {\n \t\t\tif outputFilesetID, err = jdi.withDatumFileset(pachClient, func(outputSet *datum.Set) error {\n \t\t\t\treturn datum.Merge([]datum.Iterator{parentFilesetIterator, filesetIterator}, func(metas []*datum.Meta) error {\n \t\t\t\t\tif len(metas) == 1 {\n-\t\t\t\t\t\tif metas[0].PipelineJobID != jdi.pipelineJobID {\n+\t\t\t\t\t\tif !proto.Equal(metas[0].PipelineJob, jdi.pipelineJob) {", "msg": "will this ever actually be considering datum metadata from another pipeline? happy with comparing the protos regardless"}
{"old_hunk": "@@ -359,9 +359,10 @@ func (op *pipelineOp) rcIsFresh() bool {\n \t\tlog.Errorf(\"PPS master: auth token in %q is stale %s != %s\",\n \t\t\top.ptr.Pipeline.Name, rcAuthTokenHash, hashAuthToken(op.ptr.AuthToken))\n \t\treturn false\n-\tcase rcSpecCommit != op.ptr.SpecCommit.ID:\n+\t// TODO(global ids): this won't trigger properly on start/stop pipeline which no longer updates the pipeline's OriginalSpecCommit", "hunk": "@@ -359,10 +359,9 @@ func (op *pipelineOp) rcIsFresh() bool {\n \t\tlog.Errorf(\"PPS master: auth token in %q is stale %s != %s\",\n \t\t\top.ptr.Pipeline.Name, rcAuthTokenHash, hashAuthToken(op.ptr.AuthToken))\n \t\treturn false\n-\t// TODO(global ids): this won't trigger properly on start/stop pipeline which no longer updates the pipeline's OriginalSpecCommit\n-\tcase rcSpecCommit != op.ptr.OriginalSpecCommit.ID:\n+\tcase rcSpecCommit != op.ptr.SpecCommit.ID:\n \t\tlog.Errorf(\"PPS master: spec commit in %q looks stale %s != %s\",\n-\t\t\top.ptr.Pipeline.Name, rcSpecCommit, op.ptr.OriginalSpecCommit.ID)\n+\t\t\top.ptr.Pipeline.Name, rcSpecCommit, op.ptr.SpecCommit.ID)\n \t\treturn false\n \tcase rcPachVersion != version.PrettyVersion():\n \t\tlog.Errorf(\"PPS master: %q is using stale pachd v%s != current v%s\",\n", "comment": "Still reading through api_server.go, but does `CreatePipeline` update the pipeline version? It may be all that's needed to fix this is to change the tag in `worker_rc.go` from `specCommitAnnotation` to `pipelineVersionAnnotation` and then compare that here", "ids": [47279, "718207fc77c3d2fe9184691069e27a7a3ccdffa0", "30fb18e5cc3d160db5140ffd5f856cf20341876b"], "repo": "pachyderm-pachyderm", "ghid": 6230, "old": " \t\tlog.Errorf(\"PPS master: auth token in %q is stale %s != %s\",\n \t\t\top.ptr.Pipeline.Name, rcAuthTokenHash, hashAuthToken(op.ptr.AuthToken))\n \t\treturn false\n-\t// TODO(global ids): this won't trigger properly on start/stop pipeline which no longer updates the pipeline's OriginalSpecCommit\n-\tcase rcSpecCommit != op.ptr.OriginalSpecCommit.ID:\n \t\tlog.Errorf(\"PPS master: spec commit in %q looks stale %s != %s\",\n-\t\t\top.ptr.Pipeline.Name, rcSpecCommit, op.ptr.OriginalSpecCommit.ID)\n \t\treturn false\n \tcase rcPachVersion != version.PrettyVersion():\n \t\tlog.Errorf(\"PPS master: %q is using stale pachd v%s != current v%s\",", "new": " \t\tlog.Errorf(\"PPS master: auth token in %q is stale %s != %s\",\n \t\t\top.ptr.Pipeline.Name, rcAuthTokenHash, hashAuthToken(op.ptr.AuthToken))\n \t\treturn false\n+\tcase rcSpecCommit != op.ptr.SpecCommit.ID:\n \t\tlog.Errorf(\"PPS master: spec commit in %q looks stale %s != %s\",\n+\t\t\top.ptr.Pipeline.Name, rcSpecCommit, op.ptr.SpecCommit.ID)\n \t\treturn false\n \tcase rcPachVersion != version.PrettyVersion():\n \t\tlog.Errorf(\"PPS master: %q is using stale pachd v%s != current v%s\",", "lang": "go", "y": 1, "proj": "pachyderm-pachyderm", "patch": "@@ -359,9 +359,10 @@ func (op *pipelineOp) rcIsFresh() bool {\n \t\tlog.Errorf(\"PPS master: auth token in %q is stale %s != %s\",\n \t\t\top.ptr.Pipeline.Name, rcAuthTokenHash, hashAuthToken(op.ptr.AuthToken))\n \t\treturn false\n-\tcase rcSpecCommit != op.ptr.SpecCommit.ID:\n+\t// TODO(global ids): this won't trigger properly on start/stop pipeline which no longer updates the pipeline's OriginalSpecCommit", "msg": "Still reading through api_server.go, but does `CreatePipeline` update the pipeline version? It may be all that's needed to fix this is to change the tag in `worker_rc.go` from `specCommitAnnotation` to `pipelineVersionAnnotation` and then compare that here"}
{"old_hunk": "@@ -325,6 +329,53 @@ func (m *ppsMaster) monitorPipeline(ctx context.Context, pipelineInfo *pps.Pipel\n \t\t\t}, backoff.NewInfiniteBackOff(),\n \t\t\t\tbackoff.NotifyCtx(ctx, \"monitorPipeline for \"+pipeline))\n \t\t})\n+\t\tif pipelineInfo.ParallelismSpec != nil && pipelineInfo.ParallelismSpec.Constant > 1 && pipelineInfo.Autoscaling {\n+\t\t\teg.Go(func() error {\n+\t\t\t\tpachClient := m.a.env.GetPachClient(ctx)\n+\t\t\t\treturn backoff.RetryNotify(func() error {", "hunk": "@@ -343,7 +343,7 @@ func (m *ppsMaster) monitorPipeline(ctx context.Context, pipelineInfo *pps.Pipel\n \t\t\t\t\t\tif err != nil {\n \t\t\t\t\t\t\treturn err\n \t\t\t\t\t\t}\n-\t\t\t\t\t\tif nTasks < nClaims {\n+\t\t\t\t\t\tif nClaims < nTasks {\n \t\t\t\t\t\t\tkubeClient := m.a.env.GetKubeClient()\n \t\t\t\t\t\t\tnamespace := m.a.namespace\n \t\t\t\t\t\t\trc := kubeClient.CoreV1().ReplicationControllers(namespace)\n", "comment": "should this be `RetryUntilCancel` so it can respect the context?", "ids": [47329, "05d5f4aee5531f8eeb73b45e5dc2dca7ff9e8e71", "abc94db03e34b9b1c63fb54cef9464cd02497184"], "repo": "pachyderm-pachyderm", "ghid": 6241, "old": " \t\t\t\t\t\tif err != nil {\n \t\t\t\t\t\t\treturn err\n \t\t\t\t\t\t}\n-\t\t\t\t\t\tif nTasks < nClaims {\n \t\t\t\t\t\t\tkubeClient := m.a.env.GetKubeClient()\n \t\t\t\t\t\t\tnamespace := m.a.namespace\n \t\t\t\t\t\t\trc := kubeClient.CoreV1().ReplicationControllers(namespace)", "new": " \t\t\t\t\t\tif err != nil {\n \t\t\t\t\t\t\treturn err\n \t\t\t\t\t\t}\n+\t\t\t\t\t\tif nClaims < nTasks {\n \t\t\t\t\t\t\tkubeClient := m.a.env.GetKubeClient()\n \t\t\t\t\t\t\tnamespace := m.a.namespace\n \t\t\t\t\t\t\trc := kubeClient.CoreV1().ReplicationControllers(namespace)", "lang": "go", "y": 1, "proj": "pachyderm-pachyderm", "patch": "@@ -325,6 +329,53 @@ func (m *ppsMaster) monitorPipeline(ctx context.Context, pipelineInfo *pps.Pipel\n \t\t\t}, backoff.NewInfiniteBackOff(),\n \t\t\t\tbackoff.NotifyCtx(ctx, \"monitorPipeline for \"+pipeline))\n \t\t})\n+\t\tif pipelineInfo.ParallelismSpec != nil && pipelineInfo.ParallelismSpec.Constant > 1 && pipelineInfo.Autoscaling {\n+\t\t\teg.Go(func() error {\n+\t\t\t\tpachClient := m.a.env.GetPachClient(ctx)\n+\t\t\t\treturn backoff.RetryNotify(func() error {", "msg": "should this be `RetryUntilCancel` so it can respect the context?"}
{"old_hunk": "@@ -76,3 +77,49 @@ func (s *Storage) List(ctx context.Context, cb func(id ID) error) error {\n func (s *Storage) NewDeleter() track.Deleter {\n \treturn &deleter{}\n }\n+\n+// StableHash returns a stable hash for a set of data refs.\n+func (s *Storage) StableHash(ctx context.Context, dataRefs []*DataRef) ([]byte, error) {", "hunk": "@@ -117,6 +117,11 @@ func (s *Storage) StableHash(ctx context.Context, dataRefs []*DataRef) ([]byte,\n \t\t\treturn nil, err\n \t\t}\n \t}\n+\tif cw != nil {\n+\t\tif err := cw.Close(); err != nil {\n+\t\t\treturn nil, err\n+\t\t}\n+\t}\n \treturn h.Sum(nil), nil\n }\n \n", "comment": "I don't think this really belongs in the chunk package. Can we move this to the fileset package? It's a little weird that we have to punch through all the way to the chunk level to do this in the pfs server.", "ids": [47356, "26297de1de441cf6ef0b1f7c31ae262389ccc988", "26672edca7af437df9e7f30428148f641053ba1e"], "repo": "pachyderm-pachyderm", "ghid": 6253, "old": " \t\t\treturn nil, err\n \t\t}\n \t}\n \treturn h.Sum(nil), nil\n }", "new": " \t\t\treturn nil, err\n \t\t}\n \t}\n+\tif cw != nil {\n+\t\tif err := cw.Close(); err != nil {\n+\t\t\treturn nil, err\n+\t\t}\n+\t}\n \treturn h.Sum(nil), nil\n }", "lang": "go", "y": 1, "proj": "pachyderm-pachyderm", "patch": "@@ -76,3 +77,49 @@ func (s *Storage) List(ctx context.Context, cb func(id ID) error) error {\n func (s *Storage) NewDeleter() track.Deleter {\n \treturn &deleter{}\n }\n+\n+// StableHash returns a stable hash for a set of data refs.\n+func (s *Storage) StableHash(ctx context.Context, dataRefs []*DataRef) ([]byte, error) {", "msg": "I don't think this really belongs in the chunk package. Can we move this to the fileset package? It's a little weird that we have to punch through all the way to the chunk level to do this in the pfs server."}
{"old_hunk": "@@ -20,6 +20,7 @@ type Source interface {\n }\n \n type source struct {\n+\tstorage    *fileset.Storage", "hunk": "@@ -20,14 +20,14 @@ type Source interface {\n }\n \n type source struct {\n-\tstorage    *fileset.Storage\n+\tstorage    *chunk.Storage\n \tcommitInfo *pfs.CommitInfo\n \tfileSet    fileset.FileSet\n \tfull       bool\n }\n \n // NewSource creates a Source which emits FileInfos with the information from commit, and the entries return from fileSet.\n-func NewSource(storage *fileset.Storage, commitInfo *pfs.CommitInfo, fs fileset.FileSet, opts ...SourceOption) Source {\n+func NewSource(storage *chunk.Storage, commitInfo *pfs.CommitInfo, fs fileset.FileSet, opts ...SourceOption) Source {\n \tsc := &sourceConfig{}\n \tfor _, opt := range opts {\n \t\topt(sc)\n", "comment": "Can we not depend on the whole fileset.Storage? We seem to just grab the ChunkStorage() below. I would prefer to pass that in in the constructor.", "ids": [47358, "26297de1de441cf6ef0b1f7c31ae262389ccc988", "26672edca7af437df9e7f30428148f641053ba1e"], "repo": "pachyderm-pachyderm", "ghid": 6253, "old": " }\n type source struct {\n-\tstorage    *fileset.Storage\n \tcommitInfo *pfs.CommitInfo\n \tfileSet    fileset.FileSet\n \tfull       bool\n }\n // NewSource creates a Source which emits FileInfos with the information from commit, and the entries return from fileSet.\n-func NewSource(storage *fileset.Storage, commitInfo *pfs.CommitInfo, fs fileset.FileSet, opts ...SourceOption) Source {\n \tsc := &sourceConfig{}\n \tfor _, opt := range opts {\n \t\topt(sc)", "new": " }\n type source struct {\n+\tstorage    *chunk.Storage\n \tcommitInfo *pfs.CommitInfo\n \tfileSet    fileset.FileSet\n \tfull       bool\n }\n // NewSource creates a Source which emits FileInfos with the information from commit, and the entries return from fileSet.\n+func NewSource(storage *chunk.Storage, commitInfo *pfs.CommitInfo, fs fileset.FileSet, opts ...SourceOption) Source {\n \tsc := &sourceConfig{}\n \tfor _, opt := range opts {\n \t\topt(sc)", "lang": "go", "y": 1, "proj": "pachyderm-pachyderm", "patch": "@@ -20,6 +20,7 @@ type Source interface {\n }\n \n type source struct {\n+\tstorage    *fileset.Storage", "msg": "Can we not depend on the whole fileset.Storage? We seem to just grab the ChunkStorage() below. I would prefer to pass that in in the constructor."}
{"old_hunk": "@@ -119,49 +119,50 @@ var authHandlers = map[string]authHandler{\n \t//\n \n \t// TODO: Add methods to handle repo permissions\n-\t\"/pfs_v2.API/ActivateAuth\":    clusterPermissions(auth.Permission_CLUSTER_AUTH_ACTIVATE),\n-\t\"/pfs_v2.API/CreateRepo\":      authDisabledOr(authenticated),\n-\t\"/pfs_v2.API/InspectRepo\":     authDisabledOr(authenticated),\n-\t\"/pfs_v2.API/ListRepo\":        authDisabledOr(authenticated),\n-\t\"/pfs_v2.API/DeleteRepo\":      authDisabledOr(authenticated),\n-\t\"/pfs_v2.API/StartCommit\":     authDisabledOr(authenticated),\n-\t\"/pfs_v2.API/FinishCommit\":    authDisabledOr(authenticated),\n-\t\"/pfs_v2.API/InspectCommit\":   authDisabledOr(authenticated),\n-\t\"/pfs_v2.API/ListCommit\":      authDisabledOr(authenticated),\n-\t\"/pfs_v2.API/SquashCommit\":    authDisabledOr(authenticated),\n-\t\"/pfs_v2.API/FlushCommit\":     authDisabledOr(authenticated),\n-\t\"/pfs_v2.API/SubscribeCommit\": authDisabledOr(authenticated),\n-\t\"/pfs_v2.API/ClearCommit\":     authDisabledOr(authenticated),\n-\t\"/pfs_v2.API/CreateBranch\":    authDisabledOr(authenticated),\n-\t\"/pfs_v2.API/InspectBranch\":   authDisabledOr(authenticated),\n-\t\"/pfs_v2.API/ListBranch\":      authDisabledOr(authenticated),\n-\t\"/pfs_v2.API/DeleteBranch\":    authDisabledOr(authenticated),\n-\t\"/pfs_v2.API/ModifyFile\":      authDisabledOr(authenticated),\n-\t\"/pfs_v2.API/GetFileTAR\":      authDisabledOr(authenticated),\n-\t\"/pfs_v2.API/InspectFile\":     authDisabledOr(authenticated),\n-\t\"/pfs_v2.API/ListFile\":        authDisabledOr(authenticated),\n-\t\"/pfs_v2.API/WalkFile\":        authDisabledOr(authenticated),\n-\t\"/pfs_v2.API/GlobFile\":        authDisabledOr(authenticated),\n-\t\"/pfs_v2.API/DiffFile\":        authDisabledOr(authenticated),\n-\t\"/pfs_v2.API/DeleteAll\":       authDisabledOr(authenticated),\n-\t\"/pfs_v2.API/Fsck\":            authDisabledOr(authenticated),\n-\t\"/pfs_v2.API/CreateFileset\":   authDisabledOr(authenticated),\n-\t\"/pfs_v2.API/GetFileset\":      authDisabledOr(authenticated),\n-\t\"/pfs_v2.API/AddFileset\":      authDisabledOr(authenticated),\n-\t\"/pfs_v2.API/RenewFileset\":    authDisabledOr(authenticated),\n-\t\"/pfs_v2.API/RunLoadTest\":     authDisabledOr(authenticated),\n+\t\"/pfs_v2.API/ActivateAuth\":     clusterPermissions(auth.Permission_CLUSTER_AUTH_ACTIVATE),\n+\t\"/pfs_v2.API/CreateRepo\":       authDisabledOr(authenticated),\n+\t\"/pfs_v2.API/InspectRepo\":      authDisabledOr(authenticated),\n+\t\"/pfs_v2.API/ListRepo\":         authDisabledOr(authenticated),\n+\t\"/pfs_v2.API/DeleteRepo\":       authDisabledOr(authenticated),\n+\t\"/pfs_v2.API/StartCommit\":      authDisabledOr(authenticated),\n+\t\"/pfs_v2.API/FinishCommit\":     authDisabledOr(authenticated),\n+\t\"/pfs_v2.API/InspectCommit\":    authDisabledOr(authenticated),\n+\t\"/pfs_v2.API/ListCommit\":       authDisabledOr(authenticated),\n+\t\"/pfs_v2.API/SquashCommit\":     authDisabledOr(authenticated),", "hunk": "@@ -128,7 +128,6 @@ var authHandlers = map[string]authHandler{\n \t\"/pfs_v2.API/FinishCommit\":     authDisabledOr(authenticated),\n \t\"/pfs_v2.API/InspectCommit\":    authDisabledOr(authenticated),\n \t\"/pfs_v2.API/ListCommit\":       authDisabledOr(authenticated),\n-\t\"/pfs_v2.API/SquashCommit\":     authDisabledOr(authenticated),\n \t\"/pfs_v2.API/SubscribeCommit\":  authDisabledOr(authenticated),\n \t\"/pfs_v2.API/ClearCommit\":      authDisabledOr(authenticated),\n \t\"/pfs_v2.API/InspectCommitset\": authDisabledOr(authenticated),\n", "comment": "This got removed right?", "ids": [47421, "9823c22f14c802c42493f427dcd1f1e1eb78fe5c", "17c37381f2bd7b0004c88f54828becf8d235f396"], "repo": "pachyderm-pachyderm", "ghid": 6264, "old": " \t\"/pfs_v2.API/FinishCommit\":     authDisabledOr(authenticated),\n \t\"/pfs_v2.API/InspectCommit\":    authDisabledOr(authenticated),\n \t\"/pfs_v2.API/ListCommit\":       authDisabledOr(authenticated),\n-\t\"/pfs_v2.API/SquashCommit\":     authDisabledOr(authenticated),\n \t\"/pfs_v2.API/SubscribeCommit\":  authDisabledOr(authenticated),\n \t\"/pfs_v2.API/ClearCommit\":      authDisabledOr(authenticated),\n \t\"/pfs_v2.API/InspectCommitset\": authDisabledOr(authenticated),", "new": " \t\"/pfs_v2.API/FinishCommit\":     authDisabledOr(authenticated),\n \t\"/pfs_v2.API/InspectCommit\":    authDisabledOr(authenticated),\n \t\"/pfs_v2.API/ListCommit\":       authDisabledOr(authenticated),\n \t\"/pfs_v2.API/SubscribeCommit\":  authDisabledOr(authenticated),\n \t\"/pfs_v2.API/ClearCommit\":      authDisabledOr(authenticated),\n \t\"/pfs_v2.API/InspectCommitset\": authDisabledOr(authenticated),", "lang": "go", "y": 1, "proj": "pachyderm-pachyderm", "patch": "@@ -119,49 +119,50 @@ var authHandlers = map[string]authHandler{\n \t//\n \n \t// TODO: Add methods to handle repo permissions\n-\t\"/pfs_v2.API/ActivateAuth\":    clusterPermissions(auth.Permission_CLUSTER_AUTH_ACTIVATE),\n-\t\"/pfs_v2.API/CreateRepo\":      authDisabledOr(authenticated),\n-\t\"/pfs_v2.API/InspectRepo\":     authDisabledOr(authenticated),\n-\t\"/pfs_v2.API/ListRepo\":        authDisabledOr(authenticated),\n-\t\"/pfs_v2.API/DeleteRepo\":      authDisabledOr(authenticated),\n-\t\"/pfs_v2.API/StartCommit\":     authDisabledOr(authenticated),\n-\t\"/pfs_v2.API/FinishCommit\":    authDisabledOr(authenticated),\n-\t\"/pfs_v2.API/InspectCommit\":   authDisabledOr(authenticated),\n-\t\"/pfs_v2.API/ListCommit\":      authDisabledOr(authenticated),\n-\t\"/pfs_v2.API/SquashCommit\":    authDisabledOr(authenticated),\n-\t\"/pfs_v2.API/FlushCommit\":     authDisabledOr(authenticated),\n-\t\"/pfs_v2.API/SubscribeCommit\": authDisabledOr(authenticated),\n-\t\"/pfs_v2.API/ClearCommit\":     authDisabledOr(authenticated),\n-\t\"/pfs_v2.API/CreateBranch\":    authDisabledOr(authenticated),\n-\t\"/pfs_v2.API/InspectBranch\":   authDisabledOr(authenticated),\n-\t\"/pfs_v2.API/ListBranch\":      authDisabledOr(authenticated),\n-\t\"/pfs_v2.API/DeleteBranch\":    authDisabledOr(authenticated),\n-\t\"/pfs_v2.API/ModifyFile\":      authDisabledOr(authenticated),\n-\t\"/pfs_v2.API/GetFileTAR\":      authDisabledOr(authenticated),\n-\t\"/pfs_v2.API/InspectFile\":     authDisabledOr(authenticated),\n-\t\"/pfs_v2.API/ListFile\":        authDisabledOr(authenticated),\n-\t\"/pfs_v2.API/WalkFile\":        authDisabledOr(authenticated),\n-\t\"/pfs_v2.API/GlobFile\":        authDisabledOr(authenticated),\n-\t\"/pfs_v2.API/DiffFile\":        authDisabledOr(authenticated),\n-\t\"/pfs_v2.API/DeleteAll\":       authDisabledOr(authenticated),\n-\t\"/pfs_v2.API/Fsck\":            authDisabledOr(authenticated),\n-\t\"/pfs_v2.API/CreateFileset\":   authDisabledOr(authenticated),\n-\t\"/pfs_v2.API/GetFileset\":      authDisabledOr(authenticated),\n-\t\"/pfs_v2.API/AddFileset\":      authDisabledOr(authenticated),\n-\t\"/pfs_v2.API/RenewFileset\":    authDisabledOr(authenticated),\n-\t\"/pfs_v2.API/RunLoadTest\":     authDisabledOr(authenticated),\n+\t\"/pfs_v2.API/ActivateAuth\":     clusterPermissions(auth.Permission_CLUSTER_AUTH_ACTIVATE),\n+\t\"/pfs_v2.API/CreateRepo\":       authDisabledOr(authenticated),\n+\t\"/pfs_v2.API/InspectRepo\":      authDisabledOr(authenticated),\n+\t\"/pfs_v2.API/ListRepo\":         authDisabledOr(authenticated),\n+\t\"/pfs_v2.API/DeleteRepo\":       authDisabledOr(authenticated),\n+\t\"/pfs_v2.API/StartCommit\":      authDisabledOr(authenticated),\n+\t\"/pfs_v2.API/FinishCommit\":     authDisabledOr(authenticated),\n+\t\"/pfs_v2.API/InspectCommit\":    authDisabledOr(authenticated),\n+\t\"/pfs_v2.API/ListCommit\":       authDisabledOr(authenticated),\n+\t\"/pfs_v2.API/SquashCommit\":     authDisabledOr(authenticated),", "msg": "This got removed right?"}
{"old_hunk": "@@ -369,7 +369,7 @@ func (s *debugServer) collectInputRepos(tw *tar.Writer, pachClient *client.APICl\n \tfor _, repoInfo := range repoInfos {\n \t\tif _, err := pachClient.InspectPipeline(repoInfo.Repo.Name); err != nil {\n \t\t\t// TODO: It would be better for this to be a structured error.", "hunk": "@@ -369,7 +369,7 @@ func (s *debugServer) collectInputRepos(tw *tar.Writer, pachClient *client.APICl\n \tfor _, repoInfo := range repoInfos {\n \t\tif _, err := pachClient.InspectPipeline(repoInfo.Repo.Name); err != nil {\n \t\t\t// TODO: It would be better for this to be a structured error.\n-\t\t\tif errutil.IsNotFoundError(err) {\n+\t\t\tif strings.Contains(err.Error(), \"not found\") {\n \t\t\t\trepoPrefix := join(\"input-repos\", repoInfo.Repo.Name)\n \t\t\t\treturn collectDebugFile(tw, \"commits\", func(w io.Writer) error {\n \t\t\t\t\treturn pachClient.ListCommitF(repoInfo.Repo, nil, nil, uint64(limit), false, func(ci *pfs.CommitInfo) error {\n", "comment": "This TODO can be removed.", "ids": [47438, "9823c22f14c802c42493f427dcd1f1e1eb78fe5c", "ec8543de638d71665cf26cbdc209848b703115fe"], "repo": "pachyderm-pachyderm", "ghid": 6264, "old": " \tfor _, repoInfo := range repoInfos {\n \t\tif _, err := pachClient.InspectPipeline(repoInfo.Repo.Name); err != nil {\n \t\t\t// TODO: It would be better for this to be a structured error.\n-\t\t\tif errutil.IsNotFoundError(err) {\n \t\t\t\trepoPrefix := join(\"input-repos\", repoInfo.Repo.Name)\n \t\t\t\treturn collectDebugFile(tw, \"commits\", func(w io.Writer) error {\n \t\t\t\t\treturn pachClient.ListCommitF(repoInfo.Repo, nil, nil, uint64(limit), false, func(ci *pfs.CommitInfo) error {", "new": " \tfor _, repoInfo := range repoInfos {\n \t\tif _, err := pachClient.InspectPipeline(repoInfo.Repo.Name); err != nil {\n \t\t\t// TODO: It would be better for this to be a structured error.\n+\t\t\tif strings.Contains(err.Error(), \"not found\") {\n \t\t\t\trepoPrefix := join(\"input-repos\", repoInfo.Repo.Name)\n \t\t\t\treturn collectDebugFile(tw, \"commits\", func(w io.Writer) error {\n \t\t\t\t\treturn pachClient.ListCommitF(repoInfo.Repo, nil, nil, uint64(limit), false, func(ci *pfs.CommitInfo) error {", "lang": "go", "y": 1, "proj": "pachyderm-pachyderm", "patch": "@@ -369,7 +369,7 @@ func (s *debugServer) collectInputRepos(tw *tar.Writer, pachClient *client.APICl\n \tfor _, repoInfo := range repoInfos {\n \t\tif _, err := pachClient.InspectPipeline(repoInfo.Repo.Name); err != nil {\n \t\t\t// TODO: It would be better for this to be a structured error.", "msg": "This TODO can be removed."}
{"old_hunk": "@@ -202,28 +207,12 @@ func NewPipeline(pipelineName string) *pps.Pipeline {\n \treturn &pps.Pipeline{Name: pipelineName}\n }\n \n-// CreateJob creates and runs a job in PPS.\n-// This function is mostly useful internally, users should generally run work\n-// by creating pipelines as well.\n-func (c APIClient) CreateJob(pipeline string, outputCommit, statsCommit *pfs.Commit) (*pps.Job, error) {\n-\tjob, err := c.PpsAPIClient.CreateJob(\n-\t\tc.Ctx(),\n-\t\t&pps.CreateJobRequest{\n-\t\t\tPipeline:     NewPipeline(pipeline),\n-\t\t\tOutputCommit: outputCommit,\n-\t\t\tStatsCommit:  statsCommit,\n-\t\t},\n-\t)\n-\treturn job, grpcutil.ScrubGRPC(err)\n-}\n-\n // InspectJob returns info about a specific job.\n-// blockState will cause the call to block until the job reaches a terminal state (failure or success).\n // full indicates that the full job info should be returned.\n-func (c APIClient) InspectJob(jobID string, blockState bool, full ...bool) (*pps.JobInfo, error) {\n+func (c APIClient) InspectJob(pipelineName string, jobID string, full ...bool) (*pps.JobInfo, error) {\n \treq := &pps.InspectJobRequest{\n-\t\tJob:        NewJob(jobID),\n-\t\tBlockState: blockState,\n+\t\tJob:        NewJob(pipelineName, jobID),\n+\t\tBlockState: false,", "hunk": "@@ -208,11 +208,12 @@ func NewPipeline(pipelineName string) *pps.Pipeline {\n }\n \n // InspectJob returns info about a specific job.\n+// blockState will cause the call to block until the job reaches a terminal state (failure or success).\n // full indicates that the full job info should be returned.\n-func (c APIClient) InspectJob(pipelineName string, jobID string, full ...bool) (*pps.JobInfo, error) {\n+func (c APIClient) InspectJob(pipelineName string, jobID string, blockState bool, full ...bool) (*pps.JobInfo, error) {\n \treq := &pps.InspectJobRequest{\n \t\tJob:        NewJob(pipelineName, jobID),\n-\t\tBlockState: false,\n+\t\tBlockState: blockState,\n \t}\n \tif len(full) > 0 {\n \t\treq.Full = full[0]\n", "comment": "You could just use the default here. Also, I guess there was a decision to expose the block functionality as two separate methods rather than an option on one. What was the thinking behind this decision?", "ids": [47417, "9823c22f14c802c42493f427dcd1f1e1eb78fe5c", "ec8543de638d71665cf26cbdc209848b703115fe"], "repo": "pachyderm-pachyderm", "ghid": 6264, "old": " }\n // InspectJob returns info about a specific job.\n // full indicates that the full job info should be returned.\n-func (c APIClient) InspectJob(pipelineName string, jobID string, full ...bool) (*pps.JobInfo, error) {\n \treq := &pps.InspectJobRequest{\n \t\tJob:        NewJob(pipelineName, jobID),\n-\t\tBlockState: false,\n \t}\n \tif len(full) > 0 {\n \t\treq.Full = full[0]", "new": " }\n // InspectJob returns info about a specific job.\n+// blockState will cause the call to block until the job reaches a terminal state (failure or success).\n // full indicates that the full job info should be returned.\n+func (c APIClient) InspectJob(pipelineName string, jobID string, blockState bool, full ...bool) (*pps.JobInfo, error) {\n \treq := &pps.InspectJobRequest{\n \t\tJob:        NewJob(pipelineName, jobID),\n+\t\tBlockState: blockState,\n \t}\n \tif len(full) > 0 {\n \t\treq.Full = full[0]", "lang": "go", "y": 1, "proj": "pachyderm-pachyderm", "patch": "@@ -202,28 +207,12 @@ func NewPipeline(pipelineName string) *pps.Pipeline {\n \treturn &pps.Pipeline{Name: pipelineName}\n }\n \n-// CreateJob creates and runs a job in PPS.\n-// This function is mostly useful internally, users should generally run work\n-// by creating pipelines as well.\n-func (c APIClient) CreateJob(pipeline string, outputCommit, statsCommit *pfs.Commit) (*pps.Job, error) {\n-\tjob, err := c.PpsAPIClient.CreateJob(\n-\t\tc.Ctx(),\n-\t\t&pps.CreateJobRequest{\n-\t\t\tPipeline:     NewPipeline(pipeline),\n-\t\t\tOutputCommit: outputCommit,\n-\t\t\tStatsCommit:  statsCommit,\n-\t\t},\n-\t)\n-\treturn job, grpcutil.ScrubGRPC(err)\n-}\n-\n // InspectJob returns info about a specific job.\n-// blockState will cause the call to block until the job reaches a terminal state (failure or success).\n // full indicates that the full job info should be returned.\n-func (c APIClient) InspectJob(jobID string, blockState bool, full ...bool) (*pps.JobInfo, error) {\n+func (c APIClient) InspectJob(pipelineName string, jobID string, full ...bool) (*pps.JobInfo, error) {\n \treq := &pps.InspectJobRequest{\n-\t\tJob:        NewJob(jobID),\n-\t\tBlockState: blockState,\n+\t\tJob:        NewJob(pipelineName, jobID),\n+\t\tBlockState: false,", "msg": "You could just use the default here. Also, I guess there was a decision to expose the block functionality as two separate methods rather than an option on one. What was the thinking behind this decision?"}
{"old_hunk": "@@ -66,3 +66,25 @@ func (err ErrTransactionConflict) Error() string {\n func IsErrTransactionConflict(err error) bool {\n \treturn errors.Is(err, ErrTransactionConflict{})\n }\n+\n+// ErrNotUnique indicates that an indexed query expected to have exactly one", "hunk": "@@ -68,7 +68,7 @@ func IsErrTransactionConflict(err error) bool {\n }\n \n // ErrNotUnique indicates that an indexed query expected to have exactly one\n-// result had more than one result.\n+// result but had more than one result.\n type ErrNotUnique struct {\n \tType  string\n \tIndex string\n", "comment": "This reads weird.", "ids": [47423, "9823c22f14c802c42493f427dcd1f1e1eb78fe5c", "73e9b4b39cdaf6d64b0e6c8d61e3c89f5ae5daf9"], "repo": "pachyderm-pachyderm", "ghid": 6264, "old": " }\n // ErrNotUnique indicates that an indexed query expected to have exactly one\n-// result had more than one result.\n type ErrNotUnique struct {\n \tType  string\n \tIndex string", "new": " }\n // ErrNotUnique indicates that an indexed query expected to have exactly one\n+// result but had more than one result.\n type ErrNotUnique struct {\n \tType  string\n \tIndex string", "lang": "go", "y": 1, "proj": "pachyderm-pachyderm", "patch": "@@ -66,3 +66,25 @@ func (err ErrTransactionConflict) Error() string {\n func IsErrTransactionConflict(err error) bool {\n \treturn errors.Is(err, ErrTransactionConflict{})\n }\n+\n+// ErrNotUnique indicates that an indexed query expected to have exactly one", "msg": "This reads weird."}
{"old_hunk": "@@ -320,7 +320,8 @@ func masterListObjectsPaginated(t *testing.T, pachClient *client.APIClient, mini\n \t// `LastModified` date is correct. A few minutes are subtracted/added to\n \t// each to tolerate the node time not being the same as the host time.\n \tstartTime := time.Now().Add(time.Duration(-5) * time.Minute)\n-\trepo := tu.UniqueString(\"testlistobjectspaginated\")\n+\t// bucket name cannot exceed 63 characters long\n+\trepo := tu.UniqueString(\"testLOP\")", "hunk": "@@ -320,7 +320,8 @@ func masterListObjectsPaginated(t *testing.T, pachClient *client.APIClient, mini\n \t// `LastModified` date is correct. A few minutes are subtracted/added to\n \t// each to tolerate the node time not being the same as the host time.\n \tstartTime := time.Now().Add(time.Duration(-5) * time.Minute)\n-\t// bucket name cannot exceed 63 characters long\n+\t// S3 client limits bucket name length to 63 chars, but we also want to query with commit\n+\t// so we need to be conservative with the length of the repo name here\n \trepo := tu.UniqueString(\"testLOP\")\n \trequire.NoError(t, pachClient.CreateRepo(repo))\n \tcommit, err := pachClient.StartCommit(repo, \"master\")\n", "comment": "This change was necessary because s3 puts a hard limit of 63 chars on the length of a bucket and the repo name was too long.", "ids": [47506, "eeda81fc7377b7f7d558d29f273c3f5d74a19f2b", "7104572fb282bddf143e9b82eff7a0a72c42cd05"], "repo": "pachyderm-pachyderm", "ghid": 6271, "old": " \t// `LastModified` date is correct. A few minutes are subtracted/added to\n \t// each to tolerate the node time not being the same as the host time.\n \tstartTime := time.Now().Add(time.Duration(-5) * time.Minute)\n-\t// bucket name cannot exceed 63 characters long\n \trepo := tu.UniqueString(\"testLOP\")\n \trequire.NoError(t, pachClient.CreateRepo(repo))\n \tcommit, err := pachClient.StartCommit(repo, \"master\")", "new": " \t// `LastModified` date is correct. A few minutes are subtracted/added to\n \t// each to tolerate the node time not being the same as the host time.\n \tstartTime := time.Now().Add(time.Duration(-5) * time.Minute)\n+\t// S3 client limits bucket name length to 63 chars, but we also want to query with commit\n+\t// so we need to be conservative with the length of the repo name here\n \trepo := tu.UniqueString(\"testLOP\")\n \trequire.NoError(t, pachClient.CreateRepo(repo))\n \tcommit, err := pachClient.StartCommit(repo, \"master\")", "lang": "go", "y": 1, "proj": "pachyderm-pachyderm", "patch": "@@ -320,7 +320,8 @@ func masterListObjectsPaginated(t *testing.T, pachClient *client.APIClient, mini\n \t// `LastModified` date is correct. A few minutes are subtracted/added to\n \t// each to tolerate the node time not being the same as the host time.\n \tstartTime := time.Now().Add(time.Duration(-5) * time.Minute)\n-\trepo := tu.UniqueString(\"testlistobjectspaginated\")\n+\t// bucket name cannot exceed 63 characters long\n+\trepo := tu.UniqueString(\"testLOP\")", "msg": "This change was necessary because s3 puts a hard limit of 63 chars on the length of a bucket and the repo name was too long."}
{"old_hunk": "@@ -87,71 +88,35 @@ func (c *postgresCollection) ReadWrite(tx *sqlx.Tx) PostgresReadWriteCollection\n \treturn &postgresReadWriteCollection{c, tx}\n }\n \n+type ErrTransactionConflict = dbutil.ErrTransactionConflict\n+\n // NewSQLTx starts a transaction on the given DB, passes it to the callback, and\n // finishes the transaction afterwards. If the callback was successful, the\n // transaction is committed. If any errors occur, the transaction is rolled\n // back.  This will reattempt the transaction forever.\n func NewSQLTx(ctx context.Context, db *sqlx.DB, apply func(*sqlx.Tx) error) error {\n-\tattemptTx := func() error {\n-\t\ttx, err := db.BeginTxx(ctx, &sql.TxOptions{Isolation: sql.LevelSerializable})\n-\t\tif err != nil {\n-\t\t\treturn errors.EnsureStack(err)\n-\t\t}\n-\n-\t\terr = apply(tx)\n-\t\tif err != nil {\n-\t\t\ttx.Rollback()\n-\t\t\treturn err\n-\t\t}\n-\n-\t\treturn errors.EnsureStack(tx.Commit())\n-\t}\n-\n-\tfor {\n-\t\tif err := attemptTx(); err != nil {\n-\t\t\tif !isTransactionError(err) {\n-\t\t\t\treturn err\n-\t\t\t}\n-\t\t} else {\n-\t\t\treturn nil\n-\t\t}\n-\t}\n+\treturn dbutil.WithTx(ctx, db, apply)\n }\n \n // NewDryrunSQLTx is identical to NewSQLTx except it will always roll back the\n // transaction instead of committing it.\n func NewDryrunSQLTx(ctx context.Context, db *sqlx.DB, apply func(*sqlx.Tx) error) error {\n-\tattemptTx := func() error {\n-\t\ttx, err := db.BeginTxx(ctx, nil)\n-\t\tif err != nil {\n-\t\t\treturn errors.EnsureStack(err)\n-\t\t}\n-\t\tdefer tx.Rollback()\n-\t\treturn apply(tx)\n-\t}\n-\tfor {\n-\t\tif err := attemptTx(); err != nil {\n-\t\t\tif !isTransactionError(err) {\n-\t\t\t\treturn err\n-\t\t\t}\n-\t\t} else {\n-\t\t\treturn nil\n+\terr := NewSQLTx(ctx, db, func(tx *sqlx.Tx) error {\n+\t\tif err := apply(tx); err != nil {\n+\t\t\treturn err\n \t\t}\n+\t\treturn tx.Rollback()\n+\t})\n+\tif strings.Contains(err.Error(), \"transaction has already been committed or rolled back\") {", "hunk": "@@ -107,7 +107,7 @@ func NewDryrunSQLTx(ctx context.Context, db *sqlx.DB, apply func(*sqlx.Tx) error\n \t\t}\n \t\treturn tx.Rollback()\n \t})\n-\tif strings.Contains(err.Error(), \"transaction has already been committed or rolled back\") {\n+\tif err == sql.ErrTxDone {\n \t\terr = nil\n \t}\n \treturn err\n", "comment": "This is just `sql.ErrTxDone`, right? I glanced through the errors `tx.Rollback()` can return, since we just discarded them before, and it does seem like the others suggest such bad connection state that we might as well give up", "ids": [47530, "d3de8c2b8c08a99ddaf8e8dc8299e35140aaf656", "fca010c32309f9e3b1862d3b05764fb706924fbb"], "repo": "pachyderm-pachyderm", "ghid": 6304, "old": " \t\t}\n \t\treturn tx.Rollback()\n \t})\n-\tif strings.Contains(err.Error(), \"transaction has already been committed or rolled back\") {\n \t\terr = nil\n \t}\n \treturn err", "new": " \t\t}\n \t\treturn tx.Rollback()\n \t})\n+\tif err == sql.ErrTxDone {\n \t\terr = nil\n \t}\n \treturn err", "lang": "go", "y": 1, "proj": "pachyderm-pachyderm", "patch": "@@ -87,71 +88,35 @@ func (c *postgresCollection) ReadWrite(tx *sqlx.Tx) PostgresReadWriteCollection\n \treturn &postgresReadWriteCollection{c, tx}\n }\n \n+type ErrTransactionConflict = dbutil.ErrTransactionConflict\n+\n // NewSQLTx starts a transaction on the given DB, passes it to the callback, and\n // finishes the transaction afterwards. If the callback was successful, the\n // transaction is committed. If any errors occur, the transaction is rolled\n // back.  This will reattempt the transaction forever.\n func NewSQLTx(ctx context.Context, db *sqlx.DB, apply func(*sqlx.Tx) error) error {\n-\tattemptTx := func() error {\n-\t\ttx, err := db.BeginTxx(ctx, &sql.TxOptions{Isolation: sql.LevelSerializable})\n-\t\tif err != nil {\n-\t\t\treturn errors.EnsureStack(err)\n-\t\t}\n-\n-\t\terr = apply(tx)\n-\t\tif err != nil {\n-\t\t\ttx.Rollback()\n-\t\t\treturn err\n-\t\t}\n-\n-\t\treturn errors.EnsureStack(tx.Commit())\n-\t}\n-\n-\tfor {\n-\t\tif err := attemptTx(); err != nil {\n-\t\t\tif !isTransactionError(err) {\n-\t\t\t\treturn err\n-\t\t\t}\n-\t\t} else {\n-\t\t\treturn nil\n-\t\t}\n-\t}\n+\treturn dbutil.WithTx(ctx, db, apply)\n }\n \n // NewDryrunSQLTx is identical to NewSQLTx except it will always roll back the\n // transaction instead of committing it.\n func NewDryrunSQLTx(ctx context.Context, db *sqlx.DB, apply func(*sqlx.Tx) error) error {\n-\tattemptTx := func() error {\n-\t\ttx, err := db.BeginTxx(ctx, nil)\n-\t\tif err != nil {\n-\t\t\treturn errors.EnsureStack(err)\n-\t\t}\n-\t\tdefer tx.Rollback()\n-\t\treturn apply(tx)\n-\t}\n-\tfor {\n-\t\tif err := attemptTx(); err != nil {\n-\t\t\tif !isTransactionError(err) {\n-\t\t\t\treturn err\n-\t\t\t}\n-\t\t} else {\n-\t\t\treturn nil\n+\terr := NewSQLTx(ctx, db, func(tx *sqlx.Tx) error {\n+\t\tif err := apply(tx); err != nil {\n+\t\t\treturn err\n \t\t}\n+\t\treturn tx.Rollback()\n+\t})\n+\tif strings.Contains(err.Error(), \"transaction has already been committed or rolled back\") {", "msg": "This is just `sql.ErrTxDone`, right? I glanced through the errors `tx.Rollback()` can return, since we just discarded them before, and it does seem like the others suggest such bad connection state that we might as well give up"}
{"old_hunk": "@@ -147,6 +147,13 @@ func (a *validatedAPIServer) SquashCommitSet(ctx context.Context, request *pfs.S\n \treturn a.apiServer.SquashCommitSet(ctx, request)\n }\n \n+func (a *validatedAPIServer) GetFile(request *pfs.GetFileRequest, server pfs.API_GetFileServer) error {", "hunk": "@@ -151,6 +151,15 @@ func (a *validatedAPIServer) GetFile(request *pfs.GetFileRequest, server pfs.API\n \tif request.File == nil {\n \t\treturn errors.New(\"file cannot be nil\")\n \t}\n+\tif request.File.Commit == nil {\n+\t\treturn errors.New(\"commit cannot be nil\")\n+\t}\n+\tif request.File.Commit.Branch == nil {\n+\t\treturn errors.New(\"branch cannot be nil\")\n+\t}\n+\tif request.File.Commit.Branch.Repo == nil {\n+\t\treturn errors.New(\"repo cannot be nil\")\n+\t}\n \treturn a.apiServer.GetFile(request, server)\n }\n \n", "comment": "This should probably also check that `File.Commit`, `File.Commit.Branch`, and `File.Commit.Branch.Repo` are not `nil`.", "ids": [47653, "de53df8985094ba7196c35a4f20df6599213fe1b", "489f9ce0b6c3f29b7d8d5014884b8a4edb7594a2"], "repo": "pachyderm-pachyderm", "ghid": 6394, "old": " \tif request.File == nil {\n \t\treturn errors.New(\"file cannot be nil\")\n \t}\n \treturn a.apiServer.GetFile(request, server)\n }", "new": " \tif request.File == nil {\n \t\treturn errors.New(\"file cannot be nil\")\n \t}\n+\tif request.File.Commit == nil {\n+\t\treturn errors.New(\"commit cannot be nil\")\n+\t}\n+\tif request.File.Commit.Branch == nil {\n+\t\treturn errors.New(\"branch cannot be nil\")\n+\t}\n+\tif request.File.Commit.Branch.Repo == nil {\n+\t\treturn errors.New(\"repo cannot be nil\")\n+\t}\n \treturn a.apiServer.GetFile(request, server)\n }", "lang": "go", "y": 1, "proj": "pachyderm-pachyderm", "patch": "@@ -147,6 +147,13 @@ func (a *validatedAPIServer) SquashCommitSet(ctx context.Context, request *pfs.S\n \treturn a.apiServer.SquashCommitSet(ctx, request)\n }\n \n+func (a *validatedAPIServer) GetFile(request *pfs.GetFileRequest, server pfs.API_GetFileServer) error {", "msg": "This should probably also check that `File.Commit`, `File.Commit.Branch`, and `File.Commit.Branch.Repo` are not `nil`."}
{"old_hunk": "@@ -187,3 +187,26 @@ type emptySource struct{}\n func (emptySource) Iterate(ctx context.Context, cb func(*pfs.FileInfo, fileset.File) error) error {\n \treturn nil\n }\n+\n+// singleFile iterates through the source and returns exactly one file or an error.\n+// If the source contains a directory, then singleFile errors.\n+// If the source contains more than one file of any type, then singleFile errors\n+func singleFile(ctx context.Context, src Source) (*pfs.FileInfo, fileset.File, error) {\n+\tvar retFinfo *pfs.FileInfo\n+\tvar retFile fileset.File\n+\terr := src.Iterate(ctx, func(finfo *pfs.FileInfo, fsFile fileset.File) error {\n+\t\tif retFinfo != nil {\n+\t\t\treturn errors.Errorf(\"matched multiple files\")", "hunk": "@@ -196,7 +196,7 @@ func singleFile(ctx context.Context, src Source) (*pfs.FileInfo, fileset.File, e\n \tvar retFile fileset.File\n \terr := src.Iterate(ctx, func(finfo *pfs.FileInfo, fsFile fileset.File) error {\n \t\tif retFinfo != nil {\n-\t\t\treturn errors.Errorf(\"matched multiple files\")\n+\t\t\treturn errors.Errorf(\"matched multiple files. Try GetFileTAR\")\n \t\t}\n \t\tif finfo.FileType != pfs.FileType_FILE {\n \t\t\treturn errors.Errorf(\"cannot get non-regular file. Try GetFileTAR for directories\")\n", "comment": "should this also recommend using `GetFileTAR`?", "ids": [47656, "de53df8985094ba7196c35a4f20df6599213fe1b", "489f9ce0b6c3f29b7d8d5014884b8a4edb7594a2"], "repo": "pachyderm-pachyderm", "ghid": 6394, "old": " \tvar retFile fileset.File\n \terr := src.Iterate(ctx, func(finfo *pfs.FileInfo, fsFile fileset.File) error {\n \t\tif retFinfo != nil {\n-\t\t\treturn errors.Errorf(\"matched multiple files\")\n \t\t}\n \t\tif finfo.FileType != pfs.FileType_FILE {\n \t\t\treturn errors.Errorf(\"cannot get non-regular file. Try GetFileTAR for directories\")", "new": " \tvar retFile fileset.File\n \terr := src.Iterate(ctx, func(finfo *pfs.FileInfo, fsFile fileset.File) error {\n \t\tif retFinfo != nil {\n+\t\t\treturn errors.Errorf(\"matched multiple files. Try GetFileTAR\")\n \t\t}\n \t\tif finfo.FileType != pfs.FileType_FILE {\n \t\t\treturn errors.Errorf(\"cannot get non-regular file. Try GetFileTAR for directories\")", "lang": "go", "y": 1, "proj": "pachyderm-pachyderm", "patch": "@@ -187,3 +187,26 @@ type emptySource struct{}\n func (emptySource) Iterate(ctx context.Context, cb func(*pfs.FileInfo, fileset.File) error) error {\n \treturn nil\n }\n+\n+// singleFile iterates through the source and returns exactly one file or an error.\n+// If the source contains a directory, then singleFile errors.\n+// If the source contains more than one file of any type, then singleFile errors\n+func singleFile(ctx context.Context, src Source) (*pfs.FileInfo, fileset.File, error) {\n+\tvar retFinfo *pfs.FileInfo\n+\tvar retFile fileset.File\n+\terr := src.Iterate(ctx, func(finfo *pfs.FileInfo, fsFile fileset.File) error {\n+\t\tif retFinfo != nil {\n+\t\t\treturn errors.Errorf(\"matched multiple files\")", "msg": "should this also recommend using `GetFileTAR`?"}
{"old_hunk": "@@ -416,34 +415,48 @@ func (c APIClient) RenewFileSet(ID string, ttl time.Duration) (retErr error) {\n // size limits the total amount of data returned, note you will get fewer bytes\n // than size if you pass a value larger than the size of the file.\n // If size is set to 0 then all of the data will be returned.\n-// TODO: Should we error if multiple files are matched?\n-func (c APIClient) GetFile(commit *pfs.Commit, path string, w io.Writer) error {\n-\tr, err := c.getFileTar(commit, path)\n+func (c APIClient) GetFile(commit *pfs.Commit, path string, w io.Writer) (retErr error) {\n+\tctx, cf := context.WithCancel(c.Ctx())", "hunk": "@@ -416,6 +416,9 @@ func (c APIClient) RenewFileSet(ID string, ttl time.Duration) (retErr error) {\n // than size if you pass a value larger than the size of the file.\n // If size is set to 0 then all of the data will be returned.\n func (c APIClient) GetFile(commit *pfs.Commit, path string, w io.Writer) (retErr error) {\n+\tdefer func() {\n+\t\tretErr = grpcutil.ScrubGRPC(retErr)\n+\t}()\n \tctx, cf := context.WithCancel(c.Ctx())\n \tdefer cf()\n \tgfc, err := c.PfsAPIClient.GetFile(ctx, &pfs.GetFileRequest{\n", "comment": "I think this should defer a `grpcutil.ScrupGRPC`", "ids": [47652, "de53df8985094ba7196c35a4f20df6599213fe1b", "489f9ce0b6c3f29b7d8d5014884b8a4edb7594a2"], "repo": "pachyderm-pachyderm", "ghid": 6394, "old": " // than size if you pass a value larger than the size of the file.\n // If size is set to 0 then all of the data will be returned.\n func (c APIClient) GetFile(commit *pfs.Commit, path string, w io.Writer) (retErr error) {\n \tctx, cf := context.WithCancel(c.Ctx())\n \tdefer cf()\n \tgfc, err := c.PfsAPIClient.GetFile(ctx, &pfs.GetFileRequest{", "new": " // than size if you pass a value larger than the size of the file.\n // If size is set to 0 then all of the data will be returned.\n func (c APIClient) GetFile(commit *pfs.Commit, path string, w io.Writer) (retErr error) {\n+\tdefer func() {\n+\t\tretErr = grpcutil.ScrubGRPC(retErr)\n+\t}()\n \tctx, cf := context.WithCancel(c.Ctx())\n \tdefer cf()\n \tgfc, err := c.PfsAPIClient.GetFile(ctx, &pfs.GetFileRequest{", "lang": "go", "y": 1, "proj": "pachyderm-pachyderm", "patch": "@@ -416,34 +415,48 @@ func (c APIClient) RenewFileSet(ID string, ttl time.Duration) (retErr error) {\n // size limits the total amount of data returned, note you will get fewer bytes\n // than size if you pass a value larger than the size of the file.\n // If size is set to 0 then all of the data will be returned.\n-// TODO: Should we error if multiple files are matched?\n-func (c APIClient) GetFile(commit *pfs.Commit, path string, w io.Writer) error {\n-\tr, err := c.getFileTar(commit, path)\n+func (c APIClient) GetFile(commit *pfs.Commit, path string, w io.Writer) (retErr error) {\n+\tctx, cf := context.WithCancel(c.Ctx())", "msg": "I think this should defer a `grpcutil.ScrupGRPC`"}
{"old_hunk": "@@ -33,6 +34,7 @@ type RealEnv struct {\n \tAuthServer               authapi.APIServer\n \tPFSServer                pfsapi.APIServer\n \tTransactionServer        txnserver.APIServer\n+\tProxyServer              *proxyserver.APIServer", "hunk": "@@ -34,7 +34,7 @@ type RealEnv struct {\n \tAuthServer               authapi.APIServer\n \tPFSServer                pfsapi.APIServer\n \tTransactionServer        txnserver.APIServer\n-\tProxyServer              *proxyserver.APIServer\n+\tProxyServer              proxy.APIServer\n \tMockPPSTransactionServer *MockPPSTransactionServer\n }\n \n", "comment": "This should probably use the proto interface rather than the concrete type.", "ids": [47672, "7b88a2e43a0c6b2eedb4e8061f47ce851c848930", "0fc25239fd46bd57b4c99b46c4d4e28b08327ab5"], "repo": "pachyderm-pachyderm", "ghid": 6403, "old": " \tAuthServer               authapi.APIServer\n \tPFSServer                pfsapi.APIServer\n \tTransactionServer        txnserver.APIServer\n-\tProxyServer              *proxyserver.APIServer\n \tMockPPSTransactionServer *MockPPSTransactionServer\n }", "new": " \tAuthServer               authapi.APIServer\n \tPFSServer                pfsapi.APIServer\n \tTransactionServer        txnserver.APIServer\n+\tProxyServer              proxy.APIServer\n \tMockPPSTransactionServer *MockPPSTransactionServer\n }", "lang": "go", "y": 1, "proj": "pachyderm-pachyderm", "patch": "@@ -33,6 +34,7 @@ type RealEnv struct {\n \tAuthServer               authapi.APIServer\n \tPFSServer                pfsapi.APIServer\n \tTransactionServer        txnserver.APIServer\n+\tProxyServer              *proxyserver.APIServer", "msg": "This should probably use the proto interface rather than the concrete type."}
{"old_hunk": "@@ -77,6 +78,14 @@ type NonblockingServiceEnv struct {\n \t// returned, then pachd would be unable to start)\n \tpachEg errgroup.Group\n \n+\t// servicePachClient is the \"template\" service pach client (client connected through the pachd service)\n+\t// other clients returned by this library are based on. It contains the original GRPC client\n+\t// connection and has no ctx and therefore no auth credentials or cancellation.\n+\tservicePachClient *client.APIClient\n+\t// servicePachEg coordinates the initialization of servicePachClient (see pachdEg)\n+\tservicePachEg   errgroup.Group\n+\tservicePachOnce sync.Once", "hunk": "@@ -78,14 +78,6 @@ type NonblockingServiceEnv struct {\n \t// returned, then pachd would be unable to start)\n \tpachEg errgroup.Group\n \n-\t// servicePachClient is the \"template\" service pach client (client connected through the pachd service)\n-\t// other clients returned by this library are based on. It contains the original GRPC client\n-\t// connection and has no ctx and therefore no auth credentials or cancellation.\n-\tservicePachClient *client.APIClient\n-\t// servicePachEg coordinates the initialization of servicePachClient (see pachdEg)\n-\tservicePachEg   errgroup.Group\n-\tservicePachOnce sync.Once\n-\n \t// etcdAddress is the domain name or hostport where etcd can be reached\n \tetcdAddress string\n \t// etcdClient is an etcd client that's shared by all users of this environment\n", "comment": "Either move these into local variables when initializing the proxy listener or expose them with `ServiceEnv` getters (if you have other plans for these) - otherwise I don't see the point of having them here since they're only used in one place.", "ids": [47673, "7b88a2e43a0c6b2eedb4e8061f47ce851c848930", "0fc25239fd46bd57b4c99b46c4d4e28b08327ab5"], "repo": "pachyderm-pachyderm", "ghid": 6403, "old": " \t// returned, then pachd would be unable to start)\n \tpachEg errgroup.Group\n-\t// servicePachClient is the \"template\" service pach client (client connected through the pachd service)\n-\t// other clients returned by this library are based on. It contains the original GRPC client\n-\t// connection and has no ctx and therefore no auth credentials or cancellation.\n-\tservicePachClient *client.APIClient\n-\t// servicePachEg coordinates the initialization of servicePachClient (see pachdEg)\n-\tservicePachEg   errgroup.Group\n-\tservicePachOnce sync.Once\n-\n \t// etcdAddress is the domain name or hostport where etcd can be reached\n \tetcdAddress string\n \t// etcdClient is an etcd client that's shared by all users of this environment", "new": " \t// returned, then pachd would be unable to start)\n \tpachEg errgroup.Group\n \t// etcdAddress is the domain name or hostport where etcd can be reached\n \tetcdAddress string\n \t// etcdClient is an etcd client that's shared by all users of this environment", "lang": "go", "y": 1, "proj": "pachyderm-pachyderm", "patch": "@@ -77,6 +78,14 @@ type NonblockingServiceEnv struct {\n \t// returned, then pachd would be unable to start)\n \tpachEg errgroup.Group\n \n+\t// servicePachClient is the \"template\" service pach client (client connected through the pachd service)\n+\t// other clients returned by this library are based on. It contains the original GRPC client\n+\t// connection and has no ctx and therefore no auth credentials or cancellation.\n+\tservicePachClient *client.APIClient\n+\t// servicePachEg coordinates the initialization of servicePachClient (see pachdEg)\n+\tservicePachEg   errgroup.Group\n+\tservicePachOnce sync.Once", "msg": "Either move these into local variables when initializing the proxy listener or expose them with `ServiceEnv` getters (if you have other plans for these) - otherwise I don't see the point of having them here since they're only used in one place."}
{"old_hunk": "@@ -15,52 +15,80 @@ import (\n \t\"golang.org/x/sync/errgroup\"\n \n \t\"github.com/pachyderm/pachyderm/v2/src/internal/errors\"\n+\t\"github.com/pachyderm/pachyderm/v2/src/internal/uuid\"\n \t\"github.com/pachyderm/pachyderm/v2/src/internal/watch\"\n )\n \n+type PostgresListener interface {\n+\t// Register registers a notifier with the postgres listener.\n+\t// A notifier will receive notifications for the channel it is associated\n+\t// with while registered with the postgres listner.\n+\tRegister(Notifier) error\n+\t// Unregister unregisters a notifier with the postgres listener.\n+\t// A notifier will no longer receive notifications when this call completes.\n+\tUnregister(Notifier) error\n+\t// Close closes the postgres listener.\n+\tClose() error\n+}\n+\n+type Notifier interface {\n+\t// ID is a unique identifier for the notifier.\n+\tID() string\n+\t// Channel is the channel that this notifier should receive notifications for.\n+\tChannel() string\n+\t// Notify sends a notification to the notifier.\n+\tNotify(*pq.Notification)\n+\t// Error sends an error to the notifier.\n+\tError(error)\n+}\n+\n const (\n \tminReconnectInterval = time.Second * 1\n \tmaxReconnectInterval = time.Second * 30\n-\tchannelBufferSize    = 1000\n+\tChannelBufferSize    = 1000\n )\n \n type postgresWatcher struct {\n-\tdb         *sqlx.DB\n-\tlistener   *PostgresListener\n-\tc          chan *watch.Event\n-\tbuf        chan *postgresEvent // buffer for messages before the initial table list is complete\n-\tdone       chan struct{}       // closed when the watcher is closed to interrupt selects\n-\ttemplate   proto.Message\n-\tsqlChannel string\n-\tcloser     sync.Once\n+\tdb       *sqlx.DB\n+\tlistener PostgresListener\n+\tc        chan *watch.Event\n+\tbuf      chan *postgresEvent // buffer for messages before the initial table list is complete\n+\tdone     chan struct{}       // closed when the watcher is closed to interrupt selects\n+\ttemplate proto.Message\n+\tid       string\n+\tchannel  string\n+\tcloser   sync.Once\n \n \t// Filtering variables:\n \topts  watch.WatchOptions // may filter by the operation type (put or delete)\n \tindex *string            // only set if the watch filters by an index (for dealing with hash collisions)\n \tvalue *string            // only set if 'index' is set\n }\n \n-func newPostgresWatch(\n+func NewPostgresWatcher(", "hunk": "@@ -65,7 +65,7 @@ type postgresWatcher struct {\n \tvalue *string            // only set if 'index' is set\n }\n \n-func NewPostgresWatcher(\n+func newPostgresWatcher(\n \tdb *sqlx.DB,\n \tlistener PostgresListener,\n \tchannel string,\n", "comment": "is there a reason this is exported?", "ids": [47667, "7b88a2e43a0c6b2eedb4e8061f47ce851c848930", "0fc25239fd46bd57b4c99b46c4d4e28b08327ab5"], "repo": "pachyderm-pachyderm", "ghid": 6403, "old": " \tvalue *string            // only set if 'index' is set\n }\n-func NewPostgresWatcher(\n \tdb *sqlx.DB,\n \tlistener PostgresListener,\n \tchannel string,", "new": " \tvalue *string            // only set if 'index' is set\n }\n+func newPostgresWatcher(\n \tdb *sqlx.DB,\n \tlistener PostgresListener,\n \tchannel string,", "lang": "go", "y": 1, "proj": "pachyderm-pachyderm", "patch": "@@ -15,52 +15,80 @@ import (\n \t\"golang.org/x/sync/errgroup\"\n \n \t\"github.com/pachyderm/pachyderm/v2/src/internal/errors\"\n+\t\"github.com/pachyderm/pachyderm/v2/src/internal/uuid\"\n \t\"github.com/pachyderm/pachyderm/v2/src/internal/watch\"\n )\n \n+type PostgresListener interface {\n+\t// Register registers a notifier with the postgres listener.\n+\t// A notifier will receive notifications for the channel it is associated\n+\t// with while registered with the postgres listner.\n+\tRegister(Notifier) error\n+\t// Unregister unregisters a notifier with the postgres listener.\n+\t// A notifier will no longer receive notifications when this call completes.\n+\tUnregister(Notifier) error\n+\t// Close closes the postgres listener.\n+\tClose() error\n+}\n+\n+type Notifier interface {\n+\t// ID is a unique identifier for the notifier.\n+\tID() string\n+\t// Channel is the channel that this notifier should receive notifications for.\n+\tChannel() string\n+\t// Notify sends a notification to the notifier.\n+\tNotify(*pq.Notification)\n+\t// Error sends an error to the notifier.\n+\tError(error)\n+}\n+\n const (\n \tminReconnectInterval = time.Second * 1\n \tmaxReconnectInterval = time.Second * 30\n-\tchannelBufferSize    = 1000\n+\tChannelBufferSize    = 1000\n )\n \n type postgresWatcher struct {\n-\tdb         *sqlx.DB\n-\tlistener   *PostgresListener\n-\tc          chan *watch.Event\n-\tbuf        chan *postgresEvent // buffer for messages before the initial table list is complete\n-\tdone       chan struct{}       // closed when the watcher is closed to interrupt selects\n-\ttemplate   proto.Message\n-\tsqlChannel string\n-\tcloser     sync.Once\n+\tdb       *sqlx.DB\n+\tlistener PostgresListener\n+\tc        chan *watch.Event\n+\tbuf      chan *postgresEvent // buffer for messages before the initial table list is complete\n+\tdone     chan struct{}       // closed when the watcher is closed to interrupt selects\n+\ttemplate proto.Message\n+\tid       string\n+\tchannel  string\n+\tcloser   sync.Once\n \n \t// Filtering variables:\n \topts  watch.WatchOptions // may filter by the operation type (put or delete)\n \tindex *string            // only set if the watch filters by an index (for dealing with hash collisions)\n \tvalue *string            // only set if 'index' is set\n }\n \n-func newPostgresWatch(\n+func NewPostgresWatcher(", "msg": "is there a reason this is exported?"}
{"old_hunk": "@@ -674,6 +675,80 @@ All jobs created by a pipeline will create commits in the pipeline's output repo\n \tinspectPipeline.Flags().AddFlagSet(fullTimestampsFlags)\n \tcommands = append(commands, cmdutil.CreateAlias(inspectPipeline, \"inspect pipeline\"))\n \n+\tvar editor string\n+\tvar editorArgs []string\n+\teditPipeline := &cobra.Command{\n+\t\tUse:   \"{{alias}} <pipeline>\",\n+\t\tShort: \"Edit the manifest for a pipeline in your text editor.\",\n+\t\tLong:  \"Edit the manifest for a pipeline in your text editor.\",\n+\t\tRun: cmdutil.RunFixedArgs(1, func(args []string) (retErr error) {\n+\t\t\tclient, err := pachdclient.NewOnUserMachine(\"user\")\n+\t\t\tif err != nil {\n+\t\t\t\treturn err\n+\t\t\t}\n+\t\t\tdefer client.Close()\n+\t\t\n+\t\t\tpipelineInfo, err := client.InspectPipeline(args[0])", "hunk": "@@ -687,8 +687,8 @@ All jobs created by a pipeline will create commits in the pipeline's output repo\n \t\t\t\treturn err\n \t\t\t}\n \t\t\tdefer client.Close()\n-\t\t\n-\t\t\tpipelineInfo, err := client.InspectPipeline(args[0])\n+\n+\t\t\tpipelineInfo, err := client.InspectPipeline(args[0], true)\n \t\t\tif err != nil {\n \t\t\t\treturn err\n \t\t\t}\n", "comment": "Heads up that this will break when merged with `master` - `InspectPipeline` now takes a `details bool` arg (which this should set to true).", "ids": [47678, "58ca9aeba3a779b14e18ff167d760f60d0e2b9b1", "cd0e97f211daeffc9a1564921d114b11275524a3"], "repo": "pachyderm-pachyderm", "ghid": 6406, "old": " \t\t\t\treturn err\n \t\t\t}\n \t\t\tdefer client.Close()\n-\t\t\n-\t\t\tpipelineInfo, err := client.InspectPipeline(args[0])\n \t\t\tif err != nil {\n \t\t\t\treturn err\n \t\t\t}", "new": " \t\t\t\treturn err\n \t\t\t}\n \t\t\tdefer client.Close()\n+\n+\t\t\tpipelineInfo, err := client.InspectPipeline(args[0], true)\n \t\t\tif err != nil {\n \t\t\t\treturn err\n \t\t\t}", "lang": "go", "y": 1, "proj": "pachyderm-pachyderm", "patch": "@@ -674,6 +675,80 @@ All jobs created by a pipeline will create commits in the pipeline's output repo\n \tinspectPipeline.Flags().AddFlagSet(fullTimestampsFlags)\n \tcommands = append(commands, cmdutil.CreateAlias(inspectPipeline, \"inspect pipeline\"))\n \n+\tvar editor string\n+\tvar editorArgs []string\n+\teditPipeline := &cobra.Command{\n+\t\tUse:   \"{{alias}} <pipeline>\",\n+\t\tShort: \"Edit the manifest for a pipeline in your text editor.\",\n+\t\tLong:  \"Edit the manifest for a pipeline in your text editor.\",\n+\t\tRun: cmdutil.RunFixedArgs(1, func(args []string) (retErr error) {\n+\t\t\tclient, err := pachdclient.NewOnUserMachine(\"user\")\n+\t\t\tif err != nil {\n+\t\t\t\treturn err\n+\t\t\t}\n+\t\t\tdefer client.Close()\n+\t\t\n+\t\t\tpipelineInfo, err := client.InspectPipeline(args[0])", "msg": "Heads up that this will break when merged with `master` - `InspectPipeline` now takes a `details bool` arg (which this should set to true)."}
{"old_hunk": "@@ -105,14 +106,16 @@ func TestSimplePipeline(t *testing.T) {\n \tfor _, info := range commitInfos {\n \t\tcommitRepos = append(commitRepos, info.Commit.Branch.Repo)\n \t}\n-\trequire.EqualOneOf(t, commitRepos[:2], client.NewRepo(dataRepo))\n-\trequire.EqualOneOf(t, commitRepos[:2], client.NewSystemRepo(pipeline, pfs.SpecRepoType))\n-\trequire.EqualOneOf(t, commitRepos[2:], client.NewRepo(pipeline))\n-\trequire.EqualOneOf(t, commitRepos[2:], client.NewSystemRepo(pipeline, pfs.MetaRepoType))\n+\trequire.ElementsEqual(t, commitRepos[:2], []*pfs.Repo{client.NewRepo(dataRepo), client.NewSystemRepo(pipeline, pfs.SpecRepoType)})\n+\trequire.ElementsEqual(t, commitRepos[:2], []*pfs.Repo{client.NewRepo(pipeline), client.NewSystemRepo(pipeline, pfs.MetaRepoType)})", "hunk": "@@ -106,8 +106,8 @@ func TestSimplePipeline(t *testing.T) {\n \tfor _, info := range commitInfos {\n \t\tcommitRepos = append(commitRepos, info.Commit.Branch.Repo)\n \t}\n-\trequire.ElementsEqual(t, commitRepos[:2], []*pfs.Repo{client.NewRepo(dataRepo), client.NewSystemRepo(pipeline, pfs.SpecRepoType)})\n-\trequire.ElementsEqual(t, commitRepos[:2], []*pfs.Repo{client.NewRepo(pipeline), client.NewSystemRepo(pipeline, pfs.MetaRepoType)})\n+\trequire.ElementsEqualUnderFn(t, commitRepos[:2], []*pfs.Repo{client.NewRepo(dataRepo), client.NewSystemRepo(pipeline, pfs.SpecRepoType)}, pfsdb.RepoKey)\n+\trequire.ElementsEqualUnderFn(t, commitRepos[2:], []*pfs.Repo{client.NewRepo(pipeline), client.NewSystemRepo(pipeline, pfs.MetaRepoType)}, pfsdb.RepoKey)\n \n \tvar buf bytes.Buffer\n \tfor _, info := range commitInfos {\n", "comment": "Oh whoops, you can't do ElementsEqual on proto structs because they can't be map keys. I think the previous approach to this was ok?", "ids": [47707, "c9149f6e6df56dcfa05b036fcfb76dbe775a4054", "6c71bac02d315a67d396edae366264bde3b9bc70"], "repo": "pachyderm-pachyderm", "ghid": 6414, "old": " \tfor _, info := range commitInfos {\n \t\tcommitRepos = append(commitRepos, info.Commit.Branch.Repo)\n \t}\n-\trequire.ElementsEqual(t, commitRepos[:2], []*pfs.Repo{client.NewRepo(dataRepo), client.NewSystemRepo(pipeline, pfs.SpecRepoType)})\n-\trequire.ElementsEqual(t, commitRepos[:2], []*pfs.Repo{client.NewRepo(pipeline), client.NewSystemRepo(pipeline, pfs.MetaRepoType)})\n \tvar buf bytes.Buffer\n \tfor _, info := range commitInfos {", "new": " \tfor _, info := range commitInfos {\n \t\tcommitRepos = append(commitRepos, info.Commit.Branch.Repo)\n \t}\n+\trequire.ElementsEqualUnderFn(t, commitRepos[:2], []*pfs.Repo{client.NewRepo(dataRepo), client.NewSystemRepo(pipeline, pfs.SpecRepoType)}, pfsdb.RepoKey)\n+\trequire.ElementsEqualUnderFn(t, commitRepos[2:], []*pfs.Repo{client.NewRepo(pipeline), client.NewSystemRepo(pipeline, pfs.MetaRepoType)}, pfsdb.RepoKey)\n \tvar buf bytes.Buffer\n \tfor _, info := range commitInfos {", "lang": "go", "y": 1, "proj": "pachyderm-pachyderm", "patch": "@@ -105,14 +106,16 @@ func TestSimplePipeline(t *testing.T) {\n \tfor _, info := range commitInfos {\n \t\tcommitRepos = append(commitRepos, info.Commit.Branch.Repo)\n \t}\n-\trequire.EqualOneOf(t, commitRepos[:2], client.NewRepo(dataRepo))\n-\trequire.EqualOneOf(t, commitRepos[:2], client.NewSystemRepo(pipeline, pfs.SpecRepoType))\n-\trequire.EqualOneOf(t, commitRepos[2:], client.NewRepo(pipeline))\n-\trequire.EqualOneOf(t, commitRepos[2:], client.NewSystemRepo(pipeline, pfs.MetaRepoType))\n+\trequire.ElementsEqual(t, commitRepos[:2], []*pfs.Repo{client.NewRepo(dataRepo), client.NewSystemRepo(pipeline, pfs.SpecRepoType)})\n+\trequire.ElementsEqual(t, commitRepos[:2], []*pfs.Repo{client.NewRepo(pipeline), client.NewSystemRepo(pipeline, pfs.MetaRepoType)})", "msg": "Oh whoops, you can't do ElementsEqual on proto structs because they can't be map keys. I think the previous approach to this was ok?"}
{"old_hunk": "@@ -458,43 +462,26 @@ $ {{alias}} foo@master --from XXX`,\n \tlistCommit.Flags().StringVarP(&from, \"from\", \"f\", \"\", \"list all commits since this commit\")\n \tlistCommit.Flags().IntVarP(&number, \"number\", \"n\", 0, \"list only this many commits; if set to zero, list all commits\")\n \tlistCommit.MarkFlagCustom(\"from\", \"__pachctl_get_commit $(__parse_repo ${nouns[0]})\")\n-\tlistCommit.Flags().AddFlagSet(rawFlags)\n-\tlistCommit.Flags().AddFlagSet(fullTimestampsFlags)\n+\tlistCommit.Flags().AddFlagSet(outputFlags)\n+\tlistCommit.Flags().AddFlagSet(timestampFlags)\n \tshell.RegisterCompletionFunc(listCommit, shell.RepoCompletion)\n \tcommands = append(commands, cmdutil.CreateAlias(listCommit, \"list commit\"))\n \n \tvar branches cmdutil.RepeatedStringArg\n \twaitCommit := &cobra.Command{\n-\t\tUse:   \"{{alias}} ( <commitset-id> | <repo>@<branch-or-commit> )\",\n-\t\tShort: \"Wait for the specified commit(s) to finish and return them.\",\n-\t\tLong:  \"Wait for the specified commit(s) to finish and return them.\",\n+\t\tUse:   \"{{alias}} <repo>@<branch-or-commit>\",\n+\t\tShort: \"Wait for the specified commit to finish and return it.\",\n+\t\tLong:  \"Wait for the specified commit to finish and return it.\",\n \t\tExample: `", "hunk": "@@ -473,10 +473,7 @@ $ {{alias}} foo@master --from XXX`,\n \t\tShort: \"Wait for the specified commit to finish and return it.\",\n \t\tLong:  \"Wait for the specified commit to finish and return it.\",\n \t\tExample: `\n-# return commits in the same commit set as foo@XXX\n-$ {{alias}} XXX\n-\n-# return commits caused by foo@XXX leading to branch bar@baz\n+# wait for the commit foo@XXX to finish and return it\n $ {{alias}} foo@XXX -b bar@baz`,\n \t\tRun: cmdutil.RunFixedArgs(1, func(args []string) (retErr error) {\n \t\t\tcommit, err := cmdutil.ParseCommit(args[0])\n", "comment": "move these examples down to wait commitset", "ids": [47720, "a50990b3f50a1fc4446d48902de5094b3317ff97", "0bbcda6ce99340af8c2599efb2c1818f6af5f8c3"], "repo": "pachyderm-pachyderm", "ghid": 6420, "old": " \t\tShort: \"Wait for the specified commit to finish and return it.\",\n \t\tLong:  \"Wait for the specified commit to finish and return it.\",\n \t\tExample: `\n-# return commits in the same commit set as foo@XXX\n-$ {{alias}} XXX\n-\n-# return commits caused by foo@XXX leading to branch bar@baz\n $ {{alias}} foo@XXX -b bar@baz`,\n \t\tRun: cmdutil.RunFixedArgs(1, func(args []string) (retErr error) {\n \t\t\tcommit, err := cmdutil.ParseCommit(args[0])", "new": " \t\tShort: \"Wait for the specified commit to finish and return it.\",\n \t\tLong:  \"Wait for the specified commit to finish and return it.\",\n \t\tExample: `\n+# wait for the commit foo@XXX to finish and return it\n $ {{alias}} foo@XXX -b bar@baz`,\n \t\tRun: cmdutil.RunFixedArgs(1, func(args []string) (retErr error) {\n \t\t\tcommit, err := cmdutil.ParseCommit(args[0])", "lang": "go", "y": 1, "proj": "pachyderm-pachyderm", "patch": "@@ -458,43 +462,26 @@ $ {{alias}} foo@master --from XXX`,\n \tlistCommit.Flags().StringVarP(&from, \"from\", \"f\", \"\", \"list all commits since this commit\")\n \tlistCommit.Flags().IntVarP(&number, \"number\", \"n\", 0, \"list only this many commits; if set to zero, list all commits\")\n \tlistCommit.MarkFlagCustom(\"from\", \"__pachctl_get_commit $(__parse_repo ${nouns[0]})\")\n-\tlistCommit.Flags().AddFlagSet(rawFlags)\n-\tlistCommit.Flags().AddFlagSet(fullTimestampsFlags)\n+\tlistCommit.Flags().AddFlagSet(outputFlags)\n+\tlistCommit.Flags().AddFlagSet(timestampFlags)\n \tshell.RegisterCompletionFunc(listCommit, shell.RepoCompletion)\n \tcommands = append(commands, cmdutil.CreateAlias(listCommit, \"list commit\"))\n \n \tvar branches cmdutil.RepeatedStringArg\n \twaitCommit := &cobra.Command{\n-\t\tUse:   \"{{alias}} ( <commitset-id> | <repo>@<branch-or-commit> )\",\n-\t\tShort: \"Wait for the specified commit(s) to finish and return them.\",\n-\t\tLong:  \"Wait for the specified commit(s) to finish and return them.\",\n+\t\tUse:   \"{{alias}} <repo>@<branch-or-commit>\",\n+\t\tShort: \"Wait for the specified commit to finish and return it.\",\n+\t\tLong:  \"Wait for the specified commit to finish and return it.\",\n \t\tExample: `", "msg": "move these examples down to wait commitset"}
{"old_hunk": "@@ -0,0 +1,302 @@\n+package transform\n+\n+import (\n+\t\"context\"\n+\t\"os\"\n+\t\"path/filepath\"\n+\n+\t\"github.com/gogo/protobuf/proto\"\n+\t\"github.com/pachyderm/pachyderm/v2/src/client\"\n+\t\"github.com/pachyderm/pachyderm/v2/src/internal/ppsutil\"\n+\t\"github.com/pachyderm/pachyderm/v2/src/internal/storage/renew\"\n+\t\"github.com/pachyderm/pachyderm/v2/src/internal/uuid\"\n+\t\"github.com/pachyderm/pachyderm/v2/src/internal/work\"\n+\t\"github.com/pachyderm/pachyderm/v2/src/pfs\"\n+\t\"github.com/pachyderm/pachyderm/v2/src/pps\"\n+\tpfsserver \"github.com/pachyderm/pachyderm/v2/src/server/pfs\"\n+\t\"github.com/pachyderm/pachyderm/v2/src/server/worker/datum\"\n+\t\"github.com/pachyderm/pachyderm/v2/src/server/worker/driver\"\n+\t\"github.com/pachyderm/pachyderm/v2/src/server/worker/logs\"\n+)\n+\n+type pendingJob struct {\n+\tdriver                     driver.Driver\n+\tlogger                     logs.TaggedLogger\n+\tcancel                     context.CancelFunc\n+\tji                         *pps.JobInfo\n+\tparentDit                  datum.Iterator\n+\tcommitInfo, metaCommitInfo *pfs.CommitInfo\n+\tparentMetaCommit           *pfs.Commit\n+\ttaskMaster                 *work.Master\n+\thasher                     datum.Hasher\n+\tnoSkip                     bool\n+}\n+\n+func (pj *pendingJob) writeJobInfo() error {\n+\tpj.logger.Logf(\"updating job info, state: %s\", pj.ji.State)\n+\treturn ppsutil.WriteJobInfo(pj.driver.PachClient(), pj.ji)\n+}\n+\n+// TODO: The job info should eventually just have a field with type *datum.Stats\n+func (pj *pendingJob) saveJobStats(stats *datum.Stats) {\n+\t// TODO: Need to clean up the setup of process stats.\n+\tif pj.ji.Stats == nil {\n+\t\tpj.ji.Stats = &pps.ProcessStats{}\n+\t}\n+\tdatum.MergeProcessStats(pj.ji.Stats, stats.ProcessStats)\n+\tpj.ji.DataProcessed += stats.Processed\n+\tpj.ji.DataSkipped += stats.Skipped\n+\tpj.ji.DataFailed += stats.Failed\n+\tpj.ji.DataRecovered += stats.Recovered\n+\tpj.ji.DataTotal += stats.Processed + stats.Skipped + stats.Failed + stats.Recovered\n+}\n+\n+func (pj *pendingJob) load() error {\n+\tpachClient := pj.driver.PachClient()\n+\tvar err error\n+\t// Load and clear the output commit.\n+\tpj.commitInfo, err = pachClient.PfsAPIClient.InspectCommit(\n+\t\tpachClient.Ctx(),\n+\t\t&pfs.InspectCommitRequest{\n+\t\t\tCommit: pj.ji.OutputCommit,\n+\t\t\tWait:   pfs.CommitState_STARTED,\n+\t\t})\n+\tif err != nil {\n+\t\treturn err\n+\t}\n+\tif _, err := pachClient.PfsAPIClient.ClearCommit(\n+\t\tpachClient.Ctx(),\n+\t\t&pfs.ClearCommitRequest{\n+\t\t\tCommit: pj.ji.OutputCommit,\n+\t\t}); err != nil {\n+\t\treturn err\n+\t}\n+\t// Load and clear the meta commit.\n+\tpj.metaCommitInfo, err = pachClient.PfsAPIClient.InspectCommit(\n+\t\tpachClient.Ctx(),\n+\t\t&pfs.InspectCommitRequest{\n+\t\t\tCommit: ppsutil.MetaCommit(pj.ji.OutputCommit),\n+\t\t\tWait:   pfs.CommitState_STARTED,\n+\t\t})\n+\tif err != nil {\n+\t\treturn err\n+\t}\n+\tif _, err := pachClient.PfsAPIClient.ClearCommit(\n+\t\tpachClient.Ctx(),\n+\t\t&pfs.ClearCommitRequest{\n+\t\t\tCommit: ppsutil.MetaCommit(pj.ji.OutputCommit),\n+\t\t}); err != nil {\n+\t\treturn err\n+\t}\n+\t// Find the most recent successful ancestor commit to use as the\n+\t// base for this job.\n+\t// TODO: This should be an operation supported and exposed by PFS.\n+\tpj.parentMetaCommit = pj.metaCommitInfo.ParentCommit\n+\tfor pj.parentMetaCommit != nil {\n+\t\tci, err := pachClient.PfsAPIClient.InspectCommit(\n+\t\t\tpachClient.Ctx(),\n+\t\t\t&pfs.InspectCommitRequest{\n+\t\t\t\tCommit: pj.parentMetaCommit,\n+\t\t\t\tWait:   pfs.CommitState_STARTED,\n+\t\t\t})\n+\t\tif err != nil {\n+\t\t\treturn err\n+\t\t}\n+\t\tif !ci.Error {\n+\t\t\tif ci.Finished != nil {\n+\t\t\t\tpj.parentDit = datum.NewCommitIterator(pachClient, pj.parentMetaCommit)\n+\t\t\t} else {\n+\t\t\t\tparentJi, err := pachClient.InspectJob(pj.ji.Job.Pipeline.Name, pj.parentMetaCommit.ID, true)\n+\t\t\t\tif err != nil {\n+\t\t\t\t\treturn err\n+\t\t\t\t}\n+\t\t\t\tpj.parentDit, err = datum.NewIterator(pachClient, parentJi.Details.Input)\n+\t\t\t\tif err != nil {\n+\t\t\t\t\treturn err\n+\t\t\t\t}\n+\t\t\t\tpj.parentDit = datum.NewJobIterator(pj.parentDit, parentJi.Job, pj.hasher)", "hunk": "@@ -110,11 +110,11 @@ func (pj *pendingJob) load() error {\n \t\t\t\tif err != nil {\n \t\t\t\t\treturn err\n \t\t\t\t}\n-\t\t\t\tpj.parentDit, err = datum.NewIterator(pachClient, parentJi.Details.Input)\n+\t\t\t\tdit, err := datum.NewIterator(pachClient, parentJi.Details.Input)\n \t\t\t\tif err != nil {\n \t\t\t\t\treturn err\n \t\t\t\t}\n-\t\t\t\tpj.parentDit = datum.NewJobIterator(pj.parentDit, parentJi.Job, pj.hasher)\n+\t\t\t\tpj.parentDit = datum.NewJobIterator(dit, parentJi.Job, pj.hasher)\n \t\t\t}\n \t\t\tbreak\n \t\t}\n", "comment": "Should this be setting `pj.parentDit` a second time?", "ids": [47746, "34f918b47d9372ab5aca748aa5338c745e6951f1", "9e38aa46763bc5e0cbb997a89b1325fc01fb0e20"], "repo": "pachyderm-pachyderm", "ghid": 6427, "old": " \t\t\t\tif err != nil {\n \t\t\t\t\treturn err\n \t\t\t\t}\n-\t\t\t\tpj.parentDit, err = datum.NewIterator(pachClient, parentJi.Details.Input)\n \t\t\t\tif err != nil {\n \t\t\t\t\treturn err\n \t\t\t\t}\n-\t\t\t\tpj.parentDit = datum.NewJobIterator(pj.parentDit, parentJi.Job, pj.hasher)\n \t\t\t}\n \t\t\tbreak\n \t\t}", "new": " \t\t\t\tif err != nil {\n \t\t\t\t\treturn err\n \t\t\t\t}\n+\t\t\t\tdit, err := datum.NewIterator(pachClient, parentJi.Details.Input)\n \t\t\t\tif err != nil {\n \t\t\t\t\treturn err\n \t\t\t\t}\n+\t\t\t\tpj.parentDit = datum.NewJobIterator(dit, parentJi.Job, pj.hasher)\n \t\t\t}\n \t\t\tbreak\n \t\t}", "lang": "go", "y": 1, "proj": "pachyderm-pachyderm", "patch": "@@ -0,0 +1,302 @@\n+package transform\n+\n+import (\n+\t\"context\"\n+\t\"os\"\n+\t\"path/filepath\"\n+\n+\t\"github.com/gogo/protobuf/proto\"\n+\t\"github.com/pachyderm/pachyderm/v2/src/client\"\n+\t\"github.com/pachyderm/pachyderm/v2/src/internal/ppsutil\"\n+\t\"github.com/pachyderm/pachyderm/v2/src/internal/storage/renew\"\n+\t\"github.com/pachyderm/pachyderm/v2/src/internal/uuid\"\n+\t\"github.com/pachyderm/pachyderm/v2/src/internal/work\"\n+\t\"github.com/pachyderm/pachyderm/v2/src/pfs\"\n+\t\"github.com/pachyderm/pachyderm/v2/src/pps\"\n+\tpfsserver \"github.com/pachyderm/pachyderm/v2/src/server/pfs\"\n+\t\"github.com/pachyderm/pachyderm/v2/src/server/worker/datum\"\n+\t\"github.com/pachyderm/pachyderm/v2/src/server/worker/driver\"\n+\t\"github.com/pachyderm/pachyderm/v2/src/server/worker/logs\"\n+)\n+\n+type pendingJob struct {\n+\tdriver                     driver.Driver\n+\tlogger                     logs.TaggedLogger\n+\tcancel                     context.CancelFunc\n+\tji                         *pps.JobInfo\n+\tparentDit                  datum.Iterator\n+\tcommitInfo, metaCommitInfo *pfs.CommitInfo\n+\tparentMetaCommit           *pfs.Commit\n+\ttaskMaster                 *work.Master\n+\thasher                     datum.Hasher\n+\tnoSkip                     bool\n+}\n+\n+func (pj *pendingJob) writeJobInfo() error {\n+\tpj.logger.Logf(\"updating job info, state: %s\", pj.ji.State)\n+\treturn ppsutil.WriteJobInfo(pj.driver.PachClient(), pj.ji)\n+}\n+\n+// TODO: The job info should eventually just have a field with type *datum.Stats\n+func (pj *pendingJob) saveJobStats(stats *datum.Stats) {\n+\t// TODO: Need to clean up the setup of process stats.\n+\tif pj.ji.Stats == nil {\n+\t\tpj.ji.Stats = &pps.ProcessStats{}\n+\t}\n+\tdatum.MergeProcessStats(pj.ji.Stats, stats.ProcessStats)\n+\tpj.ji.DataProcessed += stats.Processed\n+\tpj.ji.DataSkipped += stats.Skipped\n+\tpj.ji.DataFailed += stats.Failed\n+\tpj.ji.DataRecovered += stats.Recovered\n+\tpj.ji.DataTotal += stats.Processed + stats.Skipped + stats.Failed + stats.Recovered\n+}\n+\n+func (pj *pendingJob) load() error {\n+\tpachClient := pj.driver.PachClient()\n+\tvar err error\n+\t// Load and clear the output commit.\n+\tpj.commitInfo, err = pachClient.PfsAPIClient.InspectCommit(\n+\t\tpachClient.Ctx(),\n+\t\t&pfs.InspectCommitRequest{\n+\t\t\tCommit: pj.ji.OutputCommit,\n+\t\t\tWait:   pfs.CommitState_STARTED,\n+\t\t})\n+\tif err != nil {\n+\t\treturn err\n+\t}\n+\tif _, err := pachClient.PfsAPIClient.ClearCommit(\n+\t\tpachClient.Ctx(),\n+\t\t&pfs.ClearCommitRequest{\n+\t\t\tCommit: pj.ji.OutputCommit,\n+\t\t}); err != nil {\n+\t\treturn err\n+\t}\n+\t// Load and clear the meta commit.\n+\tpj.metaCommitInfo, err = pachClient.PfsAPIClient.InspectCommit(\n+\t\tpachClient.Ctx(),\n+\t\t&pfs.InspectCommitRequest{\n+\t\t\tCommit: ppsutil.MetaCommit(pj.ji.OutputCommit),\n+\t\t\tWait:   pfs.CommitState_STARTED,\n+\t\t})\n+\tif err != nil {\n+\t\treturn err\n+\t}\n+\tif _, err := pachClient.PfsAPIClient.ClearCommit(\n+\t\tpachClient.Ctx(),\n+\t\t&pfs.ClearCommitRequest{\n+\t\t\tCommit: ppsutil.MetaCommit(pj.ji.OutputCommit),\n+\t\t}); err != nil {\n+\t\treturn err\n+\t}\n+\t// Find the most recent successful ancestor commit to use as the\n+\t// base for this job.\n+\t// TODO: This should be an operation supported and exposed by PFS.\n+\tpj.parentMetaCommit = pj.metaCommitInfo.ParentCommit\n+\tfor pj.parentMetaCommit != nil {\n+\t\tci, err := pachClient.PfsAPIClient.InspectCommit(\n+\t\t\tpachClient.Ctx(),\n+\t\t\t&pfs.InspectCommitRequest{\n+\t\t\t\tCommit: pj.parentMetaCommit,\n+\t\t\t\tWait:   pfs.CommitState_STARTED,\n+\t\t\t})\n+\t\tif err != nil {\n+\t\t\treturn err\n+\t\t}\n+\t\tif !ci.Error {\n+\t\t\tif ci.Finished != nil {\n+\t\t\t\tpj.parentDit = datum.NewCommitIterator(pachClient, pj.parentMetaCommit)\n+\t\t\t} else {\n+\t\t\t\tparentJi, err := pachClient.InspectJob(pj.ji.Job.Pipeline.Name, pj.parentMetaCommit.ID, true)\n+\t\t\t\tif err != nil {\n+\t\t\t\t\treturn err\n+\t\t\t\t}\n+\t\t\t\tpj.parentDit, err = datum.NewIterator(pachClient, parentJi.Details.Input)\n+\t\t\t\tif err != nil {\n+\t\t\t\t\treturn err\n+\t\t\t\t}\n+\t\t\t\tpj.parentDit = datum.NewJobIterator(pj.parentDit, parentJi.Job, pj.hasher)", "msg": "Should this be setting `pj.parentDit` a second time?"}
{"old_hunk": "@@ -1246,7 +1246,7 @@ func TestPipelineErrorHandling(t *testing.T) {\n \n \t\t// so we expect the job to succeed, and to have recovered 2 datums\n \t\trequire.Equal(t, pps.JobState_JOB_SUCCESS, jobInfo.State)\n-\t\trequire.Equal(t, int64(1), jobInfo.DataSkipped)\n+\t\trequire.Equal(t, int64(0), jobInfo.DataSkipped)", "hunk": "@@ -1246,6 +1246,7 @@ func TestPipelineErrorHandling(t *testing.T) {\n \n \t\t// so we expect the job to succeed, and to have recovered 2 datums\n \t\trequire.Equal(t, pps.JobState_JOB_SUCCESS, jobInfo.State)\n+\t\trequire.Equal(t, int64(1), jobInfo.DataProcessed)\n \t\trequire.Equal(t, int64(0), jobInfo.DataSkipped)\n \t\trequire.Equal(t, int64(2), jobInfo.DataRecovered)\n \t\trequire.Equal(t, int64(0), jobInfo.DataFailed)\n", "comment": "I'm a little confused as to why this changed - the pipeline still has three datums in it, right? Shouldn't all three be either skipped/recovered/failed?", "ids": [47745, "34f918b47d9372ab5aca748aa5338c745e6951f1", "cfacfeabeb976b531791584c49c7a58961711e0a"], "repo": "pachyderm-pachyderm", "ghid": 6427, "old": " \t\t// so we expect the job to succeed, and to have recovered 2 datums\n \t\trequire.Equal(t, pps.JobState_JOB_SUCCESS, jobInfo.State)\n \t\trequire.Equal(t, int64(0), jobInfo.DataSkipped)\n \t\trequire.Equal(t, int64(2), jobInfo.DataRecovered)\n \t\trequire.Equal(t, int64(0), jobInfo.DataFailed)", "new": " \t\t// so we expect the job to succeed, and to have recovered 2 datums\n \t\trequire.Equal(t, pps.JobState_JOB_SUCCESS, jobInfo.State)\n+\t\trequire.Equal(t, int64(1), jobInfo.DataProcessed)\n \t\trequire.Equal(t, int64(0), jobInfo.DataSkipped)\n \t\trequire.Equal(t, int64(2), jobInfo.DataRecovered)\n \t\trequire.Equal(t, int64(0), jobInfo.DataFailed)", "lang": "go", "y": 1, "proj": "pachyderm-pachyderm", "patch": "@@ -1246,7 +1246,7 @@ func TestPipelineErrorHandling(t *testing.T) {\n \n \t\t// so we expect the job to succeed, and to have recovered 2 datums\n \t\trequire.Equal(t, pps.JobState_JOB_SUCCESS, jobInfo.State)\n-\t\trequire.Equal(t, int64(1), jobInfo.DataSkipped)\n+\t\trequire.Equal(t, int64(0), jobInfo.DataSkipped)", "msg": "I'm a little confused as to why this changed - the pipeline still has three datums in it, right? Shouldn't all three be either skipped/recovered/failed?"}
{"old_hunk": "@@ -3109,11 +3109,16 @@ func TestPFS(suite *testing.T) {\n \t\tnumFiles := 100\n \t\t_, err := env.PachClient.StartCommit(repo, \"master\")\n \t\trequire.NoError(t, err)\n-\t\tfor i := 0; i < numFiles; i++ {\n-\t\t\trequire.NoError(t, env.PachClient.PutFile(commit, fmt.Sprintf(\"file%d\", i), strings.NewReader(\"1\")))\n-\t\t\trequire.NoError(t, env.PachClient.PutFile(commit, fmt.Sprintf(\"dir1/file%d\", i), strings.NewReader(\"2\")))\n-\t\t\trequire.NoError(t, env.PachClient.PutFile(commit, fmt.Sprintf(\"dir2/dir3/file%d\", i), strings.NewReader(\"3\")))\n-\t\t}\n+\n+\t\tenv.PachClient.WithModifyFileClient(commit, func(mf client.ModifyFile) error {", "hunk": "@@ -3110,14 +3110,14 @@ func TestPFS(suite *testing.T) {\n \t\t_, err := env.PachClient.StartCommit(repo, \"master\")\n \t\trequire.NoError(t, err)\n \n-\t\tenv.PachClient.WithModifyFileClient(commit, func(mf client.ModifyFile) error {\n+\t\trequire.NoError(t, env.PachClient.WithModifyFileClient(commit, func(mf client.ModifyFile) error {\n \t\t\tfor i := 0; i < numFiles; i++ {\n \t\t\t\trequire.NoError(t, mf.PutFile(fmt.Sprintf(\"file%d\", i), strings.NewReader(\"1\")))\n \t\t\t\trequire.NoError(t, mf.PutFile(fmt.Sprintf(\"dir1/file%d\", i), strings.NewReader(\"2\")))\n \t\t\t\trequire.NoError(t, mf.PutFile(fmt.Sprintf(\"dir2/dir3/file%d\", i), strings.NewReader(\"3\")))\n \t\t\t}\n \t\t\treturn nil\n-\t\t})\n+\t\t}))\n \n \t\tchecks := func() {\n \t\t\tfileInfos, err := env.PachClient.GlobFileAll(commit, \"*\")\n", "comment": "We need to check the error here.", "ids": [47762, "1432bce8d8b08e80cf31351bccfef6250c57a181", "bd2a693df8f263a152247f3a2e8e9ae505086d28"], "repo": "pachyderm-pachyderm", "ghid": 6438, "old": " \t\t_, err := env.PachClient.StartCommit(repo, \"master\")\n \t\trequire.NoError(t, err)\n-\t\tenv.PachClient.WithModifyFileClient(commit, func(mf client.ModifyFile) error {\n \t\t\tfor i := 0; i < numFiles; i++ {\n \t\t\t\trequire.NoError(t, mf.PutFile(fmt.Sprintf(\"file%d\", i), strings.NewReader(\"1\")))\n \t\t\t\trequire.NoError(t, mf.PutFile(fmt.Sprintf(\"dir1/file%d\", i), strings.NewReader(\"2\")))\n \t\t\t\trequire.NoError(t, mf.PutFile(fmt.Sprintf(\"dir2/dir3/file%d\", i), strings.NewReader(\"3\")))\n \t\t\t}\n \t\t\treturn nil\n-\t\t})\n \t\tchecks := func() {\n \t\t\tfileInfos, err := env.PachClient.GlobFileAll(commit, \"*\")", "new": " \t\t_, err := env.PachClient.StartCommit(repo, \"master\")\n \t\trequire.NoError(t, err)\n+\t\trequire.NoError(t, env.PachClient.WithModifyFileClient(commit, func(mf client.ModifyFile) error {\n \t\t\tfor i := 0; i < numFiles; i++ {\n \t\t\t\trequire.NoError(t, mf.PutFile(fmt.Sprintf(\"file%d\", i), strings.NewReader(\"1\")))\n \t\t\t\trequire.NoError(t, mf.PutFile(fmt.Sprintf(\"dir1/file%d\", i), strings.NewReader(\"2\")))\n \t\t\t\trequire.NoError(t, mf.PutFile(fmt.Sprintf(\"dir2/dir3/file%d\", i), strings.NewReader(\"3\")))\n \t\t\t}\n \t\t\treturn nil\n+\t\t}))\n \t\tchecks := func() {\n \t\t\tfileInfos, err := env.PachClient.GlobFileAll(commit, \"*\")", "lang": "go", "y": 1, "proj": "pachyderm-pachyderm", "patch": "@@ -3109,11 +3109,16 @@ func TestPFS(suite *testing.T) {\n \t\tnumFiles := 100\n \t\t_, err := env.PachClient.StartCommit(repo, \"master\")\n \t\trequire.NoError(t, err)\n-\t\tfor i := 0; i < numFiles; i++ {\n-\t\t\trequire.NoError(t, env.PachClient.PutFile(commit, fmt.Sprintf(\"file%d\", i), strings.NewReader(\"1\")))\n-\t\t\trequire.NoError(t, env.PachClient.PutFile(commit, fmt.Sprintf(\"dir1/file%d\", i), strings.NewReader(\"2\")))\n-\t\t\trequire.NoError(t, env.PachClient.PutFile(commit, fmt.Sprintf(\"dir2/dir3/file%d\", i), strings.NewReader(\"3\")))\n-\t\t}\n+\n+\t\tenv.PachClient.WithModifyFileClient(commit, func(mf client.ModifyFile) error {", "msg": "We need to check the error here."}
{"old_hunk": "@@ -268,20 +287,28 @@ func (env *TransactionEnv) WithTransaction(ctx context.Context, cb func(Transact\n \t\treturn cb(appendTxn)\n \t}\n \n-\treturn env.WithWriteContext(ctx, func(txnCtx *txncontext.TransactionContext) error {\n-\t\tdirectTxn := NewDirectTransaction(env, txnCtx)\n-\t\tif overrideID != nil {\n-\t\t\tid, err := overrideID(txnCtx)\n-\t\t\tif err != nil {\n-\t\t\t\treturn err\n+\tuseOverride := overrideID != nil\n+\tfor {\n+\t\tif err := env.WithWriteContext(ctx, func(txnCtx *txncontext.TransactionContext) error {\n+\t\t\tdirectTxn := NewDirectTransaction(env, txnCtx)\n+\t\t\tif useOverride && overrideID != nil {", "hunk": "@@ -291,7 +291,7 @@ func (env *TransactionEnv) WithTransaction(ctx context.Context, cb func(Transact\n \tfor {\n \t\tif err := env.WithWriteContext(ctx, func(txnCtx *txncontext.TransactionContext) error {\n \t\t\tdirectTxn := NewDirectTransaction(env, txnCtx)\n-\t\t\tif useOverride && overrideID != nil {\n+\t\t\tif useOverride {\n \t\t\t\tid, err := overrideID(txnCtx)\n \t\t\t\tif err != nil {\n \t\t\t\t\treturn err\n", "comment": "The `overrideID != nil` check here is redundant because `useOverride` can never be true if it is `nil`.", "ids": [47804, "f1c607198195eb1b44a119c3f039c021ae3a4dd7", "1bd77c68941dd3f82cddbbc5f9987b8bef9c50a8"], "repo": "pachyderm-pachyderm", "ghid": 6465, "old": " \tfor {\n \t\tif err := env.WithWriteContext(ctx, func(txnCtx *txncontext.TransactionContext) error {\n \t\t\tdirectTxn := NewDirectTransaction(env, txnCtx)\n-\t\t\tif useOverride && overrideID != nil {\n \t\t\t\tid, err := overrideID(txnCtx)\n \t\t\t\tif err != nil {\n \t\t\t\t\treturn err", "new": " \tfor {\n \t\tif err := env.WithWriteContext(ctx, func(txnCtx *txncontext.TransactionContext) error {\n \t\t\tdirectTxn := NewDirectTransaction(env, txnCtx)\n+\t\t\tif useOverride {\n \t\t\t\tid, err := overrideID(txnCtx)\n \t\t\t\tif err != nil {\n \t\t\t\t\treturn err", "lang": "go", "y": 1, "proj": "pachyderm-pachyderm", "patch": "@@ -268,20 +287,28 @@ func (env *TransactionEnv) WithTransaction(ctx context.Context, cb func(Transact\n \t\treturn cb(appendTxn)\n \t}\n \n-\treturn env.WithWriteContext(ctx, func(txnCtx *txncontext.TransactionContext) error {\n-\t\tdirectTxn := NewDirectTransaction(env, txnCtx)\n-\t\tif overrideID != nil {\n-\t\t\tid, err := overrideID(txnCtx)\n-\t\t\tif err != nil {\n-\t\t\t\treturn err\n+\tuseOverride := overrideID != nil\n+\tfor {\n+\t\tif err := env.WithWriteContext(ctx, func(txnCtx *txncontext.TransactionContext) error {\n+\t\t\tdirectTxn := NewDirectTransaction(env, txnCtx)\n+\t\t\tif useOverride && overrideID != nil {", "msg": "The `overrideID != nil` check here is redundant because `useOverride` can never be true if it is `nil`."}
{"old_hunk": "@@ -739,3 +740,53 @@ func TestNoWarningTagSpecified(t *testing.T) {\n \trequire.NoError(t, err)\n \trequire.Equal(t, \"\", stderr)\n }\n+\n+func TestPipelineCrashingRecovers(t *testing.T) {\n+\tif testing.Short() {\n+\t\tt.Skip(\"Skipping integration tests in short mode\")\n+\t}\n+\trequire.NoError(t, tu.BashCmd(`\n+\t\tyes | pachctl delete all\n+\t`).Run())\n+\n+\t// prevent new pods from being scheduled\n+\trequire.NoError(t, tu.BashCmd(`\n+\t\tkubectl cordon minikube\n+\t`).Run())\n+\trequire.NoError(t, tu.BashCmd(`\n+\t\tpachctl create repo data\n+\t\tpachctl create pipeline <<EOF\n+\t\t  pipeline:\n+\t\t    name: my-pipeline\n+\t\t  input:\n+\t\t    pfs:\n+\t\t      glob: /*\n+\t\t      repo: data\n+\t\t  transform:\n+\t\t    cmd: [ /bin/bash ]\n+\t\t    stdin:\n+\t\t      - \"cp /pfs/data/* /pfs/out\"\n+\t\tEOF\n+\t\t`).Run())\n+\n+\trequire.NoErrorWithinT(t, 10*time.Second, func() error {\n+\t\treturn tu.BashCmd(`\n+\t\tpachctl list pipeline \\\n+\t\t| match 'name: my-pipeline' \\\n+\t\t| match 'crashing'\n+\t\t`).Run()\n+\t})\n+\n+\t// prevent new pods from being scheduled", "hunk": "@@ -769,7 +769,7 @@ func TestPipelineCrashingRecovers(t *testing.T) {\n \t\tEOF\n \t\t`).Run())\n \n-\trequire.NoErrorWithinT(t, 10*time.Second, func() error {\n+\trequire.NoErrorWithinTRetry(t, 30*time.Second, func() error {\n \t\treturn tu.BashCmd(`\n \t\tpachctl list pipeline \\\n \t\t| match 'name: my-pipeline' \\\n", "comment": "// allow new pods to be scheduled", "ids": [47915, "c5446fd0b2fd6484e957547dd195ea5f32046f27", "8fa13dd14ef5950123efe77508dc879e8449e5cb"], "repo": "pachyderm-pachyderm", "ghid": 6523, "old": " \t\tEOF\n \t\t`).Run())\n-\trequire.NoErrorWithinT(t, 10*time.Second, func() error {\n \t\treturn tu.BashCmd(`\n \t\tpachctl list pipeline \\\n \t\t| match 'name: my-pipeline' \\", "new": " \t\tEOF\n \t\t`).Run())\n+\trequire.NoErrorWithinTRetry(t, 30*time.Second, func() error {\n \t\treturn tu.BashCmd(`\n \t\tpachctl list pipeline \\\n \t\t| match 'name: my-pipeline' \\", "lang": "go", "y": 1, "proj": "pachyderm-pachyderm", "patch": "@@ -739,3 +740,53 @@ func TestNoWarningTagSpecified(t *testing.T) {\n \trequire.NoError(t, err)\n \trequire.Equal(t, \"\", stderr)\n }\n+\n+func TestPipelineCrashingRecovers(t *testing.T) {\n+\tif testing.Short() {\n+\t\tt.Skip(\"Skipping integration tests in short mode\")\n+\t}\n+\trequire.NoError(t, tu.BashCmd(`\n+\t\tyes | pachctl delete all\n+\t`).Run())\n+\n+\t// prevent new pods from being scheduled\n+\trequire.NoError(t, tu.BashCmd(`\n+\t\tkubectl cordon minikube\n+\t`).Run())\n+\trequire.NoError(t, tu.BashCmd(`\n+\t\tpachctl create repo data\n+\t\tpachctl create pipeline <<EOF\n+\t\t  pipeline:\n+\t\t    name: my-pipeline\n+\t\t  input:\n+\t\t    pfs:\n+\t\t      glob: /*\n+\t\t      repo: data\n+\t\t  transform:\n+\t\t    cmd: [ /bin/bash ]\n+\t\t    stdin:\n+\t\t      - \"cp /pfs/data/* /pfs/out\"\n+\t\tEOF\n+\t\t`).Run())\n+\n+\trequire.NoErrorWithinT(t, 10*time.Second, func() error {\n+\t\treturn tu.BashCmd(`\n+\t\tpachctl list pipeline \\\n+\t\t| match 'name: my-pipeline' \\\n+\t\t| match 'crashing'\n+\t\t`).Run()\n+\t})\n+\n+\t// prevent new pods from being scheduled", "msg": "// allow new pods to be scheduled"}
{"old_hunk": "@@ -301,10 +301,14 @@ func (c *amazonClient) transformError(err error, objectPath string) error {\n \tif strings.Contains(err.Error(), \"Not Found\") {\n \t\treturn pacherr.NewNotExist(c.bucket, objectPath)\n \t}\n-\tvar awsErr awserr.Error\n-\tif !errors.As(err, &awsErr) {\n+\tawsErr, ok := err.(awserr.Error)", "hunk": "@@ -301,8 +301,8 @@ func (c *amazonClient) transformError(err error, objectPath string) error {\n \tif strings.Contains(err.Error(), \"Not Found\") {\n \t\treturn pacherr.NewNotExist(c.bucket, objectPath)\n \t}\n-\tawsErr, ok := err.(awserr.Error)\n-\tif !ok {\n+\tvar awsErr awserr.Error\n+\tif !errors.As(err, &awsErr) {\n \t\treturn err\n \t}\n \t// errors.Is is unable to correctly identify context.Cancel with the amazon error types\n", "comment": "pretty sure we should be using `errors.As` instead of casting as a general rule since we have wrapped errors around", "ids": [47963, "e3ca7aca09aadf93f271913d8e25a9d4ed73024f", "2a58e6d5594f54486dc4a2e0a420f69e89b71ab8"], "repo": "pachyderm-pachyderm", "ghid": 6551, "old": " \tif strings.Contains(err.Error(), \"Not Found\") {\n \t\treturn pacherr.NewNotExist(c.bucket, objectPath)\n \t}\n-\tawsErr, ok := err.(awserr.Error)\n-\tif !ok {\n \t\treturn err\n \t}\n \t// errors.Is is unable to correctly identify context.Cancel with the amazon error types", "new": " \tif strings.Contains(err.Error(), \"Not Found\") {\n \t\treturn pacherr.NewNotExist(c.bucket, objectPath)\n \t}\n+\tvar awsErr awserr.Error\n+\tif !errors.As(err, &awsErr) {\n \t\treturn err\n \t}\n \t// errors.Is is unable to correctly identify context.Cancel with the amazon error types", "lang": "go", "y": 1, "proj": "pachyderm-pachyderm", "patch": "@@ -301,10 +301,14 @@ func (c *amazonClient) transformError(err error, objectPath string) error {\n \tif strings.Contains(err.Error(), \"Not Found\") {\n \t\treturn pacherr.NewNotExist(c.bucket, objectPath)\n \t}\n-\tvar awsErr awserr.Error\n-\tif !errors.As(err, &awsErr) {\n+\tawsErr, ok := err.(awserr.Error)", "msg": "pretty sure we should be using `errors.As` instead of casting as a general rule since we have wrapped errors around"}
{"old_hunk": "@@ -181,14 +181,19 @@ func (c APIClient) StartCommitParent(repoName string, branchName string, parentB\n // FinishCommit ends the process of committing data to a Repo and persists the\n // Commit. Once a Commit is finished the data becomes immutable and future\n // attempts to write to it with PutFile will error.\n-func (c APIClient) FinishCommit(repoName string, branchName string, commitID string) error {\n+func (c APIClient) FinishCommit(repoName string, branchName string, commitID string) (retErr error) {\n+\tdefer func() { retErr = grpcutil.ScrubGRPC(retErr) }()", "hunk": "@@ -189,10 +189,6 @@ func (c APIClient) FinishCommit(repoName string, branchName string, commitID str\n \t\t\tCommit: NewCommit(repoName, branchName, commitID),\n \t\t},\n \t)\n-\tif err != nil {\n-\t\treturn err\n-\t}\n-\t_, err = c.WaitCommit(repoName, branchName, commitID)\n \treturn err\n }\n \n", "comment": "ScrubGRPC prevents inspecting structured errors. I think we should stop using it.", "ids": [48095, "4f5beba10235fff3896f17fb99fd4399cd6ec0ec", "9563dfb4929145e4fd4a13b55f393c82131ae3dd"], "repo": "pachyderm-pachyderm", "ghid": 6613, "old": " \t\t\tCommit: NewCommit(repoName, branchName, commitID),\n \t\t},\n \t)\n-\tif err != nil {\n-\t\treturn err\n-\t}\n-\t_, err = c.WaitCommit(repoName, branchName, commitID)\n \treturn err\n }", "new": " \t\t\tCommit: NewCommit(repoName, branchName, commitID),\n \t\t},\n \t)\n \treturn err\n }", "lang": "go", "y": 1, "proj": "pachyderm-pachyderm", "patch": "@@ -181,14 +181,19 @@ func (c APIClient) StartCommitParent(repoName string, branchName string, parentB\n // FinishCommit ends the process of committing data to a Repo and persists the\n // Commit. Once a Commit is finished the data becomes immutable and future\n // attempts to write to it with PutFile will error.\n-func (c APIClient) FinishCommit(repoName string, branchName string, commitID string) error {\n+func (c APIClient) FinishCommit(repoName string, branchName string, commitID string) (retErr error) {\n+\tdefer func() { retErr = grpcutil.ScrubGRPC(retErr) }()", "msg": "ScrubGRPC prevents inspecting structured errors. I think we should stop using it."}
{"old_hunk": "@@ -186,6 +189,7 @@ func tryTxFunc(tx *sqlx.Tx, cb func(tx *sqlx.Tx) error) error {\n }\n \n func isTransactionError(err error) bool {\n-\tpqerr := &pq.Error{}\n-\treturn errors.As(err, &pqerr) && pqerr.Code.Class() == \"40\"\n+\t// TODO: remove the pq error once collections no longer needs that driver.", "hunk": "@@ -189,7 +189,6 @@ func tryTxFunc(tx *sqlx.Tx, cb func(tx *sqlx.Tx) error) error {\n }\n \n func isTransactionError(err error) bool {\n-\t// TODO: remove the pq error once collections no longer needs that driver.\n \tpgxErr := &pgconn.PgError{}\n \treturn errors.As(err, &pgxErr) && strings.HasPrefix(pgxErr.Code, \"40\")\n }\n", "comment": "Is this TODO resolved? I don't see a pq error here.", "ids": [48146, "2e431da39cee13bb7a958557c85f3c9652e262cf", "2e92064df677e0b794314d1079863276fe0de665"], "repo": "pachyderm-pachyderm", "ghid": 6619, "old": " }\n func isTransactionError(err error) bool {\n-\t// TODO: remove the pq error once collections no longer needs that driver.\n \tpgxErr := &pgconn.PgError{}\n \treturn errors.As(err, &pgxErr) && strings.HasPrefix(pgxErr.Code, \"40\")\n }", "new": " }\n func isTransactionError(err error) bool {\n \tpgxErr := &pgconn.PgError{}\n \treturn errors.As(err, &pgxErr) && strings.HasPrefix(pgxErr.Code, \"40\")\n }", "lang": "go", "y": 1, "proj": "pachyderm-pachyderm", "patch": "@@ -186,6 +189,7 @@ func tryTxFunc(tx *sqlx.Tx, cb func(tx *sqlx.Tx) error) error {\n }\n \n func isTransactionError(err error) bool {\n-\tpqerr := &pq.Error{}\n-\treturn errors.As(err, &pqerr) && pqerr.Code.Class() == \"40\"\n+\t// TODO: remove the pq error once collections no longer needs that driver.", "msg": "Is this TODO resolved? I don't see a pq error here."}
{"old_hunk": "@@ -1053,7 +1053,7 @@ func (d *driver) resolveCommit(sqlTx *sqlx.Tx, userCommit *pfs.Commit) (*pfs.Com\n \t\t}\n \t}\n \n-\t// Traverse commits' parents until you've reached the right ancestor\n+\t// Traverse commits' parents until you've reached the right/ancestor", "hunk": "@@ -1053,7 +1053,7 @@ func (d *driver) resolveCommit(sqlTx *sqlx.Tx, userCommit *pfs.Commit) (*pfs.Com\n \t\t}\n \t}\n \n-\t// Traverse commits' parents until you've reached the right/ancestor\n+\t// Traverse commits' parents until you've reached the right ancestor\n \tcommitInfo := &pfs.CommitInfo{}\n \tif ancestryLength >= 0 {\n \t\tfor i := 0; i <= ancestryLength; i++ {\n", "comment": "nit: was this change intended?", "ids": [48151, "0d409152a74f2ba6234fc594185e27233eab328e", "2aac66e99f3aeb8862f249d310ecbdf933615850"], "repo": "pachyderm-pachyderm", "ghid": 6627, "old": " \t\t}\n \t}\n-\t// Traverse commits' parents until you've reached the right/ancestor\n \tcommitInfo := &pfs.CommitInfo{}\n \tif ancestryLength >= 0 {\n \t\tfor i := 0; i <= ancestryLength; i++ {", "new": " \t\t}\n \t}\n+\t// Traverse commits' parents until you've reached the right ancestor\n \tcommitInfo := &pfs.CommitInfo{}\n \tif ancestryLength >= 0 {\n \t\tfor i := 0; i <= ancestryLength; i++ {", "lang": "go", "y": 1, "proj": "pachyderm-pachyderm", "patch": "@@ -1053,7 +1053,7 @@ func (d *driver) resolveCommit(sqlTx *sqlx.Tx, userCommit *pfs.Commit) (*pfs.Com\n \t\t}\n \t}\n \n-\t// Traverse commits' parents until you've reached the right ancestor\n+\t// Traverse commits' parents until you've reached the right/ancestor", "msg": "nit: was this change intended?"}
{"old_hunk": "@@ -12,15 +12,13 @@ func WithPipe(wcb func(w io.Writer) error, rcb func(r io.Reader) error) error {\n \tpr, pw := io.Pipe()\n \teg := errgroup.Group{}\n \teg.Go(func() error {\n-\t\tif err := wcb(pw); err != nil {\n-\t\t\treturn pw.CloseWithError(err)\n-\t\t}\n-\t\treturn pw.Close()\n+\t\terr := wcb(pw)\n+\t\tpw.CloseWithError(err)\n+\t\treturn err\n \t})\n \teg.Go(func() error {\n-\t\tif err := rcb(pr); err != nil {\n-\t\t\treturn pr.CloseWithError(err)\n-\t\t}\n+\t\terr := rcb(pr)\n+\t\tpr.CloseWithError(err)\n \t\treturn pr.Close()", "hunk": "@@ -19,7 +19,7 @@ func WithPipe(wcb func(w io.Writer) error, rcb func(r io.Reader) error) error {\n \teg.Go(func() error {\n \t\terr := rcb(pr)\n \t\tpr.CloseWithError(err)\n-\t\treturn pr.Close()\n+\t\treturn err\n \t})\n \treturn eg.Wait()\n }\n", "comment": "This seems like it should be `return err`. The pipe reader is already closed at this point and we want the error.", "ids": [48156, "9b2107515e33a929e3e335167903d252da620dcf", "14b7e846832547ddf677a1658f683bb7fa650068"], "repo": "pachyderm-pachyderm", "ghid": 6642, "old": " \teg.Go(func() error {\n \t\terr := rcb(pr)\n \t\tpr.CloseWithError(err)\n-\t\treturn pr.Close()\n \t})\n \treturn eg.Wait()\n }", "new": " \teg.Go(func() error {\n \t\terr := rcb(pr)\n \t\tpr.CloseWithError(err)\n+\t\treturn err\n \t})\n \treturn eg.Wait()\n }", "lang": "go", "y": 1, "proj": "pachyderm-pachyderm", "patch": "@@ -12,15 +12,13 @@ func WithPipe(wcb func(w io.Writer) error, rcb func(r io.Reader) error) error {\n \tpr, pw := io.Pipe()\n \teg := errgroup.Group{}\n \teg.Go(func() error {\n-\t\tif err := wcb(pw); err != nil {\n-\t\t\treturn pw.CloseWithError(err)\n-\t\t}\n-\t\treturn pw.Close()\n+\t\terr := wcb(pw)\n+\t\tpw.CloseWithError(err)\n+\t\treturn err\n \t})\n \teg.Go(func() error {\n-\t\tif err := rcb(pr); err != nil {\n-\t\t\treturn pr.CloseWithError(err)\n-\t\t}\n+\t\terr := rcb(pr)\n+\t\tpr.CloseWithError(err)\n \t\treturn pr.Close()", "msg": "This seems like it should be `return err`. The pipe reader is already closed at this point and we want the error."}
{"old_hunk": "@@ -91,6 +91,14 @@ func newDriver(env serviceenv.ServiceEnv, txnEnv *txnenv.TransactionEnv, etcdPre\n \tif err != nil {\n \t\treturn nil, err\n \t}\n+\t// test object storage.\n+\tif err := func() error {\n+\t\tctx, cf := context.WithTimeout(context.Background(), 10*time.Second)", "hunk": "@@ -93,7 +93,7 @@ func newDriver(env serviceenv.ServiceEnv, txnEnv *txnenv.TransactionEnv, etcdPre\n \t}\n \t// test object storage.\n \tif err := func() error {\n-\t\tctx, cf := context.WithTimeout(context.Background(), 10*time.Second)\n+\t\tctx, cf := context.WithTimeout(context.Background(), 30*time.Second)\n \t\tdefer cf()\n \t\treturn obj.TestStorage(ctx, objClient)\n \t}(); err != nil {\n", "comment": "We might want to bump this up slightly, maybe 30s.", "ids": [48157, "9b2107515e33a929e3e335167903d252da620dcf", "14b7e846832547ddf677a1658f683bb7fa650068"], "repo": "pachyderm-pachyderm", "ghid": 6642, "old": " \t}\n \t// test object storage.\n \tif err := func() error {\n-\t\tctx, cf := context.WithTimeout(context.Background(), 10*time.Second)\n \t\tdefer cf()\n \t\treturn obj.TestStorage(ctx, objClient)\n \t}(); err != nil {", "new": " \t}\n \t// test object storage.\n \tif err := func() error {\n+\t\tctx, cf := context.WithTimeout(context.Background(), 30*time.Second)\n \t\tdefer cf()\n \t\treturn obj.TestStorage(ctx, objClient)\n \t}(); err != nil {", "lang": "go", "y": 1, "proj": "pachyderm-pachyderm", "patch": "@@ -91,6 +91,14 @@ func newDriver(env serviceenv.ServiceEnv, txnEnv *txnenv.TransactionEnv, etcdPre\n \tif err != nil {\n \t\treturn nil, err\n \t}\n+\t// test object storage.\n+\tif err := func() error {\n+\t\tctx, cf := context.WithTimeout(context.Background(), 10*time.Second)", "msg": "We might want to bump this up slightly, maybe 30s."}
{"old_hunk": "@@ -2558,28 +2566,30 @@ func (a *apiServer) deletePipeline(ctx context.Context, request *pps.DeletePipel\n \t\treturn errors.Wrapf(err, \"collection.Delete\")\n \t}\n \n-\tif request.KeepRepo {\n-\t\t// Remove branch provenance from output and meta\n-\t\tif err := pachClient.CreateBranch(\n-\t\t\trequest.Pipeline.Name,\n-\t\t\tpipelineInfo.Details.OutputBranch,\n-\t\t\t\"\",\n-\t\t\t\"\",\n-\t\t\tnil,\n-\t\t); err != nil {\n-\t\t\treturn err\n-\t\t}\n-\t\tif _, err := pachClient.PfsAPIClient.CreateBranch(pachClient.Ctx(), &pfs.CreateBranchRequest{\n-\t\t\tBranch: client.\n-\t\t\t\tNewSystemRepo(request.Pipeline.Name, pfs.MetaRepoType).\n-\t\t\t\tNewBranch(pipelineInfo.Details.OutputBranch),\n-\t\t}); err != nil && !errutil.IsNotFoundError(err) {\n-\t\t\treturn grpcutil.ScrubGRPC(err)\n-\t\t}\n-\t} else {\n-\t\t// delete the pipeline's output repo\n-\t\tif err := pachClient.DeleteRepo(request.Pipeline.Name, request.Force); err != nil {\n-\t\t\treturn err\n+\tif !missingRepo {\n+\t\tif !request.KeepRepo {\n+\t\t\t// delete the pipeline's output repo\n+\t\t\tif _, err := a.env.PfsServer().DeleteRepo(ctx, &pfs.DeleteRepoRequest{\n+\t\t\t\tRepo:  client.NewRepo(request.Pipeline.Name),\n+\t\t\t\tForce: request.Force,\n+\t\t\t}); err != nil && !errutil.IsNotFoundError(err) {\n+\t\t\t\treturn errors.Wrap(err, \"error deleting pipeline repo\")\n+\t\t\t}\n+\t\t} else if pipelineInfo.Details != nil {", "hunk": "@@ -2590,6 +2590,8 @@ func (a *apiServer) deletePipeline(ctx context.Context, request *pps.DeletePipel\n \t\t\t}); err != nil && !errutil.IsNotFoundError(err) {\n \t\t\t\treturn err\n \t\t\t}\n+\t\t} else {\n+\t\t\treturn errors.New(\"pipeline deletion incomplete, provenance may still be intact\")\n \t\t}\n \t}\n \treturn nil\n", "comment": "should we add a final else that throws an error?", "ids": [48292, "95a3ad6ce2c8824641a3cce847b1276d3812e8d6", "50c1d4940827077da9325ba7e0104d4837f80a4d"], "repo": "pachyderm-pachyderm", "ghid": 6723, "old": " \t\t\t}); err != nil && !errutil.IsNotFoundError(err) {\n \t\t\t\treturn err\n \t\t\t}\n \t\t}\n \t}\n \treturn nil", "new": " \t\t\t}); err != nil && !errutil.IsNotFoundError(err) {\n \t\t\t\treturn err\n \t\t\t}\n+\t\t} else {\n+\t\t\treturn errors.New(\"pipeline deletion incomplete, provenance may still be intact\")\n \t\t}\n \t}\n \treturn nil", "lang": "go", "y": 1, "proj": "pachyderm-pachyderm", "patch": "@@ -2558,28 +2566,30 @@ func (a *apiServer) deletePipeline(ctx context.Context, request *pps.DeletePipel\n \t\treturn errors.Wrapf(err, \"collection.Delete\")\n \t}\n \n-\tif request.KeepRepo {\n-\t\t// Remove branch provenance from output and meta\n-\t\tif err := pachClient.CreateBranch(\n-\t\t\trequest.Pipeline.Name,\n-\t\t\tpipelineInfo.Details.OutputBranch,\n-\t\t\t\"\",\n-\t\t\t\"\",\n-\t\t\tnil,\n-\t\t); err != nil {\n-\t\t\treturn err\n-\t\t}\n-\t\tif _, err := pachClient.PfsAPIClient.CreateBranch(pachClient.Ctx(), &pfs.CreateBranchRequest{\n-\t\t\tBranch: client.\n-\t\t\t\tNewSystemRepo(request.Pipeline.Name, pfs.MetaRepoType).\n-\t\t\t\tNewBranch(pipelineInfo.Details.OutputBranch),\n-\t\t}); err != nil && !errutil.IsNotFoundError(err) {\n-\t\t\treturn grpcutil.ScrubGRPC(err)\n-\t\t}\n-\t} else {\n-\t\t// delete the pipeline's output repo\n-\t\tif err := pachClient.DeleteRepo(request.Pipeline.Name, request.Force); err != nil {\n-\t\t\treturn err\n+\tif !missingRepo {\n+\t\tif !request.KeepRepo {\n+\t\t\t// delete the pipeline's output repo\n+\t\t\tif _, err := a.env.PfsServer().DeleteRepo(ctx, &pfs.DeleteRepoRequest{\n+\t\t\t\tRepo:  client.NewRepo(request.Pipeline.Name),\n+\t\t\t\tForce: request.Force,\n+\t\t\t}); err != nil && !errutil.IsNotFoundError(err) {\n+\t\t\t\treturn errors.Wrap(err, \"error deleting pipeline repo\")\n+\t\t\t}\n+\t\t} else if pipelineInfo.Details != nil {", "msg": "should we add a final else that throws an error?"}
{"old_hunk": "@@ -1336,16 +1336,10 @@ func (d *driver) listCommitSet(ctx context.Context, cb func(*pfs.CommitSetInfo)\n \t})\n }\n \n-func (d *driver) squashCommitSet(txnCtx *txncontext.TransactionContext, commitset *pfs.CommitSet) error {\n+func (d *driver) squashCommitsInternal(txnCtx *txncontext.TransactionContext, commitInfos []*pfs.CommitInfo) error {", "hunk": "@@ -1336,7 +1336,7 @@ func (d *driver) listCommitSet(ctx context.Context, cb func(*pfs.CommitSetInfo)\n \t})\n }\n \n-func (d *driver) squashCommitsInternal(txnCtx *txncontext.TransactionContext, commitInfos []*pfs.CommitInfo) error {\n+func (d *driver) squashCommitSetInternal(txnCtx *txncontext.TransactionContext, commitInfos []*pfs.CommitInfo) error {\n \tdeleted := make(map[string]*pfs.CommitInfo) // deleted commits\n \n \t// 1) Delete each commit in the CommitSet\n", "comment": "nit, `squashCommitsInternal` -> `squashCommitSetInternal`", "ids": [48367, "ec7d87aa98b3df3896a4a130496f661821f4d484", "a883826a906baabf8618de70a3dedc3ec3dbcab7"], "repo": "pachyderm-pachyderm", "ghid": 6742, "old": " \t})\n }\n-func (d *driver) squashCommitsInternal(txnCtx *txncontext.TransactionContext, commitInfos []*pfs.CommitInfo) error {\n \tdeleted := make(map[string]*pfs.CommitInfo) // deleted commits\n \t// 1) Delete each commit in the CommitSet", "new": " \t})\n }\n+func (d *driver) squashCommitSetInternal(txnCtx *txncontext.TransactionContext, commitInfos []*pfs.CommitInfo) error {\n \tdeleted := make(map[string]*pfs.CommitInfo) // deleted commits\n \t// 1) Delete each commit in the CommitSet", "lang": "go", "y": 1, "proj": "pachyderm-pachyderm", "patch": "@@ -1336,16 +1336,10 @@ func (d *driver) listCommitSet(ctx context.Context, cb func(*pfs.CommitSetInfo)\n \t})\n }\n \n-func (d *driver) squashCommitSet(txnCtx *txncontext.TransactionContext, commitset *pfs.CommitSet) error {\n+func (d *driver) squashCommitsInternal(txnCtx *txncontext.TransactionContext, commitInfos []*pfs.CommitInfo) error {", "msg": "nit, `squashCommitsInternal` -> `squashCommitSetInternal`"}
{"old_hunk": "@@ -1486,10 +1480,59 @@ func (d *driver) squashCommitSet(txnCtx *txncontext.TransactionContext, commitse\n \t\t}\n \t}\n \n-\t// 5) notify PPS that this commitset has been squashed so it can clean up any\n+\treturn nil\n+}\n+\n+// dropCommitSet is only implemented for commits with no children, so if any\n+// commits in the commitSet have children the operation will fail.\n+func (d *driver) dropCommitSet(txnCtx *txncontext.TransactionContext, commitset *pfs.CommitSet) error {\n+\t// Look up the commits in the CommitSet\n+\tcommitInfos, err := d.inspectCommitSetImmediate(txnCtx, commitset)\n+\tif err != nil {\n+\t\treturn err\n+\t}\n+\n+\tfor _, ci := range commitInfos {\n+\t\tif len(ci.ChildCommits) != 0 {", "hunk": "@@ -1493,7 +1493,7 @@ func (d *driver) dropCommitSet(txnCtx *txncontext.TransactionContext, commitset\n \t}\n \n \tfor _, ci := range commitInfos {\n-\t\tif len(ci.ChildCommits) != 0 {\n+\t\tif len(ci.ChildCommits) > 0 {\n \t\t\treturn &pfsserver.ErrDropWithChildren{Commit: ci.Commit}\n \t\t}\n \t}\n", "comment": "nit, I usually prefer > for checking against an empty slice since they cannot have negative lengths.", "ids": [48368, "ec7d87aa98b3df3896a4a130496f661821f4d484", "a883826a906baabf8618de70a3dedc3ec3dbcab7"], "repo": "pachyderm-pachyderm", "ghid": 6742, "old": " \t}\n \tfor _, ci := range commitInfos {\n-\t\tif len(ci.ChildCommits) != 0 {\n \t\t\treturn &pfsserver.ErrDropWithChildren{Commit: ci.Commit}\n \t\t}\n \t}", "new": " \t}\n \tfor _, ci := range commitInfos {\n+\t\tif len(ci.ChildCommits) > 0 {\n \t\t\treturn &pfsserver.ErrDropWithChildren{Commit: ci.Commit}\n \t\t}\n \t}", "lang": "go", "y": 1, "proj": "pachyderm-pachyderm", "patch": "@@ -1486,10 +1480,59 @@ func (d *driver) squashCommitSet(txnCtx *txncontext.TransactionContext, commitse\n \t\t}\n \t}\n \n-\t// 5) notify PPS that this commitset has been squashed so it can clean up any\n+\treturn nil\n+}\n+\n+// dropCommitSet is only implemented for commits with no children, so if any\n+// commits in the commitSet have children the operation will fail.\n+func (d *driver) dropCommitSet(txnCtx *txncontext.TransactionContext, commitset *pfs.CommitSet) error {\n+\t// Look up the commits in the CommitSet\n+\tcommitInfos, err := d.inspectCommitSetImmediate(txnCtx, commitset)\n+\tif err != nil {\n+\t\treturn err\n+\t}\n+\n+\tfor _, ci := range commitInfos {\n+\t\tif len(ci.ChildCommits) != 0 {", "msg": "nit, I usually prefer > for checking against an empty slice since they cannot have negative lengths."}
{"old_hunk": "@@ -829,6 +830,23 @@ func (pc *pipelineController) createWorkerSvcAndRc(ctx context.Context, pipeline\n \treturn nil\n }\n \n+// GetBackendSecretVolumeAndMount returns a properly configured Volume and\n+// VolumeMount object given a backend.  The backend needs to be one of the\n+// constants defined in pfs/server.\n+func GetBackendSecretVolumeAndMount(backend string) (v1.Volume, v1.VolumeMount) {", "hunk": "@@ -831,9 +831,8 @@ func (pc *pipelineController) createWorkerSvcAndRc(ctx context.Context, pipeline\n }\n \n // GetBackendSecretVolumeAndMount returns a properly configured Volume and\n-// VolumeMount object given a backend.  The backend needs to be one of the\n-// constants defined in pfs/server.\n-func GetBackendSecretVolumeAndMount(backend string) (v1.Volume, v1.VolumeMount) {\n+// VolumeMount object\n+func GetBackendSecretVolumeAndMount() (v1.Volume, v1.VolumeMount) {\n \treturn v1.Volume{\n \t\t\tName: client.StorageSecretName,\n \t\t\tVolumeSource: v1.VolumeSource{\n", "comment": "i think we can remove the backend parameter since it's unused", "ids": [48373, "cd3e8ba1e1576996f9205dff61ba910f38dee9f8", "3e8c7c9a30e939d94e8dc2efa137b962f24ec7ef"], "repo": "pachyderm-pachyderm", "ghid": 6753, "old": " }\n // GetBackendSecretVolumeAndMount returns a properly configured Volume and\n-// VolumeMount object given a backend.  The backend needs to be one of the\n-// constants defined in pfs/server.\n-func GetBackendSecretVolumeAndMount(backend string) (v1.Volume, v1.VolumeMount) {\n \treturn v1.Volume{\n \t\t\tName: client.StorageSecretName,\n \t\t\tVolumeSource: v1.VolumeSource{", "new": " }\n // GetBackendSecretVolumeAndMount returns a properly configured Volume and\n+// VolumeMount object\n+func GetBackendSecretVolumeAndMount() (v1.Volume, v1.VolumeMount) {\n \treturn v1.Volume{\n \t\t\tName: client.StorageSecretName,\n \t\t\tVolumeSource: v1.VolumeSource{", "lang": "go", "y": 1, "proj": "pachyderm-pachyderm", "patch": "@@ -829,6 +830,23 @@ func (pc *pipelineController) createWorkerSvcAndRc(ctx context.Context, pipeline\n \treturn nil\n }\n \n+// GetBackendSecretVolumeAndMount returns a properly configured Volume and\n+// VolumeMount object given a backend.  The backend needs to be one of the\n+// constants defined in pfs/server.\n+func GetBackendSecretVolumeAndMount(backend string) (v1.Volume, v1.VolumeMount) {", "msg": "i think we can remove the backend parameter since it's unused"}
{"old_hunk": "@@ -5201,6 +5201,7 @@ func TestPFS(suite *testing.T) {\n \t\tc := env.PachClient", "hunk": "@@ -5197,11 +5197,11 @@ func TestPFS(suite *testing.T) {\n \t// TODO: This test can be refactored to remove a lot of the boilerplate.\n \tsuite.Run(\"Trigger\", func(t *testing.T) {\n \t\tt.Parallel()\n-\t\tenv := testpachd.NewRealEnv(t, dockertestenv.NewTestDBConfig(t))\n-\t\tc := env.PachClient\n \n \t\tt.Run(\"Simple\", func(t *testing.T) {\n \t\t\tt.Parallel()\n+\t\t\tenv := testpachd.NewRealEnv(t, dockertestenv.NewTestDBConfig(t))\n+\t\t\tc := env.PachClient\n \t\t\trequire.NoError(t, c.CreateRepo(\"test\"))\n \t\t\trequire.NoError(t, c.CreateBranchTrigger(\"test\", \"master\", \"\", \"\", &pfs.Trigger{\n \t\t\t\tBranch: \"staging\",\n", "comment": "Should we set up a test environment per test? I notice these occasionally break. Is that because they share the same test environment?", "ids": [48381, "187aa3a35456005fe8801a7724bec00d3a41e396", "3d4dec20e47392b5298d407cb3499a679d1aac97"], "repo": "pachyderm-pachyderm", "ghid": 6773, "old": " \t// TODO: This test can be refactored to remove a lot of the boilerplate.\n \tsuite.Run(\"Trigger\", func(t *testing.T) {\n \t\tt.Parallel()\n-\t\tenv := testpachd.NewRealEnv(t, dockertestenv.NewTestDBConfig(t))\n-\t\tc := env.PachClient\n \t\tt.Run(\"Simple\", func(t *testing.T) {\n \t\t\tt.Parallel()\n \t\t\trequire.NoError(t, c.CreateRepo(\"test\"))\n \t\t\trequire.NoError(t, c.CreateBranchTrigger(\"test\", \"master\", \"\", \"\", &pfs.Trigger{\n \t\t\t\tBranch: \"staging\",", "new": " \t// TODO: This test can be refactored to remove a lot of the boilerplate.\n \tsuite.Run(\"Trigger\", func(t *testing.T) {\n \t\tt.Parallel()\n \t\tt.Run(\"Simple\", func(t *testing.T) {\n \t\t\tt.Parallel()\n+\t\t\tenv := testpachd.NewRealEnv(t, dockertestenv.NewTestDBConfig(t))\n+\t\t\tc := env.PachClient\n \t\t\trequire.NoError(t, c.CreateRepo(\"test\"))\n \t\t\trequire.NoError(t, c.CreateBranchTrigger(\"test\", \"master\", \"\", \"\", &pfs.Trigger{\n \t\t\t\tBranch: \"staging\",", "lang": "go", "y": 1, "proj": "pachyderm-pachyderm", "patch": "@@ -5201,6 +5201,7 @@ func TestPFS(suite *testing.T) {\n \t\tc := env.PachClient", "msg": "Should we set up a test environment per test? I notice these occasionally break. Is that because they share the same test environment?"}
{"old_hunk": "@@ -1094,11 +1095,12 @@ All jobs created by a pipeline will create commits in the pipeline's output repo\n \t}\n \tcommands = append(commands, cmdutil.CreateAlias(listSecret, \"list secret\"))\n \n+\tvar seed int64\n \trunLoadTest := &cobra.Command{\n-\t\tUse:   \"{{alias}}\",\n+\t\tUse:   \"{{alias}} <spec> \",", "hunk": "@@ -1097,7 +1097,7 @@ All jobs created by a pipeline will create commits in the pipeline's output repo\n \n \tvar seed int64\n \trunLoadTest := &cobra.Command{\n-\t\tUse:   \"{{alias}} <spec> \",\n+\t\tUse:   \"{{alias}} <spec-file> \",\n \t\tShort: \"Run a PPS load test.\",\n \t\tLong:  \"Run a PPS load test.\",\n \t\tRun: cmdutil.RunBoundedArgs(0, 1, func(args []string) (retErr error) {\n", "comment": "maybe include \"path\" or \"file\" to indicate the spec is not literally passed as an argument, and that the command will read the filesystem. There is a way to hide cobra commands with `Hidden`, maybe we should do that for the load testing stuff.", "ids": [48446, "d00c87c8e81679e5788633a5225ab23b0c5ec1ee", "59acf2fa2924d73c9bffdc26cd2861d376194323"], "repo": "pachyderm-pachyderm", "ghid": 6799, "old": " \tvar seed int64\n \trunLoadTest := &cobra.Command{\n-\t\tUse:   \"{{alias}} <spec> \",\n \t\tShort: \"Run a PPS load test.\",\n \t\tLong:  \"Run a PPS load test.\",\n \t\tRun: cmdutil.RunBoundedArgs(0, 1, func(args []string) (retErr error) {", "new": " \tvar seed int64\n \trunLoadTest := &cobra.Command{\n+\t\tUse:   \"{{alias}} <spec-file> \",\n \t\tShort: \"Run a PPS load test.\",\n \t\tLong:  \"Run a PPS load test.\",\n \t\tRun: cmdutil.RunBoundedArgs(0, 1, func(args []string) (retErr error) {", "lang": "go", "y": 1, "proj": "pachyderm-pachyderm", "patch": "@@ -1094,11 +1095,12 @@ All jobs created by a pipeline will create commits in the pipeline's output repo\n \t}\n \tcommands = append(commands, cmdutil.CreateAlias(listSecret, \"list secret\"))\n \n+\tvar seed int64\n \trunLoadTest := &cobra.Command{\n-\t\tUse:   \"{{alias}}\",\n+\t\tUse:   \"{{alias}} <spec> \",", "msg": "maybe include \"path\" or \"file\" to indicate the spec is not literally passed as an argument, and that the command will read the filesystem. There is a way to hide cobra commands with `Hidden`, maybe we should do that for the load testing stuff."}
{"old_hunk": "@@ -297,10 +296,10 @@ func TestTransformPipeline(suite *testing.T) {\n \n \tsuite.Run(\"TestJobSuccessEgressEmpty\", func(t *testing.T) {\n \t\tt.Parallel()\n-\t\t_, bucket := testutil.NewObjectClient(t)\n+\t\tobjC := dockertestenv.NewTestObjClient(t)\n \t\tpi := defaultPipelineInfo()\n \t\tpi.Details.Input.Pfs.Glob = \"/\"\n-\t\tpi.Details.Egress = &pps.Egress{URL: fmt.Sprintf(\"local://%s/\", bucket)}\n+\t\tpi.Details.Egress = &pps.Egress{URL: fmt.Sprintf(\"local://%s/\", objC.BucketURL())}", "hunk": "@@ -299,7 +299,7 @@ func TestTransformPipeline(suite *testing.T) {\n \t\tobjC := dockertestenv.NewTestObjClient(t)\n \t\tpi := defaultPipelineInfo()\n \t\tpi.Details.Input.Pfs.Glob = \"/\"\n-\t\tpi.Details.Egress = &pps.Egress{URL: fmt.Sprintf(\"local://%s/\", objC.BucketURL())}\n+\t\tpi.Details.Egress = &pps.Egress{URL: objC.BucketURL().String()}\n \t\tenv := newWorkerSpawnerPair(t, dockertestenv.NewTestDBConfig(t), pi)\n \n \t\ttestJobSuccess(t, env, pi, nil)\n", "comment": "Wait, is this setting it to `local://minio://<host>/<bucket>`? That can't be right", "ids": [48628, "be4787757770fe2c294cd8a956973dcafc2810de", "4ef907cc525d9ce9b45f11d3da896e874070d63f"], "repo": "pachyderm-pachyderm", "ghid": 6894, "old": " \t\tobjC := dockertestenv.NewTestObjClient(t)\n \t\tpi := defaultPipelineInfo()\n \t\tpi.Details.Input.Pfs.Glob = \"/\"\n-\t\tpi.Details.Egress = &pps.Egress{URL: fmt.Sprintf(\"local://%s/\", objC.BucketURL())}\n \t\tenv := newWorkerSpawnerPair(t, dockertestenv.NewTestDBConfig(t), pi)\n \t\ttestJobSuccess(t, env, pi, nil)", "new": " \t\tobjC := dockertestenv.NewTestObjClient(t)\n \t\tpi := defaultPipelineInfo()\n \t\tpi.Details.Input.Pfs.Glob = \"/\"\n+\t\tpi.Details.Egress = &pps.Egress{URL: objC.BucketURL().String()}\n \t\tenv := newWorkerSpawnerPair(t, dockertestenv.NewTestDBConfig(t), pi)\n \t\ttestJobSuccess(t, env, pi, nil)", "lang": "go", "y": 1, "proj": "pachyderm-pachyderm", "patch": "@@ -297,10 +296,10 @@ func TestTransformPipeline(suite *testing.T) {\n \n \tsuite.Run(\"TestJobSuccessEgressEmpty\", func(t *testing.T) {\n \t\tt.Parallel()\n-\t\t_, bucket := testutil.NewObjectClient(t)\n+\t\tobjC := dockertestenv.NewTestObjClient(t)\n \t\tpi := defaultPipelineInfo()\n \t\tpi.Details.Input.Pfs.Glob = \"/\"\n-\t\tpi.Details.Egress = &pps.Egress{URL: fmt.Sprintf(\"local://%s/\", bucket)}\n+\t\tpi.Details.Egress = &pps.Egress{URL: fmt.Sprintf(\"local://%s/\", objC.BucketURL())}", "msg": "Wait, is this setting it to `local://minio://<host>/<bucket>`? That can't be right"}
{"old_hunk": "@@ -28,4 +28,7 @@ type Client interface {\n \n \t// Exists checks if a given object already exists\n \tExists(ctx context.Context, name string) (bool, error)\n+\n+\t// BucketURL returns the URL of the bucket this client uses.\n+\tBucketURL() string", "hunk": "@@ -30,5 +30,5 @@ type Client interface {\n \tExists(ctx context.Context, name string) (bool, error)\n \n \t// BucketURL returns the URL of the bucket this client uses.\n-\tBucketURL() string\n+\tBucketURL() ObjectStoreURL\n }\n", "comment": "Should this maybe return the `ObjectStoreURL` that we already generate?", "ids": [48629, "be4787757770fe2c294cd8a956973dcafc2810de", "4ef907cc525d9ce9b45f11d3da896e874070d63f"], "repo": "pachyderm-pachyderm", "ghid": 6894, "old": " \tExists(ctx context.Context, name string) (bool, error)\n \t// BucketURL returns the URL of the bucket this client uses.\n-\tBucketURL() string\n }", "new": " \tExists(ctx context.Context, name string) (bool, error)\n \t// BucketURL returns the URL of the bucket this client uses.\n+\tBucketURL() ObjectStoreURL\n }", "lang": "go", "y": 1, "proj": "pachyderm-pachyderm", "patch": "@@ -28,4 +28,7 @@ type Client interface {\n \n \t// Exists checks if a given object already exists\n \tExists(ctx context.Context, name string) (bool, error)\n+\n+\t// BucketURL returns the URL of the bucket this client uses.\n+\tBucketURL() string", "msg": "Should this maybe return the `ObjectStoreURL` that we already generate?"}
{"old_hunk": "@@ -1199,6 +1201,30 @@ $ {{alias}} 'foo@master:/test\\[\\].txt'`,\n \t\t\t}\n \t\t\tdefer c.Close()\n \t\t\tdefer progress.Wait()\n+\t\t\t// TODO: Decide what progress should look like in the recursive case. The files are downloaded in a batch in 2.x.\n+\t\t\tif recursive {\n+\t\t\t\tif outputPath == \"\" {\n+\t\t\t\t\treturn errors.Errorf(\"an output path needs to be specified when using the --recursive flag\")\n+\t\t\t\t}\n+\t\t\t\t// Check that the path matches one directory / file.\n+\t\t\t\t_, err := c.InspectFile(file.Commit, file.Path)\n+\t\t\t\tif err != nil {\n+\t\t\t\t\treturn err\n+\t\t\t\t}\n+\t\t\t\tr, err := c.GetFileTAR(file.Commit, file.Path)\n+\t\t\t\tif err != nil {\n+\t\t\t\t\treturn err\n+\t\t\t\t}\n+\t\t\t\tdir, err := ioutil.TempDir(\"\", \"pachyderm_get_file_recursive\")\n+\t\t\t\tif err != nil {\n+\t\t\t\t\treturn err\n+\t\t\t\t}\n+\t\t\t\tdefer os.RemoveAll(dir)\n+\t\t\t\tif err := tarutil.Import(dir, r); err != nil {\n+\t\t\t\t\treturn err\n+\t\t\t\t}\n+\t\t\t\treturn os.Rename(path.Join(dir, file.Path), outputPath)", "hunk": "@@ -1207,7 +1207,7 @@ $ {{alias}} 'foo@master:/test\\[\\].txt'`,\n \t\t\t\t\treturn errors.Errorf(\"an output path needs to be specified when using the --recursive flag\")\n \t\t\t\t}\n \t\t\t\t// Check that the path matches one directory / file.\n-\t\t\t\t_, err := c.InspectFile(file.Commit, file.Path)\n+\t\t\t\tfi, err := c.InspectFile(file.Commit, file.Path)\n \t\t\t\tif err != nil {\n \t\t\t\t\treturn err\n \t\t\t\t}\n", "comment": "Do we really just clobber everything in the entire directory? Overwriting the files we are exporting is expected, but a hard replace of the directory node itself is going to surprise someone and cause data loss.", "ids": [48674, "4bcb79220cb81432241a8906993ec42806bb40c7", "b72ce398f71d6a51406016eee41cc2520f47739a"], "repo": "pachyderm-pachyderm", "ghid": 6910, "old": " \t\t\t\t\treturn errors.Errorf(\"an output path needs to be specified when using the --recursive flag\")\n \t\t\t\t}\n \t\t\t\t// Check that the path matches one directory / file.\n-\t\t\t\t_, err := c.InspectFile(file.Commit, file.Path)\n \t\t\t\tif err != nil {\n \t\t\t\t\treturn err\n \t\t\t\t}", "new": " \t\t\t\t\treturn errors.Errorf(\"an output path needs to be specified when using the --recursive flag\")\n \t\t\t\t}\n \t\t\t\t// Check that the path matches one directory / file.\n+\t\t\t\tfi, err := c.InspectFile(file.Commit, file.Path)\n \t\t\t\tif err != nil {\n \t\t\t\t\treturn err\n \t\t\t\t}", "lang": "go", "y": 1, "proj": "pachyderm-pachyderm", "patch": "@@ -1199,6 +1201,30 @@ $ {{alias}} 'foo@master:/test\\[\\].txt'`,\n \t\t\t}\n \t\t\tdefer c.Close()\n \t\t\tdefer progress.Wait()\n+\t\t\t// TODO: Decide what progress should look like in the recursive case. The files are downloaded in a batch in 2.x.\n+\t\t\tif recursive {\n+\t\t\t\tif outputPath == \"\" {\n+\t\t\t\t\treturn errors.Errorf(\"an output path needs to be specified when using the --recursive flag\")\n+\t\t\t\t}\n+\t\t\t\t// Check that the path matches one directory / file.\n+\t\t\t\t_, err := c.InspectFile(file.Commit, file.Path)\n+\t\t\t\tif err != nil {\n+\t\t\t\t\treturn err\n+\t\t\t\t}\n+\t\t\t\tr, err := c.GetFileTAR(file.Commit, file.Path)\n+\t\t\t\tif err != nil {\n+\t\t\t\t\treturn err\n+\t\t\t\t}\n+\t\t\t\tdir, err := ioutil.TempDir(\"\", \"pachyderm_get_file_recursive\")\n+\t\t\t\tif err != nil {\n+\t\t\t\t\treturn err\n+\t\t\t\t}\n+\t\t\t\tdefer os.RemoveAll(dir)\n+\t\t\t\tif err := tarutil.Import(dir, r); err != nil {\n+\t\t\t\t\treturn err\n+\t\t\t\t}\n+\t\t\t\treturn os.Rename(path.Join(dir, file.Path), outputPath)", "msg": "Do we really just clobber everything in the entire directory? Overwriting the files we are exporting is expected, but a hard replace of the directory node itself is going to surprise someone and cause data loss."}
{"old_hunk": "@@ -161,6 +162,11 @@ func (s *Set) WithDatum(meta *Meta, cb func(*Datum) error, opts ...Option) error\n \t\t\t\tif retErr == nil || i == d.numRetries {\n \t\t\t\t\tretErr = d.finish(retErr)\n \t\t\t\t}\n+\t\t\t\tduration := time.Duration(int64(d.meta.Stats.ProcessTime.GetNanos()) + d.meta.Stats.ProcessTime.GetSeconds()*1000000000)", "hunk": "@@ -162,7 +162,7 @@ func (s *Set) WithDatum(meta *Meta, cb func(*Datum) error, opts ...Option) error\n \t\t\t\tif retErr == nil || i == d.numRetries {\n \t\t\t\t\tretErr = d.finish(retErr)\n \t\t\t\t}\n-\t\t\t\tduration := time.Duration(int64(d.meta.Stats.ProcessTime.GetNanos()) + d.meta.Stats.ProcessTime.GetSeconds()*1000000000)\n+\t\t\t\tduration := time.Duration(d.meta.Stats.ProcessTime.GetNanos()) + time.Duration(d.meta.Stats.ProcessTime.GetSeconds())*time.Second\n \t\t\t\tlabels := workerStats.DatumLabels(d.meta.Job, d.meta.State.String())\n \t\t\t\tworkerStats.DatumProcTime.With(labels).Observe(duration.Seconds())\n \t\t\t\tworkerStats.DatumProcSecondsCount.With(labels).Add(duration.Seconds())\n", "comment": "maybe use `time.Second` to avoid the zeros ```suggestion duration := time.Duration(d.meta.Stats.ProcessTime.GetNanos()) + time.Duration(d.meta.Stats.ProcessTime.GetSeconds())*time.Second ```", "ids": [48681, "300dc0ee4a8bb1841bc861b666dfaa75c0e3dca9", "dec1375237bbffddf23d4cd398571f288cb952c7"], "repo": "pachyderm-pachyderm", "ghid": 6914, "old": " \t\t\t\tif retErr == nil || i == d.numRetries {\n \t\t\t\t\tretErr = d.finish(retErr)\n \t\t\t\t}\n-\t\t\t\tduration := time.Duration(int64(d.meta.Stats.ProcessTime.GetNanos()) + d.meta.Stats.ProcessTime.GetSeconds()*1000000000)\n \t\t\t\tlabels := workerStats.DatumLabels(d.meta.Job, d.meta.State.String())\n \t\t\t\tworkerStats.DatumProcTime.With(labels).Observe(duration.Seconds())\n \t\t\t\tworkerStats.DatumProcSecondsCount.With(labels).Add(duration.Seconds())", "new": " \t\t\t\tif retErr == nil || i == d.numRetries {\n \t\t\t\t\tretErr = d.finish(retErr)\n \t\t\t\t}\n+\t\t\t\tduration := time.Duration(d.meta.Stats.ProcessTime.GetNanos()) + time.Duration(d.meta.Stats.ProcessTime.GetSeconds())*time.Second\n \t\t\t\tlabels := workerStats.DatumLabels(d.meta.Job, d.meta.State.String())\n \t\t\t\tworkerStats.DatumProcTime.With(labels).Observe(duration.Seconds())\n \t\t\t\tworkerStats.DatumProcSecondsCount.With(labels).Add(duration.Seconds())", "lang": "go", "y": 1, "proj": "pachyderm-pachyderm", "patch": "@@ -161,6 +162,11 @@ func (s *Set) WithDatum(meta *Meta, cb func(*Datum) error, opts ...Option) error\n \t\t\t\tif retErr == nil || i == d.numRetries {\n \t\t\t\t\tretErr = d.finish(retErr)\n \t\t\t\t}\n+\t\t\t\tduration := time.Duration(int64(d.meta.Stats.ProcessTime.GetNanos()) + d.meta.Stats.ProcessTime.GetSeconds()*1000000000)", "msg": "maybe use `time.Second` to avoid the zeros ```suggestion duration := time.Duration(d.meta.Stats.ProcessTime.GetNanos()) + time.Duration(d.meta.Stats.ProcessTime.GetSeconds())*time.Second ```"}
{"old_hunk": "@@ -0,0 +1,104 @@\n+package pachsql\n+\n+import (\n+\t\"net\"\n+\t\"net/url\"\n+\t\"strconv\"\n+\t\"strings\"\n+\n+\t\"github.com/go-sql-driver/mysql\"\n+\t\"github.com/jmoiron/sqlx\"\n+\t\"github.com/pachyderm/pachyderm/v2/src/internal/errors\"\n+)\n+\n+// URL contains the information needed to connect to a SQL database, except for the password.\n+type URL struct {\n+\tProtocol string\n+\tUser     string\n+\tHost     string\n+\tPort     uint16\n+\tDatabase string\n+\tParams   map[string]string\n+}\n+\n+// ParseURL attempts to parse x into a URL\n+func ParseURL(x string) (*URL, error) {\n+\tu, err := url.Parse(x)\n+\tif err != nil {\n+\t\treturn nil, err\n+\t}\n+\tport, err := strconv.Atoi(u.Port())\n+\tif err != nil {\n+\t\treturn nil, err\n+\t}\n+\tparams := make(map[string]string)\n+\tfor k, v := range u.Query() {\n+\t\tif len(v) > 0 {\n+\t\t\tparams[k] = v[len(v)-1]", "hunk": "@@ -1,14 +1,9 @@\n package pachsql\n \n import (\n-\t\"net\"\n \t\"net/url\"\n \t\"strconv\"\n \t\"strings\"\n-\n-\t\"github.com/go-sql-driver/mysql\"\n-\t\"github.com/jmoiron/sqlx\"\n-\t\"github.com/pachyderm/pachyderm/v2/src/internal/errors\"\n )\n \n // URL contains the information needed to connect to a SQL database, except for the password.\n", "comment": "do we already know what param keys may have multiple values?", "ids": [48866, "3c96bc0cb3823022fc779143d363b96b9d720a7c", "d3576f37f23fb4c9836499c0589ef16410f73984"], "repo": "pachyderm-pachyderm", "ghid": 7032, "old": " package pachsql\n import (\n-\t\"net\"\n \t\"net/url\"\n \t\"strconv\"\n \t\"strings\"\n-\n-\t\"github.com/go-sql-driver/mysql\"\n-\t\"github.com/jmoiron/sqlx\"\n-\t\"github.com/pachyderm/pachyderm/v2/src/internal/errors\"\n )\n // URL contains the information needed to connect to a SQL database, except for the password.", "new": " package pachsql\n import (\n \t\"net/url\"\n \t\"strconv\"\n \t\"strings\"\n )\n // URL contains the information needed to connect to a SQL database, except for the password.", "lang": "go", "y": 1, "proj": "pachyderm-pachyderm", "patch": "@@ -0,0 +1,104 @@\n+package pachsql\n+\n+import (\n+\t\"net\"\n+\t\"net/url\"\n+\t\"strconv\"\n+\t\"strings\"\n+\n+\t\"github.com/go-sql-driver/mysql\"\n+\t\"github.com/jmoiron/sqlx\"\n+\t\"github.com/pachyderm/pachyderm/v2/src/internal/errors\"\n+)\n+\n+// URL contains the information needed to connect to a SQL database, except for the password.\n+type URL struct {\n+\tProtocol string\n+\tUser     string\n+\tHost     string\n+\tPort     uint16\n+\tDatabase string\n+\tParams   map[string]string\n+}\n+\n+// ParseURL attempts to parse x into a URL\n+func ParseURL(x string) (*URL, error) {\n+\tu, err := url.Parse(x)\n+\tif err != nil {\n+\t\treturn nil, err\n+\t}\n+\tport, err := strconv.Atoi(u.Port())\n+\tif err != nil {\n+\t\treturn nil, err\n+\t}\n+\tparams := make(map[string]string)\n+\tfor k, v := range u.Query() {\n+\t\tif len(v) > 0 {\n+\t\t\tparams[k] = v[len(v)-1]", "msg": "do we already know what param keys may have multiple values?"}
{"old_hunk": "@@ -0,0 +1,61 @@\n+package pachsql\n+\n+import (\n+\t\"net\"\n+\t\"strconv\"\n+\t\"strings\"\n+\n+\t\"github.com/go-sql-driver/mysql\"\n+\t\"github.com/jmoiron/sqlx\"\n+\t\"github.com/pachyderm/pachyderm/v2/src/internal/errors\"\n+)\n+\n+// DB is an alias for sqlx.DB which is the standard database type used throughout the project\n+type DB = sqlx.DB\n+\n+// OpenURL returns a database connection pool to the database specified by u\n+// If password != \"\" then it will be used for authentication.\n+// This function does not confirm that the database is reachable; callers may be interested in sqlx.DB.Ping()\n+func OpenURL(u URL, password string) (*DB, error) {\n+\tvar driver string\n+\tvar dsn string\n+\tswitch u.Protocol {\n+\tcase \"postgresql\", \"postgres\":\n+\t\tdriver = \"pgx\"\n+\t\tdsn = postgresDSN(u, password)\n+\tcase \"mysql\":\n+\t\tdriver = \"mysql\"\n+\t\tdsn = mySQLDSN(u, password)\n+\tdefault:\n+\t\treturn nil, errors.Errorf(\"database protocol %q not supported\", u.Protocol)\n+\t}\n+\treturn sqlx.Open(driver, dsn)\n+}\n+\n+func postgresDSN(u URL, password string) string {", "hunk": "@@ -42,6 +42,9 @@ func postgresDSN(u URL, password string) string {\n \tif password != \"\" {\n \t\tfields[\"password\"] = password\n \t}\n+\tfor k, v := range u.Params {\n+\t\tfields[k] = v\n+\t}\n \tvar dsnParts []string\n \tfor k, v := range fields {\n \t\tdsnParts = append(dsnParts, k+\"=\"+v)\n", "comment": "Should the URL params also be passed to the PG DSN?", "ids": [48868, "ef0239d9aac0d2e433fd24d842ea7bb6a8d2ae1d", "d3576f37f23fb4c9836499c0589ef16410f73984"], "repo": "pachyderm-pachyderm", "ghid": 7032, "old": " \tif password != \"\" {\n \t\tfields[\"password\"] = password\n \t}\n \tvar dsnParts []string\n \tfor k, v := range fields {\n \t\tdsnParts = append(dsnParts, k+\"=\"+v)", "new": " \tif password != \"\" {\n \t\tfields[\"password\"] = password\n \t}\n+\tfor k, v := range u.Params {\n+\t\tfields[k] = v\n+\t}\n \tvar dsnParts []string\n \tfor k, v := range fields {\n \t\tdsnParts = append(dsnParts, k+\"=\"+v)", "lang": "go", "y": 1, "proj": "pachyderm-pachyderm", "patch": "@@ -0,0 +1,61 @@\n+package pachsql\n+\n+import (\n+\t\"net\"\n+\t\"strconv\"\n+\t\"strings\"\n+\n+\t\"github.com/go-sql-driver/mysql\"\n+\t\"github.com/jmoiron/sqlx\"\n+\t\"github.com/pachyderm/pachyderm/v2/src/internal/errors\"\n+)\n+\n+// DB is an alias for sqlx.DB which is the standard database type used throughout the project\n+type DB = sqlx.DB\n+\n+// OpenURL returns a database connection pool to the database specified by u\n+// If password != \"\" then it will be used for authentication.\n+// This function does not confirm that the database is reachable; callers may be interested in sqlx.DB.Ping()\n+func OpenURL(u URL, password string) (*DB, error) {\n+\tvar driver string\n+\tvar dsn string\n+\tswitch u.Protocol {\n+\tcase \"postgresql\", \"postgres\":\n+\t\tdriver = \"pgx\"\n+\t\tdsn = postgresDSN(u, password)\n+\tcase \"mysql\":\n+\t\tdriver = \"mysql\"\n+\t\tdsn = mySQLDSN(u, password)\n+\tdefault:\n+\t\treturn nil, errors.Errorf(\"database protocol %q not supported\", u.Protocol)\n+\t}\n+\treturn sqlx.Open(driver, dsn)\n+}\n+\n+func postgresDSN(u URL, password string) string {", "msg": "Should the URL params also be passed to the PG DSN?"}
{"old_hunk": "@@ -58,11 +58,42 @@ func (d *driver) master(ctx context.Context) {\n }\n \n func (d *driver) finishCommits(ctx context.Context) error {\n-\tcompactor, err := newCompactor(d.env.BackgroundContext, d.storage, d.etcdClient, d.prefix, d.env.StorageConfig.StorageCompactionMaxFanIn)\n+\trepos := make(map[string]context.CancelFunc)", "hunk": "@@ -59,6 +59,11 @@ func (d *driver) master(ctx context.Context) {\n \n func (d *driver) finishCommits(ctx context.Context) error {\n \trepos := make(map[string]context.CancelFunc)\n+\tdefer func() {\n+\t\tfor _, cancel := range repos {\n+\t\t\tcancel()\n+\t\t}\n+\t}()\n \tcompactor, err := newCompactor(ctx, d.storage, d.etcdClient, d.prefix, d.env.StorageConfig.StorageCompactionMaxFanIn)\n \tif err != nil {\n \t\treturn err\n", "comment": "Is this missing a deferred for loop to cancel all of these?", "ids": [48915, "d681bc907623629ad3f3bc64c451bb2ddcb767c4", "47c410a83bf7081e72a0e1283d5772b8fb2479a5"], "repo": "pachyderm-pachyderm", "ghid": 7047, "old": " func (d *driver) finishCommits(ctx context.Context) error {\n \trepos := make(map[string]context.CancelFunc)\n \tcompactor, err := newCompactor(ctx, d.storage, d.etcdClient, d.prefix, d.env.StorageConfig.StorageCompactionMaxFanIn)\n \tif err != nil {\n \t\treturn err", "new": " func (d *driver) finishCommits(ctx context.Context) error {\n \trepos := make(map[string]context.CancelFunc)\n+\tdefer func() {\n+\t\tfor _, cancel := range repos {\n+\t\t\tcancel()\n+\t\t}\n+\t}()\n \tcompactor, err := newCompactor(ctx, d.storage, d.etcdClient, d.prefix, d.env.StorageConfig.StorageCompactionMaxFanIn)\n \tif err != nil {\n \t\treturn err", "lang": "go", "y": 1, "proj": "pachyderm-pachyderm", "patch": "@@ -58,11 +58,42 @@ func (d *driver) master(ctx context.Context) {\n }\n \n func (d *driver) finishCommits(ctx context.Context) error {\n-\tcompactor, err := newCompactor(d.env.BackgroundContext, d.storage, d.etcdClient, d.prefix, d.env.StorageConfig.StorageCompactionMaxFanIn)\n+\trepos := make(map[string]context.CancelFunc)", "msg": "Is this missing a deferred for loop to cancel all of these?"}
{"old_hunk": "@@ -242,6 +242,11 @@ func (s *debugServer) handleWorkerRedirect(tw *tar.Writer, pod *v1.Pod, collectW\n \tif err != nil {\n \t\treturn err\n \t}\n+\tdefer func() {\n+\t\tif err := c.Close(); retErr == nil {", "hunk": "@@ -243,8 +243,8 @@ func (s *debugServer) handleWorkerRedirect(tw *tar.Writer, pod *v1.Pod, collectW\n \t\treturn err\n \t}\n \tdefer func() {\n-\t\tif err := c.Close(); retErr == nil {\n-\t\t\tretErr = err\n+\t\tif err := c.Close(); err != nil {\n+\t\t\tlog.Errorf(\"errored closing worker client: %v\", err)\n \t\t}\n \t}()\n \tr, err := cb(c.DebugClient, &debug.Filter{\n", "comment": "I don't think it makes sense to return this error. Just logging it would be fine.", "ids": [48911, "d681bc907623629ad3f3bc64c451bb2ddcb767c4", "47c410a83bf7081e72a0e1283d5772b8fb2479a5"], "repo": "pachyderm-pachyderm", "ghid": 7047, "old": " \t\treturn err\n \t}\n \tdefer func() {\n-\t\tif err := c.Close(); retErr == nil {\n-\t\t\tretErr = err\n \t\t}\n \t}()\n \tr, err := cb(c.DebugClient, &debug.Filter{", "new": " \t\treturn err\n \t}\n \tdefer func() {\n+\t\tif err := c.Close(); err != nil {\n+\t\t\tlog.Errorf(\"errored closing worker client: %v\", err)\n \t\t}\n \t}()\n \tr, err := cb(c.DebugClient, &debug.Filter{", "lang": "go", "y": 1, "proj": "pachyderm-pachyderm", "patch": "@@ -242,6 +242,11 @@ func (s *debugServer) handleWorkerRedirect(tw *tar.Writer, pod *v1.Pod, collectW\n \tif err != nil {\n \t\treturn err\n \t}\n+\tdefer func() {\n+\t\tif err := c.Close(); retErr == nil {", "msg": "I don't think it makes sense to return this error. Just logging it would be fine."}
{"old_hunk": "@@ -76,7 +76,9 @@ If the job fails, the output commit will not be populated with data.`,\n \t\tLong:  \"Return info about a job.\",\n \t\tRun: cmdutil.RunFixedArgs(1, func(args []string) error {\n \t\t\tjob, err := cmdutil.ParseJob(args[0])\n-\t\t\tif err != nil {\n+\t\t\tif err != nil && uuid.IsUUIDWithoutDashes(args[0]) {\n+\t\t\t\treturn errors.New(`Use \"list job <id>\" to see jobs with a given ID across different pipelines `)", "hunk": "@@ -77,7 +77,7 @@ If the job fails, the output commit will not be populated with data.`,\n \t\tRun: cmdutil.RunFixedArgs(1, func(args []string) error {\n \t\t\tjob, err := cmdutil.ParseJob(args[0])\n \t\t\tif err != nil && uuid.IsUUIDWithoutDashes(args[0]) {\n-\t\t\t\treturn errors.New(`Use \"list job <id>\" to see jobs with a given ID across different pipelines `)\n+\t\t\t\treturn errors.New(`Use \"list job <id>\" to see jobs with a given ID across different pipelines`)\n \t\t\t} else {\n \t\t\t\treturn err\n \t\t\t}\n", "comment": "```suggestion return errors.New(`Use \"list job <id>\" to see jobs with a given ID across different pipelines`) ```", "ids": [48991, "004d93410d40c9a1d01eea44a875d4f4595192a6", "0421da8c008e98c8eafd663f0c51d4e25487d10c"], "repo": "pachyderm-pachyderm", "ghid": 7085, "old": " \t\tRun: cmdutil.RunFixedArgs(1, func(args []string) error {\n \t\t\tjob, err := cmdutil.ParseJob(args[0])\n \t\t\tif err != nil && uuid.IsUUIDWithoutDashes(args[0]) {\n-\t\t\t\treturn errors.New(`Use \"list job <id>\" to see jobs with a given ID across different pipelines `)\n \t\t\t} else {\n \t\t\t\treturn err\n \t\t\t}", "new": " \t\tRun: cmdutil.RunFixedArgs(1, func(args []string) error {\n \t\t\tjob, err := cmdutil.ParseJob(args[0])\n \t\t\tif err != nil && uuid.IsUUIDWithoutDashes(args[0]) {\n+\t\t\t\treturn errors.New(`Use \"list job <id>\" to see jobs with a given ID across different pipelines`)\n \t\t\t} else {\n \t\t\t\treturn err\n \t\t\t}", "lang": "go", "y": 1, "proj": "pachyderm-pachyderm", "patch": "@@ -76,7 +76,9 @@ If the job fails, the output commit will not be populated with data.`,\n \t\tLong:  \"Return info about a job.\",\n \t\tRun: cmdutil.RunFixedArgs(1, func(args []string) error {\n \t\t\tjob, err := cmdutil.ParseJob(args[0])\n-\t\t\tif err != nil {\n+\t\t\tif err != nil && uuid.IsUUIDWithoutDashes(args[0]) {\n+\t\t\t\treturn errors.New(`Use \"list job <id>\" to see jobs with a given ID across different pipelines `)", "msg": "```suggestion return errors.New(`Use \"list job <id>\" to see jobs with a given ID across different pipelines`) ```"}
{"old_hunk": "@@ -366,7 +366,9 @@ $ {{alias}} test@fork -p XXX`,\n \t\tLong:  \"Return info about a commit.\",\n \t\tRun: cmdutil.RunFixedArgs(1, func(args []string) error {\n \t\t\tcommit, err := cmdutil.ParseCommit(args[0])\n-\t\t\tif err != nil {\n+\t\t\tif err != nil && uuid.IsUUIDWithoutDashes(args[0]) {\n+\t\t\t\treturn errors.New(`Use \"list commit <id>\" to see commits with a given ID across different repos `)", "hunk": "@@ -367,7 +367,7 @@ $ {{alias}} test@fork -p XXX`,\n \t\tRun: cmdutil.RunFixedArgs(1, func(args []string) error {\n \t\t\tcommit, err := cmdutil.ParseCommit(args[0])\n \t\t\tif err != nil && uuid.IsUUIDWithoutDashes(args[0]) {\n-\t\t\t\treturn errors.New(`Use \"list commit <id>\" to see commits with a given ID across different repos `)\n+\t\t\t\treturn errors.New(`Use \"list commit <id>\" to see commits with a given ID across different repos`)\n \t\t\t} else {\n \t\t\t\treturn err\n \t\t\t}\n", "comment": "nit: ```suggestion return errors.New(`Use \"list commit <id>\" to see commits with a given ID across different repos`) ```", "ids": [48990, "004d93410d40c9a1d01eea44a875d4f4595192a6", "fbbaf46649db3d0f1ffec64d993406ed5ff24424"], "repo": "pachyderm-pachyderm", "ghid": 7085, "old": " \t\tRun: cmdutil.RunFixedArgs(1, func(args []string) error {\n \t\t\tcommit, err := cmdutil.ParseCommit(args[0])\n \t\t\tif err != nil && uuid.IsUUIDWithoutDashes(args[0]) {\n-\t\t\t\treturn errors.New(`Use \"list commit <id>\" to see commits with a given ID across different repos `)\n \t\t\t} else {\n \t\t\t\treturn err\n \t\t\t}", "new": " \t\tRun: cmdutil.RunFixedArgs(1, func(args []string) error {\n \t\t\tcommit, err := cmdutil.ParseCommit(args[0])\n \t\t\tif err != nil && uuid.IsUUIDWithoutDashes(args[0]) {\n+\t\t\t\treturn errors.New(`Use \"list commit <id>\" to see commits with a given ID across different repos`)\n \t\t\t} else {\n \t\t\t\treturn err\n \t\t\t}", "lang": "go", "y": 1, "proj": "pachyderm-pachyderm", "patch": "@@ -366,7 +366,9 @@ $ {{alias}} test@fork -p XXX`,\n \t\tLong:  \"Return info about a commit.\",\n \t\tRun: cmdutil.RunFixedArgs(1, func(args []string) error {\n \t\t\tcommit, err := cmdutil.ParseCommit(args[0])\n-\t\t\tif err != nil {\n+\t\t\tif err != nil && uuid.IsUUIDWithoutDashes(args[0]) {\n+\t\t\t\treturn errors.New(`Use \"list commit <id>\" to see commits with a given ID across different repos `)", "msg": "nit: ```suggestion return errors.New(`Use \"list commit <id>\" to see commits with a given ID across different repos`) ```"}
{"old_hunk": "@@ -301,20 +326,63 @@ func (c *postgresCollection) list(\n \t\tquery += \" where \" + strings.Join(fields, \" and \")\n \t}\n \n+\tafterCondition := func(t SortTarget, ord SortOrder) (string, error) {\n+\t\tts, err := targetToSQL(t)\n+\t\tif err != nil {\n+\t\t\treturn \"\", err\n+\t\t}\n+\t\targs = append(args, afterRow)\n+\t\tif len(withFields) > 0 {\n+\t\t\treturn fmt.Sprintf(\" %s %s $%d\", ts, sortOrderOperator(ord), len(args)+1), nil\n+\t\t} else {\n+\t\t\treturn fmt.Sprintf(\" where key %s %s $%d\", ts, sortOrderOperator(ord), len(args)+1), nil\n+\t\t}\n+\t}\n+\n \tif opts.Order != SortNone {\n \t\tif order, err := orderToSQL(opts.Order); err != nil {\n-\t\t\treturn err\n+\t\t\treturn \"\", nil, err\n \t\t} else if target, err := targetToSQL(opts.Target); err != nil {\n-\t\t\treturn err\n+\t\t\treturn \"\", nil, err\n \t\t} else {\n+\t\t\tif afterRow != nil {\n+\t\t\t\tac, err := afterCondition(opts.Target, opts.Order)\n+\t\t\t\tif err != nil {\n+\t\t\t\t\treturn \"\", nil, err\n+\t\t\t\t}\n+\t\t\t\tquery += ac\n+\t\t\t}\n \t\t\tquery += fmt.Sprintf(\" order by %s %s\", target, order)\n+\n+\t\t}\n+\t} else {\n+\t\tif afterRow != nil {\n+\t\t\tac, err := afterCondition(SortByKey, SortAscend)\n+\t\t\tif err != nil {\n+\t\t\t\treturn \"\", nil, err\n+\t\t\t}\n+\t\t\tquery += ac\n \t\t}\n \t}\n \n \tif opts.Limit > 0 {\n \t\tquery += fmt.Sprintf(\" limit %d\", opts.Limit)\n \t}\n+\treturn query, args, nil\n+}\n \n+func (c *postgresCollection) list(", "hunk": "@@ -333,9 +333,9 @@ func (c *postgresCollection) listQueryStr(ctx context.Context, withFields map[st\n \t\t}\n \t\targs = append(args, afterRow)\n \t\tif len(withFields) > 0 {\n-\t\t\treturn fmt.Sprintf(\" %s %s $%d\", ts, sortOrderOperator(ord), len(args)+1), nil\n+\t\t\treturn fmt.Sprintf(\" and %s %s $%d\", ts, sortOrderOperator(ord), len(args)), nil\n \t\t} else {\n-\t\t\treturn fmt.Sprintf(\" where key %s %s $%d\", ts, sortOrderOperator(ord), len(args)+1), nil\n+\t\t\treturn fmt.Sprintf(\" where %s %s $%d\", ts, sortOrderOperator(ord), len(args)), nil\n \t\t}\n \t}\n \n", "comment": "This will not return a consistent view of the database if we have to do multiple queries.", "ids": [49158, "55aa02144211d6690f13dce08de6d8b2ad17a6d4", "031f528ddc515a85339eb1fdcc8fcd084ed7fe05"], "repo": "pachyderm-pachyderm", "ghid": 7152, "old": " \t\t}\n \t\targs = append(args, afterRow)\n \t\tif len(withFields) > 0 {\n-\t\t\treturn fmt.Sprintf(\" %s %s $%d\", ts, sortOrderOperator(ord), len(args)+1), nil\n \t\t} else {\n-\t\t\treturn fmt.Sprintf(\" where key %s %s $%d\", ts, sortOrderOperator(ord), len(args)+1), nil\n \t\t}\n \t}", "new": " \t\t}\n \t\targs = append(args, afterRow)\n \t\tif len(withFields) > 0 {\n+\t\t\treturn fmt.Sprintf(\" and %s %s $%d\", ts, sortOrderOperator(ord), len(args)), nil\n \t\t} else {\n+\t\t\treturn fmt.Sprintf(\" where %s %s $%d\", ts, sortOrderOperator(ord), len(args)), nil\n \t\t}\n \t}", "lang": "go", "y": 1, "proj": "pachyderm-pachyderm", "patch": "@@ -301,20 +326,63 @@ func (c *postgresCollection) list(\n \t\tquery += \" where \" + strings.Join(fields, \" and \")\n \t}\n \n+\tafterCondition := func(t SortTarget, ord SortOrder) (string, error) {\n+\t\tts, err := targetToSQL(t)\n+\t\tif err != nil {\n+\t\t\treturn \"\", err\n+\t\t}\n+\t\targs = append(args, afterRow)\n+\t\tif len(withFields) > 0 {\n+\t\t\treturn fmt.Sprintf(\" %s %s $%d\", ts, sortOrderOperator(ord), len(args)+1), nil\n+\t\t} else {\n+\t\t\treturn fmt.Sprintf(\" where key %s %s $%d\", ts, sortOrderOperator(ord), len(args)+1), nil\n+\t\t}\n+\t}\n+\n \tif opts.Order != SortNone {\n \t\tif order, err := orderToSQL(opts.Order); err != nil {\n-\t\t\treturn err\n+\t\t\treturn \"\", nil, err\n \t\t} else if target, err := targetToSQL(opts.Target); err != nil {\n-\t\t\treturn err\n+\t\t\treturn \"\", nil, err\n \t\t} else {\n+\t\t\tif afterRow != nil {\n+\t\t\t\tac, err := afterCondition(opts.Target, opts.Order)\n+\t\t\t\tif err != nil {\n+\t\t\t\t\treturn \"\", nil, err\n+\t\t\t\t}\n+\t\t\t\tquery += ac\n+\t\t\t}\n \t\t\tquery += fmt.Sprintf(\" order by %s %s\", target, order)\n+\n+\t\t}\n+\t} else {\n+\t\tif afterRow != nil {\n+\t\t\tac, err := afterCondition(SortByKey, SortAscend)\n+\t\t\tif err != nil {\n+\t\t\t\treturn \"\", nil, err\n+\t\t\t}\n+\t\t\tquery += ac\n \t\t}\n \t}\n \n \tif opts.Limit > 0 {\n \t\tquery += fmt.Sprintf(\" limit %d\", opts.Limit)\n \t}\n+\treturn query, args, nil\n+}\n \n+func (c *postgresCollection) list(", "msg": "This will not return a consistent view of the database if we have to do multiple queries."}
{"old_hunk": "@@ -65,7 +65,7 @@ func PrintJobInfo(w io.Writer, jobInfo *ppsclient.JobInfo, fullTimestamps bool)\n \tfmt.Fprintf(w, \"%s\\t\", Progress(jobInfo))\n \tfmt.Fprintf(w, \"%s\\t\", pretty.Size(jobInfo.Stats.DownloadBytes))\n \tfmt.Fprintf(w, \"%s\\t\", pretty.Size(jobInfo.Stats.UploadBytes))\n-\tif jobInfo.State == ppsclient.JobState_JOB_FAILURE {\n+\tif jobInfo.State == ppsclient.JobState_JOB_FAILURE || jobInfo.State == ppsclient.JobState_JOB_KILLED {", "hunk": "@@ -65,7 +65,7 @@ func PrintJobInfo(w io.Writer, jobInfo *ppsclient.JobInfo, fullTimestamps bool)\n \tfmt.Fprintf(w, \"%s\\t\", Progress(jobInfo))\n \tfmt.Fprintf(w, \"%s\\t\", pretty.Size(jobInfo.Stats.DownloadBytes))\n \tfmt.Fprintf(w, \"%s\\t\", pretty.Size(jobInfo.Stats.UploadBytes))\n-\tif jobInfo.State == ppsclient.JobState_JOB_FAILURE || jobInfo.State == ppsclient.JobState_JOB_KILLED {\n+\tif jobInfo.Reason != \"\" {\n \t\tfmt.Fprintf(w, \"%s: %s\\t\", JobState(jobInfo.State), safeTrim(jobInfo.Reason, jobReasonLen))\n \t} else {\n \t\tfmt.Fprintf(w, \"%s\\t\", JobState(jobInfo.State))\n", "comment": "we could just always print a reason if it's nonempty, doesn't look like we fill it with anything by default", "ids": [49199, "95e6a641cfa16c4ad50d60efa5fce96583f25dcc", "35dad80b6e8b31fefa5e6fd8eff2cb5732547895"], "repo": "pachyderm-pachyderm", "ghid": 7164, "old": " \tfmt.Fprintf(w, \"%s\\t\", Progress(jobInfo))\n \tfmt.Fprintf(w, \"%s\\t\", pretty.Size(jobInfo.Stats.DownloadBytes))\n \tfmt.Fprintf(w, \"%s\\t\", pretty.Size(jobInfo.Stats.UploadBytes))\n-\tif jobInfo.State == ppsclient.JobState_JOB_FAILURE || jobInfo.State == ppsclient.JobState_JOB_KILLED {\n \t\tfmt.Fprintf(w, \"%s: %s\\t\", JobState(jobInfo.State), safeTrim(jobInfo.Reason, jobReasonLen))\n \t} else {\n \t\tfmt.Fprintf(w, \"%s\\t\", JobState(jobInfo.State))", "new": " \tfmt.Fprintf(w, \"%s\\t\", Progress(jobInfo))\n \tfmt.Fprintf(w, \"%s\\t\", pretty.Size(jobInfo.Stats.DownloadBytes))\n \tfmt.Fprintf(w, \"%s\\t\", pretty.Size(jobInfo.Stats.UploadBytes))\n+\tif jobInfo.Reason != \"\" {\n \t\tfmt.Fprintf(w, \"%s: %s\\t\", JobState(jobInfo.State), safeTrim(jobInfo.Reason, jobReasonLen))\n \t} else {\n \t\tfmt.Fprintf(w, \"%s\\t\", JobState(jobInfo.State))", "lang": "go", "y": 1, "proj": "pachyderm-pachyderm", "patch": "@@ -65,7 +65,7 @@ func PrintJobInfo(w io.Writer, jobInfo *ppsclient.JobInfo, fullTimestamps bool)\n \tfmt.Fprintf(w, \"%s\\t\", Progress(jobInfo))\n \tfmt.Fprintf(w, \"%s\\t\", pretty.Size(jobInfo.Stats.DownloadBytes))\n \tfmt.Fprintf(w, \"%s\\t\", pretty.Size(jobInfo.Stats.UploadBytes))\n-\tif jobInfo.State == ppsclient.JobState_JOB_FAILURE {\n+\tif jobInfo.State == ppsclient.JobState_JOB_FAILURE || jobInfo.State == ppsclient.JobState_JOB_KILLED {", "msg": "we could just always print a reason if it's nonempty, doesn't look like we fill it with anything by default"}
{"old_hunk": "@@ -50,14 +50,41 @@ func (c *trackedClient) Create(ctx context.Context, md Metadata, chunkData []byt\n \t\tpanic(\"client must have a renewer to create chunks\")\n \t}\n \tchunkID := Hash(chunkData)\n+\tneedUpload, gen, err := c.beforeUpload(ctx, chunkID, md)\n+\tif err != nil {\n+\t\treturn nil, err\n+\t}\n+\tif err := c.renewer.Add(ctx, chunkID); err != nil {\n+\t\treturn nil, err\n+\t}\n+\tif !*needUpload {\n+\t\treturn chunkID, nil\n+\t}\n+\tkey := chunkKey(chunkID, *gen)\n+\tif err := c.store.Put(ctx, key, chunkData); err != nil {\n+\t\treturn nil, err\n+\t}\n+\tif err := c.afterUpload(ctx, chunkID, *gen); err != nil {\n+\t\treturn nil, err\n+\t}\n+\treturn chunkID, nil\n+}\n+\n+// beforeUpload checks the table in postgres to see if a chunk with chunkID already exists.\n+// if err != nil then needUpload and gen will be nil\n+// if *needUpload is false, then gen will be nil\n+func (c *trackedClient) beforeUpload(ctx context.Context, chunkID ID, md Metadata) (needUpload *bool, gen *uint64, _ error) {\n \tvar pointsTo []string\n \tfor _, cid := range md.PointsTo {\n \t\tpointsTo = append(pointsTo, cid.TrackerID())\n \t}\n \tchunkTID := chunkID.TrackerID()\n-\tvar needUpload bool\n-\tvar gen uint64\n-\tif err := dbutil.WithTx(ctx, c.db, func(tx *pachsql.Tx) error {\n+\tif err := dbutil.WithTx(ctx, c.db, func(tx *pachsql.Tx) (retErr error) {\n+\t\t// This function can be called multiple times.  In order to be sure\n+\t\t// that needsUpload and gen are set every run, we nil them here.\n+\t\t// This also protects this us from dbutil.WithTx dropping an error, if\n+\t\t// we only set these values before a `return nil`\n+\t\tneedUpload, gen = nil, nil", "hunk": "@@ -82,7 +82,7 @@ func (c *trackedClient) beforeUpload(ctx context.Context, chunkID ID, md Metadat\n \tif err := dbutil.WithTx(ctx, c.db, func(tx *pachsql.Tx) (retErr error) {\n \t\t// This function can be called multiple times.  In order to be sure\n \t\t// that needsUpload and gen are set every run, we nil them here.\n-\t\t// This also protects this us from dbutil.WithTx dropping an error, if\n+\t\t// This also protects us from dbutil.WithTx dropping an error, if\n \t\t// we only set these values before a `return nil`\n \t\tneedUpload, gen = nil, nil\n \t\tif err := c.tracker.CreateTx(tx, chunkTID, pointsTo, c.ttl); err != nil {\n", "comment": "I would prefer us to just set the default value for the non-pointer types rather than having to dereference elsewhere. The pattern of returning default values or an error seems fine for non-pointer types and pointer types. Also, `protects this us from` -> `protects us from`. I'm not sure if we should really be coding around bugs in our own code, but ensuring the defaults are reset after a retry makes sense to me.", "ids": [49208, "52f10967d8c03502ebde281e9177c9532204916d", "6b5e6eae62caf27c5a3675bbfe32cda83b1d0c66"], "repo": "pachyderm-pachyderm", "ghid": 7180, "old": " \tif err := dbutil.WithTx(ctx, c.db, func(tx *pachsql.Tx) (retErr error) {\n \t\t// This function can be called multiple times.  In order to be sure\n \t\t// that needsUpload and gen are set every run, we nil them here.\n-\t\t// This also protects this us from dbutil.WithTx dropping an error, if\n \t\t// we only set these values before a `return nil`\n \t\tneedUpload, gen = nil, nil\n \t\tif err := c.tracker.CreateTx(tx, chunkTID, pointsTo, c.ttl); err != nil {", "new": " \tif err := dbutil.WithTx(ctx, c.db, func(tx *pachsql.Tx) (retErr error) {\n \t\t// This function can be called multiple times.  In order to be sure\n \t\t// that needsUpload and gen are set every run, we nil them here.\n+\t\t// This also protects us from dbutil.WithTx dropping an error, if\n \t\t// we only set these values before a `return nil`\n \t\tneedUpload, gen = nil, nil\n \t\tif err := c.tracker.CreateTx(tx, chunkTID, pointsTo, c.ttl); err != nil {", "lang": "go", "y": 1, "proj": "pachyderm-pachyderm", "patch": "@@ -50,14 +50,41 @@ func (c *trackedClient) Create(ctx context.Context, md Metadata, chunkData []byt\n \t\tpanic(\"client must have a renewer to create chunks\")\n \t}\n \tchunkID := Hash(chunkData)\n+\tneedUpload, gen, err := c.beforeUpload(ctx, chunkID, md)\n+\tif err != nil {\n+\t\treturn nil, err\n+\t}\n+\tif err := c.renewer.Add(ctx, chunkID); err != nil {\n+\t\treturn nil, err\n+\t}\n+\tif !*needUpload {\n+\t\treturn chunkID, nil\n+\t}\n+\tkey := chunkKey(chunkID, *gen)\n+\tif err := c.store.Put(ctx, key, chunkData); err != nil {\n+\t\treturn nil, err\n+\t}\n+\tif err := c.afterUpload(ctx, chunkID, *gen); err != nil {\n+\t\treturn nil, err\n+\t}\n+\treturn chunkID, nil\n+}\n+\n+// beforeUpload checks the table in postgres to see if a chunk with chunkID already exists.\n+// if err != nil then needUpload and gen will be nil\n+// if *needUpload is false, then gen will be nil\n+func (c *trackedClient) beforeUpload(ctx context.Context, chunkID ID, md Metadata) (needUpload *bool, gen *uint64, _ error) {\n \tvar pointsTo []string\n \tfor _, cid := range md.PointsTo {\n \t\tpointsTo = append(pointsTo, cid.TrackerID())\n \t}\n \tchunkTID := chunkID.TrackerID()\n-\tvar needUpload bool\n-\tvar gen uint64\n-\tif err := dbutil.WithTx(ctx, c.db, func(tx *pachsql.Tx) error {\n+\tif err := dbutil.WithTx(ctx, c.db, func(tx *pachsql.Tx) (retErr error) {\n+\t\t// This function can be called multiple times.  In order to be sure\n+\t\t// that needsUpload and gen are set every run, we nil them here.\n+\t\t// This also protects this us from dbutil.WithTx dropping an error, if\n+\t\t// we only set these values before a `return nil`\n+\t\tneedUpload, gen = nil, nil", "msg": "I would prefer us to just set the default value for the non-pointer types rather than having to dereference elsewhere. The pattern of returning default values or an error seems fine for non-pointer types and pointer types. Also, `protects this us from` -> `protects us from`. I'm not sure if we should really be coding around bugs in our own code, but ensuring the defaults are reset after a retry makes sense to me."}
{"old_hunk": "@@ -777,6 +781,12 @@ func (api *pfsServerAPI) RunLoadTestDefault(ctx context.Context, req *types.Empt\n \t}\n \treturn nil, errors.Errorf(\"unhandled pachd mock pfs.RunLoadTestDefault\")\n }\n+func (api *pfsServerAPI) CheckStorage(ctx context.Context, req *pfs.CheckStorageRequest) (*pfs.CheckStorageResponse, error) {\n+\tif api.mock.CheckStorage.handler != nil {\n+\t\treturn api.mock.CheckStorage.handler(ctx, req)\n+\t}\n+\treturn nil, errors.Errorf(\"unhandled pachd mock pfs.RunLoadTestDefault\")", "hunk": "@@ -785,7 +785,7 @@ func (api *pfsServerAPI) CheckStorage(ctx context.Context, req *pfs.CheckStorage\n \tif api.mock.CheckStorage.handler != nil {\n \t\treturn api.mock.CheckStorage.handler(ctx, req)\n \t}\n-\treturn nil, errors.Errorf(\"unhandled pachd mock pfs.RunLoadTestDefault\")\n+\treturn nil, errors.Errorf(\"unhandled pachd mock CheckStorage\")\n }\n \n /* PPS Server Mocks */\n", "comment": "I think this error message is copied and pasted from the above handler?", "ids": [49205, "52f10967d8c03502ebde281e9177c9532204916d", "6b5e6eae62caf27c5a3675bbfe32cda83b1d0c66"], "repo": "pachyderm-pachyderm", "ghid": 7180, "old": " \tif api.mock.CheckStorage.handler != nil {\n \t\treturn api.mock.CheckStorage.handler(ctx, req)\n \t}\n-\treturn nil, errors.Errorf(\"unhandled pachd mock pfs.RunLoadTestDefault\")\n }\n /* PPS Server Mocks */", "new": " \tif api.mock.CheckStorage.handler != nil {\n \t\treturn api.mock.CheckStorage.handler(ctx, req)\n \t}\n+\treturn nil, errors.Errorf(\"unhandled pachd mock CheckStorage\")\n }\n /* PPS Server Mocks */", "lang": "go", "y": 1, "proj": "pachyderm-pachyderm", "patch": "@@ -777,6 +781,12 @@ func (api *pfsServerAPI) RunLoadTestDefault(ctx context.Context, req *types.Empt\n \t}\n \treturn nil, errors.Errorf(\"unhandled pachd mock pfs.RunLoadTestDefault\")\n }\n+func (api *pfsServerAPI) CheckStorage(ctx context.Context, req *pfs.CheckStorageRequest) (*pfs.CheckStorageResponse, error) {\n+\tif api.mock.CheckStorage.handler != nil {\n+\t\treturn api.mock.CheckStorage.handler(ctx, req)\n+\t}\n+\treturn nil, errors.Errorf(\"unhandled pachd mock pfs.RunLoadTestDefault\")", "msg": "I think this error message is copied and pasted from the above handler?"}
{"old_hunk": "@@ -132,6 +166,54 @@ func (c *trackedClient) Close() error {\n \treturn nil\n }\n \n+// Check checks that there are entries for the chunk and they have successfully uploaded objects.\n+func (c *trackedClient) Check(ctx context.Context, id ID, readChunk bool) error {\n+\tlast, err := c.CheckEntries(ctx, id, 1, readChunk)\n+\tif err != nil {\n+\t\treturn err\n+\t}\n+\tif last == nil {\n+\t\treturn errors.Errorf(\"no entries for chunk %v\", id)\n+\t}\n+\treturn nil\n+}\n+\n+// CheckEntries runs an integrity check on the objects in object storage.\n+// It lists through chunks with IDs >= first, lexicographically.\n+func (c *trackedClient) CheckEntries(ctx context.Context, first []byte, limit int, readChunks bool) (last ID, _ error) {", "hunk": "@@ -168,7 +168,7 @@ func (c *trackedClient) Close() error {\n \n // Check checks that there are entries for the chunk and they have successfully uploaded objects.\n func (c *trackedClient) Check(ctx context.Context, id ID, readChunk bool) error {\n-\tlast, err := c.CheckEntries(ctx, id, 1, readChunk)\n+\t_, last, err := c.CheckEntries(ctx, id, 1, readChunk)\n \tif err != nil {\n \t\treturn err\n \t}\n", "comment": "Wouldn't this produce an error if a chunk was correctly deleted between the query and the check? It seems like it should requery after an existence error to ensure that it wasn't deleted from the chunk table. Also, why is the iteration exposed through multiple layers like this (the first / last stuff). Is the concern about a long running transaction? If so, I'm of the opinion that it is fine to have a few things in the system that have longer running transactions (such as GC) in the interest of simplicity. If the concern is performance and the results becoming stale, this might be another use case for the task service similar to GC.", "ids": [49210, "52f10967d8c03502ebde281e9177c9532204916d", "6b5e6eae62caf27c5a3675bbfe32cda83b1d0c66"], "repo": "pachyderm-pachyderm", "ghid": 7180, "old": " // Check checks that there are entries for the chunk and they have successfully uploaded objects.\n func (c *trackedClient) Check(ctx context.Context, id ID, readChunk bool) error {\n-\tlast, err := c.CheckEntries(ctx, id, 1, readChunk)\n \tif err != nil {\n \t\treturn err\n \t}", "new": " // Check checks that there are entries for the chunk and they have successfully uploaded objects.\n func (c *trackedClient) Check(ctx context.Context, id ID, readChunk bool) error {\n+\t_, last, err := c.CheckEntries(ctx, id, 1, readChunk)\n \tif err != nil {\n \t\treturn err\n \t}", "lang": "go", "y": 1, "proj": "pachyderm-pachyderm", "patch": "@@ -132,6 +166,54 @@ func (c *trackedClient) Close() error {\n \treturn nil\n }\n \n+// Check checks that there are entries for the chunk and they have successfully uploaded objects.\n+func (c *trackedClient) Check(ctx context.Context, id ID, readChunk bool) error {\n+\tlast, err := c.CheckEntries(ctx, id, 1, readChunk)\n+\tif err != nil {\n+\t\treturn err\n+\t}\n+\tif last == nil {\n+\t\treturn errors.Errorf(\"no entries for chunk %v\", id)\n+\t}\n+\treturn nil\n+}\n+\n+// CheckEntries runs an integrity check on the objects in object storage.\n+// It lists through chunks with IDs >= first, lexicographically.\n+func (c *trackedClient) CheckEntries(ctx context.Context, first []byte, limit int, readChunks bool) (last ID, _ error) {", "msg": "Wouldn't this produce an error if a chunk was correctly deleted between the query and the check? It seems like it should requery after an existence error to ensure that it wasn't deleted from the chunk table. Also, why is the iteration exposed through multiple layers like this (the first / last stuff). Is the concern about a long running transaction? If so, I'm of the opinion that it is fine to have a few things in the system that have longer running transactions (such as GC) in the interest of simplicity. If the concern is performance and the results becoming stale, this might be another use case for the task service similar to GC."}
{"old_hunk": "@@ -519,25 +520,21 @@ func newLoopbackRoot(root, target string, c *client.APIClient, opts *Options) (*\n \treturn n, nil\n }\n \n-func (n *loopbackNode) downloadRepos() (retErr error) {\n-\tif n.getFileState(\"\") != none {\n-\t\treturn nil\n-\t}\n+func (n *loopbackNode) mkdirMountNames() (retErr error) {\n \tdefer func() {\n \t\tif retErr == nil {\n \t\t\tn.setFileState(\"\", meta)\n \t\t}\n \t}()\n-\tris, err := n.c().ListRepo()\n-\tif err != nil {\n-\t\treturn err\n-\t}\n-\tro := n.root().repoOpts\n-\tfor _, ri := range ris {\n-\t\tif len(ro) > 0 && ro[ri.Repo.Name] == nil {\n-\t\t\tcontinue\n-\t\t}\n-\t\tp := n.repoPath(ri)\n+\tros := n.root().repoOpts\n+\t// we only mount explicitly named repos now\n+\tfor name := range ros {\n+\t\tp := n.namePath(name)\n+\t\tfmt.Printf(\"> MKDIR %s, %s\\n\", name, p)", "hunk": "@@ -530,11 +530,6 @@ func (n *loopbackNode) mkdirMountNames() (retErr error) {\n \t// we only mount explicitly named repos now\n \tfor name := range ros {\n \t\tp := n.namePath(name)\n-\t\tfmt.Printf(\"> MKDIR %s, %s\\n\", name, p)\n-\t\tfmt.Printf(\"> repoOpts: %+v\\n\", n.root().repoOpts)\n-\t\tfmt.Printf(\"> branches: %+v\\n\", n.root().branches)\n-\t\tfmt.Printf(\"> commits: %+v\\n\", n.root().commits)\n-\t\tfmt.Printf(\"> files: %+v\\n\", n.root().files)\n \t\tif err := os.MkdirAll(p, 0777); err != nil {\n \t\t\treturn errors.WithStack(err)\n \t\t}\n", "comment": "I'm not sure if this is leftover println debugging or if we want to keep these in, but if it's the latter, it might make sense to use logrus here, and log these at DEBUG? WDYT?", "ids": [49280, "6928ff1eb8b1b71db86a67312b695ae270ff07bd", "766eddd03716b019a5af2ec332a53387ec303c19"], "repo": "pachyderm-pachyderm", "ghid": 7214, "old": " \t// we only mount explicitly named repos now\n \tfor name := range ros {\n \t\tp := n.namePath(name)\n-\t\tfmt.Printf(\"> MKDIR %s, %s\\n\", name, p)\n-\t\tfmt.Printf(\"> repoOpts: %+v\\n\", n.root().repoOpts)\n-\t\tfmt.Printf(\"> branches: %+v\\n\", n.root().branches)\n-\t\tfmt.Printf(\"> commits: %+v\\n\", n.root().commits)\n-\t\tfmt.Printf(\"> files: %+v\\n\", n.root().files)\n \t\tif err := os.MkdirAll(p, 0777); err != nil {\n \t\t\treturn errors.WithStack(err)\n \t\t}", "new": " \t// we only mount explicitly named repos now\n \tfor name := range ros {\n \t\tp := n.namePath(name)\n \t\tif err := os.MkdirAll(p, 0777); err != nil {\n \t\t\treturn errors.WithStack(err)\n \t\t}", "lang": "go", "y": 1, "proj": "pachyderm-pachyderm", "patch": "@@ -519,25 +520,21 @@ func newLoopbackRoot(root, target string, c *client.APIClient, opts *Options) (*\n \treturn n, nil\n }\n \n-func (n *loopbackNode) downloadRepos() (retErr error) {\n-\tif n.getFileState(\"\") != none {\n-\t\treturn nil\n-\t}\n+func (n *loopbackNode) mkdirMountNames() (retErr error) {\n \tdefer func() {\n \t\tif retErr == nil {\n \t\t\tn.setFileState(\"\", meta)\n \t\t}\n \t}()\n-\tris, err := n.c().ListRepo()\n-\tif err != nil {\n-\t\treturn err\n-\t}\n-\tro := n.root().repoOpts\n-\tfor _, ri := range ris {\n-\t\tif len(ro) > 0 && ro[ri.Repo.Name] == nil {\n-\t\t\tcontinue\n-\t\t}\n-\t\tp := n.repoPath(ri)\n+\tros := n.root().repoOpts\n+\t// we only mount explicitly named repos now\n+\tfor name := range ros {\n+\t\tp := n.namePath(name)\n+\t\tfmt.Printf(\"> MKDIR %s, %s\\n\", name, p)", "msg": "I'm not sure if this is leftover println debugging or if we want to keep these in, but if it's the latter, it might make sense to use logrus here, and log these at DEBUG? WDYT?"}
{"old_hunk": "@@ -108,6 +113,37 @@ func mountCmds() []*cobra.Command {\n \tmount.MarkFlagCustom(\"repos\", \"__pachctl_get_repo_branch\")\n \tcommands = append(commands, cmdutil.CreateAlias(mount, \"mount\"))\n \n+\tvar daemonize bool\n+\tvar mountDir string\n+\tvar socket string\n+\tvar logFile string\n+\tmountServer := &cobra.Command{\n+\t\tUse:   \"{{alias}}\",\n+\t\tShort: \"Start a mount server for controlling FUSE mounts via a local gRPC API.\",\n+\t\tLong:  \"Starts a gRPC mount server on the given socket, logging to the given log-file, daemonizing and exiting once it is active if requested, otherwise running in the foreground and logging to stdout.\\n\\nSee TODO:<link to docs> for documentation on available methods on the API.\",", "hunk": "@@ -113,14 +113,11 @@ func mountCmds() []*cobra.Command {\n \tmount.MarkFlagCustom(\"repos\", \"__pachctl_get_repo_branch\")\n \tcommands = append(commands, cmdutil.CreateAlias(mount, \"mount\"))\n \n-\tvar daemonize bool\n \tvar mountDir string\n-\tvar socket string\n-\tvar logFile string\n \tmountServer := &cobra.Command{\n \t\tUse:   \"{{alias}}\",\n \t\tShort: \"Start a mount server for controlling FUSE mounts via a local gRPC API.\",\n-\t\tLong:  \"Starts a gRPC mount server on the given socket, logging to the given log-file, daemonizing and exiting once it is active if requested, otherwise running in the foreground and logging to stdout.\\n\\nSee TODO:<link to docs> for documentation on available methods on the API.\",\n+\t\tLong:  \"Starts a REST mount server, running in the foreground and logging to stdout.\",\n \t\tRun: cmdutil.RunFixedArgs(0, func(args []string) error {\n \t\t\t// TODO: call into pfs/fuse/server.go\n \t\t\tc, err := client.NewOnUserMachine(\"fuse\")\n", "comment": "We should fix this TODO before merging", "ids": [49277, "6928ff1eb8b1b71db86a67312b695ae270ff07bd", "766eddd03716b019a5af2ec332a53387ec303c19"], "repo": "pachyderm-pachyderm", "ghid": 7214, "old": " \tmount.MarkFlagCustom(\"repos\", \"__pachctl_get_repo_branch\")\n \tcommands = append(commands, cmdutil.CreateAlias(mount, \"mount\"))\n-\tvar daemonize bool\n \tvar mountDir string\n-\tvar socket string\n-\tvar logFile string\n \tmountServer := &cobra.Command{\n \t\tUse:   \"{{alias}}\",\n \t\tShort: \"Start a mount server for controlling FUSE mounts via a local gRPC API.\",\n-\t\tLong:  \"Starts a gRPC mount server on the given socket, logging to the given log-file, daemonizing and exiting once it is active if requested, otherwise running in the foreground and logging to stdout.\\n\\nSee TODO:<link to docs> for documentation on available methods on the API.\",\n \t\tRun: cmdutil.RunFixedArgs(0, func(args []string) error {\n \t\t\t// TODO: call into pfs/fuse/server.go\n \t\t\tc, err := client.NewOnUserMachine(\"fuse\")", "new": " \tmount.MarkFlagCustom(\"repos\", \"__pachctl_get_repo_branch\")\n \tcommands = append(commands, cmdutil.CreateAlias(mount, \"mount\"))\n \tvar mountDir string\n \tmountServer := &cobra.Command{\n \t\tUse:   \"{{alias}}\",\n \t\tShort: \"Start a mount server for controlling FUSE mounts via a local gRPC API.\",\n+\t\tLong:  \"Starts a REST mount server, running in the foreground and logging to stdout.\",\n \t\tRun: cmdutil.RunFixedArgs(0, func(args []string) error {\n \t\t\t// TODO: call into pfs/fuse/server.go\n \t\t\tc, err := client.NewOnUserMachine(\"fuse\")", "lang": "go", "y": 1, "proj": "pachyderm-pachyderm", "patch": "@@ -108,6 +113,37 @@ func mountCmds() []*cobra.Command {\n \tmount.MarkFlagCustom(\"repos\", \"__pachctl_get_repo_branch\")\n \tcommands = append(commands, cmdutil.CreateAlias(mount, \"mount\"))\n \n+\tvar daemonize bool\n+\tvar mountDir string\n+\tvar socket string\n+\tvar logFile string\n+\tmountServer := &cobra.Command{\n+\t\tUse:   \"{{alias}}\",\n+\t\tShort: \"Start a mount server for controlling FUSE mounts via a local gRPC API.\",\n+\t\tLong:  \"Starts a gRPC mount server on the given socket, logging to the given log-file, daemonizing and exiting once it is active if requested, otherwise running in the foreground and logging to stdout.\\n\\nSee TODO:<link to docs> for documentation on available methods on the API.\",", "msg": "We should fix this TODO before merging"}
{"old_hunk": "@@ -279,7 +279,7 @@ func TestRepoOpts(t *testing.T) {\n \t\t\t},\n \t\t},\n \t\tRepoOptions: map[string]*RepoOptions{\n-\t\t\t\"repo1\": {},\n+\t\t\t\"repo1\": {Repo: \"repo1\"},", "hunk": "@@ -279,7 +279,7 @@ func TestRepoOpts(t *testing.T) {\n \t\t\t},\n \t\t},\n \t\tRepoOptions: map[string]*RepoOptions{\n-\t\t\t\"repo1\": {Repo: \"repo1\"},\n+\t\t\t\"repo1\": {Name: \"repo1\", Repo: \"repo1\"},\n \t\t},\n \t}, func(mountPoint string) {\n \t\trepos, err := ioutil.ReadDir(mountPoint)\n", "comment": "I don't actually know the fuse library's API that well, but if RepoOptions is part of the client's user-facing API, we may be obligated by semver to infer a default value for `Repo` if it's unset (in case an existing user is calling our client with `Repo` unset; they should be able to upgrade without it breaking)", "ids": [49278, "6928ff1eb8b1b71db86a67312b695ae270ff07bd", "2c1e66b773f998378ea33737b8b670a80c2f5e3e"], "repo": "pachyderm-pachyderm", "ghid": 7214, "old": " \t\t\t},\n \t\t},\n \t\tRepoOptions: map[string]*RepoOptions{\n-\t\t\t\"repo1\": {Repo: \"repo1\"},\n \t\t},\n \t}, func(mountPoint string) {\n \t\trepos, err := ioutil.ReadDir(mountPoint)", "new": " \t\t\t},\n \t\t},\n \t\tRepoOptions: map[string]*RepoOptions{\n+\t\t\t\"repo1\": {Name: \"repo1\", Repo: \"repo1\"},\n \t\t},\n \t}, func(mountPoint string) {\n \t\trepos, err := ioutil.ReadDir(mountPoint)", "lang": "go", "y": 1, "proj": "pachyderm-pachyderm", "patch": "@@ -279,7 +279,7 @@ func TestRepoOpts(t *testing.T) {\n \t\t\t},\n \t\t},\n \t\tRepoOptions: map[string]*RepoOptions{\n-\t\t\t\"repo1\": {},\n+\t\t\t\"repo1\": {Repo: \"repo1\"},", "msg": "I don't actually know the fuse library's API that well, but if RepoOptions is part of the client's user-facing API, we may be obligated by semver to infer a default value for `Repo` if it's unset (in case an existing user is calling our client with `Repo` unset; they should be able to upgrade without it breaking)"}
{"old_hunk": "@@ -81,12 +82,13 @@ func (c APIClient) InspectRepo(repoName string) (_ *pfs.RepoInfo, retErr error)\n \tdefer func() {\n \t\tretErr = grpcutil.ScrubGRPC(retErr)\n \t}()\n-\treturn c.PfsAPIClient.InspectRepo(\n+\tres, err := c.PfsAPIClient.InspectRepo(", "hunk": "@@ -82,13 +82,12 @@ func (c APIClient) InspectRepo(repoName string) (_ *pfs.RepoInfo, retErr error)\n \tdefer func() {\n \t\tretErr = grpcutil.ScrubGRPC(retErr)\n \t}()\n-\tres, err := c.PfsAPIClient.InspectRepo(\n+\treturn c.PfsAPIClient.InspectRepo(\n \t\tc.Ctx(),\n \t\t&pfs.InspectRepoRequest{\n \t\t\tRepo: NewRepo(repoName),\n \t\t},\n \t)\n-\treturn res, err\n }\n \n // ListRepo returns info about user Repos\n", "comment": "was this to satisfy some other linting rule?", "ids": [49353, "f9820560c261fce36ff801d867dc35b448ed3795", "8e37bb0b18c04a13d47921c2536a40ba872bf292"], "repo": "pachyderm-pachyderm", "ghid": 7238, "old": " \tdefer func() {\n \t\tretErr = grpcutil.ScrubGRPC(retErr)\n \t}()\n-\tres, err := c.PfsAPIClient.InspectRepo(\n \t\tc.Ctx(),\n \t\t&pfs.InspectRepoRequest{\n \t\t\tRepo: NewRepo(repoName),\n \t\t},\n \t)\n-\treturn res, err\n }\n // ListRepo returns info about user Repos", "new": " \tdefer func() {\n \t\tretErr = grpcutil.ScrubGRPC(retErr)\n \t}()\n+\treturn c.PfsAPIClient.InspectRepo(\n \t\tc.Ctx(),\n \t\t&pfs.InspectRepoRequest{\n \t\t\tRepo: NewRepo(repoName),\n \t\t},\n \t)\n }\n // ListRepo returns info about user Repos", "lang": "go", "y": 1, "proj": "pachyderm-pachyderm", "patch": "@@ -81,12 +82,13 @@ func (c APIClient) InspectRepo(repoName string) (_ *pfs.RepoInfo, retErr error)\n \tdefer func() {\n \t\tretErr = grpcutil.ScrubGRPC(retErr)\n \t}()\n-\treturn c.PfsAPIClient.InspectRepo(\n+\tres, err := c.PfsAPIClient.InspectRepo(", "msg": "was this to satisfy some other linting rule?"}
{"old_hunk": "@@ -434,47 +516,84 @@ func (c *postgresReadOnlyCollection) Count() (int64, error) {\n \treturn result, c.mapSQLError(err, \"\")\n }\n \n-func (c *postgresReadOnlyCollection) Watch(opts ...watch.Option) (watch.Watcher, error) {\n-\toptions := watch.SumOptions(opts...)\n+// This blocking function sends watch events to the client. It first sends a list of the existing elements\n+// in the collection, followed by new events.\n+func (c *postgresReadOnlyCollection) watchRoutine(watcher *postgresWatcher, options watch.WatchOptions, withFields map[string]string) {\n+\t// Do a list of the collection to get the initial state\n+\tlastTs := time.Time{}\n+\tval := cloneProtoMsg(c.template)\n \n-\twatcher, err := newPostgresWatcher(c.db, c.listener, c.tableWatchChannel(), c.template, nil, nil, options)\n-\tif err != nil {\n-\t\treturn nil, err\n+\tlastTimestamp := func(m *model, target etcd.SortTarget) time.Time {\n+\t\tif target == SortByModRevision {\n+\t\t\treturn m.UpdatedAt\n+\t\t}\n+\t\treturn m.CreatedAt\n \t}\n \n-\tgo func() {\n-\t\t// Do a list of the collection to get the initial state\n-\t\tlastUpdated := time.Time{}\n-\t\tval := cloneProtoMsg(c.template)\n-\t\tif err := c.list(nil, &Options{Target: options.SortTarget, Order: options.SortOrder}, func(m *model) error {\n-\t\t\tif err := proto.Unmarshal(m.Proto, val); err != nil {\n-\t\t\t\treturn errors.EnsureStack(err)\n-\t\t\t}\n+\tvar bufEvent *postgresEvent\n+\t// Since list is not a snapshot of the DB, we break out early and hand-off\n+\t// event emition to the watcher if we encounter a listed record that is\n+\t// in the future of a buffered event\n+\tif err := c.list(withFields, &Options{Target: options.SortTarget, Order: etcd.SortAscend}, func(m *model) error {\n+\t\tif err := proto.Unmarshal(m.Proto, val); err != nil {\n+\t\t\treturn errors.EnsureStack(err)\n+\t\t}\n+\t\tlastTs := lastTimestamp(m, options.SortTarget)\n \n-\t\t\tif lastUpdated.Before(m.UpdatedAt) {\n-\t\t\t\tlastUpdated = m.UpdatedAt\n+\t\tif bufEvent == nil {\n+\t\t\tselect {\n+\t\t\tcase eventData := <-watcher.buf:\n+\t\t\t\tbufEvent = eventData\n+\t\t\tdefault:\n \t\t\t}\n+\t\t}\n \n-\t\t\treturn watcher.sendInitial(&watch.Event{\n-\t\t\t\tKey:      []byte(m.Key),\n-\t\t\t\tValue:    m.Proto,\n-\t\t\t\tType:     watch.EventPut,\n-\t\t\t\tTemplate: c.template,\n-\t\t\t})\n-\t\t}); err != nil {\n-\t\t\t// Ignore any additional error here - we're already attempting to send an error to the user\n-\t\t\twatcher.sendInitial(&watch.Event{Type: watch.EventError, Err: err})\n-\t\t\twatcher.listener.Unregister(watcher)\n-\t\t\treturn\n+\t\tif bufEvent != nil && bufEvent.time.Unix() <= lastTs.Unix() {\n+\t\t\tif err := watcher.sendInitial(bufEvent.WatchEvent(c.ctx, watcher.db, watcher.template)); err != nil {\n+\t\t\t\treturn err\n+\t\t\t}\n+\t\t\treturn errutil.ErrBreak\n \t\t}\n \n-\t\t// Forward all buffered notifications until the watcher is closed\n-\t\twatcher.forwardNotifications(c.ctx, lastUpdated)\n-\t}()\n+\t\treturn watcher.sendInitial(&watch.Event{\n+\t\t\tKey:      []byte(m.Key),\n+\t\t\tValue:    m.Proto,\n+\t\t\tType:     watch.EventPut,\n+\t\t\tTemplate: c.template,\n+\t\t\tRev:      m.UpdatedAt.Unix(),\n+\t\t})\n+\t}); err != nil && err != errutil.ErrBreak {", "hunk": "@@ -562,7 +562,7 @@ func (c *postgresReadOnlyCollection) watchRoutine(watcher *postgresWatcher, opti\n \t\t\tTemplate: c.template,\n \t\t\tRev:      m.UpdatedAt.Unix(),\n \t\t})\n-\t}); err != nil && err != errutil.ErrBreak {\n+\t}); err != nil && !errors.Is(err, errutil.ErrBreak) {\n \t\t// Ignore any additional error here - we're already attempting to send an error to the user\n \t\twatcher.sendInitial(&watch.Event{Type: watch.EventError, Err: err})\n \t\twatcher.listener.Unregister(watcher)\n", "comment": "i think we want the `ErrBreak` check to be via `errors.Is` in general, although I'm noticing that marc's PR didn't touch this line in master. Not sure if that's actually part of the linter", "ids": [49364, "2626274251452e3a20841dc506b7390add556e74", "467ef0e7e085c3a91492453fda1e2a67279d8e09"], "repo": "pachyderm-pachyderm", "ghid": 7243, "old": " \t\t\tTemplate: c.template,\n \t\t\tRev:      m.UpdatedAt.Unix(),\n \t\t})\n-\t}); err != nil && err != errutil.ErrBreak {\n \t\t// Ignore any additional error here - we're already attempting to send an error to the user\n \t\twatcher.sendInitial(&watch.Event{Type: watch.EventError, Err: err})\n \t\twatcher.listener.Unregister(watcher)", "new": " \t\t\tTemplate: c.template,\n \t\t\tRev:      m.UpdatedAt.Unix(),\n \t\t})\n+\t}); err != nil && !errors.Is(err, errutil.ErrBreak) {\n \t\t// Ignore any additional error here - we're already attempting to send an error to the user\n \t\twatcher.sendInitial(&watch.Event{Type: watch.EventError, Err: err})\n \t\twatcher.listener.Unregister(watcher)", "lang": "go", "y": 1, "proj": "pachyderm-pachyderm", "patch": "@@ -434,47 +516,84 @@ func (c *postgresReadOnlyCollection) Count() (int64, error) {\n \treturn result, c.mapSQLError(err, \"\")\n }\n \n-func (c *postgresReadOnlyCollection) Watch(opts ...watch.Option) (watch.Watcher, error) {\n-\toptions := watch.SumOptions(opts...)\n+// This blocking function sends watch events to the client. It first sends a list of the existing elements\n+// in the collection, followed by new events.\n+func (c *postgresReadOnlyCollection) watchRoutine(watcher *postgresWatcher, options watch.WatchOptions, withFields map[string]string) {\n+\t// Do a list of the collection to get the initial state\n+\tlastTs := time.Time{}\n+\tval := cloneProtoMsg(c.template)\n \n-\twatcher, err := newPostgresWatcher(c.db, c.listener, c.tableWatchChannel(), c.template, nil, nil, options)\n-\tif err != nil {\n-\t\treturn nil, err\n+\tlastTimestamp := func(m *model, target etcd.SortTarget) time.Time {\n+\t\tif target == SortByModRevision {\n+\t\t\treturn m.UpdatedAt\n+\t\t}\n+\t\treturn m.CreatedAt\n \t}\n \n-\tgo func() {\n-\t\t// Do a list of the collection to get the initial state\n-\t\tlastUpdated := time.Time{}\n-\t\tval := cloneProtoMsg(c.template)\n-\t\tif err := c.list(nil, &Options{Target: options.SortTarget, Order: options.SortOrder}, func(m *model) error {\n-\t\t\tif err := proto.Unmarshal(m.Proto, val); err != nil {\n-\t\t\t\treturn errors.EnsureStack(err)\n-\t\t\t}\n+\tvar bufEvent *postgresEvent\n+\t// Since list is not a snapshot of the DB, we break out early and hand-off\n+\t// event emition to the watcher if we encounter a listed record that is\n+\t// in the future of a buffered event\n+\tif err := c.list(withFields, &Options{Target: options.SortTarget, Order: etcd.SortAscend}, func(m *model) error {\n+\t\tif err := proto.Unmarshal(m.Proto, val); err != nil {\n+\t\t\treturn errors.EnsureStack(err)\n+\t\t}\n+\t\tlastTs := lastTimestamp(m, options.SortTarget)\n \n-\t\t\tif lastUpdated.Before(m.UpdatedAt) {\n-\t\t\t\tlastUpdated = m.UpdatedAt\n+\t\tif bufEvent == nil {\n+\t\t\tselect {\n+\t\t\tcase eventData := <-watcher.buf:\n+\t\t\t\tbufEvent = eventData\n+\t\t\tdefault:\n \t\t\t}\n+\t\t}\n \n-\t\t\treturn watcher.sendInitial(&watch.Event{\n-\t\t\t\tKey:      []byte(m.Key),\n-\t\t\t\tValue:    m.Proto,\n-\t\t\t\tType:     watch.EventPut,\n-\t\t\t\tTemplate: c.template,\n-\t\t\t})\n-\t\t}); err != nil {\n-\t\t\t// Ignore any additional error here - we're already attempting to send an error to the user\n-\t\t\twatcher.sendInitial(&watch.Event{Type: watch.EventError, Err: err})\n-\t\t\twatcher.listener.Unregister(watcher)\n-\t\t\treturn\n+\t\tif bufEvent != nil && bufEvent.time.Unix() <= lastTs.Unix() {\n+\t\t\tif err := watcher.sendInitial(bufEvent.WatchEvent(c.ctx, watcher.db, watcher.template)); err != nil {\n+\t\t\t\treturn err\n+\t\t\t}\n+\t\t\treturn errutil.ErrBreak\n \t\t}\n \n-\t\t// Forward all buffered notifications until the watcher is closed\n-\t\twatcher.forwardNotifications(c.ctx, lastUpdated)\n-\t}()\n+\t\treturn watcher.sendInitial(&watch.Event{\n+\t\t\tKey:      []byte(m.Key),\n+\t\t\tValue:    m.Proto,\n+\t\t\tType:     watch.EventPut,\n+\t\t\tTemplate: c.template,\n+\t\t\tRev:      m.UpdatedAt.Unix(),\n+\t\t})\n+\t}); err != nil && err != errutil.ErrBreak {", "msg": "i think we want the `ErrBreak` check to be via `errors.Is` in general, although I'm noticing that marc's PR didn't touch this line in master. Not sure if that's actually part of the linter"}
